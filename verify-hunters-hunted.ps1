# Build gate for the WoD20th sheet. Checks SPEC.md V1, V3, V4, V5, V8-V40.
# With -Build it also runs the real compile and checks V6 + V7.
#
# Lives at repo root, NOT inside the plugin dir: rdk packs every file under the
# plugin folder into the .rpk, so a .ps1 there would ship to users.
#
# ASCII only, on purpose: PowerShell 5.1 reads a BOM-less .ps1 as ANSI, so a stray
# accented character here would rot the file depending on how it was saved.
#
#   .\verify-hunters-hunted.ps1            # static checks only
#   .\verify-hunters-hunted.ps1 -Build     # + rdk -l, asserts the .rpk actually changed
#   .\verify-hunters-hunted.ps1 -Quiet     # only the FAIL lines and the closing count
#
# -Quiet is an OUTPUT filter and never a check filter: every check still runs and the
# exit code still comes from the failure count, so the two modes agree on the verdict
# and differ only in what they print. A switch that let the gate SKIP a check would be
# SPEC B7 again - a check quietly turned into a no-op (SPEC I133e, V385).

param([switch]$Build, [switch]$Quiet)

$ErrorActionPreference = 'Stop'
$plugin = Join-Path $PSScriptRoot "Plugins\Sheets\World of Darkness 20th Anniversary Edition"
$dir    = Join-Path $plugin "WoD20th"
$rpk    = Join-Path $plugin "output\World of Darkness 20th Anniversary Edition.rpk"
$langFile = Join-Path $plugin "localization.lang"
$fail   = 0

function Fail($msg) { Write-Host "FAIL  $msg"; $script:fail++ }
function Pass($msg) { if (-not $Quiet) { Write-Host "ok    $msg" } }
function Doc($path) { $x = New-Object System.Xml.XmlDocument; $x.LoadXml([System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($path))); $x }
# The container that holds a form's section boxes. It was spelled <scrollBox> in every
# collector until T895 took the scrollBox out of the three Ghoul columns: they are imported
# INTO WoD20.11's own scrollBox, and a scrollBox inside a scrollBox is the frame the user
# asked to remove (SPEC V391, I134c). NOT ONE BOX MOVED - only the tag around them changed -
# so a collector that asks for a TAG NAME reports "declares no top-level layout" about a file
# whose layouts are all still there. What it has to ask for is the ROOT (SPEC V20: read the
# real artifact, not one spelling of it).
# What a COLUMN of the Ghoul or Numina tab calls itself (SPEC V361a, V221, V225, V280d, B70).
# A column that carries its own title IS the section box; a column that carries boxes answers to
# the first of them. One reader for both tabs, declared here with the other document helpers
# because every caller of it runs before V361, where it used to live (T909).
# The internal margin of a section box, and it is TWO numbers since T913 (SPEC V280a, I137c,
# user 2026-09-02). X stayed at 20 because the owner said so in writing; Y went to 15.
#
# Declared HERE, once, because V280 and V240 both enforce it and both used to carry their own
# 20. V280's own text has said "REVOGA os numeros de V240" since the 108th round and the gate
# never acted on it - two owners of one number, and the margin change is what made them disagree
# out loud (SPEC B70, B130).
#
# HEAD is an EQUALITY and FOOT is a FLOOR: the head has no freedom (67 of 67 boxes agree), while
# the foot is the free number V280(d) derives from where a column closes.
$BOX_PAD_X = 20
$BOX_PAD_Y = 15

function ColId361($node) {
    $l = $node.SelectSingleNode("label")
    if ($null -ne $l) { return $l.GetAttribute("text") }
    $l = $node.SelectSingleNode("layout/label")
    if ($null -ne $l) { return $l.GetAttribute("text") }
    return ''
}

function ContentRoot($doc) {
    $r = $doc.SelectSingleNode("//scrollBox")
    if ($null -ne $r) { return $r }
    # The fallback is reached ONLY by a form with no scrollBox at all, and there are exactly
    # two shapes of those: WoD20th.lfm, whose one align=client child is sheetBody and HOSTS the
    # eleven tab panes, and a column form like WoD20.12, whose one align=client child holds the
    # section boxes themselves. A container that carries an <import> is a HOST, not a content
    # root - so that is what separates them, and it is derived rather than a file name. Without
    # it the root enrols as a twelfth content form and V286 counts 15 where the sheet has 14.
    foreach ($c in $doc.SelectNodes("/form/layout[@align='client']")) {
        if ($null -eq $c.SelectSingleNode(".//import")) { return $c }
    }
    return $null
}

# The Lua of a file with the comments stripped - XML comments and `--` lines both. Declared
# up here with Doc and ListOf, not halfway down beside its first caller: PowerShell resolves a
# function at CALL time, so a check written above the definition dies with CommandNotFound -
# the same shape as SPEC B51/V223 in Lua, one language over.
function CodeOf($path) {
    $t = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($path))
    $t = [regex]::Replace($t, '(?s)<!--.*?-->', '')
    $t = [regex]::Replace($t, '(?m)^\s*--.*$', '')
    $t
}

# ---- SPEC V210: the book text is one module PER LANGUAGE ------------------------------
# T495 split desc<X>.lua into desc<X>_en.lua and desc<X>_pt.lua: half of every module was the
# language the session never opens, so requiring one meant parsing two (SPEC R92f). The
# generator markers migrated verbatim to BOTH halves, so the region kept the shape every
# parser here was written against - what changed is that `en` and `pt` are no longer paired
# INSIDE one file. The pairing is a join across two files now, and this is where it is made:
# five checks used to carry their own paired regex, and five copies of one parser is how a
# split like this leaves half of them reading nothing and passing (SPEC B7, V209).
#
# Returns the entries of ONE half in file order - Key, Body, and Rest, which is whatever the
# entry holds after the body and is where the optional medieval daEn/daPt lives (SPEC T470).
# $null when the file is not there or its markers are gone, so each caller says what the
# absence costs in its own words rather than passing by omission (SPEC V209).
function DescEntries($path, $marker, $lang) {
    if (-not (Test-Path -LiteralPath $path)) { return $null }
    $txt = ([System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($path))) -replace "`r`n", "`n"
    $region = [regex]::Match($txt, '-- >>> ' + $marker + '_BEGIN[^\n]*\n(.*?)-- <<< ' + $marker + '_END', 'Singleline')
    if (-not $region.Success) { return $null }
    $rx = [regex]('(?ms)^\s*\["([^"]+)"\]\s*=\s*\{\s*' + $lang + '\s*=\s*\[==\[(.*?)\]==\]\s*,?\s*(.*?)\},')
    $out = New-Object System.Collections.ArrayList
    foreach ($m in $rx.Matches($region.Groups[1].Value)) {
        [void]$out.Add([pscustomobject]@{ Key = $m.Groups[1].Value; Body = $m.Groups[2].Value; Rest = $m.Groups[3].Value })
    }
    , $out.ToArray()
}

# Key -> body for one half, ORDINAL. A case-insensitive map folds nothing on today's keys, but
# `Shadow` and `Shadows` are two numina one letter apart (SPEC V273) and the next pair added
# may differ only in case - which would silently become one entry on the way in.
function DescMap($entries) {
    $d = New-Object 'System.Collections.Generic.Dictionary[string,string]' ([StringComparer]::Ordinal)
    foreach ($e in $entries) { if (-not $d.ContainsKey($e.Key)) { $d[$e.Key] = $e.Body } }
    , $d
}

# The marker that opens a description module's region, and it lives HERE, with the other document
# helpers, because THREE rules read it now: V408 (is every kind's module covered), V411 (does the
# [pt] half carry accents) and, since T934, V210's own area roster. `Numina` answering to plain
# `DESC` is the one break in the naming convention, which is exactly why the map is a map and not
# a rule someone re-derives per caller - two owners for one fact is what B70 and V67 pay not to
# have. Declared before its first reader on purpose: PowerShell reads top to bottom, and a
# hashtable named below its use is $null with no error to show for it.
$DESC_MARKER = @{ 'Background' = 'BACKGROUND_DESC'; 'Disc' = 'DISC_DESC'; 'Merit' = 'MERIT_DESC'; 'Road' = 'ROAD_DESC';
                  'Nature' = 'NATURE_DESC'; 'Numina' = 'DESC'; 'Path' = 'PATH_DESC'; 'Ritual' = 'RITUAL_DESC';
                  'Clan' = 'CLAN_DESC'; 'Family' = 'FAMILY_DESC' }


# ---- the BAND of a tab, ONE owner (SPEC V406, I140q, Q53) ------------------------------
# The band is the row of boxes with the SMALLEST top among the direct <layout> children of the
# content root, and everything below it is a column. FOUR rules ask that question - V361, V267,
# V390 and V401 - and until T926 each wrote `top == 0` in its own line.
#
# The owner moved the band of two tabs to 10 on 2026-09-02 (SPEC Q53) and all four read those
# tabs INSIDE OUT in the same run: an empty band, the band boxes counted as columns, and
# messages like `the column at x=0 holds no box`. Forty-four failures out of one sentence
# written four times - which is B112 exactly, four owners for one number (SPEC B70).
#
# Returns -1 when there is nothing to measure, so each caller says what the absence costs in its
# own words rather than passing by omission (SPEC V209).
function BandTop($sb) {
    if ($null -eq $sb) { return -1 }
    $bt406 = [int]::MaxValue
    foreach ($c406 in $sb.SelectNodes("layout")) {
        $t406 = 0
        if (-not [int]::TryParse($c406.GetAttribute("top"), [ref]$t406)) { continue }
        if ($t406 -lt $bt406) { $bt406 = $t406 }
    }
    if ($bt406 -eq [int]::MaxValue) { return -1 }
    return $bt406
}
$files = Get-ChildItem -LiteralPath $dir -Filter *.lfm | Sort-Object Name
# ---- PICKER_LIST: where a picker list LIVES now (SPEC I27, 68th round) ---------------
# T493 moved every picker list out of items=/values= and into one map on the root form.
# SPEC V20 says a check reads the REAL artifact, and after T493 the real artifact is this
# map - so every list check below sources from here. Leaving them on the XML attribute is
# B7 in the other direction: the collectors come up empty and the checks pass having
# verified nothing (SPEC V209).
#
# Found by MARKER, never by line number (SPEC V211b): the T490 build shifted every gate
# line past 1935 by 33, which is why a coordinate is not a contract here.
$rootLfmTxt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20th.lfm")))
$plRegion   = [regex]::Match($rootLfmTxt, '(?s)PICKER_LIST_BEGIN(.*?)PICKER_LIST_END')
$PICKER     = @{}
if ($plRegion.Success) {
    foreach ($m in [regex]::Matches($plRegion.Groups[1].Value, '(?s)\["(\w+)"\]\s*=\s*\{(.*?)\n\s*\},')) {
        $vals = @()
        foreach ($q in [regex]::Matches($m.Groups[2].Value, '"((?:[^"\\]|\\.)*)"')) {
            $vals += ($q.Groups[1].Value -replace '\\"', '"' -replace '\\\\', '\')
        }
        $PICKER[$m.Groups[1].Value] = $vals
    }
}
# The three aliases are IDENTITY in Lua, not copies (SPEC V211c) - mirror them here so
# every field root resolves to the one authored list standing behind it.
$PICKER_ALIAS = @{}
foreach ($a in [regex]::Matches($rootLfmTxt, 'PICKER_LIST\["(\w+)"\]\s*=\s*PICKER_LIST\["(\w+)"\];')) {
    $PICKER_ALIAS[$a.Groups[1].Value] = $a.Groups[2].Value
    if ($PICKER.ContainsKey($a.Groups[2].Value)) { $PICKER[$a.Groups[1].Value] = $PICKER[$a.Groups[2].Value] }
}

# clanFamily stopped being an alias in the 107th round. Clan and family are two authored
# lists and the ghoul picker offers their UNION, built once in the constructor (SPEC I29b,
# T510, V433b - back since T972, after the T954 interlude of one button per list). Mirror it
# here exactly the way the aliases are mirrored above, so every check downstream measures the
# list the picker really shows and not the clan list alone.
$UNION_REGION = [regex]::Match($rootLfmTxt, '(?s)>>> CLANFAMILY_UNION_BEGIN(.*?)<<< CLANFAMILY_UNION_END')
if ($UNION_REGION.Success -and $PICKER.ContainsKey('clan') -and $PICKER.ContainsKey('family')) {
    $PICKER['clanFamily'] = @(@($PICKER['clan']) + @($PICKER['family']))
}

# Which key a comboBox reads. Mirrors fieldRoot() in WoD20.6: drop "cbo", drop the row number,
# lowercase the first letter. Template combos are named cbo$(field) in the SOURCE, so their
# key comes from what their INSTANCES bind - the gate sees the XML unexpanded.
$TPL_KEY = @{}
$allTxt  = @{}
foreach ($f in $files) { $allTxt[$f.Name] = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName)) }
foreach ($tn in ($allTxt.Values | ForEach-Object { [regex]::Matches($_, '<template\s+name="([^"]+)"') } | ForEach-Object { $_.Groups[1].Value } | Sort-Object -Unique)) {
    foreach ($u in $allTxt.Values) {
        foreach ($i in [regex]::Matches($u, "<$tn\b[^>]*field=`"([^`"]+)`"")) {
            $TPL_KEY[$tn] = ($i.Groups[1].Value -replace '_\d+$', '')
        }
    }
}

function PickerKeyOf($comboName, $fieldAttr, $tplName) {
    if ($fieldAttr) {
        $k = $fieldAttr -replace '_?\$\([^)]*\)$', '' -replace '_\d+$', ''
        if ($k -match '^[A-Za-z]+$') { return $k.Substring(0, 1).ToLower() + $k.Substring(1) }
    }
    if ($tplName -and $TPL_KEY.ContainsKey($tplName)) { return $TPL_KEY[$tplName] }
    if ($comboName -and $comboName.StartsWith("cbo")) {
        $k = $comboName.Substring(3) -replace '_?\d+$', ''
        if ($k -match '^[A-Za-z]+$') { return $k.Substring(0, 1).ToLower() + $k.Substring(1) }
    }
    return $null
}

# The list a comboBox NODE offers, wherever it lives: PICKER_LIST for the migrated ones,
# the inline attribute for WoD20.3/WoD20.6 which T493 left alone. One accessor so no check has to
# know which world its combo is in (SPEC V20).
function ListOf($cb, $tplName) {
    $k = PickerKeyOf $cb.GetAttribute("name") $cb.GetAttribute("field") $tplName
    if ($k -and $PICKER.ContainsKey($k)) { return $PICKER[$k] }
    $a = $cb.GetAttribute("items"); if (-not $a) { $a = $cb.GetAttribute("values") }
    if ($a) { return @([regex]::Matches($a, "'([^']*)'") | ForEach-Object { $_.Groups[1].Value }) }
    return @()
}

# The template a node sits inside, or $null. Templates do not nest in this sheet.
# LocalName, never .Name: PowerShell's XML adapter shadows the .NET Name property with the
# element's name= ATTRIBUTE, so $p.Name on <template name="RitualRow"> returns "RitualRow"
# and a test against 'template' silently never matches. That is a check verifying nothing.
function TplOf($node) {
    $p = $node.ParentNode
    while ($p -ne $null -and $p.NodeType -eq 'Element') {
        if ($p.LocalName -eq 'template') { return $p.GetAttribute("name") }
        $p = $p.ParentNode
    }
    return $null
}

$PICKER_SCOPE = @('WoD20th.lfm', 'WoD20.1.lfm', 'WoD20.2.lfm', 'WoD20.7.lfm', 'WoD20.11.lfm')


# ---- SPEC I24: the book text lives in one .lua module per area, at the plugin root ------
# Not inside the plugin's sheet folder: `require` is resolved by the host and the only PROVEN
# layout is a module beside module.xml (Plugins/Sheets/DnD5e/common.lua, required as
# require("common.lua") from a <script> CDATA). Discovered by pattern, not by a literal list,
# so T479-T481 add a module without touching this line (same idea as V59's file count).
$descModules = @(Get-ChildItem -LiteralPath $plugin -Filter "desc*.lua" -ErrorAction SilentlyContinue | Sort-Object Name)

# ---- XML well-formedness -----------------------------------------------------
foreach ($f in $files) {
    try { [xml](Get-Content -Raw -LiteralPath $f.FullName) | Out-Null; Pass "XML $($f.Name)" }
    catch { Fail "XML $($f.Name): $($_.Exception.Message)" }
}

# ---- template names are module-global: two files cannot define the same one ------
# rdk fails this with NO message at all - it prints "Compilando..." and exits 1, never reaching
# the lint pass. Cost an hour once (a virtues mirror on the Combat tab copied <template
# name="Attribute"> from the Main tab). No working sheet in the repo reuses a template name
# across files. Cheap to check, invisible to debug.
$tplOwners = @{}
foreach ($f in $files) {
    $raw = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName))
    foreach ($m in [regex]::Matches($raw, '<template\s+name="([^"]+)"')) {
        $nm = $m.Groups[1].Value
        if (-not $tplOwners.ContainsKey($nm)) { $tplOwners[$nm] = @() }
        $tplOwners[$nm] += $f.Name
    }
}
$tplDupes = @($tplOwners.GetEnumerator() | Where-Object { $_.Value.Count -gt 1 })
if ($tplDupes) { foreach ($d in $tplDupes) { Fail "template '$($d.Key)' is defined in $($d.Value -join ' and ') - rdk exits 1 with no message" } }
else { Pass "template names unique across files ($($tplOwners.Count) templates)" }

# ---- V59: a control name is global across the sheet's files ---------------------
# B19: the theme backdrop shipped with one shared name="themePaper" on eight tabs. rdk
# printed "Compilando..." exited 1 with no message and DELETED the .rpk - the same silent
# death as a duplicated <template name>, which is checked just above, in another family.
# Template and event names are excluded (templates have their own check; <event name="onClick">
# is an event id, not a control), and so is any name holding $( ): those expand per call.
$ctrlNames = @{}
foreach ($f in $files) {
    foreach ($n in (Doc $f.FullName).SelectNodes("//*[@name]")) {
        if ($n.LocalName -in @('template','event','form')) { continue }
        $nm = $n.GetAttribute("name")
        if ($nm -like '*$(*') { continue }
        if (-not $ctrlNames.ContainsKey($nm)) { $ctrlNames[$nm] = New-Object System.Collections.Generic.HashSet[string] }
        [void]$ctrlNames[$nm].Add($f.Name)
    }
}
$nameDupes = @($ctrlNames.GetEnumerator() | Where-Object { $_.Value.Count -gt 1 })
if ($nameDupes.Count -gt 0) {
    foreach ($d in $nameDupes) { Fail "V59 control name '$($d.Key)' is declared in $(($d.Value | Sort-Object) -join ', ') - rdk dies with no message (SPEC B19)" }
} else { Pass "V59 all $($ctrlNames.Count) control names are unique across the sheet's files" }

# ---- localization.lang, parsed once -------------------------------------------
# Ordinal dictionaries on purpose: PowerShell's default hashtable is case-insensitive,
# which silently collapsed Armor/ARMOR and Experience/EXPERIENCE (20 keys masked).
$ptK = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
$enK = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
$ptVal = New-Object 'System.Collections.Generic.Dictionary[string,string]' ([StringComparer]::Ordinal)
$cur = ''
foreach ($l in [string[]][System.IO.File]::ReadAllLines($langFile, [System.Text.Encoding]::UTF8)) {
    if ($l -match '^\[(\w+)\]') { $cur = $Matches[1]; continue }
    if ($l -cmatch '^wod\.(.+?)=(.*)$') {
        if ($cur -eq 'pt') { [void]$ptK.Add($Matches[1]); if (-not $ptVal.ContainsKey($Matches[1])) { $ptVal[$Matches[1]] = $Matches[2] } }
        elseif ($cur -eq 'en') { [void]$enK.Add($Matches[1]) }
    }
}

# ---- collect fields, expanding templates ------------------------------------
$allFields  = @{}   # expanded field name -> list of "file:tag"  (input widgets only)
$linkFields = @()   # <dataLink field="x"> - observes an existing field, never owns it (V1, B.3)
$allImages  = @()
$radios     = @()   # radioButton - shares its field with its group BY DESIGN (V1 exception, V30)

foreach ($f in $files) {
    $xml = Doc $f.FullName

    $tpl = @{}
    foreach ($t in $xml.SelectNodes("//template")) {
        $pats = @()
        foreach ($n in $t.SelectNodes(".//*[@field]")) {
            $pats += [pscustomobject]@{
                Field = $n.GetAttribute("field")
                Tag   = $n.LocalName
                Value = $n.GetAttribute("fieldValue")
                Group = $n.GetAttribute("groupName")
            }
        }
        $tpl[$t.GetAttribute("name")] = $pats
    }

    foreach ($n in $xml.SelectNodes("//*")) {
        $inTemplate = $false
        $p = $n.ParentNode
        while ($p -ne $null -and $p.NodeType -eq 'Element') {
            if ($p.LocalName -eq 'template') { $inTemplate = $true; break }
            $p = $p.ParentNode
        }

        foreach ($a in @('src','checkedImage','uncheckedImage')) {
            if ($n.HasAttribute($a)) { $allImages += [pscustomobject]@{ File=$f.Name; Path=$n.GetAttribute($a) } }
        }

        if ($inTemplate) { continue }

        if ($tpl.ContainsKey($n.LocalName)) {
            foreach ($pat in $tpl[$n.LocalName]) {
                $expanded = $pat.Field
                foreach ($a in $n.Attributes) { $expanded = $expanded.Replace("`$($($a.Name))", $a.Value) }
                if ($expanded -match '\$\(') { Fail "unresolved placeholder '$expanded' in $($f.Name) <$($n.LocalName)>"; continue }
                # A radio group shares one field on purpose - the option is in fieldValue, so N
                # radios on one field is exclusion, not two owners (SPEC V1 exception / V30).
                if ($pat.Tag -eq 'radioButton') {
                    $val = $pat.Value
                    foreach ($a in $n.Attributes) { $val = $val.Replace("`$($($a.Name))", $a.Value) }
                    if ($val -match '\$\(') { Fail "unresolved fieldValue '$val' in $($f.Name) <$($n.LocalName)>"; continue }
                    $radios += [pscustomobject]@{ File=$f.Name; Field=$expanded; Value=$val; Group=$pat.Group; Via="<$($n.LocalName)>" }
                    continue
                }
                if (-not $allFields.ContainsKey($expanded)) { $allFields[$expanded] = @() }
                $allFields[$expanded] += "$($f.Name):<$($n.LocalName)>"
            }
        }
        elseif ($n.HasAttribute("field")) {
            $v = $n.GetAttribute("field")
            if ($v -match '\$\(') { continue }
            if ($n.LocalName -eq 'dataLink') { $linkFields += [pscustomobject]@{ File=$f.Name; Field=$v }; continue }
            if ($n.LocalName -eq 'radioButton') {
                $radios += [pscustomobject]@{ File=$f.Name; Field=$v; Value=$n.GetAttribute("fieldValue"); Group=$n.GetAttribute("groupName"); Via='<radioButton>' }
                continue
            }
            if (-not $allFields.ContainsKey($v)) { $allFields[$v] = @() }
            $allFields[$v] += "$($f.Name):<$($n.LocalName)>"
        }
    }
}

# ---- V1: no duplicate field among input widgets ------------------------------
# Radios are excluded above and re-registered here as ONE owner per group field, so the
# group still counts as a real owner for V8 without tripping the duplicate check.
foreach ($rf in ($radios | ForEach-Object { $_.Field } | Sort-Object -Unique)) {
    if ($allFields.ContainsKey($rf)) { Fail "V1 field '$rf' is owned by both a radio group and $($allFields[$rf] -join ', ')" }
    else { $allFields[$rf] = @("radio group ($(@($radios | Where-Object { $_.Field -eq $rf }).Count) options)") }
}

# ---- V36: declared mirrors are the OTHER exception to one-owner-per-field --------
# A mirror is literally two widgets on one `field`: the NDB is the single source of truth, so
# both stay in step with no Lua. That is the whole point of the Combat and Progress copies.
# The list is the declaration from SPEC I3 - anything multi-owned and NOT here is the accident
# V1 exists to catch, and anything here that is NOT multi-owned is a declaration gone stale.
$mirrors = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
# healthLevels was a mirror until the 82nd round: the two HEALTH boxes each carried the
# combo. It moved to the storyteller's box, where ONE widget owns it, so listing it here
# would be the stale declaration V36's second half exists to catch (SPEC I35, V234).
1..10 | ForEach-Object { [void]$mirrors.Add("health_$_") }
1..10 | ForEach-Object { [void]$mirrors.Add("willpower_c$_") }
# The 94th round adds two of a different shape: a PICKER and a typed twin on one field, one
# visible at a time behind a storyteller flag (SPEC I71, V274b). The NDB is still the single
# source of truth and still keeps them in step - what the flag changes is which one the reader
# can reach, not what either of them writes.
# hedgeAffiliation LEFT in T874 half B and clanFamily did not, which is the whole of the
# difference between them right now: the affiliation is a PICKER since that task and a picker
# button carries no field= at all (SPEC I107a1) - only the hidden twin binds the name, so ONE
# widget owns it and listing it here would be the stale declaration V36's second half exists to
# catch. The storyteller's swap still happens; it is between a BUTTON and an edit, and V274(c)/(d)
# is what measures it now. clanFamily stays a comboBox until T876 converts wave 5 (SPEC B121).
# clanFamily joined it in T876, which converted wave 5 - so the I71 manual pair now has NO field
# in this set, and that is the correct end state rather than a gap: both members are a picker
# button plus a hidden twin, and in both the twin is the only widget binding the name. V274(b)
# asserts the emptiness out loud, so it is measured and not merely arrived at.
# Virtues were a mirror until the 12th round; they are display-only on the Combat tab now
# (SPEC V51), so the Main tab owns them alone and they must NOT be listed here.

$dupes = @($allFields.GetEnumerator() | Where-Object { $_.Value.Count -gt 1 })
$undeclared = @($dupes | Where-Object { -not $mirrors.Contains($_.Key) })
if ($undeclared) { foreach ($d in $undeclared) { Fail "V1 duplicate field '$($d.Key)' -> $($d.Value -join ', ')" } }
else { Pass "V1 no undeclared duplicate field ($($allFields.Count) fields, $($radios.Count) radios in $(@($radios | ForEach-Object { $_.Field } | Sort-Object -Unique).Count) group(s))" }

$mirrorNames = @($dupes | ForEach-Object { $_.Key })
$staleMirror = @($mirrors | Where-Object { $mirrorNames -notcontains $_ })
if ($staleMirror) { foreach ($s in $staleMirror) { Fail "V36 '$s' is declared a mirror but only one widget owns it - the declaration is stale" } }
else { Pass "V36 all $($mirrors.Count) declared mirrors really are mirrored" }

# ---- V8: every dataLink observes a field some input widget owns ---------------
# The one exception is a field no widget can own: the storyteller's snapshot is hidden from the
# player by having no widget at all, so the usual "some input owns it" test reads it as a dead
# link (SPEC B25). The list is closed and declared in SPEC I3 - a name has to be put here on
# purpose, which is not the same as letting any unowned field through.
$luaOwned = @('baseline', 'xpTotal', 'xpFree', 'xpManual', 'xpOrder', 'descFontSize')

# The floating overlays (SPEC I102a, T760). They are <layout> boxes wearing the same black
# rectangle every section box wears, so V40's neighbour rule and V280/V298's 5px ruler both
# sweep them up - and neither sentence is true of one: an overlay is drawn OVER the grid on
# purpose, and it has no neighbour to stand 5px from. Named and not pattern-matched, for
# V328c's reason - a wildcard would excuse the next box that is simply in the wrong place.
# Both sites below COUNT what they skipped and fail on a count that is not the list's own
# length, so the exclusion cannot go quiet the way B7 describes.
$OVERLAY_BOXES = @('popDesc', 'mfSearch')
foreach ($l in $linkFields) {
    if ($allFields.ContainsKey($l.Field)) { Pass "V8 dataLink '$($l.Field)' observes a real field" }
    elseif ($luaOwned -contains $l.Field) { Pass "V8 dataLink '$($l.Field)' observes a declared Lua-owned field (SPEC I3)" }
    else { Fail "V8 dataLink '$($l.Field)' in $($l.File) observes no existing field - dead link" }
}

# ---- V3: every referenced image exists --------------------------------------
foreach ($img in ($allImages | Sort-Object Path -Unique)) {
    if (Test-Path -LiteralPath (Join-Path $dir $img.Path)) { Pass "V3 image $($img.Path)" }
    else { Fail "V3 missing image '$($img.Path)' referenced by $($img.File)" }
}

# ---- V4: dataType id unique across the repo ---------------------------------
$hits = Select-String -Path (Join-Path $PSScriptRoot "Plugins\Sheets\*\*.lfm"),(Join-Path $PSScriptRoot "Plugins\Sheets\*\*\*.lfm") `
        -Pattern 'AndreOliveira\.Styllern\.WoD20th' -ErrorAction SilentlyContinue
if ($hits.Count -eq 1) { Pass "V4 dataType unique (1 declaration)" } else { Fail "V4 dataType appears $($hits.Count)x" }

# ---- V5: dot counts per trait ------------------------------------------------
function DotCount($base) { ($allFields.Keys | Where-Object { $_ -match "^$([regex]::Escape($base))_\d+$" }).Count }
function BoxCount($base) { ($allFields.Keys | Where-Object { $_ -match "^$([regex]::Escape($base))_c\d+$" }).Count }

$expect = @{}
'strength','dexterity','stamina','charisma','manipulation','appearance','perception','inteligence','wits',
'conscience','selfControl','courage'                                                                      | % { $expect[$_] = 4 }  # dot 1 is a fixed image
# Appearance is the exception: its dot 1 is a real checkBox so the rating can drop to zero
# (SPEC T95), which makes it the only attribute with five owned dots instead of four.
$expect['appearance'] = 5
'alertness','athletics','awareness','brawl','empathy','expression','intimidation','leadership','streetwise','subterfuge',
'animalKen','crafts','etiquette','firearms','larceny','melee','performance','ride','stealth','survival',
'academics','enigmas','finance','investigation','law','medicine','occult','politics','science','technology',
'faith',
'customTalent_1','customTalent_2','customSkill_1','customSkill_2','customKnowledge_1','customKnowledge_2'  | % { $expect[$_] = 5 }
$expect['humanity'] = 10
$expect['willpower'] = 10
# SPEC V5 says "backgrounds 5" dots, but only the ROW count was ever checked - a background
# row could have shipped with four dots and passed.
#
# The count itself is not written here: it is read off BACKGROUND_ROWS, the one place the
# sheet declares it (SPEC V145). A gate holding its own copy of that number is a third place
# to forget.
$bgRootTxt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20th.lfm")))
$bgDecl = [regex]::Match($bgRootTxt, '(?m)^\s*BACKGROUND_ROWS = (\d+);')
$bgRows = if ($bgDecl.Success) { [int]$bgDecl.Groups[1].Value } else { 0 }
if (-not $bgDecl.Success) { Fail "V145 BACKGROUND_ROWS is not declared on the root form - the row count would be a literal in every loop again" }
1..$bgRows | ForEach-Object { $expect["background_$_"] = 5 }
# Numina tab: hedge rows + psychic rows, 5 dots each (SPEC V5 after T56/T57). The two counts
# stopped being a literal `10` in the 90th round: each list declares its own on the root form
# and every loop reads it, so a row drawn in the XML and walked by no loop cannot pass as a
# dot that costs nothing (SPEC I59, I60, V263, and B46 for what a stale literal buys).
$nmDecl = [regex]::Match($bgRootTxt, '(?m)^\s*NUMINA_ROWS\s*=\s*(\d+);')
$nmRows = if ($nmDecl.Success) { [int]$nmDecl.Groups[1].Value } else { 0 }
if (-not $nmDecl.Success) { Fail "V263 NUMINA_ROWS is not declared on the root form - the hedge row count would be a literal in every loop" }
elseif ($nmRows -ge 1) { 1..$nmRows | ForEach-Object { $expect["numina_$_"] = 5 } }
$psDecl = [regex]::Match($bgRootTxt, '(?m)^\s*PSYCHIC_ROWS\s*=\s*(\d+);')
$psRows = if ($psDecl.Success) { [int]$psDecl.Groups[1].Value } else { 0 }
# The HOLE, read from the source and never typed here (SPEC Q32, I117c, V204, V263a). PSYCHIC_ROWS
# is the highest INDEX and four indices inside it are burned: psychic_16.._19 were deleted by the
# user's decision in T830 and the five typed rows still live above them at _20.._24, so a bound
# reduced by four would land on 20 and stop measuring the typed half. The set says WHICH, which a
# count never can - the same reason RITUAL_VACANT is a set.
$psVac = @{}
$psVacM = [regex]::Match($bgRootTxt, '(?m)^\s*PSYCHIC_VACANT\s*=\s*\{([^}]*)\}')
if ($psVacM.Success) { foreach ($m in [regex]::Matches($psVacM.Groups[1].Value, '\[\s*(\d+)\s*\]')) { $psVac[[int]$m.Groups[1].Value] = $true } }
$psDrawn = $psRows - $psVac.Count
if (-not $psDecl.Success) { Fail "V263 PSYCHIC_ROWS is not declared on the root form - the psychic row count would be a literal in every loop" }
elseif (-not $psVacM.Success) { Fail "V263 PSYCHIC_VACANT is not declared on the root form - psychic carries a hole since T830 and every loop that walks it by index needs to know WHICH index is missing (SPEC V204, Q32)" }
elseif ($psRows -ge 1) { 1..$psRows | ForEach-Object { if (-not $psVac[$_]) { $expect["psychic_$_"] = 5 } } }
# Specialities are ONE dot per row (SPEC V147), and the row count is read off the sheet's own
# declaration for the same reason BACKGROUND_ROWS is: a tenth row drawn in the XML and walked
# by neither loop would be a dot that costs nothing and never reaches the log.
$spDecl = [regex]::Match($bgRootTxt, '(?m)^\s*SPECIALITY_ROWS = (\d+);')
$spRows = if ($spDecl.Success) { [int]$spDecl.Groups[1].Value } else { 0 }
if (-not $spDecl.Success) { Fail "V147 SPECIALITY_ROWS is not declared on the root form - the row count would be a literal in every loop" }
elseif ($spRows -ge 1) { 1..$spRows | ForEach-Object { $expect["speciality_$_"] = 1 } }

foreach ($k in ($expect.Keys | Sort-Object)) {
    $got = DotCount $k
    if ($got -eq $expect[$k]) { Pass "V5 $k = $got dots" } else { Fail "V5 $k = $got dots, expected $($expect[$k])" }
}
$wb = BoxCount 'willpower'
if ($wb -eq 10) { Pass "V5 willpower = 10 boxes" } else { Fail "V5 willpower = $wb boxes, expected 10" }

# Each numina table is 8 pickers + 2 free rows (SPEC T56/T57); backgrounds are however many
# BACKGROUND_ROWS says (9 until the 46th round moved the box to the Traits tab, 20 after).
foreach ($grp in @(@('numina',$nmRows), @('psychic',$psDrawn), @('background',$bgRows), @('health',10), @('speciality',$spRows))) {
    $n = ($allFields.Keys | Where-Object { $_ -match "^$($grp[0])_\d+$" }).Count
    if ($n -eq $grp[1]) { Pass "V5 $($grp[0]) = $n rows" } else { Fail "V5 $($grp[0]) = $n rows, expected $($grp[1])" }
}

# The third numina table is a single row (SPEC T81), so it has no `_N` series to count:
# assert the picker field itself instead. Its 5 dots are covered by $expect['faith'] above,
# which brings the Powers tab to 21 rows.
if ($allFields.ContainsKey('faith')) { Pass "V5 faith = 1 row (21st numina row)" }
else { Fail "V5 no 'faith' field - the TRUE FAITH row is missing" }

# The seven semantic health names are gone: the track is positional now (SPEC T96), counted by
# the `health` group above. They are asserted as burned names in the orphan check below.

# ---- V35: a field that changed tabs lives exactly once, in its NEW file ----------
# Removing it from the old file and adding it to the new one is one change. Doing only
# half leaves either two owners (V1) or a saved rating that vanishes with no rename to
# blame (V2) - and neither half fails on its own, which is why this check names both
# ends. `faith_1`..`_5` went Main -> Powers (T79/T81); the nine inventory fields went
# Combat -> Inventory (T76/T77).
#
# Armour moved TWICE: Combat -> Inventory in T76/T77, then back in T87 because armour is
# combat gear. Its old home is therefore WoD20.8 and its new one WoD20.3 - the reverse of the
# other rows here. `transportation` and `other` are no longer listed: they own no widget
# at all now and are checked as declared orphans below instead.
$movedTo  = @{
    'WoD20.7.lfm' = @('faith_1','faith_2','faith_3','faith_4','faith_5')
    'WoD20.8.lfm' = @('items','gear','equipment')
    'WoD20.3.lfm' = @('armorClass','armorRating','armorPenalty','armorDescription')
}
$movedFrom = @{ 'WoD20.7.lfm' = 'WoD20.1.lfm'; 'WoD20.8.lfm' = 'WoD20.3.lfm'; 'WoD20.3.lfm' = 'WoD20.8.lfm' }
$movedBad  = @()
$movedSeen = 0
foreach ($newFile in $movedTo.Keys) {
    $oldFile = $movedFrom[$newFile]
    foreach ($fld in $movedTo[$newFile]) {
        $movedSeen++
        if (-not $allFields.ContainsKey($fld)) {
            $movedBad += "'$fld' has no owner - it left $oldFile and never arrived in $newFile"
            continue
        }
        $owners = @($allFields[$fld])
        $where  = @($owners | ForEach-Object { ($_ -split ':')[0] } | Sort-Object -Unique)
        if ($owners.Count -gt 1) { $movedBad += "'$fld' has $($owners.Count) owners ($($owners -join ', ')) - the move duplicated it" }
        if ($where -contains $oldFile) { $movedBad += "'$fld' is still declared in $oldFile" }
        if ($where -notcontains $newFile) { $movedBad += "'$fld' lives in {$($where -join ', ')}, expected $newFile" }
    }
}
if ($movedBad) { foreach ($m in $movedBad) { Fail "V35 $m" } }
else { Pass "V35 all $movedSeen migrated fields sit exactly once, in their new file" }

# ---- I3 declared orphans: these names must own NO widget -------------------------
# `transportation` and `other` lost their Inventory boxes by request (SPEC C, 3rd round).
# Their text is still sitting in already-saved sheets. If either name were reused for a new
# field, an old sheet would silently pour stale content into an unrelated box - so the names
# stay burned, and SPEC I3 lists them as orphans. This is the check that keeps that promise.
#
# Owning is not the only way to name a field. A dataLink can WATCH one, and a watch on a dead
# name simply never fires - it breaks nothing loudly, it just stops redrawing. WoD20.9 watched
# `stBackgroundsXP` to repaint the experience log, and the 88th round turned that flag into a
# price: the watch would have gone on pointing at a name no widget writes any more, and the log
# would have sat still while the storyteller changed what a background costs.
#
# V8 does not reach these: it reads `field=`, and every list of this shape is `fields=`. So the
# orphan check asks BOTH questions - who owns it, and who is listening for it (SPEC V209, V20).
$watched = @{}
foreach ($f in $files) {
    foreach ($dl in (Doc $f.FullName).SelectNodes("//dataLink")) {
        $names = "$($dl.GetAttribute('field')) $($dl.GetAttribute('fields'))"
        foreach ($m in [regex]::Matches($names, '[A-Za-z_][A-Za-z0-9_]*')) {
            if (-not $watched.ContainsKey($m.Value)) { $watched[$m.Value] = @() }
            if ($watched[$m.Value] -notcontains $f.Name) { $watched[$m.Value] += $f.Name }
        }
    }
}
if ($watched.Count -eq 0) { Fail "I3 no dataLink watch was read across the sheet - the dead-watch leg of this check verifies nothing (SPEC V209, V20)" }
else { Pass "I3 $($watched.Count) distinct field name(s) are watched by a dataLink somewhere on the sheet" }

# The three Vampire selectors joined in T834: wave 3 retired the radioButtons with the panes
# that read them (user 2026-08-31, ask B), so the widgets are gone and the DATA is still in every
# saved sheet. Declared here rather than deleted quietly - which is what V362(b) exists to force,
# and these three are what it caught on its first run (SPEC V176 retired, V2, T872).
# mainPath_5 joined in T879 (user 2026-09-01): the box went from five rows to four so column 2 of
# the Ghoul tab closes where column 1 does (SPEC I118f), and the WIDGET is what left - a sheet
# saved before this round still carries whatever path was in the fifth slot. ORPHAN, not DELETED.
#
# xpLog joined in T882 and it had been missing since the 27th round. It reads today like a name
# that was never a field - WoD20.9 mentions it in one comment and in the CONTROL names xpLogBox
# and xpLogScroll, and no `field=` anywhere - but the free-text block it belonged to DID carry the
# field before the ledger replaced it (SPEC I10, V91), so an old sheet holds the text and the name
# is burned like every other one here. Being outside FIELD_CONTRACT is not a reason to leave it
# out: leg (b) below reads this list only as a membership test for names that ARE in the contract,
# so an extra name is never visited - and it gains the loop underneath, which is the guard that
# actually covers it (SPEC I3 amended, T882).
#
# The three Numina selectors joined in T874 for exactly the reason the three Vampire ones did one
# wave earlier: the DESCRIPTION panes were the only thing that ever read a marked row, wave 4 took
# all four panes out of WoD20.7, and the ? took the 0..20 the radioButtons were holding. ORPHANS,
# not DELETED - a sheet saved before this round still carries which row was marked, and the name
# stays burned so nothing new can wear it (SPEC V2, V176 retired, V362b, T874).
$I3_ORPHANS = @('stShowMagika','transportation','other','bruised','hurt','injured','wounded','mauled','crippled','incapacitated','personalidade','natureza','experience','spentXP','stFreeDots','freeDots','ritual_21','ritual_22','ritual_23','ritual_24','ritual_25','stBackgroundsXP','discSel','pathSel','ritualSel','mainPath_5','xpLog','numinaSel','psychicSel','hedgeRitualSel')
# The OTHER exit, and it is not the same one (SPEC V362b, B99): here the DATA goes too. Closed at
# four names by V362(d), and a fifth needs a new Q - without the ceiling, "deleted by decision"
# becomes the door every field leaves through. Empty until T830 does the deleting.
$I3_DELETED = @('psychic_16', 'psychic_17', 'psychic_18', 'psychic_19')
foreach ($orphan in $I3_ORPHANS) {
    if ($allFields.ContainsKey($orphan)) {
        Fail "I3 '$orphan' is a declared orphan but $($allFields[$orphan] -join ', ') owns it - choose a different field name"
    } elseif ($watched.ContainsKey($orphan)) {
        Fail "I3 '$orphan' owns no widget but $($watched[$orphan] -join ', ') still WATCHES it - a dataLink on a dead name never fires, so whatever it redraws quietly stops (SPEC V2, V8)"
    } else { Pass "I3 declared orphan '$orphan' owns no widget and nothing watches it" }
}

# ---- V362: what the layout MOVES, it does not EAT ------------------------------------
# SPEC V362, I117/I118/I119, pedidos 4, 5 and 6. Three rounds of layout work moved boxes between
# tabs and between files, and the failure mode of that work is not a crash: it is a field that
# quietly stops being authored, so an already-saved sheet keeps the value and nothing ever shows
# it again. Nothing else catches that - V1 counts duplicates, V2 catches renames one name at a
# time, V5 counts dots. A field that simply VANISHES passes all three.
#
# (a) needs a BEFORE and a static check has none to read, so it is FROZEN. FIELD_CONTRACT is
# every field= authored at b5111c35 - the last clean commit before wave 3 and T832 - read the way
# this gate reads them, off the parsed XML, with template placeholders left out because $(field)
# is not a name. Frozen at an EARLIER commit and not at today's state ON PURPOSE: frozen on today
# it could never redden for anything that already happened, which arms the future and certifies
# nothing about the past.
#
# It is a FLOOR and not a ceiling (the spec says superset): a NEW field costs nothing here, and a
# field that LEAVES costs one line in I3 - exactly the price (b) exists to charge.
$FIELD_CONTRACT = @(
    'academics', 'alertness', 'allies', 'altura', 'animalKen', 'anotacoes1', 'anotacoes2', 'anotacoes3',
    'aparencia', 'appearance', 'armorClass', 'armorDescription', 'armorPenalty', 'armorRating', 'arts', 'athletics',
    'avatar', 'awareness', 'background', 'background_1', 'background_10', 'background_11', 'background_12', 'background_13',
    'background_14', 'background_15', 'background_16', 'background_17', 'background_18', 'background_19', 'background_2', 'background_20',
    'background_21', 'background_3', 'background_4', 'background_5', 'background_6', 'background_7', 'background_8', 'background_9',
    'baseArmaments', 'baseDefenses', 'baseline', 'baseOfOperation', 'baseOther', 'bgOther', 'bloodPool_1', 'bloodPool_10',
    'bloodPool_11', 'bloodPool_12', 'bloodPool_13', 'bloodPool_14', 'bloodPool_15', 'bloodPool_16', 'bloodPool_17', 'bloodPool_18',
    'bloodPool_19', 'bloodPool_2', 'bloodPool_20', 'bloodPool_3', 'bloodPool_4', 'bloodPool_5', 'bloodPool_6', 'bloodPool_7',
    'bloodPool_8', 'bloodPool_9', 'brawl', 'cabelo', 'charisma', 'chronicle', 'clan', 'clanDisc_1',
    'clanDisc_2', 'clanDisc_3', 'clanDisc_4', 'clanFamily', 'computer', 'concept', 'conscience', 'contacts',
    'cosmology', 'courage', 'crafts', 'customKnowledge_1', 'customKnowledge_2', 'customKnowledge_3', 'customSkill_1', 'customSkill_2',
    'customTalent_1', 'customTalent_2', 'customTalent_3', 'customTalent_4', 'demeanor', 'derangements', 'dexterity', 'disc_1',
    'disc_10', 'disc_11', 'disc_12', 'disc_13', 'disc_14', 'disc_15', 'disc_2', 'disc_3',
    'disc_4', 'disc_5', 'disc_6', 'disc_7', 'disc_8', 'disc_9', 'discSel', 'dominator',
    'dominatorGen', 'empathy', 'enigmas', 'equipment', 'esoterica', 'etinicidade', 'etiquette', 'expression',
    'faith', 'faith_1', 'faith_2', 'faith_3', 'faith_4', 'faith_5', 'fame', 'fears',
    'finance', 'firearms', 'game', 'gear', 'healthLevels', 'hedgeAffiliation', 'hedgeAttr', 'hedgeEssence',
    'hedgeRitual_1', 'hedgeRitual_10', 'hedgeRitual_11', 'hedgeRitual_12', 'hedgeRitual_13', 'hedgeRitual_14', 'hedgeRitual_15', 'hedgeRitual_16',
    'hedgeRitual_17', 'hedgeRitual_2', 'hedgeRitual_3', 'hedgeRitual_4', 'hedgeRitual_5', 'hedgeRitual_6', 'hedgeRitual_7', 'hedgeRitual_8',
    'hedgeRitual_9', 'hedgeRitualSel', 'humanity', 'idade', 'idadeAparente', 'idiomas', 'influence', 'inteligence',
    'intimidation', 'investigation', 'items', 'language', 'larceny', 'law', 'leadership', 'mainPath_1',
    'mainPath_2', 'mainPath_3', 'mainPath_4', 'mainPath_5', 'manipulation', 'martialArts', 'medicine', 'meditation',
    'melee', 'mentor', 'moralFlaws', 'motivation', 'nacionalidade', 'name', 'nature', 'numina_1',
    'numina_10', 'numina_11', 'numina_12', 'numina_13', 'numina_14', 'numina_15', 'numina_16', 'numina_17',
    'numina_2', 'numina_3', 'numina_4', 'numina_5', 'numina_6', 'numina_7', 'numina_8', 'numina_9',
    'numinaSel', 'occult', 'occupation', 'olhos', 'pathSel', 'perception', 'performance', 'peso',
    'player', 'politics', 'psychic_1', 'psychic_10', 'psychic_11', 'psychic_12', 'psychic_13', 'psychic_14',
    'psychic_15', 'psychic_16', 'psychic_17', 'psychic_18', 'psychic_19', 'psychic_2', 'psychic_20', 'psychic_21',
    'psychic_22', 'psychic_23', 'psychic_24', 'psychic_3', 'psychic_4', 'psychic_5', 'psychic_6', 'psychic_7',
    'psychic_8', 'psychic_9', 'psychicSel', 'quint_1', 'quint_10', 'quint_2', 'quint_3', 'quint_4',
    'quint_5', 'quint_6', 'quint_7', 'quint_8', 'quint_9', 'ranged', 'research', 'residence',
    'resources', 'retainers', 'ride', 'ritual_1', 'ritual_10', 'ritual_11', 'ritual_12', 'ritual_13',
    'ritual_14', 'ritual_15', 'ritual_16', 'ritual_17', 'ritual_18', 'ritual_19', 'ritual_2', 'ritual_20',
    'ritual_3', 'ritual_4', 'ritual_5', 'ritual_6', 'ritual_7', 'ritual_8', 'ritual_9', 'ritualSel',
    'road', 'science', 'secPath_1', 'secPath_10', 'secPath_11', 'secPath_12', 'secPath_13', 'secPath_14',
    'secPath_15', 'secPath_2', 'secPath_3', 'secPath_4', 'secPath_5', 'secPath_6', 'secPath_7', 'secPath_8',
    'secPath_9', 'selfControl', 'sexo', 'sheetTheme', 'stamina', 'status', 'stBackgroundCost', 'stealth',
    'stEditClanDisc', 'stEditSpentXP', 'stFreeBuy', 'stManualAffiliation', 'stManualClanFamily', 'stNotes1', 'stNotes2', 'streetwise',
    'strength', 'stShowDisciplines', 'stShowNumina', 'stSpecCost', 'subterfuge', 'survival', 'technology', 'willpower',
    'willpower_c1', 'willpower_c10', 'willpower_c2', 'willpower_c3', 'willpower_c4', 'willpower_c5', 'willpower_c6', 'willpower_c7',
    'willpower_c8', 'willpower_c9', 'wits', 'xpFree', 'xpTotal'
)
$v362Bad = @()
$gone362 = @()
# The census is taken HERE and with the SAME reader the contract was frozen with - every node
# carrying a field=, straight off the parsed XML. $allFields is not it: that one is built for V1
# and keys some families differently, so comparing a raw contract against it reports two hundred
# fields "gone" that are sitting right there. Both sides of a subset test have to be measured the
# same way, or the test measures the readers instead of the sheet.
$now362 = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
foreach ($f in $files) {
    foreach ($n in (Doc $f.FullName).SelectNodes("//*[@field]")) {
        $v = $n.GetAttribute("field")
        if ($v -and $v -notmatch '\$\(') { [void]$now362.Add($v) }
    }
}
foreach ($fc in $FIELD_CONTRACT) {
    if ($now362.Contains($fc)) { continue }
    $gone362 += $fc
    # (b) TWO named exits and they are NOT the same thing (SPEC B99). ORPHAN keeps the data in the
    # NDB and drops the widget; DELETED BY DECISION takes the data with it. An invariant may not
    # veto an informed decision of the product's owner - it has to RECORD which of the two it was.
    if ($I3_ORPHANS -notcontains $fc -and $I3_DELETED -notcontains $fc) {
        $v362Bad += "(a/b) '$fc' was authored at b5111c35, owns no widget now, and is in NEITHER list of I3 - a field that leaves undeclared is a saved sheet carrying a value nothing will ever show again (SPEC V362a, V362b, V2)"
    }
}
# (e) zero-guard on BOTH sides: a contract that parsed to nothing, or a census that did, would
# make every line above pass over an empty set (SPEC V20, B7). The contract is HISTORY and never
# grows, so its size is exact - a contract that changed size was edited, not measured.
if ($FIELD_CONTRACT.Count -ne 325) { $v362Bad += "(e) the frozen contract holds $($FIELD_CONTRACT.Count) name(s) and b5111c35 authored 325 - the contract is history and does not move; one that did was edited (SPEC V209, V362e)" }
if ($now362.Count -eq 0) { $v362Bad += "(e) the field census is empty - every leg above passed over nothing (SPEC V20, B7)" }
if ($I3_ORPHANS.Count -eq 0) { $v362Bad += "(e) the I3 orphan list is empty - leg (b) would refuse every field that ever leaves (SPEC V209)" }
# (d) the DELETED list is CLOSED at four and, since T830 did the deleting, FULL at four. Without
# the ceiling "deleted by decision" is the door every field walks out of unasked (SPEC V362d,
# V351d); without the floor the list is empty and (c) below walks nothing, which is the vacuous
# pass B7 is named for. Q32 answered with exactly these four, so the count is EXACT, not a bound.
if ($I3_DELETED.Count -ne 4) { $v362Bad += "(d) the DELETED-by-decision list holds $($I3_DELETED.Count) name(s) and Q32 answered with exactly 4 - a fifth needs a new Q or the exception becomes the exit, and a fourth missing means leg (c) is guarding a name nobody deleted (SPEC V362d, Q32, V351d)" }

# (c) a NEW slot takes the next number of its root and never wears a vacant id - a reused id hands
# the old field's saved value to the new one, silently, which is the one failure a size comparison
# can never see (SPEC V362c, I117c). Armed by T830, which is what created the four vacancies.
#
# Two halves, and the second is the one with teeth. FIRST: none of the four comes back, as a
# widget or as a watcher - the I3 loop above walks the ORPHANS only, so until now nothing at all
# said psychic_17 may not be authored again. SECOND: the two ledgers of the hole AGREE. I3 says
# which names the user deleted and PSYCHIC_VACANT says which indices the Lua loops skip, and they
# are written a file apart - drift between them is a row drawn into a burned id (the Lua side
# forgot) or a live row skipped by every painter (the XML side did), and V204 measures the XML
# against I3, never I3 against the Lua (SPEC V204, V263, B116).
foreach ($del362 in $I3_DELETED) {
    if ($allFields.ContainsKey($del362)) { $v362Bad += "(c) '$del362' was deleted by decision in T830 and $($allFields[$del362] -join ', ') authors it again - the id is vacant, and a new slot wearing it is handed whatever the deleted field held in every sheet saved before that round (SPEC V362c, Q32, V2)" }
    if ($watched.ContainsKey($del362)) { $v362Bad += "(c) '$del362' owns no widget and $($watched[$del362] -join ', ') still WATCHES it - a dataLink on a deleted name never fires, so whatever it redraws quietly stops (SPEC V362c, V2, V8)" }
}
$psDel362 = @($I3_DELETED | Where-Object { $_ -match '^psychic_(\d+)$' } | ForEach-Object { [int]($_ -replace '^psychic_', '') } | Sort-Object)
$psVac362 = @($psVac.Keys | Sort-Object)
if (($psDel362 -join ',') -ne ($psVac362 -join ',')) {
    $v362Bad += "(c) I3 deletes psychic index/indices $($psDel362 -join ', ') and PSYCHIC_VACANT on the root form skips $($psVac362 -join ', ') - the hole has two ledgers and they disagree, so either a painter walks into a burned id or a live row is skipped by every loop that reads the set (SPEC V362c, V204, V263, B116)"
}
if ($v362Bad) { foreach ($b in $v362Bad) { Fail "V362 $b" } }
else {
    # T882 asked for this measurement to be carried so the next round does not rediscover it. It
    # is REPORTED and not written into a comment on purpose: a comment saying "4 of them" goes
    # stale the day a field retires, which is the disease V333(g) caught three rounds running and
    # B46 named. Both lists are in scope right here, so the number is measured every run.
    #
    # What it says is how far this leg REACHES. The contract is history frozen at b5111c35, so an
    # orphan that left BEFORE the freeze is not in it and (b) never visits that name - its only
    # guard is the I3 loop above (no widget, no watcher). Whoever retires a field later needs to
    # know V362 only sees what existed at the freeze (SPEC I3 amended, V362a, T882).
    $covered362 = @($I3_ORPHANS | Where-Object { $FIELD_CONTRACT -contains $_ })
    # Which EXIT each one took, counted and not asserted: until T830 every departure was an
    # orphan and the line said so in prose, which is the stale literal B46 names - the sentence
    # was wrong the moment Q32's four went out the other door (SPEC V362b, B99).
    $goneOrph362 = @($gone362 | Where-Object { $I3_ORPHANS -contains $_ })
    $goneDel362  = @($gone362 | Where-Object { $I3_DELETED -contains $_ })
    Pass "V362 all $($FIELD_CONTRACT.Count) fields frozen at b5111c35 are still authored somewhere, or declared in one of I3's two lists - $($gone362.Count) have left so far ($($gone362 -join ', ')), $($goneOrph362.Count) of them as orphans and $($goneDel362.Count) deleted by decision. Of I3's $($I3_ORPHANS.Count) declared orphans this leg reaches $($covered362.Count) ($($covered362 -join ', ')) - the other $($I3_ORPHANS.Count - $covered362.Count) left before the freeze, are not in the contract, and are held by the I3 loop alone. Legs (c) and (d) armed in T830: the deleted-by-decision list is exactly the $($I3_DELETED.Count) Q32 named ($($I3_DELETED -join ', ')), not one of them is authored or watched again, and PSYCHIC_VACANT skips the same indices I3 burned"
}

# The three Base of Operations boxes added to Inventory (SPEC T89). Named explicitly so a
# typo produces a loud failure instead of a box that renders fine and saves nowhere.
foreach ($fld in @('baseDefenses','baseArmaments','baseOther')) {
    if (-not $allFields.ContainsKey($fld)) { Fail "V1 '$fld' has no widget - a Base of Operations box is missing" }
    else {
        $own = @($allFields[$fld])
        $wh  = @($own | ForEach-Object { ($_ -split ':')[0] } | Sort-Object -Unique)
        if ($own.Count -eq 1 -and $wh[0] -eq 'WoD20.8.lfm') { Pass "V1 '$fld' owned once, in WoD20.8.lfm" }
        else { Fail "V1 '$fld' owned by $($own -join ', ') - expected exactly one widget in WoD20.8.lfm" }
    }
}

# ---- collect every user-visible string ---------------------------------------
# label/checkBox/button text, template `nome=` args, tab titles, Lua t("..."), picker items.
$visible = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
$visiblePickerItems = 0
$padded  = @()
foreach ($f in $files) {
    $raw = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName))
    $xml = Doc $f.FullName
    foreach ($n in $xml.SelectNodes("//*")) {
        foreach ($a in @('text','nome')) {
            if (-not $n.HasAttribute($a)) { continue }
            $v = $n.GetAttribute($a)
            if (-not $v -or $v -match '\$\(' -or $v.Trim() -eq '') { continue }
            if ($n.LocalName -in @('label','checkBox','button') -or $a -eq 'nome') {
                # The ? of the description buttons is the ONE declared exception (SPEC I102b),
                # and it is the numeral precedent of V17 again: punctuation is not language, it
                # is the SAME glyph in both halves, and wod.?=? would be a translation entry no
                # translator can change. Narrow on purpose - only a btnQ* button whose whole text
                # is the single question mark opts out, so every other untranslated button still
                # fails exactly as it did. A wildcard here would be B7 arriving through the
                # exception door, which is the mistake V15's comment names twenty lines below.
                if ($n.LocalName -eq 'button' -and $v.Trim() -eq '?' -and $n.GetAttribute("name") -like 'btnQ*') { continue }
                [void]$visible.Add($v.Trim())
                if ($v -match '   ') { $padded += "$($f.Name): '$v'" }   # 3+ spaces
            }
        }
    }
    # Tab titles are translated too (WoD20.6 handles cls == "tab"), so they are visible strings.
    foreach ($n in $xml.SelectNodes("//tab[@title]")) { [void]$visible.Add($n.GetAttribute("title").Trim()) }
    # runtime strings built in Lua go through the t() helper. The lookbehind is the whole
    # point (SPEC V222, B60): without it the pattern matches the trailing `t` of any other
    # call - string.format("%.1f") read as t("%.1f") - and a format verb is demanded of the
    # .lang file as if it were prose. One-argument calls only were affected, because the
    # closing quote has to touch the paren; format("#%08X", v) escaped on its comma alone,
    # which is luck and not a rule.
    [regex]::Matches($raw, '(?<!\w)t\("([^"]+)"\)') | ForEach-Object { [void]$visible.Add($_.Groups[1].Value) }
    # Picker items are user-visible too (SPEC V17). Read them off the XML `items=` attribute,
    # NOT out of a Lua table: the lists live inline in the templates now, and a checker that
    # greps for the old Lua form would pass silently while verifying nothing.
    # Only `cbo*` pickers count - the colour and theme combos are values, not prose.
    foreach ($cb in $xml.SelectNodes("//comboBox[@name]")) {
        if ($cb.GetAttribute("name") -notlike 'cbo*') { continue }
        # The list lives in PICKER_LIST now (SPEC I27, T493). ListOf still falls back to the
        # inline attribute for the pickers T493 left alone, so both worlds land here.
        foreach ($it in (ListOf $cb (TplOf $cb))) {
            if ($it -ne '') { [void]$visible.Add($it); $visiblePickerItems++ }
        }
    }
}

# ---- V13/V14/V15: the picker pattern (SPEC R.4) -------------------------------
# The free-text `edit` was dropped by request on 2026-08-17, so the picker comboBox IS the
# field now. A picker without a field would render fine and silently save nothing.
$pickerBad = @()
$listReport = @()
foreach ($f in $files) {
    $xml = Doc $f.FullName
    foreach ($n in $xml.SelectNodes("//comboBox")) {
        $nm = $n.GetAttribute("name")
        if ($nm -like 'cbo*' -and -not $n.HasAttribute("field")) { $pickerBad += "$($f.Name): picker '$nm' owns no field - it would save nothing" }
    }
    foreach ($cb in $xml.SelectNodes("//comboBox[@name]")) {
        $nm = $cb.GetAttribute("name")
        if ($nm -notlike 'cbo*') { continue }
        $items = @(ListOf $cb (TplOf $cb))
        if ($items.Count -eq 0) { continue }
        $listReport += [pscustomobject]@{ Name = "$($f.Name)/$nm"; Items = $items }
    }
}
if ($pickerBad) { foreach ($b in $pickerBad) { Fail "V13 $b" } } else { Pass "V13 every picker comboBox owns its field" }

foreach ($l in $listReport) {
    $dupe = $l.Items | Group-Object | Where-Object Count -gt 1
    if ($dupe) { Fail "V14 $($l.Name) has duplicates: $(($dupe | ForEach-Object { $_.Name }) -join ', ')" }
    # Most pickers carry a leading empty entry that is not an option; cboSheetTheme is the
    # declared exception and has none, so counting it the same way under-reported it by one.
    elseif ($l.Items[0] -eq '') { Pass "V14 $($l.Name) has $($l.Items.Count - 1) unique entries" }
    else { Pass "V14 $($l.Name) has $($l.Items.Count) unique entries" }
    # cboSheetTheme is the declared exception (SPEC V15, 11th round): a state combo with a real
    # default, not prose to be cleared - there is no such thing as a sheet with no theme.
    # cboGame joined it in the 32nd round for the same reason and one more: it is locked, so an
    # empty row would be a value nobody could get back out of (SPEC V109).
    # cboRoad joined in the 125th round (SPEC V328d): a character with no road IS on Humanity,
    # so there is no empty road to clear back to. Named one by one on purpose - a wildcard that
    # let any combo opt out would kill V15 for the thirty-odd pickers that do need the empty
    # row, which is B7 arriving through the exception door.
    if ($l.Name -like '*cboSheetTheme*' -or $l.Name -like '*cboGame*' -or $l.Name -like '*cboRoad*') { Pass "V15 $($l.Name) n/a - state combo with a default (declared exception)" }
    elseif ($l.Items[0] -eq '') { Pass "V15 $($l.Name) starts empty" } else { Fail "V15 $($l.Name) first entry is '$($l.Items[0])', expected empty" }
    # V328b/c: the road combo's default and its leading item are the SAME literal, and both are
    # `Humanity`. Two owners for one default is how B70 started, so they are compared, not
    # assumed - and the zero-guard is that cboRoad reached this loop at all (named below).
    if ($false) {
        if ($l.Items[0] -ne 'Path of Humanity') { Fail "V328c cboRoad leads on '$($l.Items[0])', not the Path of Humanity SPEC I99b declares" }
        else { Pass "V328c cboRoad leads on Path of Humanity, the declared V15 exception" }
    }
}

# The three checks above are only worth as much as what the collector saw: a picker named
# without the `cbo` prefix, or one that lost its inline `items=`, drops out of $listReport
# and every check on it becomes a silent no-op (SPEC V20 / B.7). Name the new ones.
foreach ($must in @('WoD20.6.lfm/cboGame','WoD20.7.lfm/cboHedgeAttr','WoD20.6.lfm/cboSheetTheme')) {
    if ($listReport | Where-Object { $_.Name -eq $must }) { Pass "V14/V15/V17 $must reaches the list checks" }
    else { Fail "V14/V15/V17 $must was never collected - its list is unchecked" }
}

# cboGame is a closed roster, not an open vocabulary: assert the names themselves, so a
# dropped, renamed or added entry fails instead of passing as "no duplicates" (SPEC V109).
# Three since the 32nd round, and no leading empty one to discount.
$gameList = @($listReport | Where-Object { $_.Name -eq 'WoD20.6.lfm/cboGame' })
if ($gameList.Count -eq 1) {
    $wantGames = @('Vampire', 'Mortal', 'Mage')
    $gotGames = @($gameList[0].Items)
    if (($gotGames -join '|') -eq ($wantGames -join '|')) { Pass "V109 cboGame offers exactly $($wantGames -join ', ')" }
    else { Fail "V109 cboGame offers '$($gotGames -join ', ')', expected '$($wantGames -join ', ')'" }
}

# ---- V9: source language is English - no non-ASCII in authored strings -------
# Deliberately a codepoint range, not a list of accented letters: this file is read
# as ANSI by PowerShell 5.1 when it has no BOM, so non-ASCII literals here would rot.
# TWO codepoints are excused, and both are written as escapes for the same reason
# (SPEC V195, B42). U+2019: the core prints forty-two path and ritual names with a
# typographic apostrophe - Devil's Touch, Grave's Decay, Boukephos' Gateway - so this
# check was calling correct English "not English". U+00F0 joined it in the 92nd round
# for `Forn Jafnadr`, an Old Norse affiliation the M20 Sorcerer prints with an eth
# (SPEC I66). Spelling is not language, and neither codepoint occurs in Portuguese, so
# the door stays shut on what this check defends: every other non-ASCII codepoint still
# fails, and an authored Portuguese string is caught exactly as before.
$ptChars = $visible | Where-Object { $_ -match '[^\x00-\x7F\u2019\u00F0]' }
if ($ptChars) { foreach ($s in $ptChars) { Fail "V9 non-English string '$s' - source must be English" } }
else { Pass "V9 all $($visible.Count) visible strings authored in English" }

# ---- V11: no label aligns columns with padding runs (B.5) -------------------
if ($padded) { foreach ($s in $padded) { Fail "V11 padding run used for alignment -> $s" } }
else { Pass "V11 no label uses padding runs" }

# ---- V10: every visible string has a key in BOTH [pt] and [en] ---------------
$noPt = @($visible | Where-Object { -not $ptK.Contains($_) })
$noEn = @($visible | Where-Object { -not $enK.Contains($_) })
if ($noPt) { foreach ($s in $noPt) { Fail "V10 no [pt] key for '$s'" } } else { Pass "V10 all visible strings have a [pt] key" }
if ($noEn) { foreach ($s in $noEn) { Fail "V10 no [en] key for '$s'" } } else { Pass "V10 all visible strings have an [en] key" }

# ---- V16 + V25: a label must be wide enough for the LONGER of its EN and PT text
# Crude on purpose: no font metrics available offline, so estimate at ~6.5px/char for the
# default proportional font. Only flags clear overflow, not tight fits.
#
# V25 exists because V16 alone was blind (SPEC B.11): a template label carries
# text="$(nome)", which the `$(` guard skipped, so EVERY row label in the sheet escaped
# the check. "Animal Ken" is "Empatia com Animais" (19 chars, ~124px) in a label that
# was 80px wide, and WILLPOWER is "FORCA DE VONTADE" (16 chars) in a 60px one.
# The ruler knows the FONT SIZE since the 114th round (SPEC I86f, V312, R116). It is one
# constant per size and not one per control: a ruler picked by NAME would be a nominal
# exception, which is B38 arriving through the width door, while the physics is the same for
# every control that authors the same size.
#
# 6.0 for fontSize="12", and it is still a CEILING. R116 is what earned it: on the running
# sheet the [pt] item `Sociedade de Ideologias Altruistas Esclarecidas (SEAI)` leaves over a
# centimetre of slack inside a 362px interior, so 55 characters render at 5.89px or less. 6.0
# stays above that. Dropping it further without a NEW measurement trades a ceiling for a guess,
# and what waits on the other side is a comboBox that CLIPS rather than wraps (SPEC V196, B43).
$PX_PER_CHAR    = 6.5
$PX_PER_CHAR_12 = 6.0
function NeededPx($txt, $fontSize) {
    $longest = $txt.Length
    if ($ptVal.ContainsKey($txt) -and $ptVal[$txt].Length -gt $longest) { $longest = $ptVal[$txt].Length }
    # Callers that measure a plain label pass nothing and get the default body ruler, which is
    # the calibration this check has always run on (SPEC V312a).
    $ruler = if ("$fontSize" -eq '12') { $PX_PER_CHAR_12 } else { $PX_PER_CHAR }
    [math]::Ceiling($longest * $ruler)
}
$tooNarrow = @()
foreach ($f in $files) {
    $xml = Doc $f.FullName

    # V25 first: map each template's placeholder labels to the width they render in
    $tplLabel = @{}
    foreach ($t in $xml.SelectNodes("//template")) {
        $arr = @()
        foreach ($lb in $t.SelectNodes(".//label[@text][@width]")) {
            $w = 0; if (-not [int]::TryParse($lb.GetAttribute("width"), [ref]$w)) { continue }
            if ($lb.GetAttribute("text") -match '^\$\((\w+)\)$') { $arr += [pscustomobject]@{ Attr = $Matches[1]; Width = $w } }
        }
        if ($arr.Count) { $tplLabel[$t.GetAttribute("name")] = $arr }
    }
    foreach ($n in $xml.SelectNodes("//*")) {
        if (-not $tplLabel.ContainsKey($n.LocalName)) { continue }
        foreach ($slot in $tplLabel[$n.LocalName]) {
            if (-not $n.HasAttribute($slot.Attr)) { continue }
            $txt = $n.GetAttribute($slot.Attr).Trim()
            if (-not $txt) { continue }
            $need = NeededPx $txt
            if ($need -gt $slot.Width) { $tooNarrow += "$($f.Name): <$($n.LocalName)> $($slot.Attr)='$txt' (pt '$($ptVal[$txt])') needs ~${need}px, template label width=$($slot.Width)px" }
        }
    }

    # V16: plain literal labels
    foreach ($n in $xml.SelectNodes("//label[@text][@width]")) {
        $txt = $n.GetAttribute("text").Trim()
        if (-not $txt -or $txt -match '\$\(') { continue }
        $w = 0; if (-not [int]::TryParse($n.GetAttribute("width"), [ref]$w)) { continue }
        $need = NeededPx $txt
        if ($need -gt $w) { $tooNarrow += "$($f.Name): '$txt' (pt '$($ptVal[$txt])') needs ~${need}px, width=${w}px" }
    }
}
if ($tooNarrow) { foreach ($t in $tooNarrow) { Fail "V16/V25 $t" } } else { Pass "V16/V25 every label fits its longest translation (templates included)" }

# ---- V26: sibling rows share a dot column and an input column (SPEC B.12) -----
# Rows are `<layout left=N>` wrappers around a template call. Two rows drawn at the same
# `left` inside the same box must use templates whose first dot sits at the same x, else
# the dot columns visibly stagger - which is how Humanity (x=60) drifted from Conscience
# (x=80), and Concept's edit (x=90) from the Nature picker (x=112).
$misaligned = @()
foreach ($f in $files) {
    $xml = Doc $f.FullName

    $geom = @{}
    foreach ($t in $xml.SelectNodes("//template")) {
        $dot = $null; $inp = $null
        foreach ($c in $t.SelectNodes(".//*")) {
            $x = 0; if (-not [int]::TryParse($c.GetAttribute("left"), [ref]$x)) { continue }
            # A fixed <image> IS dot 1 in the Attribute template, so it counts as a dot.
            if ($null -eq $dot -and $c.LocalName -in @('image','imageCheckBox','checkBox')) { $dot = $x }
            if ($null -eq $inp -and $c.LocalName -in @('edit','comboBox')) { $inp = $x }
        }
        $geom[$t.GetAttribute("name")] = [pscustomobject]@{ Dot = $dot; Input = $inp }
    }
    if ($geom.Count -eq 0) { continue }

    foreach ($box in $xml.SelectNodes("//layout")) {
        $byLeft = @{}
        foreach ($row in $box.SelectNodes("layout")) {
            $lf = $row.GetAttribute("left")
            foreach ($call in $row.SelectNodes("*")) {
                if (-not $geom.ContainsKey($call.LocalName)) { continue }
                if (-not $byLeft.ContainsKey($lf)) { $byLeft[$lf] = @() }
                $byLeft[$lf] += [pscustomobject]@{ Tpl = $call.LocalName; G = $geom[$call.LocalName] }
            }
        }
        foreach ($lf in $byLeft.Keys) {
            $rows = $byLeft[$lf]
            if ($rows.Count -lt 2) { continue }
            $dots = @($rows | Where-Object { $null -ne $_.G.Dot } | ForEach-Object { $_.G.Dot } | Sort-Object -Unique)
            if ($dots.Count -gt 1) {
                $who = ($rows | Where-Object { $null -ne $_.G.Dot } | ForEach-Object { "$($_.Tpl)@$($_.G.Dot)" } | Sort-Object -Unique) -join ', '
                $misaligned += "$($f.Name): rows at left=$lf have dots at {$($dots -join ', ')} -> $who"
            }
            $dotless = @($rows | Where-Object { $null -eq $_.G.Dot -and $null -ne $_.G.Input })
            $ins = @($dotless | ForEach-Object { $_.G.Input } | Sort-Object -Unique)
            if ($ins.Count -gt 1) {
                $who = ($dotless | ForEach-Object { "$($_.Tpl)@$($_.G.Input)" } | Sort-Object -Unique) -join ', '
                $misaligned += "$($f.Name): rows at left=$lf have inputs at {$($ins -join ', ')} -> $who"
            }
        }
    }
}
if ($misaligned) { foreach ($m in $misaligned) { Fail "V26 $m" } } else { Pass "V26 sibling rows share their dot and input columns" }

# ---- V37: sibling rows on one line must not draw over each other (SPEC B16) -------
# The Background tab shipped a 310px-wide template in columns spaced 265 apart, so the third
# column's label sat inside the second column's edit. Two rows were affected and the gate was
# green throughout: V16/V25 measure text against its own label width, never a row's footprint
# against its neighbour's. This measures the footprint.
$overlaps = @()
$twinByRect = 0
foreach ($f in $files) {
    $xml = Doc $f.FullName

    $tplWidth = @{}
    foreach ($t in $xml.SelectNodes("//template")) {
        $mx = 0
        foreach ($c in $t.SelectNodes(".//*")) {
            $l = 0; $w = 0
            if ([int]::TryParse($c.GetAttribute("left"), [ref]$l) -and [int]::TryParse($c.GetAttribute("width"), [ref]$w)) {
                if (($l + $w) -gt $mx) { $mx = $l + $w }
            }
        }
        $tplWidth[$t.GetAttribute("name")] = $mx
    }
    if ($tplWidth.Count -eq 0) { continue }

    foreach ($box in $xml.SelectNodes("//layout")) {
        $rows = @()
        foreach ($row in $box.SelectNodes("layout")) {
            $l = 0
            if (-not [int]::TryParse($row.GetAttribute("left"), [ref]$l)) { continue }
            $t2 = 0; [void][int]::TryParse($row.GetAttribute("top"), [ref]$t2)
            $need = 0; $who = ''
            foreach ($call in $row.SelectNodes("*")) {
                if ($tplWidth.ContainsKey($call.LocalName) -and $tplWidth[$call.LocalName] -gt $need) {
                    $need = $tplWidth[$call.LocalName]; $who = $call.LocalName
                }
            }
            $rk = "$($row.GetAttribute('left'))/$($row.GetAttribute('top'))/$($row.GetAttribute('width'))/$($row.GetAttribute('height'))"
            if ($need -gt 0) { $rows += [pscustomobject]@{ L = $l; T = $t2; R = $l + $need; Tpl = $who; Rect = $rk; Hidden = ($row.GetAttribute('visible') -eq 'false') } }
        }
        for ($i = 0; $i -lt $rows.Count; $i++) {
            for ($j = $i + 1; $j -lt $rows.Count; $j++) {
                if ($rows[$i].T -eq $rows[$j].T -and $rows[$i].L -lt $rows[$j].R -and $rows[$j].L -lt $rows[$i].R) {
                    # A TWIN is not a neighbouring row: the pair stands on ONE rect on purpose and
                    # exactly one of them is ever visible, so overlap is the mechanism here and not
                    # the bug. Same sentence V40 already exempts a tab pane by, and measured the same
                    # way - the shared rect plus exactly one half authored hidden, never a name
                    # (SPEC V262d, I123b, V371b). Two VISIBLE rows laid over each other still fail.
                    if ($rows[$i].Rect -eq $rows[$j].Rect -and ($rows[$i].Hidden -xor $rows[$j].Hidden)) { $twinByRect++; continue }
                    $overlaps += "$($f.Name) top=$($rows[$i].T): <$($rows[$i].Tpl)> spans $($rows[$i].L)..$($rows[$i].R) and <$($rows[$j].Tpl)> starts at $($rows[$j].L)"
                }
            }
        }
    }
}
if ($overlaps) { foreach ($o in ($overlaps | Sort-Object -Unique)) { Fail "V37 $o" } }
else { Pass "V37 no sibling row draws over its neighbour ($twinByRect twin row(s) exempted by shared rect)" }

# ---- V40: and two BOXES on one tab must not overlap either (SPEC V40, T112) --------
# V37 measures a row against its neighbour inside one box; nothing ever measured box against
# box, though the tabs have had side-by-side columns since T30 (COMBAT ends at x=650, the right
# column starts at 670). T111 drops VIRTUES into the 280px gutter between ARMOR and that column,
# where a 10px slip would draw one box over another and still pass green.
# Boxes without all four numbers (align="client", flow layouts) are skipped: they have no
# rectangle to compare.
$boxHits = @()
$boxCount = 0
$paneByRect = 0
foreach ($f in $files) {
    $xml = Doc $f.FullName
    foreach ($sb in $xml.SelectNodes("//scrollBox")) {
        $boxes = @()
        # A tab pane is not a neighbouring box: the panes stack on ONE rect by design and exactly
        # one is ever visible (SPEC I32, proved in Firecast at T521). Overlap is the mechanism
        # here, not the bug.
        #
        # They were told apart by holding an <import> until the 90th round, when the three Numina
        # panes landed with their content INLINE - no import to hold, because the book left the
        # .lfm in SPEC I24 and the reason to split the file went with it. Three legal panes would
        # have reddened this check on code that is right (SPEC B7). The import was only ever a
        # PROXY for the sentence above; sharing the exact rect with another sibling IS that
        # sentence, so it is measured now instead of assumed (SPEC V262d).
        #
        # A strip is not a pane by either door - numStrip and vampStrip carry their own rect - so
        # they go on being weighed against the panes, which is B41 coming through the geometry
        # door (SPEC V221).
        $paneRect = @{}
        foreach ($bx in $sb.SelectNodes("layout")) {
            $k = "$($bx.GetAttribute('left'))/$($bx.GetAttribute('top'))/$($bx.GetAttribute('width'))/$($bx.GetAttribute('height'))"
            if ($k -notmatch '^\d+/\d+/\d+/\d+$') { continue }
            if ($paneRect.ContainsKey($k)) { $paneRect[$k]++ } else { $paneRect[$k] = 1 }
        }
        foreach ($bx in $sb.SelectNodes("layout")) {
            if ($bx.SelectSingleNode("import")) { continue }
            $pk = "$($bx.GetAttribute('left'))/$($bx.GetAttribute('top'))/$($bx.GetAttribute('width'))/$($bx.GetAttribute('height'))"
            if ($paneRect.ContainsKey($pk) -and $paneRect[$pk] -gt 1) { $paneByRect++; continue }
            $bl = 0; $bt = 0; $bw = 0; $bh = 0
            if (-not ([int]::TryParse($bx.GetAttribute("left"), [ref]$bl) -and
                      [int]::TryParse($bx.GetAttribute("top"), [ref]$bt) -and
                      [int]::TryParse($bx.GetAttribute("width"), [ref]$bw) -and
                      [int]::TryParse($bx.GetAttribute("height"), [ref]$bh))) { continue }
            $ttl = $bx.SelectSingleNode("label")
            $nm = if ($ttl) { $ttl.GetAttribute("text") } else { "box" }
            $boxes += [pscustomobject]@{ L = $bl; T = $bt; R = $bl + $bw; B = $bt + $bh; Name = $nm }
        }
        $boxCount += $boxes.Count
        for ($i = 0; $i -lt $boxes.Count; $i++) {
            for ($j = $i + 1; $j -lt $boxes.Count; $j++) {
                $p = $boxes[$i]; $q = $boxes[$j]
                if ($p.L -lt $q.R -and $q.L -lt $p.R -and $p.T -lt $q.B -and $q.T -lt $p.B) {
                    $boxHits += "$($f.Name): '$($p.Name)' ($($p.L),$($p.T))-($($p.R),$($p.B)) overlaps '$($q.Name)' ($($q.L),$($q.T))-($($q.R),$($q.B))"
                }
            }
        }
    }
}
if ($boxHits) { foreach ($o in ($boxHits | Sort-Object -Unique)) { Fail "V40 $o" } }
elseif ($boxCount -eq 0) { Fail "V40 measured zero boxes - the check reads nothing (SPEC V20)" }
else { Pass "V40 none of the $boxCount positioned boxes draws over its neighbour" }

# ---- V38: a checkBox with text needs the same width check as a label (SPEC B17) ---
# V16 only ever looked at //label. "Wounded (-2)" is "Ferido Gravemente (-2)" in pt, ~143px,
# and it sat in a 125px checkBox from T3 until this was found by hand.
$narrowChecks = @()
foreach ($f in $files) {
    $xml = Doc $f.FullName
    foreach ($n in $xml.SelectNodes("//checkBox[@text][@width]")) {
        $txt = $n.GetAttribute("text").Trim()
        if (-not $txt -or $txt -match '\$\(') { continue }
        $w = 0; if (-not [int]::TryParse($n.GetAttribute("width"), [ref]$w)) { continue }
        $need = NeededPx $txt
        if ($need -gt $w) { $narrowChecks += "$($f.Name): checkBox '$txt' (pt '$($ptVal[$txt])') needs ~${need}px, width=${w}px" }
    }
}
if ($narrowChecks) { foreach ($c in $narrowChecks) { Fail "V38 $c" } }
else { Pass "V38 every checkBox fits its longest translation" }

# ---- V39: the health level table (SPEC C, V39) ------------------------------------
# Ten explicit lists, LITERAL from the user - 4 skips Crippled instead of trimming from the top,
# so there is no rule to derive and nothing to cross-check against except the shape itself.
$rootPath = Join-Path $dir "WoD20th.lfm"
$root = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($rootPath))
$canonHealth = @('Bruised','Hurt (-1)','Injured (-1)','Wounded (-2)','Mauled (-2)','Crippled (-5)','Incapacitated')
$lvlBlock = [regex]::Match($root, 'HEALTH_LEVELS\s*=\s*\{(.*?)\n\s*\};', 'Singleline')
if (-not $lvlBlock.Success) { Fail "V39 HEALTH_LEVELS not found on the root form" }
else {
    $seen = @{}
    foreach ($m in [regex]::Matches($lvlBlock.Groups[1].Value, '\[(\d+)\]\s*=\s*\{([^}]*)\}')) {
        $n = [int]$m.Groups[1].Value
        $items = @([regex]::Matches($m.Groups[2].Value, '"([^"]*)"') | ForEach-Object { $_.Groups[1].Value })
        $seen[$n] = $items
    }
    $bad = @()
    foreach ($n in 1..10) {
        if (-not $seen.ContainsKey($n)) { $bad += "no list for $n levels"; continue }
        $items = $seen[$n]
        if ($items.Count -ne $n) { $bad += "list for $n has $($items.Count) entries" }
        if ($items[-1] -ne 'Incapacitated') { $bad += "list for $n ends in '$($items[-1])', not Incapacitated" }
        foreach ($it in $items) { if ($canonHealth -notcontains $it) { $bad += "list for $n has unknown level '$it'" } }
    }
    if ($bad) { foreach ($b in ($bad | Sort-Object -Unique)) { Fail "V39 $b" } }
    else { Pass "V39 all 10 health level lists are well formed" }

    # The renderer writes these labels itself, so its pt table is the only thing that translates
    # them - the WoD20.6 map never sees them. Same check V22 runs on that map.
    $ptBlock = [regex]::Match($root, 'HEALTH_PT\s*=\s*\{(.*?)\n\s*\};', 'Singleline')
    if (-not $ptBlock.Success) { Fail "V39 HEALTH_PT not found - the labels could never translate" }
    else {
        $htDrift = @()
        foreach ($m in [regex]::Matches($ptBlock.Groups[1].Value, '\["([^"]+)"\]\s*=\s*"([^"]*)"')) {
            $k = $m.Groups[1].Value; $v = $m.Groups[2].Value
            if (-not $ptVal.ContainsKey($k)) { $htDrift += "'$k' not in [pt]" }
            elseif ($ptVal[$k] -cne $v) { $htDrift += "'$k': table='$v' lang='$($ptVal[$k])'" }
        }
        foreach ($c in $canonHealth) {
            if ($ptBlock.Groups[1].Value -notmatch [regex]::Escape("[""$c""]")) { $htDrift += "'$c' has no pt entry" }
        }
        if ($htDrift) { foreach ($d in ($htDrift | Sort-Object -Unique)) { Fail "V39 HEALTH_PT $d" } }
        else { Pass "V39 HEALTH_PT covers all 7 levels and matches localization.lang" }
    }

    # Read WoD20.6 here rather than using $hh6: that variable is not assigned until the V12 block
    # further down, so referencing it at this point would silently test an empty string.
    $hh6Early = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20.6.lfm")))
    if ($hh6Early -match 'string\.sub\(dynName, 1, 3\) == "dyn"') { Pass "V31/V39 the traversal skips Lua-owned (dyn*) text" }
    else { Fail "V31/V39 WoD20.6 traversal has no dyn* guard - it would restore stale health labels" }
}

# ---- V41 - V43: the four-symbol damage mark (SPEC C 6th round) -------------------
# The health box stopped being a checkBox: it holds " ", "/", "X" or "*", cycles on click and
# resets on right-click. Three things can rot silently - a second copy of the list drifting from
# the first, a mark box wired for click but not for right-click, and the normalisation that keeps
# a pre-6th-round boolean from rendering as "true" in the box.
$markMatches = [regex]::Matches($root, 'HEALTH_MARKS\s*=\s*\{([^}]*)\}')
if ($markMatches.Count -ne 1) { Fail "V41 HEALTH_MARKS declared $($markMatches.Count)x on the root form, expected exactly 1" }
else {
    $marks = @([regex]::Matches($markMatches[0].Groups[1].Value, '"([^"]*)"') | ForEach-Object { $_.Groups[1].Value })
    $wantMarks = @(' ', '/', 'X', '*')
    $sameOrder = $marks.Count -eq $wantMarks.Count
    if ($sameOrder) { for ($i = 0; $i -lt $marks.Count; $i++) { if ($marks[$i] -cne $wantMarks[$i]) { $sameOrder = $false } } }
    if ($sameOrder) { Pass "V41 HEALTH_MARKS = the four symbols, space first, in cycle order" }
    else { Fail "V41 HEALTH_MARKS = {'$($marks -join "','")'} - expected {' ','/','X','*'} in that order" }
}
$strayMarks = @($files | Where-Object { $_.Name -ne 'WoD20th.lfm' } |
                Where-Object { [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($_.FullName)) -match 'HEALTH_MARKS\s*=' })
if ($strayMarks) { foreach ($s in $strayMarks) { Fail "V41 $($s.Name) declares its own HEALTH_MARKS - the two tabs would drift" } }
else { Pass "V41 the mark list lives only on the root form" }

# V42: an unknown value (boolean, nil, junk) must be written back as the empty mark by the
# renderer, or the box shows "true" until someone clicks it.
if ($root -match 'markIndex\(mark\)\s*==\s*1' -and $root -match 'setField\("health_"\s*\.\.\s*i, HEALTH_MARKS\[1\]\)') {
    Pass "V42 renderHealthTrack normalises an unknown mark to the empty one"
} else {
    Fail "V42 no normalisation in renderHealthTrack - a pre-6th-round boolean would render as 'true'"
}

# V43: every mark box answers BOTH buttons. Checked on the template, which is where the box is
# declared once per tab; the ten rows per tab are template calls and are counted by V5.
$markBoxes = 0
foreach ($f in $files) {
    $xml = Doc $f.FullName
    foreach ($t in $xml.SelectNodes("//template")) {
        $lbl = $t.SelectSingleNode(".//label[@field]")
        if ($null -eq $lbl -or $lbl.GetAttribute("field") -notmatch '^health_') { continue }
        $markBoxes++
        $box = $lbl.ParentNode
        $where = "$($f.Name) <template $($t.GetAttribute('name'))>"
        if ($box.GetAttribute("onClick") -notmatch 'cycleHealthMark') { Fail "V43 $where mark box has no cycleHealthMark on onClick" }
        if ($box.GetAttribute("onMenu") -notmatch 'healHealthMark') { Fail "V43 $where mark box has no healHealthMark on onMenu - right-click would do nothing" }
        if ($box.GetAttribute("hitTest") -ne 'true') { Fail "V43 $where mark box has no hitTest - it would never see a click" }
    }
}
if ($markBoxes -eq 0) { Fail "V43 no health mark box found - the check reads nothing (SPEC V20)" }
else { Pass "V43 all $markBoxes mark boxes answer click and right-click" }

# ---- V44 + V45: clearing a mark heals the BOTTOM of the track (SPEC C 7th round) --
# The click the player makes and the row that empties are deliberately different rows, so the
# two ways to get it wrong are: cycleHealthMark emptying the row it was handed, and the heal
# scanning rows the player cannot see (a mark left above a shortened track eats the click).
function LuaFn($txt, $name) {
    $m = [regex]::Match($txt, "(?ms)function\s+$name\s*\(.*?\r?\n\t\t\tend;")
    if ($m.Success) { return $m.Value }
    return ''
}
$cycFn  = LuaFn $root 'cycleHealthMark'
$healFn = LuaFn $root 'healHealthMark'
$rendFn = LuaFn $root 'renderHealthTrack'

if (-not $cycFn) { Fail "V44 cycleHealthMark not found on the root form" }
elseif ($cycFn -match 'HEALTH_MARKS\[1\]') { Fail "V44 cycleHealthMark can still write the empty mark - it would clear the clicked row instead of healing" }
elseif ($cycFn -notmatch 'healHealthMark\(') { Fail "V44 cycleHealthMark never calls healHealthMark - the fourth click would do nothing" }
else { Pass "V44 cycleHealthMark heals instead of emptying the clicked row" }

if (-not $healFn) { Fail "V44 healHealthMark not found on the root form" }
else {
    if ($healFn -notmatch 'for\s+\w+\s*=\s*\w+\s*,\s*1\s*,\s*-1') { Fail "V44 healHealthMark does not scan the track downwards - it would clear the topmost mark, not the last" }
    else { Pass "V44 healHealthMark clears the bottom-most mark" }

    if ($healFn -notmatch 'healthLevels') { Fail "V45 healHealthMark is not bounded by healthLevels - a mark on a hidden row would eat the click" }
    else { Pass "V45 healHealthMark only looks at rows the player can see" }
}

if ($rendFn -match 'healHealthMark\(') { Fail "V45 renderHealthTrack calls healHealthMark - normalising an old value is not healing" }
else { Pass "V45 the renderer never heals" }

# ---- V46 + V47: marks stay grouped, worst first (SPEC C 8th round) ---------------
# Upgrading a mark in the middle of the stack leaves the track out of order, and a new mark
# started on a far-down empty row leaves a gap. Both are fixed by regrouping, so the checks are:
# the routine exists, it walks severity downwards, it is called from the two paths that can
# break the order, and it takes its severity order from HEALTH_MARKS rather than a copy.
$regroupFn = LuaFn $root 'regroupHealthMarks'
if (-not $regroupFn) { Fail "V46 regroupHealthMarks not found on the root form" }
else {
    if ($regroupFn -notmatch 'for\s+\w+\s*=\s*#HEALTH_MARKS\s*,\s*2\s*,\s*-1') { Fail "V46 regroupHealthMarks does not walk HEALTH_MARKS from the worst down - the grouping order would not be severity" }
    else { Pass "V46 regroupHealthMarks lays marks out worst-first" }

    if ($regroupFn -notmatch 'healthLevels') { Fail "V47 regroupHealthMarks is not bounded by healthLevels - it would shuffle rows the player cannot see" }
    else { Pass "V47 regroupHealthMarks only touches visible rows" }

    if ($regroupFn -match '\{\s*"') { Fail "V47 regroupHealthMarks declares its own symbol list - it must read HEALTH_MARKS (SPEC V41)" }
    else { Pass "V47 regroupHealthMarks reads the one symbol list" }
}
if ($cycFn -notmatch 'regroupHealthMarks\(') { Fail "V46 cycleHealthMark never regroups - an upgrade in the middle of the stack would stay out of order" }
else { Pass "V46 cycleHealthMark regroups after changing a mark" }
if ($rendFn -notmatch 'regroupHealthMarks\(') { Fail "V46 renderHealthTrack never regroups - a sheet marked before the 8th round would open out of order" }
else { Pass "V46 the renderer groups what it finds on an older sheet" }

# ---- V27: a section title spanning its box must actually be centred on it -----
# `horzTextAlign="center"` centres inside the label, not inside the box, so a title with
# left="5" and width=<box width> sits 5px right of centre (SPEC B.13). Column headers are
# narrow relative to their box, so only labels spanning >=80% of the box are section titles.
#
# The ruler is SYMMETRY, not left=0 (103rd round, SPEC T618). Until I73 the title spanned the
# whole box, so "left=0 and width=box" was the cheapest way to say centred - but with a 20px
# margin on all four sides the title CANNOT start at 0 without breaking V280a, and left=20 in
# a 340px box with width=300 is dead centre. Measuring the two margins against each other says
# what the check was always for and stops being a literal the next margin change has to edit -
# the same move T618 made on the 1270 ruler of V262c/V267a.
$offCentre = @()
foreach ($f in $files) {
    $xml = Doc $f.FullName
    foreach ($box in $xml.SelectNodes("//layout")) {
        $bw = 0; if (-not [int]::TryParse($box.GetAttribute("width"), [ref]$bw)) { continue }
        foreach ($lb in $box.SelectNodes("label")) {
            if ($lb.GetAttribute("horzTextAlign") -ne "center") { continue }
            if ($lb.HasAttribute("rotationAngle")) { continue }
            $w = 0; if (-not [int]::TryParse($lb.GetAttribute("width"), [ref]$w)) { continue }
            if ($w -lt ($bw * 0.8)) { continue }
            $lf = 0; [void][int]::TryParse($lb.GetAttribute("left"), [ref]$lf)
            $rt = $bw - $lf - $w
            if ($lf -ne $rt) { $offCentre += "$($f.Name): '$($lb.GetAttribute('text'))' leaves $lf on the left and $rt on the right in a ${bw}px box - a centred title has to sit on equal margins (SPEC V27, I73)" }
        }
    }
}
if ($offCentre) { foreach ($o in $offCentre) { Fail "V27 $o" } } else { Pass "V27 every section title is centred on its box" }

# ---- V77: a section title is not glued to the box border (SPEC 26th round) --------
# The outline is 3px thick (THEME_STROKE), so a title at top=0 sits on the border itself.
# 44 titles were shifted to 4 in one sweep; without a check the next box copied from an
# older file goes straight back to 0, exactly the drift V68 exists for on the corner.
# The Progress tab is out by request - the user asked for every tab but that one.
$titleTop = @()
$titleCount = 0
foreach ($f in $files) {
    if ($f.Name -eq 'WoD20.9.lfm') { continue }
    foreach ($box in (Doc $f.FullName).SelectNodes("//layout[rectangle[@color='black']]")) {
        $ttl = $box.SelectSingleNode("label[@horzTextAlign='center']")
        if ($null -eq $ttl) { continue }
        $t = 0
        if (-not [int]::TryParse($ttl.GetAttribute("top"), [ref]$t)) { continue }
        $titleCount++
        if ($t -lt 4) { $titleTop += "$($f.Name): '$($ttl.GetAttribute('text'))' sits at top=$t - the title would touch the 3px outline" }
    }
}
if ($titleCount -eq 0) { Fail "V77 no section title found - the check reads nothing (SPEC V20)" }
elseif ($titleTop) { foreach ($t in $titleTop) { Fail "V77 $t" } }
else { Pass "V77 all $titleCount section titles clear the box border" }

# ---- V68: and every section box carries the SAME corner ---------------------------
# The corner detail - cornerType over a radius - lives in the XML rather than in the palettes
# (SPEC V66), so it is authored 53 times and nothing kept those 53 in step. A box copied from
# an older file would sit among its siblings with a different corner and read as a mistake.
# Exactly the drift that produced B18, on another property.
#
# The two deliberate outliers are excluded the same way V48 excludes them: they are not black.
# The avatar frame is DimGray and keeps its own radius; the health mark box is transparent.
#
# The third declared exception is the strip floor, and it is the FIRST that is black: the
# other two fall out for free because the XPath below only takes black, and this one does
# not. It is cut by CONSTRUCTION rather than by name, and emphatically not by "authors no
# corner" - that last one would be a hole wide enough for B18 to walk straight back through,
# since any box that simply forgot its radius would then be excused in silence. A strip floor
# is an align="client" rectangle whose parent layout also holds clickable rectangles;
# measured across the sheet that matches ONE layout (tabStrip) and no section box, because
# the health track's clickable rectangles are not siblings of their box background.
# The corner is not dropped, it changes owner: V229 requires the floor to author none
# (SPEC I33, user request 2026-08-22).
#
# EMENDED in the 117th round and NOT renumbered (SPEC I89e): what this measures is the AUTHORED
# corner - the floor every era without a `boxCorner` falls back to - and no longer what is on
# SCREEN, because Modern Nights rewrites the corner at runtime (SPEC I89a). What it buys is
# still real: a box copied from an older file lands among its siblings with a different corner
# and reddens here. What it no longer promises is ONE corner across the four eras.
$corners = @{}
$boxSeen = 0
$floorCut = 0
foreach ($f in $files) {
    foreach ($r in (Doc $f.FullName).SelectNodes("//rectangle[@color='black']")) {
        if ($r.GetAttribute("align") -eq 'client' -and $r.ParentNode.SelectSingleNode("rectangle[@onClick]")) { $floorCut++; continue }
        $boxSeen++
        $key = "{0}|{1}|{2}" -f $r.GetAttribute("cornerType"), $r.GetAttribute("xradius"), $r.GetAttribute("yradius")
        if (-not $corners.ContainsKey($key)) { $corners[$key] = New-Object System.Collections.Generic.HashSet[string] }
        [void]$corners[$key].Add($f.Name)
    }
}
if ($floorCut -ne 1) { Fail "V68 the strip-floor cut matched $floorCut rectangle(s), expected exactly 1 - a wider cut is a hole B18 walks back through (SPEC V209)" }
elseif ($boxSeen -lt 60) { Fail "V68 only $boxSeen black section box(es) were read, expected at least 60 - this check is covering less than the sheet has (SPEC V209)" }
elseif ($corners.Count -eq 0) { Fail "V68 no black section box found - the check has nothing to measure" }
elseif ($corners.Count -gt 1) {
    foreach ($k in $corners.Keys) {
        $p = $k -split '\|'
        Fail "V68 section boxes with cornerType='$($p[0])' radius $($p[1])/$($p[2]) in $(($corners[$k] | Sort-Object) -join ', ') - the corner must match across every box"
    }
} else {
    $p = ($corners.Keys | Select-Object -First 1) -split '\|'
    Pass "V68 all $boxSeen section boxes AUTHOR cornerType='$($p[0])' radius $($p[1])/$($p[2]) - the floor every era without a boxCorner falls back to - and the one strip floor is cut out by construction"
}


# ---- V69: the Main tab closes on ONE line, and the map says which ------------------
# V40 only asks that two boxes never overlap, so growing VIRTUES and shrinking the
# HUMANITY/WILLPOWER box under it by the wrong amount would leave the middle column
# ending short and still pass green - the 18th round moves both, and this keeps it honest.
#
# The ruler has moved twice. It was the BACKGROUNDS box beside it until the 46th round,
# then the literal y=810 the grid comment declared. The 50th round raised the whole bottom
# row and that literal had to be hand-edited again - a gate measuring the round instead of
# the rule. So it is READ from the map comment now and cross-checked against every box that
# is supposed to close on it, the avatar included: the comment cannot rot either.
#
# HEALTH is the declared exception: its authored height is the ten-row case and the renderer
# shrinks it to the chosen track (V49), so it does not close on the line - a corner taken in
# the 11th round, and measured here rather than skipped.
#
# The exception changed SIDES in the 112th round and the sign is the whole record of it: it
# hung BELOW the line from the 11th round until then, and the 30px the tail lost (SPEC I82b,
# HEALTH_BOX_PAD 79 -> 49) put it ABOVE. Two rounds move this literal for two different
# reasons - T682 raised the line to 852 and T683 shortened the box to 823 - so a pass that
# leaves it stale fails here rather than somewhere quieter (SPEC V69, V49).
# -29 until the 125th round, when the bottom line went 852 -> 878 to seat the ROAD box
# (SPEC I99i, I99j). HEALTH did NOT move - it is the declared exception that does not close
# on the line - so the gap it sits at grew by exactly the 26 the line rose: -29 - 26 = -55.
# The literal is the whole point: it moves only with a round behind it, never on its own.
# One vertical ruler for the whole Main tab since T859 (SPEC I124, V370). Declared here
# because V146 reads it too - the SPECIALTIES box asks whether its own rows still fit down it,
# and two literals for one ruler is how the tab grew three of them in the first place.
$MAIN_ROW_PITCH = 25
# -3 since T861. The line went 853 -> 928 in T858 (three more ability rows), 928 -> 895 in
# T859 (one 25px ruler for the tab, SPEC I124, V370) and 895 -> 891 in T861 (VIRTUES opening
# on its own title hairline, SPEC V374, I125c). HEALTH moved with the bottom row all three
# times and its own height never changed: its track is 27px and follows no ruler, so the gap
# it sits at is whatever the three moves left. Three rounds, three reasons, one literal.
$HEALTH_TEN_ROW_OVERHANG = 0    # 561 + 327 = 888, and since T862 the tab closes on 888 too (SPEC V376, I126h)
$mainRawForMap = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20.1.lfm")))
$mapY = [regex]::Match($mainRawForMap, 'Everything closes on x=\d+ / y=(\d+)')
$mainDocV69 = Doc (Join-Path $dir "WoD20.1.lfm")
$mainBottom = @{}
foreach ($bx in $mainDocV69.SelectNodes("//scrollBox/layout")) {
    $bt = 0; $bh = 0
    if (-not ([int]::TryParse($bx.GetAttribute("top"), [ref]$bt) -and
              [int]::TryParse($bx.GetAttribute("height"), [ref]$bh))) { continue }
    foreach ($lb in $bx.SelectNodes("label")) {
        $t = $lb.GetAttribute("text")
        if ($t) { $mainBottom[$t] = $bt + $bh }
    }
}
$avatarImg = @($mainDocV69.SelectNodes("//scrollBox/image[@field='avatar']"))[0]
$avatarBottom = if ($avatarImg) { [int]$avatarImg.GetAttribute("top") + [int]$avatarImg.GetAttribute("height") } else { -1 }
# V376 SUPERSEDES V69 (SPEC V376, I126h, T865, T862). V69 read THREE boxes closing on one y
# with HEALTH carrying the one declared exception; since the reflow the bottom row is FOUR
# boxes - the avatar, VIRTUES, ROAD and HEALTH - and HEALTH is the box that SETS the line
# rather than the one let off it. So the overhang is ZERO and the literal STAYS at zero: it
# is what proves the exception died, and deleting it would leave a rule that can no longer
# fail (SPEC V20). SPECIALTIES left this row entirely - it is the fourth box of the GRID row
# now (V375), so a check still looking for it down here would be measuring the old tab.
# VIRTUES left this list in T904 and that is the whole of the change: it stopped being a bottom
# box the moment ROAD went UNDER it, and asking a box in the middle of a stack to close on the
# tab's floor would redden correct code (SPEC I134m, B106). ROAD closed that column until T914.
#
# T908 put SPECIALTIES back on the bottom row and it does NOT close the line either: the owner
# asked for its height to be its own content plus a foot, so it ends at 872 (SPEC I136c, I136d,
# Q47). Which box is let off is NOT typed here - it is READ from the V393 roster, so adding a
# box to V393 takes it out of this list by construction, and a bottom-row box that is off the
# line and in NEITHER list still reddens. That last half is the point: a box that quietly stops
# closing the line leaves no other trace (SPEC V393, B112, B119).
$closers = @("HEALTH")
# ROAD CAME BACK to the pinned roster in T914, and the two lists stay exact complements: a box
# PINNED to its own content cannot also be a box that CLOSES the tab line, because the line is a
# total and the foot is what pays for it. T913 put ROAD on the closing side because the VIRTUES +
# ROAD stack is two boxes losing 10 each against HEALTH losing 10, and someone had to pay the
# missing 10 - ROAD paid it with a 25px foot. The owner asked for that foot to be the 15 every
# other box takes now, so ROAD ends at 856 and HEALTH plus the avatar close the line at 866. The
# rule of B131 did not change shape - only which side ROAD stands on (SPEC B131, I138c, Q49).
$V393_PINNED = @("VIRTUES", "ROAD")
# SPECIALTIES left the PINNED roster on 2026-09-02 and did NOT become a closer: the owner asked it
# to end where ROAD ends (856), which is neither the tab line nor its own content plus a foot
# (SPEC V402, I139a, I139m, Q49). The two rosters are read as a UNION just below, so the promise
# V393 and V376 share - every bottom-row box is a closer, or is named in a roster - is untouched,
# and a box that quietly stops closing the line while standing in NEITHER list still reddens.
$V402_ALIGNED = @("SPECIALTIES")
# The AVATAR left the closing side on 2026-09-04: the owner asked it to end where ROAD ends (856),
# which is V402's answer wearing another face, and V415 below is what pins it there (SPEC V415,
# I144a, T939, Q63). It is named here rather than inferred because it is the one bottom-row thing
# that is NOT a `<layout>` box - the sweep below cannot see it, so a roster that left it out would
# be a roster that shrank without anybody noticing.
$V415_AVATAR = @("avatar")
# The zero-guard MOVED with it, and this is the whole reason V376 had to be re-formed in the same
# commit rather than left alone (SPEC Q63, B7, B145). Closers fell to ONE. An equality over one
# element is true for whatever height that element happens to have, so the old guard - count the
# closers - would have gone on passing while measuring nothing at all. What cannot go vacuous is
# the ROSTER: every bottom-row thing is a closer or is NAMED, so if the names drain away the
# sweep below has nothing left to excuse and this floor is what says so. THREE is a floor and not
# an equality - four are named today (VIRTUES, ROAD, SPECIALTIES, avatar) and a fifth is welcome.
$v376Roster = @($V393_PINNED) + @($V402_ALIGNED) + @($V415_AVATAR)
$missing = @($closers | Where-Object { -not $mainBottom.ContainsKey($_) })
if (-not $mapY.Success) { Fail "V376 the tab's grid comment no longer says where the tab closes - the ruler would be a literal in the gate again (SPEC V20)" }
elseif ($missing.Count -gt 0) { Fail "V376 $($missing -join '/') not found on WoD20.1 - the check measured nothing (SPEC V20)" }
elseif ($avatarBottom -lt 0) { Fail "V376 the avatar image was not found on WoD20.1 - it closes the left column and this rule has measured it since the 50th round" }
else {
    $line = [int]$mapY.Groups[1].Value
    $off = @()
    foreach ($c in $closers) { if ($mainBottom[$c] -ne $line) { $off += "$c ends at $($mainBottom[$c])" } }
    # Anything else standing on the bottom row is either a closer or a box V393 PINS to its own
    # content. A third case does not exist, and it is the third case this leg is for.
    $unaccounted = @()
    foreach ($bx376 in $mainDocV69.SelectNodes("//scrollBox/layout")) {
        $bt376 = 0; $bh376 = 0
        if (-not ([int]::TryParse($bx376.GetAttribute("top"), [ref]$bt376) -and
                  [int]::TryParse($bx376.GetAttribute("height"), [ref]$bh376))) { continue }
        if ($bt376 -lt 500) { continue }
        if (($bt376 + $bh376) -eq $line) { continue }
        $ttl376 = $bx376.SelectSingleNode("label[@text]")
        $nm376 = if ($null -ne $ttl376) { $ttl376.GetAttribute("text") } else { "an untitled box at top=$bt376" }
        if ($closers -contains $nm376) { continue }
        if ($V393_PINNED -contains $nm376 -or $V402_ALIGNED -contains $nm376) { continue }
        $unaccounted += "$nm376 ends at $($bt376 + $bh376)"
    }
    if ($v376Roster.Count -lt 3) { Fail "V376 the bottom row names only $($v376Roster.Count) box(es) off the closing line and three were measured there on 2026-09-04 - closers fell to one when the avatar left, so the roster is the only half of this rule that can still go empty, and an empty one excuses the whole row (SPEC Q63, V20, B7)" }
    elseif ($off.Count -gt 0) { Fail "V376 the map says the tab closes on y=$line but $($off -join ', ') - the Main bottom row must close on ONE line" }
    elseif ($unaccounted.Count -gt 0) { Fail "V376 $($unaccounted -join ', ') - on the bottom row of Main, off the y=$line line, and named in no V393 pinned roster (SPEC V393, I136d)" }
    elseif (($mainBottom["HEALTH"] - $line) -ne $HEALTH_TEN_ROW_OVERHANG) { Fail "V376 HEALTH sits $($mainBottom['HEALTH'] - $line)px off the line, not the $HEALTH_TEN_ROW_OVERHANG px declared - below the line its sign FLIPS and it starts overhanging (SPEC V49, I126h)" }
    else { Pass "V376 HEALTH closes the tab on y=$line and the $($v376Roster.Count) box(es) off it are each named - $($V393_PINNED -join ', ') pinned by V393, $($V402_ALIGNED -join ', ') aligned to ROAD by V402, $($V415_AVATAR -join ', ') pinned to ROAD by V415 - and HEALTH's declared overhang is $HEALTH_TEN_ROW_OVERHANG" }
}

# ---- V393: the pinned boxes of the Main bottom row are their own content -------------
# SPEC V393, I134l, I136c, I138c, Q47, Q49. Three boxes are held to LAST CONTENT plus a BOX_PAD_Y foot and no
# more: VIRTUES and ROAD, the stack the TALENTS band has carried since T904, and SPECIALTIES,
# which the owner took back out of the grid row in T908 and asked to fit its content. BY NAME
# and not every box on the tab: HEALTH is authored at its ten-row case (V49), so a general
# minimum-foot ruler would be born red against correct code (SPEC B106).
#
# V376 reads this same roster to know which bottom-row box may miss the closing line, so the
# two cannot drift: a box that leaves this list has V376 demanding the floor of it in the same
# run, and a box that joins it stops being demanded. One roster, two readers (SPEC B70).
$v393Bad = @()
$v393Seen = 0
foreach ($nm393 in $V393_PINNED) {
    $bx393 = $null
    foreach ($cand393 in $mainDocV69.SelectNodes("//scrollBox/layout")) {
        foreach ($l393 in $cand393.SelectNodes("label")) {
            if ($l393.GetAttribute("text") -eq $nm393) { $bx393 = $cand393; break }
        }
        if ($null -ne $bx393) { break }
    }
    if ($null -eq $bx393) {
        $v393Bad += "$nm393 was not found on WoD20.1 - a pinned box the sweep cannot see is a rule measuring nothing (SPEC V20, V209)"
        continue
    }
    $h393 = 0
    [void][int]::TryParse($bx393.GetAttribute("height"), [ref]$h393)
    $content393 = 0
    foreach ($ch393 in $bx393.ChildNodes) {
        if ($ch393.NodeType -ne [System.Xml.XmlNodeType]::Element) { continue }
        $ct393 = 0; $chh393 = 0
        if ([int]::TryParse($ch393.GetAttribute("top"), [ref]$ct393) -and
            [int]::TryParse($ch393.GetAttribute("height"), [ref]$chh393)) {
            if (($ct393 + $chh393) -gt $content393) { $content393 = $ct393 + $chh393 }
        }
    }
    if ($content393 -le 0) {
        $v393Bad += "$nm393 has no child carrying a top and a height - the foot would be measured against zero (SPEC V209)"
        continue
    }
    $v393Seen++
    $foot393 = $h393 - $content393
    if ($foot393 -ne $BOX_PAD_Y) {
        $v393Bad += "$nm393 is ${h393}px tall over content that closes at $content393 - a ${foot393}px foot where a pinned box takes exactly $BOX_PAD_Y (SPEC V280a, V393, I137c)"
    }
}
if ($v393Seen -lt $V393_PINNED.Count) { $v393Bad += "only $v393Seen of the $($V393_PINNED.Count) pinned boxes were measured - the roster outlived the boxes (SPEC V20, V209)" }
if ($v393Bad) { foreach ($b393 in $v393Bad) { Fail "V393 $b393" } }
else { Pass "V393 $($V393_PINNED -join ', ') are each their own content plus a ${BOX_PAD_Y}px foot - the roster V376 reads to know who is off the closing line" }

# ---- V402: SPECIALTIES closes where ROAD closes ---------------------------------------
# SPEC V402, I139a, I139m, Q49, user 2026-09-02 item 1. The owner asked for the two bottom-row
# boxes that are NOT the tab line to end together, so SPECIALTIES gained a 29px foot and left the
# V393 foot roster in the same commit - a 29px foot is not "content plus BOX_PAD_Y", and pretending
# it was would have cost V393 the only thing it promises.
#
# NEITHER y is typed here. Both are read off the two boxes, so the day ROAD moves again SPECIALTIES
# has to move with it or this reddens - which is the whole of what the owner asked for. A literal
# would be the second owner of 856 and the first to age (SPEC B112, B70).
$v402Bad = @()
if ($V402_ALIGNED.Count -eq 0) { $v402Bad += "the aligned roster is empty - this rule would certify any height at all (SPEC V20, V209)" }
elseif (-not $mainBottom.ContainsKey("ROAD")) { $v402Bad += "the ROAD box was not found on WoD20.1 - the y this rule aligns to cannot be read (SPEC V20)" }
else {
    foreach ($nm402 in $V402_ALIGNED) {
        if (-not $mainBottom.ContainsKey($nm402)) {
            $v402Bad += "$nm402 was not found on WoD20.1 - the roster outlived the box (SPEC V20, V209)"
            continue
        }
        if ($mainBottom[$nm402] -ne $mainBottom["ROAD"]) {
            $v402Bad += "$nm402 ends at $($mainBottom[$nm402]) and ROAD at $($mainBottom['ROAD']) - the two boxes V376 lets off the tab line close together, or neither is aligned to anything (SPEC V402, I139a)"
        }
    }
}
if ($v402Bad) { foreach ($b402 in $v402Bad) { Fail "V402 $b402" } }
else { Pass "V402 $($V402_ALIGNED -join ', ') closes on y=$($mainBottom['ROAD']) with ROAD - the two bottom-row boxes V376 lets off the tab line end together" }

# ---- V415: the AVATAR closes where ROAD closes (SPEC V415, I144a, T939, Q63) -----------
# The owner's ask of 2026-09-04, item 1, and it is V402 wearing the avatar's face: a bottom-row
# thing that is neither on the tab line nor on its own content, but tied to another box.
#
# NEITHER y is typed here, for the same reason V402 types neither: a literal 856 would be the
# second owner of a number the XML already holds, and the first of the two to age (SPEC B70,
# B112). Move ROAD and the avatar has to move with it or this reddens - which is the ask.
#
# The avatar is THREE controls carrying the same four numbers (avatarFrame, the image, ornAvatar)
# and this rule deliberately does NOT re-check that they agree: V309 owns that sentence and has
# since the 50th round, so measuring it again here would be two owners for one fact (SPEC V309,
# B70). What this rule owns is the bottom edge; what V309 owns is who follows it. Between them a
# height changed on one control of the three reddens twice, which is the intent.
#
# It reads the IMAGE because $avatarBottom already does, one line, no second parse.
#
# Mutation (SPEC V20, V222): height back to 455 on the image -> RED here AND on V309 - ROAD
# growing 5 without the avatar -> RED. Probe: move all three to 445 together -> GREEN on both.
$v415Bad = @()
if ($avatarBottom -lt 0) { $v415Bad += "the avatar image was not found on WoD20.1 - the edge this rule pins cannot be read, and a rule that cannot find its subject must not pass (SPEC V20, B94)" }
elseif (-not $mainBottom.ContainsKey("ROAD")) { $v415Bad += "the ROAD box was not found on WoD20.1 - the y this rule aligns to cannot be read (SPEC V20)" }
elseif ($avatarBottom -ne $mainBottom["ROAD"]) {
    $v415Bad += "the avatar ends at $avatarBottom and ROAD at $($mainBottom['ROAD']) - the owner asked the photo to close where that box closes, and off the line it is a grey tail hanging under the column (SPEC V415, I144a)"
}
if ($v415Bad) { foreach ($b415 in $v415Bad) { Fail "V415 $b415" } }
else { Pass "V415 the avatar closes on y=$avatarBottom with ROAD - the left column ends on one edge, and V309 keeps the frame and the ornament on it" }

# ---- V416: the six EXPERIENCE edits take WHOLE NUMBERS only (SPEC V416, I144d, I144f) --
# The owner's ask of 2026-09-04, item 2, and the ask names the spread explicitly: "e isso deve se
# espelhar para todos os outros blocos de experiencia". So the subject is SIX edits across four
# files, not the one box the ask was typed about (SPEC T940).
#
# NO `min` and NO `max`, and that half is the reason this is a rule and not a comment. V131 owns
# the sentence "experience does not go below zero" and it REFUSES - a message, and the number
# goes back to what it was - where the host's `min` would CLAMP in silence. Both present is two
# owners for one fact and the cheaper one to read is the one that never speaks (SPEC B70, V131).
# The `xpSetCurrent` guard STAYS whatever the keyboard does: paste and Lua writes never touch the
# keyboard, and a guard only the keyboard can reach is not a guard.
#
# The collector matches by NAME because that is the only thing the six share - they sit in four
# files, three of them are read-only, and one carries no onUserChange at all. So the zero-guard is
# a COUNT: an XP edit born under a seventh name would leave this rule measuring five and saying
# nothing, which is how a roster rots (SPEC V209, V20).
#
# `type=` is new vocabulary on this sheet - zero occurrences before T940 - so the count is also
# what proves the attribute reached the XML at all rather than being silently dropped.
#
# Mutation (SPEC V20, V222): drop decimalPlaces from one of the six -> RED - add min="0" to one
# -> RED - rename one of the six -> RED on the count. Probe: put type="number" on an edit that is
# NOT an XP box -> GREEN, the subject is the six and not the attribute.
$V416_XP = @('edtCurrentXPMain', 'edtCurrentXPNumina', 'edtCurrentXPGhoul', 'edtTotalXP', 'edtSpentXP', 'edtCurrentXP')
$v416Bad = @()
$v416Seen = @{}
foreach ($f416 in $files) {
    $t416 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f416.FullName))
    foreach ($m416 in [regex]::Matches($t416, '<edit\s+name="([^"]+)"([^>]*)>')) {
        $nm416 = $m416.Groups[1].Value
        if ($V416_XP -notcontains $nm416) { continue }
        $at416 = $m416.Groups[2].Value
        $v416Seen[$nm416] = $true
        if ($at416 -notmatch '\btype="number"') { $v416Bad += "$nm416 ($($f416.Name)) has no type=`"number`" - the field takes letters and punctuation from the keyboard and xpSetCurrent sees a string it cannot read (SPEC I144d)" }
        if ($at416 -notmatch '\bdecimalPlaces="0"') { $v416Bad += "$nm416 ($($f416.Name)) has no decimalPlaces=`"0`" - type=`"number`" alone still takes 3.5 experience points (SPEC I144d)" }
        foreach ($bad416 in @('min', 'max')) {
            if ($at416 -match "\b$bad416=") { $v416Bad += "$nm416 ($($f416.Name)) declares $bad416= - the host CLAMPS in silence and V131 REFUSES with a message, and two owners of one bound means the quiet one wins (SPEC V131, B70)" }
        }
    }
}
if ($v416Seen.Count -lt $V416_XP.Count) {
    $gone416 = @($V416_XP | Where-Object { -not $v416Seen.ContainsKey($_) })
    $v416Bad += "only $($v416Seen.Count) of the $($V416_XP.Count) experience edits were found - missing $($gone416 -join ', '). The roster matches by NAME, so an XP box renamed or born under a seventh name drops out of this rule without a word (SPEC V209, V20)"
}
if ($v416Bad) { foreach ($b416 in $v416Bad) { Fail "V416 $b416" } }
else { Pass "V416 all $($v416Seen.Count) experience edits take whole numbers only - type=`"number`" with decimalPlaces=`"0`", and none of them declares min or max, so V131 stays the only owner of the zero floor" }

# ---- V309: the avatar FRAME tracks the avatar IMAGE (SPEC I82d, V309, B71) ------------
# Four numbers, both sides read from the XML - a RELATION, so there is no literal here to go
# stale at the next tightening of the gap.
#
# B71 is why it exists. The frame is a loose <rectangle>, not a section box: V280/V298 collect
# layout[rectangle[@color='black'][@xradius]] and this one is DimGray with no parent layout, so
# the collector never saw it; V69 measures the IMAGE's bottom; V48 names it as a deliberate
# non-black rectangle and stops there. Three families of check walked past the same control,
# and three rounds of reflow (T647, T668, T675) moved the image from 471 down to 441 while the
# frame stayed put - thirty pixels of grey hanging below the photo, on screen, green in the gate
# the whole time. Zero owners for one relation: nothing would ever have reddened on its own.
# THREE controls since the 113th round (SPEC I84d): the DimGray frame underneath, the picture,
# and the transparent carrier above that the filigree hangs on. All three share the picture's
# four numbers - the carrier has to sit exactly over the photo or the ornament draws off it.
$frameV309 = @($mainDocV69.SelectNodes("//scrollBox/rectangle[@color='DimGray']"))[0]
$ornCarrier = @($mainDocV69.SelectNodes("//scrollBox/rectangle[@name='ornAvatar']"))[0]
if ($null -eq $avatarImg)    { Fail "V309 the avatar image is gone from WoD20.1 - there is nothing to track the frame against (SPEC V209)" }
elseif ($null -eq $frameV309) { Fail "V309 the avatar frame is gone from WoD20.1 - a check that goes quiet when its only target disappears is B7 waiting (SPEC V209, B71)" }
elseif ($null -eq $ornCarrier) { Fail "V309 the avatar ornament carrier (ornAvatar) is gone from WoD20.1 - the filigree would fall back behind the picture, which is B71's symptom by another door (SPEC V209, I84d)" }
else {
    $offV309 = @()
    foreach ($pair in @(@($frameV309, 'frame'), @($ornCarrier, 'carrier'))) {
        foreach ($a in @("left", "top", "width", "height")) {
            $fv = $pair[0].GetAttribute($a); $iv = $avatarImg.GetAttribute($a)
            if ($fv -ne $iv) { $offV309 += "$a is $fv on the $($pair[1]) and $iv on the image" }
        }
    }
    # The carrier must be declared AFTER the image, or its path draws underneath and the whole
    # point of I84d is lost - silently, because the filigree still exists and still paints.
    $kidsV309 = @($ornCarrier.ParentNode.ChildNodes | Where-Object { $_.NodeType -eq 'Element' })
    $iImg = [array]::IndexOf($kidsV309, $avatarImg)
    $iOrn = [array]::IndexOf($kidsV309, $ornCarrier)
    if ($iOrn -lt $iImg) { $offV309 += "the carrier is declared before the image, so its filigree draws behind the photo (SPEC I84d)" }
    if ($offV309.Count -gt 0) { Fail "V309 the avatar frame/carrier has drifted off the picture - $($offV309 -join '; ') (SPEC I82d, I84d, B71)" }
    else { Pass "V309 the avatar frame and the ornament carrier both carry the picture's own left/top/width/height, and the carrier is declared above it" }
}

# ---- V311: the health box's tail equals the title's margin (SPEC I84c, V311) ----------
# The user's rule, not a number: the gap under the last VISIBLE health row reads the same as the
# gap from the top edge down to the word HEALTH. Three values, all from the XML - box height, the
# bottom of the last row, and the title's top - so HEALTH_BOX_PAD is a CONSEQUENCE and no id here
# writes 57 down. V49 chains it to the Lua (height = 10*pitch + pad), so the two together pin the
# constant without either of them owning a literal.
#
# That is the difference from B72, which cost a build: there the number was asserted without being
# measured. Here there is no number to assert.
#
# V280a survives and is not duplicated - it demands >= 20 on all four sides of every box; this
# demands EQUALITY between two sides of one box. The probe separates them: move the title to
# top=30 and V280a stays green while this reddens.
$v311Bad = @()
$v311Seen = 0
foreach ($pair in @(@("WoD20.1.lfm", "dynHealth_"), @("WoD20.3.lfm", "dynHealth3_"))) {
    $d311 = Doc (Join-Path $dir $pair[0])
    $box311 = $d311.SelectSingleNode("//layout[@name='$($pair[1])box']")
    if ($null -eq $box311) { $v311Bad += "$($pair[0]) has no <layout name='$($pair[1])box'> - there is no box to measure (SPEC V209)"; continue }
    $rows311 = @($box311.SelectNodes("layout[starts-with(@name,'$($pair[1])row')]"))
    $ttl311  = $box311.SelectSingleNode("label[@horzTextAlign='center']")
    if ($rows311.Count -eq 0) { $v311Bad += "$($pair[0]) health box holds no row - a box with nothing in it has no tail to read (SPEC V209)"; continue }
    if ($null -eq $ttl311)    { $v311Bad += "$($pair[0]) health box has no centred title - the ruler this leg measures against is gone (SPEC V209)"; continue }
    $v311Seen++
    $last311 = ($rows311 | Sort-Object { [int]$_.GetAttribute("top") })[-1]
    $tail311 = [int]$box311.GetAttribute("height") - ([int]$last311.GetAttribute("top") + [int]$last311.GetAttribute("height"))
    $head311 = [int]$ttl311.GetAttribute("top")
    if ($tail311 -ne $head311) {
        $v311Bad += "$($pair[0]) leaves $tail311 under the last health row but $head311 above the title - the two margins are one rule (SPEC I84c)"
    }
}
if ($v311Seen -ne 2) { Fail "V311 measured $v311Seen health box(es), expected 2 - both tabs carry one and a check that reads half the sheet is B7 waiting (SPEC V209)" }
elseif ($v311Bad) { foreach ($b in $v311Bad) { Fail "V311 $b" } }
else { Pass "V311 both health boxes close the same distance under the last row as they open above the title - HEALTH_BOX_PAD follows from it" }
# ---- V48: a section box is filled black (SPEC B18) -------------------------------
# ABILITIES shipped with the Mage sheet's transparent fill, so the tab background showed
# through and it read as grey beside its black siblings. Nothing measured colour until now.
# A section box is a layout holding a rectangle AND a centred title - which is exactly what
# leaves out the two deliberate non-black rectangles: the avatar frame (DimGray, no title) and
# the health mark box (#00000000, declared inside a template and sitting on a black box).
$greyBoxes = 0
$litBoxes = 0
foreach ($f in $files) {
    $xml = Doc $f.FullName
    foreach ($box in $xml.SelectNodes("//layout")) {
        $rect = $box.SelectSingleNode("rectangle")
        if ($null -eq $rect) { continue }
        $title = $null
        foreach ($lb in $box.SelectNodes("label")) {
            if ($lb.GetAttribute("horzTextAlign") -eq "center") { $title = $lb; break }
        }
        if ($null -eq $title) { continue }
        $litBoxes++
        $fill = $rect.GetAttribute("color")
        if ($fill -ne 'black') {
            $greyBoxes++
            Fail "V48 $($f.Name): box '$($title.GetAttribute('text'))' is filled '$fill', not black - the tab shows through it"
        }
    }
}
if ($litBoxes -eq 0) { Fail "V48 no section box found - the check reads nothing (SPEC V20)" }
elseif ($greyBoxes -eq 0) { Pass "V48 all $litBoxes section boxes are filled black" }

# ---- V51: willpower on the Combat tab - dots read-only, boxes editable -----------
# The point of the box is spending willpower mid-fight without letting anyone raise the
# permanent rating from here, so the two halves must stay split: dots with no field at all
# (painted from the real values) over boxes that own willpower_c* and mirror the Main tab.
$hh3x = Doc (($files | Where-Object { $_.Name -eq 'WoD20.3.lfm' }).FullName)
$hh3t = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20.3.lfm")))
$wpTpl = $hh3x.SelectSingleNode("//template[@name='WillpowerMirror']")
if ($null -eq $wpTpl) { Fail "V51 WoD20.3 has no WillpowerMirror template" }
else {
    $wpDots = @($wpTpl.SelectNodes("imageCheckBox"))
    $wpBoxes = @($wpTpl.SelectNodes("checkBox"))
    $owning = @($wpDots | Where-Object { $_.HasAttribute("field") })
    $clickable = @($wpDots | Where-Object { $_.GetAttribute("autoChange") -ne 'false' })

    if ($wpDots.Count -ne 10) { Fail "V51 WillpowerMirror has $($wpDots.Count) dots, expected 10" }
    elseif ($owning.Count -gt 0) { Fail "V51 $($owning.Count) willpower dots own a field - they would be a second owner of the Main tab rating (V1)" }
    elseif ($clickable.Count -gt 0) { Fail "V51 $($clickable.Count) willpower dots lack autoChange='false' - a click would toggle a display-only dot" }
    else { Pass "V51 all 10 willpower dots are display only" }

    $boxFields = @($wpBoxes | ForEach-Object { $_.GetAttribute("field") } | Where-Object { $_ -match '^willpower_c\d+$' })
    if ($boxFields.Count -ne 10) { Fail "V51 WillpowerMirror has $($boxFields.Count) willpower_c* boxes, expected 10" }
    else { Pass "V51 all 10 willpower boxes mirror the Main tab" }

    if ($hh3t -notmatch 'paintWillpower') { Fail "V51 WoD20.3 never paints the willpower dots - they would stay empty" }
    elseif ($hh3t -notmatch "'willpower_1'") { Fail "V51 the WoD20.3 dataLink does not observe willpower_* - the dots would not follow the Main tab" }
    else { Pass "V51 the dots are painted and follow the Main tab" }
}

# Virtues joined the display-only side in the 12th round: same rule, same two ways to break it.
$vTpl = $hh3x.SelectSingleNode("//template[@name='VirtueMirror']")
if ($null -eq $vTpl) { Fail "V51 WoD20.3 has no VirtueMirror template" }
else {
    $vDots = @($vTpl.SelectNodes("imageCheckBox"))
    $vOwning = @($vDots | Where-Object { $_.HasAttribute("field") })
    $vClickable = @($vDots | Where-Object { $_.GetAttribute("autoChange") -ne 'false' })

    if ($vDots.Count -eq 0) { Fail "V51 VirtueMirror has no dots to check" }
    elseif ($vOwning.Count -gt 0) { Fail "V51 $($vOwning.Count) virtue dots own a field - virtues are display-only on this tab since the 12th round" }
    elseif ($vClickable.Count -gt 0) { Fail "V51 $($vClickable.Count) virtue dots lack autoChange='false' - a click would toggle a display-only dot" }
    else { Pass "V51 all $($vDots.Count) virtue dots are display only" }

    # Anchored on the paint CALL, not just the name: the list can be renamed or emptied while
    # the token still appears somewhere in the file.
    if ($hh3t -notmatch 'paint\(form,\s*RO_VIRTUES\[') { Fail "V51 WoD20.3 never paints the virtue dots - they would stay empty" }
    elseif ($hh3t -notmatch 'RO_VIRTUES\s*=\s*\{\s*"conscience"') { Fail "V51 RO_VIRTUES does not list the three virtues" }
    elseif ($hh3t -notmatch "'conscience_2'") { Fail "V51 the WoD20.3 dataLink does not observe the virtue fields - the dots would not follow the Main tab" }
    else { Pass "V51 the virtue dots are painted and follow the Main tab" }
}

# ---- V49: the health box height follows the track length (SPEC C 10th round) -----
# The renderer computes the height from a pitch that lives in Lua while the rows themselves are
# positioned in the .lfm files. Measure the real spacing and compare - if someone re-spaces the
# rows, the Lua constant has to move with them. The declared height must be the TEN-row case:
# the renderer only shrinks, so the file stays the worst case for the box-overlap check (V40).
$pitchLua = 0; $padLua = 0
if ($root -match 'HEALTH_ROW_PITCH\s*=\s*(\d+)') { $pitchLua = [int]$Matches[1] }
if ($root -match 'HEALTH_BOX_PAD\s*=\s*(\d+)')   { $padLua   = [int]$Matches[1] }
if ($pitchLua -eq 0 -or $padLua -eq 0) { Fail "V49 HEALTH_ROW_PITCH / HEALTH_BOX_PAD not found on the root form" }
else {
    $measured = 0
    foreach ($pair in @(@('WoD20.1.lfm','dynHealth_'), @('WoD20.3.lfm','dynHealth3_'))) {
        $fx = $files | Where-Object { $_.Name -eq $pair[0] }
        if (-not $fx) { Fail "V49 $($pair[0]) missing"; continue }
        $xml = Doc $fx.FullName

        $r1 = $xml.SelectSingleNode("//layout[@name='$($pair[1])row1']")
        $r2 = $xml.SelectSingleNode("//layout[@name='$($pair[1])row2']")
        $bx = $xml.SelectSingleNode("//layout[@name='$($pair[1])box']")
        if ($null -eq $r1 -or $null -eq $r2) { Fail "V49 $($pair[0]) has no $($pair[1])row1/row2 - the renderer could not find its rows"; continue }
        if ($null -eq $bx) { Fail "V49 $($pair[0]) has no <layout name='$($pair[1])box'> - the renderer cannot resize the box"; continue }

        $measured++
        $pitchXml = [int]$r2.GetAttribute("top") - [int]$r1.GetAttribute("top")
        $hXml = [int]$bx.GetAttribute("height")
        if ($pitchXml -ne $pitchLua) { Fail "V49 $($pair[0]) rows are $pitchXml apart but HEALTH_ROW_PITCH is $pitchLua - the box would not end under the last row" }
        else { Pass "V49 $($pair[0]) row pitch ($pitchXml) matches HEALTH_ROW_PITCH" }

        $hMax = 10 * $pitchLua + $padLua
        if ($hXml -ne $hMax) { Fail "V49 $($pair[0]) box height=$hXml but the ten-row case is $hMax - the file must declare the tallest it can get (V40 reads this file)" }
        else { Pass "V49 $($pair[0]) box declares the ten-row height ($hXml)" }
    }
    if ($measured -eq 0) { Fail "V49 no health box measured - the check reads nothing (SPEC V20)" }

    # The height has to DERIVE from the pitch, and that is not the same as being written on one
    # line with it. B62's fix hands the measurement to the filigree as well as to the box, so it
    # is computed into a local first - one source of truth for two readers. Anchoring on the
    # one-liner would have forced the expression to be written twice, which is the second truth
    # this check exists to prevent. So: find what is assigned to `.height`, and accept it if the
    # RHS carries the pitch itself OR names a local that was assigned from it (SPEC V222).
    $hAsg = [regex]::Match($rendFn, '(?m)\.height\s*=\s*([^;\r\n]+);')
    $pitchOk = $false
    if ($hAsg.Success) {
        $rhs = $hAsg.Groups[1].Value
        if ($rhs -match 'HEALTH_ROW_PITCH') { $pitchOk = $true }
        elseif ($rhs -match '^\s*(\w+)\s*$') {
            $ln = [regex]::Escape($Matches[1])
            if ($rendFn -match ('(?m)^\s*local\s+' + $ln + '\s*=[^;\r\n]*HEALTH_ROW_PITCH')) { $pitchOk = $true }
        }
    }

    if (-not $hAsg.Success) { Fail "V49 renderHealthTrack never writes the box height - the box would stay ten rows tall" }
    elseif (-not $pitchOk) { Fail "V49 renderHealthTrack sets the box height from something other than HEALTH_ROW_PITCH - the box would not end under the last row" }
    elseif ($rendFn -notmatch 'healthLevels') { Fail "V49 renderHealthTrack does not read healthLevels for the height" }
    else { Pass "V49 the renderer sizes the box from the chosen track length" }
}

# ---- V12: combo items are values - items must agree with the Lua comparison ---
# The theme combo was removed on 2026-08-17. Absent is fine; present-but-inconsistent is not.
$hh6 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20.6.lfm")))

# The radii and corner types a PALETTE can put on a section box at RUNTIME (SPEC I89a, V316).
# Read ONCE, here, because three checks downstream need the same fact: V258 measures the corner
# bite against the LARGEST radius any era can render, V66 fences the write that produces it, and
# V316 owns the key itself. Anchored at line start so a comment mentioning boxCorner never
# counts as a declaration.
$boxCornerDecl  = @([regex]::Matches($hh6, '(?m)^\s*boxCorner\s*=\s*\{\s*type\s*=\s*"([^"]*)"\s*,\s*radius\s*=\s*([0-9.]+)\s*\}\s*,'))
$boxCornerRadii = @($boxCornerDecl | ForEach-Object { [double]$_.Groups[2].Value })
$boxCornerTypes = @($boxCornerDecl | ForEach-Object { $_.Groups[1].Value })
$boxCornerAny   = @([regex]::Matches($hh6, '(?m)^\s*boxCorner\s*=')).Count
if ($hh6 -match 'field="theme"[^>]*items="\{([^}]*)\}"') {
    $items = ([regex]::Matches($Matches[1], "'([^']*)'") | ForEach-Object { $_.Groups[1].Value })
    if ($hh6 -match 'theme\s*==\s*"([^"]+)"') {
        $cmp = $Matches[1]
        if ($items -contains $cmp) { Pass "V12 theme comparison '$cmp' is one of {$($items -join ', ')}" }
        else { Fail "V12 Lua compares theme == '$cmp' but items are {$($items -join ', ')} - theme switching is broken" }
    } else { Fail "V12 theme combo present but no comparison found in WoD20.6.lfm" }
} else { Pass "V12 n/a - no theme combo (removed)" }

# ---- V21: language traversal starts at the ROOT, not at this form (SPEC B.9) --
if ($hh6 -match 'recursiveFindControls\s*\(\s*self') { Fail "V21 traversal still starts at self - sibling tabs unreachable" }
elseif ($hh6 -notmatch 'getParent\(\)') { Fail "V21 no getParent() walk found - traversal cannot reach the sheet root" }
else { Pass "V21 traversal walks up to the sheet root" }

# ---- V109 + V110: the Game box is locked, defaulted, and kept inside its roster ---
# Information, not a choice. Which makes what it holds the sheet's problem: a value outside
# the three - one of the four longer names this sheet used to offer, or nothing at all - would
# sit in a box nobody can open, so the load puts it back inside the roster.
$gameNode = @((Doc (Join-Path $dir "WoD20.6.lfm")).SelectNodes("//comboBox[@name='cboGame']"))
if ($gameNode.Count -ne 1) { Fail "V109 expected exactly one cboGame, found $($gameNode.Count)" }
elseif ($gameNode[0].GetAttribute("enabled") -ne 'false') { Fail "V109 cboGame is editable - the game is information, not a choice" }
elseif ($hh6 -notmatch 'dataLink field="game" defaultValue="Mortal"') { Fail "V109 cboGame has no default - a new sheet would show an empty locked box" }
elseif ($root -notmatch 'setField\("game", "Mortal"\)') { Fail "V110 nothing normalises a game outside the roster - the locked box would hold a value nobody can clear" }
else { Pass "V109/V110 cboGame is locked, defaulted and normalised on load" }

# ---- V22: translation is non-destructive and the PT map matches localization.lang
if ($hh6 -notmatch 'originalText\[') { Fail "V22 no snapshot table - translating in place cannot be undone" }
else { Pass "V22 original English is snapshotted before translating" }

$embedded = New-Object 'System.Collections.Generic.Dictionary[string,string]' ([StringComparer]::Ordinal)
$mapBlock = [regex]::Match($hh6, 'local PT = \{(.*?)\n\s*\};', 'Singleline')
if (-not $mapBlock.Success) { Fail "V22 PT map not found in WoD20.6.lfm" }
else {
    foreach ($m in [regex]::Matches($mapBlock.Groups[1].Value, '\["([^"]+)"\]\s*=\s*"([^"]*)"')) {
        $embedded[$m.Groups[1].Value] = $m.Groups[2].Value
    }
    $drift = @()
    foreach ($k in $embedded.Keys) {
        if (-not $ptVal.ContainsKey($k)) { $drift += "'$k' in map but not in [pt]" }
        elseif ($ptVal[$k] -cne $embedded[$k]) { $drift += "'$k': map='$($embedded[$k])' lang='$($ptVal[$k])'" }
    }
    if ($drift) { foreach ($d in $drift) { Fail "V22 PT map out of sync - $d" } }
    else { Pass "V22 PT map ($($embedded.Count) entries) matches localization.lang" }
}

# ---- V28: the map must also COVER every visible string (SPEC B.14) ------------
# V22 only checks map -> lang. Without the reverse, a new string can have a [pt] key,
# be missing from the embedded map, never translate at runtime, and still pass green.
$uncovered = @($visible | Where-Object { -not $embedded.ContainsKey($_) })
if ($uncovered) { foreach ($s in $uncovered) { Fail "V28 '$s' has a [pt] key but is absent from the PT map - it would never translate" } }
else { Pass "V28 PT map covers all $($visible.Count) visible strings" }

# ---- V23: language is a two-value combo, not a bool ---------------------------
if ($hh6 -match 'field="localization"') { Fail "V23 old boolean 'localization' field still present" }
elseif ($hh6 -notmatch 'field="language"') { Fail "V23 no 'language' field found" }
else {
    $lm = [regex]::Match($hh6, 'field="language"[^>]*values="\{([^}]*)\}"')
    $lv = @([regex]::Matches($lm.Groups[1].Value, "'([^']*)'") | ForEach-Object { $_.Groups[1].Value })
    if ($lv.Count -eq 2 -and $lv -contains 'en' -and $lv -contains 'pt') { Pass "V23 language combo values = {$($lv -join ', ')}" }
    else { Fail "V23 language combo values are {$($lv -join ', ')}, expected {en, pt}" }
}

# ---- V24: picker values stay canonical English -------------------------------
# Same two-codepoint excuse V9 carries, and for the same names (SPEC V195, B42): a
# canonical value out of the core keeps the book's typographic apostrophe, and the one
# out of M20 Sorcerer keeps its eth (SPEC I66).
$notEn = @()
$v24Seen = 0
foreach ($f in $files) {
    $xml = Doc $f.FullName
    foreach ($cb in $xml.SelectNodes("//comboBox[@name]")) {
        if ($cb.GetAttribute("name") -notlike 'cbo*') { continue }
        foreach ($v in (ListOf $cb (TplOf $cb))) {
            $v24Seen++
            if ($v -ne '' -and $v -match '[^\x00-\x7F\u2019\u00F0]') { $notEn += "$($f.Name): stored value '$v' is not English" }
        }
    }
}
if ($notEn) { foreach ($n in $notEn) { Fail "V24 $n" } }
elseif ($v24Seen -eq 0) { Fail "V24 no picker value was read - the check verifies nothing (SPEC V20, B7, V209c)" }
else { Pass "V24 all $v24Seen picker values are canonical English" }

# ---- V195: a straight apostrophe inside a list would split the item in two -------
# The other half of the exception above, and the half that makes it compulsory rather
# than convenient. A list attribute is written {'a', 'b'}, so an ASCII ' INSIDE an entry
# is the delimiter: Firecast's parser and the regex two blocks up would both read
# "Devil's Touch" as "Devil" followed by rubbish, silently and identically. That is why
# the forty-two names keep U+2019 instead of being flattened to a straight quote.
#
# Measured on the STRUCTURE of the whole attribute, not by hunting for the character
# (SPEC V20): a well-formed list is quoted entries separated by commas and nothing else,
# so any stray quote breaks the shape no matter where it lands. Every comboBox, not only
# the cbo* pickers - a split entry is just as wrong in a state combo.
$listShape = '^\{\s*''[^'']*''(\s*,\s*''[^'']*'')*\s*\}$'
$listBad  = @()
$listSeen = 0
foreach ($f in $files) {
    foreach ($cb in (Doc $f.FullName).SelectNodes("//comboBox")) {
        foreach ($a in @('items','values')) {
            if (-not $cb.HasAttribute($a)) { continue }
            $listSeen++
            $raw = $cb.GetAttribute($a)
            $id = $cb.GetAttribute("name"); if (-not $id) { $id = $cb.GetAttribute("field") }; if (-not $id) { $id = 'comboBox' }
            if ($raw -notmatch $listShape) { $listBad += "$($f.Name): $id $a= is not a list of quoted entries - a straight apostrophe inside one splits it (SPEC V195)" }
        }
    }
}
if ($listBad) { foreach ($b in $listBad) { Fail "V195 $b" } }
elseif ($listSeen -eq 0) { Fail "V195 no items=/values= attribute was read - the check verifies nothing (SPEC V20, B7)" }
else { Pass "V195 all $listSeen list attributes are quoted entries and commas, with no straight apostrophe to split them" }

# ---- V194: the era renames the LABEL, never the value (SPEC I22, T469) ---------------
# B23 is the bug this exists to stop happening a second time: the 19th round expressed an era
# as a rename of the row, so the same name changed which field it stood for and ratings walked
# between traits when the sheet changed period. ERA_NAME is the answer - one table of DISPLAY
# strings keyed by the canonical value - and this is what keeps it a display table.
#
# Three things, and the third is the one with teeth: a display must never turn up as a stored
# value. "Creatio Ignis (The Creation of Fire)" is what a Dark Ages sheet SHOWS for Lure of
# Flames; the moment it is also something a picker can save, a character built in one era
# stops opening the same in another (SPEC V2, V24).
$eraTbl = [regex]::Match($root, '(?ms)-- >>> ERA_NAME_BEGIN[^\n]*\n(.*?)-- <<< ERA_NAME_END')
$eraMap = New-Object 'System.Collections.Generic.Dictionary[string,string]' ([StringComparer]::Ordinal)
if ($eraTbl.Success) {
    foreach ($m in [regex]::Matches($eraTbl.Groups[1].Value, '\["([^"]+)"\]\s*=\s*"([^"]*)"')) { $eraMap[$m.Groups[1].Value] = $m.Groups[2].Value }
}

# Every value a picker offers, split two ways: the vampire tab's, which is what ERA_NAME is
# allowed to rename, and the sheet's whole set, which is where a display must never appear.
#
# AMENDED in the 159th round (SPEC V194, T872): the vampire half is read from PICKER_LIST and
# no longer off //comboBox. Wave 3 replaced the four Vampire row dropdowns with the search box,
# so the collector came back EMPTY and this check went red for want of a subject rather than
# for drift - the false-red twin of B7's false-green. The five vampire roots are NAMED, and
# clanDisc and secPath are aliases BY IDENTITY (V211c) so the set does not grow for them.
$VAMP_ROOTS = @('clanDisc', 'disc', 'mainPath', 'secPath', 'ritual')
$vampVals = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
$anyVals  = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
foreach ($vr in $VAMP_ROOTS) {
    if (-not $PICKER.ContainsKey($vr)) { continue }
    foreach ($v in @($PICKER[$vr])) { if ($v -ne '') { [void]$vampVals.Add($v); [void]$anyVals.Add($v) } }
}
foreach ($f in $files) {
    foreach ($cb in (Doc $f.FullName).SelectNodes("//comboBox")) {
        foreach ($v in (ListOf $cb (TplOf $cb))) {
            if ($v -eq '') { continue }
            [void]$anyVals.Add($v)
        }
    }
}

$eraDrift = @()
if (-not $eraTbl.Success) { $eraDrift += "the ERA_NAME region is gone from the root form - nothing below reads anything (SPEC V20, B7)" }
elseif ($eraMap.Count -eq 0) { $eraDrift += "the ERA_NAME region parsed to zero rows - the check verifies nothing (SPEC V20, B7)" }
elseif ($vampVals.Count -eq 0) { $eraDrift += "PICKER_LIST holds none of the five vampire roots ($($VAMP_ROOTS -join ', ')) - there is nothing for ERA_NAME to key by (SPEC V20, V211)" }
else {
    foreach ($k in $eraMap.Keys) {
        if (-not $vampVals.Contains($k)) { $eraDrift += "'$k' is keyed in ERA_NAME but no vampire picker offers it - a display rule for a row nobody can choose" }
    }
    foreach ($d in @($eraMap.Values | Sort-Object -Unique)) {
        if (-not $ptK.Contains($d))          { $eraDrift += "display '$d' has no [pt] key - the era name skips the translation every other label goes through (SPEC V17)" }
        if (-not $enK.Contains($d))          { $eraDrift += "display '$d' has no [en] key (SPEC V17)" }
        if (-not $embedded.ContainsKey($d))  { $eraDrift += "display '$d' is absent from the PT map - it would never translate at runtime (SPEC V28, B14)" }
        if ($anyVals.Contains($d))           { $eraDrift += "display '$d' is ALSO a stored value - a sheet saved in one era would not open the same in another (SPEC V2, V24, B23)" }
    }
}
if ($eraDrift) { foreach ($e in $eraDrift) { Fail "V194 $e" } }
else { Pass "V194 all $($eraMap.Count) era names rename a value the pickers offer, carry both keys, and none of them is a value" }

# The renamer itself: one reader, and the two switches that already walk the sheet call it.
# A dataLink of its own on sheetTheme would be a second era trigger, which is the drift T469
# was written to make impossible.
$eraFn = [regex]::Match($root, '(?ms)function eraName\(v, era\)(.*?)\n\t\t\tend;')
if (-not $eraFn.Success) { Fail "V194 eraName is not declared on the root form - ERA_NAME has no reader" }
elseif ($eraFn.Groups[1].Value -notmatch 'ERA_NAME\[v\]') { Fail "V194 eraName does not read ERA_NAME - the table is dead again" }
elseif ($eraFn.Groups[1].Value -notmatch '"Dark Ages"' -or $eraFn.Groups[1].Value -notmatch '"Classical Age"') { Fail "V194 eraName does not gate on both medieval eras - one table serves the two (SPEC C, I22)" }
elseif ($hh6 -notmatch 'c\.items = shown;') { Fail "V194 nothing writes a picker's items in WoD20.6 - the era and the language would both stop reaching the rows" }
elseif ($hh6 -notmatch 'pickerItems\(c, lang, currentTheme\(\), levels\);') { Fail "V194 applyLanguage no longer rebuilds the items through pickerItems" }
elseif ($hh6 -notmatch 'pickerItems\(c, \(sheet ~= nil and sheet\.language\) or "en", v, levels\);') { Fail "V194 applyTheme no longer rebuilds the items - changing era would leave the old era's words on the rows (SPEC T469)" }
elseif ($hh6 -match 'dataLink[^>]*field="sheetTheme"[^>]*pickerItems') { Fail "V194 a second era trigger was added - the era must ride applyTheme's own walk (SPEC T469)" }
else { Pass "V194 one reader of ERA_NAME, called from both switches, with no era trigger of its own" }

# The ONE named width exception on the sheet, read by V196 and by V312c and asserted by V351d
# (SPEC C Q27, I99m). Q27 put the ROAD label on the combo's own line, which is 25px of height
# off three boxes and leaves the combo 239 where the ruler wants 246/265. MEASURED: exactly one
# item of the 55 clips, and the next longest wants 204. A NAME and never a wildcard - a pattern
# here would take the ruler off the other sixteen pickers in one line, which is B7 arriving
# through the exception door. ONE list because two would age apart (SPEC V135).
$NARROW_OK = @()

# ---- V196: a picker must be wide enough for what it offers (SPEC B43) ---------------
# V16 has measured //label since the first round and never once looked at a comboBox, so
# every picker on the sheet went 58 rounds unmeasured. That was survivable while the lists
# were short words; T460 put 384 book names in them and three of the four could not hold
# their own contents. A comboBox CLIPS - it does not wrap - so the text simply disappears,
# exactly the way a checkBox did in B17.
#
# Two axes, because the list has two (SPEC V24): the language, which NeededPx already folds
# in, and the ERA. A value can DISPLAY as something much longer than itself: WoD20.13's widest
# entry is "Potestas Tempestatum (Poder sobre as Tempestades)", the Dark Ages name for
# "Weather Control" - fifteen characters that render as forty-nine. Sizing off the canonical
# values alone would have passed a picker that clips in two of the four eras.
#
# The list is read WHERE IT LIVES - ListOf, which resolves PICKER_LIST for the migrated
# pickers and falls back to the inline attribute for the ones T493 left alone (SPEC I27,
# V209 leg f). Until the 87th round this check read //comboBox[@items][@width] instead, and
# T493 had removed items= from precisely the five vampire rows: 5 of the sheet's 17 combos
# were measured and NONE of the five carrying the longest strings it owns - 284 rituals and
# 64 paths (SPEC B56). ListOf was written for this in the 68th round and had zero callers.
#
# Same ruler as V16 - one $PX_PER_CHAR for labels and pickers alike - plus room for the
# dropdown arrow, measured off cboNature: 25 characters living comfortably in 215px.
$ARROW = 24
$comboNarrow = @()
$comboSeen = 0
$comboVamp = 0
foreach ($f in $files) {
    foreach ($cb in (Doc $f.FullName).SelectNodes("//comboBox[@width]")) {
        $w = 0; if (-not [int]::TryParse($cb.GetAttribute("width"), [ref]$w)) { continue }
        $shown = @(ListOf $cb (TplOf $cb) | Where-Object { $_ -ne '' })
        # The era axis is scoped by SENSE, not by the cbo* prefix (SPEC V197, B44): only the
        # Vampire rows are renamed, so only they may be sized for a Dark Ages name. Measuring
        # WoD20.7 against "Potestas Tempestatum" would demand width for a string it never shows.
        $fieldTpl = $cb.GetAttribute("field")
        $isVampRow = $fieldTpl -match '^\$\((field)\)$' -and $f.Name -eq 'WoD20.11.lfm'
        if ($isVampRow) {
            $canon = @($shown)
            foreach ($v in $canon) { if ($eraMap.ContainsKey($v)) { $shown += $eraMap[$v] } }
        }
        if ($shown.Count -eq 0) { continue }
        $comboSeen++
        if ($isVampRow) { $comboVamp++ }
        $worst = ''; $need = 0
        # The control's OWN fontSize picks the ruler (SPEC V312a): fourteen of the sheet's
        # twenty-one pickers author 12, and measuring them against the body ruler was charging
        # them for glyphs they do not draw (SPEC R116).
        $cbSize = $cb.GetAttribute("fontSize")
        foreach ($it in $shown) { $n = NeededPx $it $cbSize; if ($n -gt $need) { $need = $n; $worst = $it } }
        $need += $ARROW
        $id = $cb.GetAttribute("name"); if (-not $id) { $id = $cb.GetAttribute("field") }; if (-not $id) { $id = 'comboBox' }
        if ($need -gt $w -and $NARROW_OK -notcontains $id) { $comboNarrow += "$($f.Name): picker $id is ${w}px but '$worst' (pt '$($ptVal[$worst])') needs ~${need}px - a comboBox clips, it does not wrap" }
    }
}
# Two zero-guards, and neither is a control count: the old one asked whether ANY combo had
# been measured, and WoD20.6 keeps three inline lists alive, so it stayed green through the
# whole of B56. The first guard measures COVERAGE against the sheet's own census, the second
# names the rows that carry the worst case - a list that resolves to nothing there is exactly
# the failure this check exists to catch (SPEC V209).
#
# AMENDED again in T874: 15 -> 12. Wave 4 turned the three Numina row templates into search
# boxes the same way wave 3 turned the four Vampire ones, and for the same gain - the box WRAPS
# where a comboBox clips (SPEC I107, V339), so the three longest lists on the tab stopped being
# this rule's problem and became V383's. The number is COVERAGE and the prohibition is unchanged.
#
# AMENDED in the 159th round (SPEC V196, T872): 17 -> 15, and the NOMINAL guard is gone with
# its subject. It named "none of the vampire rows among them" because those four carried the
# longest strings the sheet owns; wave 3 turned them into search boxes, so a nominal guard on
# a name that cannot exist is a permanent red against correct code (B106). The content guard
# stays and is now the only one. What the four families lost here they did not lose at all:
# the search box WRAPS instead of clipping (I107, V339) and their button label is V383's.
if ($comboNarrow) { foreach ($c in $comboNarrow) { Fail "V196 $c" } }
elseif ($comboSeen -lt 9) { Fail "V196 only $comboSeen of the sheet's 9 pickers resolved to a list - this check is covering less than the sheet has (SPEC V209, B56). Was 12 until T874 half B converted cboHedgeAffiliation" }
else { Pass "V196 all $comboSeen pickers fit their longest item, in both languages and every era" }

# ---- V197: the era rename is scoped by SENSE, not by the cbo* prefix (SPEC B44) -------
# ERA_NAME is keyed by the canonical value and a value does not carry its sense. "Weather
# Control" is a Thaumaturgy path on the Vampire tab and a hedge magic path on WoD20.7; renaming
# every picker showed the hedge wizard a Latin blood-sorcery name. V194 proves the KEY sits
# in a vampire picker; this proves the EFFECT does not leave one. Third time this same rule
# has been asked for - B15 for hedge magic, T472 for path, B44 for this.
$scopeFn = [regex]::Match($hh6, '(?ms)local function pickerItems\(c, lang, era, levels\)(.*?)\n\t\t\tend;')
$vampTbl = [regex]::Match($hh6, '(?ms)local VAMP_ROW = \{(.*?)\}')
if (-not $scopeFn.Success) { Fail "V197 pickerItems is gone from WoD20.6 - nothing builds a picker's items" }
elseif (-not $vampTbl.Success) { Fail "V197 VAMP_ROW is not declared - the era has no scope and renames the whole sheet (SPEC B44)" }
else {
    $wantRoots = @('clanDisc', 'disc', 'mainPath', 'secPath', 'ritual')
    $gotRoots = @([regex]::Matches($vampTbl.Groups[1].Value, '(\w+)\s*=\s*true') | ForEach-Object { $_.Groups[1].Value })
    $body = $scopeFn.Groups[1].Value
    $missing = @($wantRoots | Where-Object { $gotRoots -notcontains $_ })
    $extra = @($gotRoots | Where-Object { $wantRoots -notcontains $_ })
    if ($missing) { Fail "V197 VAMP_ROW is missing $($missing -join ', ') - those rows would stop being renamed by the era (SPEC I16)" }
    elseif ($extra) { Fail "V197 VAMP_ROW carries $($extra -join ', ') on top of the five vampire rows - the era would reach a picker that is not one" }
    elseif ($body -notmatch 'local vamp\s+= isVampireRow\(nm\);') { Fail "V197 pickerItems does not ask whether the row is a vampire one" }
    elseif ($body -notmatch 'if vamp then v = eraName\(v, era\); end;') { Fail "V197 the era rename is not gated on that answer - every cbo* would be renamed again (SPEC B44)" }
    elseif ($body -notmatch 'if lang == "pt" then v = PT\[v\] or v; end;') { Fail "V197 the language axis is gone - a translation is sheet-wide and must not be scoped with the era" }
    else { Pass "V197 the era renames the five vampire rows only; the language axis stays sheet-wide" }
}

# The gate must scope it the same way, or it sizes a picker for a name it never shows. Proved
# by a value that IS an ERA_NAME key and IS offered outside the Vampire tab.
$leak = @()
foreach ($k in $eraMap.Keys) {
    foreach ($f in $files) {
        if ($f.Name -eq 'WoD20.11.lfm') { continue }
        foreach ($cb in (Doc $f.FullName).SelectNodes("//comboBox[@values]")) {
            foreach ($m in [regex]::Matches($cb.GetAttribute("values"), "'([^']*)'")) {
                if ($m.Groups[1].Value -eq $k) { $leak += "$($f.Name)/$($cb.GetAttribute('name')) offers '$k'" }
            }
        }
    }
}
if ($leak.Count -eq 0) { Pass "V197 no ERA_NAME key is shared with a non-vampire picker today - the scope is untested but armed" }
else { Pass "V197 $($leak.Count) shared name(s) - $($leak -join '; ') - kept unrenamed off the Vampire tab by the scope above" }

# ---- V29 - V33: the Powers tab description block ------------------------------
# The block shows book text: read-only, no field, fed from the DESC table by the renderer.
$hh7Path = Join-Path $dir "WoD20.7.lfm"
if (-not (Test-Path $hh7Path)) { Fail "V29 WoD20.7.lfm missing - the Powers tab is gone" }
else {
    $hh7 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($hh7Path))
    $hh7x = Doc $hh7Path

    # V29: read-only, and no `field=` - a field here would write book text into the character.
    # Scoped to edtNuminaDesc until the 55th round, when the Vampire tab added three more
    # description blocks and the check would have gone on passing while none of them was
    # read. V29 was never about one widget: it is the rule for the DESCRIPTION block, so the
    # blocks are found by their owning box - every one of them, in every file.
    $descBlocks = @()
    foreach ($f in $files) {
        foreach ($lay in (Doc $f.FullName).SelectNodes("//layout[label/@text='DESCRIPTION']")) {
            foreach ($te in $lay.SelectNodes("textEditor")) { $descBlocks += @{ File = $f.Name; Node = $te } }
        }
    }
    # RETIRED in T874. V29 measured the DESCRIPTION boxes of the TABS - readOnly, no field - and
    # wave 4 took the last four off the sheet with the Numina panes (SPEC V365d). The rule is not
    # orphaned and that is measured rather than assumed: both surviving surfaces have owners that
    # already charge exactly these two things. edtPopDesc is born readOnly and owns no field under
    # V365(f) and V333(f); edtMfDesc under V360. Kept as a loop over an empty set this would say
    # PASS over nothing, which is B7 with its own check id (SPEC V20).
    #
    # What stays is the retirement's own guard: a DESCRIPTION box authored anywhere on the sheet
    # again would be a pane that came back UNMEASURED, so it is refused here (SPEC V135, B98).
    if ($descBlocks.Count -gt 0) {
        foreach ($d in $descBlocks) {
            Fail "V29 $($d.File) draws a DESCRIPTION box holding '$($d.Node.GetAttribute('name'))' - waves 3 and 4 took every tab pane off the sheet and this ruler retired with them, so a pane that comes back comes back UNMEASURED (SPEC V365d, V135, B98, T874)"
        }
    }
    else { Pass "V29 retired with the last four panes in T874 - no .lfm draws a DESCRIPTION box, and the two surfaces that remain are charged readOnly and field-less by V365(f) and V333(f)" }

    # V30: TWO groups since the 90th round (SPEC I58) - numinaSel drove the Hedge Magic block and
    # psychicSel the Psychic Phenomena one, each pane painting the row its own group marked.
    #
    # RETIRED in T874 for the reason its Vampire half retired in T872: the DESCRIPTION panes were
    # the only thing that ever read a MARKED row, wave 4 took all four off WoD20.7, and the ? took
    # the 0..20 the radioButtons were holding (user 2026-08-31, ask B). EVERY leg of this rule was
    # a relation between a radio and a pane - the pairing, the distinct fieldValues, the coverage
    # of every row - and there is no radio left on the sheet at all. Kept as loops over an empty
    # set they would report four PASSes over nothing, which is B7 four times with one check id.
    #
    # The retirement's own guard is the zero this used to Fail on, read the other way round: a
    # radio APPEARING on the Numina tab again would be a selector with nothing reading it and no
    # ruler on it. V176 refuses it a second time, sheet-wide and by groupName (SPEC V365d, B98).
    $numinaRadios = @($radios | Where-Object { $_.File -eq 'WoD20.7.lfm' })
    if ($numinaRadios.Count -eq 0) { Pass "V30 retired with the four Numina panes in T874 - not one radio is left on the tab, and nothing reads a marked row anywhere on the sheet" }
    else { Fail "V30 $($numinaRadios.Count) radio(s) are back on the Numina tab - wave 4 took the panes that read them and this ruler retired with them, so a selector that comes back comes back UNMEASURED (SPEC V365d, V176, T874)" }

    # V31: language switch re-renders from DESC; the traversal must not touch the block
    if ($hh7 -match "fields\s*=\s*`"\{[^`"]*'language'") { Pass "V31 the dataLink observes 'language' - the block follows the language switch" }
    else { Fail "V31 dataLink does not observe 'language' - the description would stay in the old language" }
    if ($hh6 -match "cls\s*==\s*`"textEditor`"") { Fail "V31 WoD20.6 translates textEditor in place - it would overwrite the rendered description" }
    else { Pass "V31 WoD20.6 traversal leaves textEditor alone" }

    # V32: every picker item must have a description in both languages.
    # Read the region between the generator markers, not `local DESC = {...};`: the entries are
    # generated one per line and close with `]==] },`, so a regex expecting a newline before the
    # brace matches nothing and reports 43 phantom failures (this check was wrong once already).
    # T478 moved the table out of the .lfm and into descNumina.lua (SPEC I24 / V189), and T495
    # then split that in two, one module per language (SPEC V210). The markers migrated
    # verbatim both times, so the region still has the shape this parser was written for - what
    # moved is that the two languages of an entry sit in two FILES, and the pairing this check
    # is about is made by key across them.
    $numEn = DescEntries (Join-Path $plugin "descNumina_en.lua") 'DESC' 'en'
    $numPt = DescEntries (Join-Path $plugin "descNumina_pt.lua") 'DESC' 'pt'
    # V189: one DESC per area, in a module - the .lfm keeps the renderer only. A table left
    # behind in the .lfm would be a second copy of the same text, free to drift (SPEC I24).
    # Measured across ALL the .lfm files, not just WoD20.7, so T479-T481 are covered too.
    $dupTables = @($files | Where-Object {
        ([System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($_.FullName))) -match '(?m)^\s*local\s+DESC\s*=\s*\{'
    })
    if ($dupTables) { foreach ($d in $dupTables) { Fail "V189 $($d.Name) still declares its own DESC table - the text would live in two files at once (SPEC I24)" } }
    else { Pass "V189 all $($files.Count) .lfm files carry the renderer only; the book text lives in the desc*.lua modules" }
    if ($null -eq $numEn -or $null -eq $numPt) {
        Fail "V32 descNumina_en.lua or descNumina_pt.lua is missing from the plugin root, or its DESC markers are gone - the Powers tab has no text to render (SPEC I24, V210, V209)"
    }
    else {
        $descKeys = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
        $thin = @()
        # The two languages of an entry are joined by KEY across the halves. A key on one side
        # and not the other is the very empty block V32 forbids - it just opens in one language
        # instead of in both, which is harder to notice rather than easier.
        $ptBody = DescMap $numPt
        foreach ($e in $numEn) {
            [void]$descKeys.Add($e.Key)
            if ($e.Body.Trim().Length -lt 200) { $thin += "'$($e.Key)' has no usable en text" }
            if (-not $ptBody.ContainsKey($e.Key)) { $thin += "'$($e.Key)' is in the [en] half and nowhere in the [pt] one" }
            elseif ($ptBody[$e.Key].Trim().Length -lt 200) { $thin += "'$($e.Key)' has no usable pt text" }
        }
        if ($descKeys.Count -eq 0) { Fail "V32 the DESC region parsed to zero entries - the parser or the generated shape drifted" }
        foreach ($t in $thin) { Fail "V32 $t" }

        # Which pickers this check is about: the ones a RADIO can point at, whose chosen item
        # becomes the DESC key the block looks up. That is the two numina lists and nothing else.
        #
        # It used to be 'every cbo* on the tab except cboFaith' - an exclusion by NAME, which
        # held only while those were the only pickers there. The Hedge Magic box put an ATTRIBUTE
        # picker on the tab in the 90th round (SPEC I60) and the check demanded a book entry for
        # 'Strength'. Naming what is IN scope closes the other door too: a numina list added
        # under a new name cannot slip out of the check by simply not being excluded (SPEC V20).
        # Read straight off PICKER_LIST since T874: wave 4 turned both boxes into pickers, so
        # there is no comboBox left on the tab to walk and the walk would have come back empty -
        # which is this leg's own "no numina picker items found" firing on a correct sheet. The
        # ROOTS are what the rule was always about and they have not moved (SPEC I27, V211): the
        # box builds its pool out of PICKER_LIST[MF.list], so what the player can choose is
        # exactly these two lists (SPEC I113b, V354b).
        # T928 and T929 made it FOUR. The HEDGE MAGIC RITUALS box and the Affiliation picker hand
        # mfOpen the `Numina` kind too, so the item a player picks in either is looked up in this
        # very module - which means their fifty-two and twenty-six names have to be covered by the
        # missing leg, and, more to the point, that the orphan leg below would call every one of
        # them dead text (SPEC I140i, I140l, I140m, V408).
        $numinaLists = @('numina', 'psychic', 'hedgeRitual', 'hedgeAffiliation')
        $numinaItems = @()
        foreach ($nl32 in $numinaLists) {
            if (-not $PICKER.ContainsKey($nl32)) { continue }
            foreach ($it in @($PICKER[$nl32])) {
                if ($it -ne '' -and $numinaItems -notcontains $it) { $numinaItems += $it }
            }
        }
        $missing = @($numinaItems | Where-Object { -not $descKeys.Contains($_) })
        if ($missing) { foreach ($m in $missing) { Fail "V32 picker offers '$m' but DESC has no entry - its radio would open an empty block" } }
        elseif ($numinaItems.Count -eq 0) { Fail "V32 no numina picker items found to check" }
        else { Pass "V32 all $($numinaItems.Count) picker items have en+pt descriptions ($($descKeys.Count) DESC entries)" }
        # 'True Faith' is reached by the fixed key the faith row uses, not by a picker item
        # (SPEC V50), so it is not dead text.
        $orphans = @($descKeys | Where-Object { $numinaItems -notcontains $_ -and $_ -ne 'True Faith' })
        if ($orphans) { foreach ($o in $orphans) { Fail "V32 DESC entry '$o' matches no picker item - dead text or a spelling drift" } }

        # ---- V50: every religion opens the True Faith text ----------------------------
        # V50 measured a MECHANISM the 90th round removed: it asked that the faith ROW be
        # special-cased in the renderer so that any of the four religions resolved to the one
        # True Faith entry. True Faith now has a pane of its own with a single row and no radio,
        # so there is no selection to special-case and the block reads the fixed key directly
        # (SPEC I58, I61). Keeping the old legs would fail correct code (SPEC B7); V265 replaces
        # them in T594 by measuring the opposite - that no picker drives that block at all.
        #
        # The one leg that outlives the mechanism stays here, because it is about the BOOK and
        # not about the widget: the entry the block is hard-wired to has to exist.
        if (-not $descKeys.Contains('True Faith')) { Fail "V50 DESC has no 'True Faith' entry - the True Faith block would open empty (SPEC I61)" }
        else { Pass "V50 the True Faith block has its fixed entry in DESC" }
    }

    # ---- V78: one list per box, split by book chapter (SPEC T236) -----------------
    # Both pickers declare their combo as name="cbo$(field)", so a collector keyed on the
    # combo NAME cannot tell the two boxes apart and every list check on them would read the
    # same bucket (SPEC V20). Read each template BY NAME instead.
    # Twenty-seven since the 94th round: the three books were swept whole and six paths the
    # sheet did not carry came back with a page each - Chronomancy, Dowsing and Spirit-Chasing
    # from the Sorcerer's Companion, Curses and the two Theurgy Vias from HH2 (SPEC I69, R107,
    # R108). The roster is spelled out rather than counted because 'Shadows' (ch.1) and 'Shadow'
    # (ch.2) are one letter apart and a count would pass with the pair swapped.
    $hedgeCanon = @('Alchemy','Chronomancy','Conjuration','Conveyance','Curses','Divination','Dowsing','Ephemera','Enchantment','Fascination','Fortune','Healing','Hellfire','Illusion','Maelstroms','Necromancy','Necronics','Oneiromancy','Quintessence Manipulation','Shadows','Shapeshifting','Spirit-Chasing','Starlight','Summoning, Binding, and Warding','Via Geniorum','Via Necromantiae','Weather Control')
    $psychicCanon = @('Animal Psychics','Anti-Psychic','Astral Projection','Biocontrol','Channeling','Clairvoyance','Cyberkinesis','Cyberpathy','Ectoplasmic Generation','Mind Shields','Precognition','Psychic Healing','Psychic Hypnosis','Psychic Invisibility','Psychic Vampirism','Psychokinesis','Psychometry','Psychoportation','Pyrokinesis','Shadow','Synergy','Telepathy')
    $boxItems = @{}
    # Keyed by the ROOT since T874 and no longer by the template: wave 4 replaced both combos with
    # buttons, and a lookup through the template would find no <comboBox> and report two correct
    # lists as unchecked. The two boxes were always told apart by the field root their rows bind -
    # numina_* against psychic_* - and that is now simply the PICKER_LIST key the button opens on
    # (SPEC I27, I113b, V354b). Nothing about WHAT is compared moved.
    foreach ($box in @(@('numina', $hedgeCanon, 'HedgePicker'), @('psychic', $psychicCanon, 'PsychicPicker'))) {
        $root78 = $box[0]
        $canon = $box[1]
        $tpl = $box[2]
        if (-not $PICKER.ContainsKey($root78)) { Fail "V78 PICKER_LIST has no '$root78' key - the box $tpl opens would offer nothing and its list is unchecked (SPEC V20, V211)"; continue }
        $got = @(@($PICKER[$root78]) | Where-Object { $_ -ne '' })
        $boxItems[$tpl] = $got
        # 'Shadows' (ch.1) and 'Shadow' (ch.2) are different numina one letter apart, so match
        # the names themselves - counting them would pass with the pair swapped.
        $extra = @($got | Where-Object { $canon -notcontains $_ })
        $absent = @($canon | Where-Object { $got -notcontains $_ })
        foreach ($e in $extra) { Fail "V78 $tpl offers '$e' - it belongs to the other box (SPEC R.14)" }
        foreach ($a in $absent) { Fail "V78 $tpl is missing '$a' (SPEC R.14)" }
        if ($extra.Count -eq 0 -and $absent.Count -eq 0) { Pass "V78 $tpl offers exactly its $($canon.Count) book entries" }
        # The items/values leg went with the dropdown in T874, and it went because it CANNOT be
        # measured any more: a combo authored two aligned attributes, and what the box offers now
        # is one list read once (SPEC I113b) - there is no second half left to drift from the
        # first. What it bought - the sheet cannot store a name the box does not offer - is V200's
        # and V346b's, and mfOpen appends the row's own value to the pool for exactly that reason.
    }
    if ($boxItems.Count -eq 2) {
        $shared = @($boxItems['HedgePicker'] | Where-Object { $boxItems['PsychicPicker'] -contains $_ })
        if ($shared) { Fail "V78 offered by BOTH boxes: $($shared -join ', ')" }
        else { Pass "V78 no numina is offered by both boxes" }
        $total = $boxItems['HedgePicker'].Count + $boxItems['PsychicPicker'].Count
        if ($total -eq 49) { Pass "V78 the two boxes still cover all 49 numina" }
        else { Fail "V78 the two boxes cover $total numina, expected 49 - a name was dropped in the split (SPEC V32, I69)" }
    }

    # V33: no silent blank - each unresolved path must produce its own visible text.
    #
    # RETIRED in T874. The three state tables were the PANE's - the pane opened on a selection, so
    # it had to say "nothing marked", "row empty" and "no entry on file" in words. Wave 4 took the
    # panes (SPEC V365d) and the ? has no such states: it is opened BY a row, so there is nothing
    # unmarked to report, and a key the book does not carry falls to the ONE sentence popOpen and
    # mfSelect both say - "No description available", through translateSheetText (SPEC V360c,
    # V364b, V365c). One state instead of three, with one owner instead of a copy per pane.
    #
    # The guard is the retirement's own: the three tables coming back on this tab would be state
    # text nothing reaches, which is what V20 refuses.
    $v33Back = @()
    foreach ($state in @('NO_SELECTION','EMPTY_ROW','NO_ENTRY')) {
        if ($hh7 -match "local $state = \{") { $v33Back += $state }
    }
    if ($v33Back) { Fail "V33 WoD20.7 declares $($v33Back -join ', ') again - the panes those sentences belonged to left in T874 and this ruler retired with them, so the text comes back with nothing reading it (SPEC V365d, V360c, V20)" }
    else { Pass "V33 retired with the four Numina panes in T874 - the three pane states collapse into V360c's one sentence, said by popOpen and mfSelect through the one descText" }
}

# ---- V34: one PT wording for the hedge-magic family ---------------------------
# B.15: the Powers tab shipped four renderings of one term ("magia menor", "mago menor",
# "magos menores", "bruxo menor") and never the canonical one. Locked glossary (SPEC C):
#   hedge magic -> magia estatica | hedge magician & hedge wizard -> mago estatico
#   sorcerer -> feiticeiro
# The regex hunts the BANNED wording, so this file stays ASCII (see the header note) -
# it never has to spell the accented replacement.
$banned = [regex]'(?i)(?:mag(?:o|a|os|as|ia|ias)|brux(?:o|a|os|as))\s+menor(?:es)?'
$bannedHits = 0
# The description modules are scanned too: T478 moved 380 KB of [pt] prose out of WoD20.7.lfm
# and into descNumina.lua, and a glossary check that only reads .lfm would have gone quiet on
# the very text it was written for (SPEC V34, amended in the 64th round).
foreach ($p in (@($files | ForEach-Object { $_.FullName }) + @($descModules | ForEach-Object { $_.FullName }) + @($langFile))) {
    $txt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($p))
    foreach ($m in $banned.Matches($txt)) {
        $ln = ($txt.Substring(0, $m.Index) -split "`n").Count
        Fail "V34 $(Split-Path $p -Leaf):$ln '$($m.Value)' - hedge magic is 'magia estatica', hedge magician/wizard is 'mago estatico' (SPEC B.15)"
        $bannedHits++
    }
}
if ($bannedHits -eq 0) { Pass "V34 no banned hedge-magic wording in the .lfm files, the $($descModules.Count) description module(s) or the .lang" }

# ---- V52..V58: the sheet theme (SPEC 14th round) --------------------------------
# The Theme combo stopped being dead state in the 14th round: its three values are the keys
# a palette table is looked up by, and picking one repaints the whole sheet at runtime.
# Everything below reads the real artifacts - the XML attributes and the Lua source - rather
# than any intermediate the code could be refactored away from (SPEC V20 / B.7).

# The palette table itself, and the names it declares. Everything downstream counts from this
# rather than from a number written into the gate, so a fifth period added later cannot slip
# past a check that still expects four (SPEC V20 / B.7).
$themesMatch = [regex]::Match($hh6, 'local THEMES = \{(.*?)\n\t\t\t\};', 'Singleline')
if (-not $themesMatch.Success) { Fail "V52/V53/V63 THEMES block not found - every palette check is a no-op" }
$themesBlock = $themesMatch.Groups[1].Value
$themeKeys = @([regex]::Matches($themesBlock, '(?m)^\t{4}\["([^"]+)"\] = \{') | ForEach-Object { $_.Groups[1].Value })
if ($themeKeys.Count -eq 0) { Fail "V52/V53/V63 no palette found inside THEMES" }
else { Pass "V52 THEMES declares $($themeKeys.Count) palettes: $($themeKeys -join ', ')" }

$themeFn = [regex]::Match($hh6, 'local function applyTheme\(v, from\)(.*?)\n\t\t\tend;', 'Singleline')
if (-not $themeFn.Success) { Fail "V52..V58 applyTheme not found in WoD20.6.lfm - every theme check below is a no-op" }
else {
    $body = $themeFn.Groups[1].Value

    # V52: the three values, the table that answers them, and the fallback.
    $themeCb = @($listReport | Where-Object { $_.Name -eq 'WoD20.6.lfm/cboSheetTheme' })
    $wantThemes = @('Modern Nights', 'Victorian Age', 'Dark Ages', 'Classical Age')
    if ($themeCb.Count -ne 1) { Fail "V52 cboSheetTheme was never collected - its value list is unchecked" }
    elseif (Compare-Object $themeCb[0].Items $wantThemes) {
        Fail "V52 cboSheetTheme offers {$($themeCb[0].Items -join ', ')}, expected {$($wantThemes -join ', ')}"
    } else { Pass "V52 cboSheetTheme offers the $($wantThemes.Count) declared themes" }

    # The combo saves what it shows, so items and values must not drift apart (SPEC V24).
    $themeNode = (Doc (Join-Path $dir "WoD20.6.lfm")).SelectSingleNode("//comboBox[@name='cboSheetTheme']")
    if ($null -eq $themeNode) { Fail "V52 cboSheetTheme node missing" }
    elseif ($themeNode.GetAttribute("items") -ne $themeNode.GetAttribute("values")) {
        Fail "V52 cboSheetTheme items and values differ - a [pt] sheet would save a translated theme name"
    } else { Pass "V52 cboSheetTheme items and values agree" }

    # (e) T957 renamed the VALUE 'Victorian Era' to 'Victorian Age' (SPEC I146b, Q66.3, V52 as
    # amended). The old spelling may survive in exactly ONE place of code: the migration seed on
    # the root form, which hands a saved sheet the new key through setField. Anywhere else it is
    # a key nobody answers to any more - a palette or PT entry the combo can no longer reach, an
    # ERA_ABILITIES row no era selects, a .lang key with no reader - and a sheet saved before the
    # rename would open on the DEFAULT palette without a sound. Comments are stripped first
    # (CodeOf): the history of the name is allowed to say it.
    $old52   = 'Victorian Era'
    $hits52  = @()
    foreach ($f52 in (Get-ChildItem (Join-Path $dir '*.lfm'))) {
        $n52 = ([regex]::Matches((CodeOf $f52.FullName), [regex]::Escape($old52))).Count
        if ($n52 -gt 0) { $hits52 += "$($f52.Name) x$n52" }
    }
    $lang52 = ([regex]::Matches([System.IO.File]::ReadAllText($langFile), [regex]::Escape($old52))).Count
    $seed52 = [regex]::Match((CodeOf (Join-Path $dir 'WoD20th.lfm')), 'sheet\.sheetTheme == "Victorian Era" then setField\("sheetTheme", "Victorian Age"\)')
    if (-not $seed52.Success) { Fail "V52 (e) the migration seed is gone from the root form - a sheet saved on 'Victorian Era' would open on the DEFAULT palette with a fifth item in the combo (SPEC I146b, V52e)" }
    elseif ($hits52.Count -ne 1 -or $hits52[0] -ne 'WoD20th.lfm x1' -or $lang52 -ne 0) {
        Fail "V52 (e) 'Victorian Era' is still spelled outside the seed - [.lfm: $($hits52 -join ', ')] [localization.lang x$lang52] - the seed rewrites the value and every other reader of the old key is dead code (SPEC I146b, V52e)"
    }
    else { Pass "V52 (e) 'Victorian Era' survives only in the root seed that rewrites it to 'Victorian Age'" }

    # Palette names are read out of the THEMES block at its own indent level, not from a list
    # written here: adding a period to the combo and forgetting its palette has to fail, and so
    # does a palette nothing can reach. Both directions are checked below.
    foreach ($w in $wantThemes) {
        if ($themeKeys -contains $w) { Pass "V52 THEMES answers '$w'" }
        else { Fail "V52 '$w' is offered by the combo but has no THEMES entry - picking it would do nothing" }
    }
    $strayThemes = @($themeKeys | Where-Object { $wantThemes -notcontains $_ })
    if ($strayThemes.Count -gt 0) { Fail "V52 THEMES declares $($strayThemes -join ', ') - the combo does not offer it, so it can never be picked" }
    else { Pass "V52 no palette is stranded outside the combo" }

    # V63: a period declares the whole shape or none of it. With four palettes hand-written,
    # a missing `paper` or `corner` is a sheet painted halfway (SPEC I5).
    # Eight keys since the 16th round: `corner` went away entirely (V66) and `strokeSize`
    # became one shared constant (V67), so neither belongs to a period any more.
    $required = @('form','fill','stroke','font','fontFamily','dotOn','dotOff','paper')
    foreach ($k in $themeKeys) {
        $pal = [regex]::Match($themesBlock, "(?ms)^\t{4}\[""$([regex]::Escape($k))""\] = \{(.*?)^\t{4}\},")
        if (-not $pal.Success) { Fail "V63 palette '$k' could not be read - its shape is unchecked"; continue }
        $missing = @($required | Where-Object { $pal.Groups[1].Value -notmatch "(?m)^\t{5}$_\s*=" })
        if ($missing.Count -gt 0) { Fail "V63 palette '$k' is missing $($missing -join ', ')" }
        else { Pass "V63 palette '$k' declares all $($required.Count) keys" }
    }

    # V54: the ledger records the value the XML authored, once, before that property is first
    # painted. Overwriting it on a later repaint would record Victorian and call it authored -
    # and every later period would then map from the wrong original.
    #
    # The other half of V54 was retired in the 16th round: there is no palette-less option left
    # to restore to, so `Modern` naming a real palette is now correct rather than a violation.
    if ($hh6 -notmatch 'local themePainted = \{\}') { Fail "V54 no ledger - the authored values are not recorded" }
    elseif ($hh6 -notmatch 'if rec\[prop\] == nil then rec\[prop\] = original; end;') { Fail "V54 the ledger is overwritten on repaint - it would record Victorian and call it Modern" }
    else { Pass "V54 the authored value is recorded once and never overwritten" }

    # V66: REVOKED in the letter by the 117th round (SPEC I89d, V316). applyTheme DOES write the
    # corner now - that is the whole of the coffin bevel - but only where sectionBox says yes.
    # What this invariant was BUYING is still bought and merely changed owner: a theme-wide
    # radius must never reach the three rectangles that carry a radius of their own (the health
    # mark box at 2, the avatar frame at 5), and a palette key `corner` = 0 flattening the lot is
    # still forbidden outright. The guard stops being "the write does not exist" and becomes "the
    # write is FENCED", which is a thing this gate can still count.
    #
    # The fence is measured by SUBTRACTION: cut the sectionBox block out of applyTheme and demand
    # that nothing left over mentions a corner property. Asking only that the fence EXISTS would
    # pass a version that also wrote the radius on the way past every pill.
    #
    # The comment strip is inline because NoComments is declared further down this file than this
    # check runs.
    $body66  = [regex]::Replace($body, '(?m)^\s*--.*$', '')
    $fence66 = '(?ms)^(\t+)if sectionBox\(c, fill\) then\r?\n.*?^\1end;'
    # TWO fences since T960 (SPEC V66 as amended, B154, V427d): the six XP frames are fenced by
    # NAME - XP_FRAME[nm] - and their own radius stays the ceiling (V427c). The existence check
    # above still asks for the sectionBox fence alone; the subtraction below cuts both.
    $fences66 = '(?ms)^(\t+)if (sectionBox\(c, fill\)|XP_FRAME\[nm\]) then\r?\n.*?^\1end;'
    if ($themesBlock -match '(?m)^\s+corner\s*=') { Fail "V66 a palette declares ``corner`` again - a theme-wide radius flattens the boxes that set their own" }
    elseif ($body66 -notmatch '"(xradius|yradius|cornerType)"') { Fail "V66 applyTheme writes no corner at all - the bevel of I89 reaches not one box, and every leg of V316 below is measuring a feature that is not there (SPEC V209)" }
    elseif ($body66 -notmatch $fence66) { Fail "V66 the corner write is not fenced by sectionBox - unfenced this branch runs on EVERY rectangle the sheet has and bevels the 19 tab pills, whose width is priced for the concave arc (SPEC V228, V316d)" }
    else {
        $outside66 = [regex]::Replace($body66, $fences66, '')
        if ($outside66 -match '"(xradius|yradius|cornerType)"') { Fail "V66 applyTheme touches a corner property OUTSIDE the sectionBox and XP_FRAME fences - that reaches the pills, the mark boxes and the avatar frame, which is exactly what this invariant has bought since the 16th round" }
        else { Pass "V66 no palette declares ``corner``, and every corner write applyTheme makes is fenced inside sectionBox or the named XP_FRAME roster (V427)" }
    }

    # V67: one outline width, declared once. The requirement is that the four MATCH, and four
    # copies that agree today can drift tomorrow - so the equality is structural, like
    # HEALTH_MARKS on the root form (SPEC V41).
    #
    # V67 AMENDED 2026-09-06 (user): the shared width still governs, and there is now ONE declared
    # way out of it - a palette key named `outline`, fenced by sectionBox so it reaches the boxes
    # and the avatar frame and nothing else. The fence is what has teeth: THEME_STROKE also draws
    # the open sub-tab's foot rule (I140b, V404), so an unfenced per-era width halves the only
    # signal an open tab carries, in an era away from whoever wrote the key (family B6).
    if ($themesBlock -match '(?m)^\s+strokeSize\s*=') { Fail "V67 a palette declares its own strokeSize - the width is either the shared one or the declared `outline` key, not a third spelling" }
    elseif ($hh6 -notmatch 'local THEME_STROKE = (\d+);') { Fail "V67 THEME_STROKE is not declared" }
    else {
        $null = $hh6 -match 'local THEME_STROKE = (\d+);'
        $strokeVal = $Matches[1]

        # The fallback is still THEME_STROKE and the authored value is still the way back out
        # (V316c): drop either and the eras that declare nothing lose their outline the moment the
        # Classical has been on, with rdk -l exiting 0 and this gate green (B58, B62).
        $hairRe = 'paint\(c, "strokeSize", \(hair and t\.outline\) or THEME_STROKE, authored\(c, "strokeSize"\)\);'
        $fenceRe = 'local hair = t\.outline ~= nil and sectionBox\(c, fill\);'
        $outlines = [regex]::Matches($themesBlock, '(?m)^\s+outline\s*=\s*([0-9.]+),')

        if ($body -notmatch $hairRe) { Fail "V67 applyTheme does not paint the shared width in the declared shape - `(hair and t.outline) or THEME_STROKE` with the authored value behind it (SPEC V316c)" }
        elseif ($outlines.Count -gt 0 -and $body -notmatch $fenceRe) {
            Fail "V67 the per-era outline is not fenced by sectionBox - unfenced it thins EVERY rectangle the era repaints, including the open sub-tab's 3px foot rule, which is the only signal that tab has (SPEC I140b, V404)"
        }
        elseif ($outlines.Count -gt 1) { Fail "V67 $($outlines.Count) palettes declare `outline` - the exception is one era wide, and a second is a width nobody asked for in an era nobody asked" }
        elseif ($outlines.Count -eq 1) { Pass "V67 shared outline width $strokeVal for all $($themeKeys.Count) periods, with one fenced exception of $($outlines[0].Groups[1].Value) on the section boxes" }
        else { Pass "V67 one shared outline width ($strokeVal) for all $($themeKeys.Count) periods" }
    }

    # V61: writing a property is NOT free - every font setter detaches the control from the
    # theme before it writes, so writing back a value just read still changes the sheet. That
    # assumption cost B21: Modern rewrote every text control at load and came back wrong.
    if ($hh6 -match 'local function put\(') { Fail "V61 the old unconditional put() is back - it writes properties the XML never authored (SPEC B21)" }
    elseif ($hh6 -notmatch 'if value == nil then return; end;') { Fail "V61 paint() does not refuse an unmapped target - it would write over an unknown colour" }
    elseif ($hh6 -notmatch 'if original == nil then return; end;') { Fail "V61 paint() does not refuse a property the XML never authored - that write could not be undone (SPEC B21)" }
    else { Pass "V61 paint() writes only a mapped value over an authored one" }

    # Everything the repaint does must go through paint(), or the ledger has holes. The backdrop
    # is the one exception: it exists only for the theme, so its authored state is known.
    $direct = @([regex]::Matches($body, 'c\.(\w+)\s*=[^=]') | ForEach-Object { $_.Groups[1].Value } | Sort-Object -Unique)
    $allowedDirect = @('src','visible')
    $strayDirect = @($direct | Where-Object { $allowedDirect -notcontains $_ })
    if ($strayDirect.Count -gt 0) { Fail "V61 applyTheme writes $($strayDirect -join ', ') outside paint() - those changes are not in the ledger" }
    else { Pass "V61 every repaint goes through paint(), only the backdrop is written directly" }

    # V62: restoring reads the ledger, and the ORIGINAL value is what a repaint maps from -
    # otherwise Light -> Dark would look its palette up in a colour Light had already written.
    # V62: restore() went with the palette-less baseline in the 16th round, but the ledger did
    # not - it is what makes Victorian -> Classical map from the ORIGINAL colour instead of the
    # one Victorian left behind.
    if ($hh6 -notmatch 'if rec ~= nil and rec\[prop\] ~= nil then return rec\[prop\]; end;') { Fail "V62 authored() does not prefer the ledger - a second period would map from the first period's colours" }
    elseif ($body -notmatch 'authored\(c, "color"\)') { Fail "V62 the repaint maps from the live value, not the authored one" }
    else { Pass "V62 repaints map from the authored value, not the previous period" }

    # And with no palette-less option left, the lookup must never resolve to nil.
    if ($body -notmatch 'THEMES\[v\] or THEMES\[THEME_DEFAULT\]') { Fail "V52 applyTheme can still resolve to nil - a stale saved value would leave the sheet unpainted" }
    else { Pass "V52 an unknown saved value falls back to the default palette" }


    # V55: the rdk generates findClass starting from `self`, so it only ever reaches the tab
    # it runs on - which is why the five base sheets only ever painted their Credits tab
    # (SPEC R21 / B.9). The theme must use the same root walk the translation does.
    # Match the CALL, not the word: the code comments name findClass to explain why it is not
    # used, and a check that cannot tell those apart is a check nobody can keep green.
    if ($hh6 -match 'findClass\s*\(') { Fail "V55 WoD20.6 calls findClass - it stops at this tab (SPEC R21/B.9)" }
    elseif ($body -notmatch 'rootOf\(from\)') { Fail "V55 applyTheme does not walk to the sheet root - eight tabs would stay unpainted" }
    else { Pass "V55 the repaint walks from the sheet root" }

    # V57: no geometry. The overlap checks above measure the static XML; if the theme moved or
    # resized anything, a sheet could collide at runtime with the gate still green.
    # V57 AMENDED in the 123rd round (SPEC B79, I98a, V327b). Geometry stays FORBIDDEN and the
    # reason is unchanged - V37/V40/V49 measure the static XML, so anything the theme resizes is
    # invisible to them. What changed is that there is now ONE declared exception: the height of
    # the eleven tabStrip labels, which the Classical palette writes through tabLabelHeight so the
    # word can sit in the middle of its button (SPEC I98).
    #
    # The exception is CUT OUT of the body before the count and then measured on its own terms
    # below. Cutting rather than loosening the pattern is the point: a second `height` write
    # anywhere else in applyTheme still turns this red, which is exactly what V57 was buying.
    $tabHRe = 'local lp = c\.parent;[\s\S]*?paint\(c, "height", t\.tabLabelHeight or ah, ah\);'
    $tabH   = [regex]::Match($body, $tabHRe)
    $bodyGeo = if ($tabH.Success) { $body.Remove($tabH.Index, $tabH.Length) } else { $body }

    $geo = [regex]::Matches($bodyGeo, '"(left|top|width|height)"|\.(left|top|width|height)\s*=')
    if ($geo.Count -gt 0) { Fail "V57 applyTheme writes geometry ($($geo.Count) hit(s)) outside the one declared exception - V37/V40/V49 only measure the static XML (SPEC V57, B79)" }
    else { Pass "V57 applyTheme writes no geometry beyond the declared tabStrip label height" }

    # ---- V327: the era writes the top bar's label height, and the column lets go of the word --
    # SPEC I98, V327. Four legs, and the first one is the WAY BACK OUT - the same shape V316c
    # cobbled for the corner, on a second target.
    if (-not $tabH.Success) {
        Fail "V327b the tabStrip label-height write is missing or not in the declared shape - without `t.tabLabelHeight or ah` the eleven labels stay at 30 forever once the Classical era has been on, with rdk -l exiting 0 and this gate green (SPEC V327b, B58, B62)"
    } else {
        Pass "V327b the label-height write carries the authored value as its way back out"
    }

    # (a) exactly ONE palette declares the key. A second is a shape nobody asked for in an era
    # nobody asked, and the symptom shows up one era away from whoever caused it (family B6).
    $tlh = [regex]::Matches($hh6, 'tabLabelHeight\s*=\s*\d+')
    if ($tlh.Count -ne 1) { Fail "V327a $($tlh.Count) palette(s) declare tabLabelHeight, expected exactly 1 (SPEC V327a)" }
    else { Pass "V327a exactly 1 palette declares tabLabelHeight" }

    # (c) the selector is the GRANDPARENT and it reaches the eleven - not ten, not twelve - and
    # reaches none of the eight sub-bar labels, which are authored 30 already (V122).
    # Read OUTSIDE the branch: leg (d) measures against the same strip, and a $stripDoc that only
    # exists on the happy path made the (c) mutation throw instead of failing - a check that dies
    # is not a check that says no (SPEC V20, V209).
    $stripDoc = (Doc (Join-Path $dir "WoD20th.lfm")).SelectSingleNode("//layout[@name='tabStrip']")
    $topLbl   = if ($null -eq $stripDoc) { 0 } else { $stripDoc.SelectNodes("rectangle[@onClick]/label").Count }

    if ($body -notmatch 'lp\.parent\.name == "tabStrip"') {
        Fail "V327c the label-height write is not fenced by the tabStrip grandparent - unfenced it would resize labels that are not the top bar's (SPEC V327c)"
    } elseif ($topLbl -ne 11) {
        Fail "V327c the tabStrip carries $topLbl label(s) under a clickable button, expected 11 - the selector is covering less than the bar has (SPEC V209)"
    } else {
        Pass "V327c the grandparent selector reaches the 11 top-bar labels and none of the 8 sub-tab ones"
    }

    # (d) ORN_COL_EDGE exists inside markCoronaMuralis, and the GAP it buys on the NARROWEST
    # button is measured with the gate's own estimator - a floor with two owners is B69.
    $edgeM = [regex]::Match($hh6, 'local ORN_COL_EDGE\s*=\s*([\d.]+)')
    $wM    = [regex]::Match($hh6, 'local ORN_COL_W\s*=\s*([\d.]+)')
    if (-not $edgeM.Success -or -not $wM.Success -or $null -eq $stripDoc) {
        Fail "V327d ORN_COL_EDGE, ORN_COL_W or the tabStrip itself is missing - there is no column position left to measure (SPEC V209, V327d)"
    } else {
        $edge = [double]$edgeM.Groups[1].Value
        $colW = [double]$wM.Groups[1].Value
        $capEnd = $edge + $colW                      # where the capital closes, in from the rim
        $worst = $null
        foreach ($n in $stripDoc.SelectNodes("rectangle[@onClick]")) {
            $l = $n.SelectSingleNode("label[@text]"); if ($null -eq $l) { continue }
            $bw = 0; [void][int]::TryParse($n.GetAttribute("width"), [ref]$bw)
            $gap = ($bw - (NeededPx $l.GetAttribute("text"))) / 2 - $capEnd
            if ($null -eq $worst -or $gap -lt $worst) { $worst = $gap }
        }
        $GAP_FLOOR = 4
        if ($worst -lt $GAP_FLOOR) { Fail "V327d the narrowest tab leaves ${worst}px between the column and the word, under the $GAP_FLOOR floor - that is the crowding the user named (SPEC I98b, V327d)" }
        else { Pass "V327d the column clears the word on every tab - worst gap $worst px (capital closes at $capEnd) against a floor of $GAP_FLOOR" }
    }

    # V56: an ornament that is not there must degrade to no ornament, never to a sheet that is
    # half painted or covered by a backdrop it cannot switch off.
    if ($body -notmatch 't\.paper == nil') { Fail "V56 applyTheme does not handle a palette with no backdrop" }
    elseif ($body -notmatch 'c\.visible = false') { Fail "V56 the backdrop is never hidden - Modern would keep the paper" }
    else { Pass "V56 a missing backdrop degrades to no backdrop" }
}

# V53: every colour the XML authors must be a key in EVERY palette. Without this a
# box added later keeps its black fill in the middle of the parchment and nothing complains -
# the same blind spot that let ABILITIES ship translucent (SPEC B18).
$paletteOk = $true
foreach ($section in @(
    @{ Attr = 'color';       Lua = 'fill';   Nodes = "//rectangle[@color]" },
    @{ Attr = 'strokeColor'; Lua = 'stroke'; Nodes = "//*[@strokeColor]" },
    @{ Attr = 'fontColor';   Lua = 'font';   Nodes = "//*[@fontColor]" })) {

    $authored = New-Object System.Collections.Generic.HashSet[string]
    foreach ($f in $files) {
        foreach ($n in (Doc $f.FullName).SelectNodes($section.Nodes)) {
            [void]$authored.Add($n.GetAttribute($section.Attr))
        }
    }

    # Anchored to the start of a line: the normaliser below the table declares
    # `local fill, stroke, font = {}, {}, {}` and an unanchored pattern counted that as a map.
    $blocks = @([regex]::Matches($hh6, "(?m)^\s*$($section.Lua)\s*=\s*\{(.*?)\}", 'Singleline'))
    if ($blocks.Count -ne $themeKeys.Count) {
        Fail "V53 expected one '$($section.Lua)' map per palette ($($themeKeys.Count)), found $($blocks.Count) - the check cannot see them all"
        $paletteOk = $false
        continue
    }

    foreach ($b in $blocks) {
        $keys = @([regex]::Matches($b.Groups[1].Value, '\["([^"]+)"\]') | ForEach-Object { $_.Groups[1].Value })
        foreach ($a in $authored) {
            if ($keys -notcontains $a) {
                Fail "V53 '$a' is authored as $($section.Attr)= but is not a key in one of the '$($section.Lua)' maps - it would keep its Modern colour"
                $paletteOk = $false
            }
        }
    }
}
if ($paletteOk) { Pass "V53 every authored colour is mapped by all $($themeKeys.Count) palettes" }

# V58: V3 walks the XML for src=/checkedImage=, so art a palette points at from Lua never got
# checked. A typo there is a dot that silently stops rendering the moment a theme is picked.
#
# V60: and it must be the PLUGIN-ABSOLUTE path. The rdk rewrites src=/checkedImage= at compile
# time - the generated Lua carries "/WoD20th/images/prime_on.png" - so a relative path
# handed to a setter at runtime resolves to nothing and the art quietly fails to load. This
# check measures the FORM of the path; V58 below measures the file. B20 passed V58 green while
# every dot in both Victorian themes was broken, because only the file was ever checked.
$artPrefix = '/WoD20th/images/'
$themeArt = @([regex]::Matches($hh6, '(?:dotOn|dotOff|paper)\s*=\s*"([^"]+)"') | ForEach-Object { $_.Groups[1].Value } | Sort-Object -Unique)
if ($themeArt.Count -eq 0) { Fail "V58/V60 no theme art referenced - the palettes point at nothing" }
else {
    foreach ($img in $themeArt) {
        if (-not $img.StartsWith($artPrefix)) {
            Fail "V60 palette path '$img' is not plugin-absolute - the rdk only resolves the relative form at compile time (SPEC B20)"
            continue
        }
        # Strip only the sheet folder: what is left ("images/x.png") is the path on disk.
        $onDisk = Join-Path $dir ($img.Substring('/WoD20th/'.Length))
        if (Test-Path -LiteralPath $onDisk) { Pass "V58/V60 theme art $img" }
        else { Fail "V58 theme art $img is referenced by a palette but missing from images/" }
    }
}

# V65: fixed art follows the palette too. The first dot of an attribute or a virtue cannot be
# switched off, so it is a plain <image> rather than an imageCheckBox - and being neither a
# colour nor a checkbox, it fell through every net and stayed white in all four periods (B22).
# V53 makes this promise for every colour the XML authors; this is the same promise for art.
$fixedArt = @()
foreach ($f in $files) {
    foreach ($n in (Doc $f.FullName).SelectNodes("//image[@src]")) {
        if ($n.GetAttribute("name") -like 'themePaper*') { continue }
        $fixedArt += [pscustomobject]@{ File = $f.Name; Leaf = (Split-Path $n.GetAttribute("src") -Leaf) }
    }
}
if ($fixedArt.Count -eq 0) { Fail "V65 no fixed <image src> found - the check has nothing to measure, so it proves nothing" }
else {
    $unmapped = @($fixedArt | Where-Object { @('prime_on.png','prime_off.png') -notcontains $_.Leaf })
    if ($unmapped.Count -gt 0) {
        foreach ($u in $unmapped) { Fail "V65 $($u.File) shows fixed art '$($u.Leaf)' that no palette remaps - it would stay Modern inside every period" }
    } else { Pass "V65 all $($fixedArt.Count) fixed <image src> use art the palettes remap" }
}
if ($hh6 -notmatch 'local DOT_ON\s+= "prime_on\.png";') { Fail "V65 the base dot name is gone - applyTheme cannot recognise the fixed dot" }
elseif ($themeFn.Success -and $themeFn.Groups[1].Value -notmatch 'art == DOT_ON') { Fail "V65 applyTheme no longer remaps the fixed dot (SPEC B22)" }
else { Pass "V65 applyTheme remaps the fixed dot to the palette" }

# V64: and nothing else lives in images/. Art belongs to a theme; when a theme is dropped its
# art has to go with it, or the folder turns into a graveyard that still ships inside the .rpk.
# The 15th round deleted the light Victorian, and its three files with it.
$referencedArt = New-Object System.Collections.Generic.HashSet[string]
foreach ($img in $themeArt) { [void]$referencedArt.Add((Split-Path $img -Leaf)) }
foreach ($f in $files) {
    foreach ($n in (Doc $f.FullName).SelectNodes("//*[@src] | //*[@checkedImage] | //*[@uncheckedImage]")) {
        foreach ($a in @('src','checkedImage','uncheckedImage')) {
            if ($n.HasAttribute($a)) { [void]$referencedArt.Add((Split-Path $n.GetAttribute($a) -Leaf)) }
        }
    }
}
$orphanArt = @(Get-ChildItem -LiteralPath (Join-Path $dir "images") -Filter *.png |
                Where-Object { -not $referencedArt.Contains($_.Name) })
if ($orphanArt.Count -gt 0) {
    foreach ($o in $orphanArt) { Fail "V64 images/$($o.Name) is referenced by nothing - art of a dropped theme still shipping in the .rpk" }
} else { Pass "V64 all $($referencedArt.Count) images in images/ are referenced by the XML or a palette" }

# The backdrop is declared per tab, and Settings is left out ON PURPOSE: if the draw order or
# hitTest ever behaved differently than assumed, a full-tab image over the Settings tab would
# cover the very combo used to switch back to Modern.
$paperTabs = @()
foreach ($f in $files) {
    $n = (Doc $f.FullName).SelectSingleNode("//image[starts-with(@name,'themePaper')]")
    if ($null -ne $n) {
        $paperTabs += $f.Name
        if ($n.GetAttribute("hitTest") -ne 'false') { Fail "V56 $($f.Name) backdrop does not set hitTest=false - it would eat clicks" }
        if ($n.GetAttribute("visible") -ne 'false') { Fail "V56 $($f.Name) backdrop is not hidden - Modern would ship with paper" }
        foreach ($g in @('left','top','width','height')) {
            if ($n.HasAttribute($g)) { Fail "V57 $($f.Name) backdrop declares $g - it must be align=client so the box checks stay meaningful" }
        }
    }
}
# Read off the files rather than counted off the root's tabs: the 51st round split ONE tab
# into a shell plus three sub-forms (SPEC I16), so "one backdrop per root <tab>" stopped
# describing the sheet. What actually decides it is whether a form carries CONTENT - every
# content form wraps its widgets in a <scrollBox>, and a shell that only holds a tabControl
# has none. Derived, so a tab added later is picked up without editing a number here.
$wantPaper = @($files | Where-Object {
    $_.Name -ne 'WoD20.6.lfm' -and $null -ne (Doc $_.FullName).SelectSingleNode("//scrollBox")
} | ForEach-Object { $_.Name })
$missPaper = @($wantPaper | Where-Object { $paperTabs -notcontains $_ })
$extraPaper = @($paperTabs | Where-Object { $wantPaper -notcontains $_ })
if ($paperTabs -contains 'WoD20.6.lfm') { Fail "V56 the Settings tab has a backdrop - a covered theme combo cannot be switched back" }
elseif ($missPaper) { Fail "V56 $($missPaper -join ', ') carries content but no backdrop" }
elseif ($extraPaper) { Fail "V56 $($extraPaper -join ', ') carries a backdrop but no content to put it behind" }
else { Pass "V56 all $($wantPaper.Count) content forms carry a hidden, click-through backdrop; Settings and the Vampire shell have none" }

# ---- V190: align=client is exclusive per container -----------------------------------
# SPEC B41. The backdrop is align=client and so is a tabControl; put side by side they claim
# one client rect and whichever loses is simply not drawn - which is how the Vampire tab
# shipped with its sub-tabs opening onto nothing. The sheet had obeyed this for fifty rounds
# without it ever being written down: one align=client per container, everything else placed
# with left/top/width/height.
$alignClash = @()
foreach ($f in $files) {
    foreach ($n in (Doc $f.FullName).SelectNodes("//*")) {
        $kids = @($n.ChildNodes | Where-Object { $_.NodeType -eq 'Element' -and $_.GetAttribute('align') -eq 'client' })
        # The tab panes are the one licensed exception (SPEC I32, T521): N align=client siblings
        # with at most one authored visible. B41 does NOT forbid this and never did - there the
        # two siblings were both VISIBLE (the theme paper lit OVER the tabControl), so it was
        # overlap, not sizing. The Firecast probe of T521 answered the case B41 never tested.
        # Scoped, not revoked: every pane must hold an <import> and none may be authored open,
        # so a second visible client sibling anywhere still reddens this.
        $panes = @($kids | Where-Object { $_.SelectSingleNode("import") })
        if ($panes.Count -eq $kids.Count -and $panes.Count -gt 1) {
            $openPanes = @($panes | Where-Object { $_.GetAttribute('visible') -ne 'false' })
            if ($openPanes.Count -eq 0) { continue }
        }
        if ($kids.Count -gt 1) {
            $who = ($kids | ForEach-Object { "$($_.LocalName)/$($_.GetAttribute('name'))" }) -join ' + '
            $alignClash += "$($f.Name) <$($n.LocalName)>: $who"
        }
    }
}
if ($alignClash) { foreach ($a in $alignClash) { Fail "V190 two controls claim one client rect - $a" } }
else { Pass "V190 no container hands align=client to more than one child" }

# ---- V188: the Vampire tab is ONE tabControl with three SIBLING tabs -----------------
# 54th round. It shipped as two nested controls - a `Blood Sorcery` tab holding a second
# tabControl with `Paths` and `Rituals` inside it - and the inner control drew its tabs
# without their labels, so the sub-tabs were two unmarked click targets (SPEC R49). The
# grouping moved into the titles instead, which is why the titles are checked here and not
# just the names: `Blood Sorcery: Paths` is now the ONLY thing telling the player those two
# tabs belong together, so a title edited back to `Paths` silently loses the grouping.
#
# Read off the XML because that is the point of the invariant: the structure is authored,
# never built by Lua, so a sheet whose script did not run still opens with it (SPEC V18, V94).
$vampX   = Doc (Join-Path $dir "WoD20.11.lfm")
# Rewritten for the strip (SPEC I32, T523): the control is gone, the CONTRACT is not. Three
# sibling panes, one per file, and the grouping still lives in the titles - which moved from
# @title into each button's label, so that is where it is read now.
# AMENDED in the 159th round (SPEC V188 amended, T832): the SUB-TAB half changed subject, the
# tabControl half below did not. The Ghoul tab lost its strip and its three stacked panes became
# three COLUMNS side by side, all visible (SPEC I118, I118d) - so there is no btnTab*/lblOn* left
# to carry a TITLE, and "the grouping lives in the title" dies with the buttons: there is nothing
# to group once all three are on screen at once, which is the whole of pedido 5.
#
# The roster of the three columns LEFT in T830 (SPEC V188 amended, V361a, T830). It was held here
# on loan from T832: the Ghoul was the only rearranged tab then, and V361 had no second tab to
# compare it against, so deleting the roster before V361 existed would have left three columns
# with no ruler at all for a whole task (B7 with a date on it). Both tabs are rearranged now and
# V361(a) asks the SAME three questions of both - count, identity, hidden, name outliving
# selectTab - so a copy here would be a second owner for one ruler (SPEC V135, B70).
#
# What stays is what was never on loan: the ghost sweep below and the tabControl census after it.
$vampBad = @()
# The strip's own controls have to be gone with it, not merely unreferenced: a btnTab left in the
# XML is a click target that calls selectTab for a group SUB_TABS no longer holds.
foreach ($ghost188 in @('vampStrip', 'btnTabDisc', 'btnTabPaths', 'btnTabRituals', 'tabOnDisc', 'tabOnPaths', 'tabOnRituals')) {
    if ($null -ne $vampX.SelectSingleNode("//*[@name='$ghost188']")) { $vampBad += "WoD20.11 still carries '$ghost188' - the Ghoul strip went out in T832 and a leftover button calls selectTab for a group that is not in SUB_TABS any more (SPEC I118d, V268)" }
}
if ($vampBad) { foreach ($b in $vampBad) { Fail "V188 $b" } }
else { Pass "V188 no control of the Ghoul strip outlived it in WoD20.11 - the roster of the three columns moved to V361(a) in T830, where both rearranged tabs answer it together" }

# No tabControl inside a tabControl, in any file: that is the shape R49 broke. Checked per
# file because a nested control is authored in one - the root reaching WoD20.11 through an
# <import> is the arrangement that WORKS and is what the two names below account for.
$nestedCtl = @()
foreach ($f in $files) {
    foreach ($n in (Doc $f.FullName).SelectNodes("//tabControl//tabControl")) {
        $nestedCtl += "$($f.Name): $($n.GetAttribute('name')) sits inside another tabControl"
    }
}
$allCtl = @()
foreach ($f in $files) {
    foreach ($n in (Doc $f.FullName).SelectNodes("//tabControl")) { $allCtl += "$($f.Name)/$($n.GetAttribute('name'))" }
}
# The census flipped from "exactly two" to "none at all" in the 78th round (SPEC I32). Both
# strips are ours now, and a native tabControl coming back is not a style slip - it is a
# control the theme cannot reach, because gui.TabItem has no colour, font or background to
# set and rdk refuses the attributes outright (SPEC B19, B49, V226).
if ($nestedCtl) { foreach ($n in $nestedCtl) { Fail "V188 $n - R49 says the inner one loses its tab labels" } }
elseif ($allCtl.Count -ne 0) { Fail "V188 the sheet carries $($allCtl.Count) native tabControl(s) ($($allCtl -join ', ')) - both strips are ours since I32 and a native one cannot be themed" }
else { Pass "V188 no native tabControl left in the sheet - both strips are ours" }

# ---- V198: the description require is deferred, and the module is data only ----------
# SPEC I24 / V198. The <script> CDATA of a .lfm is copied RAW into constructNew_<form>() -
# you can read it back in output/rdkObjs/WoD20th/*.lfm.lua - so a require at the top of
# the CDATA runs once per sheet OPENED, not once per app. The .lfm shrinks, the .rpk shrinks,
# the parse does not, and the gate stays green while nothing was actually deferred: the same
# shape of silent failure as B.6. So this measures POSITION, not existence.
#
# Position is read off the indentation, which is what the files are actually written with: the
# nearest preceding line with FEWER leading tabs must be a `function` header. Mutation (SPEC
# V20): move the require to the top of the CDATA and this goes red, because the shallower line
# above it is then `<![CDATA[`.
# T495: the require names a HALF and builds it from the language - require("desc<X>_" .. lang
# .. ".lua") - so the anchor is the module STEM, not a whole file name. Left on the old literal
# this check would have matched nothing and passed by omission, which is B7 walking in through
# the door held open by the check written to shut it (SPEC V209, V210b).
# WIDENED in T874: wave 4 took the last direct `require("descNumina_" .. half .. ".lua")` off the
# sheet with the four Numina panes, and every description module is loaded through a GUARDED
# `pcall(require, "desc" .. ...)` now - descText for the ? and the pane, mfOpen for the warm-up
# (SPEC V364a, V365d). The old shape asked for `require(` and would have read ZERO from this
# round on, which is the zero-guard below firing on a sheet that is entirely correct - and, worse,
# the leg would have stopped watching the two requires that DO exist. What is measured is
# unchanged: where the parse SITS (SPEC I24, V198).
$reqRx = [regex]'require\s*[(,]\s*["'']desc'
$reqSeen = 0
$reqBad  = @()
foreach ($f in $files) {
    $txt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName)) -replace "`r`n", "`n"
    $lines = $txt -split "`n"
    for ($i = 0; $i -lt $lines.Count; $i++) {
        if (-not $reqRx.IsMatch($lines[$i])) { continue }
        $reqSeen++
        $ind = ($lines[$i] -replace '^(\t*).*$', '$1').Length
        $enclosing = $null
        for ($j = $i - 1; $j -ge 0; $j--) {
            if ($lines[$j].Trim() -eq '') { continue }
            $jind = ($lines[$j] -replace '^(\t*).*$', '$1').Length
            if ($jind -ge $ind) { continue }
            $enclosing = $lines[$j]
            break
        }
        # \bfunction\b and not (?:^|\s)function\b: the enclosing header can be ANONYMOUS -
        # setTimeout(function() is a function body every bit as much as unction mfOpen( is,
        # and I112e puts the descMerit require inside exactly that one. The old form asked for a
        # space before the word and so read setTimeout(function() as NOT a function, which
        # reddens correct code. The mutation this leg exists for is untouched: at the top of the
        # CDATA the shallower line is <![CDATA[, which carries no unction either way.
        if ($null -eq $enclosing -or $enclosing -notmatch '\bfunction\b') {
            $reqBad += "$($f.Name):$($i + 1) '$($lines[$i].Trim())' sits outside a function body - it would run on every sheet open (SPEC I24, V198)"
        }
    }
}
if ($reqBad) { foreach ($r in $reqBad) { Fail "V198 $r" } }
elseif ($reqSeen -eq 0) { Fail "V198 no .lfm requires a description module - the check reads nothing (SPEC V20)" }
else { Pass "V198 all $reqSeen description require(s) sit inside a function body, so the parse is paid once and cached" }

# ---- V206: the description require is not REACHABLE from onNodeReady ----------------
# SPEC I26 / V206, the backprop of B47. V198 above measures where the require SITS, and it has
# sat in the right place since T478 with the gate green - while onNodeReady called the renderer
# on EVERY open and dragged all four modules (2,754,357 bytes) in behind it. Position in the
# FILE is not reach in the FLOW. V198 was written citing B6 and fell to B6; this is the other
# half, and the two only work as a pair. Same lesson as B30 and B34: the gate reads source and
# cannot count a call at runtime, so what it cannot count it must forbid outright.
#
# Two legs, because the fix has two and either alone is a half-fix (SPEC I26):
#   (a) no render<Area>Desc call inside an onNodeReady event, in any .lfm;
#   (b) every render<Area>Desc body opens with the isShowing guard - onShow firing for a form
#       inside an INACTIVE tab is not measured yet (SPEC R92b), so the trigger alone is a bet.
# Mutation (SPEC V20), one per leg: put renderDiscDesc(self) back in WoD20.12's onNodeReady -> (a)
# goes red; drop the guard from any of the four renderers -> (b) goes red.
$readyRx    = [regex]'(?s)<event\s+name="onNodeReady"\s*>(.*?)</event>'
$descCallRx = [regex]'\brender\w*Desc\s*\('
$v206Bad    = @()
$v206Seen   = 0
foreach ($f in $files) {
    $txt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName)) -replace "`r`n", "`n"

    foreach ($m in $readyRx.Matches($txt)) {
        foreach ($c in $descCallRx.Matches($m.Groups[1].Value)) {
            $v206Bad += "$($f.Name) reaches $($c.Value)) from onNodeReady - that fires on every open, before the tab is on screen, and pulls the whole book module in with it (SPEC B47)"
        }
    }

    # (b) RETIRED in T874, and it retired because its subject did. The isShowing guard was the
    # second contract under a PANE that painted itself on onShow; wave 4 took the last four panes
    # off the sheet (SPEC V365d) and there is no render<Area>Desc left anywhere. What replaced it
    # is stronger rather than equal: the parse is reached from popOpen and from mfOpen, which are
    # CLICKS - strictly later than onShow, and later than any bet about whether onShow fires for a
    # form in an inactive tab (SPEC R92b). Kept as a loop over an empty set it would report PASS
    # over nothing, which is B7 with its own check id.
    #
    # What stays is the retirement's own guard, and it is leg (a) widened: a renderer coming BACK
    # would come back UNMEASURED, so its mere existence is refused here rather than passing.
    $lines = $txt -split "`n"
    for ($i = 0; $i -lt $lines.Count; $i++) {
        if ($lines[$i] -match '^\s*function\s+(render\w*Desc)\s*\(') {
            $v206Bad += "$($f.Name):$($i + 1) declares $($Matches[1]) - the description panes left with waves 3 and 4, and the isShowing guard that used to be charged here retired with them, so a renderer that comes back comes back with no ruler on it (SPEC V365d, B105, T872, T874)"
        }
    }
}
if ($v206Bad) { foreach ($b in $v206Bad) { Fail "V206 $b" } }
else { Pass "V206 retired with the last four panes in T874 - no .lfm declares a description renderer and none is reached from onNodeReady; the book is parsed from popOpen and mfOpen, which are clicks and land strictly after onShow (SPEC I26, V365d)" }

# ---- V207: renderVampPickers runs at most ONCE per open ------------------------------
# SPEC V207, the backprop of B48. T484 wired the filter into the onNodeReady of BOTH WoD20.13
# and WoD20.14 because each tab asked for its own without looking at its sibling. The filter is
# the SHEET's, not a tab's (SPEC V203): renderVampPickers xpFinds from the ROOT and refilters
# all 45 combos in one pass, so the second call re-walked 1900 controls to write the very same
# lists again. Nothing LOOKED wrong - it was pure work - which is exactly why no check caught
# it, and why this one counts the wiring instead of trying to measure an effect.
#
# dataLink onChange calls are deliberately NOT counted: those fire when a row actually
# changes, which is when the lists genuinely have to be rebuilt (SPEC V181, V200). Only the
# OPEN path is capped.
#
# Zero is red as well (SPEC V20 / B7): with no call on the open path a SAVED sheet would open
# offering rituals the character cannot learn, and a check phrased only as "at most one" would
# call that clean - the same silent no-op B7 is made of. Mutation, one per leg: put
# renderVampPickers(self) back in WoD20.14 onNodeReady -> red on the count; delete WoD20.13 as well
# -> red on the zero-guard.
#
# RETIRED in the 159th round (SPEC V207, T872) and ABSORBED BY V205. renderVampPickers was
# deleted with the dropdowns it filtered, so there is no call left to count - and what this
# check BOUGHT (the expensive pass does not run twice) is exactly V205's new leg: discLevels()
# and hedgePathLevels() read ONCE per opening of mfOpen, never once per item. Amending it
# without a subject would give two names to one rule and leave a dead V standing, which is
# B70 being born again (V135).
#
# The one thing kept: the name must stay gone. A renderVampPickers that comes back would be a
# second filter beside the pool build, and the two would disagree the first time one changes.
#
# CodeOf and not the raw bytes: three files still EXPLAIN in prose why the call is gone, and a
# check that reads a comment as if it were code fails on the note that documents the fix.
$vampGhost = @()
foreach ($f in $files) {
    if ((CodeOf $f.FullName) -match '\brenderVampPickers\b') { $vampGhost += $f.Name }
}
if ($vampGhost) { Fail "V207 renderVampPickers is back in $($vampGhost -join ', ') - wave 3 replaced it with the pool build inside mfOpen, and a second filter beside that one is two rules for one list (SPEC V205 amended, V135, T872)" }
else { Pass "V207 retired with renderVampPickers in T872 - the once-per-open guarantee is V205's leg on mfOpen now" }

# The module is DATA (SPEC I24): one table, no function, no state, no SDK require. A module
# that pulled in gui/ndb/locale would be a second door for building the block, and V31's
# "one way to render" would stop being true.
$modBad = @()
foreach ($m in $descModules) {
    $txt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($m.FullName)) -replace "`r`n", "`n"
    $code = ($txt -split "`n" | Where-Object { $_ -notmatch '^\s*--' }) -join "`n"
    if ($code -notmatch '(?m)^\s*return\s*\{') { $modBad += "$($m.Name) does not return a table - require would hand the renderer a nil" }
    if ($code -match '(?m)^\s*(local\s+)?function\b') { $modBad += "$($m.Name) declares a function - the module is data, not code (SPEC I24)" }
    if ($code -match 'require\s*\(\s*["''](?:gui|ndb|locale|firecast|rrpg)') { $modBad += "$($m.Name) requires an SDK module - a data table must not reach the host (SPEC I24)" }
}
if ($modBad) { foreach ($m in $modBad) { Fail "V189 $m" } }
elseif ($descModules.Count -eq 0) { Fail "V189 no desc*.lua module found beside module.xml - the text has nowhere to live (SPEC I24)" }
else { Pass "V189 all $($descModules.Count) description module(s) return one table and nothing else" }

# ---- V210: one description module per language, and no require names a constant --------
# SPEC T495 / R92f: half of every desc<X>.lua was the language the session never opens, so
# requiring one module meant parsing two. Two legs:
#   (a) the two halves of an area carry the SAME keys, 1:1 - the split was a move, not a cut,
#       and losing content is the one thing the user ruled out;
#   (b) no .lfm requires a description module under a CONSTANT name - the language enters by
#       concatenation, so no renderer can end up nailed to one half in silence.
# The medieval body follows the same line: daEn belongs in the [en] file and daPt in the [pt]
# one (SPEC T495). There is no daEn/daPt written yet, so that leg is armed and quiet.
#
# Mutation (SPEC V20): delete one key from one half -> red on (a); put a literal
# require("descDisc.lua") back into WoD20.12 -> red on (b). Probe (SPEC V222b): edit the BODY
# of an entry -> green, because the shape of the text belongs to V192 and V273, not here.
$v210Bad = @()
$v210Keys = 0
# quantas entradas cada metade carrega, medido AQUI e lido por V335e: a regeneracao de T768
# reescreve o bloco 3 dos 8 e uma entrada que sumisse no caminho passaria em V210a (as duas
# metades cairiam juntas) e so apareceria numa contagem.
$v210Count = @{}
# The roster is a GLOB and no longer a literal list (SPEC T934, V210 amended 2026-09-03). The
# literal one was written so that nobody could add an area in silence, and it produced the exact
# opposite: whoever adds an area does not touch this rule, and this rule does not complain. It
# was TWO areas short on 2026-09-03 - descNature and descRoad - and the mutation measured what
# that costs: dropping one key from descRoad_pt.lua left the WHOLE gate green, 0 FAIL, because
# the fifty-five roads of T931 were born outside every parity rule there is. descNature escaped
# only by accident, since V366 asks the same question about that one pair for its own reasons -
# coverage that depends on another rule having cared is not coverage.
#
# The marker comes from DESC_MARKER, the one V408 and V411 also read. An area with a module on
# disk and no marker is a FAIL and not a `continue`: skipping in silence trades one bin for
# another, which is the thing V408 was written to refuse (SPEC I140k, I140m).
#
# Built through an ArrayList and not `+= , @(...)`: a one-element collection is not a collection
# in PowerShell and a pair appended the plain way arrives as its first CHARACTER (SPEC I142k).
$v210Roster = New-Object System.Collections.ArrayList
foreach ($f210e in (Get-ChildItem -LiteralPath $plugin -Filter "desc*_en.lua" | Sort-Object Name)) {
    $area210 = $f210e.BaseName -replace "_en$", ""
    $kind210 = $area210 -replace "^desc", ""
    if (-not $DESC_MARKER.ContainsKey($kind210)) {
        $v210Bad += "$($f210e.Name) is on disk and DESC_MARKER names no marker for '$kind210' - an area whose region this rule cannot find is an area it certifies without reading, which is the bin V408 refuses (SPEC V210, V408, V20)"
        continue
    }
    [void]$v210Roster.Add(@($area210, $DESC_MARKER[$kind210]))
}
$v210Areas = $v210Roster.ToArray()
# Zero-guard as a MEASURED floor and not `-gt 0`: a glob that stopped matching and a deleted half
# both read zero, and `-gt 0` waves through a roster that lost seven of eight. Eight is what the
# plugin root held on 2026-09-03. It is a FLOOR - a ninth area is welcome and costs nothing here.
if ($v210Areas.Count -lt 8) {
    $v210Bad += "the area roster read $($v210Areas.Count) module(s) at the plugin root and eight were measured there on 2026-09-03 - a roster that shrank is a deleted half or a glob that stopped matching, and both make every leg below pass over nothing (SPEC V20, B7)"
}
foreach ($a210 in $v210Areas) {
    $half210 = @{}
    $gone210 = $false
    foreach ($lang210 in @('en', 'pt')) {
        $half210[$lang210] = DescEntries (Join-Path $plugin "$($a210[0])_$lang210.lua") $a210[1] $lang210
        if ($null -eq $half210[$lang210]) { $v210Bad += "$($a210[0])_$lang210.lua is not at the plugin root, or its $($a210[1]) markers are gone - half the book has nowhere to live (SPEC V209)"; $gone210 = $true }
    }
    if ($gone210) { continue }
    if ($half210['en'].Count -eq 0 -or $half210['pt'].Count -eq 0) {
        $v210Bad += "$($a210[0]) parsed to $($half210['en'].Count) [en] and $($half210['pt'].Count) [pt] entries - a side with nothing in it makes every comparison below true by default (SPEC V209)"
        continue
    }
    $v210Keys += $half210['en'].Count
    foreach ($lang210 in @('en', 'pt')) { $v210Count["$($a210[0])|$lang210"] = $half210[$lang210].Count }
    $set210 = @{}
    foreach ($lang210 in @('en', 'pt')) {
        $s = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
        foreach ($e210 in $half210[$lang210]) { [void]$s.Add($e210.Key) }
        $set210[$lang210] = $s
    }
    foreach ($k210 in $set210['en']) { if (-not $set210['pt'].Contains($k210)) { $v210Bad += "$($a210[0]) carries '$k210' in [en] and not in [pt] - the split dropped text the user ruled out dropping (SPEC V210a)" } }
    foreach ($k210 in $set210['pt']) { if (-not $set210['en'].Contains($k210)) { $v210Bad += "$($a210[0]) carries '$k210' in [pt] and not in [en] - the split dropped text the user ruled out dropping (SPEC V210a)" } }
    # daEn to the [en] half, daPt to the [pt] one: a medieval body written into the wrong file
    # is text the renderer of that language can never reach (SPEC T495, T470).
    foreach ($lang210 in @('en', 'pt')) {
        $wrong210 = $(if ($lang210 -eq 'en') { 'daPt' } else { 'daEn' })
        foreach ($e210 in $half210[$lang210]) {
            if ($e210.Rest -match "\b$wrong210\s*=") { $v210Bad += "$($a210[0])_$lang210.lua '$($e210.Key)' carries a $wrong210 body - the medieval text of a language belongs in that language's file (SPEC V210, T495)" }
        }
    }
}
# (b) read off the code with the comments stripped: this sheet is thick with prose that NAMES
# the old call, and a check that reddens on a comment is the false positive V222 was written
# for - it was measured twice already, on 'denied' and on maxDiscLevel(.
$v210Req = 0
foreach ($f210 in $files) {
    $code210 = CodeOf $f210.FullName
    # Both spellings, since T874: `require("descX_" .. lang)` was the panes' shape and the panes
    # are gone (SPEC V365d), while `pcall(require, "desc" .. kind .. "_" .. lang)` is descText's
    # and mfOpen's. A CONSTANT is a constant either way - it always ends in .lua, because a name
    # built by concatenation never carries the extension in its first literal - and the counter
    # has to see the surviving form or leg (b) reads zero on a correct sheet (SPEC V209, B7).
    foreach ($m210 in [regex]::Matches($code210, 'require\s*[(,]\s*"desc[^"]*\.lua"')) {
        $v210Bad += "$($f210.Name) has $($m210.Value) - a constant module name pins that reader to one language and the other half would never be read (SPEC V210b)"
    }
    $v210Req += ([regex]::Matches($code210, 'require\s*[(,]\s*"desc[A-Za-z0-9]*_?"\s*\.\.')).Count
}
if ($v210Req -eq 0) { $v210Bad += "no .lfm builds a description module name out of the language - leg (b) read nothing and would pass on anything (SPEC V209, B7)" }
if ($v210Bad) { foreach ($b210 in $v210Bad) { Fail "V210 $b210" } }
else { Pass "V210 $($v210Areas.Count) areas in $($v210Areas.Count * 2) modules, $v210Keys entries keyed alike on both sides, and all $v210Req require(s) name the half by language" }

# ---- V335: a power description carries the System block and no prose (SPEC I104, T768) ----
# T768 rewrote block 3 of the eight power modules to hold only the mechanics: the discipline's
# introductory prose and each level's narrative prose came out, and what the book prints under
# `System:` stayed (SPEC I104b). This check is what stops that regeneration from being undone,
# and from having eaten something on the way in.
#
# Five legs plus the zero-guard:
#   (a) blocks 1 and 2 of I21 survived VERBATIM - three blocks and block 2 is still the key;
#   (b) every LEVEL block (the one that opens with a dot) carries the system marker - `System`
#       in _en, `Sistema` in _pt. A level block without it is either prose that stayed or
#       mechanics that went, and both are errors;
#   (c) the exception is a NAMED, FINITE list, written out below and MEASURED by
#       research/gen_desc_system.ps1 - never an `if empty then skip`. A name on the list that
#       no longer fires is stale and reddens too, so the list cannot rot into a blanket pardon;
#   (d) no entry with an EMPTY block 3 - V210a catches an entry that VANISHED, not one that
#       went hollow, and hollow is exactly what a bad cut produces (Q9);
#   (e) the entry count per file has not dropped below the number T768 measured - a LITERAL,
#       because V210a counts the same files in the same run: an entry deleted from BOTH halves
#       keeps V210a green and only a fixed number sees it (SPEC B7).
#
# descNumina is the INVERSE form and leg (b) knows it (SPEC I104e, R133): there `System` is a
# section HEADER on a line of its own, without the colon, and the dot levels live INSIDE it
# next to `Modifiers:` `Time:` `Duration:` `Effects:` `Price of Failure:`. When block 3 opens
# with that header the whole block IS the mechanics and there is nothing to ask per level.
# Reading I104b onto descNumina is what would have wiped the mechanics off 50 numina in
# silence, which is why gen_desc_system.ps1 refused -Apply until the rule was written.
#
# The LADDER is why a bullet is not automatically a level: a level header is the NEXT dot of
# the ladder (1, then 2, then 3). `Auspex` puts `* Obfuscate:` `* Chimerstry:` `* Other
# Powers:` as a LIST inside one level's System, and `Sanguinus` and `Grave~s Decay` do the
# same. Counting those as levels cut the owner's System in half (SPEC R133, I104c(i)).
#
# Mutation (SPEC V20): drop the `System:` line out of one level -> red on (b); blank one
# entry's block 3 -> red on (d); delete an entry from one module -> red on (e); take one name
# off the list below while its entry still lacks the marker -> red on (b); leave a name on the
# list whose entry now carries the marker -> red on (c).
$v335Bad = @()
$DOT335 = [char]0x2022
# `~` stands for the curly apostrophe: this file is 100% ASCII on purpose, because PowerShell
# 5.1 reads a .ps1 without BOM as ANSI and a literal accented char in a comparison arrives
# mojibake and fails SILENTLY (SPEC B84). Nothing in these names is a real tilde.
$v335ExcRaw = @'
descDisc|Abyss Mysticism
descDisc|Assamite Sorcery
descDisc|Dark Thaumaturgy
descDisc|Flight
descDisc|Koldunic Sorcery
descDisc|Setite Sorcery
descDisc|Thaumaturgy
descNumina|Animal Psychics
descNumina|Anti-Psychic
descNumina|Astral Projection
descNumina|Biocontrol
descNumina|Channeling
descNumina|Clairvoyance
descNumina|Cyberkinesis
descNumina|Cyberpathy
descNumina|Ectoplasmic Generation
descNumina|Mind Shields
descNumina|Precognition
descNumina|Psychic Healing
descNumina|Psychic Hypnosis
descNumina|Psychic Invisibility
descNumina|Psychic Vampirism
descNumina|Psychokinesis
descNumina|Psychometry
descNumina|Psychoportation
descNumina|Pyrokinesis
descNumina|Shadow
descNumina|Synergy
descNumina|Telepathy
descNumina|True Faith
descNumina|1. Death Curse
descNumina|1. Deathsight
descNumina|1. Eldritch Mark
descNumina|1. Face Theft
descNumina|1. Healing Slumber
descNumina|1. Lifting Shadows
descNumina|1. Sprint
descNumina|1. Symbol Interpretation
descNumina|2. Bedtime Story
descNumina|2. Cruel Whispers
descNumina|2. Enhance Craftsmanship
descNumina|2. Fire's Weal
descNumina|2. Hellblade
descNumina|2. Jolt
descNumina|2. Object Permanence
descNumina|2. Quintessence Infusion
descNumina|2. Rest in Peace
descNumina|2. Step on a Crack
descNumina|2. Teleport Ward
descNumina|3. Always Armed
descNumina|3. Bashert
descNumina|3. Belle/Beau/Bright of the Ball
descNumina|3. Calm Above, Hell Below
descNumina|3. Get Me the Heck Outta Here!
descNumina|3. Grip of Shades
descNumina|3. Hard-Light Constructs
descNumina|3. Invade Demesne
descNumina|3. Mike's Cure-All
descNumina|3. Purification of the Inferno
descNumina|3. Shape Quintessence
descNumina|3. Shitstorm
descNumina|3. Shroud Bubble
descNumina|3. Shut It Down
descNumina|3. Wrapped in a Shroud
descNumina|4. Doxxing
descNumina|4. Dream Scream
descNumina|4. Extraction
descNumina|4. Fix the True Form
descNumina|4. Forced Medium
descNumina|4. Freudian Slip
descNumina|4. Humor Alignment
descNumina|4. Information Superhighway
descNumina|4. Instant Feast
descNumina|4. Love Potion Number 9
descNumina|5. Generational Wealth
descNumina|5. Megafauna Transformation
descNumina|5. Oubliette
descNumina|5. Overwrite
descNumina|5. Shelter for the Dead
descNumina|5. Smoldering Ruin
descNumina|5. Steal Life
descNumina|5. Teleportal
descNumina|Balamo~ob
descNumina|Followers of Set
descNumina|Giovanni
descNumina|Lone Practitioner
descNumina|Maison Liban
descNumina|Mogen HaLev
descNumina|Nebuu-Afef, The Order of The Golden Fly
descNumina|Path-Dancers
descNumina|Thal~hun
descNumina|The Ancient Order of The Aeon Rites
descNumina|The Arcanum
descNumina|The Children of Osiris
descNumina|The Crystal Circle
descNumina|The Cult of Isis
descNumina|The Cult of Mercury
descNumina|The Dozen Priests of the Pythian Order
descNumina|The Fenian
descNumina|The Nephite Priesthood
descNumina|The Seven Thunders
descNumina|The Silver Portal
descNumina|The Society of Enlightened Altruistic Ideologies (SEAI)
descNumina|The Society of Leopold
descNumina|The Star Council
descNumina|U.S. Government (Project Twilight)
descNumina|Uzoma
descPath|Iter Pernix
descPath|Lure of Flames
descPath|Movement of the Mind
descPath|Rego Calatio
descPath|The Covenant of Enki
descPath|The Fires of the Inferno
descPath|The Flow of Ashe
descPath|The Hand of the Magi
descPath|The Keeper~s Way
descPath|The Laws of Suleiman
descPath|The Path of Jinn~s Gift
descPath|The Path of Life~s Waters
descPath|The Path of Praapti
descPath|The Path of the Ailing Jackal
descPath|The Revelations of Eden
descPath|The Taking of the Spirit
descPath|The Way of the Spirit
descPath|Ushabti
descPath|Weather Control
descRitual|1. Ambrus Kelemen~s Aegis
descRitual|1. Call of the Hungry Dead
descRitual|1. Eldritch Beacon
descRitual|1. Final Sight
descRitual|1. Foxfire
descRitual|1. Insight
descRitual|1. Knowing Stone
descRitual|1. Memento Mori
descRitual|1. Minestra di Morte
descRitual|1. Preserve Corpse
descRitual|1. Rite of Introduction
descRitual|1. Ritual of Return
descRitual|1. Ritual of the Smoking Mirror
descRitual|1. Shew-Stone
descRitual|1. Strength of Haqim
descRitual|1. Tame the Maddening Flame
descRitual|2. Antonius~s Denial
descRitual|2. Banning Rituals
descRitual|2. Blade of Smoke and Shadow
descRitual|2. Blood Calls to Blood
descRitual|2. Commune with Cainite
descRitual|2. Eyes of the Grave
descRitual|2. Flaming Weapon
descRitual|2. Generation of the Acheron Vortex
descRitual|2. Haunting Breeze
descRitual|2. Occhio d~Uomo Morto
descRitual|2. Prepare the Vessel
descRitual|2. Puppet
descRitual|2. Summon Guardian Spirit
descRitual|2. The Hand of Glory
descRitual|2. The Ritual of Pochtli
descRitual|2. Totenpass
descRitual|2. Two Centimes
descRitual|2. Ward
descRitual|2. Witch Eye
descRitual|2. Witness of Whispers
descRitual|3. A Shield of Mirror
descRitual|3. Animated Assistants
descRitual|3. Balthazar~s Revelation
descRitual|3. Bind the Familiar
descRitual|3. Blood Dance
descRitual|3. Death~s Head
descRitual|3. Din of the Damned
descRitual|3. Displacement of the Pneuma
descRitual|3. Divine Sign
descRitual|3. Nightmare Drums
descRitual|3. Ritual of The Unearthed Fetter
descRitual|3. Samira~s Kihanah
descRitual|3. Scry the Hearthstone
descRitual|3. Strength in Wisdom
descRitual|3. Tempesta Scudo
descRitual|3. The Third Eye of Rickard Argentis
descRitual|3. Wield the Spear of Damnation
descRitual|4. Animated Weapon
descRitual|4. Baleful Doll
descRitual|4. Bastone Diabolico
descRitual|4. Cadaver~s Touch
descRitual|4. Candle of Haunting
descRitual|4. Impregnable Soul
descRitual|4. Invisible Chains of Binding
descRitual|4. Peek Past the Shroud
descRitual|4. Rebirth in the Light of Haqim
descRitual|4. Return of the Heart
descRitual|4. Ritual of Xipe Totec
descRitual|4. Vision of St. Anthony
descRitual|5. Cheat the Scale of Hades
descRitual|5. Chill of Oblivion
descRitual|5. Dead Man~s Hand
descRitual|5. Dedicate the Haven
descRitual|5. Enochian Passage
descRitual|5. Esilio
descRitual|5. From Marduk~s Throat
descRitual|5. Grasp the Ghostly
descRitual|5. Occlude the Western Road
descRitual|5. Orphic Sojourn
descRitual|5. Ravening Walls of Ceoris
descRitual|5. Rickard Argentis~s Homunculus
descRitual|5. Rite of Marduk Slain and Risen
descRitual|5. Stone Slumber
descRitual|5. Treasures of Hades
descRitual|7. Evocation of the Oubliette
descRitual|8. Lord Aludian~s Claim
'@
$v335Exc = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
foreach ($l335 in ($v335ExcRaw -split "`r?`n")) {
    $t335 = $l335.Trim()
    if ($t335 -eq '') { continue }
    [void]$v335Exc.Add($t335.Replace('~', [char]0x2019))
}
[void]$v335Exc.Add('descNumina|Forn Jafna' + [char]0xF0 + 'r')
$v335Seen = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
# Quantas entradas cada modulo tem, MEDIDO por research/gen_desc_system.ps1 ao regenerar em
# T768. Literal de proposito - ver o comentario da perna (e) mais abaixo.
# descNumina moved 50 -> 102 in T928 and 102 -> 128 in T929: the fifty T768 regenerated, plus the
# fifty-two hedge rituals, plus the twenty-six affiliations. Both new groups are printed as prose
# with no System block (SPEC I140l, I140m).
$v335Expect = @{ descDisc = 36; descNumina = 128; descPath = 64; descRitual = 284 }
$v335Entries = 0
$v335Blocks = 0
$v335Inverse = 0
foreach ($a335 in @(@('descDisc', 'DISC_DESC'), @('descNumina', 'DESC'), @('descPath', 'PATH_DESC'), @('descRitual', 'RITUAL_DESC'))) {
    foreach ($lang335 in @('en', 'pt')) {
        $ent335 = DescEntries (Join-Path $plugin "$($a335[0])_$lang335.lua") $a335[1] $lang335
        if ($null -eq $ent335 -or $ent335.Count -eq 0) {
            $v335Bad += "$($a335[0])_$lang335.lua parsed to nothing - the module or its $($a335[1]) markers are gone and every leg below would pass on an empty list (SPEC V209)"
            continue
        }
        # (e) a contagem tem de bater com o LITERAL medido em T768. Comparar so com V210a seria
        # NO-OP (SPEC B7): V210 conta os MESMOS arquivos na MESMA rodada, entao uma entrada
        # apagada das duas metades derruba as duas contagens juntas e nada acende. O literal e
        # o unico lado da comparacao que nao anda junto com o artefato. A checagem contra V210a
        # fica, mas com outro papel: as duas leituras tem de CONCORDAR, senao um dos dois
        # parsers esta lendo o arquivo errado.
        $k335 = "$($a335[0])|$lang335"
        if ($ent335.Count -lt $v335Expect[$a335[0]]) {
            $v335Bad += "$($a335[0])_$lang335.lua holds $($ent335.Count) entries and T768 measured $($v335Expect[$a335[0]]) - the regeneration lost one, and V210a cannot see it because both halves fell together (SPEC V335e)"
        }
        if ($v210Count.ContainsKey($k335) -and $ent335.Count -ne $v210Count[$k335]) {
            $v335Bad += "$($a335[0])_$lang335.lua counts $($ent335.Count) here and $($v210Count[$k335]) in V210 - the two parsers disagree about the same file (SPEC V209)"
        }
        $mk335 = $(if ($lang335 -eq 'en') { 'System' } else { 'Sistema' })
        foreach ($e335 in $ent335) {
            $v335Entries++
            $body335 = $e335.Body -replace "`r", ''
            $bl335 = $body335 -split "`n`n`n"
            if ($bl335.Count -lt 3) {
                $v335Bad += "$($a335[0])_$lang335.lua '$($e335.Key)' has $($bl335.Count) block(s) - the regeneration ate the source header or the name (SPEC V335a, I104a)"
                continue
            }
            if ($bl335[1] -cne $e335.Key) {
                $v335Bad += "$($a335[0])_$lang335.lua '$($e335.Key)' opens block 2 with '$($bl335[1])' - block 2 is the key, VERBATIM (SPEC V335a, I104a)"
            }
            $b3335 = ($bl335[2..($bl335.Count - 1)] -join "`n`n`n")
            if ($b3335.Trim() -eq '') {
                $v335Bad += "$($a335[0])_$lang335.lua '$($e335.Key)' has an EMPTY block 3 - V210a sees the key and passes; a hollow entry is what a bad cut leaves behind (SPEC V335d)"
                continue
            }
            $lines335 = $b3335 -split "`n"
            # A forma INVERSA do descNumina: o bloco 3 abre com o cabecalho `System` sozinho e
            # a secao inteira e mecanica (SPEC I104e, R133).
            if ($lines335[0].Trim() -ceq $mk335) { $v335Inverse++; $v335Blocks++; continue }
            $heads335 = @()
            $want335 = 1
            for ($i335 = 0; $i335 -lt $lines335.Count; $i335++) {
                $mm335 = [regex]::Match($lines335[$i335], '^(' + $DOT335 + '+) \S')
                if (-not $mm335.Success) { continue }
                if ($mm335.Groups[1].Value.Length -ne $want335) { continue }
                $heads335 += $i335
                $want335++
            }
            $ex335 = "$($a335[0])|$($e335.Key)"
            $rxMk335 = '(?m)^' + [regex]::Escape($mk335) + ':'
            $chunks335 = @()
            if ($heads335.Count -eq 0) { $chunks335 += $b3335 }
            else {
                for ($h335 = 0; $h335 -lt $heads335.Count; $h335++) {
                    $s335 = $heads335[$h335]
                    $z335 = $(if ($h335 -lt ($heads335.Count - 1)) { $heads335[$h335 + 1] - 1 } else { $lines335.Count - 1 })
                    $chunks335 += (@($lines335[$s335..$z335]) -join "`n")
                }
            }
            foreach ($c335 in $chunks335) {
                $v335Blocks++
                if ($c335 -match $rxMk335) { continue }
                if ($v335Exc.Contains($ex335)) { [void]$v335Seen.Add($ex335); continue }
                $head335 = ($c335 -split "`n")[0]
                if ($head335.Length -gt 48) { $head335 = $head335.Substring(0, 48) + '...' }
                $v335Bad += "$($a335[0])_$lang335.lua '$($e335.Key)' has a block with no $mk335 marker ('$head335') and is not on the I104c list - either the prose stayed or the mechanics went (SPEC V335b)"
            }
        }
    }
}
# (c) a lista nao pode apodrecer: nome que nunca dispara e perdao em branco esperando a
# proxima entrada cair nele por acidente.
foreach ($x335 in $v335Exc) {
    if (-not $v335Seen.Contains($x335)) {
        $v335Bad += "V335c list names '$($x335.Replace([char]0x2019, [char]0x27))' but that entry carries the marker now (or no longer exists) - a pardon nobody uses is a pardon waiting for the wrong entry (SPEC V335c)"
    }
}
if ($v335Entries -eq 0 -or $v335Blocks -eq 0) {
    $v335Bad += "V335 measured $v335Entries entry(ies) and $v335Blocks block(s) - zero on either side means this check verified nothing (SPEC V209, B7)"
}
if ($v335Bad) { foreach ($b335 in $v335Bad) { Fail "V335 $b335" } }
else { Pass "V335 $v335Entries power descriptions across 8 modules keep blocks 1 and 2, none hollow, and all $v335Blocks mechanics block(s) carry the System marker - $v335Inverse of them in the descNumina section form (SPEC I104e) and $($v335Exc.Count) named I104c exceptions, every one of them still firing" }

# ---- V346: pickerItems keeps the SAVED value in EVERY picker's list (SPEC V200, B91) ----
# The user's report was "the dropdown choices are not being saved - I close the sheet, reopen
# it and they are gone". Nothing was lost: pickerItems built `field` INSIDE `if filter then`
# and `current` reads from `field`, so for the fourteen unfiltered roots `current` was nil and
# V200's rule - the row's own value stays in the list however illegal it has become - had no
# executor. While no list changed its TEXT there was no symptom, because the saved value had
# come from that same list and was in it anyway. Two rounds of renaming woke it up.
#
# Five legs plus the zero-guard:
#   (a) neither `field` nor `current` is assigned under a `filter` gate - both are born at the
#       function's own indent and hold for every combo;
#   (b) AFTER the loop that builds kept/shown there is ONE explicit append of `current` when it
#       is not already in `kept`, and it sits inside the `if hit == nil` branch, before the memo
#       stores the pair - one place appends, never two (SPEC V135, B38);
#   (c) the memo key carries that orphan value in EVERY case and not just the road one: two
#       controls of the same root holding DIFFERENT orphans must not share a list (SPEC B48,
#       V272), and the way to know is a scan of the authored list for `current`;
#   (d) `cboSpeciality<n>` is the NAMED spelling exception: the control has no underscore before
#       the number and the field has one, so string.sub(nm, 4) answers `Speciality1` and
#       sheet[field] comes out nil in silence. The field is built from fieldRoot plus the number;
#   (e) zero-guard: pickerItems not found, or zero cbo* combos on the sheet.
#
# Mutation (SPEC V20, V222): put `current` back inside `if filter then` -> red on (a); delete
# the append -> red on (b); take `odd` out of the memo key, or drop the vals scan -> red on
# (c); build the field with string.sub(nm, 4) again -> red on (d).
$v346Bad = @()
$v346Combos = 0
foreach ($f346 in $files) {
    $v346Combos += ([regex]::Matches((CodeOf $f346.FullName), '<comboBox[^>]*name="cbo')).Count
}
$code346 = CodeOf (Join-Path $dir 'WoD20.6.lfm')
# Fecha no `end;` do MESMO indent da declaracao, por backreferencia. Um `(.*?)\n\t+end;`
# preguicoso fecharia no primeiro `end;` de dentro - o do laco - e todas as pernas abaixo
# mediriam meia funcao, vermelhas por engano.
$m346 = [regex]::Match($code346, '(?s)(\t+)local function pickerItems\(c, lang, era, levels\)\r?\n(.*?)\r?\n\1end;')
if (-not $m346.Success) {
    $v346Bad += "pickerItems is not in WoD20.6.lfm under the name this check reads - every leg below would measure nothing (SPEC V346e, V209)"
}
else {
    $body346 = $m346.Groups[2].Value
    $base346 = ([regex]::Match($body346, '(?m)^(\t+)local nm = c:getName\(\);')).Groups[1].Value
    if ($base346 -eq '') {
        $v346Bad += "pickerItems does not open with `local nm = c:getName();` - the indent of the function body is what leg (a) measures against (SPEC V209)"
    }
    else {
        # (a) as duas nascem no indent da FUNCAO. Dentro de `if filter then` elas ficariam mais
        # fundo, que e exatamente a forma que B91 tinha.
        if ($body346 -notmatch ('(?m)^' + $base346 + 'local field = ')) {
            $v346Bad += "pickerItems does not assign `field` at its own indent - inside a filter gate it is nil for the fourteen unfiltered roots and V200 loses its executor (SPEC V346a, B91)"
        }
        if ($body346 -notmatch ('(?m)^' + $base346 + 'local current = ')) {
            $v346Bad += "pickerItems does not assign `current` at its own indent - that is the B91 shape, and it has no symptom until a list changes an item's text (SPEC V346a)"
        }
        if ($body346 -match '(?m)^\s*if\s+filter\s+then\s*$') {
            $v346Bad += "pickerItems still opens an `if filter then` block - `field` and `current` belong to every combo, not to the six filtered ones (SPEC V346a)"
        }
        # (d) a grafia do speciality
        $fl346 = [regex]::Match($body346, '(?m)^\s*local field = .*$').Value
        if ($fl346 -match 'string\.sub\(nm, 4\)') {
            $v346Bad += "pickerItems builds the field with string.sub(nm, 4) - cboSpeciality1 answers `Speciality1`, sheet[field] is nil and nothing says so (SPEC V346d)"
        }
        if ($fl346 -notmatch 'fieldRoot\(nm\)') {
            $v346Bad += "pickerItems does not build the field from fieldRoot(nm) - that is the one spelling that serves both the rows and the singletons (SPEC V346d)"
        }
        if ($body346 -notmatch '_\?\(%d\+\)\$') {
            $v346Bad += "pickerItems does not match the row number with `_?(%d+)`\$` - cboSpeciality1 has no underscore before its number and cbonumina_1 has one (SPEC V346d)"
        }
        # (b) o append, DEPOIS do laco e ANTES do memo, e UM so
        $nApp346 = ([regex]::Matches($body346, 'kept\[#kept \+ 1\]\s*=\s*current;')).Count
        if ($nApp346 -eq 0) {
            $v346Bad += "pickerItems never appends `current` to kept - a comboBox whose value is not among its values renders BLANK, so the sheet looks like it threw the data away (SPEC V346b, V200)"
        }
        elseif ($nApp346 -gt 1) {
            $v346Bad += "pickerItems appends `current` in $nApp346 places - one place appends or the two drift apart (SPEC V346b, V135, B38)"
        }
        else {
            $iLoop346 = $body346.IndexOf('for k = 1, #vals, 1 do')
            $iApp346 = $body346.IndexOf('kept[#kept + 1]   = current;')
            if ($iApp346 -lt 0) { $iApp346 = ([regex]::Match($body346, 'kept\[#kept \+ 1\]\s*=\s*current;')).Index }
            $iMemo346 = $body346.IndexOf('memo.lists[key] = {')
            if ($iLoop346 -lt 0 -or $iMemo346 -lt 0) {
                $v346Bad += "pickerItems no longer has the vals loop or the memo store this check anchors on (SPEC V209)"
            }
            elseif (-not ($iLoop346 -lt $iApp346 -and $iApp346 -lt $iMemo346)) {
                $v346Bad += "the append of `current` is not between the vals loop and the memo store - appended before the loop it would be filtered back out, appended after the store the memo would serve the list without it (SPEC V346b)"
            }
        }
        # (c) a chave do memo carrega o orfao, e o orfao e medido contra a lista AUTORADA
        $k346 = [regex]::Match($body346, '(?m)^\s*local key\s*=.*$').Value
        if ($k346 -eq '' -or $k346 -notmatch '\bodd\b') {
            $v346Bad += "the memo key does not carry `odd` - two controls of one root holding different orphans would share a list, which is B48 through the same door (SPEC V346c, V272)"
        }
        if ($body346 -notmatch 'vals\[k\] == current') {
            $v346Bad += "`odd` is never measured against the authored list - then it means only `the filter would drop it` and a renamed item is invisible to the memo key (SPEC V346c, B91)"
        }
    }
}
if ($v346Combos -eq 0) {
    $v346Bad += "zero cbo* comboBox scanned across the sheet - pickerItems would have nothing to serve and this check verified nothing (SPEC V346e, B7)"
}
if ($v346Bad) { foreach ($b346 in $v346Bad) { Fail "V346 $b346" } }
else { Pass "V346 pickerItems builds field and current for all $v346Combos cbo* combos, appends the saved value in exactly one place between the loop and the memo, keys the memo on the orphan, and spells cboSpeciality<n> through fieldRoot" }

# ---- V192: a DESCRIPTION entry carries its source header (SPEC I21) ------------------
# 54th round, the user's ask: a description opens with the book and the exact page it came
# from, then the item's name, then the text - three blocks, two blank lines apart. The header
# lives INSIDE the string, never concatenated at render time (SPEC I21), so it is checkable
# here and there is only one way to build the block.
#
# This measures FORM, not existence: the three tables are still empty until SPEC T444-T446,
# and an empty region passes. What it must not let through is an entry that is on file but
# malformed - no source line, or a name block that drifted from the key the picker looks up.
$descForm = @()
$descSeen = 0
$srcRx  = [regex]'^.+,\s+(p\.|p(\xE1|a)g\.)\s*\d+$'
# T470: an entry may carry a SECOND body under daEn/daPt, written from the Dark Ages book.
# It is optional, but when it is there it is the SAME three blocks (SPEC I21) - a medieval
# body with no source line would open a description the reader cannot trace back to a page.
$eraRx  = [regex]'(?ms)\b(daEn|daPt)\s*=\s*\[==\[(.*?)\]==\]'
# T479-T481 moved the three tables out of the .lfm files and into one module per area
# (SPEC I24 / V189) and T495 then split each into two halves, one per language (SPEC V210).
# The markers migrated verbatim both times, so the region has the shape it always had - what
# changed is that a language is a FILE now, and this check reads each half on its own terms.
# Which key is missing from which half is V210's business; this one measures FORM.
foreach ($area in @(
    @('descDisc', 'DISC_DESC'),
    @('descPath', 'PATH_DESC'),
    @('descRitual', 'RITUAL_DESC'))) {
    $half192 = @{}
    $gone192 = $false
    foreach ($lang in @('en', 'pt')) {
        $half192[$lang] = DescEntries (Join-Path $plugin "$($area[0])_$lang.lua") $area[1] $lang
        if ($null -eq $half192[$lang]) { $descForm += "$($area[0])_$lang.lua is not at the plugin root, or its $($area[1]) markers are gone - the area has no [$lang] text to render (SPEC I24, V210)"; $gone192 = $true }
    }
    if ($gone192) { continue }
    foreach ($lang in @('en', 'pt')) {
        foreach ($e in $half192[$lang]) {
            $key = $e.Key
            $descSeen++
            $blocks = @(($e.Body.Trim()) -split "`n`n`n")
            if ($blocks.Count -ne 3) { $descForm += "$($area[0])_$lang.lua '$key' is $($blocks.Count) block(s), not source + name + text two blank lines apart" }
            else {
                if (-not $srcRx.IsMatch($blocks[0].Trim())) { $descForm += "$($area[0])_$lang.lua '$key' opens with '$($blocks[0].Trim())' - block one is '<book>, p. <page>'" }
                if ($blocks[1].Trim() -ne $key) { $descForm += "$($area[0])_$lang.lua '$key' names itself '$($blocks[1].Trim())' - block two is the picker value the entry is keyed by" }
                if ($blocks[2].Trim().Length -lt 1) { $descForm += "$($area[0])_$lang.lua '$key' has a header and no description under it" }
            }
            # The optional medieval body, same shape, checked only when the entry carries one.
            foreach ($er in $eraRx.Matches($e.Rest)) {
                $tag = $er.Groups[1].Value
                $descSeen++
                $eb = @(($er.Groups[2].Value.Trim()) -split "`n`n`n")
                if ($eb.Count -ne 3) { $descForm += "$($area[0])_$lang.lua '$key' [$tag] is $($eb.Count) block(s), not source + name + text two blank lines apart"; continue }
                if (-not $srcRx.IsMatch($eb[0].Trim())) { $descForm += "$($area[0])_$lang.lua '$key' [$tag] opens with '$($eb[0].Trim())' - block one is '<book>, p. <page>'" }
                if ($eb[1].Trim() -ne $key) { $descForm += "$($area[0])_$lang.lua '$key' [$tag] names itself '$($eb[1].Trim())' - block two is the picker value the entry is keyed by" }
                if ($eb[2].Trim().Length -lt 1) { $descForm += "$($area[0])_$lang.lua '$key' [$tag] has a header and no description under it" }
            }
        }
    }
}

# T470: the reader picks the body by ERA before it picks it by LANGUAGE, and the block is
# re-rendered when the era changes. Without the dataLink field, switching theme would leave
# the previous era's text on screen - stale, not blank, so V33 would never see it (SPEC V20:
# drop 'sheetTheme' from a dataLink, or the pickEra call, and this goes red).
# AMENDED in the 159th round (SPEC V192, T872, B115): the reader FOLLOWED ITS SUBJECT. This
# used to measure a `pickEra` local inside each of WoD20.12/.13/.14; wave 3 took the three
# panes away and the era reader went out with them, silently - there is no daEn/daPt on disk
# yet, so the check was guarding an empty path and an empty path stays green when it is
# deleted. The one reader is descText now (SPEC V381, V135), and both doors onto a
# description - the ? box and the search pane - come through it.
#
# The sheetTheme dataLink leg went with the panes and is NOT replaced: there is no pane
# renderer left to repaint, the box reads on OPEN, and a link of its own here would be the
# second era trigger T469 exists to forbid.
$descTextFn = LuaFn $root 'descText'
if (-not $descTextFn) { $descForm += "descText is gone from the root form - the ? box and the search pane have no reader at all (SPEC V381, V209)" }
else {
    $dtBare = [regex]::Replace($descTextFn, '(?m)^\s*--.*$', '')
    if ($dtBare -notmatch 'era == "Dark Ages" or era == "Classical Age"') { $descForm += "descText does not test both medieval themes - the Dark Ages body would never be read on either of them (SPEC C, I22, T470)" }
    if ($dtBare -notmatch 'ent\.daPt' -or $dtBare -notmatch 'ent\.daEn') { $descForm += "descText reads neither daEn nor daPt - the second body I21 allows is dead data (SPEC T470)" }
    # ERA before LANGUAGE, and the order is the rule: read the other way round the core body
    # always answers first and the medieval one is never reached on any theme.
    elseif ($dtBare.IndexOf('ent.daEn') -gt $dtBare.IndexOf('ent[lang]')) { $descForm += "descText reads ent[lang] before the medieval body - the core text always answers first and daEn/daPt would never be reached (SPEC T470, I22)" }
    # The fall is FREE: an entry with no medieval body shows the core one, never an empty box.
    if ($dtBare -notmatch 'if txt == "" then txt = ent\[lang\] or ent\.en or ""; end;') { $descForm += "descText has no fall back to the core body - an entry with no daEn/daPt would open blank in Dark Ages, which V33 refuses (SPEC V33, T470)" }
}
if ($descForm) { foreach ($d in $descForm) { Fail "V192 $d" } }
elseif ($descSeen -eq 0) { Pass "V192 the three DESC regions are still empty (SPEC T444-T446); the form check is armed" }
else { Pass "V192 all $descSeen description strings open with their book, page and name (SPEC I21)" }

# ---- V193: the description block closes with the column beside it --------------------
# 55th round. The rituals box went from 20 rows to 25 and grew 550 -> 675; had the
# DESCRIPTION been left at 550, nothing would have OVERLAPPED - the two boxes stand side by
# side - so V40 would have passed it and the only symptom would be a black tail on the tab.
# The relation held in all three areas by convention and by nothing else.
#
# Measured as a RELATION, not against the literals (the same move V171 makes): the block
# opens at the top of the left column and ends where the lowest box on the left ends, and its
# textEditor fills that minus the 35px title band. So it survives the NEXT row-count change,
# which is the whole point - the layout gate is still open.
# RETIRED in the 159th round (SPEC V193, T872). The relation had TWO terms - the DESCRIPTION
# box and the column beside it - and wave 3 took the box out of all three files, so one term
# is left and a relation with one term is a green over nothing (B7). What still measures these
# three: V40 for overlap and V225(a) for content fitting its container on both axes. No
# replacement is written here on purpose - the left column is now the ONLY thing in each file
# and there is no neighbour for it to keep step with. T832 gives the pair back, and V225(b)
# is what comes back with it, named on that task's own row.
#
# The zero-guard for the retirement itself: a DESCRIPTION box REAPPEARING in one of the three
# would mean the pane came back without its ruler, so it is refused here rather than passing
# unmeasured (SPEC V365d, B105).
$v193Back = @()
foreach ($areaFile in @('WoD20.11.lfm')) {
    $ax  = Doc (Join-Path $dir $areaFile)
    $n193 = @(@($ax.SelectNodes("//scrollBox/layout")) | Where-Object { $null -ne $_.SelectSingleNode("label[@text='DESCRIPTION']") }).Count
    if ($n193 -gt 0) { $v193Back += "$areaFile holds $n193 DESCRIPTION box(es) again" }
}
if ($v193Back) { Fail "V193 $($v193Back -join '; ') - wave 3 took the panes out and this ruler retired with them, so a pane that comes back comes back UNMEASURED (SPEC V365d, B105, T872)" }
else { Pass "V193 retired with the three panes in T872 - none of the three areas holds a DESCRIPTION box, and V225(b) comes back with them in T832" }

# ---- V175: the clan discipline row is locked unless the storyteller opened it ---------
# SPEC I8g. The four CLAN DISCIPLINES rows are the clan's own three-plus, not the player's
# picks, so they are the table's to set until stEditClanDisc is ticked.
#
# The EXECUTOR changed in the 159th round and the RULE did not (SPEC V175 amended, T872, user
# 2026-08-31 ask B). renderClanDiscLock painted `enabled` on four comboBoxes and wave 3 took
# those combos away; what is left is clanDiscOpen() - ONE function owning the test - and a
# refusal inside mfOpen, before the box can open. That is the same move V161 made for the
# speciality gift in the 146th, and it answers at the same moment the disabled combo did.
#
# Deliberately NOT looking for enabled/opacity any more: a button has a click hook, so the
# refusal is the lock, and the button stays fully legible - opacity would fade the text with
# the frame, which is what V241 refuses. What survives is what the rule always was: an
# explicit `== true` so nil falls CLOSED on an old sheet (V89, V80), the clan that settles
# nothing opening on its own (I37, V236d), and exactly one place asking (V135).
# NoComments is declared further down the file, so the strip is spelled out here: the two
# comment blocks around this code NAME the thing they describe, and a check that reads prose
# as if it were code is a check that passes on a deleted rule (SPEC V222, B60).
$lockFn  = LuaFn $root 'clanDiscOpen'
$openFn  = LuaFn $root 'mfOpen'
$rootBare = [regex]::Replace($root, '(?m)^\s*--.*$', '')
$openBare = if ($openFn) { [regex]::Replace($openFn, '(?m)^\s*--.*$', '') } else { '' }
$lockCalls = ([regex]::Matches($rootBare, '\bclanDiscOpen\s*\(')).Count
if (-not $lockFn) { Fail "V175 clanDiscOpen not found on the root form - nothing decides whether the clan slots may be changed (SPEC I8g, V175)" }
elseif ($lockFn -notmatch 'sheet\.stEditClanDisc == true') { Fail "V175 clanDiscOpen does not read stEditClanDisc as an explicit true - nil would fall open on an old sheet (SPEC V89, V80)" }
elseif ($lockFn -notmatch 'entry\.choice == nil and entry\.open == nil') { Fail "V175 clanDiscOpen no longer asks whether the clan SETTLES its trio - the six entries carrying a choice or an open have nobody but the table to fill them (SPEC I37, V236d)" }
elseif ($lockFn -match 'c\.enabled' -or $lockFn -match 'c\.opacity') { Fail "V175 clanDiscOpen paints a control - it is the QUESTION and not the painter, and a second owner of the answer is what V135 forbids (SPEC V175 amended, T872)" }
elseif (-not $openFn) { Fail "V175 mfOpen is gone - there is no opener left to refuse in (SPEC V209)" }
elseif ($openBare -notmatch 'string\.match\(field, "\^clanDisc_%d\+\$"\) ~= nil and not clanDiscOpen\(\)') { Fail "V175 mfOpen does not refuse a clan slot before the box opens - the lock the disabled combo used to give is gone and the four rows are the player's again (SPEC I8g, V95, V161)" }
elseif ($lockCalls -ne 2) { Fail "V175 clanDiscOpen is written $lockCalls time(s) in code, expected the declaration plus exactly ONE asker - two askers are two rules for one decision and the second is the one that goes stale (SPEC V135, V199)" }
elseif ($rootBare -match 'renderClanDiscLock') { Fail "V175 renderClanDiscLock is back - it painted enabled on four comboBoxes wave 3 removed, so it would walk for controls that cannot be found and the refusal would have two owners (SPEC T872, V135)" }
else { Pass "V175 the four clan slots are refused in the opener from stEditClanDisc, fail-closed, through one clanDiscOpen() and with the button left legible" }

# ---- V179: a main path dot is read-only, and owns nothing --------------------------
# SPEC I16/V51. The main path's rating IS the blood sorcery's rating, painted per render.
# Giving these dots a field would put a second number on the sheet to drift from the first,
# and giving them an onClick would sell the player a dot the discipline already paid for.
$pathX   = Doc (Join-Path $dir "WoD20.11.lfm")
$roTpl   = $pathX.SelectSingleNode("//template[@name='MainPathRow']")
if ($null -eq $roTpl) { Fail "V179 MainPathRow is not declared on WoD20.11 - the read-only row is gone" }
else {
    $roDots = @($roTpl.SelectNodes("imageCheckBox"))
    $owning = @($roDots | Where-Object { $_.HasAttribute("field") -or $_.HasAttribute("onClick") })
    $bright = @($roDots | Where-Object { $_.GetAttribute("opacity") -ne '0.40' })
    $live   = @($roDots | Where-Object { $_.GetAttribute("autoChange") -ne 'false' })
    if ($roDots.Count -ne 5) { Fail "V179 MainPathRow carries $($roDots.Count) dots, not the five the scale has" }
    elseif ($owning) { Fail "V179 $($owning.Count) main path dot(s) own a field or an onClick - the rating belongs to the discipline (SPEC I9, I10)" }
    elseif ($live) { Fail "V179 $($live.Count) main path dot(s) still toggle themselves - autoChange must be false" }
    elseif ($bright) { Fail "V179 $($bright.Count) main path dot(s) are drawn at full brightness - a read-only widget is dimmed (SPEC V111)" }
    else { Pass "V179 all five main path dots are read-only: no field, no click, no self-toggle, dimmed" }
}

# ---- V70/V74/V75/V76: the era ability lists (SPEC I7, 21st round) ----------------
# A row on the Main tab is a slot: the era decides which ability sits there and the renderer
# rebinds the dots to that ability's field. Two bugs came out of getting this wrong - B23 (a
# name changing field between eras) and B24 (one field carrying several names) - so what is
# checked here is the shape that makes both impossible: one name, one field, both ways.
#
# None of these strings live in the XML, which is why the width and translation checks further
# up cannot see them - the same blind spot as B11 and B17, one family further out.
$rootLfm = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20th.lfm")))

# ABILITY_FIELD: name -> field.
$abilityField = @{}
$afBlock = [regex]::Match($rootLfm, 'ABILITY_FIELD = \{(.*?)\n\t{3}\};', 'Singleline')
if (-not $afBlock.Success) { Fail "V74 ABILITY_FIELD not found on the root form - every era check is a no-op (SPEC V20)" }
else {
    foreach ($m in [regex]::Matches($afBlock.Groups[1].Value, '\["([^"]+)"\]\s*=\s*"(\w+)"')) { $abilityField[$m.Groups[1].Value] = $m.Groups[2].Value }
}

# ERA_ABILITIES: era -> column -> ten names. A column may point at a shared list declared just
# above it (BASE_TALENTS), so those are resolved first - the alternative is writing the same ten
# names four times, which is exactly how lists drift apart.
$sharedLists = @{}
foreach ($m in [regex]::Matches($rootLfm, 'local ([A-Z_]+) = \{("[^}]*")\};')) {
    $names = @()
    foreach ($s in [regex]::Matches($m.Groups[2].Value, '"([^"]+)"')) { $names += $s.Groups[1].Value }
    $sharedLists[$m.Groups[1].Value] = $names
}
$eraLists = @{}
$eaBlock = [regex]::Match($rootLfm, 'ERA_ABILITIES = \{(.*?)\n\t{3}\};', 'Singleline')
if (-not $eaBlock.Success) { Fail "V75 ERA_ABILITIES not found on the root form - the era lists are unchecked (SPEC V20)" }
else {
    foreach ($m in [regex]::Matches($eaBlock.Groups[1].Value, '\["([^"]+)"\]\s*=\s*\{(.*?)\n\t{4}\}', 'Singleline')) {
        $cols = @{}
        foreach ($c in [regex]::Matches($m.Groups[2].Value, '(\w+)\s*=\s*(\{[^}]*\}|[A-Z_]+)')) {
            $raw = $c.Groups[2].Value
            if ($raw -match '^[A-Z_]+$') {
                if ($sharedLists.ContainsKey($raw)) { $cols[$c.Groups[1].Value] = $sharedLists[$raw] }
                else { Fail "V75 $($m.Groups[1].Value)/$($c.Groups[1].Value) points at $raw, which is not a list declared on the root form" }
            } else {
                $names = @()
                foreach ($s in [regex]::Matches($raw, '"([^"]+)"')) { $names += $s.Groups[1].Value }
                $cols[$c.Groups[1].Value] = $names
            }
        }
        $eraLists[$m.Groups[1].Value] = $cols
    }
}

# The Main tab authors the Victorian list: a sheet whose renderer never ran shows the base era,
# coherent rather than half-bound. field= and nome= per row are read here for that comparison.
$authoredRows = @{}
$mainDoc = Doc (Join-Path $dir "WoD20.1.lfm")
foreach ($r in $mainDoc.SelectNodes("//Ability")) {
    $col = $r.GetAttribute("col"); $num = $r.GetAttribute("num")
    if (-not $col -or -not $num) { Fail "V76 an <Ability> row has no col/num - the renderer addresses rows by slot"; continue }
    $authoredRows["$col/$num"] = @{ Field = $r.GetAttribute("field"); Name = $r.GetAttribute("nome") }
}

if ($abilityField.Count -eq 0 -or $eraLists.Count -eq 0) { Fail "V74/V75 the era tables read empty - the checks below measure nothing (SPEC V20)" }
else {
    # V74: one name, one field - and the other way round too. B24 was the reverse direction
    # going unchecked: `enigmas` carried Enigmas, Computer and Philosophy, so two dots in one
    # surfaced as two dots in another the moment the era changed.
    $fieldOwner = @{}
    $bijection = @()
    foreach ($n in $abilityField.Keys) {
        $f = $abilityField[$n]
        if ($fieldOwner.ContainsKey($f)) { $bijection += "'$f' is the field of both '$($fieldOwner[$f])' and '$n' - a rating would surface under the other name (SPEC B24)" }
        else { $fieldOwner[$f] = $n }
    }
    if ($bijection) { foreach ($b in ($bijection | Sort-Object -Unique)) { Fail "V74 $b" } }
    else { Pass "V74 all $($abilityField.Count) ability names own a field of their own" }

    # V75: every name in a column is distinct and owns a field. How MANY names a column
    # carries stopped being ten in the 154th round and now differs by era (SPEC I123), so the
    # count is owned by V371e - one ruler per rule (SPEC V135), and a literal 10 here would
    # have to be hand-edited every time a book adds a trait.
    $listBad = @()
    foreach ($era in $eraLists.Keys) {
        foreach ($col in @('talents', 'skills', 'knowledges')) {
            if (-not $eraLists[$era].ContainsKey($col)) { $listBad += "$era has no $col list"; continue }
            $names = $eraLists[$era][$col]
            if ($names.Count -eq 0) { $listBad += "$era/$col reads empty - a column with no names is a column nothing renders (SPEC V20)" }
            $seen = @{}
            foreach ($n in $names) {
                if (-not $abilityField.ContainsKey($n)) { $listBad += "$era/$col lists '$n', which has no field in ABILITY_FIELD" }
                if ($seen.ContainsKey($n)) { $listBad += "$era/$col lists '$n' twice" } else { $seen[$n] = $true }
            }
        }
    }
    if ($listBad) { foreach ($b in ($listBad | Sort-Object -Unique)) { Fail "V75 $b" } }
    else { Pass "V75 all $($eraLists.Count) eras fill their 3 columns with named abilities that are distinct and owned" }

    # V70: an era name is a visible string like any other - both keys, the embedded map that
    # actually translates at runtime, and a width that fits the 125px template label.
    $eraBad = @()
    $eraStrings = New-Object System.Collections.Generic.HashSet[string]
    foreach ($era in $eraLists.Keys) { foreach ($col in $eraLists[$era].Keys) { foreach ($n in $eraLists[$era][$col]) { [void]$eraStrings.Add($n) } } }
    foreach ($txt in $eraStrings) {
        if (-not $ptK.Contains($txt)) { $eraBad += "'$txt' has no [pt] key" }
        if (-not $enK.Contains($txt)) { $eraBad += "'$txt' has no [en] key" }
        if (-not $embedded.ContainsKey($txt)) { $eraBad += "'$txt' is missing from the PT map - it would never translate at runtime" }
        $need = NeededPx $txt
        if ($need -gt 125) { $eraBad += "'$txt' (pt '$($ptVal[$txt])') needs ~${need}px, the Ability label is 125px" }
    }
    if ($eraBad) { foreach ($b in ($eraBad | Sort-Object -Unique)) { Fail "V70 $b" } }
    else { Pass "V70 all $($eraStrings.Count) ability names translate and fit the row" }

    # V76, static half: what the XML authors has to BE an era - the Victorian one - with each
    # row bound to the field its own name owns. Otherwise a sheet whose renderer never ran shows
    # a mix no era ever had.
    $authBad = @()
    $vic = $eraLists['Victorian Age']
    if ($null -eq $vic) { $authBad += "Victorian Age has no list, so the authored rows cannot be checked against it" }
    else {
        # 1..what the VICTORIAN list holds, not 1..10: the columns are 11/13/12 since the
        # 154th round and the era is what says how far the authored rows run. Slots past the
        # end of the Victorian list exist for the eras that are longer and ship hidden - they
        # are V371's, which is the only check that knows about the twin (SPEC I123b, V371b).
        foreach ($col in @('talents', 'skills', 'knowledges')) {
            for ($i = 1; $i -le $vic[$col].Count; $i++) {
                $row = $authoredRows["$col/$i"]
                if ($null -eq $row) { $authBad += "$col slot $i is not authored on WoD20.1"; continue }
                $want = $vic[$col][$i - 1]
                if ($row.Name -cne $want) { $authBad += "$col slot $i authors '$($row.Name)', the Victorian list says '$want'" }
                elseif ($abilityField[$want] -cne $row.Field) { $authBad += "$col slot $i authors field='$($row.Field)' for '$want', which owns '$($abilityField[$want])'" }
            }
        }
    }
    if ($authBad) { foreach ($b in $authBad) { Fail "V76 $b" } }
    else { Pass "V76 the authored rows are the Victorian era, each bound to its own field" }
}

# V76, runtime half: rebinding without reloading would leave the dot showing the previous
# field's state, which looks exactly like the bug this round was opened for.
if ($hh6 -notmatch 'if c\.field ~= full then c\.field = full; end;') { Fail "V76 the renderer never rebinds a dot - the slots would keep the authored field" }
elseif ($hh6 -notmatch 'local want = sheet\[full\] == true;') { Fail "V76 the renderer rebinds without reloading checked - the dot would show the old field's state" }
else { Pass "V76 the renderer rebinds each dot and reloads it from the NDB" }

# The Combat mirror: one row follows the era, and it resolves the name off the era's own list
# instead of a second table that could drift (the reason HEALTH_MARKS is declared once, V41).
$hh3Text = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20.3.lfm")))
if ($rootLfm -notmatch 'function eraRangedName\(') { Fail "V74 eraRangedName is missing - the Combat mirror cannot follow the era" }
elseif ($hh3Text -notmatch 'ReadOnlyTrait field="ranged"') { Fail "V74 the Combat ranged row is not a slot - Archery has its own field now" }
elseif ($hh3Text -notmatch 'paintAs\(form, "ranged", ABILITY_FIELD\[eraRangedName\(\)\], 1\)') { Fail "V74 the ranged row is not painted from the era's own ranged skill" }
elseif ($hh3Text -notmatch "'archery_1'" -or $hh3Text -notmatch "'sheetTheme'") { Fail "V74 the WoD20.3 dataLink does not observe archery_* and sheetTheme - the mirror would go stale" }
else { Pass "V74 the Combat mirror follows the era's ranged skill" }

# V72: the labels are dyn*, so the language traversal skips them (V31). That makes the renderer
# the ONLY thing that writes them, and it has to run on both switches - hanging it off the era
# alone leaves a [pt] sheet in English until the era is touched.
if ($hh6 -notmatch 'local function renderAbilityLabels\(') { Fail "V72 renderAbilityLabels is missing - the era labels would never be written" }
else {
    $langEvt = [regex]::Match($hh6, '<dataLink field="language".*?</dataLink>', 'Singleline')
    $themeEvt = [regex]::Match($hh6, '<dataLink field="sheetTheme".*?</dataLink>', 'Singleline')
    if (-not $langEvt.Success -or $langEvt.Value -notmatch 'renderAbilityLabels\(') { Fail "V72 the language dataLink does not call renderAbilityLabels - era labels would fall back to English on a [pt] sheet" }
    elseif (-not $themeEvt.Success -or $themeEvt.Value -notmatch 'renderAbilityLabels\(') { Fail "V72 the sheetTheme dataLink does not call renderAbilityLabels - switching era would repaint but not rename" }
    else { Pass "V72 the era labels are rendered on load and on both switches" }
}

# And the templates have to carry the dyn* / slot names the renderer looks for.
$abilityLbl = $mainDoc.SelectSingleNode("//template[@name='Ability']/label")
$abilityDot = $mainDoc.SelectSingleNode("//template[@name='Ability']/imageCheckBox")
$roLbl = (Doc (Join-Path $dir "WoD20.3.lfm")).SelectSingleNode("//template[@name='ReadOnlyTrait']/label")
if ($null -eq $abilityLbl -or $abilityLbl.GetAttribute("name") -ne 'dynAbil$(col)$(num)') { Fail "V76 the Ability label is not named per slot - the renderer cannot find it and the translation would fight it" }
elseif ($null -eq $abilityDot -or $abilityDot.GetAttribute("name") -ne 'abil$(col)$(num)_1') { Fail "V76 the Ability dots are not named per slot - nothing could rebind them" }
elseif ($null -eq $roLbl -or $roLbl.GetAttribute("name") -ne 'dynRo$(field)') { Fail "V72 the ReadOnlyTrait label is not named dynRo - the Combat rows would keep their authored name" }
else { Pass "V72/V76 both row templates hand their label and dots to the renderer" }

# ==== 27th round: storyteller tab + derived experience ledger =====================

# ---- V79 + V80: the storyteller gate, and that it fails CLOSED -------------------
# The one tab a player must not reach. Every way the check can go wrong - no room, no player
# object, an API that raises - has to read as "not the storyteller", so the test is not that
# the gate exists but that its failure paths return false.
$stFn = LuaFn $root 'isStoryteller'
if (-not $stFn) { Fail "V79 isStoryteller not found on the root form" }
elseif ($stFn -notmatch 'Firecast\.getMesaDe') { Fail "V79 isStoryteller does not ask the room who this is (SPEC R29)" }
elseif ($stFn -notmatch 'isMestre') { Fail "V79 isStoryteller never reads isMestre" }
elseif ($stFn -notmatch 'pcall') { Fail "V80 isStoryteller does not wrap the API call - a raise would escape instead of hiding the tab" }
elseif ($stFn -notmatch 'if mesa == nil then return false') { Fail "V80 isStoryteller does not close on a sheet outside a room" }
elseif ($stFn -notmatch 'if me == nil then return false') { Fail "V80 isStoryteller does not close on a missing player object" }
elseif ($stFn -notmatch 'return ok and res == true') { Fail "V80 isStoryteller does not close on a failed pcall" }
else { Pass "V79/V80 storyteller gate reads the room and fails closed" }

# ---- V89: which flag drives which tab, and the default each one carries ----------
# Numina has been visible since the sheet shipped, so a sheet saved before this round - nil in
# all three flags - must keep it. That is the difference between `~= false` and `== true`, and
# swapping them would silently take a tab away from every existing character.
$visFn = LuaFn $root 'applyTabVisibility'
if (-not $visFn) { Fail "V89 applyTabVisibility not found on the root form" }
elseif ($visFn -notmatch 'tabNumina\s*=\s*sheet\.stShowNumina ~= false') { Fail "V89 tabNumina does not default to visible - an existing sheet would lose the tab" }
elseif ($visFn -notmatch 'tabDisciplines\s*=\s*sheet\.stShowDisciplines == true') { Fail "V89 tabDisciplines does not default to hidden" }
elseif ($visFn -notmatch 'tabStoryteller\s*=\s*isStoryteller\(\)') { Fail "V89 tabStoryteller is not wired to the gate" }
else { Pass "V89 three tabs switch on their own flag, Numina defaulting visible" }

# ---- V92: a hidden tab is an EMPTY tab -------------------------------------------
# Whether the host takes the tab button away with the tab is still an open question (SPEC
# R31). Hiding the contents as well is what makes the answer stop mattering.
if (-not $visFn) { } # already reported above
# V92 asked the old code to walk the tab's children because nothing confirmed the host took
# the tab BUTTON away with the tab (SPEC R31). The strip is ours now, so the button is a
# control we hide ourselves, and the pane IS the contents - hiding it hides everything it
# holds, which the T521 probe watched happen. The promise is unchanged; what changed is that
# it is now kept by construction rather than by a loop. What is still worth checking is that
# BOTH halves are written: a pane hidden with its button left on screen is a dead tab.
elseif ($visFn -notmatch 'pane\.visible = live') { Fail "V92 applyTabVisibility never hides the pane - denied content would stay on screen (SPEC R31)" }
elseif ($visFn -notmatch 'btn\.visible  = want') { Fail "V92 the tab button does not follow the flag - a denied tab would still offer its button" }
elseif ($visFn -notmatch 'local live = want and \(nm == activeTab\)') { Fail "V92 the pane is not gated by BOTH the flag and the active tab - one of the two would win alone" }
else { Pass "V92 a denied tab loses its button and its contents" }

# ---- V217 (revokes V93): recalculating visibility moves nobody --------------------
# The jump to Main was paid on every toggle by the only person who can toggle - the
# checkboxes live on the Storyteller tab (SPEC I8, V95) - to cover the rare reader sitting
# on the tab as it went. That reader now stays put and finds the tab empty, never holding
# denied content, because V92 above still hides the children. ABSENCE is the only way this
# can be measured at all: SPEC R33 says there is no tab-change event and no trustworthy
# tabIndex, so runtime cannot answer it and the source is all there is.
#
# Comments come off first. V92's prose and V217's own both say "a denied tab" in plain
# English, and prose is not what this measures - code is. NoComments is defined further
# down this file, too late to call from here, so the strip is inline. The patterns are
# code-SHAPED for the same reason: a trailing comment survives the strip (NoComments only
# eats whole lines), and a check that reddens over prose sends the next round hunting a
# comment. Proven by mutation: `-- a denied tab` must NOT redden this.
$visFnCode = [regex]::Replace([string]$visFn, '(?m)^\s*--.*$', '')
if (-not $visFn) { } # already reported above
elseif ($visFnCode -match ':activate\(\)') { Fail "V217 applyTabVisibility still activates a tab - hiding one must leave the reader where they are (SPEC V93 revoked)" }
elseif ($visFnCode -match 'denied\s*=|if\s+denied') { Fail "V217 the shown-to-hidden flag is still live in applyTabVisibility - a dead variable is how the jump crawls back" }
else { Pass "V217 hiding a tab moves nobody" }

# The four switched tabs must exist under those names, and every tab needs one (SPEC I1b).
# The panes ARE the tabs since the 78th round (SPEC I32): a <layout> holding an <import>,
# which in the root form is exactly the eleven and nothing else. Selecting on the import
# rather than on a name prefix keeps tabStrip out without naming it.
$tabNodes = (Doc $rootPath).SelectNodes("//layout[import]")
$tabNames = @($tabNodes | ForEach-Object { $_.GetAttribute("name") })
$unnamed = @($tabNames | Where-Object { -not $_ })
$wantTabs = @('tabNumina', 'tabDisciplines', 'tabStoryteller')
$missingTabs = @($wantTabs | Where-Object { $tabNames -notcontains $_ })
if ($tabNodes.Count -ne 11) { Fail "V89 expected 11 panes (SPEC I1b/I1c, I32), found $($tabNodes.Count)" }
elseif ($unnamed.Count) { Fail "V89 $($unnamed.Count) pane(s) carry no name - the renderer addresses them by name" }
elseif ($missingTabs) { foreach ($t in $missingTabs) { Fail "V89 no pane named $t - its flag would switch nothing" } }
else { Pass "V89 all 11 panes named, including the three the renderer switches" }

# ---- V81 + V82: saving the initial character is one-shot, and says so ------------
# The only irreversible action on this sheet. It must ask first, refuse a second write, and
# leave a button that cannot be pressed again.
$stDoc = Join-Path $dir "WoD20.10.lfm"
if (-not (Test-Path $stDoc)) { Fail "V81 WoD20.10.lfm (Storyteller tab) is missing" }
else {
    $stTxt  = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($stDoc))
    $saveFn = LuaFn $stTxt 'saveBaseline'
    $stateFn = LuaFn $stTxt 'renderBaselineState'

    if (-not $saveFn) { Fail "V81 saveBaseline not found on WoD20.10" }
    elseif ($saveFn -notmatch 'Dialogs\.confirmOkCancel') { Fail "V81 saveBaseline writes without asking (SPEC R30) - the action cannot be undone" }
    elseif (([regex]::Matches($saveFn, 'sheet\.baseline ~= nil and sheet\.baseline ~= ""')).Count -lt 2) { Fail "V81 saveBaseline does not re-check the baseline inside the callback - a second client could overwrite it" }
    elseif ($saveFn -notmatch 'setField\("baseline", ndb\.exportXML\(sheet\)\)') { Fail "V81 saveBaseline does not snapshot the sheet (SPEC R32)" }
    else { Pass "V81 baseline is written once, behind a confirmation" }

    if (-not $stateFn) { Fail "V82 renderBaselineState not found on WoD20.10" }
    elseif ($stateFn -notmatch 'btnSaveBaseline\.enabled = not saved') { Fail "V82 the Save button stays live after the baseline exists" }
    elseif ($stateFn -notmatch 'btnSaveBaseline\.opacity') { Fail "V82 the Save button locks without dimming - the dead button IS the message now, so it has to read dead (SPEC V112, V243b)" }
    elseif ($stateFn -match 'dynBaselineState') { Fail "V82 renderBaselineState still writes dynBaselineState - the state sentence was removed in the 85th round and the name is burned (SPEC I42, V243b)" }
    else { Pass "V82 Save goes dead once the baseline exists, and the dead button is the message (SPEC V243b)" }

    # No field of the ledger's own may be edited from here, and the flags must be real fields.
    $stFields = @((Doc $stDoc).SelectNodes("//*[@field]") | ForEach-Object { $_.GetAttribute("field") })
    $wantFlags = @('stBackgroundCost', 'stShowNumina', 'stShowDisciplines', 'stFreeBuy', 'stSpecCost')
    $missFlags = @($wantFlags | Where-Object { $stFields -notcontains $_ })
    if ($missFlags) { foreach ($f in $missFlags) { Fail "V89 WoD20.10 has no widget for $f" } }
    else { Pass "V89 all $($wantFlags.Count) storyteller flags are owned by WoD20.10" }
}

# ---- V94: a switched tab is AUTHORED in the state its flag defaults to ------------
# The 27th round authored all of them visible and left the hiding to the renderer, so a fresh
# sheet with every checkbox unticked opened with three tabs the player was never meant to see
# (SPEC B26). The static state fails closed too now.
# Rewritten for the strip (SPEC I32, T524). B26 was "a fresh sheet whose script never ran
# showed three tabs nobody was meant to see", and that failure moved from the <tab> to the
# BUTTON: the pane is authored hidden either way now (nothing shows until Lua picks a tab,
# which fails closed harder than before), so what has to fail closed is the button.
$stripDoc = Doc $rootPath
$openPane = @($tabNodes | Where-Object { $_.GetAttribute("visible") -ne 'false' })
$btnHidden = @('btnTabDisciplines', 'btnTabStoryteller')
$btnOpen = @($btnHidden | Where-Object {
    $b = $stripDoc.SelectSingleNode("//rectangle[@name='$_']")
    $null -eq $b -or $b.GetAttribute("visible") -ne 'false'
})
$btnNumina = $stripDoc.SelectSingleNode("//rectangle[@name='btnTabNumina']")
$hiddenByDefault = @('tabDisciplines', 'tabStoryteller')
$authoredOpen = @($tabNodes | Where-Object { $hiddenByDefault -contains $_.GetAttribute("name") -and $_.GetAttribute("visible") -ne 'false' })
$numinaTab = @($tabNodes | Where-Object { $_.GetAttribute("name") -eq 'tabNumina' })
if ($openPane.Count) { Fail "V94 $($openPane.Count) pane(s) are authored visible - a sheet whose script never ran would show content nobody chose (SPEC B26)" }
elseif ($btnOpen.Count) { foreach ($b in $btnOpen) { Fail "V94 $b is missing or authored visible - a fresh sheet would offer a tab its flag has not granted (SPEC B26)" } }
elseif ($null -eq $btnNumina) { Fail "V94 btnTabNumina does not exist - the tab its flag defaults ON for has no button" }
elseif ($btnNumina.GetAttribute("visible") -eq 'false') { Fail "V94 btnTabNumina is authored hidden - its flag defaults ON (SPEC V89)" }
else { Pass "V94 every pane authored hidden, the two managed buttons too, Numina's button open" }

# ---- V95: the switch is triggered from the ROOT, not from the tab it hides ---------
# WoD20.10 owns the checkboxes but is itself one of the four tabs being switched. A tab cannot be
# trusted to run the code that hides its neighbours - that is exactly how B26 happened.
$rootReady = [regex]::Match($root, '<event name="onNodeReady">(.*?)</event>', 'Singleline')
if ($root -notmatch "dataLink fields=""{'stShowNumina', 'stShowDisciplines'}""") { Fail "V95 the root form does not watch the two show-a-tab flags" }
elseif (-not $rootReady.Success) { Fail "V95 the root form has no onNodeReady - nothing would apply tab visibility on load" }
elseif ($rootReady.Groups[1].Value -notmatch 'applyTabVisibility\(') { Fail "V95 the root onNodeReady does not apply tab visibility" }
elseif ($stTxt -match 'applyTabVisibility') { Fail "V95 WoD20.10 still triggers the switch - it is one of the tabs being hidden (SPEC B26)" }
else { Pass "V95 the tab switch is triggered from the root form" }

# ---- V96: visibility is DERIVED, asked again every time the sheet is shown ---------
# Nothing announces a change of role in the room - neither `jogador` nor `mesa` publishes an
# event (SPEC R34) - so an answer taken once at load freezes. A storyteller who had opened
# this sheet as a player came back to it with no Storyteller tab, and the checkboxes that
# would switch it back are on that tab (SPEC B27).
$rootShow = [regex]::Match($root, '<event name="onShow">(.*?)</event>', 'Singleline')
if (-not $rootShow.Success) { Fail "V96 the root form has no onShow - tab visibility would be decided once and frozen (SPEC B27)" }
elseif ($rootShow.Groups[1].Value -notmatch 'applyTabVisibility\(') { Fail "V96 the root onShow does not recompute tab visibility" }
else { Pass "V96 tab visibility is asked again every time the sheet is shown" }

# ---- V83 + V84: the log is derived, not stored -----------------------------------
# Four columns written from ONE row list, so a line reads across, and none of them owns a
# field: a stored copy of a derived number is the one thing that can drift from what produced
# it (the same call V29 made for the description block and the total above).
$progDoc = Join-Path $dir "WoD20.9.lfm"
$progTxt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($progDoc))
$ledFn   = LuaFn $progTxt 'renderXPLedger'
$rowsFn  = LuaFn $root 'xpLedgerRows'

$cols = @('dynXpType', 'dynXpTrait', 'dynXpLevel', 'dynXpCost')
$colNodes = @((Doc $progDoc).SelectNodes("//textEditor") | Where-Object { $cols -contains $_.GetAttribute("name") })
$colWithField = @($colNodes | Where-Object { $_.GetAttribute("field") })
if ($colNodes.Count -ne 4) { Fail "V84 expected 4 ledger columns on WoD20.9, found $($colNodes.Count)" }
elseif ($colWithField.Count) { Fail "V84 a ledger column owns a field - the derived log would be stored twice" }
elseif (@($colNodes | Where-Object { $_.GetAttribute("readOnly") -ne 'true' }).Count) { Fail "V84 a ledger column is editable - the log is derived, not typed" }
else { Pass "V84 four read-only ledger columns, none owning a field" }

if (-not $ledFn) { Fail "V83 renderXPLedger not found on WoD20.9" }
elseif ($ledFn -notmatch 'if rows == nil then rows = xpLedgerRows\(\); end;') { Fail "V83 renderXPLedger does not rebuild the rows - it would render stale state" }
elseif ($ledFn -notmatch 'if rows == nil then') { Fail "V83 renderXPLedger has no state text for a sheet with no baseline (SPEC V33)" }
elseif (@($cols | Where-Object { $ledFn -notmatch $_ }).Count) { Fail "V83 renderXPLedger does not write all four columns - a line would stop reading across" }
else { Pass "V83 the ledger is rebuilt from scratch on every render" }

if (-not $rowsFn) { Fail "V83 xpLedgerRows not found on the root form" }
elseif ($rowsFn -notmatch 'local rows = \{\};') { Fail "V83 xpLedgerRows does not start from an empty list - rows would accumulate across renders" }
elseif ($rowsFn -notmatch 'if base == nil then return nil; end;') { Fail "V83 xpLedgerRows does not report a missing baseline" }
else { Pass "V83 xpLedgerRows starts empty and reports a missing baseline" }

# ---- V97: the report is rebuilt every time the tab is shown -----------------------
# What feeds the log is ~460 dots on other tabs. The only thing watching them was an SDK
# observer with no precedent in any sheet in this repo; while it silently did nothing, the
# report stayed frozen at whatever it showed when the sheet loaded (SPEC B28).
$progShow = [regex]::Match($progTxt, '<event name="onShow">(.*?)</event>', 'Singleline')
if (-not $progShow.Success) { Fail "V97 WoD20.9 has no onShow - the report would be built once and then frozen (SPEC B28)" }
elseif ($progShow.Groups[1].Value -notmatch 'renderXPLedger\(') { Fail "V97 WoD20.9's onShow does not rebuild the ledger" }
elseif ($progShow.Groups[1].Value -notmatch 'renderXPBoxes\(') { Fail "V97 WoD20.9's onShow does not rebuild the three numbers" }
else { Pass "V97 the Progress report is rebuilt on every show" }

# ---- V98: an empty log says WHY it is empty ---------------------------------------
# A saved character with nothing bought on top of it wrote four blank columns, which reads
# like a tab that failed - the same call V33 makes for the no-baseline case.
if ($ledFn -notmatch '#rows == 0') { Fail "V98 an empty ledger writes four blank columns - indistinguishable from a broken tab" }
elseif ($ledFn -notmatch 'xpEmptyLog\(form, translateSheetText\(') { Fail "V98 the empty-log text is not translated (SPEC V70)" }
else { Pass "V98 an empty log says nothing was bought yet" }

# ---- V99: every rating dot goes through the guard ---------------------------------
# One dot wired straight to its field is one trait that can be raised with an empty pocket,
# and nothing would show it: the log would price the point and the balance would go negative.
# That used to be every imageCheckBox on the sheet - the willpower boxes are checkBoxes and
# the Combat mirror is images. The blood pool broke the premise (SPEC V219, B50): twenty dots
# wearing the rating art that are a RESOURCE, spent and regained inside a scene, never priced
# and never in XP_TRAIT. They are named here by field PREFIX rather than by loosening the rule
# for everyone, because a loosened V134 lets ANY dot mark itself, and that is B38 walking back
# in. Same standing as willpower_c*: spent points, not a rating.
#
# quint_* is the second of that family (SPEC I60, V264b): ten dots of Quintessence, spent and
# regained like blood, with no price in I9 and no line in the ledger. Named by prefix for the
# same reason and on the same terms - the exception is NOMINAL, never a loosened rule.
$dots = @()
$dotsUnguarded = @()
foreach ($f in $files) {
    foreach ($n in (Doc $f.FullName).SelectNodes("//imageCheckBox")) {
        if (-not $n.GetAttribute("field")) { continue }   # display-only mirror (V51): owns nothing, buys nothing
        if ($n.GetAttribute("field") -match '^bloodPool_\d+$') { continue }   # free resource, not a rating (V219)
        if ($n.GetAttribute("field") -match '^quint_\d+$') { continue }       # free resource, not a rating (V264b)
        $dots += $n
        if ($n.GetAttribute("onClick") -notmatch 'xpClick\(') { $dotsUnguarded += "$($f.Name) $($n.GetAttribute('field'))" }
    }
}
if ($dotsUnguarded) { foreach ($d in $dotsUnguarded) { Fail "V99 dot '$d' does not call xpClick on the click - that trait could be raised with no experience left" } }
else { Pass "V99 all $($dots.Count) rating dots ask xpClick before anything is marked" }

# ---- V134: the host marks nothing on its own (SPEC B38) ------------------------------
# Three rounds tried to let the click land and take it back. It cannot be taken back: a
# write made from inside the dot own onChange does not survive the dispatch. So the dot is
# told not to change itself, and the mark becomes the sheet decision to make.
$dotsAuto = @($dots | Where-Object { $_.GetAttribute("autoChange") -ne 'false' })
$dotsOnChange = @($dots | Where-Object { $_.GetAttribute("onChange") -ne "" })
if ($dotsAuto.Count -gt 0) { Fail "V134 $($dotsAuto.Count) rating dot(s) still mark themselves - autoChange is not false, so the host writes before anyone is asked (SPEC B38)" }
elseif ($dotsOnChange.Count -gt 0) { Fail "V134 $($dotsOnChange.Count) rating dot(s) still carry an onChange - two decision paths for one click" }
else { Pass "V134 all $($dots.Count) rating dots leave the marking to xpClick" }

# The guard only knows the traits XP_TRAIT declares, and it is built from the ledger's own
# lists so the two cannot disagree (SPEC V86). A dropped category would leave those dots
# silently unguarded - the call is there, the table just says nothing about them.
$traitTbl = [regex]::Match($root, 'XP_TRAIT = \{\};(.*?)local function numinaTypeInUse', 'Singleline')
$catMiss = @()
if (-not $traitTbl.Success) { Fail "V99 XP_TRAIT is not declared on the root form" }
else {
    foreach ($cat in @('XP_ATTRS', '"appearance"', 'ABILITY_FIELD', 'XP_CUSTOM', 'XP_VIRTUES', '"humanity"', '"willpower"', '"background_"', 'XP_NUMINA', '"faith"')) {
        if ($traitTbl.Groups[1].Value -notmatch [regex]::Escape($cat)) { $catMiss += $cat }
    }
    if ($catMiss) { foreach ($c in $catMiss) { Fail "V99 XP_TRAIT never declares $c - dots of that kind would pass the guard untouched" } }
    else { Pass "V99 XP_TRAIT covers all ten trait kinds the ledger prices" }
}

# ---- V100: experience is worked out, never booked ---------------------------------
# The balance is xpTotal minus the log. A deducted balance would be a second count of the
# same purchases, and the two would part company the first time a click was missed.
$guardFn = LuaFn $root 'xpClick'
$boxesFn = LuaFn $root 'renderXPBoxes'
$paintFn = LuaFn $root 'xpPaint'
$writes = @([regex]::Matches($root, 'setField\("xpTotal",')).Count
if (-not $guardFn) { Fail "V100 xpClick not found on the root form" }
elseif (-not $boxesFn) { Fail "V100 renderXPBoxes not found on the root form" }
elseif (-not $paintFn) { Fail "V100 xpPaint not found on the root form" }
elseif ($guardFn -match 'setField\("xpTotal",') { Fail "V100 the guard writes experience - a purchase would be counted twice (SPEC V83)" }
elseif ($writes -ne 2) { Fail "V100 xpTotal is written in $writes places - it may only be seeded once and set by the player (SPEC I11)" }
elseif ($paintFn -notmatch 'xpSpent\(\)') { Fail "V100 the boxes do not read the log - Spent would be a stored number" }
else { Pass "V100 experience is derived: one number saved, two worked out" }

# ---- V107: a write the sheet makes to ITSELF is not a click -----------------------
# The era renderer rebinds 150 dots, and both the language and the theme link fire it on
# load. With the guard live on every one of those writes that was ~300 walks of the ledger
# before the sheet drew - the freeze in B30. imageCheckBox has no onUserChange to tell a
# click from a write (SPEC R37), so the flag is how the sheet says which is which.
$eraFn = LuaFn $hh6 'renderAbilityLabels'
if ($root -notmatch 'xpQuiet = false;') { Fail "V107 xpQuiet is not declared on the root form" }
elseif ($boxesFn -notmatch 'if xpQuiet then return') { Fail "V107 the dataLink entry runs on the sheet's own writes (SPEC B30) - that entry is the one the flag exists for" }
elseif ($paintFn -notmatch 'xpQuiet = true') { Fail "V107 seeding xpTotal is not done quietly - the links watching it call straight back in" }
elseif (-not $eraFn) { Fail "V107 renderAbilityLabels not found on WoD20.6" }
elseif ($eraFn -notmatch 'xpQuiet = true') { Fail "V107 the era renderer rebinds its dots with the guard live - the sheet freezes on load (SPEC B30)" }
elseif ($eraFn -notmatch 'xpQuiet = false') { Fail "V107 the era renderer never lowers xpQuiet - the guard would stay asleep for good" }
else { Pass "V107 the sheet's own writes are quiet, and the flag comes back down" }

# ---- V108: one walk of the ledger per click ---------------------------------------
# The dot used to call the guard AND the box renderer, and each walked the whole ledger.
$dotsDouble = @($dots | Where-Object { $_.GetAttribute("onChange") -match 'renderXPBoxes' })
if ($dotsDouble) { Fail "V108 $($dotsDouble.Count) dot(s) call renderXPBoxes beside the guard - each click would walk the ledger twice" }
elseif ($guardFn -notmatch 'renderAllXPBoxes\(form, spent\)') { Fail "V108 the guard does not hand its count to the boxes - they would build the same one again" }
else { Pass "V108 a click walks the ledger once" }

# ---- V101: the guard sleeps until the character is frozen -------------------------
# Building a character spends nothing, so a balance of zero must not stop the first dot.
if ($guardFn -notmatch 'baselineOf\(\)') { Fail "V101 the guard does not look for a baseline - a character could not be built at all" }
elseif ($guardFn -notmatch '(?s)if base == nil then.{0,220}return;') { Fail "V101 the guard does not stand down without a baseline (SPEC I8c)" }
else { Pass "V101 the guard sleeps until the storyteller freezes the character" }

# ---- V103: only a point that is IN the log can be sold back -----------------------
if ($guardFn -notmatch 'traitLevel\(base,') { Fail "V103 the guard never reads the baseline rating - it would let a frozen point be sold" }
elseif ($guardFn -notmatch '(?s)if after < traitLevel\(base,.{0,80}return;') { Fail "V103 the guard does not refuse a sale that would drop the trait below the frozen character" }
else { Pass "V103 a point the storyteller froze cannot be sold back" }

# ---- V102: the three experience boxes own no field --------------------------------
# The orphan check above already refuses `experience` and `spentXP` anywhere; this is the
# other half - the boxes that replaced them must not have quietly taken a field of their own.
$xpBoxes = @('edtTotalXP', 'edtSpentXP', 'edtCurrentXP', 'edtCurrentXPMain')
$xpOwning = @()
foreach ($f in $files) {
    foreach ($n in (Doc $f.FullName).SelectNodes("//edit")) {
        if ($xpBoxes -contains $n.GetAttribute("name") -and $n.GetAttribute("field")) { $xpOwning += $n.GetAttribute("name") }
    }
}
if ($xpOwning) { foreach ($b in $xpOwning) { Fail "V102 '$b' owns a field - a derived number would be stored beside the log that produces it" } }
elseif ($paintFn -notmatch 'sheet\.experience') { Fail "V102 nothing seeds xpTotal from the old balance - every sheet would come back to zero (SPEC I11)" }
else { Pass "V102 the four experience boxes own no field, and the old balance seeds the total" }

# ---- V91: xpLog is gone, and stays gone ------------------------------------------
# The free-text box the ledger replaced. Reusing the name would bring a player's old notes
# back up inside a column that means something else now (SPEC I3, orphans).
$xpLogUsers = @($files | Where-Object {
    ([System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($_.FullName))) -match 'field="xpLog"'
})
if ($xpLogUsers) { foreach ($f in $xpLogUsers) { Fail "V91 $($f.Name) still owns xpLog - it is an orphan now" } }
else { Pass "V91 xpLog owned by nothing" }

# ---- V85: ONE reading of a trait's rating ----------------------------------------
# Both sides of the diff have to count the same way, and willpower has two rows of ten - the
# dots are the rating, the boxes are spent points. Counting the boxes would price a spent
# point as a purchase.
$lvlFn = LuaFn $root 'traitLevel'
if (-not $lvlFn) { Fail "V85 traitLevel not found on the root form" }
elseif ($rowsFn -match 'willpower_c') { Fail "V85 the ledger reads the willpower BOXES - those are spent points, not rating" }
elseif ($rowsFn -notmatch 'traitLevel\(base,' -or $rowsFn -notmatch 'liveLevel\(') { Fail "V85 the ledger does not read both sides through traitLevel" }
elseif ($rowsFn -match '(?m)^\s*for i = 1, 5, 1 do') { Fail "V85 xpLedgerRows counts dots itself instead of calling traitLevel" }
else { Pass "V85 both sides of the diff read ratings through traitLevel (the live side through liveLevel, which adds only the pending click)" }

# Attributes and virtues start at _2 with a fixed first dot; Appearance is the declared
# exception (SPEC I3). Getting this wrong prices every attribute one step off.
if ($rowsFn -notmatch 'traitLevel\(base, field, 2, 5, 1\)') { Fail "V85 virtues are not read with the fixed first dot" }
elseif ($rowsFn -notmatch 'if field == "appearance" then first, fixed = 1, 0; end;') { Fail "V85 Appearance is not read as the exception it is (its first dot can be switched off)" }
else { Pass "V85 fixed first dots and the Appearance exception are read correctly" }

# ---- V104: Appearance starts at one, and only when untouched ----------------------
# It is the one attribute drawn without a fixed first dot, so a character CAN be at zero -
# but zero was never the default. nil is nobody having touched the dot; false is the player's
# decision, and a seed that ignored that would put the dot back on at every load.
$rootReady2 = [regex]::Match($root, '<event name="onNodeReady">(.*?)</event>', 'Singleline')
if (-not $rootReady2.Success) { Fail "V104 the root form has no onNodeReady - nothing would seed Appearance" }
elseif ($rootReady2.Groups[1].Value -notmatch 'sheet\.appearance_1 == nil') { Fail "V104 the Appearance seed is not guarded by nil - a dot the player switched off would come back on every load" }
elseif ($rootReady2.Groups[1].Value -notmatch 'setField\("appearance_1", true\)') { Fail "V104 nothing seeds appearance_1 - a new sheet would read Appearance 0" }
else { Pass "V104 Appearance is seeded to one, and only when nobody has touched it" }

# ---- V86: the cost table, once, and the dormant rules NOT written -----------------
# Every active rule from SPEC I9 exactly once. The dormant ones - Discipline, Path, Sphere,
# Arete - have no field to read while those tabs are empty, so writing them would be code that
# nothing can reach and nothing can test.
$costFn = LuaFn $root 'xpCost'
if (-not $costFn) { Fail "V86 xpCost not found on the root form" }
else {
    $costWant = @{
        'Attribute'  = 'return from \* 4'
        'Ability'    = 'if from == 0 then return 3; end; return from \* 2'
        'Virtue'     = 'if kind == "Virtue"    then return from \* 2'
        'Humanity'   = 'if kind == "Humanity"  then return from \* 2'
        'Willpower'  = 'if kind == "Willpower" then return from;'
        # A PRICE since the 88th round, not a flag (SPEC I51). Two keys and both anchored on the
        # kind, for the reason spelled out below: `return from * ctx.backgroundCost` is the whole
        # rule, so a bare pattern would go green off either half of it.
        'Background (first dot)' = 'kind == "Background" then[\s\S]+?if from == 0 then return ctx\.backgroundCost; end;'
        'Background (rest)'      = 'if from == 0 then return ctx\.backgroundCost; end;\s+return from \* ctx\.backgroundCost;'
        'Numina'     = 'kind == "Numina" then[\s\S]+?return from \* 7;'
        # SPEC I9b, 51st round: the two rules that were dormant since the 27th are awake now
        # that the Vampire tab owns fields to read. Sphere and Arete stay dormant below - the
        # Magika tab that would have owned them was deleted in the 39th round.
        #
        # Prices rewritten 2026-08-22 (SPEC I9). Every key is anchored on its own `kind` from
        # here on, because the new table makes bare patterns lie: both first dots now cost 20,
        # so 'Discipline (first dot)' would pass reading Path's line and vice versa - the rule
        # could be DELETED and the check would still go green (SPEC B7, V20). The non-clan rule
        # gets a key of its own for the same reason: it had none, and its old x7 was what the
        # Numina key was matching all along.
        'Discipline (first dot)' = 'kind == "Discipline" then\s+if from == 0 then return 20; end;'
        'Discipline (clan)'      = 'if ctx\.inClan then return from \* 15; end;'
        'Discipline (non-clan)'  = 'if ctx\.inClan then return from \* 15; end;\s+return from \* 25;'
        'Path (first dot)'       = 'kind == "Path" then\s+if from == 0 then return 20; end;'
        'Path (rest)'            = 'kind == "Path" then\s+if from == 0 then return 20; end;\s+return from \* 15;'
    }
    $costBad = @()
    foreach ($k in $costWant.Keys) { if ($costFn -notmatch $costWant[$k]) { $costBad += "$k is not priced as SPEC I9 states" } }
    # Read the CODE, not the prose: the comment above xpCost names the dormant rules on purpose,
    # so the check looks for a dormant kind being handled - a quoted kind string - rather than
    # for the word appearing anywhere in the file.
    foreach ($dead in @('Sphere', 'Arete')) {
        if ($costFn -match "kind == `"$dead`"") { $costBad += "$dead is priced here - SPEC I9 keeps it dormant until a field exists to read" }
        if ($rowsFn -match "`"$dead`"") { $costBad += "$dead produces ledger rows - the tab that would own it is still empty" }
    }
    # The guard that used to sit here is GONE, and its absence is the invariant now (SPEC I51):
    # a background at price 0 has to come out of the arithmetic, because a guard returning 0
    # early would make `stBackgroundCost` = 0 indistinguishable from a rule that never ran. The
    # flag it read is an ORPHAN and the name is burned on both sides (SPEC I3, V2).
    if ($costFn -match 'backgroundsXP') { $costBad += "xpCost still reads ctx.backgroundsXP - the flag became a price in the 88th round and zero comes out of the arithmetic now (SPEC I51)" }
    if ($root -match 'sheet\.stBackgroundsXP') { $costBad += "the root form still reads sheet.stBackgroundsXP - it is a declared ORPHAN since the 88th round (SPEC I3, V2, I51)" }
    if ($costFn -notmatch 'if ctx\.affinity then return from \* 6; end;') { $costBad += "the affinity path is not cheaper than the rest" }

    if ($costBad) { foreach ($b in $costBad) { Fail "V86 $b" } }
    else { Pass "V86 every active cost rule written once, every dormant one left out" }
}

# ---- V87: 21 buys the first point of a KIND, not of a numina ---------------------
# A character with static magic paying for their first psychic phenomenon pays 21 once; the
# next psychic numina costs the ordinary 7. Pricing it per numina would charge 21 every time.
if ($costFn -notmatch 'if ctx\.newType then return 21; end;') { Fail "V87 the 21 is not gated on the kind being new" }
elseif ($costFn -notmatch '(?ms)if from == 0 then.*?return 7;') { Fail "V87 an ordinary first point does not cost 7" }
elseif ($rowsFn -notmatch 'ctx\.newType\s*=\s*\(from == 0\) and not unlocked\[grp\.kind\]') { Fail "V87 the ledger does not decide newType from the kinds the baseline already had" }
elseif ($rowsFn -notmatch 'unlocked\[grp\.kind\] = true;') { Fail "V87 a kind is never marked bought - every numina of it would cost 21" }
else { Pass "V87 21 is charged once per new kind of numina" }

# ---- V88: the affinity path is the FIRST hedge row, always ------------------------
# Marked in the XML, priced in the Lua, and the two have to name the same row. Bold is the
# whole of the mark since the 28th round - the star that stood in the box's left margin came
# off at the user's request, and only the footnote under TRUE FAITH keeps one.
$numDoc = Join-Path $dir "WoD20.7.lfm"
$numTxt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($numDoc))
if ($numTxt -match '<label[^>]*text="\*"') { Fail "V88 the star label is back on the hedge box - bold is the whole mark now (28th round)" }
elseif ($numTxt -notmatch '<HedgePicker field="numina_1" style="bold"/>') { Fail "V88 the first hedge row is not marked bold" }
elseif ($numTxt -match '<HedgePicker field="numina_(?!1")\d+" style="bold"/>') { Fail "V88 a row other than the first is marked as the affinity path" }
elseif ($numTxt -notmatch 'text="\* The first path is the Affinity Path"') { Fail "V88 the note explaining the affinity path is missing - it rides at the right end of hedgeStrip since the 91st round (SPEC C, I64)" }
elseif ($rowsFn -notmatch 'ctx\.affinity = \(field == "numina_1"\)') { Fail "V88 the cheaper rate is not tied to numina_1" }
else { Pass "V88 numina_1 is the affinity path in both the XML and the pricing" }

# ---- V90: the two empty tabs stay empty ------------------------------------------
# A field name cannot be renamed after release without losing what players saved under it
# (SPEC V2), so none is spent before the content that would use it exists.
# WoD20.11 left this list on 2026-08-22 (SPEC B50): it carries the Dominator and Blood Pool
# boxes now, so it declares fields on purpose. Magika, the other subject this invariant was
# written for, was deleted in the 39th round. The list is empty and the check says so rather
# than looping over nothing and reporting a pass it did not earn (SPEC V209).
$emptyShells = @()
if (-not $emptyShells.Count) { Pass "V90 no empty shell left to guard - WoD20.11 carries content (B50), Magika is gone (V128)" }
foreach ($empty in $emptyShells) {
    $ep = Join-Path $dir $empty
    if (-not (Test-Path $ep)) { Fail "V90 $empty is missing" ; continue }
    $eFields = @((Doc $ep).SelectNodes("//*[@field]"))
    if ($eFields.Count) { Fail "V90 $empty declares $($eFields.Count) field(s) before it has content" }
    else { Pass "V90 $empty declares no field" }
}


# ---- V111: dimmed <=> read-only, in both directions -------------------------------
# The sheet has no other way to say "look, do not touch": a mirror dot and a real dot are the
# same art, and a read-only edit is the same box as an editable one. Opacity is the whole of
# the signal (SPEC C, 33rd round), so it has to be exact and it has to be exclusive - a live
# field wearing the dim value lies in the other direction and would teach the player to
# The theme never writes opacity (V57/V66 family), so what the XML says is what renders in
# all four eras.
# TWO numbers since 2026-08-22, one per NATURE of widget (SPEC I43, V244, user's third item):
# text that is dimmed goes to 0.75 and ART stays at 0.40. The request was literally "whenever
# some TEXT is given transparency, use 75%", and a dot is not text - it is a mark whose whole
# job is to read as on or off, and 0.40 is what tells a mirrored dot from a live one.
#
# These two are NOT the only place the numbers live: V112 (Lua, a button - TEXT) and V179
# (an attribute, main path dots - ART) each carry their own literal, because they match a
# different thing than this sweep does. Change one, change all three - they are named here by
# CHECK ID rather than by line, because line numbers rot (SPEC V209). V162 and V175 hold no
# literal at all: they forbid a combo painting opacity AT ALL (SPEC V241).
$DIM_TEXT = '0.75'
$DIM_ART  = '0.40'

# The six description panes are the ONE read-only thing that stays bright (SPEC V111(2),
# user 2026-08-22). They are read-only and they are also the text the player opened the tab
# to read, and dimming the content to signal "do not edit" charges legibility for a warning
# the frame and the missing caret already give. Named, like bloodPool_* in V219 and
# willpower_c* in V99 - loosening (a) instead would let ANY read-only widget go bright, and
# (a) exists because a mirror dot and a real dot are the same art.
# NINE until T872: edtDiscDesc, edtPathDesc and edtRitualDesc left with the three Ghoul panes
# wave 3 removed (SPEC V111 amended, I118e, which measured this 9 -> 6 before the round began).
# TWO since T874: wave 4 took the four Numina panes with it - edtHedgeDesc, edtPsychicDesc,
# edtHedgeRitualDesc and edtFaithDesc - and what is left is the pair that is not a pane at all,
# the ? box and the search box (SPEC V365d, V365f, I102i). The roster shrinks to what still
# exists rather than keeping names as documentation: the count leg below reads it, so a name
# with no widget behind it would fail this check every round for a pane nobody wants back.
$DESC_BRIGHT = @('edtPopDesc','edtMfDesc')
$comboBright = 0
$descSeen = 0
$dimSeen = 0
$dimBefore = $fail
foreach ($f in $files) {
    foreach ($n in (Doc $f.FullName).SelectNodes("//edit | //textEditor | //comboBox | //checkBox | //imageCheckBox")) {
        # `enabled="false"` means READ-ONLY only while the control is on screen. A widget
        # authored `visible="false"` is HIDDEN, not locked: nobody is reading it, so there is
        # nothing to dim - and dimming it would be wrong the moment Lua reveals it, because the
        # reveal writes `visible` and `enabled` and never opacity (SPEC I71, V274c, V274d). The
        # branch below still refuses opacity on a hidden one, so this is an exemption from the
        # dim rule and not from measurement.
        $ro = ($n.GetAttribute("readOnly") -eq 'true') -or
              ($n.GetAttribute("enabled")  -eq 'false' -and $n.GetAttribute("visible") -ne 'false') -or
              ($n.LocalName -eq 'imageCheckBox' -and $n.GetAttribute("autoChange") -eq 'false' -and -not $n.GetAttribute("onClick"))
        $op = $n.GetAttribute("opacity")
        $id = $n.GetAttribute("name"); if (-not $id) { $id = $n.GetAttribute("field") }; if (-not $id) { $id = $n.LocalName }

        # A comboBox never carries opacity, locked or not (SPEC I41, V241, user
        # 2026-08-22): the property dims the control and the TEXT with it, and there is
        # no text-only opacity to reach for - so the only way to keep a locked dropdown
        # readable is not to have one. Nominal, exactly like $DESC_BRIGHT above:
        # loosening (a) for every read-only widget is a different rule, and not this one.
        if ($n.LocalName -eq 'comboBox') {
            $comboBright++
            if ($op -ne '') { Fail "V111 $($f.Name) $id is a comboBox carrying opacity '$op' - it would dim the text the player has to read (SPEC I41, V241)" }
        } elseif ($DESC_BRIGHT -contains $id) {
            $descSeen++
            if ($op -ne '') { Fail "V111 $($f.Name) $id is a description pane and still carries opacity '$op' - the text the player opened the tab to READ is the one read-only widget that stays bright (SPEC V111(2))" }
        } elseif ($ro) {
            $dimSeen++

            # Which of the two numbers this widget answers to (SPEC V244). An imageCheckBox is
            # ART - a mark that reads on or off - and everything else the sweep sees carries
            # TEXT the player has to read.
            $want = if ($n.LocalName -eq 'imageCheckBox') { $DIM_ART } else { $DIM_TEXT }
            $kind = if ($n.LocalName -eq 'imageCheckBox') { 'art' } else { 'text' }

            if ($op -ne $want) { Fail "V111/V244 $($f.Name) $id is read-only $kind but reads live (opacity '$op', expected $want)" }
        } elseif ($op -ne '') {
            Fail "V111 $($f.Name) $id is editable but reads locked (opacity '$op')"
        }
    }
}
if ($descSeen -ne $DESC_BRIGHT.Count) { Fail "V111 $descSeen of the $($DESC_BRIGHT.Count) named description panes were found - a renamed pane would fall back into the dim rule silently, or the exception is now covering nothing (SPEC V209)" }
elseif ($comboBright -lt 9) { Fail "V111 only $comboBright comboBox(es) were read, expected the 9 the sheet declares - the exception would be covering less than it exempts (SPEC V209). Eleven DECLARATIONS, not eleven on screen: two of them live in a template and are drawn once per row. Was 17 until T872 took the four Vampire row combos away, 15 until T874 took the three Numina ones and 12 until its half B took cboHedgeAffiliation." }
elseif ($fail -eq $dimBefore) { Pass "V111/V244 $dimSeen read-only widgets dimmed - text at $DIM_TEXT, art at $DIM_ART - $descSeen description panes and $comboBright combos left bright, every editable one untouched" }

# The fixed first dot of a virtue is an <image>, not an input, so the sweep above cannot see
# it - and a bright dot beside four dimmed ones is exactly the row reading half-locked (SPEC C).
# Only the Combat mirror has one; the same art on Main belongs to an editable row.
$imgBefore = $fail
foreach ($f in $files) {
    foreach ($n in (Doc $f.FullName).SelectNodes("//image[@src]")) {
        $op   = $n.GetAttribute("opacity")
        $want = ($f.Name -eq 'WoD20.3.lfm')
        if ($want -and $op -ne $DIM_ART) { Fail "V111 $($f.Name) mirror dot1 reads live (opacity '$op', expected $DIM_ART)" }
        elseif (-not $want -and $op -ne '') { Fail "V111 $($f.Name) a dot on an editable row is dimmed (opacity '$op')" }
    }
}
if ($fail -eq $imgBefore) { Pass "V111 the fixed dot1 art is dimmed on the mirror tab and bright everywhere else" }

# ---- V244: the two numbers hold in LUA too --------------------------------------------
# The XML sweep above is only half of V244: opacity is also written at runtime, and a Lua
# write is exactly where the art number could come back in through the side door - it is the
# number that stood alone until this round, so it is the one a hand types from memory.
#
# Every runtime write on this sheet lands on a TEXT control (a button, an edit), so the ART
# literal has no business inside Lua at all. `1` and `0.75` are the two legal values: `1` is
# "not dimmed" and is not a third number for the same reason `opacity` absent is not one.
# The numbers Lua is allowed to write, NAMED. 0.75 is read-only TEXT (I43); 0.80 is the empty
# picker button of I107a2 - "not filled in", which is a different sentence from "read-only" and
# needs a different weight or the two cues collapse into one (user 2026-08-29). The ART number
# is deliberately NOT here: every runtime write on this sheet lands on text, so 0.40 inside Lua
# is the side door this check was written to shut. A number added here has to earn a clause.
$LUA_OPACITY = @($DIM_TEXT, '0.80')
$luaDimBad  = @()
$luaDimSeen = 0
foreach ($f in $files) {
    $code = CodeOf $f.FullName
    foreach ($m in [regex]::Matches($code, '(?m)\.opacity\s*=\s*([^;\r\n]+)')) {
        $luaDimSeen++
        $expr = $m.Groups[1].Value.Trim()
        foreach ($num in [regex]::Matches($expr, '\d+\.\d+')) {
            if ($LUA_OPACITY -notcontains $num.Value) {
                $luaDimBad += "$($f.Name) writes .opacity = $expr - the runtime numbers are a NAMED list and that is not one of them: $($LUA_OPACITY -join ", ") (SPEC I43, V244)"
            }
        }
    }
}
if ($luaDimSeen -lt 1) { Fail "V244 no runtime opacity write was found at all - the Lua leg is reading nothing (SPEC V20, V209)" }
elseif ($luaDimBad) { foreach ($b in $luaDimBad) { Fail "V244 $b" } }
else { Pass "V244 all $luaDimSeen runtime opacity writes use one of the named numbers ($($LUA_OPACITY -join ", ")) or 1" }

# ---- V429: a picker button never shows the text the XML authored ---------------------------
# SPEC V429, I146g, T962, user 2026-09-04 item 11. The dashed invitation is painted by mfLabel,
# and a button nothing paints keeps its authored "Select Clan" (SPEC I107a2). The clan/family
# pair was reached from the root dataLink alone - an EMPTY field never fires it - and the
# specialities from onNodeReady alone. Both tabs paint on SHOW as well now, and the dim of the
# invitation is the 0.80 the owner asked for, the second named number of V244.
$v429Bad = @()
$show429 = @(
    [pscustomobject]@{ F = 'WoD20.11.lfm'; Fn = 'renderClanFamilyButton'; Leg = 'a' }
    [pscustomobject]@{ F = 'WoD20.1.lfm';  Fn = 'renderSpecialities';     Leg = 'b' }
)
foreach ($r429 in $show429) {
    $t429 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir $r429.F)))
    $ev429 = [regex]::Match($t429, '(?s)<event name="onShow">(.*?)</event>')
    if (-not $ev429.Success) { $v429Bad += "($($r429.Leg)) $($r429.F) has no onShow event - $($r429.Fn) has no show to paint on (SPEC V429, V209)"; continue }
    if (([regex]::Replace($ev429.Groups[1].Value, '(?m)^\s*--.*$', '')) -notmatch ('\b' + $r429.Fn + '\(self\);')) { $v429Bad += "($($r429.Leg)) the onShow of $($r429.F) never calls $($r429.Fn)(self) - a sheet whose field is empty opens on the authored button text instead of the dashed invitation (SPEC V429$($r429.Leg), I146g)" }
}
$root429 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir 'WoD20th.lfm')))
$mf429 = LuaFn $root429 'mfLabel'
if (-not $mf429) { $v429Bad += "(c) mfLabel is gone from the root form - the invitation has no painter (SPEC V209)" }
elseif (([regex]::Replace($mf429, '(?m)^\s*--.*$', '')) -notmatch 'ctrl\.opacity\s*=\s*0\.80;') { $v429Bad += "(c) mfLabel does not dim the empty invitation at 0.80 - the owner asked for 80%, and it is the second named number of V244 (SPEC V429c, V244)" }
if ($v429Bad) { foreach ($b429 in $v429Bad) { Fail "V429 $b429" } }
else { Pass "V429 both picker tabs paint their buttons on show and mfLabel dims the empty invitation at 0.80" }

# ---- V112: a control that locks at runtime dims in the same breath ----------------
# btnSaveBaseline is the only one. Writing `enabled` without writing `opacity` leaves a dead
# button with a live button's face, which is the same lie V111 refuses in the XML.
$stTxt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20.10.lfm")))
if ($stTxt -notmatch 'btnSaveBaseline\.enabled\s*=\s*not saved;') { Fail "V112 the Save button is not disabled once the baseline is saved (SPEC V82)" }
elseif ($stTxt -notmatch 'btnSaveBaseline\.opacity\s*=\s*saved and 0\.75 or 1;') { Fail "V112 the Save button locks without dimming, or dims at the wrong number - a button is TEXT and dims at 0.75 (SPEC V244)" }
else { Pass "V112 the Save button dims in the same breath it locks" }


# ---- V121: nothing redraws off a whole-node observer -------------------------------
# B34: WoD20.9 registered ndb.newObserver(sheet) and redrew the whole experience log from its
# onChanged. That fires for EVERY attribute written, including the hundreds the sheet writes
# to itself while loading, and it does not pass through xpQuiet - so the shield that fixed
# B30 never covered it. On a sheet with a saved baseline every one of those writes bought a
# full walk of ~91 traits across two nodes, and opening the sheet froze Firecast.
# A new sheet did not freeze: with no baseline the ledger returns on its first line, which is
# why this only ever showed up on a character somebody had already played.
#
# A comment is allowed to name the thing it explains, so XML comments and Lua line comments
# come out before the grep - otherwise documenting the removal would fail the check.
# Lua comment lines, dropped. Every check that reads the inside of a function is looking for
# CODE, and a comment that quotes code - "written flat rather than nested under `if want
# then`" - would otherwise answer for it: two checks went red on exactly that, reading a
# sentence about the balance branch as the balance branch (SPEC V20, the B7 failure mode).
function NoComments($t) { [regex]::Replace($t, '(?m)^\s*--.*$', '') }

$obsUsers = @($files | Where-Object { (CodeOf $_.FullName) -match 'ndb\.newObserver' })
if ($obsUsers) { foreach ($o in $obsUsers) { Fail "V121 $($o.Name) hangs a redraw off ndb.newObserver - it fires on every attribute and bypasses xpQuiet (SPEC B34)" } }
else { Pass "V121 no whole-node observer - redraws come from the click, a named dataLink or onShow" }

# The replacement path must actually be wired, or the log simply stops following purchases.
# Since the 44th round that path carries no registration: the click hands the log the sheet
# ROOT and the renderer finds its own columns from there (SPEC V133/V143, B39).
$rootTxt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20th.lfm")))
$ledgerTxt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20.9.lfm")))
if ($rootTxt -notmatch 'renderXPLedger\(tabRootOf\(from\), rows\)') { Fail "V121 the click never redraws the log from the sheet root - a purchase would leave it stale" }
elseif ($rootTxt -notmatch 'xpLedgerRefresh\(form, rows\);') { Fail "V121 xpClick does not redraw the log after an accepted click" }
else { Pass "V121 the click redraws the log from the sheet root, with no handover in between" }

# ---- V122: a renderer writes a field only when the value actually changes -----------
# Writing back a value that is already there still wakes every dataLink on that field, and
# those renderers write more fields. One helper, so there is one thing to check.
if ($rootTxt -notmatch 'function setField\(name, value\)') { Fail "V122 setField is missing from the root form" }
elseif ($rootTxt -notmatch 'if sheet\[name\] ~= value then sheet\[name\] = value; end;') { Fail "V122 setField writes without comparing first - the whole point of it" }
else { Pass "V122 setField compares before it writes" }

# Every field write in the sheet goes through it. The `sheet[name]` pair inside setField
# itself is the only assignment allowed to be direct.
$directWrites = @()
foreach ($f in $files) {
    $raw = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName))
    foreach ($m in [regex]::Matches($raw, 'sheet(\[[^\]]+\]|\.[A-Za-z_][A-Za-z0-9_]*)\s*=(?!=)')) {
        if ($m.Value -match '^sheet\[name\]') { continue }
        $directWrites += "$($f.Name): $($m.Value.Trim())"
    }
}
if ($directWrites) { foreach ($w in $directWrites) { Fail "V122 field written without comparing - $w - use setField (SPEC B34)" } }
else { Pass "V122 every field write goes through setField" }

# ---- V123: a dataLink watches only what its renderer reads --------------------------
# WoD20.3 had one link over the health track AND the ~80 mirrored trait dots, so ticking any
# trait re-rendered the health track, and that render wrote all ten health rows back.
$combatTxt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20.3.lfm")))
$healthLinks = @([regex]::Matches($combatTxt, '<dataLink fields="\{([^}]*)\}"[^>]*renderHealthTrack'))
if ($healthLinks.Count -ne 1) { Fail "V123 expected exactly one dataLink driving renderHealthTrack on WoD20.3, found $($healthLinks.Count)" }
else {
    $watched = @($healthLinks[0].Groups[1].Value -split ',' | ForEach-Object { $_.Trim().Trim("'") })
    $strays  = @($watched | Where-Object { $_ -ne 'healthLevels' -and $_ -ne 'language' })
    if ($strays) { Fail "V123 the health link on WoD20.3 also watches $($strays -join ', ') - fields renderHealthTrack never reads (SPEC B34)" }
    else { Pass "V123 the health link on WoD20.3 watches only healthLevels and language" }
}
if ($combatTxt -notmatch '<dataLink fields="\{[^}]*\}"[^>]*renderCombatTraits\(self\);"\s*/>') { Fail "V123 the trait mirror on WoD20.3 has no link of its own" }
elseif ($combatTxt -match '<dataLink fields="\{[^}]*\}"[^>]*renderHealthTrack[^>]*renderCombatTraits') { Fail "V123 one link still drives both renderers on WoD20.3" }
else { Pass "V123 the trait mirror on WoD20.3 has its own link" }

# ---- V124: a whole-tree renderer runs once per open for the same state --------------
# language and sheetTheme both carry a defaultValue, so BOTH links fire when the sheet opens.
# renderAbilityLabels walked the whole control tree and rebound 150 dots each time.
$setTxt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20.6.lfm")))
if ($setTxt -notmatch 'local abilityMemo') { Fail "V124 renderAbilityLabels keeps no memo - it walks the tree twice on every open" }
elseif ($setTxt -notmatch 'if abilityMemo == memo then return; end;') { Fail "V124 the memo is kept but never short-circuits" }
elseif ($setTxt -notmatch 'abilityMemo = memo;') { Fail "V124 the memo is read but never written - it would short-circuit forever" }
else { Pass "V124 renderAbilityLabels returns early when neither era nor language moved" }

# The two writes it makes per dot are compared, like setField compares (SPEC V122). These are
# control properties, not fields, so the setField check above cannot see them.
if ($setTxt -notmatch 'if c\.field ~= full then c\.field = full; end;') { Fail "V124 renderAbilityLabels rebinds a dot without checking whether the binding changed" }
elseif ($setTxt -notmatch 'if c\.checked ~= want then c\.checked = want; end;') { Fail "V124 renderAbilityLabels reloads a dot without checking whether the state changed" }
else { Pass "V124 the dot rebind writes only what actually differs" }

# ---- V125: one walk of the ledger per click, shared by all three drawers -------------
# V108 said this when there were two drawers. There are three now: the log joined the two
# boxes, and it takes the rows rather than building them again.
if ($rootTxt -notmatch 'function xpSum\(rows\)') { Fail "V125 xpSum is missing - xpSpent cannot hand its rows to anyone" }
elseif ($rootTxt -notmatch 'function xpSpent\(\)\s*\r?\n\s*return xpSum\(xpLedgerRows\(\)\);') { Fail "V125 xpSpent does not total the rows through xpSum" }
else { Pass "V125 totalling is split from walking" }

$guardFn = [regex]::Match($rootTxt, 'function xpClick\(field, form\)(.*?)\n\t\t\tend;', 'Singleline')
if (-not $guardFn.Success) { Fail "V125 xpClick not found on the root form" }
else {
    $g = NoComments $guardFn.Groups[1].Value
    # The accepted path walks once. The refused path reads the restored state a second time -
    # the rows in hand describe the state being turned down, so they cannot be reused.
    $walks = @([regex]::Matches($g, 'xpLedgerRows\(\)')).Count
    if ($walks -gt 2) { Fail "V125 xpClick walks the ledger $walks times - one for the click, at most one more to read back a refused move" }
    elseif ($g -notmatch 'local spent = xpSum\(rows\);') { Fail "V125 xpClick does not total the rows it already built" }
    elseif ($g -notmatch 'renderAllXPBoxes\(form, spent\);') { Fail "V125 xpClick makes the boxes walk the ledger again" }
    elseif ($g -notmatch 'xpLedgerRefresh\(form, rows\);') { Fail "V125 xpClick does not hand its rows to the log" }
    else { Pass "V125 one walk per accepted click, shared by the boxes and the log" }
}
if ($ledgerTxt -notmatch 'function renderXPLedger\(node, rows\)') { Fail "V125 renderXPLedger does not accept rows - it always walks the ledger itself" }
elseif ($ledgerTxt -notmatch 'if rows == nil then rows = xpLedgerRows\(\); end;') { Fail "V125 renderXPLedger cannot build its own rows when called without them" }
else { Pass "V125 renderXPLedger takes the rows when the caller has them" }

# ---- V126: the theme art is not a third of the .rpk ---------------------------------
# The four papers were 1.10 MB of a 3.72 MB package. They go behind the content, stretched to
# fill a tab, so the pixels were never being read at that size.
$pngCap   = 120KB
$pngTotal = 400KB
$papers = @(Get-ChildItem -LiteralPath (Join-Path $dir "images") -Filter *.png)
$tooBig = @($papers | Where-Object { $_.Length -gt $pngCap })
$sum = ($papers | Measure-Object -Property Length -Sum).Sum
if ($tooBig) { foreach ($p in $tooBig) { Fail "V126 images/$($p.Name) is $([int]($p.Length/1KB)) KB, over the $([int]($pngCap/1KB)) KB ceiling" } }
elseif ($sum -gt $pngTotal) { Fail "V126 images/ totals $([int]($sum/1KB)) KB, over the $([int]($pngTotal/1KB)) KB budget" }
else { Pass "V126 all $($papers.Count) images fit the ceiling ($([int]($sum/1KB)) KB total)" }

# ---- V128: three managed tabs, and Magika is gone for good ---------------------------
# The user had the empty Magika window removed in the 39th round. Its field name stays burned
# (checked with the other orphans above): a sheet saved with it ticked still carries the value.
# 51st round: WoD20.12.lfm is BACK, this time as the Disciplines area of the Vampire tab (SPEC
# C, I16). The file NAME was only ever a proxy for "the Magika tab is gone"; what actually has
# to stay gone is the tab's IDENTITY. Kept as a name check it would now fail on a file that has
# nothing to do with Magika, so the proxy is replaced by the two things it stood for: no <tab>
# anywhere in the sheet is a Magika tab, and WoD20.12, if it is on disk at all, is the file WoD20.11
# imports for Disciplines rather than a name that came loose again.
$magikaTabs = @()
foreach ($f in $files) {
    foreach ($n in (Doc $f.FullName).SelectNodes("//tab")) {
        $id = (("{0} {1}" -f $n.GetAttribute('name'), $n.GetAttribute('title'))).Trim()
        if ($id -match 'Magika') { $magikaTabs += "$($f.Name): $id" }
    }
}
if ($magikaTabs) { foreach ($m in $magikaTabs) { Fail "V128 a Magika tab is back - $m" } }
else { Pass "V128 no tab in the sheet is a Magika tab" }

$hh12 = Join-Path $dir "WoD20.12.lfm"
if (Test-Path $hh12) {
    $imp = @((Doc (Join-Path $dir "WoD20.11.lfm")).SelectNodes("//import") | ForEach-Object { $_.GetAttribute("file") })
    if ($imp -notcontains "WoD20.12.lfm") { Fail "V128 WoD20.12.lfm is on disk but WoD20.11 does not import it - the freed name came loose again" }
    else { Pass "V128 WoD20.12.lfm is the Vampire tab's Disciplines area, imported by WoD20.11" }
}
else { Pass "V128 WoD20.12.lfm is not on disk" }

$managed = @('tabNumina', 'tabDisciplines', 'tabStoryteller')
$magikaLeft = @($files | Where-Object { (CodeOf $_.FullName) -match '(tabMagika|stShowMagika|"Show Magika")' })
if ($magikaLeft) { foreach ($m in $magikaLeft) { Fail "V128 $($m.Name) still refers to the Magika tab" } }
else { Pass "V128 nothing refers to the Magika tab any more" }

$shownMap = [regex]::Match($rootTxt, 'local shown = \{(.*?)\};', 'Singleline')
if (-not $shownMap.Success) { Fail "V128 applyTabVisibility has no shown map" }
else {
    $keys = @([regex]::Matches($shownMap.Groups[1].Value, 'tab[A-Za-z]+') | ForEach-Object { $_.Value })
    $extra = @($keys | Where-Object { $managed -notcontains $_ })
    $miss  = @($managed | Where-Object { $keys -notcontains $_ })
    if ($extra) { Fail "V128 the shown map carries $($extra -join ', ') - not a managed tab" }
    elseif ($miss) { Fail "V128 the shown map is missing $($miss -join ', ')" }
    else { Pass "V128 exactly three managed tabs" }
}
$langMagika = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($langFile))
if ($langMagika -match 'wod\.(Magika|Show Magika)=') { Fail "V128 localization.lang still carries a Magika key" }
else { Pass "V128 the Magika keys are out of localization.lang" }

# ---- V129 + V130: the refusal that speaks, and the one that must not -----------------
# The pop-up is what made B36 and B38 findable: it proved the click reached the sheet's own
# code (SPEC R42) while the mark stayed anyway. The refusal now happens BEFORE the mark, so
# the message describes a sheet nothing was written to. It stays fenced to the balance
# branch: selling back a point the storyteller paid for (V103) is a different refusal, and a
# message naming the wrong reason is worse than one that never comes.
$guardFn = [regex]::Match($rootTxt, 'function xpClick\(field, form\)(.*?)\n\t\t\tend;', 'Singleline')
if (-not $guardFn.Success) { Fail "V129 xpClick not found on the root form" }
else {
    $g     = NoComments $guardFn.Groups[1].Value
    $buy   = [regex]::Match($g, 'if want then(.*?)else', 'Singleline')
    $warns = @([regex]::Matches($g, 'xpWarn\("([^"]*)"\)'))
    # EIGHT refusals speak, and each one says its own reason: want of experience (this check),
    # the three a speciality row adds - no trait chosen, the storyteller allowing none
    # (SPEC V154/V156), a gift that is not the player's to drop - and the two a secondary path
    # adds: an empty row, and a dot above what its blood sorcery allows (SPEC V183) - and since
    # 2026-08-28 the family ceiling on a trait, which is the one T781 brought (SPEC V337). The count
    # is a census on purpose: a refusal added without coming here first turns this red.
    # What may NOT happen is a second voice inside the balance branch, or
    # the sale refusal of V103 growing one.
    $buyWarns = @([regex]::Matches($buy.Groups[1].Value, 'xpWarn\("([^"]*)"\)'))
    if (-not $buy.Success) { Fail "V129 xpClick has no purchase branch" }
    elseif ($warns.Count -ne 8) { Fail "V129 xpClick raises $($warns.Count) pop-ups - eight refusals speak: the balance, the empty trait, the closed door, the generation ceiling (SPEC V220), the gift that is not the player's to take off (SPEC V161), the secondary path row with nothing picked in it, the secondary path above what its blood sorcery allows (SPEC V183), and the family ceiling on a trait (SPEC V337, T781)" }
    elseif ($buyWarns.Count -ne 1) { Fail "V129 the balance branch raises $($buyWarns.Count) pop-ups - only the refusal for want of experience may speak there" }
    elseif ($buy.Groups[1].Value -notmatch 'xpWarn\(') { Fail "V129 the refusal for want of experience says nothing" }
    elseif ($buy.Groups[1].Value -match 'markDot\(') { Fail "V129 the refused purchase marks the dot anyway (SPEC V135)" }
    else { Pass "V129 only the balance refusal speaks, and it speaks over a sheet nothing was written to" }
}

$warnFn   = LuaFn $rootTxt 'xpWarn'
$warnMsgs = @([regex]::Matches($rootTxt, 'xpWarn\("([^"]*)"\)') | ForEach-Object { $_.Groups[1].Value } | Sort-Object -Unique)

# The picker guard does not hand xpWarn a literal: pickRefusal RETURNS the reason and
# guardPick passes it along (SPEC V181/V182/V185). Those are pop-up messages just the same,
# and a sweep that only saw xpWarn("...") would let every one of them skip V10/V28 - which is
# the exact hole this invariant exists to close. Missing pickRefusal is a failure, not a
# quiet zero: the guard being deleted must not read as "nothing left to check".
$refusalFn = LuaFn $rootTxt 'pickRefusal'
if (-not $refusalFn) { Fail "V130 pickRefusal not found on the root form - the picker refusals cannot be checked (SPEC V186)" }
else { $warnMsgs = @(($warnMsgs + @([regex]::Matches($refusalFn, 'return "([^"]*)"') | ForEach-Object { $_.Groups[1].Value })) | Sort-Object -Unique) }

if (-not $warnFn) { Fail "V130 xpWarn not found on the root form" }
elseif ($warnFn -notmatch 'translateSheetText\(') { Fail "V130 xpWarn shows its message untranslated - a [pt] sheet would read English (SPEC V70)" }
elseif ($warnFn -notmatch 'Dialogs\.showMessage\(') { Fail "V130 xpWarn opens no dialog - the refusal would stay silent" }
elseif ($warnMsgs.Count -eq 0) { Fail "V130 no pop-up message found - nothing tells the player why the dot came back" }
else {
    $badMsg = @()
    foreach ($m in $warnMsgs) {
        if (-not $enK.Contains($m)) { $badMsg += "'$m' has no [en] key - it is not authored English (SPEC V9)" }
        elseif (-not $ptK.Contains($m)) { $badMsg += "'$m' has no [pt] key" }
        elseif (-not $embedded.ContainsKey($m)) { $badMsg += "'$m' is absent from the PT map - it would never translate" }
    }
    if ($badMsg) { foreach ($b in $badMsg) { Fail "V130 $b (a string living only in Lua escapes the XML checks of V10/V28)" } }
    else { Pass "V130 all $($warnMsgs.Count) pop-up messages are English, keyed both ways and in the PT map" }
}

# ---- V131: no path leaves the character owing experience ----------------------------
# The guard only ever runs on a dot click, so the balance box was the way round it: a typed
# -5 wrote xpTotal = -5 + spent and the sheet came up owing (SPEC B35). Refused, not clamped -
# clamping would write xpTotal = Spent, and one mistyped minus sign would cost the player
# every point they had earned.
$setFn = LuaFn $rootTxt 'xpSetCurrent'
$boxFn = LuaFn $rootTxt 'xpPaint'
if (-not $setFn) { Fail "V131 xpSetCurrent not found on the root form" }
elseif ($setFn -notmatch 'if n < 0 then') { Fail "V131 xpSetCurrent takes any number it is handed - a typed -5 leaves the character owing (SPEC B35)" }
else {
    $refusal = [regex]::Match($setFn, 'if n < 0 then(.*?)end;', 'Singleline')
    if (-not $refusal.Success) { Fail "V131 the refusal branch of xpSetCurrent cannot be read" }
    elseif ($refusal.Groups[1].Value -match 'setField\("xpTotal"') { Fail "V131 the refused balance is written anyway" }
    elseif ($refusal.Groups[1].Value -notmatch 'return;') { Fail "V131 the refusal falls through and writes the negative balance below it" }
    elseif ($refusal.Groups[1].Value -notmatch 'xpWarn\(') { Fail "V131 the refused balance says nothing - the box would snap back with no reason given" }
    elseif (@([regex]::Matches($setFn, 'setField\("xpTotal"')).Count -ne 1) { Fail "V131 xpSetCurrent writes xpTotal more than once - V100 counts two writes in the file" }
    else { Pass "V131 a balance below zero is refused, not clamped, and says so" }
}
if (-not $boxFn) { Fail "V131 xpPaint not found on the root form" }
elseif ($boxFn -notmatch 'setField\("xpTotal", math\.max\(0,') { Fail "V131 the migration seed has no floor - an old sheet holding a negative experience value is born owing (SPEC I11)" }
else { Pass "V131 the one-shot migration cannot seed a negative balance" }

# ---- V133: a click repaints from the sheet ROOT, with nothing registered -------------
# Buying a dot never writes xpTotal (V100), so no dataLink wakes up and this call is the only
# thing that moves a number after a purchase. It used to reach the other tabs through a
# registry filled at onNodeReady - a promise nobody could see being kept, and silent when it
# was not (SPEC B39). Walking from the root needs no promise: what is on the sheet is found.
$boxAllFn = LuaFn $rootTxt 'renderAllXPBoxes'
$registry = @($files | Where-Object { (CodeOf $_.FullName) -match 'xpBoxForms|registerXPBoxes|xpLedgerForm' })
if ($registry) { foreach ($r in $registry) { Fail "V133 $($r.Name) still hands a form to a registry - a tab that never opened would leave the numbers stale (SPEC B39)" } }
elseif (-not $boxAllFn) { Fail "V133 renderAllXPBoxes not found on the root form" }
elseif ($boxAllFn -notmatch 'tabRootOf\(from\)') { Fail "V133 the click's repaint does not climb to the sheet root - only the clicked tab would follow" }
elseif ($guardFn -notmatch 'renderAllXPBoxes\(form') { Fail "V133 xpClick does not repaint from the form that was clicked" }
elseif ($guardFn -match 'renderXPBoxes\(form') { Fail "V133 the guard paints only the form the dot lives on - a numina purchase would move no number (SPEC B37)" }
elseif ((LuaFn $rootTxt 'xpSetCurrent') -notmatch 'renderAllXPBoxes\(form\)') { Fail "V133 a typed balance never repaints - the box would keep the number that was refused (SPEC V131)" }
elseif ((LuaFn $rootTxt 'xpSetCurrent') -match 'renderXPBoxes\(') { Fail "V133 a typed balance repaints one tab only - the balance is drawn twice and the other copy would sit on the old number (SPEC I11)" }
else { Pass "V133 a click and a typed balance both repaint from the sheet root, with nothing registered" }

# Both tabs that carry boxes must still be reachable BY NAME from a walk: the names are what
# the painter matches on, so a rename here is what would quietly stop the repaint.
$boxFiles = @()
foreach ($bf in $files) {
    if ($bf.Name -eq 'WoD20th.lfm') { continue }
    $bd = Doc $bf.FullName
    if (@($bd.SelectNodes("//edit[starts-with(@name,'edtTotalXP') or starts-with(@name,'edtSpentXP') or starts-with(@name,'edtCurrentXP')]")).Count -gt 0) { $boxFiles += $bf }
}
$boxNames = @()
foreach ($bf in $boxFiles) {
    foreach ($n in (Doc $bf.FullName).SelectNodes("//edit[starts-with(@name,'edtTotalXP') or starts-with(@name,'edtSpentXP') or starts-with(@name,'edtCurrentXP')]")) { $boxNames += $n.GetAttribute("name") }
}
$unpainted = @($boxNames | Where-Object { $paintFn -notmatch [regex]::Escape($_) })
if ($boxFiles.Count -lt 2) { Fail "V133 expected two tabs carrying experience boxes, found $($boxFiles.Count)" }
elseif ($unpainted) { foreach ($u in $unpainted) { Fail "V133 box '$u' is on the sheet but the painter never names it - it would never follow a purchase" } }
else { Pass "V133 all $($boxNames.Count) boxes across $($boxFiles.Count) tabs are named by the painter" }

# ---- V142: a click's repaint is never silenced -----------------------------------------
# xpQuiet exists so the sheet's OWN writes do not drag a repaint along (V107). A player's
# click is not one of those, and a flag left stuck true would otherwise mute every number on
# the sheet for the rest of the session with nothing on screen to say so (SPEC B39).
if (-not $paintFn) { Fail "V142 xpPaint not found on the root form" }
elseif ($paintFn -match 'xpQuiet then return') { Fail "V142 the painting core stands down on xpQuiet - a stuck flag would mute the sheet (SPEC B39)" }
elseif ($boxAllFn -match 'xpQuiet') { Fail "V142 the click's repaint asks xpQuiet - a click is not a batch of the sheet's own writes" }
elseif ($boxesFn -notmatch 'if xpQuiet then return') { Fail "V142 the dataLink entry does not stand down - the load would repaint per write (SPEC V107/B30)" }
else { Pass "V142 the click paints unconditionally, and only the dataLink entry stands down" }

# ---- V143: one way to find a control by name -------------------------------------------
# form.<name> does not cross an <import> (SPEC B9), which is the whole reason a registry
# existed. One walker serves the boxes and the log; a second one would be a second answer to
# the same question, and the two would drift.
$finders = @([regex]::Matches($rootTxt, 'function xpFind\('))
$ledgerUsesFind = ((CodeOf (Join-Path $dir "WoD20.9.lfm")) -match 'xpFind\(node, XP_LOG')
if ($finders.Count -ne 1) { Fail "V143 xpFind is declared $($finders.Count) times on the root form - exactly one walker" }
elseif ($rootTxt -notmatch 'xpFind\(node, XP_BOXES, \{\}\)') { Fail "V143 the box painter does not find its controls through xpFind" }
elseif (-not $ledgerUsesFind) { Fail "V143 WoD20.9 does not find its columns through xpFind - a second way to reach a control by name" }
elseif ((CodeOf (Join-Path $dir "WoD20.9.lfm")) -match 'function renderXPLedger\(node, rows\)[\s\S]{0,400}?form\.dynXpType\.text' -and (CodeOf (Join-Path $dir "WoD20.9.lfm")) -notmatch 'local form = xpFind') { Fail "V143 WoD20.9 still addresses its columns off a form handed to it" }
else { Pass "V143 one walker finds every control by name, for both the boxes and the log" }

# ---- V144: the storyteller's notes are the storyteller's ------------------------------
# Two boxes that look exactly like the player's, on fields of their own. Pointing one of
# them at anotacoes* would put what the storyteller wrote inside the player's Notes tab -
# two owners of one text (V1), and the tab being hidden would stop hiding anything.
#
# THREE until the 82nd round, when the settings box took the third column (SPEC I35) and
# stNotes3 became an orphan. The count going 3 -> 2 is not the check getting weaker: the
# field pattern below narrows to stNotes1..2, so the retired name coming back anywhere -
# here or on another tab - is now a failure rather than a pass (SPEC I3, V2).
$stDocPath = Join-Path $dir "WoD20.10.lfm"
$hh5Path   = Join-Path $dir "WoD20.5.lfm"
$stNoteFields = @((Doc $stDocPath).SelectNodes("//textEditor[@field]") | ForEach-Object { $_.GetAttribute("field") })
$stElsewhere  = @()
foreach ($nf in $files) {
    if ($nf.Name -eq 'WoD20.10.lfm') { continue }
    foreach ($n in (Doc $nf.FullName).SelectNodes("//*[@field]")) {
        if ($n.GetAttribute("field") -match '^stNotes\d$') { $stElsewhere += "$($nf.Name)/$($n.GetAttribute('field'))" }
    }
}
$playerNotes = @((Doc $hh5Path).SelectNodes("//textEditor[@field]") | ForEach-Object { $_.GetAttribute("field") })
$crossed = @($stNoteFields | Where-Object { $playerNotes -contains $_ })
if ($stNoteFields.Count -ne 2) { Fail "V144 WoD20.10 carries $($stNoteFields.Count) note box(es) - the storyteller was left two when the settings box took the third column (SPEC I35)" }
elseif (@($stNoteFields | Sort-Object -Unique).Count -ne 2) { Fail "V144 the two storyteller note boxes share a field - one of them would never be read back" }
elseif ($crossed) { foreach ($c in $crossed) { Fail "V144 WoD20.10 note box owns '$c', which is the player's on WoD20.5 - the storyteller's text would show up in the player's Notes tab (SPEC V1)" } }
elseif (@($stNoteFields | Where-Object { $_ -notmatch '^stNotes[12]$' })) { Fail "V144 a storyteller note box is on a field outside stNotes1..2 - stNotes3 is an ORPHAN since the 82nd round and the name may never be reused (SPEC I3, V2)" }
elseif ($stElsewhere) { foreach ($s in $stElsewhere) { Fail "V144 $s - a storyteller field is owned outside WoD20.10, where a player can reach it" } }
else { Pass "V144 the storyteller's two notes own their own fields, and no tab shares them ($($stNoteFields -join ', '))" }

# ---- V145: one place says how many backgrounds there are -------------------------------
# The XML draws the rows, two loops on the root form walk them - one to let experience buy a
# background, one to price it for the log. The count was a literal `9` in both until the 46th
# round doubled it. A row added to the XML and to neither loop is a background nothing
# charges for and nothing shows in the log: free, and invisible.
#
# 87th round: the last five rows are TYPED (SPEC I50, V255), so the census counts both
# templates. A typed background is still a background - same five dots, same prices - and
# the two loops still walk all twenty of them.
$bgLoops   = @([regex]::Matches($rootTxt, 'for i = 1, BACKGROUND_ROWS, 1 do'))
$bgLiteral = @([regex]::Matches($rootTxt, 'for i = 1, \d+, 1 do[^\r\n]*background'))
$bgXml     = @()
foreach ($bf in $files) {
    foreach ($n in (Doc $bf.FullName).SelectNodes("//OpenAbility[@field] | //OpenAbilityFreeRow[@field]")) {
        if ($n.GetAttribute("field") -match '^background_(\d+)$') { $bgXml += [int]$Matches[1] }
    }
}
$bgMax = if ($bgXml.Count -gt 0) { ($bgXml | Measure-Object -Maximum).Maximum } else { 0 }
if ($bgRows -lt 1) { Fail "V145 BACKGROUND_ROWS is not declared - the count would be a literal in every loop again" }
elseif ($bgLoops.Count -ne 2) { Fail "V145 $($bgLoops.Count) of the two background loops read BACKGROUND_ROWS - the other one is carrying its own copy of the number" }
elseif ($bgLiteral.Count -gt 0) { Fail "V145 a background loop still counts to a literal - the XML and the ledger would drift apart" }
elseif ($bgXml.Count -ne $bgRows) { Fail "V145 the XML draws $($bgXml.Count) background row(s) but BACKGROUND_ROWS says $bgRows - the extra rows cost nothing and never reach the log" }
elseif ($bgMax -ne $bgRows) { Fail "V145 the background rows run up to background_$bgMax with BACKGROUND_ROWS at $bgRows - the loops walk 1..$bgRows and would miss it" }
elseif (@($bgXml | Sort-Object -Unique).Count -ne $bgRows) { Fail "V145 two background rows carry the same field - one of them would never be read back (SPEC V1)" }
else { Pass "V145 $bgRows background rows, one declared count, both loops reading it" }

# ---- V135: priced first, marked only if it is allowed --------------------------------
# The dot cannot flip itself any more (V134), so the order inside xpClick IS the rule: work
# out what the click is asking for, price the sheet as it WOULD be, and only then write. The
# three rounds before this one all let the mark land and tried to take it back, and a write
# made from inside the dot's own onChange never survived the dispatch (SPEC B36/B38).
$clickFn = [regex]::Match($rootTxt, 'function xpClick\(field, form\)(.*?)\n\t\t\tend;', 'Singleline')
$markFn  = LuaFn $rootTxt 'markDot'
if (-not $clickFn.Success) { Fail "V135 xpClick not found on the root form" }
else {
    $c     = NoComments $clickFn.Groups[1].Value
    $iPr   = $c.IndexOf('xpLedgerRows(field, want, key, want and okey or nil)')
    $iMark = $c.LastIndexOf('markDot(form, field, want)')
    $marks = @([regex]::Matches($c, 'markDot\('))
    if ($c -notmatch 'local want = not \(sheet\[field\] == true\);') { Fail "V135 xpClick never works out what the click is asking for - with autoChange off, nothing else does" }
    elseif ($c -match 'if undo then') { Fail "V135 xpClick still has an undo branch - a mark taken back does not stick (SPEC B36/B38)" }
    elseif ($iPr -lt 0) { Fail "V135 xpClick does not price the click before deciding" }
    elseif ($iMark -lt $iPr) { Fail "V135 xpClick marks the dot before it prices the click (SPEC B38)" }
    elseif ($marks.Count -ne 3) { Fail "V135 xpClick marks the dot in a place it should not - exactly three marks: no baseline, nothing to price, and the one the rules allowed" }
    else { Pass "V135 the click is priced first and marked only if the rules allow it" }
}
if (-not $markFn) { Fail "V135 markDot not found on the root form" }
elseif ($markFn -notmatch 'setField\(field, v\);') { Fail "V135 markDot does not write the field - the mark would not be saved" }
elseif ($markFn -notmatch 'findDot\(form, field\)') { Fail "V135 markDot never looks the dot up" }
elseif ($markFn -notmatch 'dot\.checked = v;') { Fail "V135 markDot does not paint the dot - with autoChange off nothing else will" }
else { Pass "V135 the accepted mark writes the field and paints the dot in one step" }

# ---- V136: pricing a click writes nothing ---------------------------------------------
# The ledger is asked what the sheet WOULD cost, and it answers without touching it. The
# simulation is switched on after the baseline check and put away before the only way out,
# so no later price can carry a click that has already been answered.
$rowsSim = LuaFn $rootTxt 'xpLedgerRows'
$liveFn  = LuaFn $rootTxt 'liveLevel'
if (-not $rowsSim) { Fail "V136 xpLedgerRows not found on the root form" }
elseif ($rowsSim -notmatch 'function xpLedgerRows\(clickField, clickValue, clickFree, clickOrder\)') { Fail "V136 xpLedgerRows cannot be handed a pending click" }
elseif ($rowsSim -notmatch 'simField, simValue = clickField, clickValue;') { Fail "V136 the pending click is never put in front of the ledger" }
elseif ($rowsSim -notmatch 'simTrait, simField, simValue = nil, nil, nil;') { Fail "V136 the simulation is never cleared - a later price would carry an answered click" }
elseif ($rowsSim.IndexOf('simTrait, simField, simValue = nil, nil, nil;') -gt $rowsSim.IndexOf('return rows;')) { Fail "V136 the simulation is cleared after the ledger has returned, which is never" }
elseif ($rowsSim -match 'traitLevel\(sheet,') { Fail "V136 the ledger still reads the live sheet directly - that read would miss the pending click" }
elseif ($rowsSim -match 'setField\(') { Fail "V136 pricing a click writes to the sheet (SPEC B38)" }
else { Pass "V136 the click is priced by simulation, and the simulation is put away before the ledger returns" }
if (-not $liveFn) { Fail "V136 liveLevel not found on the root form" }
elseif ($liveFn -notmatch 'traitLevel\(sheet, base, first, last, fixed\)') { Fail "V136 liveLevel does not read the sheet through traitLevel (SPEC V85)" }
elseif ($liveFn -notmatch 'if simTrait == base') { Fail "V136 liveLevel would apply the pending click to a trait it does not belong to" }
else { Pass "V136 the live side is traitLevel plus at most the one dot being clicked" }

# ---- V137: Free dots is read at the click, and nowhere else --------------------------
# "Free only if the box was ticked at the MOMENT of purchase" is not a rule anyone has to
# remember while editing: it holds because nothing except xpClick can see the flag. A
# renderer that read it would re-price the past every time the storyteller changed their
# mind, which is the lens the 31st round shipped and the user rejected (SPEC V105).
$flagReads = @()
foreach ($ff in $files) {
    foreach ($m in [regex]::Matches((CodeOf $ff.FullName), '(?<!field=")stFreeBuy')) { $flagReads += $ff.Name }
}
if ($flagReads.Count -ne 1) { Fail "V137 stFreeBuy is read in $($flagReads.Count) place(s) ($($flagReads -join ', ')) - exactly one, inside xpClick" }
elseif ($flagReads[0] -ne 'WoD20th.lfm') { Fail "V137 stFreeBuy is read from $($flagReads[0]) - the flag belongs to the click, not to a tab" }
elseif ($clickFn.Groups[1].Value -notmatch 'local free = want and sheet\.stFreeBuy == true;') { Fail "V137 xpClick does not read the flag at the click - the answer would come from somewhere else in time" }
else { Pass "V137 Free dots is read once, at the click, and by nothing else" }

# ---- V138: the stamp holds names, keyed by the rating REACHED ------------------------
# strength_5 clicked with two dots lit is level 3, and the log asks for the level, not for
# the dot - keying off the clicked field is what made the 34th round charge for points it
# had stamped (SPEC B31). '#' keeps a key from ever reading as a field name. The burned
# names stay burned: sheets from the 31st..37th rounds carry `freeDots` written under two
# incompatible schemes, and reviving it would zero the cost of whichever dot it happened to
# name (SPEC I3, V2).
$burnedUse  = @($files | Where-Object { (CodeOf $_.FullName) -match 'stFreeDots|freeDots' })
$freeWrites = @([regex]::Matches($rootTxt, 'setField\("xpFree"'))
# A tab may WATCH the stamps - WoD20.1 locks its rows off them (SPEC V164) - but naming the
# field in any other way outside the root form means a second owner of the gift.
#
# Only the `field=` attribute is dropped, not the whole tag: a handler written inside that
# same tag (onChange="setField('xpFree', ...)") is exactly the write this forbids, and
# stripping the element wholesale would have hidden it.
$freeElse   = @($files | Where-Object { $_.Name -ne 'WoD20th.lfm' -and ([regex]::Replace((CodeOf $_.FullName), '<dataLink field="xpFree"', '<dataLink')) -match 'xpFree' })
if ($burnedUse) { Fail "V138 $(($burnedUse | ForEach-Object { $_.Name }) -join ', ') names a burned field (stFreeDots/freeDots) - an old sheet would resurrect stamps under a scheme nothing reads any more (SPEC I3)" }
elseif ($freeElse) { Fail "V138 xpFree is touched outside the root form ($(($freeElse | ForEach-Object { $_.Name }) -join ', ')) - the stamp belongs to the click" }
elseif ($freeWrites.Count -ne 4) { Fail "V138 xpFree is written in $($freeWrites.Count) place(s) - exactly four: the stamp a free purchase leaves, the one a sale takes away, the gift given, and the gift revoked (SPEC V152/V159)" }
elseif ((LuaFn $rootTxt 'grantSpeciality') -notmatch 'setField\("xpFree"') { Fail "V138 the gift leaves no stamp - the free line would be charged on the next render" }
elseif ((LuaFn $rootTxt 'revokeSpeciality') -notmatch 'setField\("xpFree"') { Fail "V138 the revoked gift keeps its stamp - the row would stay locked with nothing in it (SPEC V159)" }
elseif ($clickFn.Groups[1].Value -notmatch 'trait \.\. "#" \.\. \(level \+ 1\)') { Fail "V138 the stamp is not keyed off the rating the click REACHES (SPEC B31)" }
elseif ($rowsSim -notmatch 'free\s+= \(sheet\.xpFree or ""\) \.\. "\|" \.\. \(clickFree or ""\) \.\. "\|"') { Fail "V138 the ledger never reads the stamps - every point would be priced as if the box had never been ticked" }
else { Pass "V138 the stamp keeps names keyed by the rating reached, and no burned name is back" }

# ---- V139: a stamp DISCOUNTS a price the table worked out ----------------------------
# Not a second cost table (SPEC V86): xpCost prices the point and the stamp zeroes it after
# the fact. That is also what stops a free point from making the next one cheaper - the
# price still reads the rating reached, never how many points were paid for.
$riseFn    = LuaFn $rootTxt 'pushRise'
$riseCalls = @([regex]::Matches($rootTxt, 'ctx, (field|"humanity"|"willpower"|"faith")\);'))
# Derived, not a literal: the 51st round took the ledger from ten row groups to thirteen,
# and a number typed here would have to be edited by hand every time a box is added.
$riseTotal = @([regex]::Matches($rootTxt, [regex]::Escape('pushRise(rows, "'))).Count
if (-not $riseFn) { Fail "V139 pushRise not found on the root form" }
elseif ($riseFn -notmatch 'function pushRise\(rows, kind, name, from, to, ctx, field\)') { Fail "V139 pushRise cannot tell which trait the row belongs to - it could not read a stamp" }
elseif ($riseFn -notmatch 'local cost = xpCost\(kind, lvl - 1, ctx\);') { Fail "V139 pushRise no longer prices the row through xpCost (SPEC V86)" }
elseif ($riseFn -notmatch 'string\.find\(ctx\.free, "\|" \.\. field \.\. "#" \.\. lvl \.\. "\|", 1, true\)') { Fail "V139 pushRise does not look the row's level up in the stamps" }
elseif ($riseFn.IndexOf('string.find(ctx.free') -lt $riseFn.IndexOf('local cost = xpCost(')) { Fail "V139 the discount lands before the price exists - it must zero a cost the table already worked out (SPEC V86)" }
elseif ($riseCalls.Count -ne $riseTotal) { Fail "V139 $($riseTotal - $riseCalls.Count) ledger row group(s) do not hand pushRise their field - one that does not could never go free" }
else { Pass "V139 all ten row groups carry their field, and a stamp zeroes a price xpCost worked out" }

# ---- V140: the stamp goes down with the mark, never on a refusal ---------------------
# Pricing writes nothing (V136), so the stamp reaches the sheet only after the rules have
# said yes, in the same pass as the mark. A free point skips the balance test outright
# rather than passing it on the arithmetic: `spent` does not move, but a sheet the
# storyteller has already put in the red - by charging for backgrounds after the fact -
# would refuse a point that costs nothing (SPEC C, 40th round).
$cc      = NoComments $clickFn.Groups[1].Value
$iStamp  = $cc.IndexOf('setField("xpFree"')
$iMarkOk = $cc.LastIndexOf('markDot(form, field, want)')
$buyBr   = [regex]::Match($cc, 'if want then(.*?)else', 'Singleline')
if ($iStamp -lt 0) { Fail "V140 xpClick never writes the stamp - a free point would be charged again on the next render" }
elseif ($iStamp -lt $iMarkOk) { Fail "V140 the stamp is written before the mark - a refused click would leave one behind (SPEC V135)" }
elseif (-not $buyBr.Success) { Fail "V140 xpClick has no purchase branch" }
elseif ($buyBr.Groups[1].Value -notmatch 'if not free and') { Fail "V140 a free purchase is put through the balance test - a sheet already in the red would refuse a point that costs nothing" }
elseif ($buyBr.Groups[1].Value -match 'setField\(') { Fail "V140 the purchase branch writes before the decision is made (SPEC V135)" }
else { Pass "V140 the stamp goes down with the mark, and a free point never meets the balance" }

# ---- V141: selling a point takes its stamp with it -----------------------------------
# The rule is about the moment of purchase, so a point sold and bought again obeys the flag
# of the NEW click. One key, not a sweep: a sale drops the rating by one and V103 forbids
# going below the frozen character, so no stamp can be left stranded above the rating.
if ($cc -notmatch 'elseif not want and sheet\.xpFree ~= nil then') { Fail "V141 a sale never reaches the stamps - a sold point would come back free" }
elseif ($cc -notmatch 'string\.gsub\(sheet\.xpFree, "\|" \.\. trait \.\. "#" \.\. level \.\. "\|", "\|"\)') { Fail "V141 the sale does not drop the stamp of the level it gave up" }
else { Pass "V141 a sold point hands its stamp back" }

# ---- V146: the speciality box has no coordinates of its own -------------------------
# It took the corner BACKGROUNDS emptied in the 46th round and shipped in the 47th with
# 890..1210 x 510..810 written into this check. The 50th round moved it again - the second
# time a literal here had to be hand-edited - so where the box SITS is now measured against
# KNOWLEDGES above it (V168) and the tab's closing line (V69). What stays here is what only
# this box knows: how many rows fit down, and whether one row still fits across.
#
# 82nd round: it hangs from SKILLS now, not KNOWLEDGES - the user had it trade places with
# HEALTH (item 14). Only the column name in the V168 map moved: both boxes are 290 wide on
# the same top, so nothing this check measures changed with them.
function BoxOf($doc, $title) { @($doc.SelectNodes("//layout[label/@text='$title']"))[0] }
$mainDoc = Doc (Join-Path $dir "WoD20.1.lfm")
$sb = BoxOf $mainDoc "SPECIALTIES"
$specTpl = @($mainDoc.SelectNodes("//template[@name='SpecialityRow']"))[0]
if (-not $sb) { Fail "V146 WoD20.1 declares no SPECIALTIES box - the tab's map says exactly one" }
elseif (-not $specTpl) { Fail "V146 SpecialityRow is not declared on WoD20.1" }
else {
    $sbW = [int]$sb.GetAttribute("width"); $sbH = [int]$sb.GetAttribute("height")
    # Where the first row opens is DERIVED from the title, not typed (103rd round, SPEC T618):
    # the hairline under the title band. It read 31 while the margin was 10 and reads 41 under
    # I73, and a literal here is the third one this check has had to hand-edit.
    $sTtl  = $sb.SelectSingleNode("label[@text='SPECIALTIES']")
    $sBody = if ($null -ne $sTtl) { [int]$sTtl.GetAttribute("top") + [int]$sTtl.GetAttribute("height") + 1 } else { -1 }
    $sRows = @($sb.SelectNodes("layout[SpecialityRow] | layout[SpecialityFreeRow]"))
    $sTops = @($sRows | ForEach-Object { [int]$_.GetAttribute("top") } | Sort-Object)
    $sLefts = @($sRows | ForEach-Object { [int]$_.GetAttribute("left") } | Sort-Object -Unique)
    $sWide = @($sRows | ForEach-Object { [int]$_.GetAttribute("width") } | Sort-Object -Unique)
    $sPitchBad = 0
    # 25 since T859, and it is V370 that owns WHY - this line only asks that the rows the box
    # declares still fit down it (SPEC V370, I124c).
    for ($i = 1; $i -lt $sTops.Count; $i++) { if (($sTops[$i] - $sTops[$i - 1]) -ne $MAIN_ROW_PITCH) { $sPitchBad++ } }
    # The row is measured as a SUM, not as an extent: with the widgets laid end to end,
    # each has to start at or after the one before it ends, and the last has to close on
    # the row. The first cut of this check took the furthest right edge instead, and a
    # mutation that widened the text field straight through the dot survived it green
    # (SPEC V20, B7) - the dot was still the right-most thing, it just had the edit
    # sitting on top of it.
    $rowCells = @()
    foreach ($w in $specTpl.ChildNodes) {
        if ($w.NodeType -ne 'Element') { continue }
        # The hidden twin of I107a1 occupies NO pixels and is REQUIRED to stand on the button's
        # own left (SPEC V354a) - measuring it as a cell would make V146 and V354 contradict each
        # other, and one of them has to be wrong. It is this one: a widget authored visible=false
        # is not on the row. Keyed on the AUTHORED attribute and not on a name, so a VISIBLE
        # widget laid over another is still caught (T842, B100).
        if ($w.GetAttribute("visible") -eq 'false') { continue }
        $wl = 0; $ww = 0
        if ([int]::TryParse($w.GetAttribute("left"), [ref]$wl) -and [int]::TryParse($w.GetAttribute("width"), [ref]$ww)) {
            $rowCells += [pscustomobject]@{ L = $wl; R = $wl + $ww; N = $w.LocalName }
        }
    }
    $rowCells = @($rowCells | Sort-Object L)
    $rowOverlap = @()
    for ($i = 1; $i -lt $rowCells.Count; $i++) {
        if ($rowCells[$i].L -lt $rowCells[$i - 1].R) { $rowOverlap += "$($rowCells[$i - 1].N) ends at $($rowCells[$i - 1].R) but $($rowCells[$i].N) starts at $($rowCells[$i].L)" }
    }
    $rowSpan = if ($rowCells.Count -gt 0) { $rowCells[-1].R } else { 0 }
    if ($sRows.Count -ne $spRows) { Fail "V146 SPECIALTIES draws $($sRows.Count) row(s) with SPECIALITY_ROWS at $spRows" }
    elseif ($sBody -lt 0) { Fail "V146 the SPECIALTIES box carries no title - the row that opens under it would be measured against nothing (SPEC V209)" }
    elseif ($sTops[0] -ne $sBody) { Fail "V146 the first speciality row starts at $($sTops[0]), not on the hairline under the title at $sBody - the row opens where the title band ends, whatever margin I73 gives it (SPEC I40, I73, V240)" }
    elseif ($sPitchBad -gt 0) { Fail "V146 $sPitchBad speciality row(s) break the pitch of $MAIN_ROW_PITCH - the box would not hold $spRows of them" }
    elseif (($sTops[-1] + 25) -gt $sbH) { Fail "V146 the last speciality row ends at $($sTops[-1] + 25), past a box $sbH tall" }
    elseif ($sLefts.Count -ne 1 -or $sWide.Count -ne 1) { Fail "V146 the speciality rows do not share one left/width - one row would sit differently from its neighbours" }
    elseif ($sWide[0] -ne ($sbW - 2 * $sLefts[0])) { Fail "V146 the rows are $($sWide[0]) wide at left $($sLefts[0]) in a $sbW box - the right margin no longer matches the left" }
    elseif ($rowOverlap.Count -gt 0) { Fail "V146 SpecialityRow widgets sit on each other - $($rowOverlap -join '; ') - a narrower box needs the row refitted, not the widgets stacked" }
    elseif ($rowSpan -ne $sWide[0]) { Fail "V146 SpecialityRow spans $rowSpan in a row $($sWide[0]) wide - narrowing the box without refitting the row leaves the dot short of the edge or past it" }
    else { Pass "V146 SPECIALTIES holds $($sRows.Count) rows and its row spans exactly the $rowSpan it is given" }
}

# ---- V147: three fields and one dot per row, counted in one place --------------------
# Same shape as BACKGROUND_ROWS (V145): the XML draws the rows and two loops on the root form
# walk them - one to let experience buy the row, one to price it for the log. A tenth row
# added to the XML alone would be a speciality nothing charges for and nothing shows.
$specXmlRows = @()
foreach ($f in $files) {
    foreach ($n in (Doc $f.FullName).SelectNodes("//SpecialityRow[@num] | //SpecialityFreeRow[@num]")) { $specXmlRows += [int]$n.GetAttribute("num") }
}
$specMax     = if ($specXmlRows.Count -gt 0) { ($specXmlRows | Measure-Object -Maximum).Maximum } else { 0 }
$specLoops   = @([regex]::Matches($rootTxt, 'for i = 1, SPECIALITY_ROWS, 1 do'))
$specLiteral = @([regex]::Matches($rootTxt, 'for i = 1, \d+, 1 do[^\r\n]*speciality'))
$specFldBad  = @()
if ($spRows -ge 1) {
    for ($i = 1; $i -le $spRows; $i++) {
        foreach ($fld in @("speciality_$i", "specialityName_$i", "speciality_${i}_1")) {
            if (-not $allFields.ContainsKey($fld)) { $specFldBad += "$fld is not owned by any widget" }
            elseif ($allFields[$fld].Count -ne 1) { $specFldBad += "$fld has $($allFields[$fld].Count) owners (SPEC V1)" }
        }
    }
}
if ($spRows -lt 1) { Fail "V147 SPECIALITY_ROWS is not declared - the count would be a literal in every loop" }
elseif ($specLoops.Count -lt 3) { Fail "V147 only $($specLoops.Count) loop(s) read SPECIALITY_ROWS over its full range - the three that walk every row are declareTrait, the ledger and freeRowOf; the lock and the gift stop at the picker rows since the 87th round (SPEC I50, V255d)" }
elseif ($specLiteral.Count -gt 0) { Fail "V147 a speciality loop still counts to a literal - the XML and the ledger would drift apart" }
elseif ($specXmlRows.Count -ne $spRows) { Fail "V147 the XML draws $($specXmlRows.Count) speciality row(s) but SPECIALITY_ROWS says $spRows" }
elseif ($specMax -ne $spRows) { Fail "V147 the speciality rows run up to num=$specMax with SPECIALITY_ROWS at $spRows - the loops walk 1..$spRows and would miss it" }
elseif (@($specXmlRows | Sort-Object -Unique).Count -ne $spRows) { Fail "V147 two speciality rows carry the same num - they would share their fields (SPEC V1)" }
elseif ($specFldBad.Count -gt 0) { foreach ($b in $specFldBad) { Fail "V147 $b" } }
else { Pass "V147 $spRows speciality rows, three owned fields each, one declared count read by both loops" }

# ---- V148: the trait picker is the two Lua tables, not a third copy of them ----------
# The grant writes a canonical trait NAME into the combo (V152), so a name the picker does not
# carry is a value the player would see as blank. Both directions are checked: an era ability
# added to ABILITY_FIELD and not to the picker, and an item in the picker that names no trait,
# are the same failure from either end (SPEC B23/B24).
$attrBlk = [regex]::Match($rootTxt, 'local XP_ATTRS = \{(.*?)\};', 'Singleline')
$abilBlk = [regex]::Match($rootTxt, 'ABILITY_FIELD = \{(.*?)\n\t\t\t\};', 'Singleline')
$mainRaw =[System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20.1.lfm")))
$pickBlk = [regex]::Match($mainRaw, '<template name="SpecialityRow">(.*?)</template>', 'Singleline')
if (-not $attrBlk.Success) { Fail "V148 XP_ATTRS not found on the root form" }
elseif (-not $abilBlk.Success) { Fail "V148 ABILITY_FIELD not found on the root form" }
elseif (-not $pickBlk.Success) { Fail "V148 the SpecialityRow template is not in WoD20.1" }
else {
    $luaNames = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
    foreach ($m in [regex]::Matches($attrBlk.Groups[1].Value, '\{"([^"]+)", "[^"]+"\}')) { [void]$luaNames.Add($m.Groups[1].Value) }
    foreach ($m in [regex]::Matches($abilBlk.Groups[1].Value, '\["([^"]+)"\]\s*=\s*"[^"]+"')) { [void]$luaNames.Add($m.Groups[1].Value) }

    # Lua x Lua now, not XML x Lua: the trait list moved to PICKER_LIST (SPEC I27, V148).
    # items and values are ONE authored list there, so the pair cannot fall out of step by
    # construction - what is still worth proving is that it matches the two Lua tables.
    $pickItems = @($PICKER['speciality'])
    $pickVals  = @($PICKER['speciality'])
    $itemsAttr = [pscustomobject]@{ Success = ($pickItems.Count -gt 0) }
    $valsAttr  = [pscustomobject]@{ Success = ($pickVals.Count -gt 0) }
    $pickSet   = @($pickItems | Where-Object { $_ -ne '' })

    $missPick = @($luaNames | Where-Object { $pickSet -notcontains $_ })
    $missLua  = @($pickSet | Where-Object { -not $luaNames.Contains($_) })
    if (-not $itemsAttr.Success -or -not $valsAttr.Success) { Fail "V148 PICKER_LIST has no 'speciality' list - the trait picker offers nothing (SPEC I27, V211a)" }
    elseif ($pickItems.Count -ne $pickVals.Count) { Fail "V148 the trait picker shows $($pickItems.Count) items for $($pickVals.Count) values - one of them would save nothing" }
    elseif ($missPick.Count -gt 0) { foreach ($m in $missPick) { Fail "V148 '$m' is a trait in Lua and not in the picker - a grant on it would write a value the combo cannot show" } }
    elseif ($missLua.Count -gt 0) { foreach ($m in $missLua) { Fail "V148 '$m' is in the picker and names no trait - picking it would buy a speciality of nothing" } }
    else { Pass "V148 the trait picker carries exactly the $($pickSet.Count) traits XP_ATTRS and ABILITY_FIELD name" }
}

# ---- V149: which trait gives a speciality, and at which rating ------------------------
# SPEC_TRAIT is DERIVED from the same two tables the picker is checked against, so the fifty
# names exist once. Only the five abilities the book hands a speciality to at the first dot
# are named here - and naming them is also what stops them getting a second one at four.
$specTblBlk = [regex]::Match($rootTxt, 'SPEC_TRAIT = \{\};(.*?)XP_TRAIT = \{\};', 'Singleline')
if (-not $specTblBlk.Success) { Fail "V149 SPEC_TRAIT is not declared on the root form" }
else {
    $st = $specTblBlk.Groups[1].Value
    $stQuoted = @([regex]::Matches($st, '"([^"]+)"') | ForEach-Object { $_.Groups[1].Value } | Sort-Object -Unique)
    $wantFive = @('academics', 'crafts', 'expression', 'performance', 'technology')
    if ($st -notmatch 'for i = 1, #XP_ATTRS, 1 do SPEC_TRAIT') { Fail "V149 SPEC_TRAIT does not read the attributes off XP_ATTRS" }
    elseif ($st -notmatch 'for name, field in pairs\(ABILITY_FIELD\) do SPEC_TRAIT') { Fail "V149 SPEC_TRAIT does not read the abilities off ABILITY_FIELD" }
    elseif ($st -notmatch 'free = 4') { Fail "V149 nothing grants a speciality at four - that is the rule for every trait but five" }
    elseif ($st -notmatch 'free = 1') { Fail "V149 nothing grants a speciality at one - the five abilities that do would get nothing" }
    elseif (Compare-Object $stQuoted $wantFive) { Fail "V149 SPEC_TRAIT names [$($stQuoted -join ', ')] - it must name the five one-dot abilities and nothing else, or the fifty are typed out twice" }
    else { Pass "V149 SPEC_TRAIT is derived from the two tables, with five abilities granting at one and the rest at four" }
}

# ---- V150 + V158: the gift moves with the click, both ways, and with nothing else -----
# No observer, no sweep at load (V121). Two call sites, both inside xpClick - the character
# being built and the click the rules allowed - and each of them asks ONE question, so the
# rise and the fall cannot drift apart (SPEC V158).
$grantFn      = LuaFn $rootTxt 'grantSpeciality'
$revokeFn     = LuaFn $rootTxt 'revokeSpeciality'
$movedFn      = LuaFn $rootTxt 'specialityMoved'
$movedInClick = @([regex]::Matches($cc, 'specialityMoved\('))
$specOutside  = @()
foreach ($f in $files) {
    if ($f.Name -eq 'WoD20th.lfm') { continue }
    $raw = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName))
    if ($raw -match 'grantSpeciality|revokeSpeciality|specialityMoved') { $specOutside += $f.Name }
}
$grantTotal  = @([regex]::Matches($rootTxt, 'grantSpeciality\('))
$revokeTotal = @([regex]::Matches($rootTxt, 'revokeSpeciality\('))
if (-not $grantFn) { Fail "V150 grantSpeciality not found on the root form" }
elseif (-not $revokeFn) { Fail "V158 revokeSpeciality not found on the root form - a gift could never be lost" }
elseif (-not $movedFn) { Fail "V158 specialityMoved not found - the two directions would each need their own call site" }
elseif ($movedInClick.Count -ne 2) { Fail "V158 xpClick asks specialityMoved $($movedInClick.Count) time(s) - once while the character is being built, once on an accepted click" }
elseif ($grantTotal.Count -ne 2) { Fail "V150 grantSpeciality is called from $($grantTotal.Count - 1) place(s) - only specialityMoved may reach it" }
elseif ($revokeTotal.Count -ne 2) { Fail "V158 revokeSpeciality is called from $($revokeTotal.Count - 1) place(s) - only specialityMoved may reach it" }
elseif ($movedFn -notmatch 'return grantSpeciality\(form, trait, level \+ 1\);') { Fail "V158 the rise does not ask for the level the click REACHES" }
elseif ($movedFn -notmatch 'return revokeSpeciality\(form, trait, level - 1\);') { Fail "V158 the fall does not ask for the level the click LEAVES BEHIND" }
elseif ($specOutside.Count -gt 0) { Fail "V150 the gift is moved by $($specOutside -join ', ') - a renderer or dataLink granting would re-grant on every load (SPEC V121)" }
elseif ($grantFn -notmatch 'if t == nil or reached ~= t\.free then return false; end;') { Fail "V150 the grant does not test the rating the click REACHED - it would fire on every dot of the trait" }
elseif ($revokeFn -notmatch 'if t == nil or reached >= t\.free then return false; end;') { Fail "V158 the revoke fires at the wrong side of the line - Expression grants at one, so selling from four to three must take nothing" }
else { Pass "V150/V158 the gift is born on the click that reaches the rating and dies on the click that leaves it" }

# ---- V151 + V160: one lookup, name AND stamp, serving the dedup and the revoke --------
# A row only counts as the gift if it carries the stamp as well as the name: a speciality
# the player PAID for on the same trait neither eats the free one nor is taken away with it.
$isFreeFn  = LuaFn $rootTxt 'isFreeRow'
$freeRowFn = LuaFn $rootTxt 'freeRowOf'
$stampFinds = @([regex]::Matches($rootTxt, 'string\.find\(sheet\.xpFree'))
if (-not $isFreeFn) { Fail "V160 isFreeRow not found - the stamp question would be asked three different ways" }
elseif (-not $freeRowFn) { Fail "V160 freeRowOf not found - the dedup and the revoke would each look the row up their own way" }
elseif ($stampFinds.Count -ne 1) { Fail "V160 the stamps are read directly in $($stampFinds.Count) places - isFreeRow is meant to be the only one" }
elseif ($isFreeFn -notmatch '"\|" \.\. field \.\. "#1\|"') { Fail "V160 isFreeRow does not key off the row's own stamp" }
elseif ($freeRowFn -notmatch 'sheet\["speciality_" \.\. i\] == name and isFreeRow\("speciality_" \.\. i\)') { Fail "V151 freeRowOf does not check name AND stamp together - a speciality the player PAID for would be taken for the gift" }
elseif ($grantFn -notmatch 'if freeRowOf\(t\.name\) ~= nil then return false; end;') { Fail "V151 the grant does not ask whether the trait already has its gift" }
elseif ($revokeFn -notmatch 'local slot = freeRowOf\(t\.name\);') { Fail "V151 the revoke does not find its row through the same lookup - it could take a paid speciality" }
else { Pass "V151/V160 one lookup, name and stamp together, serving both the dedup and the revoke" }

# ---- V152: name, mark and stamp in one step; the text stays the player's --------------
# Exactly what a free purchase writes, so the line is priced at zero by the rule that prices
# any stamped dot (V139) - there is no second cost table to disagree with it.
$nameWrites = @()
foreach ($f in $files) {
    if ($f.Name -eq 'WoD20th.lfm') { continue }
    $raw = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName))
    if ($raw -match 'setField\("specialityName') { $nameWrites += $f.Name }
}
# The typed text has exactly ONE writer in the whole sheet, and it is the revoke taking the
# row away (SPEC V163). Anything else writing it would be Lua typing for the player.
$rootNameWrites = @([regex]::Matches($rootTxt, 'setField\("specialityName'))
if (-not $grantFn) { Fail "V152 grantSpeciality not found on the root form" }
elseif ($grantFn -notmatch 'setField\("speciality_" \.\. slot, t\.name\);') { Fail "V152 the grant never writes the trait into the row" }
elseif ($grantFn -notmatch 'markDot\(form, "speciality_" \.\. slot \.\. "_1", true\);') { Fail "V152 the grant never lights the dot - with autoChange off nothing else will (SPEC V134)" }
elseif ($grantFn -notmatch 'setField\("xpFree", stamps') { Fail "V152 the grant leaves no stamp - the free line would be charged on the next render (SPEC V139)" }
elseif ($grantFn -match 'specialityName') { Fail "V163 the grant touches specialityName - what the specialty IS is the player's to type" }
elseif ($nameWrites.Count -gt 0) { Fail "V163 $($nameWrites -join ', ') writes specialityName - only the revoke on the root form may (SPEC V159)" }
elseif ($rootNameWrites.Count -ne 1) { Fail "V163 specialityName is written in $($rootNameWrites.Count) places - exactly one, the revoke clearing the row it takes away" }
elseif ($revokeFn -notmatch 'setField\("specialityName_" \.\. slot, ""\);') { Fail "V163 the one writer is not the revoke - the text would be cleared by something that is not taking the row away" }
else { Pass "V152/V163 the gift writes trait, mark and stamp and leaves the text alone; only the revoke clears it" }

# ---- V153: a full table says so, and the dot that earned it stays bought --------------
if (-not $grantFn) { Fail "V153 grantSpeciality not found on the root form" }
else {
    $iNoSlot = $grantFn.IndexOf('if slot == nil then')
    $iWrite  = $grantFn.IndexOf('setField("speciality_" .. slot')
    if ($iNoSlot -lt 0) { Fail "V153 the grant never handles a full table - it would write into a row that does not exist" }
    elseif ($grantFn -notmatch 'xpWarn\("No free specialty slot"\);') { Fail "V153 a full table refuses in silence" }
    elseif ($iWrite -lt $iNoSlot) { Fail "V153 the grant writes before it knows there is a slot" }
    elseif ($grantFn -notmatch '(?s)if slot == nil then.*?return false;') { Fail "V153 the grant carries on after a full table - the caller would think it wrote something" }
    else { Pass "V153 a full table warns and writes nothing; the dot that earned the gift stays bought" }
}

# ---- V154 + V156: bought only where the storyteller allowed, and never blind ----------
# ONE field now, not two flags (SPEC I49, V254, 87th round): a number 0..5 is the price and
# the sentinel is the door shut. ZERO IS A PRICE - the flag pair could not tell "the table
# gives them away" from "the table does not sell them", both read as specCost() == 0, so the
# door has a reader of its own and xpClick asks THAT one. Free dots still does not open the
# door: one says WHETHER, the other says HOW MUCH.
$costSpecFn = LuaFn $rootTxt 'specCost'
$forbidFn   = LuaFn $rootTxt 'specForbidden'
$xpCostFn   = LuaFn $rootTxt 'xpCost'
$specGuard  = [regex]::Match($cc, 'if trait ~= nil and string\.match\(trait, "\^speciality_%d\+\$"\) ~= nil then(.*?)\n\t\t\t\tend;', 'Singleline')
if (-not $costSpecFn) { Fail "V154 specCost not found on the root form" }
elseif (-not $forbidFn) { Fail "V154 specForbidden not found - the closed door has no reader of its own, so zero would have to mean both price and refusal again (SPEC V254b)" }
elseif ($forbidFn -notmatch 'if sheet == nil then return true; end;') { Fail "V154 specForbidden does not fail closed on a sheet with no room (SPEC V80)" }
elseif ($forbidFn -notmatch 'sheet\.stSpecCost == SPEC_FORBIDDEN') { Fail "V154 specForbidden does not compare stSpecCost against the sentinel - the door would answer to something else than the combo writes" }
elseif ($costSpecFn -notmatch 'tonumber\(sheet\.stSpecCost\) or 3') { Fail "V154 specCost does not read stSpecCost with the same 3 the dataLink declares - a sheet that predates the combo would price differently than the XML says (SPEC V254c)" }
elseif ($xpCostFn -notmatch 'if kind == "Specialty" then return specCost\(\); end;') { Fail "V154 xpCost does not price a specialty through specCost - the log would show it free" }
elseif (-not $specGuard.Success) { Fail "V154 xpClick has no speciality branch - a row could be bought with the door shut" }
elseif ($specGuard.Groups[1].Value -match 'specCost\(\) == 0') { Fail "V154 xpClick still reads a zero price as the closed door - a specialty the table gives away would be REFUSED instead (SPEC V254b)" }
elseif ($specGuard.Groups[1].Value -notmatch 'if want and base ~= nil and specForbidden\(\) then') { Fail "V154 the closed door is not asked about, or is asked before the character is frozen (SPEC V101)" }
elseif ($specGuard.Groups[1].Value -notmatch 'xpWarn\("Specialties cannot be bought"\);') { Fail "V154 a closed door refuses in silence" }
elseif ($specGuard.Groups[1].Value -notmatch 'xpWarn\("Choose a trait first"\);') { Fail "V156 a row with no trait refuses in silence" }
elseif ($specGuard.Groups[1].Value -match 'markDot\(|setField\(') { Fail "V154/V156 the speciality branch writes before it has decided (SPEC V135)" }
elseif ($cc.IndexOf('if trait ~= nil and string.match(trait, "^speciality_%d+$")') -gt $cc.LastIndexOf('markDot(form, field, want)')) { Fail "V154/V156 the speciality refusals come after the mark - there would be nothing to refuse" }
else { Pass "V154/V156 a specialty is bought only where the storyteller allowed it, zero is a price and not a shut door, and every refusal says why" }

# ---- V157: selling a speciality does not take the typing with it ---------------------
# The sale is the ordinary one: the dot goes out and the stamp with it (V141). The trait and
# the text stay where the player left them - one click may not destroy what was typed.
if ($cc -match 'setField\("speciality_"') { Fail "V157 xpClick writes a speciality row itself - the sale would clear the trait the player chose" }
elseif ($cc -match 'setField\("specialityName') { Fail "V157 xpClick clears the speciality text - one click would destroy what was typed" }
else { Pass "V157 a sold speciality keeps its trait and its text" }

# ---- V159: the revoked gift takes the whole row with it ------------------------------
# Trait, text, dot and stamp leave together, so the slot goes back to the pool. Half a row -
# the trait still showing with the dot out - would hold a slot for ever and say nothing. The
# typed text goes with it: that is what losing the gift costs.
if (-not $revokeFn) { Fail "V159 revokeSpeciality not found on the root form" }
elseif ($revokeFn -notmatch 'setField\("speciality_" \.\. slot, ""\);') { Fail "V159 the revoke leaves the trait in the row - the slot would never come back" }
elseif ($revokeFn -notmatch 'setField\("specialityName_" \.\. slot, ""\);') { Fail "V159 the revoke leaves the text behind - the next gift would land under someone else's words" }
elseif ($revokeFn -notmatch 'markDot\(form, "speciality_" \.\. slot \.\. "_1", false\);') { Fail "V159 the revoke leaves the dot lit - with autoChange off nothing else will put it out (SPEC V134)" }
elseif ($revokeFn -notmatch 'string\.gsub\(sheet\.xpFree or "", "\|speciality_" \.\. slot \.\. "#1\|", "\|"\)') { Fail "V159 the revoke leaves the stamp - the empty row would stay locked (SPEC V161)" }
elseif ($revokeFn -notmatch 'if slot == nil then return false; end;') { Fail "V159 the revoke carries on with no row to revoke" }
else { Pass "V159 a revoked gift gives back trait, text, dot and stamp in one step" }

# ---- V161 + V162: the gift is not the player's to change ------------------------------
# Two widgets, two mechanisms, one truth each: a comboBox has no click hook, so `enabled` is
# the only lock there (the same one cboGame carries, V109); the dot has an onClick, which is
# what marks a dot editable (V111), so it is refused inside xpClick instead of being dimmed.
#
# The lock is settled BEFORE the baseline test: a gift is never the player's to take off,
# not even while the character is being built.
$renderSpecFn = LuaFn $rootTxt 'renderSpecialities'
$iSpecGuard   = $cc.IndexOf('if trait ~= nil and string.match(trait, "^speciality_%d+$")')
$iBaseTest    = $cc.IndexOf('if base == nil then')
$specDotOpacity = @($mainDoc.SelectNodes("//template[@name='SpecialityRow']//imageCheckBox[@opacity]"))
if (-not $renderSpecFn) { Fail "V161 renderSpecialities not found on the root form" }
elseif ($iSpecGuard -lt 0) { Fail "V161 xpClick has no speciality branch - the gift could be taken off with a click" }
elseif ($iSpecGuard -gt $iBaseTest) { Fail "V161 the lock is tested after the baseline check - the gift would be the player's to remove while the character is being built" }
elseif ($specGuard.Groups[1].Value -notmatch 'if not want and isFreeRow\(trait\) then') { Fail "V161 selling a stamped row is not refused - the gift would go with one click" }
elseif ($specGuard.Groups[1].Value -notmatch 'xpWarn\("Free specialties are lost only by lowering the trait"\);') { Fail "V161 the locked dot refuses in silence" }
elseif ($specDotOpacity.Count -gt 0) { Fail "V161 the speciality dot carries opacity - a dot with an onClick is editable and must not read as read-only (SPEC V111)" }
# V161 REWRITTEN by T842: the lock is of the WIDGET, not of the gift. It used to read
# `c.enabled = not locked` on the row's comboBox, and the reason written for that was "a
# comboBox has no click hook, so this is the only lock available here" - true of the combo,
# false of the gift. Wave 2 of I113e replaced the combo with a button, and a button HAS an
# onClick, so the refusal moved into the OPENER: the box does not open at all on a stamped
# row, which is the same answer at the same moment the disabled combo gave. The button stays
# fully legible - reading WHICH speciality the trait handed over is the point of the row
# (SPEC V161 amended in the 146th, I41, V241).
elseif ($renderSpecFn -match 'c\.enabled') { Fail "V161 renderSpecialities still writes enabled on a control - the combo it locked is gone, and a lock left behind on a widget nobody draws is a lock that stopped locking (SPEC V161, T842)" }
elseif (-not ($mfOpen161 = LuaFn $rootTxt 'mfOpen')) { Fail "V161 mfOpen is gone - the lock of a picker row now lives in the opener and there is no opener left (SPEC V161, V209)" }
elseif ($mfOpen161 -notmatch 'if sheet ~= nil and isFreeRow\(field\) then return; end;') { Fail "V161 mfOpen does not refuse a stamped row - with the combo gone this is the ONLY thing standing between the player and a gift the trait handed out (SPEC V161, T842)" }
elseif ($mfOpen161.IndexOf('isFreeRow(field)') -gt $mfOpen161.IndexOf('MF.field = field;')) { Fail "V161 the refusal is tested AFTER the box has taken the field - a box that opens and then refuses is a lock the player finds at the end (SPEC V161)" }
elseif ($renderSpecFn -notmatch 'mfLabel\(found\["dynspeciality_" \.\. i\]') { Fail "V161 renderSpecialities does not paint the seven buttons - a button's text is not a field, so the granted speciality would show as an empty row (SPEC I107a2, T842)" }
elseif ($renderSpecFn -match 'c\.opacity') { Fail "V162 the speciality row still paints opacity - a locked row has to keep its text readable (SPEC I41, V241)" }
elseif ($renderSpecFn -match 'specialityName|markDot') { Fail "V163 the renderer touches the text or the dot - it paints the button and nothing else" }
else { Pass "V161/V162 a gift row is refused in the opener before the box can open, its button is painted from the field and stays legible, and the dot refuses inside xpClick" }

# ---- V164: the lock is painted from three places, and finds its controls the one way ---
# The click covers the sheet in front of the player; onNodeReady covers the sheet being
# opened; the xpFree link covers a grant made on ANOTHER client at the table. `form.<name>`
# does not cross the <import>, which is why this walks with the one finder (V143).
$mainRawTxt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20.1.lfm")))
$renderCalls = @([regex]::Matches($rootTxt, 'renderSpecialities\('))
$specLinks2  = @($mainDoc.SelectNodes("//dataLink[@field='xpFree']"))
if (-not $renderSpecFn) { Fail "V164 renderSpecialities not found on the root form" }
elseif ($renderSpecFn -notmatch 'xpFind\(tabRootOf\(from\), names, found\);') { Fail "V164 the renderer does not use the one control finder - form.<name> does not cross the import (SPEC V143, B9)" }
# 4 since T842: the declaration, the two accepted click paths, and the speciality dataLink the
# BUTTON needs. The combo painted itself from the field; a button's text is not a field, so the
# seven rows need the same watcher the sixteen backgrounds have had since T808 (SPEC I107a2).
elseif ($renderCalls.Count -ne 4) { Fail "V164 renderSpecialities is called from $($renderCalls.Count - 1) place(s) on the root form - the two accepted click paths, the speciality dataLink, and no more" }
elseif ($specLinks2.Count -ne 1) { Fail "V164 WoD20.1 carries $($specLinks2.Count) xpFree links - one, so a grant made on another client locks the row here too" }
elseif ($mainRawTxt -notmatch '<dataLink field="xpFree" onChange="renderSpecialities\(self\);"/>') { Fail "V164 the xpFree link does not repaint the lock" }
elseif ($mainRawTxt -notmatch '(?s)<event name="onNodeReady">.*?renderSpecialities\(self\);.*?</event>') { Fail "V164 opening the sheet does not paint the lock - a saved gift would show as editable" }
else { Pass "V164 the lock is painted on the click, on open and on a remote grant, through the one finder" }

# ---- V165: the rename is text, never a field ------------------------------------------
# speciality_N and specialityName_N shipped in 4.6, so they stay whatever the label says
# (SPEC V2) - the same lie `retainers` tells the GUIDES box. What must not survive is a key
# nobody reads: the old English strings are gone from the .lang and from the PT map.
$oldStrings = @('SPECIALITIES', 'Speciality', 'Specialities cannot be bought', 'No free speciality slot')
$staleKeys  = @($oldStrings | Where-Object { $enK.Contains($_) -or $ptK.Contains($_) -or $embedded.ContainsKey($_) })
$kindPush   = @([regex]::Matches($rootTxt, 'pushRise\(rows, "Specialty"'))
$fieldsKept = (($allFields.Keys | Where-Object { $_ -match '^speciality_\d+$' }).Count -eq $spRows) -and
              (($allFields.Keys | Where-Object { $_ -match '^specialityName_\d+$' }).Count -eq $spRows)
if ($staleKeys.Count -gt 0) { foreach ($s in $staleKeys) { Fail "V165 '$s' is still keyed - the old spelling would sit in the .lang and the PT map with nothing reading it" } }
elseif ($kindPush.Count -ne 1) { Fail "V165 the ledger does not push a 'Specialty' row - the kind and the label must be the same string (SPEC V12)" }
elseif ($xpCostFn -notmatch 'kind == "Specialty"') { Fail "V165 xpCost prices a kind the ledger never produces - the two halves of the rename came apart" }
elseif (-not $fieldsKept) { Fail "V165 the speciality FIELDS were renamed - they shipped in 4.6 and a saved sheet would lose them (SPEC V2)" }
else { Pass "V165 the rename is text and keys only; the fields kept the name they shipped with" }

# ---- V166: the sheet is authored hidden, and one place lights it ---------------------
# B40: the host draws before any hook of ours runs (SPEC R47), so every open showed a
# second of the sheet as the XML authors it - a look no theme has offered since the 16th
# round. The fix is not to show it, which only holds while exactly one place can show it.
# The reveal target is a <layout> since the 78th round (SPEC I32). Authored hidden for the
# same reason the tabControl was: the host paints before any hook of ours runs (SPEC R47).
$tcTag = [regex]::Match($rootLfm, '<layout name="sheetBody"[^>]*>')
$revealFn = [regex]::Match($rootLfm, '(?s)function sheetReveal\(from\)(.*?)\r?\n\t{3}end;')
$revealBody = if ($revealFn.Success) { $revealFn.Groups[1].Value } else { '' }
$tabsHHTotal = 0
foreach ($f in $files) {
    # XML comments are stripped before counting. SPEC V166 measures the one WRITE that lights
    # the sheet, and a comment cannot write. Naming the layout in prose is something I102a now
    # HAS to do - the floating overlays hang off it since B107/V367 - and counting those
    # mentions reddened correct authorship, which is B92's shape. Real mentions are unchanged.
    $raw = [regex]::Replace([System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName)), '(?s)<!--.*?-->', '')
    $tabsHHTotal += ([regex]::Matches($raw, 'sheetBody')).Count
}
$tabsHHInReveal = ([regex]::Matches($revealBody, 'sheetBody')).Count
$tabsHHInTag = ([regex]::Matches($tcTag.Value, 'sheetBody')).Count
$revealLights = ([regex]::Matches($revealBody, '\.visible\s*=\s*true')).Count
$tabsHHLoose = $tabsHHTotal - $tabsHHInReveal - $tabsHHInTag
if (-not $tcTag.Success) { Fail "V166 no sheetBody layout on the root form - nothing to reveal (SPEC I32)" }
elseif ($tcTag.Value -notmatch 'align="client"') { Fail "V166 sheetBody does not fill the form - the sheet would draw in a corner (SPEC I32)" }
elseif ($tcTag.Value -notmatch 'visible="false"') { Fail "V166 sheetBody is authored visible - the raw sheet is on screen before anything paints it (SPEC B40)" }
elseif (-not $revealFn.Success) { Fail "V166 sheetReveal not found on the root form - nothing would ever show the sheet" }
elseif ($revealLights -ne 1) { Fail "V166 sheetReveal writes visible=true $revealLights time(s) - it is the one place that lights the sheet" }
elseif ($tabsHHLoose -ne 0) { Fail "V166 sheetBody is named $tabsHHLoose time(s) outside sheetReveal and its own declaration - a second place could show the sheet early" }
else { Pass "V166 sheetBody is authored hidden and sheetReveal is the only thing that lights it" }

# ---- V167: the reveal is idempotent and cannot leave the sheet blank -----------------
# The mirror of B26: there the static state failed OPEN and showed tabs nobody asked for;
# here it fails open on purpose. A paint that never runs has to leave the sheet ugly, never
# blank, so one of the two entries must not depend on any paint. A timer cannot fire early
# and cannot depend on the paint (SPEC R44 - it is only asynchronous above zero).
$revealCallCount = -1
foreach ($f in $files) {
    $raw = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName))
    $revealCallCount += ([regex]::Matches($raw, 'sheetReveal\(')).Count
}
$timerArm = [regex]::Match($rootLfm, 'setTimeout\(\s*function\(\)\s*sheetReveal\(self\);\s*end\s*,\s*(\d+)\s*\)')
$themeEntry = ($hh6 -match '(?s)<dataLink field="sheetTheme".*?sheetReveal\(self\);.*?</dataLink>')
$armedOnReady = ($rootLfm -match '(?s)<event name="onNodeReady">.*?setTimeout\(\s*function\(\)\s*sheetReveal')
if ($revealCallCount -lt 2) { Fail "V167 sheetReveal is called from $revealCallCount place(s) - one entry means one way to fail (SPEC I15)" }
elseif ($revealBody -notmatch 'tc\.visible ~= true') { Fail "V167 sheetReveal writes without looking - it is called more than once and must be idempotent" }
elseif (-not $timerArm.Success) { Fail "V167 no timer arms the reveal on the root form - every entry would hang off a paint that may never run (SPEC B40)" }
elseif ([int]$timerArm.Groups[1].Value -le 0) { Fail "V167 the reveal timer is armed at $($timerArm.Groups[1].Value)ms - setTimeout runs synchronously at zero (SPEC R44)" }
elseif (-not $armedOnReady) { Fail "V167 the timer is not armed from onNodeReady - the floor has to go down when the sheet opens" }
elseif (-not $themeEntry) { Fail "V167 the theme paint does not call sheetReveal - the normal entry is the last load paint there is (SPEC I15)" }
else { Pass "V167 the reveal is idempotent, armed by a $($timerArm.Groups[1].Value)ms floor and by the theme paint" }

# ---- V168: the Main tab is a three-column grid ---------------------------------------
# The 49th round lined up the two outer ends of the top row (TALENTS with VIRTUES,
# KNOWLEDGES with EXPERIENCE) and this check measured that one pair. In the 50th the user
# asked for the other three, so the rule is the grid itself: every box on the bottom row
# takes BOTH x edges from the column above it, and the three columns are one row - same
# top, same width, same height, same gap.
$bTal = BoxOf $mainDoc "TALENTS"; $bSki = BoxOf $mainDoc "SKILLS"; $bKno = BoxOf $mainDoc "KNOWLEDGES"
$bExp = BoxOf $mainDoc "EXPERIENCE"
# V375 SUPERSEDES V168 (SPEC V375, I126, T865, T862, T908). V168 measured THREE bands and
# carried SPECIALTIES under SKILLS; the 157th round moved SPECIALTIES INTO the grid row as its
# fourth box, and T908 put it back under SKILLS at the height its own content asks for - so the
# tab is FOUR bands wide again and the fifth lasted two days (SPEC I136b, I136c, user 2026-09-02
# items 1 and 2). What did NOT change is the ruler: a box on the bottom row still takes BOTH x
# edges from the column above it, and the grid still closes on EXPERIENCE's right edge - which
# is 1345 for both now, because item 6 took EXPERIENCE back to one number and 330 wide in the
# SAME commit. Splitting those two asks reddens this leg BETWEEN tasks, which is why T908
# carries three of the user's items and not one (SPEC I136b).
$BANDS = @(0, 340, 345, 675, 680, 1010, 1015, 1345)
# T904 (SPEC I134m): the user asked for the ROAD box to sit UNDER VIRTUES, so the TALENTS band
# carries a STACK of two. under was already a LIST, so the roster could say that before it had
# to - nothing here was loosened. Every box in a under list is still held to BOTH x edges of its
# column, which is what keeps a stack a stack instead of two boxes that happen to be near each
# other. That round left the SKILLS band EMPTY (Q44, a decision and not a pending move) and T908
# filled it with SPECIALTIES, which is where V168 had it before the fifth band existed.
$GRID = @(
    @{ col = "TALENTS";     under = @("VIRTUES", "ROAD") },
    @{ col = "SKILLS";      under = @("SPECIALTIES") },
    @{ col = "KNOWLEDGES";  under = @("HEALTH") }
)
$colW = @(); $abilRowOk = $false
$gridMissing = @()
foreach ($g in $GRID) {
    if (-not (BoxOf $mainDoc $g.col)) { $gridMissing += $g.col }
    foreach ($u in $g.under) { if (-not (BoxOf $mainDoc $u)) { $gridMissing += $u } }
}
if (-not $bExp) { $gridMissing += "EXPERIENCE" }

# (c) EVERY box on the tab starts and ends on a band edge, and (e) the zero-guard: fewer than
# ten boxes measured means the sweep broke, not that the tab shrank. Boxes are the <layout>
# children of the scrollBox that carry all four coordinates - the same set V40 overlaps.
$mainBoxes = @()
foreach ($bx in $mainDoc.SelectNodes("//scrollBox/layout")) {
    $bl = 0; $bw = 0
    if ([int]::TryParse($bx.GetAttribute("left"), [ref]$bl) -and
        [int]::TryParse($bx.GetAttribute("width"), [ref]$bw)) {
        $ttlN = $bx.SelectSingleNode("label[@text]")
        $mainBoxes += @{
            L    = $bl
            R    = $bl + $bw
            Name = if ($null -ne $ttlN) { $ttlN.GetAttribute("text") } else { "an untitled box at left=$bl" }
        }
    }
}
$offBand = @()
foreach ($mb in $mainBoxes) {
    if ($BANDS -notcontains $mb.L -or $BANDS -notcontains $mb.R) { $offBand += "$($mb.Name) runs $($mb.L)..$($mb.R)" }
}

# (d) LEFT this rule in T908 and became V396. It measured popScrim against the right edge of
# THIS tab's EXPERIENCE box - ONE named tab - which is a ruler of MAXIMUM written as an equality
# against one member. It was green while Main was WIDER than the widest tab, which is not the
# same as measuring the widest: Traits closes at 1595 and the scrim carried 1680, so 85px of
# slack read as correct. Main is 1345 now, and the leg would have DEMANDED a scrim 250px too
# narrow for Traits (SPEC B127, B129, I136g). V396 derives the width off the same sweep V389
# uses for the height, so neither axis is tied to a tab by name.

if ($gridMissing.Count -gt 0) { Fail "V375 not a titled box on WoD20.1: $($gridMissing -join ', ')" }
elseif ($mainBoxes.Count -lt 10) { Fail "V375 only $($mainBoxes.Count) box(es) read off WoD20.1 - the sweep broke and the band rule measured nothing (SPEC V20)" }
else {
    $offGrid = @()
    foreach ($g in $GRID) {
        $c = BoxOf $mainDoc $g.col
        $cL = [int]$c.GetAttribute("left"); $cR = $cL + [int]$c.GetAttribute("width")
        foreach ($u in $g.under) {
            $b = BoxOf $mainDoc $u
            $bL = [int]$b.GetAttribute("left"); $bR = $bL + [int]$b.GetAttribute("width")
            if ($bL -ne $cL -or $bR -ne $cR) { $offGrid += "$u runs $bL..$bR under $($g.col) at $cL..$cR" }
        }
    }
    $cols = @($bTal, $bSki, $bKno)
    $colW = @($cols | ForEach-Object { [int]$_.GetAttribute("width") } | Sort-Object -Unique)
    $colT = @($cols | ForEach-Object { [int]$_.GetAttribute("top") } | Sort-Object -Unique)
    $colH = @($cols | ForEach-Object { [int]$_.GetAttribute("height") } | Sort-Object -Unique)
    $talL = [int]$bTal.GetAttribute("left")
    $gridR = [int]$bKno.GetAttribute("left") + [int]$bKno.GetAttribute("width")
    $expR = [int]$bExp.GetAttribute("left") + [int]$bExp.GetAttribute("width")
    $gaps = @()
    for ($i = 1; $i -lt $cols.Count; $i++) {
        $gaps += [int]$cols[$i].GetAttribute("left") - ([int]$cols[$i-1].GetAttribute("left") + [int]$cols[$i-1].GetAttribute("width"))
    }
    $gapU = @($gaps | Sort-Object -Unique)
    if ($offGrid.Count -gt 0) { Fail "V375 the grid is broken - $($offGrid -join '; ')" }
    elseif ($offBand.Count -gt 0) { Fail "V375 off the four bands ($($BANDS -join '/')): $($offBand -join '; ')" }
    elseif ($gridR -ne $expR) { Fail "V375 the grid closes at $gridR and EXPERIENCE at $expR - the header row and the grid share the tab's right edge" }
    elseif ($colW.Count -ne 1) { Fail "V375 the three grid columns are $($colW -join '/') wide - one would read narrower than its neighbours" }
    elseif ($colT.Count -ne 1) { Fail "V375 the three grid columns start at $($colT -join '/') - they are one row and share a top" }
    elseif ($colH.Count -ne 1) { Fail "V375 the three grid columns are $($colH -join '/') tall - they are one row and share a height" }
    elseif ($gapU.Count -ne 1) { Fail "V375 the column gaps are $($gaps -join '/') - the row would read lopsided" }
    else { $abilRowOk = $true; Pass "V375 the grid runs $talL..${gridR}: three $($colW[0])px columns $($gapU[0]) apart on four bands, each carrying the bottom row under it, with $($mainBoxes.Count) box(es) on band edges" }
}

# ---- V171: an ability column fits its content ----------------------------------------
# The columns shipped 390 tall with twelve rows closing at 325 - 65px of black tail under
# the last ability, which is what the user asked to be rid of. "Fits" is not a number here:
# the bottom margin is whatever ATTRIBUTES already leaves under its own last row, so the
# three columns and the box beside them agree by construction, and a thirteenth ability has
# to grow the box instead of moving into slack nobody declared.
function TailOf($box, $rowXPath) {
    $rows = @($box.SelectNodes($rowXPath))
    if ($rows.Count -eq 0) { return $null }
    $last = 0
    foreach ($r in $rows) {
        $rb = [int]$r.GetAttribute("top") + [int]$r.GetAttribute("height")
        if ($rb -gt $last) { $last = $rb }
    }
    return ([int]$box.GetAttribute("height") - $last)
}
$bAttr = BoxOf $mainDoc "ATTRIBUTES"
$refTail = if ($bAttr) { TailOf $bAttr "layout[Attribute or AttributeZeroable]" } else { $null }
if (-not $abilRowOk) { Fail "V171 not measured - the ability grid did not pass V375" }
elseif ($null -eq $refTail) { Fail "V171 ATTRIBUTES has no rows to take the bottom margin from - the ruler measured nothing (SPEC V20)" }
else {
    $tails = @()
    foreach ($n in @("TALENTS", "SKILLS", "KNOWLEDGES")) {
        $t = TailOf (BoxOf $mainDoc $n) "layout[Ability or CustomAbility]"
        if ($t -ne $refTail) { $tails += "$n leaves ${t}px" }
    }
    if ($tails.Count -gt 0) { Fail "V171 ATTRIBUTES leaves ${refTail}px under its last row but $($tails -join ', ') - the column carries dead tail (or clips its last row)" }
    else { Pass "V171 all three ability columns close ${refTail}px under their last row, the margin ATTRIBUTES sets" }
}

# ---- V169: the ABILITIES frame is gone, key and all ----------------------------------
# It was a box whose title named what the three titles inside it already said, and its
# frame cost 30px of width the names wanted. A label that no longer exists must not leave
# a key behind in the .lang or the PT map (the rule V165 wrote for the rename).
$abilTitles = @($mainDoc.SelectNodes("//label[@text='ABILITIES']"))
$looseCols = @($mainDoc.SelectNodes("/form/scrollBox/layout[label/@text='TALENTS' or label/@text='SKILLS' or label/@text='KNOWLEDGES']"))
if ($abilTitles.Count -ne 0) { Fail "V169 WoD20.1 still draws an ABILITIES title - the frame the user asked to remove is back" }
elseif ($looseCols.Count -ne 3) { Fail "V169 $($looseCols.Count) of the three ability columns are direct children of the scrollBox - nested again, V40 stops weighing them against the tab" }
elseif ($ptK.Contains("ABILITIES") -or $enK.Contains("ABILITIES")) { Fail "V169 ABILITIES is still keyed in localization.lang with no label reading it" }
elseif ($hh6 -match '\["ABILITIES"\]') { Fail "V169 ABILITIES is still in the PT map with no label reading it" }
else { Pass "V169 the ABILITIES frame is gone, the three columns stand loose and no key survives it" }

# ---- V371: the slot an era does not fill GOES, and the typed twin takes its place -----
# The renderer was `if name ~= nil then show(c, name); end;` with no else, and the SAME test
# guarded the dot rebinding: a named slot an era does not reach kept the PREVIOUS era's label
# and stayed bound to the previous era's field, so the points a player put on it landed on a
# trait this era has not got. It slept while all four eras had ten a column; I123 gives
# knowledges thirteen names in two eras and twelve in the other two, and wakes it in one slot.
#
# The ruler does NOT ask the era lists to be the same length. The owner decided that an era
# borrows a trait from no other era (Q34), so what is measured is that the difference is
# HANDLED - and (e) is what stops the decision being undone later by a name that costs nothing.
$V371_COUNTS = @{
    # MEASURED 2026-08-30 from the books the user named, talents/skills/knowledges (SPEC I123).
    # A literal on purpose: there is no second side to derive it from, and a count that moves
    # with no round behind it IS the borrowed name coming in the back door (SPEC V371e).
    'Modern Nights' = @{ talents = 11; skills = 13; knowledges = 13 }
    'Victorian Age' = @{ talents = 11; skills = 13; knowledges = 12 }
    'Dark Ages'     = @{ talents = 11; skills = 13; knowledges = 12 }
    'Classical Age' = @{ talents = 11; skills = 13; knowledges = 13 }
}
$V371_ROWS_PER_COLUMN = 15
$v371Bad  = @()
$v371Cols = @{ TALENTS = 'talents'; SKILLS = 'skills'; KNOWLEDGES = 'knowledges' }
$v371Rows = @{}
$v371Twin = @{}

foreach ($title in @('TALENTS', 'SKILLS', 'KNOWLEDGES')) {
    $col = $v371Cols[$title]
    $box = BoxOf $mainDoc $title
    if ($null -eq $box) { $v371Bad += "the $title column is not on WoD20.1 - the check measured nothing (SPEC V20)"; continue }
    $rows = @()
    foreach ($row in $box.SelectNodes("layout")) {
        $top = 0
        if (-not [int]::TryParse($row.GetAttribute("top"), [ref]$top)) { continue }
        $a = @($row.SelectNodes("Ability"))[0]
        $c = @($row.SelectNodes("CustomAbility"))[0]
        if ($null -eq $a -and $null -eq $c) { continue }
        $num = 0
        $fld = ''
        if ($null -ne $a) { [void][int]::TryParse($a.GetAttribute("num"), [ref]$num); $fld = $a.GetAttribute("field") }
        else { $fld = $c.GetAttribute("field") }
        $rows += [pscustomobject]@{
            Top    = $top
            Named  = ($null -ne $a)
            Num    = $num
            Field  = $fld
            RowNm  = $row.GetAttribute("name")
            Hidden = ($row.GetAttribute("visible") -eq 'false')
        }
    }
    $v371Rows[$col] = @($rows)
}

# The twin pair is found by the NAMED half: the CustomAbility widgets carry no name= of their
# own (measured in the template), so the renderer cannot reach them through the name loop it
# already runs - it reaches them through the row layouts, which is what these two names are.
foreach ($col in @($v371Rows.Keys)) {
    foreach ($r in $v371Rows[$col]) {
        if ($r.RowNm -match '^dynAbilRow_(.+)_(\d+)$') {
            if ($Matches[1] -ne $col) { $v371Bad += "$($r.RowNm) sits in the $col column - the renderer would look its slot up in another column and find nothing" }
            $v371Twin["$col/$($Matches[2])"] = @{ Named = $r }
        }
    }
    foreach ($r in $v371Rows[$col]) {
        if ($r.RowNm -match '^dynAbilFill_(.+)_(\d+)$') {
            $k = "$col/$($Matches[2])"
            if ($Matches[1] -ne $col) { $v371Bad += "$($r.RowNm) sits in the $col column - the renderer would look its slot up in the wrong column" }
            if (-not $v371Twin.ContainsKey($k)) { $v371Bad += "$($r.RowNm) has no named half - a typed row that hides for an era nothing fills leaves a 25px hole (SPEC V371b)" }
            else { $v371Twin[$k].Fill = $r }
        }
    }
}

$vic371 = $eraLists['Victorian Age']

if ($v371Bad.Count -eq 0) {
    # (f) zero-guard, first: every leg below is quiet on an empty read.
    if ($eraLists.Count -eq 0) { $v371Bad += "no era list was read - every leg below would pass on nothing (SPEC V20)" }
    if ($v371Twin.Count -eq 0) { $v371Bad += "no twin pair found on WoD20.1 - the one mechanism that pays for an era being a name short is gone (SPEC V371f)" }
    foreach ($col in @('talents', 'skills', 'knowledges')) {
        if (-not $v371Rows.ContainsKey($col) -or @($v371Rows[$col] | Where-Object { $_.Named }).Count -eq 0) {
            $v371Bad += "the $col column has no named slot in the XML - there is nothing to weigh the era lists against (SPEC V20)"
        }
    }
}

if ($v371Bad.Count -eq 0) {
    foreach ($col in @('talents', 'skills', 'knowledges')) {
        $named   = @($v371Rows[$col] | Where-Object { $_.Named })
        $slots   = @($named | ForEach-Object { $_.Num } | Sort-Object)
        $maxSlot = ($slots | Measure-Object -Maximum).Maximum
        $tops    = @($v371Rows[$col] | ForEach-Object { $_.Top } | Sort-Object -Unique)

        # The slot number IS the renderer's index into the era list, so a gap or a repeat is a
        # slot reading someone else's name.
        for ($i = 1; $i -le $maxSlot; $i++) {
            $hit = @($slots | Where-Object { $_ -eq $i }).Count
            if ($hit -ne 1) { $v371Bad += "the $col column authors slot $i $hit time(s) - the renderer indexes the era list by this number" }
        }

        # (c) three columns, the same number of LINES, in every era. Twins share a top, so the
        # count of distinct tops IS what the player sees whichever era is on.
        if ($tops.Count -ne $V371_ROWS_PER_COLUMN) { $v371Bad += "the $col column shows $($tops.Count) lines, not the $V371_ROWS_PER_COLUMN the other two do - 'paid for with a typed row' means nothing if nobody counts (SPEC V371c)" }

        $longest = 0
        foreach ($era in $eraLists.Keys) {
            if (-not $eraLists[$era].ContainsKey($col)) { continue }
            $n = $eraLists[$era][$col].Count
            if ($n -gt $longest) { $longest = $n }

            # (a) a named slot shows for exactly the eras that fill it. Fewer names than slots
            # is legal ONLY where the spare slot has a twin to swap in; without one the row
            # keeps the last era's label and field.
            if ($n -gt $maxSlot) { $v371Bad += "$era/$col lists $n names and the XML authors $maxSlot slot(s) - the last $($n - $maxSlot) would have nowhere to show (SPEC V371a)" }
            for ($i = $n + 1; $i -le $maxSlot; $i++) {
                if (-not $v371Twin.ContainsKey("$col/$i")) { $v371Bad += "$era/$col leaves slot $i empty and that slot has no typed twin - it would render with the previous era's name and field (SPEC V371a, I123c)" }
            }

            # (d) the bijection of V74 is what makes (a) enough: one name, one field.
            foreach ($nm in $eraLists[$era][$col]) {
                if (-not $abilityField.ContainsKey($nm)) { $v371Bad += "$era/$col lists '$nm', which owns no field - the slot would show a name and bind nothing (SPEC V371d, V74)" }
            }

            # (e) the count the owner signed off on, per era and per column.
            if ($null -eq $V371_COUNTS[$era]) { $v371Bad += "'$era' is an era the measured counts do not know - a list nobody signed off on (SPEC V371e, Q34)" }
            elseif ($V371_COUNTS[$era][$col] -ne $n) { $v371Bad += "$era/$col carries $n names, not the $($V371_COUNTS[$era][$col]) measured from that era's own books - an era borrows a trait from no other era (SPEC V371e, Q34)" }
        }
        if ($longest -ne $maxSlot) { $v371Bad += "the $col column authors $maxSlot slot(s) and the longest era list holds $longest - a slot no era ever fills is a line that renders empty in every era (SPEC V371a)" }
    }

    # (b) the pair stands on ONE top and exactly one half shows. Authored state is the
    # Victorian era like every other row (V76), so WHICH half ships hidden is derived.
    foreach ($k in @($v371Twin.Keys)) {
        $p = $v371Twin[$k]
        $tc = $k.Split('/')[0]; $tn = [int]$k.Split('/')[1]
        if ($null -eq $p.Fill) { $v371Bad += "slot $k has a named half and no typed twin - the era that is a name short would show a hole (SPEC V371b)"; continue }
        if ($p.Named.Top -ne $p.Fill.Top) { $v371Bad += "slot $k puts its halves on top=$($p.Named.Top) and top=$($p.Fill.Top) - two tops leave 25px of hole in whichever era hides the upper one (SPEC V371b, I123b)" }
        $namedState = if ($p.Named.Hidden) { 'hidden' } else { 'visible' }
        if ($p.Named.Hidden -eq $p.Fill.Hidden) { $v371Bad += "slot $k authors both halves $namedState - exactly one of the pair is ever on screen (SPEC V371b)" }
        if ($null -ne $vic371) {
            $vicFills = ($vic371[$tc].Count -ge $tn)
            $vicWord = if ($vicFills) { 'fills' } else { 'does not fill' }
            if ($p.Named.Hidden -eq $vicFills) { $v371Bad += "slot $k ships with the named half $namedState and the Victorian era $vicWord it - a sheet whose renderer never ran must read as one era (SPEC V76, V19)" }
        }
    }

    # The else itself. Both halves read the SAME list index in OPPOSITE senses: one branch on
    # its own would hide the named row and leave the line blank.
    if ($hh6 -notmatch 'elseif string\.sub\(nm, 1, 11\) == "dynAbilRow_" then') { $v371Bad += "renderAbilityLabels never touches a dynAbilRow_ layout - the named slot would keep the previous era's label and field (SPEC I123c)" }
    elseif ($hh6 -notmatch 'elseif string\.sub\(nm, 1, 12\) == "dynAbilFill_" then') { $v371Bad += "renderAbilityLabels never touches a dynAbilFill_ layout - the typed twin would never come up in the era that needs it (SPEC V371b)" }
    elseif ($hh6 -notmatch '(?s)dynAbilRow_.{0,900}?local want = lists\[col\]\[tonumber\(num\)\] ~= nil;') { $v371Bad += "the dynAbilRow_ branch does not show the slot exactly when the era names it (SPEC V371a)" }
    elseif ($hh6 -notmatch '(?s)dynAbilFill_.{0,900}?local want = lists\[col\]\[tonumber\(num\)\] == nil;') { $v371Bad += "the dynAbilFill_ branch does not show the typed row exactly when the era does NOT name the slot - both halves on at once, or both off (SPEC V371b)" }
}

if ($v371Bad.Count -gt 0) { foreach ($b in ($v371Bad | Sort-Object -Unique)) { Fail "V371 $b" } }
else { Pass "V371 all $($eraLists.Count) eras fill their authored slots, $($v371Twin.Count) twin pair(s) cover what they leave over, and every column shows $V371_ROWS_PER_COLUMN lines" }

# ---- V370: ONE vertical ruler on the Main tab, and it is 25 --------------------------
# The ability grid always stepped 25 (41 66 91 ...). The header stepped 30, VIRTUES 29 and
# SPECIALTIES 30, and none of those three was a decision - each was inherited from the round
# that drew it. Three rulers on one screen read as three, and with the grid three rows taller
# (I123) the slack stopped being free and turned into height the tab could not spare.
#
# Measured by BLOCK and not by tab: a block is a thing with SIBLING rows, and siblings are the
# rows of one column - grouped by `left`, because the header lays three columns side by side on
# the same three tops. Blocks with no sibling row (ATTRIBUTES groups its nine in threes with a
# deliberate gap between them; the ROAD box stacks two unlike tracks) are not on the list and
# are not measured: the ruler is for a pile of like rows.
#
# What it does NOT measure is FOLGA - the margin over the first row and under the last (V370b).
# Tightening a pitch lines rows up; tightening a margin squashes them, and the two moves are
# not the same request. That is also why the tab's closing line came out of the new heights
# rather than being chosen: the left column saved 23 and SPECIALTIES 45, and the 22 SPECIALTIES
# came up short is FOLGA in its box, never a wider step (SPEC I124f).
$v370Bad   = @()
$v370Boxes = @()
$v370Steps = 0
$hdr370 = @($mainDoc.SelectNodes("/form/scrollBox/layout[layout[Header or HeaderNarrow or HeaderPicker]]"))[0]
if ($null -eq $hdr370) { $v370Bad += "the header box is not on WoD20.1 - it is one of the four blocks the ruler covers and it went unmeasured (SPEC V20, V370d)" }
else { $v370Boxes += [pscustomobject]@{ Name = 'the header box'; Node = $hdr370 } }
foreach ($t in @('VIRTUES', 'SPECIALTIES', 'TALENTS', 'SKILLS', 'KNOWLEDGES')) {
    $b = BoxOf $mainDoc $t
    if ($null -eq $b) { $v370Bad += "$t is not on WoD20.1 - one of the blocks the ruler covers went unmeasured (SPEC V20, V370d)" }
    else { $v370Boxes += [pscustomobject]@{ Name = $t; Node = $b } }
}

foreach ($bx in $v370Boxes) {
    $byLeft = @{}
    foreach ($row in $bx.Node.SelectNodes("layout")) {
        $l = 0; $tp = 0
        if (-not [int]::TryParse($row.GetAttribute("left"), [ref]$l)) { continue }
        if (-not [int]::TryParse($row.GetAttribute("top"), [ref]$tp)) { continue }
        if (-not $byLeft.ContainsKey($l)) { $byLeft[$l] = @() }
        $byLeft[$l] += $tp
    }
    if ($byLeft.Count -eq 0) { $v370Bad += "$($bx.Name) declares no positioned row - the block reads empty and its pitch went unmeasured (SPEC V370d)"; continue }
    foreach ($l in @($byLeft.Keys)) {
        # Unique: a twin pair shares one top on purpose (V371b) and is ONE line, not a step of 0.
        $tops = @($byLeft[$l] | Sort-Object -Unique)
        if ($tops.Count -lt 2) { $v370Bad += "$($bx.Name) has a single row at left=$l - one row has no pitch, so nothing there was measured (SPEC V370d)"; continue }
        for ($i = 1; $i -lt $tops.Count; $i++) {
            $step = $tops[$i] - $tops[$i - 1]
            $v370Steps++
            if ($step -ne $MAIN_ROW_PITCH) { $v370Bad += "$($bx.Name) steps $step from top=$($tops[$i - 1]) to top=$($tops[$i]) - the Main tab has ONE vertical ruler and it is $MAIN_ROW_PITCH (SPEC V370a). A block that will not fit changes HEIGHT, not pitch (SPEC V370c, I124f)" }
        }
    }
}
if ($v370Steps -eq 0 -and $v370Bad.Count -eq 0) { $v370Bad += "not one step was measured - the check is a no-op (SPEC V20, V370d)" }

if ($v370Bad.Count -gt 0) { foreach ($b in ($v370Bad | Sort-Object -Unique)) { Fail "V370 $b" } }
else { Pass "V370 all $v370Steps step(s) across $($v370Boxes.Count) block(s) of the Main tab measure $MAIN_ROW_PITCH" }

# ---- V374: a TITLED box opens its first row on the title's HAIRLINE -------------------
# V280a measures 20 from the border to the first child - and the first child IS the title.
# V240 measures the same 20. Between the title and the first ROW there was no owner at all for
# an ordinary box: the two stretched boxes of V267b are the only ones with a ruler there, and
# they get it by CENTRING. So VIRTUES opened its rows at 45 while every other titled box on the
# tab opened at 41, for rounds on end, and nothing reddened (SPEC I125c, T861).
#
# The hairline is the same RELATION V146 has read off SPECIALTIES since the 103rd round:
# title.top + title.height + 1. A relation and not the number 41, so the day the margin moves
# the check moves with it instead of being hand-edited.
#
# ATTRIBUTES is on the list even though V370 leaves it off (its nine rows walk in three groups
# with a deliberate 35px gap between them): it is already the RODAPE reference V171 measures the
# ability columns against, and where a box's body OPENS is the other end of the same box. One
# box handing out both rulers is what stops two rulers from disagreeing.
#
# The header box is OUT - it has no title, so there is no hairline to measure from, and its
# first row opens on the V280a margin, which already has an owner. HEALTH is out for the same
# reason it is out of V370a: a 20px row at a pitch of 27, height governed by V49 and mirrored on
# the Combat tab by V284c. Neither is an exception BY NAME (V370c forbids those) - it is the
# boundary V370 already drew, cited once here instead of redrawn.
$v374Bad = @()
$v374Ok  = 0
foreach ($t in @('ATTRIBUTES', 'TALENTS', 'SKILLS', 'KNOWLEDGES', 'VIRTUES', 'SPECIALTIES')) {
    $bx374 = BoxOf $mainDoc $t
    if ($null -eq $bx374) { $v374Bad += "$t is not on WoD20.1 - a box the hairline covers went unmeasured (SPEC V20, V374e)"; continue }
    $ttl374 = $bx374.SelectSingleNode("label[@text='$t']")
    if ($null -eq $ttl374) { $v374Bad += "$t carries no title label - the hairline has nothing to measure from (SPEC V374e)"; continue }
    $tTop = 0; $tHi = 0
    if (-not ([int]::TryParse($ttl374.GetAttribute("top"), [ref]$tTop) -and
              [int]::TryParse($ttl374.GetAttribute("height"), [ref]$tHi))) {
        $v374Bad += "$t's title declares no top/height - the hairline is unmeasurable (SPEC V374e)"; continue
    }
    $hair = $tTop + $tHi + 1
    $rowTops = @()
    foreach ($row in $bx374.SelectNodes("layout")) {
        $rt = 0
        if ([int]::TryParse($row.GetAttribute("top"), [ref]$rt)) { $rowTops += $rt }
    }
    if ($rowTops.Count -eq 0) { $v374Bad += "$t declares no positioned row - where its body opens went unmeasured (SPEC V374e)"; continue }
    $firstTop = @($rowTops | Sort-Object)[0]
    if ($firstTop -ne $hair) { $v374Bad += "$t opens its first row at top=$firstTop, not the ${hair}px hairline its own title sets ($tTop + $tHi + 1) - a titled box opens on the hairline (SPEC V374a, I125c)" }
    else { $v374Ok++ }
}
if ($v374Ok -eq 0 -and $v374Bad.Count -eq 0) { $v374Bad += "not one box was measured - the check is a no-op (SPEC V20, V374e)" }

if ($v374Bad.Count -gt 0) { foreach ($b374 in ($v374Bad | Sort-Object -Unique)) { Fail "V374 $b374" } }
else { Pass "V374 all $v374Ok titled box(es) of the Main tab open their first row on the title hairline" }

# ---- V170: the width the columns gained went to the NAMES ----------------------------
# The whole point of the request was the ability names, so the 10px each column picked up
# has to be readable as label, not as air between the last dot and the border. Ability and
# CustomAbility are measured together: they are siblings in every column (SPEC V26).
$tplA = @($mainDoc.SelectNodes("//template[@name='Ability']"))[0]
$tplC = @($mainDoc.SelectNodes("//template[@name='CustomAbility']"))[0]
$rowW = @()
foreach ($col in $looseCols) {
    foreach ($r in $col.SelectNodes("layout[Ability or CustomAbility]")) { $rowW += [int]$r.GetAttribute("width") }
}
$rowW = @($rowW | Sort-Object -Unique)
if (-not $abilRowOk) { Fail "V170 not measured - the ability row did not pass V375" }
elseif (-not ($tplA -and $tplC)) { Fail "V170 Ability or CustomAbility is not declared on WoD20.1" }
elseif ($rowW.Count -ne 1) { Fail "V170 the ability rows are $($rowW -join '/') wide - one column would hold a different name width" }
else {
    $aInput = [int](@($tplA.SelectNodes("label"))[0].GetAttribute("width"))
    $cInput = [int](@($tplC.SelectNodes("edit"))[0].GetAttribute("width"))
    $aDots = @($tplA.SelectNodes("imageCheckBox") | ForEach-Object { [int]$_.GetAttribute("left") } | Sort-Object)
    $cDots = @($tplC.SelectNodes("imageCheckBox") | ForEach-Object { [int]$_.GetAttribute("left") } | Sort-Object)
    # The row spans what the 20px margin I73 gives every box leaves - it read colW - 30 while
    # the inset was 15 (103rd round, SPEC T618). The leg itself is unchanged: the width a column
    # gains has to arrive as NAME, and a row that stands still while its box grows turns it into
    # air between the last dot and the border, which is what V170 was written to stop.
    $colInner = $colW[0] - 40
    if ($aDots[0] -ne $cDots[0]) { Fail "V170 Ability puts its first dot at $($aDots[0]) and CustomAbility at $($cDots[0]) - they are siblings in every column (SPEC V26)" }
    elseif ($rowW[0] -ne $colInner) { Fail "V170 the rows are $($rowW[0]) wide inside a $($colW[0])px column - $($colW[0] - $rowW[0])px of the column reaches no row" }
    elseif (($aDots[-1] + 25) -ne $rowW[0]) { Fail "V170 the last dot ends at $($aDots[-1] + 25) in a row $($rowW[0]) wide - the leftover is dead margin, not name" }
    elseif ($aInput -gt $aDots[0] -or $cInput -gt $cDots[0]) { Fail "V170 the name field runs under the first dot ($aInput / $cInput against $($aDots[0]))" }
    elseif (($aDots[0] - $aInput) -gt 3) { Fail "V170 $($aDots[0] - $aInput)px sit between the ability label and its first dot - that width was asked for by the names" }
    else { Pass "V170 the $($colW[0])px column hands ${aInput}px to the name and closes on its last dot" }
}

# ---- V6 + V7: real build, and proof the artifact actually changed -------------
# B.1: `rdk p` is PREPARE, not pack. It exits 0 without touching the .rpk.
# Exit 0 alone is not proof of a build - the artifact must change.

# =====================================================================================
# SPEC T448: the checks the Vampire tab was owed. V172-V189, minus the ones that already
# shipped with the structure they measure (V175/V179 in T457, V188/V192 in T463, V193 in
# T466, V189/V198 in T478-T481). Every leg below was mutated red before it was accepted
# (SPEC V20) - a check that cannot fail is worse than no check, because it reads as cover.
# =====================================================================================

function ItemsOf($xml, $tplName) {
    $tpl = @($xml.SelectNodes("//template[@name='$tplName']"))[0]
    if ($null -eq $tpl) { return $null }
    # The template still says WHICH rows it draws; the list those rows offer now comes from
    # PICKER_LIST (SPEC I27). Reading items= here would return nothing and V177/V178/V184
    # would all go quiet together - three checks, one silent no-op (SPEC V20, V209l).
    #
    # T872 took the comboBox out of the four Vampire row templates and the same three checks
    # went quiet for the same reason one wave later - the widget changed, the list did not.
    # A converted row carries the I107a1 pair instead, so the picker is the button that opens
    # the box, and PickerKeyOf resolves its root off the TEMPLATE name exactly as it did off
    # the combo (SPEC V178 amended, T872). Falling back and not replacing: WoD20.7 and the
    # rest still author combos, and a reader that knew only one of the two worlds would be the
    # next silent no-op.
    $cb = @($tpl.SelectNodes(".//comboBox"))[0]
    if ($null -eq $cb) { $cb = @($tpl.SelectNodes(".//button[contains(@onClick,'mfOpen(')]"))[0] }
    if ($null -eq $cb) { return $null }
    @((ListOf $cb $tplName) | Where-Object { $_ -ne '' })
}

# ---- V172: the rename was text, and only text ----------------------------------------
# 51st round: the managed tab stopped reading "Disciplines" and started reading "Vampire".
# What must NOT have moved with it: the field (`stShowDisciplines`), the control name
# (`tabDisciplines`) and the ledger category (`Discipline`) - those are saved data and 4.9
# is out (SPEC V2). Scoped to the MANAGED tab on purpose: WoD20.11's sub-tab is still titled
# "Disciplines" and SHOULD be (SPEC V188), so a check that banned the string everywhere
# would be born wrong - the same trap SPEC C spells out for `path`.
$rootXdoc = Doc (Join-Path $dir "WoD20th.lfm")
$managedTab = $rootXdoc.SelectSingleNode("//layout[@name='tabDisciplines']")
if ($null -eq $managedTab) { Fail "V172 the root form has no pane named tabDisciplines - the control name was renamed with the title (SPEC V2)" }
# The visible title moved out of @title and into the button's label (SPEC I32) - same string,
# same rule, one control further out.
$managedLbl = $rootXdoc.SelectSingleNode("//rectangle[@name='btnTabDisciplines']/label")
if ($null -eq $managedLbl) { Fail "V172 btnTabDisciplines carries no label - nothing shows the tab's title" }
elseif ($managedLbl.GetAttribute("text") -ne 'Ghoul') { Fail "V172 the managed tab reads '$($managedLbl.GetAttribute("text"))', expected 'Ghoul' (SPEC V171, V233)" }
elseif ($rootTxt -notmatch 'sheet\.stShowDisciplines') { Fail "V172 stShowDisciplines is no longer read - the field was renamed with the tab (SPEC V2)" }
elseif ($rootTxt -match '(?m)kind\s*==\s*"Ghoul"') { Fail "V172 'Ghoul' is a ledger category - the rename reached the experience log (SPEC I10)" }
elseif ($rootTxt -notmatch '(?m)kind\s*==\s*"Discipline"') { Fail "V172 the ledger no longer prices 'Discipline' - the category was renamed with the tab" }
else { Pass "V172 the rename is title and visible text; the field, the control name and the ledger category all kept their old spelling" }

# ---- V173/V174: `clan` is authored asleep, and CLANS is declared on the root form -----
# A combo that is invisible but ENABLED, or a table with one live caller, is a feature that
# shipped by accident. Both halves are measured because either one alone would let it in.
$clanCbo = $null
foreach ($f in $files) { $c = (Doc $f.FullName).SelectSingleNode("//comboBox[@field='clan']"); if ($null -ne $c) { $clanCbo = $c; $clanFile = $f.Name } }
if ($null -eq $clanCbo) { Fail "V173 no combo carries field='clan' - the dormant picker is gone (SPEC V8)" }
elseif ($clanCbo.GetAttribute("visible") -ne 'false') { Fail "V173 the clan combo in $clanFile is visible - waking it is a round of its own, not a side effect" }
elseif ($clanCbo.GetAttribute("enabled") -ne 'false') { Fail "V173 the clan combo in $clanFile is enabled - invisible and live is the worst of both (SPEC V111)" }
else {
    $clanWrites = @()
    foreach ($f in $files) {
        $t = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName))
        foreach ($m in [regex]::Matches($t, 'setField\(\s*"clan"|sheet\.clan\s*=')) { $clanWrites += "$($f.Name): $($m.Value)" }
    }
    if ($clanWrites) { foreach ($c in $clanWrites) { Fail "V173 something writes the clan field - $c - the field is dormant, nothing may fill it" } }
    else { Pass "V173 the clan combo is authored invisible AND disabled, and no Lua writes the field" }
}
$clansReaders = @()
foreach ($f in $files) {
    $t = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName))
    foreach ($m in [regex]::Matches((NoComments $t), 'CLANS\s*\[')) { $clansReaders += "$($f.Name)" }
}
# The no-reader leg is REVOKED (SPEC V174, user 2026-08-22): the user asked the domitor's
# clan to fill the ghoul's Discipline slots, so CLANS has a reader ON PURPOSE now
# (renderClanDisc, SPEC I37, T547). What guarded the research by keeping it unused is V236,
# which measures the table itself. What stays here is WHERE it lives: one table on the root
# form, which is what lets V236 and renderClanDisc read the same sixty entries.
if ($rootTxt -notmatch '(?m)^\s*local CLANS\s*=\s*\{') { Fail "V174 CLANS is not declared on the root form - the check reads nothing (SPEC V20)" }
elseif ($clansReaders.Count -lt 1) { Fail "V174 nothing reads CLANS - the clan would no longer fill the Discipline slots (SPEC I37, T547)" }
else { Pass "V174 CLANS is declared on the root form and read by $(($clansReaders | Sort-Object -Unique) -join ', ')" }

# ---- V176: the selector roster is CLOSED --------------------------------------------
# Same shape V30 measures on the Numina tab, now that the Vampire tab has three of them.
# The roster is five since the 90th round and no longer one per DESCRIPTION block: the
# Numina tab split its single block into three and grew a second selector with it, while
# the True Faith block has a block and NO group - one Path, nothing to select (SPEC I61).
# The rows all share one field - that is how a radio group binds here (SPEC V1) - so the
# option is the fieldValue, and two rows answering to one value would light together.
# RETIRED as a per-file roster in the 159th round (SPEC V176, T872). Wave 3 of I113e turned
# DiscRow, MainPathRow, SecPathRow and RitualRow into pickers, and the ? took the 0..20 the
# radioButton was holding (user 2026-08-31, ask B). The three Vampire selectors - discSel,
# pathSel, ritualSel - went out with the DESCRIPTION panes that were the only thing reading
# them, so the per-file half of this check has no subject at all. Kept as an empty loop it
# would report PASS over nothing, which is B7 with its own check id.
#
# What does NOT retire is the CLOSED roster: a groupName nobody declared is a selector nobody
# guards, and that half still has three subjects on the Numina tab. The zero-guard is what
# keeps the survivor honest - reading fewer than three means a selector vanished and the
# closed list quietly stopped closing anything (SPEC V209).
#
# RETIRED WHOLE in T874: wave 4 took the last three selectors with the last four panes, so the
# roster is EMPTY and there is no survivor left to keep honest. A closed list over an empty set
# closes nothing, which is why the zero-guard below became the whole check - it is the same shape
# V193 kept when its subject went, and it fails in the one direction that is still meaningful.
$KNOWN_GROUPS = @()
$strayGroups = @()
foreach ($f in $files) {
    foreach ($g in (Doc $f.FullName).SelectNodes("//*[@groupName]")) {
        $strayGroups += "$($f.Name): '$($g.GetAttribute('groupName'))'"
    }
}
if ($strayGroups) { Fail "V176 a radio group exists again - $(($strayGroups | Sort-Object -Unique) -join '; ') - waves 3 and 4 took every selector on the sheet with the panes that read them, and a group that comes back comes back with nothing reading it and no ruler on it (SPEC I58, V30 retired, V365d, T872, T874)" }
else { Pass "V176 retired with the last three selectors in T874 - the sheet carries no groupName at all, and the ? has taken the 0..20 every radio used to hold" }

# ---- V177/V178/V184: the picker lists, the tables behind them, and the level prefix ----
# V177 is V32 for the Vampire tab: every item a picker offers must open text in BOTH
# languages, or its radio opens an empty box. V178 measures the OTHER direction plus the
# sibling rule: the two path templates offer the same list, and a key may not be written
# twice in one module. V184 measures the ritual prefix the level is read off (SPEC I19).
$areaSpec = @(
    @{ File = 'WoD20.11.lfm'; Base = 'descDisc';   Marker = 'DISC_DESC';   Templates = @('DiscRow');                   Name = 'discipline' },
    @{ File = 'WoD20.11.lfm'; Base = 'descPath';   Marker = 'PATH_DESC';   Templates = @('MainPathRow','SecPathRow');  Name = 'path' },
    @{ File = 'WoD20.11.lfm'; Base = 'descRitual'; Marker = 'RITUAL_DESC'; Templates = @('RitualRow');                 Name = 'ritual' })
$areaBad = @(); $areaOk = @()
foreach ($a in $areaSpec) {
    $x = Doc (Join-Path $dir $a.File)
    $lists = @{}
    foreach ($t in $a.Templates) {
        $it = ItemsOf $x $t
        if ($null -eq $it) { $areaBad += "V178 $($a.File) has no <template name='$t'> holding a picker that resolves to a list - neither a comboBox nor an mfOpen button, so the rows have nothing to offer (SPEC V178 amended, T872)"; continue }
        $lists[$t] = $it
    }
    if ($lists.Count -ne $a.Templates.Count) { continue }
    # V178: sibling boxes read ONE list. Declared twice is allowed; DIVERGING is not.
    $first = $a.Templates[0]
    foreach ($t in $a.Templates) {
        if (($lists[$t] -join "$([char]1)") -ne ($lists[$first] -join "$([char]1)")) {
            $only = @(@($lists[$t] | Where-Object { $lists[$first] -notcontains $_ }) + @($lists[$first] | Where-Object { $lists[$t] -notcontains $_ }))
            $areaBad += "V178 $($a.File) $t and $first offer different $($a.Name) lists - $(($only | Select-Object -First 4) -join ', ') is in one box and not its sibling"
        }
    }
    $items = @($lists[$first])
    $dupItems = @($items | Group-Object | Where-Object { $_.Count -gt 1 })
    foreach ($d in $dupItems) { $areaBad += "V178 $($a.File) offers '$($d.Name)' $($d.Count) times - one item, one line (SPEC V14)" }
    # V184: the level is READ OFF the ritual name, so the name has to carry it.
    if ($a.Name -eq 'ritual') {
        $noPrefix = @($items | Where-Object { $_ -notmatch '^[1-9]\. ' })
        foreach ($n in $noPrefix) { $areaBad += "V184 ritual '$n' carries no '<level>. ' prefix - the guard reads the level off the name and would price it at zero (SPEC I19)" }
        if ($rootTxt -notmatch '\^\(%d\+\)%\. ') { $areaBad += "V184 the root form no longer reads the level off the ritual name - a second table for the level is exactly what this invariant forbids" }
    }
    # V177: every item has text on file, in both languages - which since T495 means in both
    # HALVES, joined by key (SPEC V210). An item written into only one of them opens an empty
    # box in the other language, which is the same failure read one language later.
    $modEn = DescEntries (Join-Path $plugin "$($a.Base)_en.lua") $a.Marker 'en'
    $modPt = DescEntries (Join-Path $plugin "$($a.Base)_pt.lua") $a.Marker 'pt'
    if ($null -eq $modEn -or $null -eq $modPt) { $areaBad += "V177 $($a.Base)_en.lua or $($a.Base)_pt.lua is missing, or its $($a.Marker) markers are gone - every $($a.Name) item would open an empty box (SPEC V210, V209)"; continue }
    $keys = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
    $thin2 = @()
    $ptBody2 = DescMap $modPt
    foreach ($e in $modEn) {
        [void]$keys.Add($e.Key)
        if ($e.Body.Trim().Length -lt 1) { $thin2 += "V177 $($a.Base)_en.lua '$($e.Key)' has an empty [en] body" }
        if (-not $ptBody2.ContainsKey($e.Key)) { $thin2 += "V177 $($a.Base)_pt.lua has no '$($e.Key)' - the item would open an empty box in Portuguese" }
        elseif ($ptBody2[$e.Key].Trim().Length -lt 1) { $thin2 += "V177 $($a.Base)_pt.lua '$($e.Key)' has an empty [pt] body" }
    }
    foreach ($t in $thin2) { $areaBad += $t }
    # V178, table half: a book that repeats an item must not become two entries. Both halves,
    # because a duplicate written into one of them alone is the same unreachable second copy.
    $allKeys = @($modEn | ForEach-Object { $_.Key })
    foreach ($side in @(@('en', $modEn), @('pt', $modPt))) {
        foreach ($d in @(@($side[1] | ForEach-Object { $_.Key }) | Group-Object | Where-Object { $_.Count -gt 1 })) {
            $areaBad += "V178 $($a.Base)_$($side[0]).lua declares '$($d.Name)' $($d.Count) times - the second copy is unreachable and free to drift"
        }
    }
    if ($keys.Count -eq 0) { $areaBad += "V177 $($a.Base)_en.lua parsed to zero entries - the parser or the generated shape drifted"; continue }
    $miss = @($items | Where-Object { -not $keys.Contains($_) })
    foreach ($m in $miss) { $areaBad += "V177 the $($a.Name) picker offers '$m' and $($a.Base)_en.lua has no entry - its radio would open an empty block" }
    $orph = @($allKeys | Where-Object { $items -notcontains $_ })
    foreach ($o in ($orph | Sort-Object -Unique)) { $areaBad += "V177 $($a.Base)_en.lua carries '$o' and no picker offers it - dead text or a spelling drift" }
    if (-not ($areaBad | Where-Object { $_ -like "*$($a.Base)*" -or $_ -like "*$($a.File)*" })) {
        $areaOk += "$($items.Count) $($a.Name) items, all with en+pt text on file"
    }
}
if ($areaBad) { foreach ($b in $areaBad) { Fail $b } }
else { Pass "V177/V178/V184 $($areaOk -join '; ')" }

# ---- V180: the main path level is worked out, never saved -----------------------------
# Two numbers for one fact is how B12 and B16 both started. The dots of a main path row are
# painted from the Discipline at every render; nothing writes them, and an empty row answers
# zero rather than inheriting the row above it.
$pathLevelFn = LuaFn $rootTxt 'pathLevel'
$mainRenderFn = LuaFn $rootTxt 'renderMainPaths'
if (-not $pathLevelFn) { Fail "V180 pathLevel not found on the root form - the level would have to come from somewhere else" }
elseif ($pathLevelFn -notmatch 'return 0;') { Fail "V180 pathLevel has no zero answer - an unclaimed or empty row would inherit a level (SPEC V180)" }
elseif (-not $mainRenderFn) { Fail "V180 renderMainPaths not found - nothing paints the read-only dots" }
elseif ($mainRenderFn -match 'setField\(') { Fail "V180 renderMainPaths writes to the sheet - the painted level would become a second saved number free to disagree with the Discipline" }
elseif ($mainRenderFn -notmatch 'pathLevel\(') { Fail "V180 renderMainPaths does not read pathLevel - it is painting from something else" }
elseif ($mainRenderFn -notmatch '\.checked\s*=') { Fail "V180 renderMainPaths sets no dot - the row would never light" }
else { Pass "V180 the main path dots are painted from the Discipline at every render and nothing is written back" }

# ---- V181/V182/V185/V186: the picker guard, and the door it comes through --------------
# The refusal is REVERTED through a dataLink, never from the combo's own onChange: a write
# made inside a control's dispatch does not survive it, which is how B36 and B38 both got
# their refusals ignored. Each rule speaks its own reason (SPEC V129).
$refusalFn2 = LuaFn $rootTxt 'pickRefusal'
$guardFn2   = LuaFn $rootTxt 'guardPick'
$guardBad = @()
if (-not $refusalFn2) { $guardBad += "pickRefusal not found on the root form" }
else {
    $r = NoComments $refusalFn2
    if ($r -notmatch 'value == ""') { $guardBad += "V15 pickRefusal does not let the empty item through - the revert below it would never terminate" }
    if ($r -notmatch 'pathLevel\(value, levels\) < 1') { $guardBad += "V182 a path whose blood sorcery is not on the sheet is accepted" }
    if ($r -notmatch 'sharesDisc\(') { $guardBad += "V181 nothing compares the owners of two main paths - a blood sorcery could take two" }
    if ($r -notmatch 'MAIN_PATH_ROWS') { $guardBad += "V181 the main path rule looks at no other row - it cannot see the duplicate" }
    if ($r -notmatch 'best < 1 then return') { $guardBad += "V185 a ritual whose blood sorcery is absent is accepted" }
    if ($r -notmatch 'best < need then return') { $guardBad += "V185 a ritual above what its blood sorcery allows is accepted" }
    $reasons = @([regex]::Matches($r, 'return "([^"]*)"') | ForEach-Object { $_.Groups[1].Value } | Sort-Object -Unique)
    if ($reasons.Count -lt 4) { $guardBad += "V129 pickRefusal gives $($reasons.Count) distinct reasons - the four rules it enforces are four different problems with four different fixes" }
}
if (-not $guardFn2) { $guardBad += "guardPick not found on the root form" }
else {
    $g2 = NoComments $guardFn2
    if ($g2 -notmatch 'pickRefusal\(field, newValue, levels\)') { $guardBad += "V186 guardPick does not ask pickRefusal about the new value" }
    if ($g2 -notmatch 'pickRefusal\(field, oldValue, levels\)') { $guardBad += "V186 guardPick reverts to oldValue without testing it - two illegal values would bounce forever" }
    if ($g2 -notmatch 'setField\(field, back\)') { $guardBad += "V186 guardPick does not write the revert" }
    if ($g2 -notmatch 'xpWarn\(why\)') { $guardBad += "V186 the refusal is silent - the combo would snap back with no reason given (SPEC V129)" }
}
$onChangeGuards = @()
foreach ($f in $files) {
    foreach ($cb in (Doc $f.FullName).SelectNodes("//comboBox[@onChange]")) {
        if ($cb.GetAttribute("onChange") -match 'guardPick\(') { $onChangeGuards += "$($f.Name): $($cb.GetAttribute("name"))" }
    }
}
if ($onChangeGuards) { $guardBad += "V186 guardPick is called from a comboBox onChange - $(($onChangeGuards) -join '; ') - the revert would not survive the dispatch (SPEC B36, B38)" }
$guardLinks = @()
foreach ($f in $files) {
    $t = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName))
    foreach ($m in [regex]::Matches($t, '<dataLink[^>]*guardPick\(')) { $guardLinks += $f.Name }
}
if ($guardLinks.Count -eq 0) { $guardBad += "V186 no dataLink calls guardPick - nothing enforces the picker rules at all" }
if ($guardBad) { foreach ($b in $guardBad) { Fail "V181/V182/V185/V186 $b" } }
else { Pass "V181/V182/V185/V186 the four picker rules each speak their own reason, and the revert comes through $(($guardLinks | Sort-Object -Unique).Count) dataLink(s), never a combo's onChange" }

# ---- V183/V187: the ceiling is measured on the click, never swept at load --------------
# V183: a secondary path is capped at its blood sorcery minus one, tested INSIDE xpClick
# before anything is written (SPEC V135). V187: lowering a Discipline does NOT erase the
# paths and rituals bought under it - one click may not destroy what the player built, and
# a load-time sweep is exactly the shape that reopened V121.
$clickFn2 = LuaFn $rootTxt 'xpClick'
$sweepBad = @()
if (-not $clickFn2) { $sweepBad += "V183 xpClick not found on the root form" }
else {
    $c2 = NoComments $clickFn2
    if ($c2 -notmatch 'secPath_') { $sweepBad += "V183 xpClick does not special-case a secondary path row - the cap is not enforced where the dot is bought" }
    if ($c2 -notmatch 'markDot\(') { $sweepBad += "V183 xpClick never marks the dot - the check reads nothing (SPEC V20)" }
    $capIdx  = $c2.IndexOf('secPath_')
    $markIdx = $c2.IndexOf('markDot(')
    if ($capIdx -ge 0 -and $markIdx -ge 0 -and $capIdx -gt $markIdx) { $sweepBad += "V183/V135 the secondary path cap is tested AFTER the dot is written - a refused purchase would leave the dot behind" }
}
# The erasing sweep: a loop that clears a path or ritual field outside a click handler.
foreach ($fn in @('sheetReveal', 'applyTheme', 'applyLanguage', 'renderMainPaths', 'renderVampire')) {
    $body = LuaFn $rootTxt $fn
    if (-not $body) { continue }
    foreach ($m in [regex]::Matches((NoComments $body), 'setField\("(?:secPath_|mainPath_|ritual_|disc_|clanDisc_)')) {
        $sweepBad += "V187 $fn writes $($m.Value)... - a sweep over the saved rows is how one click comes to destroy what the player built (SPEC V138, V157)"
    }
}
if ($sweepBad) { foreach ($s in $sweepBad) { Fail $s } }
else { Pass "V183/V187 the secondary path ceiling is tested on the click before anything is written, and no render or reveal clears a saved row" }

# ---- V199 - V205: the picker filter (SPEC I25) -----------------------------------------
# The rule the guard enforces AFTER a pick is now also what decides which items a picker
# OFFERS. One predicate serves both, so a list cannot hold something the guard would throw
# back - and a new rule lands in one place instead of two that drift.
$filterBad = @()

$allowFn   = LuaFn $rootTxt 'pickAllowed'
$levelsFn  = LuaFn $rootTxt 'discLevels'
$vampFn    = LuaFn $hh6 'renderVampPickers'
$pickerFn  = [regex]::Match($hh6, '(?ms)local function pickerItems\(c, lang, era, levels\)(.*?)\n\t\t\tend;')

# V199: one predicate, one body. pickAllowed is the only door the filter has to the rule,
# and it is pickRefusal phrased as a yes - nothing more.
if (-not $allowFn) { $filterBad += "V199 pickAllowed is gone from the root form - the filter has no way to ask what is legal" }
elseif ((NoComments $allowFn) -notmatch 'pickRefusal\(field, value, levels\) == nil') {
    $filterBad += "V199 pickAllowed does not answer from pickRefusal - a second copy of the rule is how the list and the guard start disagreeing (SPEC I25)"
}
# The knowledge must not leak into WoD20.6: a table read there is a second test by definition.
foreach ($tbl in @('RITUAL_DISC', 'PATH_DISC', 'BLOOD_SORCERY')) {
    if ((NoComments $hh6) -match $tbl) {
        $filterBad += "V199 WoD20.6 reads $tbl - the list is deciding legality for itself instead of asking pickAllowed (SPEC I25)"
    }
}

if (-not $pickerFn.Success) {
    $filterBad += "V199 pickerItems is gone from WoD20.6, or no longer takes the level map - nothing filters a picker (SPEC I25, V205)"
} else {
    $pk = NoComments $pickerFn.Groups[1].Value

    # The FOURTH argument is the hedge path map and it is named here on purpose (SPEC I83f,
    # V310e): dropping it costs nothing a reader would notice - the ritual filter simply stops
    # pruning, rdk exits 0 and every other check stays green.
    if ($pk -notmatch 'pickAllowed\(field, raw, levels, memo\.hedge\)') {
        $filterBad += "V199 pickerItems does not ask pickAllowed with the level map AND the hedge path map - whatever drops an item now, it is not the one rule (SPEC V199, I83f)"
    }

    # V200: the row's own value survives its own list however illegal it went. A comboBox
    # whose value is not among its values renders BLANK, so dropping it would make lowering
    # a Discipline look like the sheet had thrown the row away (SPEC V187).
    if ($pk -notmatch 'local current\s*=' -or $pk -notmatch 'sheet\[field\]') {
        $filterBad += "V200 pickerItems never reads what the row is holding - the value would leave its own list and the combo would go blank (SPEC V187)"
    }
    if ($pk -notmatch 'raw ~= current') {
        $filterBad += "V200 the filter does not spare the row's own value - lowering a Discipline would blank the rows the player already filled"
    }
    if ($pk -notmatch 'raw ~= ""') {
        $filterBad += "V15/V200 the filter does not spare the empty item - a row could not be cleared once filled"
    }

    # V201, rewritten in the 68th round with SPEC I27. The source is the AUTHORED list and it
    # is now a CONSTANT: PICKER_LIST on the root form, which nothing writes. It used to be a
    # per-handle snapshot of c.values, and BOTH branches of that ended at the XML attribute -
    # so the moment T493 took the attribute away the snapshot would have caught an EMPTY list,
    # once and for ever, and every dropdown on the sheet would have come up blank with rdk
    # exiting 0 and this gate green. A constant cannot go stale the way a snapshot can.
    if ($pk -notmatch 'PICKER_LIST\[fieldRoot\(nm\)\]') {
        $filterBad += "V201 pickerItems does not start from PICKER_LIST - reading c.values back filters an already filtered list, which shrinks every render and never grows again when the Discipline goes up (SPEC B8, I27)"
    }
    if ((NoComments $hh6) -match 'AUTHORED_VALUES') {
        $filterBad += "V201 AUTHORED_VALUES is back - the snapshot it takes is of c.values, which T493 emptied; the authored list is PICKER_LIST now (SPEC I27)"
    }
    # Second leg (SPEC V201, 68th): the authored table is CONSTANT. One table now stands
    # behind twenty-five ritual combos, so a single table.remove would poison all of them at
    # once and there is no authored copy left anywhere to recover from.
    foreach ($mut in @('table\.insert\s*\(\s*PICKER_LIST', 'table\.remove\s*\(\s*PICKER_LIST', 'table\.sort\s*\(\s*PICKER_LIST')) {
        foreach ($f in $files) {
            $t = NoComments ([System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName)))
            if ($t -match $mut) { $filterBad += "V201 $($f.Name) mutates PICKER_LIST in place - it is the authored constant twenty-five combos share (SPEC V201, I27)" }
        }
    }
    $plWrites = [regex]::Matches((NoComments $rootLfmTxt), '(?m)^\s*PICKER_LIST\["(\w+)"\]\s*=\s*(.+)$')
    foreach ($w in $plWrites) {
        if ($w.Groups[2].Value -match '^PICKER_LIST\["\w+"\];') { continue }
        # One write is not an alias and is still legal: the clan+family union, assembled into
        # a LOCAL inside its marked region and handed over whole (SPEC I29b, V294, V433b). It is
        # as constant as the two lists behind it - born in the constructor, never written again -
        # and it is accepted only from INSIDE the markers, so a stray assignment anywhere else
        # still fails right here.
        if ($w.Groups[1].Value -eq 'clanFamily' -and $UNION_REGION.Success -and
            $UNION_REGION.Groups[1].Value -match [regex]::Escape($w.Value.Trim())) { continue }
        $filterBad += "V201 PICKER_LIST[$($w.Groups[1].Value)] is assigned something that is not an alias - the map is authored once and never written (SPEC V201)"
    }

    # V202: items and values are written as one aligned pair, item N showing value N. The
    # filter only ever REMOVES a row of the pair - it never rewrites a value (SPEC V24).
    if ($pk -notmatch 'kept\[#kept \+ 1\]' -or $pk -notmatch 'shown\[#shown \+ 1\]') {
        $filterBad += "V202 the two lists are not appended together - one can fall out of step with the other and item N stops meaning value N"
    }
    # Unconditional, not `if filter then`: that guard was right only while the XML authored
    # values=. The eight unfiltered pickers took their values from the attribute and needed
    # items alone; with the list coming from PICKER_LIST a guarded write leaves them showing
    # every entry and STORING none (SPEC V202, I27).
    # The ONLY guard allowed on these two writes is SAME-CONTENT (sameList). It is a no-op by
    # definition - the control ends up holding the same pair either way - and it is what keeps a
    # walk that changed nothing from making the host rebuild the closed display and drop the text
    # anchor the XML authored (SPEC B153, V419, V122). Any OTHER condition is the `if filter then`
    # shape again, which left the unfiltered pickers showing every entry and STORING none.
    if ($pk -notmatch '(?m)^\s*(if not sameList\(c\.values, kept\) then )?c\.values = kept;') {
        $filterBad += "V202 pickerItems does not write values - an unfiltered picker would show every entry and store none (SPEC I27)"
    }
    $pairs202 = @(
        @('c.values = kept;', 'if not sameList(c.values, kept) then c.values = kept; end;'),
        @('c.items = shown;', 'if not sameList(c.items, shown) then c.items = shown; end;')
    )
    foreach ($w202 in $pairs202) {
        foreach ($m202 in [regex]::Matches($pk, ('(?m)^[^\r\n]*' + [regex]::Escape($w202[0]) + '[^\r\n]*\r?$'))) {
            $ln202 = $m202.Value.Trim()
            if ($ln202 -ne $w202[0] -and $ln202 -ne $w202[1]) {
                $filterBad += "V202 the write carries a guard that is not same-content: '$ln202' - only sameList is allowed, because it alone cannot change what the control ends up holding (SPEC V202, B153)"
            }
        }
    }
    if ($pk -match 'if filter then c\.values = kept; end;') {
        $filterBad += "V202 the values write is still gated on `filter` - correct only while the XML authored values=, which T493 removed (SPEC I27)"
    }
    if ($pk -notmatch 'c\.items = shown;') {
        $filterBad += "V202 pickerItems no longer writes items - nothing reaches the dropdown"
    }

    # V203: the filter scope is its own table and narrower than the era's. A Discipline is
    # what the player BUYS: filtering its list by what he owns leaves a new sheet nineteen
    # empty combos. Fourth time this rule is asked for - B15, T472, B44, and now this.
    if ($pk -notmatch 'isFilterRow\(nm\)') {
        $filterBad += "V203 pickerItems does not ask whether the row is a FILTERED one - the scope would be the era's five roots (SPEC V197)"
    }
}

$filterTbl = [regex]::Match($hh6, '(?ms)local FILTER_ROW = \{(.*?)\}')
if (-not $filterTbl.Success) {
    $filterBad += "V203 FILTER_ROW is not declared - the filter has no scope of its own and would reuse the era's (SPEC I25)"
} else {
    # `hedgeRitual` joined the three in the 112th round (SPEC I83g, V310f). It belongs HERE and
    # not in FILTER_NAME: all twelve picked ritual rows answer to one question, unlike the numina
    # box where only the first row is the Affinity Path. The list stays closed either way - the
    # extra-name arm below is what stops a Discipline picker being filtered by what the player
    # already owns, which would leave a new sheet with nineteen empty combos.
    $wantFilter = @('mainPath', 'secPath', 'ritual', 'hedgeRitual')
    $gotFilter  = @([regex]::Matches($filterTbl.Groups[1].Value, '(\w+)\s*=\s*true') | ForEach-Object { $_.Groups[1].Value })
    $missF = @($wantFilter | Where-Object { $gotFilter -notcontains $_ })
    $extraF = @($gotFilter | Where-Object { $wantFilter -notcontains $_ })
    if ($missF) { $filterBad += "V203 FILTER_ROW is missing $($missF -join ', ') - those rows would offer everything the book has (SPEC V182, V185)" }
    if ($extraF) { $filterBad += "V203 FILTER_ROW carries $($extraF -join ', ') - a Discipline picker filtered by what the player already owns leaves a new sheet empty (SPEC C)" }
}

# V204: a row count constant that does not match the XML is a loop that stops early in
# silence. RITUAL_ROWS said 20 while WoD20.14 drew 25 for three rounds, and only survived
# because nothing read it (SPEC B46).
#
# V248(a) rides along since the 85th round: three of these boxes end in rows the storyteller
# TYPES, and those rows have a count of their own. The total constant covers BOTH kinds - a
# loop over DISC_ROWS reaches the typed rows too, because a typed discipline is still a
# discipline with dots - and the typed ones must be the LAST rows of the box, which is what
# lets every reader say "picker = total - typed" instead of carrying a list of indices.
$rowSpec = @(
    @{ Const = 'CLAN_DISC_ROWS'; File = 'WoD20.11.lfm'; Row = 'DiscRow';     Field = 'clanDisc'; Free = '';                   FreeRow = '' },
    @{ Const = 'DISC_ROWS';      File = 'WoD20.11.lfm'; Row = 'DiscRow';     Field = 'disc';     Free = 'DISC_FREE_ROWS';     FreeRow = 'DiscFreeRow' },
    @{ Const = 'MAIN_PATH_ROWS'; File = 'WoD20.11.lfm'; Row = 'MainPathRow'; Field = 'mainPath'; Free = '';                   FreeRow = '' },
    @{ Const = 'SEC_PATH_ROWS';  File = 'WoD20.11.lfm'; Row = 'SecPathRow';  Field = 'secPath';  Free = 'SEC_PATH_FREE_ROWS'; FreeRow = 'SecPathFreeRow' },
    @{ Const = 'RITUAL_ROWS';    File = 'WoD20.11.lfm'; Row = 'RitualRow';   Field = 'ritual';   Free = 'RITUAL_FREE_ROWS';   FreeRow = 'RitualFreeRow' },
	@{ Const = 'SPECIALITY_ROWS'; File = 'WoD20.1.lfm';  Row = 'SpecialityRow'; Field = 'speciality'; Free = 'SPECIALITY_FREE_ROWS'; FreeRow = 'SpecialityFreeRow'; Attr = 'num' },
	@{ Const = 'BACKGROUND_ROWS'; File = 'WoD20.2.lfm';  Row = 'OpenAbility';   Field = 'background'; Free = 'BACKGROUND_FREE_ROWS'; FreeRow = 'OpenAbilityFreeRow' }
)
foreach ($rs in $rowSpec) {
    $m = [regex]::Match($rootTxt, "(?m)^\s*$($rs.Const)\s*=\s*(\d+);")
    if (-not $m.Success) { $filterBad += "V204 $($rs.Const) is not declared on the root form - the count would live in the XML alone"; continue }
    $declared = [int]$m.Groups[1].Value
    $txt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir $rs.File)))

    # Most boxes call their template with field="<root>_<n>"; SPECIALTIES calls it with num="<n>"
    # and builds the three field names inside the template (SPEC V147). Which attribute carries
    # the index is part of the row spec, so one census answers for both shapes.
    $keyRe = if ($rs.Attr -eq 'num') { "num=`"(\d+)`"" } else { "field=`"$($rs.Field)_(\d+)`"" }
    $pickIdx = @([regex]::Matches($txt, "<$($rs.Row)\s+$keyRe") | ForEach-Object { [int]$_.Groups[1].Value })
    $freeIdx = @()
    if ($rs.FreeRow) { $freeIdx = @([regex]::Matches($txt, "<$($rs.FreeRow)\s+$keyRe") | ForEach-Object { [int]$_.Groups[1].Value }) }

    # The 159th round (SPEC V204 amended, B116) replaced the COUNT with the SET, because a count
    # cannot see a swap inside itself: exchanging disc_15 for disc_99 left the whole gate at 0
    # FAIL - fifteen rows are still fifteen rows and the constant still says fifteen. A row
    # outside the range is a field no reader reaches: declareTrait and the ledger both walk
    # 1..CONST, so its dots cost no experience, never show in the log, and quietly keep whatever
    # the player writes.
    #
    # AMENDED for T880 (SPEC V204 amended again, V263a, I118f): the set is no longer required to
    # be DENSE. ritual is the first family to take a slot with a burned name in the middle of its
    # numbering - ritual_21..ritual_25 came off in the 85th round and can never come back (V251,
    # I46) - so its two new rows are _26 and _27 and RITUAL_ROWS is the HIGHEST INDEX, 27, not the
    # count, 22. The rewrite V263(a) already took for psychic, arriving here.
    #
    # The line between the two is one question and it is the whole of this leg: a hole is legal
    # only where every index in it is a NAMED exit in I3. Anything looser and B116 walks back in
    # through the gap ("it is just a vacancy"); anything tighter reddens correct code, which is
    # B106. So the constant must equal the top of the set, no index may sit outside or repeat,
    # and every missing one has to be a name somebody wrote down on purpose.
    $idxAll  = @(@($pickIdx) + @($freeIdx) | Sort-Object)
    $gapIdx  = @()
    if ($idxAll.Count -eq 0) { $filterBad += "V204 $($rs.File) draws no $($rs.Row) at all - $($rs.Const) says $declared and every leg below would measure an empty set (SPEC V20, V209, B7)"; continue }

    $maxIdx  = ($idxAll | Measure-Object -Maximum).Maximum
    $overIdx = @($idxAll | Where-Object { $_ -lt 1 -or $_ -gt $declared } | Sort-Object -Unique)
    $dupIdx  = @($idxAll | Group-Object | Where-Object { $_.Count -gt 1 } | ForEach-Object { $_.Name })
    $gapIdx  = @(1..$declared | Where-Object { $idxAll -notcontains $_ })
    # A gap is a VACANCY only if the field it would name is one of I3's two declared exits. The
    # orphan list is where ritual_21..ritual_25 have lived since the 85th round, so the hole this
    # round opens is already written down - and a gap that is NOT written down is B116's swap
    # wearing a vacancy's clothes.
    $unnamed = @($gapIdx | Where-Object { $I3_ORPHANS -notcontains "$($rs.Field)_$_" -and $I3_DELETED -notcontains "$($rs.Field)_$_" })

    if ($overIdx.Count -gt 0) { $filterBad += "V204 $($rs.File) draws $($rs.Field)_$($overIdx -join ", $($rs.Field)_") outside 1..$declared - no reader reaches it, so its dots cost no experience and never show in the log (SPEC V204 amended, B116)" }
    elseif ($dupIdx.Count -gt 0) { $filterBad += "V204 $($rs.File) draws $($rs.Field)_$($dupIdx -join ", $($rs.Field)_") twice - two rows on one field write over each other and the second is unreachable (SPEC V204 amended, V1, B116)" }
    elseif ($maxIdx -ne $declared) { $filterBad += "V204 $($rs.Const) says $declared and the highest $($rs.Field) row $($rs.File) draws is $($rs.Field)_$maxIdx - the constant IS the highest index, so every loop that reads it walks past the end of the box or stops short of it, in silence (SPEC B46, V204 amended, V263a)" }
    elseif ($unnamed.Count -gt 0) { $filterBad += "V204 $($rs.File) draws no $($rs.Field)_$($unnamed[0]) and '$($rs.Field)_$($unnamed[0])' is in neither list of I3 - a hole is a vacancy only where the name in it was retired on purpose; otherwise the count agrees with $($rs.Const) because another index took its place, which is the swap a size comparison cannot see (SPEC V204 amended, B116, V362b)" }

    # DECLARING the vacancy is half the job; the LOOPS have to skip it. sortRituals is what makes
    # this leg load-bearing rather than tidy: it reads sheet["ritual_"..i] AND writes
    # setField("ritual_"..i, ...) across its whole range, so an unguarded walk gathers the dead
    # values a pre-85th sheet still holds in the burned names, sorts them into the visible rows,
    # and writes fresh values back INTO the burned names. Silent in both directions (V251, V2, I46).
    #
    # The BOUND stays the highest index and is deliberately NOT cut down to the count: cutting it
    # would stop the loops at 22 and leave ritual_26 and ritual_27 with no painter at all, which is
    # B112 exactly. A count drops the vacancies, an index does not (SPEC V263b).
    if ($gapIdx.Count -gt 0 -and $unnamed.Count -eq 0) {
        $vacName = $rs.Const -replace '_ROWS$', '_VACANT'
        $vacDecl = [regex]::Match((NoComments $rootTxt), '(?m)^\s*' + $vacName + '\s*=\s*\{([^}]*)\}\s*;')
        if (-not $vacDecl.Success) {
            $filterBad += "V204 $($rs.File) leaves $($rs.Field)_$($gapIdx -join ", $($rs.Field)_") vacant and the root form declares no $vacName - the hole is in the XML and nowhere the Lua can read it, so every walk bounded by $($rs.Const) runs straight through it (SPEC V204 amended, V263a)"
        } else {
            $vacIdx = @([regex]::Matches($vacDecl.Groups[1].Value, '\[\s*(\d+)\s*\]') | ForEach-Object { [int]$_.Groups[1].Value } | Sort-Object)
            if (($vacIdx -join ',') -ne ($gapIdx -join ',')) {
                $filterBad += "V204 $vacName names index $(if ($vacIdx.Count) { $vacIdx -join ', ' } else { 'nothing' }) and $($rs.File) leaves $($gapIdx -join ', ') vacant - one set or the walks skip a row that IS drawn, or step into a name that is burned (SPEC V204 amended, V263a)"
            }
        }
        # Every loop in the drawing file that walks the family BY INDEX has to ask. Read off the
        # source and not counted: a number here would go green the day a loop is added.
        foreach ($lp204 in [regex]::Matches((NoComments $txt), '(?s)for\s+i\s*=\s*[^,]+,\s*[^,]+,\s*1\s+do(.*?)\bend;')) {
            $body204 = $lp204.Groups[1].Value
            if ($body204 -notmatch ('"(dyn)?' + $rs.Field + '_"\s*\.\.')) { continue }
            if ($body204 -notmatch ([regex]::Escape($vacName) + '\[')) {
                $filterBad += "V204 a loop in $($rs.File) walks $($rs.Field)_<i> over the whole of $($rs.Const) without asking $vacName - $($gapIdx.Count) index/indices in that range name fields that were retired, and a sheet saved before they went still carries their values (SPEC V204 amended, V251, V2)"
            }
        }
    }

    if (-not $rs.Free) {
        if ($freeIdx.Count -gt 0) { $filterBad += "V248 $($rs.File) draws $($freeIdx.Count) typed row(s) for $($rs.Field), a box SPEC I45 gives none - the clan and main path boxes are picked, both of them" }
        continue
    }

    $mf = [regex]::Match($rootTxt, "(?m)^\s*$($rs.Free)\s*=\s*(\d+);")
    if (-not $mf.Success) { $filterBad += "V248 $($rs.Free) is not declared on the root form - the typed rows would be counted in the XML alone (SPEC V204)"; continue }

    $declaredFree = [int]$mf.Groups[1].Value
    if ($declaredFree -ne $freeIdx.Count) {
        $filterBad += "V248 $($rs.Free) says $declaredFree and $($rs.File) draws $($freeIdx.Count) - every reader that says 'picker = total - typed' would be reading a picker row as typed, or the other way round"
    } elseif ($freeIdx.Count -gt 0 -and $pickIdx.Count -gt 0 -and ($freeIdx | Measure-Object -Minimum).Minimum -le ($pickIdx | Measure-Object -Maximum).Maximum) {
        $filterBad += "V248 $($rs.File) has a typed $($rs.Field) row above a picked one - typed rows are the LAST ones, which is the whole of what makes 'total - typed' true (SPEC I45)"
    }
}

# V205: the Disciplines are read ONCE per render and handed down. Asking per item is
# twenty-five combos x 284 items x nineteen rows x five dots on every Discipline dot click:
# the tab locks up and there is no error for anyone to see.
if (-not $levelsFn) { $filterBad += "V205 discLevels is gone from the root form - there is no map to hand down" }
elseif ((NoComments $levelsFn) -notmatch 'CLAN_DISC_ROWS' -or (NoComments $levelsFn) -notmatch 'DISC_ROWS') {
    $filterBad += "V205 discLevels does not walk both Discipline boxes - a Discipline in an open slot would count for nothing (SPEC V178)"
}
# Word-anchored since 2026-08-22 (SPEC B50, V222): -match is case-INSENSITIVE in PowerShell,
# so the bare 'discLevel\(' also matched maxDiscLevel( and reddened a check that is about
# something else entirely. The boundary keeps it on the function this invariant names.
if ($rootTxt -match '\bdiscLevel\(') {
    $filterBad += "V205 the per-name discLevel is back - two readings of the same rating is one more than this sheet has room for (SPEC V145)"
}
if ($refusalFn2 -and (NoComments $refusalFn2) -match 'discLevels\(') {
    $filterBad += "V205 pickRefusal builds the map itself - that is the per-item walk this invariant exists to stop"
}
if ($refusalFn2 -and (NoComments $refusalFn2) -notmatch 'levels\[') {
    $filterBad += "V205 pickRefusal does not read the map it is given - the level is coming from somewhere else (SPEC V199)"
}
# AMENDED in the 159th round (SPEC V205, T872, B114): the TRIGGER moved from renderVampPickers
# to mfOpen. Wave 3 took the filtered dropdown off main path, secondary path and ritual and the
# search box builds the list now - so the pool has to ask the same predicate the guard asks, or
# the box offers a ritual guardPick throws straight back on the click after. That is the list
# and the guard disagreeing, the single thing V199 exists to make impossible, and it shipped
# GREEN because both of the filter's rulers pointed at the side the subject had just left.
#
# Two things measured here and they are not the same thing: that the maps are read ONCE per
# opening (the cost leg V207 used to own), and that pickAllowed is asked INSIDE the pool loop
# with those maps handed down (the correctness leg). Either alone passes on the other's bug.
foreach ($fn in @('renderMainPaths')) {
    $body = LuaFn $rootTxt $fn
    if (-not $body) { $filterBad += "V205 $fn not found - the filter has no trigger"; continue }
    $n = ([regex]::Matches((NoComments $body), 'discLevels\(\)')).Count
    if ($n -ne 1) { $filterBad += "V205 $fn builds the level map $n time(s) - it must be built once and handed to every item" }
}
$openFn205 = LuaFn $rootTxt 'mfOpen'
if (-not $openFn205) { $filterBad += "V205 mfOpen not found - the filter has no trigger left at all (SPEC V209, T872)" }
else {
    $ob205 = NoComments $openFn205
    $nLev205 = ([regex]::Matches($ob205, 'discLevels\(\)')).Count
    $nHed205 = ([regex]::Matches($ob205, 'hedgePathLevels\(\)')).Count
    if ($nLev205 -ne 1) { $filterBad += "V205 mfOpen builds the Discipline level map $nLev205 time(s), expected once per OPENING - inside the pool loop it is 284 rituals against nineteen rows on every open, the walk this invariant exists to forbid (SPEC B48, B114)" }
    if ($nHed205 -ne 1) { $filterBad += "V205 mfOpen builds the hedge path map $nHed205 time(s), expected once per OPENING (SPEC I83f, B114)" }
    if ($ob205 -notmatch 'pickAllowed\(MF\.field, vals\[i\], levels, hedge\)') {
        $filterBad += "V205 the pool mfOpen builds does not ask pickAllowed - main path, secondary path and ritual would offer everything the book has and guardPick would revert it on the next click, which is the list and the guard disagreeing (SPEC V199, I113c, B114)"
    }
    else {
        # ORDER, and it is the whole of V200/V346b: the row's own value joins the pool AFTER
        # the filter, however illegal a rename left it. Filtered out, it would vanish from the
        # only door the field has left - B91 walking back in through the new box.
        $iFilt205 = $ob205.IndexOf('pickAllowed(MF.field, vals[i], levels, hedge)')
        $iCur205  = $ob205.IndexOf('MF.pool[#MF.pool + 1] = cur;')
        if ($iCur205 -ge 0 -and $iCur205 -lt $iFilt205) { $filterBad += "V205 the row's own value is appended BEFORE the filter runs - the filter would drop it and the player could never put it back (SPEC V200, V346b)" }
    }
    # The maps are read OUTSIDE the loop that spends them. Read inside, the count above still
    # says one and the walk is per item all the same - which is the bug wearing the fix's face.
    $loop205 = [regex]::Match($ob205, '(?s)for i = 1, #vals, 1 do(.*?)end;')
    if ($loop205.Success -and $loop205.Groups[1].Value -match 'discLevels\(\)|hedgePathLevels\(\)') {
        $filterBad += "V205 mfOpen builds a level map INSIDE the pool loop - once per item is what this invariant forbids, and the count above cannot see it (SPEC B48, V222)"
    }
}
foreach ($fn in @('applyLanguage', 'applyTheme')) {
    $m = [regex]::Match($hh6, "(?ms)local function $fn\(.*?\n\t\t\tend;")
    if (-not $m.Success) { $filterBad += "V205 $fn not found in WoD20.6"; continue }
    $n = ([regex]::Matches((NoComments $m.Value), 'discLevels\(\)')).Count
    if ($n -ne 1) { $filterBad += "V205 $fn builds the level map $n time(s) - the traversal must build it once, outside its loop" }
}

# V205, second leg (SPEC T491, B48): the map is read once per render, and now the TRANSFORMED
# pair is built once per LIST rather than once per control. Twenty-five ritual combos read the
# same 284 authored items and each rebuilt them, eraName plus the PT lookup item by item, on
# every one of the three passes an opening makes. Nothing is invalidated here either: the stamp
# names what the answer depends on, so a stale entry is never SERVED, it is simply never found.
if ($pickerFn.Success) {
    $pkm = NoComments $pickerFn.Groups[1].Value

    # BOTH ends, named apart. A memo that is written and never read is the shape of SPEC B7:
    # the string is there, the work still happens, and this check reports on nothing.
    # Anchored on the CONSTRUCTION and never on the identifiers (SPEC V222, B60), the same
    # cure V275 already carries: whatever indexes memo.lists IS the key, and whatever the read
    # assigns IS the hit, under any name. Renaming either local is invisible here - that is the
    # neighbour probe - while dropping the read, the store, or the miss gate still goes red.
    # The old form spelled `key` and `hit` into the pattern, so a rename that changed no
    # behaviour at all turned this check red (found by the T611 probe, 96th round).
    $idx205 = [regex]::Match($pkm, 'memo\.lists\[\s*(\w+)\s*\]')
    $rd205 = [regex]::Match($pkm, '(?m)^\s*local\s+(\w+)\s*=\s*memo\.lists\[\s*\w+\s*\];')
    $read205 = $idx205.Success -and $rd205.Success
    $store205 = $false
    if ($read205) {
        $k205 = [regex]::Escape($idx205.Groups[1].Value)
        $h205 = [regex]::Escape($rd205.Groups[1].Value)
        $store205 = ($pkm -match "memo\.lists\[\s*$k205\s*\]\s*=\s*\{ kept = kept, shown = shown \};") -and ($pkm -match "if\s+$h205\s+== nil then")
    }
    if (-not $read205) {
        $filterBad += "V205 pickerItems never READS the memo - the same list is rebuilt once per control, and the ritual rows alone pay that 25 times a pass (SPEC T491, B48)"
    }
    elseif (-not $store205) {
        $filterBad += "V205 pickerItems never STORES the pair it built, or does not gate the build on the miss - the read would find nothing and every control would rebuild anyway (SPEC T491)"
    }
    # discLevels() hands back a FRESH table each traversal, so this identity test IS "a new
    # render started". Drop it and a Discipline dot click repaints the list it just invalidated.
    elseif ($pkm -notmatch 'memo\.levels ~= levels') {
        $filterBad += "V205 the memo is not dropped when the level map changes - a Discipline dot click would repaint the list it just invalidated (SPEC T491, I25)"
    }
    elseif ($pkm -notmatch 'memo\.stamp ~= stamp' -or $pkm -notmatch 'tostring\(lang\) \.\. "\|" \.\. tostring\(era\)') {
        $filterBad += "V205 the memo is not stamped with language and era - switching either would leave the old era's words on the rows (SPEC T469, V197)"
    }
    # One main path per blood sorcery (SPEC V181) makes that answer per ROW: row 1 offers what
    # row 2 must not. Key those five by anything shared and one row's list reaches all five.
    elseif ($pkm -notmatch '\(root == "mainPath"\) and nm or root') {
        $filterBad += "V205 the memo key does not single out mainPath - its answer is per row, so row 1's list would be served to row 2 (SPEC V181)"
    }
    # The row's own value only changes the list when the filter would otherwise drop it
    # (SPEC V200). Leave it out of the key and a row holding an illegal value hands that value
    # to every other row on the same list.
    elseif ($pkm -notmatch 'not pickAllowed\(field, current, levels, memo\.hedge\)' -or $pkm -notmatch '\.\. "\|" \.\. odd') {
        $filterBad += "V205 the memo key ignores a row's own illegal value - it would leak into every other row sharing the list, or split the key for every filled row (SPEC V200)"
    }
}

# The trigger leg RETIRED in the 159th round (SPEC V203 amended, T872). It charged WoD20.13
# and WoD20.14 with a dataLink on the nineteen Discipline slots and the ninety-five dots under
# them, calling renderVampPickers so the lists were rebuilt when a rating moved (V123). Wave 3
# deleted renderVampPickers: the box builds MF.pool on EVERY opening, so a dot that moves while
# the box is shut leaves no stale list anywhere, and a link here would be work for work's sake.
#
# What replaces it is stronger and cheaper: the name is gone from the WHOLE sheet. While it
# stood, "WoD20.12 must NOT call it" had to be written as an exception; with the function
# deleted the exception disappears and the rule reads the same for all three files.
$vampAny = @()
foreach ($fn in @('WoD20.11.lfm')) {
    $p203 = Join-Path $dir $fn
    if (-not (Test-Path $p203)) { $filterBad += "V203 $fn is gone"; continue }
    # CodeOf: two of the three carry a comment saying the call USED to ride here, and reading
    # prose as code would fail on the note that records the removal (SPEC V222, B60).
    if ((CodeOf $p203) -match 'renderVampPickers') { $vampAny += $fn }
}
if ($vampAny.Count -gt 0) {
    $filterBad += "V203 $($vampAny -join ', ') still names renderVampPickers - the filter lives in mfOpen's pool build now, and a second trigger is a second list that drifts (SPEC V205 amended, V135, T872)"
}

if ($filterBad) { foreach ($b in $filterBad) { Fail $b } }
else { Pass "V199-V205 one predicate feeds both the list and the guard; the lists filter from the authored constant, keep their own value, and are rebuilt from a map read once per render" }

# ---- V208 / V211 / V212: the map itself (SPEC I27, 68th round) -----------------------
# T493 took every picker list out of the XML and put it in one map on the root form. These
# three measure the map: that it holds each list ONCE, that every picker actually reaches
# it, and that the sixty clan names did not quietly fork into two spellings.
$v208Bad = @()

# V208a: no list is in the map twice. The three ALIASES are excluded on purpose - they are
# identity assignments (PICKER_LIST["secPath"] = PICKER_LIST["mainPath"]), which is one
# table with two keys and the exact opposite of a copy (SPEC V211c).
$plSeen = @{}
foreach ($k in ($PICKER.Keys | Sort-Object)) {
    if ($PICKER_ALIAS.ContainsKey($k)) { continue }
    $sig = ($PICKER[$k] -join ([char]1))
    if ($plSeen.ContainsKey($sig)) {
        $v208Bad += "PICKER_LIST['$k'] and PICKER_LIST['$($plSeen[$sig])'] hold the same $($PICKER[$k].Count) entries twice - one of them belongs as an alias (SPEC V211c)"
    } else { $plSeen[$sig] = $k }
}

# V208b: nothing in the migrated scope still authors a list inline. A survivor is a second
# home for a list that is supposed to have exactly one.
foreach ($f in $files) {
    if ($f.Name -notin $PICKER_SCOPE) { continue }
    foreach ($cb in (Doc $f.FullName).SelectNodes("//comboBox[@name]")) {
        if ($cb.GetAttribute("name") -notlike 'cbo*') { continue }
        if ($cb.HasAttribute("items") -or $cb.HasAttribute("values")) {
            $v208Bad += "$($f.Name)/$($cb.GetAttribute('name')) still authors items=/values= inline - the list would live in two places (SPEC I27)"
        }
    }
}
if ($v208Bad) { foreach ($b in $v208Bad) { Fail "V208 $b" } }
elseif ($PICKER.Count -eq 0) { Fail "V208 PICKER_LIST parsed to nothing - the check verifies nothing (SPEC V20, B7)" }
else { Pass "V208 $($plSeen.Count) distinct picker lists, $($PICKER.Count) keys, no list authored twice" }

# V211: the map covers every picker, is findable, and its aliases are identity.
$v211Bad = @()
if (-not $plRegion.Success) { $v211Bad += "the PICKER_LIST_BEGIN/END markers are gone from the root form - nothing below reads anything (SPEC V20, B7)" }

# (a) every cbo* in the migrated scope resolves to a key. A combo with no key falls through
# to c.values, which T493 emptied: a blank dropdown that rdk and this gate both call fine.
# This is the single most likely way the migration breaks, and the quietest.
$v211Seen = 0
foreach ($f in $files) {
    if ($f.Name -notin $PICKER_SCOPE) { continue }
    foreach ($cb in (Doc $f.FullName).SelectNodes("//comboBox[@name]")) {
        $nm = $cb.GetAttribute("name")
        if ($nm -notlike 'cbo*') { continue }
        $v211Seen++
        $k = PickerKeyOf $nm $cb.GetAttribute("field") (TplOf $cb)
        if (-not $k) { $v211Bad += "$($f.Name)/$nm resolves to no PICKER_LIST key at all" }
        elseif (-not $PICKER.ContainsKey($k)) { $v211Bad += "$($f.Name)/$nm wants PICKER_LIST['$k'] and the map has no such key - the dropdown opens EMPTY and nothing says so" }
        elseif (@($PICKER[$k]).Count -eq 0) { $v211Bad += "$($f.Name)/$nm reads PICKER_LIST['$k'], which is empty" }
    }
}
if ($v211Seen -eq 0) { $v211Bad += "no cbo* picker was examined in the migrated scope - the check verifies nothing (SPEC V20, B7)" }

# (c) the aliases are identity, never a second literal list.
foreach ($a in $PICKER_ALIAS.Keys) {
    if (-not $PICKER.ContainsKey($PICKER_ALIAS[$a])) {
        $v211Bad += "PICKER_LIST['$a'] aliases '$($PICKER_ALIAS[$a])', which the map does not declare"
    }
}
if ($v211Bad) { foreach ($b in $v211Bad) { Fail "V211 $b" } }
else { Pass "V211 all $v211Seen pickers in scope resolve to a list, found by marker, with $($PICKER_ALIAS.Count) alias(es) by identity" }

# V212: the sixty clan names exist twice - as the ordered list the picker shows, and as the
# keys of CLANS (SPEC I17), which maps a clan to its three fixed Disciplines. Not a V208
# violation: the two have different SHAPES and CLANS carries data the picker does not. What
# they share is a key set, so what they risk is DRIFT. Compared here in the SOURCE, never
# read at runtime, so CLANS stays the dormant table V174 requires.
$clanTbl = [regex]::Match($rootLfmTxt, '(?s)local CLANS = \{(.*?)\n\t+\};')
$v212Bad = @()
if (-not $clanTbl.Success) { $v212Bad += "the CLANS region is gone from the root form - the check reads nothing (SPEC V20)" }
else {
    $clanKeys = @([regex]::Matches($clanTbl.Groups[1].Value, '\["([^"]+)"\]\s*=\s*\{') | ForEach-Object { $_.Groups[1].Value })
    # Since T510 the field holds a clan OR a revenant family, so the set CLANS has to answer
    # is BOTH lists, never the clan list alone (SPEC I29b, V294).
    $clanPick = @(@($PICKER['clan']) + @($PICKER['family']) | Where-Object { $_ -ne '' })
    if ($clanKeys.Count -eq 0) { $v212Bad += "CLANS parsed to zero clans - the check verifies nothing (SPEC V20, B7)" }
    elseif ($clanPick.Count -eq 0) { $v212Bad += "PICKER_LIST['clan'] is empty - there is nothing to compare CLANS against" }
    else {
        foreach ($c in $clanPick) { if ($clanKeys -notcontains $c) { $v212Bad += "'$c' is offered by the picker and absent from CLANS" } }
        foreach ($c in $clanKeys) { if ($clanPick -notcontains $c) { $v212Bad += "'$c' is keyed in CLANS and the picker does not offer it" } }
    }
}
if ($v212Bad) { foreach ($b in $v212Bad) { Fail "V212 $b" } }
else { Pass "V212 the picker and CLANS name the same $(@(@($PICKER['clan']) + @($PICKER['family']) | Where-Object { $_ -ne '' }).Count) clans and families" }

# ---- V294: clan and family are TWO authored lists - disjoint, constant, translated ------
# clanFamily stopped being an alias when clan and family became two lists (SPEC I29b), and
# from then until T954 the ghoul dropdown offered their UNION, which leg (a) measured loop by
# loop. It left in T954 (one button per list) and came BACK in T972 with the one-button line
# (SPEC I149a, V433b) - so leg (a) measures the union again.
# Legs (b), (c) and (d) outlive it, because they were never about the union: two lists that
# share a name make `CLANS[sheet.clanFamily]` ambiguous whichever button wrote the field, a
# push onto either list grows the Main tab's clan picker (SPEC B50), and a family with no
# `.lang` key shows raw in a dropdown V16 cannot see into.
$v294Bad = @()
$famList = @()
if ($PICKER.ContainsKey('family')) { $famList = @($PICKER['family']) }
if ($famList.Count -eq 0) {
    $v294Bad += "PICKER_LIST['family'] is not declared - the family picker would offer nothing (SPEC I29b)"
} else {
    $uClan = @($PICKER['clan'])
    # (a) the union really walks BOTH lists. This has to read the SOURCE, not the mirror the
    # parser builds: the mirror is clan+family by construction, so comparing it against
    # clan+family would be a check that cannot fail - B7 in a new costume. What can rot is the
    # code, so the code is what gets measured: one loop per authored list, both copying into
    # the same local, and that local handed to the map (SPEC V294a, V433b).
    if (-not $UNION_REGION.Success) {
        $v294Bad += "the CLANFAMILY_UNION markers are gone from the root form - the union is unreadable and the one picker of the Ghoul line offers nothing (SPEC V20, B7, V433b)"
    } else {
        $uSrc = $UNION_REGION.Groups[1].Value
        $uLocal = ''
        if ($uSrc -match 'PICKER_LIST\["clanFamily"\]\s*=\s*(\w+);') { $uLocal = $Matches[1] }
        if (-not $uLocal) {
            $v294Bad += "nothing inside the markers is handed to PICKER_LIST['clanFamily'] - the picker would resolve to nothing at all (SPEC V294a, B6)"
        } else {
            foreach ($src in @('clan', 'family')) {
                if ($uSrc -notmatch ('#PICKER_LIST\["' + $src + '"\]')) {
                    $v294Bad += "the union never walks PICKER_LIST['$src'] - the picker would offer only what the other loop copies (SPEC V294a)"
                }
                if ($uSrc -notmatch ([regex]::Escape($uLocal) + '\[[^\]\r\n]*\]\s*=\s*PICKER_LIST\["' + $src + '"\]')) {
                    $v294Bad += "PICKER_LIST['$src'] is walked but never copied into $uLocal - the entries counted are not the entries offered (SPEC V294a)"
                }
            }
        }
    }
    # (b) the two lists are disjoint
    $clanSet = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
    foreach ($x in $uClan) { if ($x -ne '') { [void]$clanSet.Add($x) } }
    foreach ($x in $famList) {
        if ($clanSet.Contains($x)) { $v294Bad += "'$x' is authored by BOTH clan and family - CLANS[sheet.clanFamily] cannot say which one was picked, and the pertinence test of V412b/V426c answers wrong (SPEC V294b)" }
        if ($x -eq '') { $v294Bad += "PICKER_LIST['family'] carries an empty item - the way out of the row is btnMfRemove, not a blank entry (SPEC I29b, V369a)" }
    }
    # (c) neither authored list is grown, anywhere
    $rootNoC294 = NoComments $rootLfmTxt
    foreach ($k in @('clan', 'family', 'clanFamily')) {
        # WRITING is the verb, so the pattern demands the assignment: a reader indexes
        # PICKER_LIST["clan"][i] too, and a check that could not tell the two apart would fail
        # on the very code it exists to protect.
        if ($rootNoC294 -match ('PICKER_LIST\["' + $k + '"\]\s*\[[^\]\r\n]*\]\s*=[^=]')) {
            $v294Bad += "PICKER_LIST['$k'] is indexed for writing - the authored lists are constants, which is what keeps an append off the Main tab's clan picker (SPEC V294c, V201, V211c)"
        }
    }
    # (d) every family name is translated in both languages
    foreach ($x in $famList) {
        if (-not $ptK.Contains($x)) { $v294Bad += "'$x' has no [pt] entry - the picker shows the raw key, and the list lives in Lua where V16 does not reach it (SPEC V294d)" }
        if (-not $enK.Contains($x)) { $v294Bad += "'$x' has no [en] entry - the picker shows the raw key, and the list lives in Lua where V16 does not reach it (SPEC V294d)" }
    }
}
if ($famList.Count -lt 23) { Fail "V294 PICKER_LIST['family'] parsed to $($famList.Count) entries, expected at least the 23 left after T938, with I108b and Q21 splitting two of them - this check is covering less than the sheet has (SPEC V209)" }
elseif ($v294Bad) { foreach ($b in ($v294Bad | Select-Object -First 12)) { Fail "V294 $b" } }
else { Pass "V294 clan and family are two authored lists - no overlap, no growth, all $($famList.Count) families translated in both languages" }

# ---- zero-guards for the five sites that would otherwise go GREEN covering less --------
# SPEC V209 a/c/d/f/g. Each of these reads a list; none of them had a guard that fires when
# the list stops arriving, so after T493 they would have passed while verifying a fraction
# of what they used to. V24 got its guard inline above; these are the other four.
if ($visiblePickerItems -eq 0) { Fail "V17/V28 no picker item reached the visible-string set - V9, V10 and V28 all measure less and none of them says so (SPEC V20, B7, V209a)" }
else { Pass "V17/V28 $visiblePickerItems picker items reached the visible-string set" }


# ---- V213: the old sheet name does not survive anywhere in the new plugin -------------
# A half-done rename is the failure mode here, and nothing downstream reports it: WoD20.6
# carries twelve absolute "/WoD20th/images/..." theme paths and the root carries fourteen
# <import file="WoD20.<n>.lfm"/>, so one survivor points at a file that is not there. The
# rdk will not say so - B49 proved it exits 1 with no message at all - which leaves the
# source as the only place the difference is still legible.
if ($files.Count -ne 12) { Fail "V213 read $($files.Count) .lfm, expected 12 - the check would pass having looked at less (SPEC V209). Was 15 until T909 dissolved WoD20.12/.13/.14 into WoD20.11 (SPEC I136i, V397)" }
$v213Bad = @()
$modTxt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $plugin "module.xml")))
foreach ($f in $files) {
    $t = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName))
    foreach ($pat in @('HuntersHunted', 'frmHH_', 'HH\.\d+', 'Hunters Hunted - Mortal')) {
        foreach ($m in [regex]::Matches($t, $pat)) { $v213Bad += "$($f.Name) still carries '$($m.Value)'" }
    }
}
foreach ($pat in @('HuntersHunted', 'HH\.\d+')) {
    foreach ($m in [regex]::Matches($modTxt, $pat)) { $v213Bad += "module.xml still carries '$($m.Value)'" }
}
if ($v213Bad) { foreach ($b in ($v213Bad | Select-Object -First 12)) { Fail "V213 $b" } }
else { Pass "V213 no HuntersHunted / HH.<n> leftover across 15 .lfm + module.xml" }

# ---- V213: the credit to the base plugin is an OBLIGATION, not a tolerance ------------
# The user asked for the thank-you to stay when the plugin stopped being its first
# author's, and then (2026-08-22) cut it down to a single line. This check holds that
# line in the two places the sheet itself carries it; V230 further down owns the rest of
# the rule - that the brand is gone, and that the name appears nowhere else at all.
$creditTxt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20.6.lfm")))
$creditBad = @()
foreach ($needle in @('text="Based on the sheet plugin by Vinny (Ambesek)"',
                      '["Based on the sheet plugin by Vinny (Ambesek)"] = "Baseado no plugin de fichas de Vinny (Ambesek)"')) {
    if ($creditTxt.IndexOf($needle) -lt 0) { $creditBad += "missing: $needle" }
}
if ($creditBad) { foreach ($b in $creditBad) { Fail "V213 credit $b" } }
else { Pass "V213 the credit to the base plugin stands on the Settings tab (label + pt entry)" }

# ---- V213: the Game roster keeps its "Mortal" item ------------------------------------
# The item read "Hunters Hunted" until the 82nd round, when the user renamed it (SPEC C,
# V233): this is a mortal's sheet and the roster says so. Counting the bare word would be
# a HOLE - "Mortal" already appears inside two power names in this same file - so the
# three sites are asserted as needles, the shape the credit check above already uses.
$rosterBad = @()
foreach ($needle in @("items=""{'Vampire', 'Mortal', 'Mage'}""",
                      "values=""{'Vampire', 'Mortal', 'Mage'}""",
                      '<dataLink field="game" defaultValue="Mortal"/>')) {
    if ($creditTxt.IndexOf($needle) -lt 0) { $rosterBad += "missing: $needle" }
}
if ($rosterBad) { foreach ($b in $rosterBad) { Fail "V213 Game roster $b" } }
else { Pass "V213 the Game roster offers Vampire, Mortal, Mage and defaults to Mortal" }

# ---- V214: the old plugin no longer declares the sheet --------------------------------
# V4 hunts the NEW dataType, so a forgotten copy under the old plugin is invisible to it
# and alive for the Firecast: two sheets in the list, the old one requiring desc*.lua that
# no longer sit beside it.
$oldPlugin = Join-Path $PSScriptRoot "Plugins\Sheets\World of Darkness 20th"
$v214Bad = @()
if (-not (Test-Path -LiteralPath $oldPlugin)) { $v214Bad += "the old plugin folder is gone - V214 measured nothing (SPEC V209)" }
if (Test-Path -LiteralPath (Join-Path $oldPlugin "HuntersHunted")) { $v214Bad += "HuntersHunted/ is still under the old plugin" }
# The EIGHT halves T495 made, not the four monoliths they replaced: with the old names gone
# from the repo the old list would be vacuously true, and a forgotten copy of the new modules
# under the old plugin would light nothing (SPEC V210, V20).
foreach ($m in @('descDisc_en.lua', 'descDisc_pt.lua', 'descNumina_en.lua', 'descNumina_pt.lua',
                 'descPath_en.lua', 'descPath_pt.lua', 'descRitual_en.lua', 'descRitual_pt.lua')) {
    if (Test-Path -LiteralPath (Join-Path $oldPlugin $m)) { $v214Bad += "$m is still at the old plugin root" }
}
$v214Stale = Select-String -Path (Join-Path $oldPlugin "*\*.lfm") -Pattern 'Ambesek\.HuntersHunted\.20th' -ErrorAction SilentlyContinue
if ($v214Stale) { $v214Bad += "the old dataType is still declared in $($v214Stale[0].Filename)" }
if ($v214Bad) { foreach ($b in $v214Bad) { Fail "V214 $b" } }
else { Pass "V214 the old plugin is back to the five Ambesek sheets" }

# ---- V215: every require() resolves at the NEW plugin root -----------------------------
# require is relative to the PLUGIN root, not to the sheet folder, so moving the sheet
# without the four desc modules breaks in runtime and nowhere else: the rdk does not
# resolve require (SPEC B1) and every other check here stays green.
$v215Bad = @()
$v215Seen = 0
foreach ($f in $files) {
    $t = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName))
    foreach ($m in [regex]::Matches($t, 'require\("([^"]+\.lua)"\)')) {
        $v215Seen++
        $mod = $m.Groups[1].Value
        if (-not (Test-Path -LiteralPath (Join-Path $plugin $mod))) { $v215Bad += "$($f.Name) requires '$mod', missing at the plugin root" }
    }
    # T495: a description module is named by the language - require("desc<X>_" .. lang ..
    # ".lua") - so BOTH halves have to be on file. Matched on the literal alone this check
    # would have counted zero and reddened on its own zero-guard while nothing was wrong.
    foreach ($m in [regex]::Matches($t, 'require\("([A-Za-z0-9]+)_"\s*\.\.\s*\w+\s*\.\.\s*"(\.lua)"\)')) {
        foreach ($lang in @('en', 'pt')) {
            $v215Seen++
            $mod = "$($m.Groups[1].Value)_$lang$($m.Groups[2].Value)"
            if (-not (Test-Path -LiteralPath (Join-Path $plugin $mod))) { $v215Bad += "$($f.Name) builds '$mod' out of the language and it is missing at the plugin root" }
        }
    }
}
if ($v215Seen -eq 0) { Fail "V215 found no require() at all - the check verified nothing (SPEC V209, B7)" }
elseif ($v215Bad) { foreach ($b in ($v215Bad | Sort-Object -Unique)) { Fail "V215 $b" } }
else { Pass "V215 all $v215Seen require() calls resolve at the plugin root" }

# ---- V216: the id charset the rdk actually enforces ------------------------------------
# Alphanumerics, underscore and dot, 5..40 - the rule is written in the module.xml that
# "rdk -p" generates. A hyphen makes rdk -c and rdk -l exit 1 with NO message and emit no
# .rpk, which is byte-for-byte the signature of a Lua syntax error (SPEC B19), so the only
# cheap place to tell the two apart is here, before the rdk runs at all (SPEC B49).
$idRule = '^[A-Za-z0-9_.]{5,40}$'
$v216Bad = @()
$idM = [regex]::Match($modTxt, '<id>([^<]*)</id>')
if (-not $idM.Success) { $v216Bad += "module.xml declares no <id>" }
elseif ($idM.Groups[1].Value -notmatch $idRule) { $v216Bad += "module.xml <id> '$($idM.Groups[1].Value)' breaks it" }
$v216Seen = 0
foreach ($f in $files) {
    $t = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName))
    foreach ($m in [regex]::Matches($t, 'dataType="([^"]*)"')) {
        $v216Seen++
        if ($m.Groups[1].Value -notmatch $idRule) { $v216Bad += "$($f.Name) dataType '$($m.Groups[1].Value)' breaks it" }
    }
}
if ($v216Seen -eq 0) { Fail "V216 found no dataType at all - the check verified nothing (SPEC V209)" }
elseif ($v216Bad) { foreach ($b in $v216Bad) { Fail "V216 $b (alphanumerics, underscore, dot, 5..40 - SPEC B49)" } }
else { Pass "V216 <id> and $v216Seen dataType match the rdk id rule" }

# Comments come off before either of the next two checks reads the form. Both of them were
# caught reddening over PROSE by their own probe (SPEC V222): a comment saying GEN_MAX = { or
# naming declareTrait("bloodPool is a comment, not a second map and not a declaration.
$rootCode = NoComments $rootTxt

# ---- V218: one GEN->MAX map, and both readers on it ------------------------------
# The label says what the ceiling is and xpClick enforces it. Two copies is how the number
# shown and the number charged start disagreeing, and the sheet would be lying on screen
# about a rule it applies itself (SPEC B46 is the same shape: a constant with no reader).
$genDecl = [regex]::Matches($rootCode, 'GEN_MAX\s*=\s*\{')
$genReads = [regex]::Matches($rootCode, 'GEN_MAX\[')
$ceilReads = [regex]::Matches($rootCode, 'maxDiscLevel\(\)')
if ($genDecl.Count -ne 1) { Fail "V218 GEN_MAX is declared $($genDecl.Count) time(s) - the label and the ceiling must read one map" }
elseif ($genReads.Count -lt 1) { Fail "V218 nothing indexes GEN_MAX - the map has no reader" }
elseif ($ceilReads.Count -lt 3) { Fail "V218 maxDiscLevel has $($ceilReads.Count - 1) reader(s) - the label and the refusal must share it" }
else { Pass "V218 one GEN_MAX, read by the label and by the refusal" }

# ---- V219: the blood pool is free, and is not a trait -----------------------------
# What V219 buys has never been "the host paints them": it is that NOTHING PRICES these dots -
# no declareTrait, no ledger line, nothing reaching xpClick. That half is untouched.
#
# The OTHER half - autoChange on, no onClick - was amended on 2026-08-28 (SPEC I108c, Q20). A
# revenant family can be capped at eight blood points (Grimaldi, da p.454); V337d demands that a
# cap refuse BEFORE the write; and a self-marking dot has no before, because the host has already
# written by the time any Lua runs. So the dots are Lua-driven now, through a refuser of their
# OWN. What replaces the old rule is STRICTER, not looser: every dot carries autoChange="false"
# AND names bloodClick with its own field, and bloodClick reaches nothing that prices.
$bpDoc = Doc (Join-Path $dir "WoD20.11.lfm")
$bpDots = @($bpDoc.SelectNodes("//imageCheckBox") | Where-Object { $_.GetAttribute("field") -match '^bloodPool_\d+$' })
$bpBad = @()
if ($bpDots.Count -ne 20) { $bpBad += "the blood pool has $($bpDots.Count) dots, expected 20 - and with the count wrong every leg below is reading a fraction of the box (SPEC I29, V209)" }
else {
    foreach ($d219 in $bpDots) {
        $f219 = $d219.GetAttribute("field")
        if ($d219.GetAttribute("autoChange") -ne 'false') { $bpBad += "$f219 still marks itself - with autoChange on there is no BEFORE for a family cap to refuse in (SPEC I108c, V337d)" }
        $c219 = $d219.GetAttribute("onClick")
        if ($c219 -notmatch "^bloodClick\('$f219', self\);$") { $bpBad += "$f219 runs '$c219' on the click, expected bloodClick('$f219', self); - one refuser, named, and each dot hands it its OWN field" }
    }
}
if ($rootCode -match 'declareTrait\("bloodPool') { $bpBad += "bloodPool is declared in XP_TRAIT - it would be priced and would write ledger lines" }
if ($rootCode -notmatch 'bloodPool_1 == nil') { $bpBad += "the ten-dot seed is gone - a new sheet would open empty (SPEC I29)" }
$bc219 = LuaFn $rootTxt 'bloodClick'
if (-not $bc219) { $bpBad += "bloodClick is not on the root form - twenty dots name a function that does not exist, and every one of them is dead (SPEC V209)" }
else {
    $bcb219 = NoComments $bc219
    foreach ($banned219 in @('xpClick', 'declareTrait')) {
        if ($bcb219 -match "\b$banned219\b") { $bpBad += "bloodClick reaches $banned219 - the dots are FREE, and that is the half of V219 the amendment did NOT touch (SPEC I108c)" }
    }
    if ($bcb219 -notmatch 'familyCap\("bloodPool"\)') { $bpBad += "bloodClick never asks familyCap for the ceiling - the cap would be data nobody reads, which V337a calls worse than absent (SPEC V337c)" }
    if ($bcb219 -notmatch 'setField\(field, false\)') { $bpBad += "bloodClick cannot turn a dot OFF - a cap limits what the family HOLDS, and spending blood is the one thing it never refuses" }
}
if ($bpBad) { foreach ($b in $bpBad) { Fail "V219 $b" } }
else { Pass "V219 twenty free blood pool dots, Lua-driven through one refuser that prices nothing and reads the family ceiling from familyCap" }

# ---- V220: the ceiling refuses BEFORE anything is written -------------------------
# Same spot and same shape as the path ceiling it sits beside. A guard that lets the mark
# land and argues afterwards cost three rounds once already (SPEC B38, V135).
$clickBody = LuaFn $rootTxt 'xpClick'
if (-not $clickBody) { Fail "V220 xpClick not found on the root form" }
else {
    $cb = NoComments $clickBody
    $capAt = $cb.IndexOf("maxDiscLevel()")
    $markAt = $cb.IndexOf("markDot(")
    if ($capAt -lt 0) { Fail "V220 nothing in xpClick asks for the generation ceiling" }
    elseif (-not $cb.Contains('"^clanDisc_%d+$"')) { Fail "V220 the ceiling does not cover clan Discipline dots" }
    elseif (-not $cb.Contains('"^disc_%d+$"')) { Fail "V220 the ceiling does not cover out-of-clan Discipline dots" }
    elseif ($cb -notmatch 'genCap ~= nil') { Fail "V220 the ceiling applies with no generation chosen - a mortal with no domitor is not a fourteenth-generation ghoul (SPEC V220a)" }
    elseif ($markAt -ge 0 -and $capAt -gt $markAt) { Fail "V220 the ceiling is asked after the dot is marked - a mark made inside onChange does not come back (SPEC B38)" }
    else { Pass "V220 the generation ceiling refuses before anything is written" }
}

# ---- V221: the sub-tabs sit BELOW the boxes, not under them -----------------------
# V190 catches two siblings claiming one client rect. It does not catch an absolute layout
# sitting under a tabControl that still claims it - same symptom as B41 (content vanishes),
# different door, so this one measures geometry.
# REWRITTEN in the 159th round (SPEC V221 rewritten, T832), and it is the rewrite I118e ordered:
# from "panes STACKED on one rect" to "columns DISJOINT in x". The strip went out and the three
# panes stopped sharing a rectangle, so the old proof does not transfer - stacking and disjointness
# are different facts, and keeping the old one would let THREE COLUMNS LYING ON TOP OF EACH OTHER
# pass green. V40 cannot catch that either: all three are panels, and a panel is exempt from V40
# by V262(d), precisely because panes used to be allowed to share a rect.
# T909 (SPEC I136i, V397): a column used to be a <layout> holding an <import>, and that is how
# every leg below found one. The three imported forms are gone - each was painting its own theme
# ground under the paper (SPEC R136, R137, B128) - so a column is now what V361 already called
# one on the Numina tab: a direct <layout> child of the scrollBox BELOW the band at top=0. The
# partition is by TOP and needs no name, which is the same reason V361 uses it (SPEC V135).
$vampDoc = Doc (Join-Path $dir "WoD20.11.lfm")
$bandTop221 = BandTop ($vampDoc.SelectSingleNode("//scrollBox"))
$vampCols221 = @($vampDoc.SelectNodes("//scrollBox/layout") | Where-Object { [int]$_.GetAttribute("top") -gt $bandTop221 } | Sort-Object { [int]$_.GetAttribute("left") })
# (1) no strip, and no bar of any kind: a bar coming back is a sub-tab coming back.
$strip221 = $vampDoc.SelectSingleNode("//layout[@name='vampStrip']")
if ($null -ne $strip221) { Fail "V221 vampStrip is back in WoD20.11 - the Ghoul tab has no sub-tabs since T832, and a strip here is the arrangement pedido 5 asked to be rid of (SPEC I118)" }
elseif ($vampCols221.Count -lt 3) { Fail "V221 only $($vampCols221.Count) column(s) found in WoD20.11, expected 3 - every leg below reads an empty set (SPEC V209, V20)" }
else {
    # the boxes ABOVE: everything that is not one of the columns
    $lowest = 0
    foreach ($l in $vampDoc.SelectNodes("//scrollBox/layout")) {
        if ([int]$l.GetAttribute("top") -gt $bandTop221) { continue }
        $b = [int]$l.GetAttribute("top") + [int]$l.GetAttribute("height")
        if ($b -gt $lowest) { $lowest = $b }
    }
    $tops221 = @($vampCols221 | ForEach-Object { [int]$_.GetAttribute("top") } | Sort-Object -Unique)
    # (3)+(4) measured together: each column is exactly as wide as the form it holds DECLARES
    # itself to be, and the next one starts clear of it. The lefts are never literals here - they
    # fall out of the widths plus the gutter, the way V171 and V193 measured relations.
    $bad221 = @()
    $prevEnd = $null
    foreach ($c in $vampCols221) {
        $cl = [int]$c.GetAttribute("left"); $cw = [int]$c.GetAttribute("width")
        $sub = ColId361 $c
        if (-not $sub) { $sub = "the column at x=$cl" }
        # The column is exactly as wide as the boxes it HOLDS, read off the boxes themselves
        # rather than off a second file. Same subtraction as before T909, one document instead
        # of two - and this is the artefact the host actually loads (SPEC V20).
        $own = 0
        foreach ($l in $c.SelectNodes("layout")) {
            $r = [int]$l.GetAttribute("left") + [int]$l.GetAttribute("width")
            if ($r -gt $own) { $own = $r }
        }
        if ($own -eq 0) { $bad221 += "the column at x=$cl holds no box - it would be measured against nothing (SPEC V209)" }
        elseif ($cw -ne $own) { $bad221 += "the column holding $sub is ${cw}px wide and its own boxes close at $own - slack hides a column that shrank, and a shortfall is the scrollbar V225(a) exists to stop (SPEC V221(4), I118d)" }
        if ($null -ne $prevEnd -and $cl -lt $prevEnd) { $bad221 += "the column holding $sub opens at x=$cl and the one before it closes at $prevEnd - they overlap, and a panel is exempt from V40 so nothing else on the sheet would say so (SPEC V221(3), V262d)" }
        $prevEnd = $cl + $cw
    }
    # the gutter between columns is the sheet's own 5 (SPEC V298), read as a relation
    $gaps221 = @()
    for ($i = 1; $i -lt $vampCols221.Count; $i++) {
        $gaps221 += ([int]$vampCols221[$i].GetAttribute("left")) - ([int]$vampCols221[$i-1].GetAttribute("left")) - ([int]$vampCols221[$i-1].GetAttribute("width"))
    }
    $oddGap = @($gaps221 | Where-Object { $_ -ne 5 })
    if ($bad221) { foreach ($b in $bad221) { Fail "V221 $b" } }
    elseif ($lowest -eq 0) { Fail "V221 no box was found above the columns - this check measured nothing (SPEC V209)" }
    elseif ($tops221.Count -ne 1) { Fail "V221 the three columns open at $($tops221 -join ', ') - they are one row of columns and share one top (SPEC V221(2), I118a)" }
    elseif ($tops221[0] -lt $lowest) { Fail "V221 the columns open at $($tops221[0]), inside the boxes that end at $lowest" }
    elseif ($oddGap.Count -gt 0) { Fail "V221 the columns are $($gaps221 -join ', ')px apart, expected 5 - it is the same gutter every section box on the sheet keeps (SPEC V298, V221(3))" }
    else { Pass "V221 no strip; the three columns open together at $($tops221[0]) clear of the boxes ending at $lowest, are disjoint in x 5px apart, and each is exactly as wide as the boxes it holds" }
}

# ---- V361: the two tabs that lost their sub-tabs lost them the SAME way ----------------
# SPEC V361, I117/I118, pedidos 4 and 5. Numina (T830) and Ghoul (T832) were rearranged by one
# request two tasks apart: the strip goes, the stacked panes become three columns side by side
# under the top band, and the band itself was told not to move. Every other ruler on this pair
# measures ONE tab - V221 the Ghoul, V267 the Numina band - so a DIVERGENCE between the two is
# precisely what nothing here sees, and two identical gestures wearing two different shapes is
# the bill V308 exists to refuse.
#
# This is where the roster of the three columns lives now. It was on LOAN inside V188 from T832
# until this task (SPEC V188 amended, T830): V188 could hold it because the Ghoul was the only
# rearranged tab, and deleting it there before this block existed would have left three columns
# with no ruler at all for a whole task, which is B7 with a date on it. Both tabs are on the
# table now, so the roster changes owner once, here, for both of them.
#
# The partition is by TOP and needs no second shape to work in both files: a direct <layout>
# child of the <scrollBox> at top=0 is the BAND, anything below it is a COLUMN. A strip coming
# back lands in the column set and breaks the count of three - that is "no sub-tabs" said in
# geometry instead of in names, and the names are already swept by V268's retirement guard, so
# this block does not sweep them a fourth time (SPEC V135, B70).
$V361_TABS = @(
    [pscustomobject]@{ F = 'WoD20.7.lfm';  Tab = 'Numina'; Cols = @('PSYCHIC PHENOMENA', 'HEDGE MAGIC PATHS', 'HEDGE MAGIC RITUALS') }
    [pscustomobject]@{ F = 'WoD20.11.lfm'; Tab = 'Ghoul';  Cols = @('CLAN DISCIPLINES', 'MAIN PATHS', 'RITUALS') }
)
# (a) half two: the band does not MOVE. A static check has no BEFORE to read, so it is FROZEN -
# the same answer $FIELD_CONTRACT gave V362(a) and for the same reason (SPEC V362a). These are the
# rects each band box was AUTHORED with at b5111c35, the last clean commit before wave 3 and T832.
# The Numina three were inside tabHedge at the freeze and the Ghoul two were already direct
# children of the scrollBox, and that is exactly why the contract records the RECT and not the
# parent: what the user asked to leave alone is where the box SITS in its tab, not which node
# holds it. Frozen at an earlier commit and not at today's state on purpose - frozen on today it
# could never redden for anything that has already happened.
#
# It is a FLOOR and not a ceiling, like the field contract: a box ADDED to a band costs nothing
# here (V40 and V298 own overlap and gap), and a box that MOVED or lost its title costs a named
# line in the exception list below, which is the whole price leg (c) exists to charge.
$V361_BAND = @(
    [pscustomobject]@{ F = 'WoD20.7.lfm';  T = 'HEDGE MAGIC';  L = 0;    Y = 0; W = 586; H = 146 }
    [pscustomobject]@{ F = 'WoD20.7.lfm';  T = 'QUINTESSENCE'; L = 591;  Y = 0; W = 407; H = 146 }
    [pscustomobject]@{ F = 'WoD20.7.lfm';  T = 'WILLPOWER';    L = 1003; Y = 0; W = 407; H = 146 }
    [pscustomobject]@{ F = 'WoD20.11.lfm'; T = 'DOMINATOR';    L = 0;    Y = 0; W = 660; H = 138 }
    [pscustomobject]@{ F = 'WoD20.11.lfm'; T = 'BLOOD POOL';   L = 665;  Y = 0; W = 405; H = 138 }
)
# (c) the exception is a LIST, not a door. One entry, and it is the one the user declared out
# loud while saying "nada aqui sera removido ou criado": the willpower block leaves Numina and
# True Faith takes its rect (SPEC I117d, V361c). A deviation the frozen band finds and this list
# does not name is a FAIL - that is the difference between an exception and an exit, and it is
# the same ceiling V362(d) puts on the deleted-fields list for the same reason (SPEC V351d).
#
# What left WITH the block - the ten permanent dots, the ten temporary boxes, the painter, and
# the ten willpower_c fields still authored in WoD20.3 - is V267's retirement guard and is not
# restated here (SPEC V135, B70, B98). This leg owns the TITLE at the frozen rect, which V267
# names in a message and measures nowhere.
$V361_EXCEPT = @(
    [pscustomobject]@{ F = 'WoD20.7.lfm'; Was = 'WILLPOWER'; Now = 'TRUE FAITH' }
)
# (a) half three, and it is the OWNER's door rather than a leak in the freeze. The frozen band
# above is HISTORY and stays byte for byte; what this list adds is the rect a named box reads
# TODAY because the user asked for it after the freeze, each line naming the ask that moved it.
# The check then matches the frozen rect OR the named one, so an UNNAMED slide still reddens -
# which is the whole value of leg (a) and the reason the freeze is not simply re-taken at HEAD.
# V362(b) is the rule being applied: an invariant does not veto an informed choice by the owner,
# it RECORDS which one was made.
#
# 2026-09-02, T896 (SPEC I134e): every box on the Numina tab takes the width of PSYCHIC
# PHENOMENA, so the top band goes 586/407/407 at 0/591/1003 to 466/466/466 at 0/471/942 and the
# tab closes on 1408 instead of 1410. The third is keyed by its FROZEN title, WILLPOWER, because
# leg (c) is what swaps it to TRUE FAITH and the two legs read the same row.
# 2026-09-02, T913 (SPEC I137): the Y margin of every box went 20 -> 15, so a band box loses
# 10 like any other. A BAND keeps ONE height or the bar reads ragged, and the tallest member sets
# it: HEDGE MAGIC needs 131 and the other two would have shrunk to 105 on their own content. The
# Ghoul band lands at 128 the same way, off BLOOD POOL. These rows REPLACE the T896 ones rather
# than sitting beside them - a rect has one current value, and two live rows for one box is the
# stale line V361 exists to refuse.
$V361_RESIZED = @(
    [pscustomobject]@{ F = 'WoD20.7.lfm';  T = 'HEDGE MAGIC';  L = 0;   Y = 10; W = 466; H = 131; Ask = 'T926' }
    [pscustomobject]@{ F = 'WoD20.7.lfm';  T = 'QUINTESSENCE'; L = 471; Y = 10; W = 326; H = 131; Ask = 'T926' }
    [pscustomobject]@{ F = 'WoD20.7.lfm';  T = 'WILLPOWER';    L = 802; Y = 10; W = 358; H = 131; Ask = 'T926' }
    [pscustomobject]@{ F = 'WoD20.11.lfm'; T = 'DOMINATOR';    L = 0;   Y = 10; W = 590; H = 128; Ask = 'T964' }
    [pscustomobject]@{ F = 'WoD20.11.lfm'; T = 'BLOOD POOL';   L = 595; Y = 10; W = 307; H = 128; Ask = 'T964' }
)
$v361ResizeHit = @{}
$v361Bad = @()
$v361Gesture = @()
$v361Cols = 0
$v361BandSeen = 0
$v361ExceptHit = @{}
foreach ($t361 in $V361_TABS) {
    $p361 = Join-Path $dir $t361.F
    if (-not (Test-Path $p361)) { $v361Bad += "(e) $($t361.F) is not on disk - the $($t361.Tab) tab would be measured by nothing (SPEC V209)"; continue }
    $sb361 = (Doc $p361).SelectSingleNode("//scrollBox")
    if ($null -eq $sb361) { $v361Bad += "(e) $($t361.F) has no scrollBox - the $($t361.Tab) tab would be measured by nothing (SPEC V209, B7)"; continue }
    $band361 = @()
    $cols361 = @()
    $bandTop361 = BandTop $sb361
    foreach ($l361 in $sb361.SelectNodes("layout")) {
        $ly361 = 0
        [void][int]::TryParse($l361.GetAttribute("top"), [ref]$ly361)
        if ($ly361 -eq $bandTop361) { $band361 += $l361 } else { $cols361 += $l361 }
    }
    $cols361 = @($cols361 | Sort-Object { [int]$_.GetAttribute("left") })
    $v361Cols += $cols361.Count

    # (a) half one, the ROSTER, inherited from V188 and now asked of BOTH tabs. A column names
    # itself by its own title when it IS a section box and by the title of its FIRST box when it
    # is a container of them - one reader, two tabs, and since T909 the two tabs are the same
    # shape: the Ghoul columns stopped being imported forms and became plain containers, so the
    # 'name yourself by the file you import' branch has no caller left (SPEC I136i, V397, B128).
    if ($cols361.Count -ne 3) {
        $v361Bad += "(a) the $($t361.Tab) tab holds $($cols361.Count) layout(s) below its band, expected the 3 columns - one per entry of its roster, authored in the XML so a sheet whose Lua did not run still opens with them, and a strip coming back lands in this same set (SPEC I117, I118a, V18, V94)"
    } else {
        foreach ($c361 in $cols361) {
            $id361 = ColId361 $c361
            if ($t361.Cols -notcontains $id361) {
                $v361Bad += "(a) the $($t361.Tab) tab holds a column calling itself '$id361' and its roster is $($t361.Cols -join ', ') - the tab IS those three and nothing else (SPEC I117, I118a)"
            }
            if ($c361.GetAttribute("visible") -eq 'false') {
                $v361Bad += "(a) the $($t361.Tab) column '$id361' is authored hidden - the three stand side by side now and none of them is a sub-tab waiting to be selected (SPEC I117, I118d, T830, T832)"
            }
            if ($c361.GetAttribute("name")) {
                $v361Bad += "(a) the $($t361.Tab) column '$id361' still carries name='$($c361.GetAttribute('name'))' - those names existed for selectTab, and a name that outlives its only reader is the control-level orphan V2 refuses for fields (SPEC T832, V2)"
            }
        }
        foreach ($want361 in $t361.Cols) {
            $found361 = $false
            foreach ($c361 in $cols361) {
                if ((ColId361 $c361) -eq $want361) { $found361 = $true }
            }
            if (-not $found361) { $v361Bad += "(a) no column of the $($t361.Tab) tab is '$want361' - the roster names three and this one answers to nothing on the sheet (SPEC I117, I118a, V209)" }
        }
    }

    # (a) half two: every frozen rect of THIS file is still authored in its band, and the title
    # standing on it is the frozen one unless leg (c) names the swap.
    $contract361 = @($V361_BAND | Where-Object { $_.F -eq $t361.F })
    foreach ($fr361 in $contract361) {
        # The rect this box may read: the frozen one, plus any the owner named in $V361_RESIZED.
        $rects361 = @($fr361) + @($V361_RESIZED | Where-Object { $_.F -eq $fr361.F -and $_.T -eq $fr361.T })
        $hit361 = $null
        $via361 = $null
        foreach ($want in $rects361) {
            foreach ($b361 in $band361) {
                if ([int]$b361.GetAttribute("left") -eq $want.L -and [int]$b361.GetAttribute("top") -eq $want.Y -and
                    [int]$b361.GetAttribute("width") -eq $want.W -and [int]$b361.GetAttribute("height") -eq $want.H) { $hit361 = $b361; $via361 = $want }
            }
            if ($null -ne $hit361) { break }
        }
        if ($null -eq $hit361) {
            $alt361 = if ($rects361.Count -gt 1) { " nor at the " + (($rects361 | Select-Object -Skip 1 | ForEach-Object { "$($_.L),$($_.Y) $($_.W)x$($_.H) its $($_.Ask) line names" }) -join ' nor ') } else { '' }
            $v361Bad += "(a) the $($t361.Tab) band no longer holds a box at $($fr361.L),$($fr361.Y) $($fr361.W)x$($fr361.H) where b5111c35 authored '$($fr361.T)'$alt361 - the request that took the sub-tabs out said the top box does not move, so a band that slid without a line naming the ask is the one change that rearrangement was told not to make (SPEC I117a, I118, V361a)"
            continue
        }
        if ($null -ne $via361.Ask) { $v361ResizeHit["$($fr361.F)|$($fr361.T)"] = $via361.Ask }
        $v361BandSeen++
        $ttl361 = $hit361.SelectSingleNode("label")
        $now361 = if ($null -ne $ttl361) { $ttl361.GetAttribute("text") } else { '' }
        if ($now361 -eq $fr361.T) { continue }
        $ex361 = @($V361_EXCEPT | Where-Object { $_.F -eq $t361.F -and $_.Was -eq $fr361.T -and $_.Now -eq $now361 })
        if ($ex361.Count -eq 0) {
            $v361Bad += "(c) the box at $($fr361.L),$($fr361.Y) of the $($t361.Tab) band read '$($fr361.T)' at b5111c35 and reads '$now361' now, and no line of the exception list names that swap - an exception without a name is not an exception, it is the door every box leaves through (SPEC V361c, V351d)"
        } else {
            $v361ExceptHit["$($t361.F)|$($fr361.T)"] = $now361
        }
    }

    # (d) the gesture, recorded per tab and COMPARED between them below. Nothing here is measured
    # against a literal on purpose: V221 already charges the Ghoul its 5 and V298 charges the
    # Numina columns theirs, and a third owner for one number is B70. What has NO owner is the two
    # tabs AGREEING, and that is the whole of (d) - one request, one drawing, two files.
    if ($cols361.Count -ge 2) {
        $tops361 = @($cols361 | ForEach-Object { [int]$_.GetAttribute("top") } | Sort-Object -Unique)
        $gaps361 = @()
        for ($i361 = 1; $i361 -lt $cols361.Count; $i361++) {
            $gaps361 += ([int]$cols361[$i361].GetAttribute("left")) - ([int]$cols361[$i361-1].GetAttribute("left")) - ([int]$cols361[$i361-1].GetAttribute("width"))
        }
        $bandLow361 = 0
        foreach ($b361 in $band361) {
            $bb361 = [int]$b361.GetAttribute("top") + [int]$b361.GetAttribute("height")
            if ($bb361 -gt $bandLow361) { $bandLow361 = $bb361 }
        }
        $v361Gesture += [pscustomobject]@{
            Tab     = $t361.Tab
            Tops    = $tops361.Count
            Gutters = @($gaps361 | Sort-Object -Unique)
            Hidden  = @($cols361 | Where-Object { $_.GetAttribute("visible") -eq 'false' }).Count
            Clear   = ($tops361.Count -eq 1 -and $bandLow361 -gt 0 -and $tops361[0] -ge $bandLow361)
            Low     = $bandLow361
            Top     = $tops361[0]
        }
    }
}
# (d) the comparison. Two tabs, one drawing: the same single gutter, one shared top each, none
# hidden, and each opening clear of its own band. A tab that drifts on any of the four is the
# second shape of the same gesture, and it is invisible to every per-tab ruler here.
if ($v361Gesture.Count -eq 2) {
    $g361a = $v361Gesture[0]
    $g361b = $v361Gesture[1]
    if (($g361a.Gutters -join ',') -ne ($g361b.Gutters -join ',')) {
        $v361Bad += "(d) the $($g361a.Tab) columns are $($g361a.Gutters -join '/')px apart and the $($g361b.Tab) ones $($g361b.Gutters -join '/')px - two tabs rearranged by one request draw their columns the same way, and a gutter that drifts on one side is a second shape for one gesture (SPEC V361d, V308)"
    }
    foreach ($g361 in $v361Gesture) {
        if ($g361.Tops -ne 1) { $v361Bad += "(d) the $($g361.Tab) columns open at $($g361.Tops) different tops - they are one row of columns and share one, the way the other tab's do (SPEC V361d, I118a)" }
        elseif (-not $g361.Clear) { $v361Bad += "(d) the $($g361.Tab) columns open at $($g361.Top) and its band closes at $($g361.Low) - the columns sit BELOW the top box in both tabs, and one opening inside the band is the arrangement the request replaced (SPEC V361a, V361d, I117a)" }
        if ($g361.Hidden -ne 0) { $v361Bad += "(d) $($g361.Hidden) of the $($g361.Tab) columns are authored hidden and none of the other tab's are - all three are on screen at once in both, which is the whole of pedidos 4 and 5 (SPEC V361d, I117, I118d)" }
    }
}
# (c) the list is CLOSED at one, and the one it holds has to be REACHED. An exception nobody
# reaches is an exception that stopped measuring, which is the shape V240 already guards its own
# stretched-box carve-out with (SPEC V209, B7).
if ($V361_EXCEPT.Count -ne 1) { $v361Bad += "(c) the exception list holds $($V361_EXCEPT.Count) entr(y/ies) and V361(c) names exactly one - the willpower block leaving Numina. A second needs a new Q, or the list becomes the door (SPEC V361c, V362d, V351d)" }
# A named resize that nobody reaches is a name outliving its reader - the same shape leg (c)
# guards its own list with, and the reason V240 guards its carve-out (SPEC V209, B7).
foreach ($rz361 in $V361_RESIZED) {
    if (-not $v361ResizeHit.ContainsKey("$($rz361.F)|$($rz361.T)")) {
        $v361Bad += "(a) $($rz361.Ask) names '$($rz361.T)' as resized to $($rz361.L),$($rz361.Y) $($rz361.W)x$($rz361.H) in $($rz361.F) and the box does not read that rect - either the ask was not built or the line is stale, and a stale line excuses the next slide (SPEC V209, V20)"
    }
}
foreach ($ex361 in $V361_EXCEPT) {
    if (-not $v361ExceptHit.ContainsKey("$($ex361.F)|$($ex361.Was)")) {
        $v361Bad += "(c) the named exception '$($ex361.Was)' -> '$($ex361.Now)' in $($ex361.F) was never reached - the frozen rect still reads '$($ex361.Was)', so either the block the user asked to move is still there or this list is describing a swap that did not happen (SPEC V361c, I117d, V209)"
    }
}
# (e) zero-guard over (a)..(d): every leg above walks a set, and a set that came back empty makes
# all of them pass over nothing (SPEC V20, B7).
if ($v361Gesture.Count -ne 2) { $v361Bad += "(e) $($v361Gesture.Count) of the 2 rearranged tabs were read - leg (d) compares two drawings and cannot compare one (SPEC V209, V20)" }
if ($v361Cols -eq 0) { $v361Bad += "(e) no column was found in either tab - legs (a) and (d) passed over an empty set (SPEC V20, B7)" }
if ($V361_BAND.Count -eq 0) { $v361Bad += "(e) the frozen band contract is empty - leg (a) would certify any band at all (SPEC V209)" }
if ($v361BandSeen -eq 0) { $v361Bad += "(e) not one frozen band rect was matched - leg (c) reads the titles of boxes this run never found (SPEC V20, B7)" }
if ($v361Bad) { foreach ($b in $v361Bad) { Fail "V361 $b" } }
else { Pass "V361 both tabs that lost their sub-tabs lost them the same way - $v361Cols columns over 2 tabs, each roster answered by name, none hidden and none still named for selectTab; all $($V361_BAND.Count) band rects frozen at b5111c35 are still authored, $($v361ResizeHit.Count) of them at a rect their own T-line names and the rest exactly as frozen, with the one named title swap (WILLPOWER -> TRUE FAITH) reached; and the two tabs agree on the drawing - one top each, $($v361Gesture[0].Gutters -join '/')px gutters on both, all three visible, both opening clear of their band" }

# ---- V223: nothing calls a `local function` from above the line declaring it ------
# Lua resolves a non-local name at COMPILE time, so a body written above the `local` that
# names it compiles to a GETGLOBAL and calls nil at runtime. That is invisible to every
# verifier this round has: `rdk -l` exits 0 because an undefined global is legal Lua, and
# the other 443 checks read structure, never order. It cost the user the Max Discipline
# Level label, dead in BOTH paths while the gate stayed green (SPEC B51, V223).
#
# Measured on the 73rd: 24 local functions on the root form and exactly ONE violation in
# the whole sheet - the one B51 is about. So this check ships with zero teeth left, by
# design: it is a regression guard, not an audit. The convention it defends is measured,
# not invented - a function called from ANOTHER .lfm is global, an internal helper is
# local - so the fix for a hit here is to move the CALLER down, never to promote the
# helper (that spends a name in a namespace shared with every plugin Firecast loads).
$orderBad = @()
$orderSeen = 0
foreach ($f in $files) {
    $ls = [System.IO.File]::ReadAllLines($f.FullName)
    $decl = @{}
    for ($i = 0; $i -lt $ls.Count; $i++) {
        $m = [regex]::Match($ls[$i], '^\s*local function ([A-Za-z_][A-Za-z0-9_]*)')
        if ($m.Success -and -not $decl.ContainsKey($m.Groups[1].Value)) { $decl[$m.Groups[1].Value] = $i }
    }
    foreach ($nm in $decl.Keys) {
        $orderSeen++
        for ($i = 0; $i -lt $decl[$nm]; $i++) {
            if ($ls[$i] -match '^\s*(--|<!--)') { continue }   # prose, not a call
            if ($ls[$i] -match ('\b' + $nm + '\s*\(')) {
                $orderBad += "$($f.Name):$($i + 1) calls $nm(), which is local from line $($decl[$nm] + 1) down"
            }
        }
    }
}
if ($orderSeen -lt 20) { Fail "V223 only $orderSeen local function(s) were collected - the root form alone had 24, so this check is measuring almost nothing (SPEC V209)" }
elseif ($orderBad.Count) { foreach ($b in $orderBad) { Fail "V223 $b - Lua compiles that as a GETGLOBAL and it is nil at runtime (SPEC B51)" } }
else { Pass "V223 all $orderSeen local functions are declared above every call to them" }

# ---- V224: the DOMINATOR box is a GRID, not four controls that happen to line up ---
# Two label columns and two input columns, both rows on the same four (SPEC I30). The
# failure this guards is drift, not a wrong pixel: B12 was sibling templates whose entry x
# fell out of step (Header edit@90 vs HeaderPicker combo@112) and B13 a title centred 5px
# off, and both survived rounds because every width and position check here reads ONE
# control at a time. Nothing measured a column, so nothing saw the column break.
#
# The header label is excluded by spanning the full box width - it is a caption, not a cell.
$gridDoc = Doc (Join-Path $dir "WoD20.11.lfm")
$gridBox = $gridDoc.SelectSingleNode("//layout[label/@name='lblMaxDisc']")
if ($null -eq $gridBox) { Fail "V224 the DOMINATOR box is gone from WoD20.11 - nothing left to measure (SPEC V209)" }
else {
    $boxW = [int]$gridBox.GetAttribute("width")
    $placed = @()
    foreach ($c in $gridBox.ChildNodes) {
        if ($c.NodeType -ne "Element" -or $c.LocalName -eq "rectangle") { continue }
        if (-not $c.HasAttribute("left")) { continue }
        # The caption, told the way V27 and V240 tell a title since I73 (103rd round, SPEC T618):
        # it is the CENTRED label taking most of the width, not one that spans the box edge to
        # edge. With a 20px margin on all four sides "width == box" matches nothing, and the
        # caption would be counted as a ninth cell of a grid that has eight.
        $capW = 0
        if ($c.LocalName -eq 'label' -and $c.GetAttribute("horzTextAlign") -eq 'center' -and
            [int]::TryParse($c.GetAttribute("width"), [ref]$capW) -and $capW -ge ($boxW * 0.8)) { continue }
        $placed += $c
    }
    # A declared MIRROR is not a second cell (SPEC I71, V274b, V36): the typed twin of a picker
    # is authored on the SAME rectangle as it and starts hidden, so exactly one of the pair is
    # ever on the grid. It leaves the count only while BOTH hold - authored invisible AND exactly
    # superimposed on one other control. An edit that drifts off the combo it mirrors, or that
    # someone makes visible, is counted again and this check reddens, which is what keeps it from
    # becoming the blanket exemption V20 refuses.
    $rectOf = { param($n) "$($n.GetAttribute('left'))|$($n.GetAttribute('top'))|$($n.GetAttribute('width'))|$($n.GetAttribute('height'))" }
    $cells = @()
    foreach ($c in $placed) {




        # The ? does not REPLACE the entry in the count, it STANDS for it: the cell opens where
        # the ? opens and the button behind it is the same cell, which is exactly how V297 reads
        # the affiliation entry (SPEC V297a, I107a1). Excluding the ? instead moved the column
        # from 135 to 160 and reported five columns in a four-column grid.
        $ledBy = @($placed | Where-Object {
            $_.GetAttribute("name") -like 'btnQ*' -and
            $_.GetAttribute("top") -eq $c.GetAttribute("top") -and
            ([int]$_.GetAttribute("left") + [int]$_.GetAttribute("width") + 5) -eq [int]$c.GetAttribute("left") })
        if ($ledBy.Count -gt 0) { continue }
        $twins = @($placed | Where-Object { -not [object]::ReferenceEquals($_, $c) -and (& $rectOf $_) -eq (& $rectOf $c) })
        if ($c.GetAttribute("visible") -eq "false" -and $twins.Count -eq 1) { continue }
        # The RIGHT edge of the cell, and for a ? it is the SPAN and not the button: the entry is
        # the ? plus the control it leads, which is the same reading V297(a) makes of the
        # affiliation row and the same one that keeps the column at 135 instead of 160 above.
        # Taking the button's own 20px here would compare the Name edit against a question mark
        # (SPEC V297a, I107a1, I136k).
        $rr224 = [int]$c.GetAttribute("left") + [int]$c.GetAttribute("width")
        if ($c.GetAttribute("name") -like 'btnQ*') {
            foreach ($led224 in $placed) {
                if ($led224.GetAttribute("top") -ne $c.GetAttribute("top")) { continue }
                if ([int]$led224.GetAttribute("left") -ne ([int]$c.GetAttribute("left") + [int]$c.GetAttribute("width") + 5)) { continue }
                $e224 = [int]$led224.GetAttribute("left") + [int]$led224.GetAttribute("width")
                if ($e224 -gt $rr224) { $rr224 = $e224 }
            }
        }
        $cells += [pscustomobject]@{ L = [int]$c.GetAttribute("left"); R = $rr224 }
    }
    $cols = $cells | Group-Object L | Sort-Object { [int]$_.Name }
    # TWO rows again since T972 and still one grid (SPEC I149b, V433): the LEFT pair - label at
    # 20, entry at 97 - and the RIGHT pair - 354, 520 - carry two rows each. So the count a
    # column has to show is its PAIR's - 2 and 2 on both sides - and a column off its partner's
    # count is one row that left the column, which is B12 again (the third row of T954 left in T972).
    $offGrid = @()
    if ($cols.Count -eq 4) {
        if ($cols[0].Count -ne 2 -or $cols[1].Count -ne 2) { $offGrid += "the left pair x=$($cols[0].Name)/$($cols[1].Name) is used by $($cols[0].Count)/$($cols[1].Count) control(s), not 2/2" }
        if ($cols[2].Count -ne 2 -or $cols[3].Count -ne 2) { $offGrid += "the right pair x=$($cols[2].Name)/$($cols[3].Name) is used by $($cols[2].Count)/$($cols[3].Count) control(s), not 2/2" }
    }
    # The right edge, asked of the SAME columns (SPEC I136k, user 2026-09-02 item 4). V224 has
    # measured where a column OPENS since B12 and never where it closes, so two entries could
    # share an x and end 18px apart - which is what Name and the Clan/Family picker did, and what
    # the owner asked to be rid of. It is one grid: both edges or neither.
    $offRight = @($cols | Where-Object { (@($_.Group | ForEach-Object { $_.R } | Sort-Object -Unique)).Count -ne 1 })
    if ($cells.Count -ne 8) { Fail "V224 the DOMINATOR box holds $($cells.Count) placed controls, expected 8 - two label/input pairs on each side (SPEC I30, I149b)" }
    elseif ($cols.Count -ne 4) { Fail "V224 the DOMINATOR box uses $($cols.Count) x positions, expected 4 - two label columns and two input columns (SPEC I30)" }
    elseif ($offGrid.Count) { Fail "V224 $($offGrid[0]) - a row left its column (SPEC B12, I145b)" }
    elseif ($offRight.Count) { Fail "V224 the column opening at x=$($offRight[0].Name) closes at $(@($offRight[0].Group | ForEach-Object { $_.R } | Sort-Object -Unique) -join ' and ') - one column, one right edge, or the rows only look aligned from the left (SPEC I136k, B12)" }
    else { Pass "V224 the DOMINATOR box is a grid on both edges: 10 controls on 4 shared columns, three rows on the left pair and two on the right ($(($cols | ForEach-Object { "$($_.Name)..$($_.Group[0].R)" }) -join ", "))" }
}

# ---- V225: the right rule of the Vampire tab, in two legs ------------------------
# (a) content fits its container, on BOTH AXES. The first cut of this check measured width
# only - it was written answering a report of a HORIZONTAL scrollbar - and the very next
# thing the user saw was the same scrollbar on the other axis: WoD20.14 closes at 675 in a
# tabsVamp of 650 (SPEC B52). The failure mode is "content bigger than container" and it
# has two axes; one of them was built. Worse, the Pass said "fit inside tabsVamp (1395)",
# which READS as "they fit" while having compared width alone - a green claiming more than
# it measured, which is V209, in a message that cited V209.
#
# The vertical inset is bigger than the horizontal one: it holds the row of tab BUTTONS,
# not just a border (SPEC R94). Bounded by deduction rather than guessed - WoD20.13 closes
# at 610 and shows no bar, so the client is at least 610 and the inset is at most 40.
#
# This was the one the user had to find by looking: WoD20.13
# closed at 1385 inside a tabsVamp of 1210 - 175px of overflow, shipped in the 65th round and
# green ever since, because every box here was measured ALONE and each one passed. Nothing
# compared a child form against the parent that has to hold it. Same shape as V224 one round
# earlier, one level out: there the columns inside a box, here the boxes inside a tab.
#
# (b) the ruler itself. Four boxes in four files close on one number that none of them
# declares, because the user asked for that alignment (SPEC I31) - the classic way a number
# drifts. Cheap to hold: the gate already has all four files open.
#
# AMENDED in the 159th round (SPEC V225 amended, T832): leg (a) is measured PER COLUMN. While the
# three panes were stacked they all shared one container, so the first one served for all three;
# side by side they have three DIFFERENT widths on purpose (I118d says not one box is redrawn), and
# reading them all against the first would fail columns 2 and 3 for being wider than column 1.
# AMENDED again in T909: the columns are no longer imported forms, so leg (a) reads the boxes
# inside each column instead of opening a second file for them (SPEC I136i, V397). The question
# it asks is unchanged - does the content fit the container it was given, on both axes.
$vampTop = Doc (Join-Path $dir "WoD20.11.lfm")
$bandTop225 = BandTop ($vampTop.SelectSingleNode("//scrollBox"))
$cols225 = @($vampTop.SelectNodes("//scrollBox/layout") | Where-Object { [int]$_.GetAttribute("top") -gt $bandTop225 })
if ($cols225.Count -eq 0) { Fail "V225 no column below the band in WoD20.11 - there is no container left to measure against (SPEC V209)" }
else {
    $tvW = ($cols225 | ForEach-Object { [int]$_.GetAttribute("width") } | Measure-Object -Maximum).Maximum
    $tvH = ($cols225 | ForEach-Object { [int]$_.GetAttribute("height") } | Measure-Object -Maximum).Maximum
    $over = @()
    $edges = @{}
    $subsSeen = 0

    foreach ($col in $cols225) {
        $sub = ColId361 $col
        if (-not $sub) { $sub = "the column at x=$($col.GetAttribute('left'))" }
        $subsSeen++
        # the container is THIS column, never the first one on the tab
        $cW = [int]$col.GetAttribute("width")
        $cH = [int]$col.GetAttribute("height")
        $widest = 0
        $lowest = 0
        foreach ($l in $col.SelectNodes("layout")) {
            $r = [int]$l.GetAttribute("left") + [int]$l.GetAttribute("width")
            $b = [int]$l.GetAttribute("top") + [int]$l.GetAttribute("height")
            if ($r -gt $widest) { $widest = $r }
            if ($b -gt $lowest) { $lowest = $b }
            if ($l.SelectSingleNode("textEditor[starts-with(@name,'edt') and contains(@name,'Desc')]")) { $edges[$sub] = $r }
        }
        if (-not $widest) { $over += "$sub holds no box - leg (a) measured nothing there (SPEC V209)" }
        elseif ($widest -gt $cW) { $over += "$sub closes at x=$widest inside its own column of $cW wide - $($widest - $cW)px of overflow is a horizontal scrollbar (SPEC I31)" }
        elseif ($lowest -gt $cH) { $over += "$sub ends at y=$lowest inside its own column of $cH tall - $($lowest - $cH)px of overflow is a vertical scrollbar (SPEC B52)" }
    }

    foreach ($l in $vampTop.SelectNodes("//scrollBox/layout")) {
        if ($l.SelectSingleNode("imageCheckBox[@field='bloodPool_1']")) {
            $edges["WoD20.11.lfm (BLOOD POOL)"] = [int]$l.GetAttribute("left") + [int]$l.GetAttribute("width")
        }
    }

    # LEG (b) RETIRED in the 159th round (SPEC V225 amended, T872) and it comes BACK in T832,
    # named on that task's own row. The ruler was FOUR boxes agreeing on one x - the three
    # DESCRIPTION panes and BLOOD POOL - and wave 3 took the three panes away, leaving one.
    # Agreement measured on a single box is the count-guard reading its own floor. MEASURED
    # 2026-08-31: there is no second candidate today either, the three sub-tabs closing at
    # 390 / 565 / 520, so pointing (b) at them would be a ruler born red against correct code
    # (B106). Leg (a) is untouched and is the one that keeps a scrollbar out of the tab.
    $ruler = @($edges.Values | Sort-Object -Unique)
    if ($subsSeen -lt 3) { Fail "V225 only $subsSeen Ghoul column(s) were read, expected 3 - leg (a) is covering less than the tab holds (SPEC V209)" }
    elseif ($over.Count) { foreach ($o in $over) { Fail "V225 $o" } }
    elseif ($edges.Count -gt 1 -and $ruler.Count -ne 1) { Fail "V225 the $($edges.Count) boxes close on $($ruler.Count) different x ($($ruler -join ', ')) - they are one ruler (SPEC I31): $(($edges.GetEnumerator() | ForEach-Object { "$($_.Key)=$($_.Value)" }) -join '; ')" }
    else { Pass "V225 (a) each of the $subsSeen Ghoul columns fits its OWN container on both axes, widest $($tvW)x$($tvH); leg (b) retired for good in T832, and since T909 there is no second file to disagree with either (I136i)" }
}

# ---- V226: the strip is themed BY CONSTRUCTION -----------------------------------
# Every button is a <rectangle> plus a <label>, and that is not a tag preference. The theme
# repaints THEME_SHAPES = { rectangle } for fill and stroke and THEME_TEXTS = { label,
# checkBox, button, comboBox, edit, textEditor, radioButton } for fontColor, so rectangle +
# label is the only pair it reaches on BOTH ends. <button> is in THEME_TEXTS for its
# fontColor alone - its face stays host-drawn, and the tabs would go back to clashing with
# the sheet, which is the whole thing I32 was asked to fix.
$stripBad = @()
$btnSeen = 0
foreach ($pair in @(,@("WoD20th.lfm", "tabStrip", 11))) {
    $sDoc = Doc (Join-Path $dir $pair[0])
    $strip = $sDoc.SelectSingleNode("//layout[@name='$($pair[1])']")
    if ($null -eq $strip) { $stripBad += "$($pair[0]) has no $($pair[1]) - the tabs have no strip (SPEC I32)"; continue }

    foreach ($bad in $strip.SelectNodes(".//button | .//tab | .//tabControl")) {
        $stripBad += "$($pair[0]) $($pair[1]) holds a <$($bad.LocalName)> - the theme cannot paint its face (SPEC V226)"
    }

    $btns = @($strip.SelectNodes("rectangle[@onClick]"))
    if ($btns.Count -ne $pair[2]) { $stripBad += "$($pair[1]) offers $($btns.Count) button(s), expected $($pair[2])" }
    foreach ($b in $btns) {
        $btnSeen++
        # ONE label on the top bar, TWO on a sub-tab since the 109th round: the sub-tab says its
        # state in colour as well as weight, and one label cannot carry two colours without a Lua
        # write applyTheme undoes (SPEC I78c, V57, V305). What this leg measures is that the word
        # is a <label> the theme reaches - never how many the pair needs (SPEC B67).
        $lbl = @($b.SelectNodes("label"))
        $lblWant = if ($pair[1] -eq 'tabStrip') { 1 } else { 2 }
        if ($lbl.Count -ne $lblWant) { $stripBad += "$($b.GetAttribute('name')) carries $($lbl.Count) label(s), expected $lblWant - the word is a label the theme paints on both ends, and on a sub-tab it is the pair V305 measures" }
        else {
            foreach ($l226 in $lbl) {
                if ($l226.GetAttribute("hitTest") -ne 'false') { $stripBad += "$($b.GetAttribute('name'))'s label '$($l226.GetAttribute('name'))' is hit-testable - it would eat the click meant for the rectangle" }
            }
        }
    }
}
if ($btnSeen -lt 11) { Fail "V226 only $btnSeen strip button(s) were read, expected 11 - this check is covering less than the sheet has (SPEC V209)" }
elseif ($stripBad) { foreach ($b in $stripBad) { Fail "V226 $b" } }
else { Pass "V226 all $btnSeen tab buttons are a rectangle over a label - one on the top bar, the pair of V305 on a sub-tab - the one pair the theme reaches on both ends" }

# ---- V227: which tab is open is SESSION state, never a field ---------------------
# A field on a Firecast sheet syncs to every client at the table - that is what makes
# dataLink worth having (SPEC V175). Store the open tab in one and the player clicking
# Combat drags the storyteller off whatever they were reading: a two-person bug that never
# shows up testing alone, which is the worst kind to ship. It is also irreversible in the
# other direction, because a field name is for ever (SPEC I3, V2).
$tabStateBad = @()
foreach ($f in $files) {
    foreach ($n in (Doc $f.FullName).SelectNodes("//layout[@name='tabStrip']//*[@field]")) {
        $tabStateBad += "$($f.Name) $($n.LocalName) owns field '$($n.GetAttribute("field"))' inside the strip"
    }
}
$rootNoCom = NoComments $rootTxt
if ($rootNoCom -match 'setField\(\s*"(activeTab|SUB_TABS|tabIndex)') { $tabStateBad += "setField writes the open tab - it would sync to every client at the table" }
if ($rootNoCom -notmatch '(?m)^\s*activeTab = ') { $tabStateBad += "activeTab is not a plain Lua global - session state has nowhere else to live" }
if ($rootNoCom -match '(?m)^\s*SUB_TABS = ') { $tabStateBad += "SUB_TABS is back on the root form - T830 took the last sub-tab off the sheet and the table has no group left to hold, so a table here is state for a strip that does not exist (SPEC I117, V268 retired, B55)" }
foreach ($orphan in @('activeTab')) {
    if ($allFields.ContainsKey($orphan)) { $tabStateBad += "'$orphan' is owned by $($allFields[$orphan] -join ', ') as a FIELD" }
}
if ($tabStateBad) { foreach ($b in $tabStateBad) { Fail "V227 $b" } }
else { Pass "V227 the open tab lives in Lua, not in a field - one reader moving does not move the others" }

# ---- V228: a tab button has an INTERIOR, not just a width ------------------------
# cornerType="innerRound" curves the border INWARD, so the drawable interior is
# width - 2*xradius, and the V16 ruler has always measured the raw width (SPEC B53). The
# arc is concave - the disc of radius r centred on the corner is OUTSIDE the shape - so at
# height y from the top the edge sits at x = sqrt(r*r - y*y): about 8.7px per side at the
# top of a glyph on a 30px pill, 0 at mid-height (SPEC R96). Nothing is CLIPPED, which is
# why no overflow check ever saw it; the title simply touches the curve and reads cramped.
#
# The radius is priced at its MAXIMUM (2*r = 28, the bite at the vertex) rather than the ~17
# measured at glyph height, and the ~11px of slack that buys is deliberate: $PX_PER_CHAR is
# one constant while the theme swaps the font family, and Georgia, Book Antiqua and Times
# New Roman all run wider than Segoe UI (SPEC R96 leaves that open for the Firecast test).
#
# The radius is CHECKED here and not assumed, because the formula is written in terms of it:
# let xradius drift and the requirement loosens silently along with the thing it measures.
# V68 cannot cover that - it reads //rectangle[@color='black'] and the pills are DimGray
# since I33.
$tabWide = @()
$tabBtnSeen = 0
foreach ($pair in @(,@("WoD20th.lfm", "tabStrip"))) {
    $strip228 = (Doc (Join-Path $dir $pair[0])).SelectSingleNode("//layout[@name='$($pair[1])']")
    if ($null -eq $strip228) { $tabWide += "$($pair[0]) has no $($pair[1]) - there is no button to measure (SPEC I32)"; continue }

    foreach ($btn in $strip228.SelectNodes("rectangle[@onClick]")) {
        $bn  = $btn.GetAttribute("name")
        $lbl = $btn.SelectSingleNode("label[@text]")
        if ($null -eq $lbl) { $tabWide += "$bn carries no titled label - there is no text to measure it against"; continue }
        $tabBtnSeen++
        $txt = $lbl.GetAttribute("text")

        # The pill is measured too: it is the SHOWN half of the pair, so a narrow tabOn
        # would clip the very state this sheet uses to say which tab is open.
        $pill = $strip228.SelectSingleNode("rectangle[@name='tabOn$($bn.Substring(6))']")
        if ($null -eq $pill) { $tabWide += "$bn has no tabOn twin - the open tab would have nothing to fill it (SPEC I33)"; continue }

        foreach ($r in @($btn, $pill)) {
            $rn = $r.GetAttribute("name")
            if ($r.GetAttribute("cornerType") -ne 'innerRound') { $tabWide += "$rn is cornerType='$($r.GetAttribute('cornerType'))', not innerRound - the width below is priced for the concave arc (SPEC R96)"; continue }
            $rx = 0; $ry = 0; $rw = 0
            [void][int]::TryParse($r.GetAttribute("xradius"), [ref]$rx)
            [void][int]::TryParse($r.GetAttribute("yradius"), [ref]$ry)
            [void][int]::TryParse($r.GetAttribute("width"), [ref]$rw)
            if ($rx -ne 14 -or $ry -ne 14) { $tabWide += "$rn carries radius $rx/$ry, not the house 14/14 - the formula is written in terms of the radius, so it would loosen with it"; continue }
            $txtPx = NeededPx $txt
            $need  = $txtPx + 2 * $rx + 12
            if ($rw -lt $need) { $tabWide += "$rn is $($rw)px wide for '$txt' - it needs $need ($txtPx of text + $(2 * $rx) of arc + 12 of clearance) or the title touches the curve (SPEC B53)" }
        }
    }
}
if ($tabBtnSeen -lt 11) { Fail "V228 only $tabBtnSeen tab button(s) were measured, expected 11 - this check is covering less than the sheet has (SPEC V209)" }
elseif ($tabWide) { foreach ($t in $tabWide) { Fail "V228 $t" } }
else { Pass "V228 all $tabBtnSeen tab buttons clear the concave arc - width >= text + 2*radius + 12" }

# ---- V229: the strip has a themed floor, and the open tab may not share its colour
# The 34px strip band was the ONE region of the sheet with no paper behind it - the thirteen
# themePaper images all live in the IMPORTED forms and the root has none - so it showed the
# host background of form theme="dark" and belonged to no period at all (SPEC I33).
#
# The cure is the house idiom rather than a new mechanism: an align="client" rectangle as the
# first child, which is exactly how every section box on this sheet is built, so applyTheme
# already repaints it per period for free. It must be FIRST because the buttons are absolute
# and paint over it in document order; authored later it would bury them.
#
# And the pills must NOT be black once the floor is. That failure is silent and total: a black
# pill on a black floor breaks no build, reddens no other check, and opens the sheet with no
# tab looking open. Two different palette keys is what makes the contrast survive all four
# periods without anyone picking a colour per period by hand (SPEC V53, V57).
$stripSkin = @()
$strip229 = (Doc $rootPath).SelectSingleNode("//layout[@name='tabStrip']")
if ($null -eq $strip229) { $stripSkin += "tabStrip is gone from the root form - the tabs have no strip (SPEC I32)" }
else {
    $els229 = @($strip229.ChildNodes | Where-Object { $_.NodeType -eq 'Element' })
    $cli229 = @($els229 | Where-Object { $_.GetAttribute('align') -eq 'client' })
    if ($cli229.Count -ne 1) { $stripSkin += "tabStrip holds $($cli229.Count) align='client' child(ren), expected exactly the floor (SPEC I33, V190)" }
    elseif ($cli229[0].LocalName -ne 'rectangle') { $stripSkin += "the strip floor is a <$($cli229[0].LocalName)> - only a rectangle is in THEME_SHAPES (SPEC V226)" }
    elseif ($els229[0].GetAttribute('align') -ne 'client') { $stripSkin += "the strip floor is not the first child - authored after the buttons it would paint over them" }
    elseif ($cli229[0].GetAttribute("color") -ne 'black') { $stripSkin += "the strip floor is filled '$($cli229[0].GetAttribute('color'))', not black - it would not read as one of the section boxes (SPEC I33)" }
    else {
        # The floor is SQUARE by request (user, 2026-08-22, after seeing the 79th round on
        # screen), and it is the only square rectangle on the sheet - all 99 others author a
        # radius. A section box is an object resting on the paper and the cut corner is what
        # lifts it off; the floor is not an object but the ground itself, spanning the band
        # end to end with nothing behind it, so a concave corner there just bites four notches
        # out of the screen. V68 stepped aside for exactly this rectangle, so the corner is
        # checked HERE - stepping aside and checking nowhere would be the hole.
        foreach ($a in @('xradius', 'yradius', 'cornerType')) {
            $av = $cli229[0].GetAttribute($a)
            if ($av) { $stripSkin += "the strip floor authors $a='$av' - it is square by request (SPEC I33) and V68 gave up its corner on that basis" }
        }
    }
}
# The pills' half of this moved out in the 108th round. The open tab is no longer a DimGray
# lozenge - it is a foot rule the marker draws itself (SPEC I77b) - so what used to be measured
# here is measured by V301b, in the shape the marker has now. The FLOOR stays: it is the same
# rectangle, black and square, and V68 still steps aside for it. Two owners for one fact is the
# no-op V20 forbids, which is why this half left rather than being rewritten in both places.
if ($stripSkin) { foreach ($t in $stripSkin) { Fail "V229 $t" } }
else { Pass "V229 the strip floor is black and square - V301b carries the other half now, that the open tab cannot wear the floor's own colour" }
# ---- V230: the credit to the base plugin is ONE reference, not a block ----------------
# The user cut the roll-call down to a single line (SPEC C, 2026-08-22). Two ways that
# can rot, and they pull in opposite directions: the block grows back, or the line gets
# swept along with the brand and the box ends up crediting nobody. V10 and V28 do not
# catch the second one - they demand a key for a visible string that EXISTS, and a
# deleted label is not a visible string, so an empty box sails through both of them
# green. That is the B7 shape, and it is why leg (c) is written out here.
$V230_EN = 'Based on the sheet plugin by Vinny (Ambesek)'
$V230_PT = 'Baseado no plugin de fichas de Vinny (Ambesek)'
$v230Src = @()
foreach ($f in $files) { $v230Src += ,@($f.Name, [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName))) }
$v230Src += ,@("module.xml", [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $plugin "module.xml"))))
$v230Lang = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($langFile))
$v230Src += ,@("localization.lang", $v230Lang)
if ($files.Count -ne 12) { Fail "V230 read $($files.Count) .lfm, expected 12 - the check would pass having looked at less (SPEC V209). Was 15 until T909 (SPEC I136i)" }
elseif ($v230Lang.Length -eq 0) { Fail "V230 localization.lang read empty - every needle below would pass by vacuum (SPEC V209)" }
else {
    # (a) the brand is gone outright - no exception, comments included
    $v230Brand = @()
    foreach ($p in $v230Src) {
        $n = ([regex]::Matches($p[1], 'RPGmeister')).Count
        if ($n -gt 0) { $v230Brand += "$($p[0]) still carries the brand name x$n" }
    }
    if ($v230Brand) { foreach ($b in $v230Brand) { Fail "V230 $b" } }
    else { Pass "V230 the brand name is gone from all $($v230Src.Count) source files, comments included" }

    # (b) the author is named ONLY inside the two canonical strings. Subtract them and
    # count what is left: a fixed occurrence count would break the day the PT map or the
    # .lang changes shape, and the ask was "only in that credit", not "exactly N times".
    $v230Loose = @()
    foreach ($p in $v230Src) {
        $rest = $p[1].Replace($V230_EN, '').Replace($V230_PT, '')
        $n = ([regex]::Matches($rest, 'Ambesek')).Count
        if ($n -gt 0) { $v230Loose += "$($p[0]) names the author $n time(s) outside the credit line" }
    }
    if ($v230Loose) { foreach ($b in $v230Loose) { Fail "V230 $b" } }
    else { Pass "V230 the author is named only inside the one credit line" }

    # (c) and that line really is in all four places V10/V22/V28 expect it to be
    $v230Txt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20.6.lfm")))
    $v230Gone = @()
    if ($v230Txt.IndexOf('text="' + $V230_EN + '"') -lt 0) { $v230Gone += "the label is gone from WoD20.6.lfm" }
    if ($v230Txt.IndexOf('["' + $V230_EN + '"] = "' + $V230_PT + '"') -lt 0) { $v230Gone += "the PT map entry is gone from WoD20.6.lfm" }
    if ($v230Lang.IndexOf('wod.' + $V230_EN + '=' + $V230_PT) -lt 0) { $v230Gone += "the [pt] key is gone from localization.lang" }
    if ($v230Lang.IndexOf('wod.' + $V230_EN + '=' + $V230_EN) -lt 0) { $v230Gone += "the [en] key is gone from localization.lang" }
    if ($v230Gone) { foreach ($b in $v230Gone) { Fail "V230 credit $b - an empty box would pass V10 and V28 by vacuum (SPEC B7)" } }
    else { Pass "V230 the one credit line stands in all four places (label, PT map, [pt], [en])" }
}

# ---- V231: what is LEFT - the strip band is symmetric by construction ------------
# Legs (a) and (c) were REVOKED in the 106th round (SPEC I74a, V286). The content gap the
# 82nd round asked for is now ZERO on the eleven top-level tabs, so "the strip breathes LESS
# than the content it heads" reads 15 < 0 and cannot hold; and the gap itself is no longer
# one number for fourteen forms, so it moved to V286, which measures it BY SCOPE.
#
# The band stays, and it is the half that never depended on the content: 2*gap + pill, so the
# space above the pill equals the space below by construction and neither can slide without
# the other lighting up (SPEC I73, V281). The floor rectangle is skipped for free - it carries
# no name, while all 22 pills and buttons do.
$gapBad = @()
$stripGap = -1
$pillH = -1
$strip231 = (Doc $rootPath).SelectSingleNode("//layout[@name='tabStrip']")
if ($null -eq $strip231) { $gapBad += "tabStrip is gone from the root form - there is no strip to measure (SPEC I32)" }
else {
    $rects231 = @($strip231.SelectNodes("rectangle[@name]"))
    $rTops = @($rects231 | ForEach-Object { $_.GetAttribute("top") } | Sort-Object -Unique)
    $bandH = 0; [void][int]::TryParse($strip231.GetAttribute("height"), [ref]$bandH)
    if ($rects231.Count -ne 22) { $gapBad += "tabStrip holds $($rects231.Count) named rectangles, expected 22 - eleven pills over eleven buttons" }
    elseif ($rTops.Count -ne 1) { $gapBad += "the strip rectangles sit at top $($rTops -join ', ') - a pill and its button share one geometry" }
    else {
        [void][int]::TryParse($rTops[0], [ref]$stripGap)
        [void][int]::TryParse($rects231[0].GetAttribute("height"), [ref]$pillH)
        if ($bandH -ne (2 * $stripGap + $pillH)) { $gapBad += "the strip band is $($bandH)px for a gap of $stripGap over a $($pillH)px pill - it must be $(2 * $stripGap + $pillH), so the space above equals the space below" }
    }
}
if ($gapBad) { foreach ($b in $gapBad) { Fail "V231 $b" } }
else { Pass "V231 the strip band is $(2 * $stripGap + $pillH)px - $stripGap above and below a $($pillH)px pill, symmetric by construction" }

# ---- V286: the top gap, BY SCOPE (SPEC I74a, user 2026-08-25) ---------------------
# ONE number again since T909. The eleven tabs that hang off the top menu open at ZERO - the
# box meets the black band and there is no seam. There used to be a second group: the three
# SUB-tab forms hanging off vampStrip, which kept the 20 they had so a sub-tab was not glued to
# its own sub-bar. WoD20.12/.13/.14 WERE that group and they are gone - dissolved into WoD20.11
# because each was painting its own theme ground under the paper (SPEC I136i, V397, B128).
#
# The two groups are DERIVED from the root form - a tab layout is one that imports a file -
# so a tab added later falls into the right group without anyone editing a list here. Naming
# the eleven would go stale exactly once, silently, and the zero-guard is what says so.
# The 0 gained a DECLARED roster on 2026-09-02 (SPEC Q53, I140q, V286 amended): the owner asked
# for a 10px gap above the first row of Numina and Ghoul, and REFUSED the one-node answer that
# would have given it to all eleven. Still an EQUALITY on both sides - `= 0` or `= the declared
# value` - and never `>= 0`, which is the defect leg (a) was written not to have.
#
# A roster line may not name 0: a line that changes nothing is a dead line, and a dead line is what
# excuses the next tab to slide (SPEC V20, V328d).
$TAB_TOP_GAP = @{ 'WoD20.7.lfm' = 10; 'WoD20.11.lfm' = 10 }
$v286Bad = @()
$v286Top = @($(Doc $rootPath).SelectNodes("//layout[@name][import/@file]") | ForEach-Object { $_.SelectSingleNode("import").GetAttribute("file") })
if ($v286Top.Count -ne 11) {
    Fail "V286 the root form imports $($v286Top.Count) tab form(s), expected 11 - the two groups below would be split by a ruler that stopped matching (SPEC V209, I32)"
} else {
    $v286Seen = 0
    $v286Sub = 0
    foreach ($f in $files) {
        $sb = ContentRoot (Doc $f.FullName)
        if ($null -eq $sb) { continue }
        $tops = @()
        foreach ($c in $sb.ChildNodes) {
            if ($c.NodeType -ne 'Element') { continue }
            $tv = 0
            if ([int]::TryParse($c.GetAttribute("top"), [ref]$tv)) { $tops += $tv }
        }
        if (-not $tops.Count) { continue }
        $v286Seen++
        $min = ($tops | Measure-Object -Minimum).Minimum
        if ($v286Top -contains $f.Name) {
            # (a) top-level: EXACTLY 0, and something has to sit on the ruler. ">= 0" is true
            # of every sheet ever authored and would pass a tab that opened at 200.
            $want286 = 0
            if ($TAB_TOP_GAP.ContainsKey($f.Name)) { $want286 = $TAB_TOP_GAP[$f.Name] }
            if ($min -ne $want286) { $v286Bad += "$($f.Name) hangs off the top menu and opens its first box at top=$min, not the $want286 its scope asks for (SPEC I74a, V286a, I140q)" }
        } else {
            # (b) sub-tab: still 20. Their content answers to vampStrip, not to the top menu.
            $v286Sub++
            if ($min -ne 20) { $v286Bad += "$($f.Name) is a sub-tab form and opens at top=$min, not the 20 it keeps - only the eleven that hang off the top menu went to zero (SPEC I74a, V286b)" }
        }
    }
    if ($v286Seen -ne 11) { $v286Bad += "only $v286Seen content form(s) were measured, expected 11 - this leg is covering less than the sheet has (SPEC V209)" }
    # LEG (b) RETIRED in T909, and what replaces it is its own complement. The group is empty, and
    # a rule with nothing under it is a rule that can no longer fail (SPEC V20, B7) - so the count
    # is inverted: a content form the root does NOT import is a file nothing on the sheet opens,
    # which is the control-level orphan V2 refuses for fields.
    if ($v286Sub -ne 0) { $v286Bad += "$v286Sub content form(s) are imported by no tab - since T909 every form hangs off the top menu, so a form outside that list is one nothing opens (SPEC I136i, V209, V286b retired)" }
    if ($v286Bad) { foreach ($b in $v286Bad) { Fail "V286 $b" } }
    else { Pass "V286 all 11 content forms hang off the top menu and open at top=0 - leg (b) retired with the sub-tab forms in T909" }
}

# ---- V287: the strip floor has no contour, in any era (SPEC I74b) ----------------
# applyTheme paints strokeColor and a 3px strokeSize onto every THEME_SHAPES rectangle whose
# authored colour the palette knows, and all four stroke maps key "black" - so the band was
# getting the same 3px rule the 73 section boxes wear (SPEC I5). The user asked for that rule
# gone and the fill kept.
#
# The cure is authored rather than special-cased in the Lua, and it is proof against the
# getter: what a rectangle with no authored stroke hands back cannot be settled from disk
# (SPEC R25), but with #00000000 authored, `authored()` returns THAT in either case - and it
# then either paints transparent (the key is there) or falls out of paint's no-mapped-target
# guard (SPEC V61). Invisible down both paths.
#
# The palette side is NOT re-checked here: V53 already demands every authored strokeColor be
# a key in all four stroke maps, and a second owner for one fact is the no-op V20 forbids.
# BOTH directions, though: the floor has it, and nothing else does. Without the second the
# idiom leaks onto a section box and silently kills its gold fillet.
$v287Bad = @()
$strip287 = (Doc $rootPath).SelectSingleNode("//layout[@name='tabStrip']")
if ($null -eq $strip287) { $v287Bad += "tabStrip is gone from the root form (SPEC I32)" }
else {
    $floor287 = $strip287.SelectSingleNode("rectangle[@align='client']")
    if ($null -eq $floor287) { $v287Bad += "tabStrip has no align='client' floor to measure (SPEC I33, V229)" }
    elseif ($floor287.GetAttribute("strokeColor") -ne '#00000000') {
        $v287Bad += "the strip floor authors strokeColor='$($floor287.GetAttribute('strokeColor'))' - it must author #00000000, or applyTheme hands it the 3px rule every black rectangle gets (SPEC I74b, V53)"
    }
}
$v287Others = @()
foreach ($f in $files) {
    foreach ($n287 in (Doc $f.FullName).SelectNodes("//*[@strokeColor='#00000000']")) {
        if ($f.Name -eq 'WoD20th.lfm' -and $n287.LocalName -eq 'rectangle' -and $n287.GetAttribute("align") -eq 'client' -and $n287.ParentNode.GetAttribute("name") -eq 'tabStrip') { continue }
        # 108th round: the 19 tab buttons lost their box and author the same transparent contour
        # (SPEC I77a, V301a). They are admitted by NAME, so the idiom still cannot spread to a
        # section box - which is the direction this leg exists to guard.
        if ($n287.LocalName -eq 'rectangle' -and $n287.GetAttribute("name") -like 'btnTab*') { continue }
        # 109th round: the 19 markers and the 5 separators carry a path and paint nothing of their
        # own, so they author the same transparent contour (SPEC I78a, I78d). Admitted by NAME, so
        # the second direction below still catches the idiom leaking onto a section box.
        if ($n287.LocalName -eq 'rectangle' -and ($n287.GetAttribute("name") -like 'tabOn*' -or $n287.GetAttribute("name") -like 'sep*')) { continue }
        # 113th round: the avatar's ornament carrier (SPEC I84d, V309). It is transparent in BOTH
        # senses because the photo has to show through it - it holds no content, it only gives the
        # filigree somewhere to hang above the picture. Admitted by its exact NAME, not a wildcard:
        # it is one control, and the second direction below - a section box authoring a transparent
        # contour - stays exactly as strict as it was.
        # 168th round: popScrim (SPEC B136, V404, I140a). It authors the transparent contour for
        # the same reason the strip floor does - to keep the era's 3px rule off it - and it needs it
        # more than any of them: it is 1595x936, so the rule it was silently taking was a frame around
        # the WHOLE sheet, lit on every picker and every ?. Admitted by exact NAME, so the second
        # direction below stays as strict as it was.
        if ($n287.LocalName -eq 'rectangle' -and $n287.GetAttribute("name") -eq 'ornAvatar') { continue }
        if ($n287.LocalName -eq 'rectangle' -and $n287.GetAttribute("name") -eq 'popScrim') { continue }
        $v287Others += "$($f.Name) <$($n287.LocalName) name='$($n287.GetAttribute('name'))'>"
    }
}
if ($v287Others.Count) {
    $v287Bad += "a transparent contour is authored outside the strip floor, the 19 buttons, the 19 markers, the 5 separators, the avatar carrier and popScrim, on $($v287Others -join ', ') - the era paints a rule on every black rectangle and this idiom silently erases it (SPEC I74b, I5)"
}
if ($v287Bad) { foreach ($b in $v287Bad) { Fail "V287 $b" } }
else { Pass "V287 the strip floor, the 19 buttons, the 19 markers, the 5 separators, the avatar carrier and popScrim are the only controls authoring a transparent contour, and the floor does author it" }

# ---- V288..V292: the five alignments the 106th round was asked for -----------------
# Every one of them is a RELATION between two things in the sheet, never a literal here: the
# 103rd round moved BACKGROUNDS and RESOURCES apart and no check noticed, because the only
# place that alignment was written down was a comment (SPEC V289). A literal would have gone
# stale the same way - and a relation is what makes the mutation bite from BOTH sides.
function BoxByTitle($doc, $title) {
    foreach ($b in $doc.SelectNodes("//scrollBox/layout")) {
        foreach ($lb in $b.SelectNodes("label")) {
            if ($lb.GetAttribute("text") -eq $title) { return $b }
        }
    }
    return $null
}
function BoxRect($b) {
    $l = 0; $t = 0; $w = 0; $h = 0
    [void][int]::TryParse($b.GetAttribute("left"), [ref]$l)
    [void][int]::TryParse($b.GetAttribute("top"), [ref]$t)
    [void][int]::TryParse($b.GetAttribute("width"), [ref]$w)
    [void][int]::TryParse($b.GetAttribute("height"), [ref]$h)
    return [pscustomobject]@{ L = $l; T = $t; W = $w; H = $h; R = $l + $w; B = $t + $h }
}

# ---- V288: the three ATTRIBUTES headings centre on the group they name -------------
# They were 7.5px low - all three by the same amount, since the 41st round. Nothing could see
# it: V27, V239, V240 and V280c all CUT a child carrying rotationAngle, because its authored
# box is the pre-rotation one and reading it as a margin reddens code that is right (SPEC B61).
# That cut is correct for margins and wrong to inherit here - what is useless as an edge is
# exactly what gives the CENTRE, because rotation is about the box's own middle.
$v288Bad = @()
$doc288 = Doc (Join-Path $dir "WoD20.1.lfm")
$box288 = BoxByTitle $doc288 "ATTRIBUTES"
if ($null -eq $box288) { Fail "V288 the ATTRIBUTES box was not found on WoD20.1 - this check measured nothing (SPEC V209)" }
else {
    $rot288 = @($box288.SelectNodes("label[@rotationAngle]") | ForEach-Object {
        $t = 0; $h = 0
        [void][int]::TryParse($_.GetAttribute("top"), [ref]$t)
        [void][int]::TryParse($_.GetAttribute("height"), [ref]$h)
        [pscustomobject]@{ Text = $_.GetAttribute("text"); T = $t; H = $h }
    } | Sort-Object T)
    $rows288 = @($box288.SelectNodes("layout") | ForEach-Object {
        $t = 0; $h = 0
        if ([int]::TryParse($_.GetAttribute("top"), [ref]$t) -and [int]::TryParse($_.GetAttribute("height"), [ref]$h)) {
            [pscustomobject]@{ T = $t; H = $h }
        }
    } | Sort-Object T)
    if ($rot288.Count -ne 3) { $v288Bad += "the box holds $($rot288.Count) rotated heading(s), expected 3 (SPEC V209)" }
    elseif ($rows288.Count -ne 9) { $v288Bad += "the box holds $($rows288.Count) attribute row(s), expected 9 - three groups of three (SPEC V209)" }
    else {
        for ($g = 0; $g -lt 3; $g++) {
            $first = $rows288[$g * 3]
            $last = $rows288[$g * 3 + 2]
            $groupMid = ($first.T + $last.T + $last.H) / 2
            # The extent alone is blind to the MIDDLE row: 41/70/91 centres exactly where
            # 41/66/91 does. One pitch per group is what makes that mutation bite.
            $mid = $rows288[$g * 3 + 1]
            if (($mid.T - $first.T) -ne ($last.T - $mid.T)) {
                $v288Bad += "the group under '$($rot288[$g].Text)' runs $($first.T)/$($mid.T)/$($last.T) - three rows on one pitch, or the centre it is measured against means nothing (SPEC V288)"
            }
            $labelMid = $rot288[$g].T + $rot288[$g].H / 2
            $off = [Math]::Abs($labelMid - $groupMid)
            if ($off -gt 0.5) {
                $v288Bad += "'$($rot288[$g].Text)' centres on y=$labelMid while its three rows centre on y=$groupMid - $($off)px out (SPEC V288)"
            }
        }
    }
    if ($v288Bad) { foreach ($b in $v288Bad) { Fail "V288 $b" } }
    else { Pass "V288 Physical, Social and Mental each centre on the three rows they name, within half a pixel" }
}

# ---- V289: BACKGROUNDS closes where RESOURCES closes -------------------------------
# The comment on WoD20.2 has claimed this since the 46th round and nothing enforced it, so
# the 103rd round moved both boxes and left them 67px apart with a green gate (SPEC B7).
$v289Bad = @()
$doc289 = Doc (Join-Path $dir "WoD20.2.lfm")
$bg289 = BoxByTitle $doc289 "BACKGROUNDS"
$rs289 = BoxByTitle $doc289 "OTHER"
if ($null -eq $bg289 -or $null -eq $rs289) { Fail "V289 BACKGROUNDS or OTHER was not found on WoD20.2 - this check measured nothing (SPEC V209)" }
else {
    $a = BoxRect $bg289; $b = BoxRect $rs289
    if ($a.B -ne $b.B) { Fail "V289 BACKGROUNDS closes at y=$($a.B) and OTHER at y=$($b.B) - the two columns of the Traits tab end on one line" }
    else { Pass "V289 BACKGROUNDS and RESOURCES both close on y=$($a.B)" }
}

# ---- V290: the Merit/Flaw tables - four columns, and the row they sit on -----------
# (a) one template serves both tables, so the column ORDER is asked of it once. Heading and
# field are checked as a PAIR: a heading that keeps its old x while the field moves is the
# failure that looks right in the XML and lies on screen.
# (b) the top row of the tab lands on the grid the bottom row already keeps.
$v290Bad = @()
$doc290 = Doc (Join-Path $dir "WoD20.2.lfm")
# T783 split the one template in two (SPEC I100a, I107a): the PICKED half carries the search
# BUTTON where the free half carries a plain edit, and the field of the picked half moved to a
# HIDDEN edit behind that button. So the columns are collected from what is VISIBLE - and the
# two templates then have to AGREE, because the twelve picked rows and the eight typed ones sit
# in ONE table and a seam between them is exactly what the eye catches first.
$colsBy290 = @{}
foreach ($tn290 in @('MeritPicked')) {
    $node290 = $doc290.SelectSingleNode("//template[@name='$tn290']")
    if ($null -eq $node290) { $v290Bad += "the $tn290 template is gone - the two tables have no shared column order left to check (SPEC V209)"; continue }
    # The ? is NOT a column of the table: it stands in the 20px gutter the name column gave
    # back and the entry x does not move for it (SPEC I102b, I102f). Read as a column it would
    # be a fifth one in the picked half that the typed half has no answer for, and the seam leg
    # below would redden on a table that lines up perfectly on screen.
    $colsBy290[$tn290] = @($node290.SelectNodes("edit|button") | Where-Object { $_.GetAttribute("visible") -ne "false" -and $_.GetAttribute("name") -notlike 'btnQ*' } | ForEach-Object {
        $l = 0; $w = 0
        [void][int]::TryParse($_.GetAttribute("left"), [ref]$l)
        [void][int]::TryParse($_.GetAttribute("width"), [ref]$w)
        $root290 = if ($_.HasAttribute("field")) { $_.GetAttribute("field") -replace '\$\(num\)$', '' }
                   elseif ($_.GetAttribute("name") -like 'dynMerit_*') { 'merit_' }
                   else { '?' }
        [pscustomobject]@{ L = $l; W = $w; Root = $root290 }
    } | Sort-Object L)
}
if ($colsBy290.Count -ge 2) {
    $sigP290 = (($colsBy290['MeritPicked'] | ForEach-Object { "$($_.Root)@$($_.L)+$($_.W)" }) -join ' ')
    $sigF290 = (($colsBy290['MeritFree'] | ForEach-Object { "$($_.Root)@$($_.L)+$($_.W)" }) -join ' ')
    if ($sigP290 -ne $sigF290) {
        $v290Bad += "MeritPicked tiles '$sigP290' and MeritFree tiles '$sigF290' - both halves draw rows of the SAME table and a column that moves in one is a seam in the middle of it (SPEC V290a, I100a)"
    }
}
$cols290 = @($colsBy290['MeritPicked'])
if ($cols290.Count -eq 0) { $v290Bad += "no visible column was collected from MeritPicked - the collector is reading the wrong node and every leg below would pass on the empty set (SPEC V209, V20, B7)" }
else {
    $want290 = @('merit_', 'book_', 'type_', 'costy_')
    if ($cols290.Count -ne 4) { $v290Bad += "MeritPicked draws $($cols290.Count) visible column(s), expected 4 (SPEC V209)" }
    elseif ((($cols290 | ForEach-Object { $_.Root }) -join ',') -ne ($want290 -join ',')) {
        $v290Bad += "the columns run $((($cols290 | ForEach-Object { $_.Root }) -join ' ')) - Book goes between the name and Page (SPEC I74, V290a)"
    } else {
        for ($i = 0; $i -lt 3; $i++) {
            if (($cols290[$i].L + $cols290[$i].W) -ne $cols290[$i + 1].L) {
                $v290Bad += "column '$($cols290[$i].Root)' closes at $($cols290[$i].L + $cols290[$i].W) and '$($cols290[$i + 1].Root)' opens at $($cols290[$i + 1].L) - the four columns tile with no seam (SPEC V290a)"
            }
        }
        foreach ($tbl in @('MERITS', 'FLAWS')) {
            $box = BoxByTitle $doc290 $tbl
            if ($null -eq $box) { $v290Bad += "the $tbl table was not found (SPEC V209)"; continue }
            $row = @($box.SelectNodes("layout"))[0]
            if ($null -eq $row) { $v290Bad += "the $tbl table draws no rows (SPEC V209)"; continue }
            $rowL = 0; [void][int]::TryParse($row.GetAttribute("left"), [ref]$rowL)
            $heads = @($box.SelectNodes("label") | ForEach-Object {
                $l = 0; $w = 0
                [void][int]::TryParse($_.GetAttribute("left"), [ref]$l)
                [void][int]::TryParse($_.GetAttribute("width"), [ref]$w)
                [pscustomobject]@{ Text = $_.GetAttribute("text"); L = $l; W = $w }
            })
            if ($heads.Count -ne 4) { $v290Bad += "the $tbl table carries $($heads.Count) heading(s) for 4 columns (SPEC V290a)"; continue }
            foreach ($c in $cols290) {
                $over = @($heads | Where-Object { $_.L -eq ($rowL + $c.L) -and $_.W -eq $c.W })
                if ($over.Count -ne 1) {
                    $v290Bad += "in $tbl no single heading stands over column '$($c.Root)' (x=$($rowL + $c.L), $($c.W) wide) - heading and field must move together (SPEC V290a)"
                }
            }
        }
    }
}
# Re-aimed in the 125th round: the user moved ALLIES/MENTOR/RESOURCES/GUIDES up into the hole
# FLAWS left, so the boxes standing under the top row are no longer the ones named here. The
# RULE is untouched - the top row lands on the grid the bottom row keeps - only its occupants
# moved, and both edges are checkable now where before only the right one was.
foreach ($pair in @(@('MERITS', 'FLAWS', 'LR'), @('MENTOR', 'FAME', 'LR'), @('DERANGEMENTS', 'ARTIFACTS', 'LR'))) {
    $top = BoxByTitle $doc290 $pair[0]
    $bot = BoxByTitle $doc290 $pair[1]
    if ($null -eq $top -or $null -eq $bot) { $v290Bad += "$($pair[0]) or $($pair[1]) was not found on WoD20.2 (SPEC V209)"; continue }
    $a = BoxRect $top; $b = BoxRect $bot
    if ($pair[2].Contains('L') -and $a.L -ne $b.L) { $v290Bad += "$($pair[0]) opens at x=$($a.L) and $($pair[1]) at x=$($b.L) (SPEC V290b)" }
    if ($a.R -ne $b.R) { $v290Bad += "$($pair[0]) closes at x=$($a.R) and $($pair[1]) at x=$($b.R) (SPEC V290b)" }
}
if ($v290Bad) { foreach ($b in $v290Bad) { Fail "V290 $b" } }
else { Pass "V290 Merit|Book|Page|Cost tile in that order under their own headings, and the top row lands on the grid the bottom row keeps" }

# ---- V291: the Background tab closes on two rulers, one per axis -------------------
$v291Bad = @()
$doc291 = Doc (Join-Path $dir "WoD20.4.lfm")
$pre291 = BoxByTitle $doc291 "PRELUDE"
$goal291 = BoxByTitle $doc291 "GOALS"
$head291 = @($doc291.SelectNodes("//scrollBox/layout"))[0]
if ($null -eq $pre291 -or $null -eq $goal291) { $v291Bad += "Prelude or Goals was not found on WoD20.4 (SPEC V209)" }
elseif ($null -eq $head291) { $v291Bad += "the identity header was not found on WoD20.4 (SPEC V209)" }
else {
    $p = BoxRect $pre291; $g = BoxRect $goal291; $h = BoxRect $head291
    if ($p.B -ne $g.B) { $v291Bad += "Prelude closes at y=$($p.B) and Goals at y=$($g.B) - the two columns end on one line (SPEC V291a)" }
    if ($h.R -ne $p.R) { $v291Bad += "the header closes at x=$($h.R) and Prelude at x=$($p.R) - the tab has one right edge (SPEC V291b)" }
    # and the width the header holds is spent on its columns, not on air at the end
    $cols291 = @($head291.SelectNodes("layout") | ForEach-Object {
        $l = 0; $w = 0
        [void][int]::TryParse($_.GetAttribute("left"), [ref]$l)
        [void][int]::TryParse($_.GetAttribute("width"), [ref]$w)
        [pscustomobject]@{ L = $l; W = $w }
    })
    $lefts291 = @($cols291 | ForEach-Object { $_.L } | Sort-Object -Unique)
    $widths291 = @($cols291 | ForEach-Object { $_.W } | Sort-Object -Unique)
    if ($lefts291.Count -ne 3) { $v291Bad += "the header holds $($lefts291.Count) column(s), expected 3 (SPEC V209)" }
    elseif ($widths291.Count -ne 1) { $v291Bad += "the header columns are $($widths291 -join '/') wide - the three share one width (SPEC V291b)" }
    else {
        $gut1 = $lefts291[1] - ($lefts291[0] + $widths291[0])
        $gut2 = $lefts291[2] - ($lefts291[1] + $widths291[0])
        $mL = $lefts291[0]
        $mR = $h.W - ($lefts291[2] + $widths291[0])
        if ($gut1 -ne $gut2) { $v291Bad += "the header gutters are $gut1 and $gut2 - the three columns are evenly spread or they are not (SPEC V291b)" }
        if ($mL -ne $mR) { $v291Bad += "the header leaves $mL on the left and $mR on the right - width the box gains goes to the columns, never to air at the end (SPEC V170, V291b)" }
    }
}
if ($v291Bad) { foreach ($b in $v291Bad) { Fail "V291 $b" } }
else { Pass "V291 Prelude closes with Goals on Y, the header closes with Prelude on X, and the 130px it gained went to its three columns" }

# ---- V292: the two Settings boxes close on one edge --------------------------------
$v292Bad = @()
$doc292 = Doc (Join-Path $dir "WoD20.6.lfm")
$boxes292 = @($doc292.SelectNodes("//scrollBox/layout"))
if ($boxes292.Count -ne 2) { $v292Bad += "the Settings tab holds $($boxes292.Count) section box(es), expected 2 - the ruler is one against the other and there is no literal to fall back on (SPEC V209)" }
else {
    $a = BoxRect $boxes292[0]; $b = BoxRect $boxes292[1]
    if ($a.R -ne $b.R) { $v292Bad += "the boxes close at x=$($a.R) and x=$($b.R) - the tab has one right edge (SPEC V292)" }
    foreach ($bx in $boxes292) {
        $r = BoxRect $bx
        $ends = @($bx.ChildNodes | Where-Object { $_.NodeType -eq 'Element' -and $_.LocalName -ne 'rectangle' -and $_.LocalName -ne 'dataLink' } | ForEach-Object {
            $l = 0; $w = 0
            if ([int]::TryParse($_.GetAttribute("left"), [ref]$l) -and [int]::TryParse($_.GetAttribute("width"), [ref]$w)) { $l + $w }
        })
        if (-not $ends.Count) { $v292Bad += "a Settings box holds nothing measurable (SPEC V209)"; continue }
        $far = ($ends | Measure-Object -Maximum).Maximum
        if (($r.W - $far) -ne 20) { $v292Bad += "a Settings box is $($r.W) wide and its widest child closes $($r.W - $far) short of the edge, not 20 - the width goes to the field, not to margin (SPEC V170, V292)" }
    }
}
if ($v292Bad) { foreach ($b in $v292Bad) { Fail "V292 $b" } }
else { Pass "V292 both Settings boxes close on one edge and each spends its width on its widest child" }

# ---- V293: hedgeStrip breathes the same on both sides, RETIRED (SPEC B64, I117) ------------
# It measured ONE bar - the only one on the sheet with content above AND below it - and asked that
# the gap be equal on the two sides, because B64 put hedgeStrip eight pixels INSIDE the row of
# boxes above it and nothing saw. T830 took hedgeStrip with the sub-tabs (SPEC I117), and there is
# no bar left anywhere with a neighbour on both sides: the root tabStrip has one side, exactly as
# numStrip and vampStrip did, and inventing a relation where the sheet has one term is the green
# over nothing this file calls B7.
#
# What still catches the shape B64 arrived in: V298 holds the gap between two section boxes at 5
# on BOTH axes, and V262(c) holds every row of boxes to one ruler - so a box sliding into its
# neighbour reddens on the number rather than on the symmetry.
if ($null -ne (Doc (Join-Path $dir 'WoD20.7.lfm')).SelectSingleNode("//layout[@name='hedgeStrip']")) { Fail "V293 hedgeStrip is back on WoD20.7 - T830 took it with the sub-tabs and this ruler retired with it, so the one bar with a neighbour on both sides returns UNMEASURED (SPEC B64, I117, B98, T830)" }
else { Pass "V293 retired with hedgeStrip in T830 - no bar on the sheet has content on both sides of it any more, and V298 holds the gap the symmetry used to stand in for" }

# ---- V296: the two rows of the Numina tab are ONE grid --------------------------------
# The list boxes open and close where HEDGE MAGIC does, and the two DESCRIPTIONs span from
# where QUINTESSENCE opens to where WILLPOWER closes (SPEC I75, user 2026-08-25). Until T642
# that was a COINCIDENCE - 450/470/1410 happened to line up and nothing measured it - so
# widening one box on its own would have left the gate green over a crooked screen, which is
# B52 on the other axis.
#
# Found by TITLE, never by coordinate: a coordinate is what T490 already proved fragile
# (SPEC V18), and the ruler here IS a coordinate. The XPaths are scoped to the pane that owns
# each box because tabPsychic carries a DESCRIPTION of its own, on its own ruler (SPEC I75).
#
# RETIRED in T874, the way V193 retired in T872 and for the same reason at the root: both legs of
# this rule are RELATIONS with the DESCRIPTION box as one term, and wave 4 took every DESCRIPTION
# box off this tab (SPEC V365d). Leg (b) lost both its subjects outright. Leg (a) lost its sense a
# step later but just as completely: with no column beside it the list box IS the row, so it opens
# at 0 and closes on the pane, and asking it to span what HEDGE MAGIC spans would now demand a
# 586-wide box in a 1410-wide row - a ruler that reddens correct code, which is B106.
#
# What still measures this: V262(c), which is the leg that survived the change rather than a
# replacement written for it - every row of boxes on the three panes opens at 0 and closes on the
# widest row's ruler, so a list box that shrank on its own still reddens. V267(a) keeps the top
# band of tabHedge on one base, which is where HEDGE MAGIC, QUINTESSENCE and WILLPOWER live.
#
# The guard is the retirement's own: a DESCRIPTION box back on any of the four panes would be the
# grid coming back with no ruler on it (SPEC V365d, B98).
$doc296 = Doc (Join-Path $dir "WoD20.7.lfm")
$v296Bad = @()
$n296 = @($doc296.SelectNodes("//layout[label/@text='DESCRIPTION']")).Count
if ($n296 -gt 0) { $v296Bad += "WoD20.7 holds $n296 DESCRIPTION box(es) again - wave 4 took them and this grid rule retired with them, so the box comes back UNMEASURED (SPEC V365d, V262c, B98, T874)" }
# The term that did NOT leave has to still be there, or the retirement note above is describing a
# tab that no longer exists (SPEC V209).
if ($null -eq @($doc296.SelectNodes("//scrollBox/layout[label/@text='HEDGE MAGIC']"))[0]) {
    $v296Bad += "the HEDGE MAGIC box is gone from tabHedge - V296 retired into V262(c) and V267(a), and V267(a) measures the band this box leads (SPEC V209, V267a)"
}
if ($v296Bad) { foreach ($b in $v296Bad) { Fail "V296 $b" } }
else { Pass "V296 retired with the four Numina panes in T874 - no pane holds a DESCRIPTION box, the list boxes are the whole row now and V262(c) is what holds them to it" }

# ---- V297: HEDGE MAGIC is a GRID, not three rows that happen to line up ----------------
# Same shape V224 measures on the DOMINATOR box, and for the same reason it exists there
# (SPEC B12, B13): three labels on one x, three entries on another, and the three entries
# CLOSING on one x. Until T645 the entries were 386/220/220, which read as a staircase -
# the user asked for that to go, and without a check the next round quietly brings it back.
#
# The mirror edtHedgeAffiliation is left OUT of the column count on purpose: it is the SAME
# rectangle as the combo it hides behind (SPEC I71, V274), so counting it would put two
# controls in one cell and make the grid look broken while it is right.
# Found under the scrollBox since T830: tabHedge went with the sub-tabs and the band is a direct
# child of the form (SPEC I117, V361a). By TITLE as before - a coordinate is what T490 proved
# fragile (SPEC V18) - and the grid inside the box did not move a pixel.
$box297 = @($doc296.SelectNodes("//scrollBox/layout[label/@text='HEDGE MAGIC']"))[0]
$v297Bad = @()
if ($null -eq $box297) {
    Fail "V297 the HEDGE MAGIC box was not found by title on WoD20.7 - this check measured nothing (SPEC V209, B7)"
} else {
    $lab297 = @()
    foreach ($nm in @('Affiliation', 'Essence', 'Casting Attribute')) {
        $n = @($box297.SelectNodes("label[@text='$nm']"))[0]
        if ($null -ne $n) { $lab297 += ,@($nm, [int]$n.GetAttribute("left"), [int]$n.GetAttribute("width")) }
    }
    # An ENTRY is whatever the row puts in the entry column, and since T874 half B one of the
    # three is TWO widgets: the affiliation is a picker and its ? leads the entry the way the
    # BACKGROUNDS picker's does (SPEC I107a1, I117, T874). So an entry is read as the SPAN of its
    # widgets - min(left) to max(left+width) - which is one reader and not a special case: for a
    # single-widget row the span IS the widget. Measuring the button alone would report the entry
    # opening at 205 and call the grid broken while it is right.
    $ent297 = @()
    # pscustomobject and NOT an array of arrays: @('a', @('x','y')) FLATTENS to three elements in
    # PowerShell, so the roster would hand the loop a string where it expects the xpath list and
    # the leg would measure nothing (SPEC B7 - the same flattening that cost T830 two runs).
    foreach ($grp297 in @(
        [pscustomobject]@{ N = 'affiliation'; X = @("button[@name='dynhedgeAffiliation']") }
        [pscustomobject]@{ N = 'essence';     X = @("edit[@field='hedgeEssence']") }
        [pscustomobject]@{ N = 'attribute';   X = @("comboBox[@name='cboHedgeAttr']") }
    )) {
        $lo297 = [int]::MaxValue; $hi297 = [int]::MinValue
        foreach ($xp in $grp297.X) {
            $n = @($box297.SelectNodes($xp))[0]
            if ($null -eq $n) { continue }
            $l297 = [int]$n.GetAttribute("left")
            if ($l297 -lt $lo297) { $lo297 = $l297 }
            if (($l297 + [int]$n.GetAttribute("width")) -gt $hi297) { $hi297 = $l297 + [int]$n.GetAttribute("width") }
        }
        # The span is computed into a variable and NOT written inside the ,@(...) literal: the
        # unary comma binds looser than the minus there, so PowerShell reads it as (the array)
        # MINUS $lo297 and dies on op_Subtraction over Object[] - green code, wrong parse.
        if ($hi297 -ne [int]::MinValue) {
            $w297 = $hi297 - $lo297
            $ent297 += ,@($grp297.N, $lo297, $w297)
        }
    }
    if (($lab297.Count + $ent297.Count) -lt 6) {
        Fail "V297 only $($lab297.Count) label(s) and $($ent297.Count) entr(y/ies) of the six the grid needs were found - this check is covering less than the box has (SPEC V209)"
    } else {
        # (a) two shared columns
        $lx = @($lab297 | ForEach-Object { $_[1] } | Sort-Object -Unique)
        $ex = @($ent297 | ForEach-Object { $_[1] } | Sort-Object -Unique)
        if ($lx.Count -ne 1) { $v297Bad += "the three labels sit on $($lx.Count) different x ($($lx -join ', ')) - a grid shares one label column (SPEC V297a)" }
        if ($ex.Count -ne 1) { $v297Bad += "the three entries sit on $($ex.Count) different x ($($ex -join ', ')) - a grid shares one entry column (SPEC V297a)" }
        # (b) the three entries close on one x
        $er = @($ent297 | ForEach-Object { $_[1] + $_[2] } | Sort-Object -Unique)
        if ($er.Count -ne 1) {
            $v297Bad += "the three entries close on $($er.Count) different x ($($er -join ', ')) - shrinking one on its own reopens the staircase the user asked to remove (SPEC V297b)"
        }
        if ($v297Bad) { foreach ($b in $v297Bad) { Fail "V297 $b" } }
        else { Pass "V297 HEDGE MAGIC is a grid - labels on x=$($lx[0]), entries on x=$($ex[0]), all three closing on $($er[0])" }
    }
}

# ---- V232: a hidden tab leaves no HOLE in the strip -----------------------------------
# The storyteller can switch Numina and Ghoul off and both sit in the MIDDLE of the bar, so
# hiding one used to leave a gap exactly where its button had been. The strip reflows now:
# the cursor only advances past a tab that is shown (SPEC I36, user 2026-08-22).
#
# Three legs. (a) TAB_W is one number per TAB_LIST name, in that order, and it equals the
# width AUTHORED on both rectangles of the pair - there is no second truth about how wide a
# tab is. (b) the authored left chain is exactly what the reflow produces with all eleven
# shown, so the file is not born lying about the ruler the Lua applies to it. (c) the left
# write lives in the SAME loop as the visible write and the cursor advances only when the
# tab is wanted - position and state written together, the shape V112/V162 ask for.
#
# The widths are NOT read back off the control at run time: writing geometry from Lua is
# proved on this sheet (WoD20.9 writes width and height), reading it is not, and a nil
# coming back would scatter an invalid left across twenty-two rectangles in silence.
$reflowBad = @()
$tabNames = @()
$tabW = @()
$stripX0 = -1
$stripGapPx = -1
if ($rootTxt -match '(?s)>>> TAB_LIST_BEGIN.*?TAB_LIST\s*=\s*\{(.*?)\}') {
    $tabNames = @([regex]::Matches($Matches[1], '"(\w+)"') | ForEach-Object { $_.Groups[1].Value })
} else { $reflowBad += "the root form declares no TAB_LIST between its markers (SPEC I32)" }
if ($rootTxt -match '(?s)>>> TAB_W_BEGIN(.*?)<<< TAB_W_END') {
    $wBlk = $Matches[1]
    if ($wBlk -match '(?s)TAB_W\s*=\s*\{(.*?)\}') { $tabW = @([regex]::Matches($Matches[1], '\d+') | ForEach-Object { [int]$_.Value }) }
    if ($wBlk -match 'STRIP_X0\s*=\s*(\d+)') { $stripX0 = [int]$Matches[1] }
    if ($wBlk -match 'STRIP_GAP\s*=\s*(\d+)') { $stripGapPx = [int]$Matches[1] }
} else { $reflowBad += "the root form declares no TAB_W block between its markers - the reflow has no ruler (SPEC I36)" }

$tabPairSeen = 0
$strip232 = (Doc $rootPath).SelectSingleNode("//layout[@name='tabStrip']")
if ($null -eq $strip232) { $reflowBad += "tabStrip is gone from the root form (SPEC I32)" }
elseif (-not $reflowBad) {
    $btns232 = @($strip232.SelectNodes("rectangle[@onClick]"))
    if ($tabNames.Count -ne 11) { $reflowBad += "TAB_LIST names $($tabNames.Count) tab(s), expected 11" }
    elseif ($tabW.Count -ne $tabNames.Count) { $reflowBad += "TAB_W holds $($tabW.Count) width(s) for $($tabNames.Count) name(s) in TAB_LIST - one number per tab (SPEC V232a)" }
    elseif ($btns232.Count -ne $tabNames.Count) { $reflowBad += "the strip draws $($btns232.Count) button(s) for $($tabNames.Count) name(s) in TAB_LIST" }
    elseif ($stripX0 -lt 0 -or $stripGapPx -lt 0) { $reflowBad += "STRIP_X0 / STRIP_GAP are not declared beside TAB_W - the chain has no origin (SPEC V232b)" }
    else {
        $chainX = $stripX0
        for ($i = 0; $i -lt $tabNames.Count; $i++) {
            $tn = $tabNames[$i]
            # Document order must BE TAB_LIST order: the reflow lays the bar out in that order,
            # so a strip authored in another one would scramble on the first toggle.
            if ($btns232[$i].GetAttribute("name") -ne "btnTab$tn") {
                $reflowBad += "strip button $($i + 1) is $($btns232[$i].GetAttribute('name')) where TAB_LIST puts $tn - the reflow lays the bar out in TAB_LIST order"
                continue
            }
            $pill232 = $strip232.SelectSingleNode("rectangle[@name='tabOn$tn']")
            if ($null -eq $pill232) { $reflowBad += "tabOn$tn is missing - the pair is what the reflow moves (SPEC I33)"; continue }
            foreach ($r in @($btns232[$i], $pill232)) {
                $rn232 = $r.GetAttribute("name")
                $rw232 = -1
                $rl232 = -1
                [void][int]::TryParse($r.GetAttribute("width"), [ref]$rw232)
                [void][int]::TryParse($r.GetAttribute("left"), [ref]$rl232)
                if ($rw232 -ne $tabW[$i]) { $reflowBad += "$rn232 is $($rw232)px wide against TAB_W[$($i + 1)]=$($tabW[$i]) - the Lua and the XML disagree about how wide a tab is (SPEC V232a)" }
                if ($rl232 -ne $chainX) { $reflowBad += "$rn232 sits at left=$rl232 where the chain puts it at $chainX - the file must be the state the reflow produces with all eleven shown (SPEC V232b)" }
            }
            $chainX = $chainX + $tabW[$i] + $stripGapPx
            $tabPairSeen++
        }
    }
}
$vis232 = LuaFn $rootTxt 'applyTabVisibility'
if (-not $vis232) { $reflowBad += "applyTabVisibility is gone - nothing reflows the strip (SPEC I8b)" }
else {
    if ($vis232 -notmatch 'btn\.left\s*=\s*x' -or $vis232 -notmatch 'on\.left\s*=\s*x') { $reflowBad += "applyTabVisibility does not write the left of BOTH halves of the pair - the pill would stay behind its button (SPEC V232c)" }
    if ($vis232 -notmatch 'x\s*=\s*x\s*\+\s*TAB_W\[i\]\s*\+\s*STRIP_GAP') { $reflowBad += "the cursor does not advance by TAB_W[i] + STRIP_GAP - the runtime chain would not be the one the XML declares (SPEC V232b)" }
    # Leg (c) is STRUCTURAL, not a regex over the text: a lazy .{0,N} match reads straight
    # through the end; that closes the block, so the first version went GREEN on the very
    # mutation it exists to catch - the cursor advancing for a tab that is hidden. Walk the
    # lines and count block depth instead (SPEC V20: a check that cannot fail is a no-op).
    $vLines = $vis232 -split "`n"
    $iWant = -1
    for ($k = 0; $k -lt $vLines.Count; $k++) { if ($vLines[$k] -match '^\s*if want then\s*$') { $iWant = $k; break } }
    if ($iWant -lt 0) { $reflowBad += "applyTabVisibility has no if-want block around the left write - the cursor cannot be conditional on a tab being shown (SPEC V232c)" }
    else {
        $depth232 = 1
        $moveInside = $false
        for ($k = $iWant + 1; $k -lt $vLines.Count -and $depth232 -gt 0; $k++) {
            if ($vLines[$k] -match '^\s*end;\s*$') { $depth232--; continue }
            if ($vLines[$k] -match 'x\s*=\s*x\s*\+\s*TAB_W\[i\]') { $moveInside = $true }
        }
        if (-not $moveInside) { $reflowBad += "the cursor advances OUTSIDE the if-want block - a hidden tab would keep reserving its slot, which is the hole this invariant exists to close (SPEC V232c)" }
    }
}
if ($tabPairSeen -lt 11) { Fail "V232 only $tabPairSeen tab pair(s) were measured, expected 11 - this check is covering less than the strip has (SPEC V209)" }
elseif ($reflowBad) { foreach ($b in $reflowBad) { Fail "V232 $b" } }
else { Pass "V232 the strip reflows - TAB_W matches the XML on all 11 pairs, the left chain closes at $chainX, and the cursor moves only on a shown tab" }


# ---- V238: the storyteller's box has an ORDER, not just contents ----------------------
# Two legs, both of them things the user asked for out loud on 2026-08-22 and neither of
# them derivable from anything else the gate measures. (a) the two flags that hide a TAB
# come first: they are the settings that change what the table can see at all, and they had
# drifted to third and fourth under a pricing rule. (b) saving the initial character is
# last, after everything: it is the one irreversible action on this sheet (SPEC R30).
#
# This is not taste and it is not V40 in another suit. Without a check, both legs come
# undone the first time somebody adds a row in the middle - which is exactly how this box
# grew four times already (SPEC I8d, I8f, I8g).
$orderBad = @()
$stBox = BoxOf (Doc (Join-Path $dir "WoD20.10.lfm")) "STORYTELLER SETTINGS"
$stChecks = @()
if ($null -eq $stBox) { $orderBad += "WoD20.10 has no STORYTELLER SETTINGS box - the settings the tab exists for are gone (SPEC I8)" }
else {
    $stChecks = @($stBox.SelectNodes("checkBox[@name][@top]"))
    $stInputs = @($stBox.SelectNodes("checkBox[@top] | button[@top] | comboBox[@top] | edit[@top] | textEditor[@top] | radioButton[@top]"))
    if ($stChecks.Count -lt 7) { $orderBad += "the box holds $($stChecks.Count) named checkBox(es), expected at least the SEVEN flags it carries - five until the 93rd round, plus the two manual-entry flags of the 94th (SPEC I49, I51, I71, V209)" }
    else {
        $ranked = @($stChecks | Sort-Object { [int]$_.GetAttribute("top") })
        $firstTwo = @($ranked[0].GetAttribute("name"), $ranked[1].GetAttribute("name")) | Sort-Object
        $wantTwo = @('chkShowDisciplines', 'chkShowNumina')
        if (($firstTwo -join ',') -ne ($wantTwo -join ',')) {
            $orderBad += "the box opens with $($firstTwo -join ' and '), not the two Show flags - the settings that hide a tab were asked to come first (SPEC I35)"
        }
        $lowest = @($stInputs | Sort-Object { [int]$_.GetAttribute("top") })[-1]
        if ($lowest.GetAttribute("name") -ne 'btnSaveBaseline') {
            $orderBad += "the last input in the box is $($lowest.LocalName) '$($lowest.GetAttribute('name'))' at top=$($lowest.GetAttribute('top')) - Save Initial Character was asked to come after everything (SPEC I35)"
        }
    }
}
if ($stChecks.Count -lt 7) { Fail "V238 only $($stChecks.Count) flag(s) were read in the storyteller box, expected 7 or more - this check is covering less than the box has (SPEC V209)" }
elseif ($orderBad) { foreach ($b in $orderBad) { Fail "V238 $b" } }
else { Pass "V238 the storyteller box opens with the two Show flags and ends on Save Initial Character" }


# ---- V234: healthLevels has ONE owner, and it lives on the storyteller's tab ----------
# How long the health track is was never the player's call, and until the 82nd round it was
# two combos on one field - a declared mirror (SPEC I3, V36) reachable from Main and from
# Combat. It moved to the storyteller's box on the user's ask (SPEC I35).
#
# Three legs. (a) exactly one input widget owns the field, and it is in WoD20.10 - which is
# also what retires the mirror declaration, so V36's stale half now guards the way back.
# (b) neither HEALTH box carries a combo on it any more. (c) the HEALTH title of both boxes
# spans its box with left=0, so horzTextAlign centres it on the BOX rather than on the
# 215px it was left with when the combo shared the row. V27 cannot hold (c) alone: it only
# looks at labels spanning >=80% of their box, so a title narrowed back to 215 drops out of
# its reach and reads off-centre in silence - which is the whole of what item 13 was.
$hlBad = @()
$hlOwners = @()
foreach ($f in $files) {
    foreach ($n in (Doc $f.FullName).SelectNodes("//*[@field='healthLevels']")) {
        if ($n.LocalName -eq 'dataLink') { continue }
        $hlOwners += "$($f.Name)/$($n.LocalName)"
    }
}
if ($hlOwners.Count -ne 1) { $hlBad += "healthLevels is owned by $($hlOwners.Count) widget(s) [$($hlOwners -join ', ')] - it is one storyteller setting, not a mirror (SPEC V1, V36)" }
elseif ($hlOwners[0] -notlike 'WoD20.10.lfm/*') { $hlBad += "healthLevels is owned in $($hlOwners[0]) - the setting belongs to the storyteller's tab (SPEC I35)" }

foreach ($pair in @(@("WoD20.1.lfm", "dynHealth_box", 330), @("WoD20.3.lfm", "dynHealth3_box", 320))) {
    $hDoc = Doc (Join-Path $dir $pair[0])
    foreach ($c in $hDoc.SelectNodes("//comboBox[@field='healthLevels']")) {
        $hlBad += "$($pair[0]) still carries a $($c.LocalName) on healthLevels - the HEALTH boxes gave the dropdown up (SPEC I35)"
    }
    $hBox = $hDoc.SelectSingleNode("//layout[@name='$($pair[1])']")
    if ($null -eq $hBox) { $hlBad += "$($pair[0]) has no $($pair[1]) - there is no HEALTH box to measure"; continue }
    $hbw = 0; [void][int]::TryParse($hBox.GetAttribute("width"), [ref]$hbw)
    if ($hbw -ne $pair[2]) { $hlBad += "$($pair[1]) is $($hbw)px wide, expected $($pair[2]) - the title width below is written against it" }
    $hTitle = $hBox.SelectSingleNode("label[@text='HEALTH']")
    if ($null -eq $hTitle) { $hlBad += "$($pair[1]) carries no HEALTH title"; continue }
    $tl = -1; $tw = -1
    [void][int]::TryParse($hTitle.GetAttribute("left"), [ref]$tl)
    [void][int]::TryParse($hTitle.GetAttribute("width"), [ref]$tw)
    # SYMMETRY, not left=0 (103rd round, SPEC T618): since I73 the title spans what the 20px
    # margin leaves, so "left=0 and width=box" is no longer how this sheet says centred - it is
    # how it says wrong. What the leg is really for survives the change: a title narrowed back
    # to the 215 the combo left it with reads 20 on the left and 95 on the right and reddens
    # here, which is the silence item 13 was about.
    if ($tl -ne ($hbw - $tl - $tw)) { $hlBad += "$($pair[1])'s HEALTH title leaves $tl on the left and $($hbw - $tl - $tw) on the right in a $($hbw)px box - with the combo gone it has to sit on equal margins, or centring centres it on nothing (SPEC V27, I73)" }
    if ($hTitle.GetAttribute("horzTextAlign") -ne 'center') { $hlBad += "$($pair[1])'s HEALTH title is not horzTextAlign='center' - spanning the box does nothing on its own" }
}

# The two links that redraw each tab's own track are NOT owners and must stay (SPEC V123,
# B.3): they observe, and without them a change made on the storyteller's tab would leave
# the labels of the other two where they were.
foreach ($wf in @("WoD20.1.lfm", "WoD20.3.lfm")) {
    $wTxt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir $wf)))
    if ($wTxt -notmatch "dataLink fields=""\{'healthLevels', 'language'\}""") { $hlBad += "$wf no longer watches healthLevels - its track would keep the old length until the tab was reopened (SPEC V123)" }
}

if ($hlOwners.Count -lt 1) { Fail "V234 no widget owns healthLevels at all - the storyteller cannot set the track length (SPEC I35, V209)" }
elseif ($hlBad) { foreach ($b in $hlBad) { Fail "V234 $b" } }
else { Pass "V234 healthLevels is owned once, on the storyteller's tab, and both HEALTH titles span their box" }


# ---- V235: an empty XP log has the SHAPE of a full one --------------------------------
# The Type column used to be widened to 442 in both empty states so the state sentence could
# paint over its three empty neighbours. That is what made the tab read as broken with no
# purchases on it: the columns MOVED when there was nothing in them (SPEC I39, item 10 of
# the user's 2026-08-22 list).
#
# Two legs. (a) no column width is written from Lua at all, in any state. (b) the sentence
# has a widget of its own, authored hidden and toggled in BOTH directions - V33 and V98 are
# not being undone here, the sentence still gets said.
#
# The third leg was the box HEIGHT, written on every path through the renderer. It is gone
# with the growing box (SPEC I57, 90th round): there is no box height to write any more, and
# the column height that replaced it has one writer and one caller, which V261 measures. The
# two could not both be kept - this one demanded the write that V261a forbids - so it moved
# in the same commit rather than being left to fail correct code (SPEC B7).
#
# What stays here from that leg is the ONE empty branch: both empty states differ by their
# sentence and by nothing else, which is what makes them the same shape as a full log.
$logBad = @()
$logCode = CodeOf (Join-Path $dir "WoD20.9.lfm")
$logDoc = Doc (Join-Path $dir "WoD20.9.lfm")

if ($logCode -match 'XP_MSG_W') { $logBad += "XP_MSG_W is back - it is the width the Type column grew to in order to cover its neighbours (SPEC V235a)" }
foreach ($m in [regex]::Matches($logCode, '(?m)form\.(dynXp\w+)\.width')) {
    $logBad += "$($m.Groups[1].Value).width is written from Lua - a column that changes width between states is the whole of what item 10 reported (SPEC V235a)"
}

$emptyLbl = $logDoc.SelectSingleNode("//label[@name='dynXpEmpty']")
if ($null -eq $emptyLbl) { $logBad += "dynXpEmpty is gone - the state sentence would have to borrow a column again (SPEC V235b)" }
elseif ($emptyLbl.GetAttribute("visible") -ne 'false') { $logBad += "dynXpEmpty is authored visible - a log with rows in it would carry a sentence saying it has none (SPEC V235b)" }
if ($logCode -notmatch 'dynXpEmpty\.visible\s*=\s*true') { $logBad += "nothing ever shows dynXpEmpty - an empty log would say nothing at all (SPEC V33, V98)" }
if ($logCode -notmatch 'dynXpEmpty\.visible\s*=\s*false') { $logBad += "nothing ever hides dynXpEmpty - the sentence would sit over the first rows bought (SPEC V235b)" }
if ($logCode -notmatch 'if rows == nil or #rows == 0 then') { $logBad += "the two empty states are not one branch - they differ in their sentence and in nothing else, and splitting them is how one of them keeps a stale height (SPEC V235b)" }

$eCalls = @([regex]::Matches($logCode, '(?m)^\s*xpEmptyLog\('))
if ($eCalls.Count -ne 1) { $logBad += "xpEmptyLog is called $($eCalls.Count) time(s), expected once - both empty states go through the same call (SPEC V235b)" }

if ($null -eq $logDoc.SelectSingleNode("//textEditor[@name='dynXpType']")) { Fail "V235 WoD20.9 has no dynXpType column - this check is reading a tab that is not there (SPEC V209)" }
elseif ($logBad) { foreach ($b in $logBad) { Fail "V235 $b" } }
else { Pass "V235 the log keeps its four columns in every state - no width is written, and the sentence has a label of its own" }


# ---- V236: CLANS covers the whole picker and speaks only the picker's names -----------
# Filling Clan/Family fills the four clan Discipline slots (SPEC I37). The map that says
# which is CLANS - the table T430 froze from the books and left dormant, woken on the user's
# call in the 83rd round. The 82nd built a SECOND table of the same sixty entries before
# noticing this one (SPEC B55); it is gone, and this reads what was always here.
#
# Four legs. (a) one entry per clan the picker offers and not one more: clanFamily's list IS
# the clan list (SPEC B50). (b) every Discipline named is spelled as PICKER_LIST["disc"]
# spells it - a name off that list leaves the combo unable to resolve its own value and the
# slot opens EMPTY with nothing raised anywhere (the core writes Daimoinon, the picker offers
# Daimonion). (c) `fixed` never promises more names than there are slots. (d) an entry that
# carries `choice` or `open` is RELEASED by construction - the book leaves that slot to a
# pick or to the sire's clan - so released is DECLARED in the data rather than inferred from
# an absence, and no list of named exceptions has to be kept in step here.
$clanBad = @()
$clanEntries = @{}
$clanSeen = 0
if ($rootTxt -match '(?s)>>> CLANS_BEGIN(.*?)<<< CLANS_END') {
    $clansBlk = $Matches[1]
    foreach ($e in [regex]::Matches($clansBlk, '\["([^"]+)"\]\s*=\s*\{')) {
        $key = $e.Groups[1].Value
        # brace-match the entry: `fixed = {...}` nests, and a lazy [^}]* would cut it short
        $i = $e.Index + $e.Length
        $depth = 1
        while ($i -lt $clansBlk.Length -and $depth -gt 0) {
            if ($clansBlk[$i] -eq '{') { $depth++ } elseif ($clansBlk[$i] -eq '}') { $depth-- }
            $i++
        }
        $body = $clansBlk.Substring($e.Index + $e.Length, $i - 1 - ($e.Index + $e.Length))
        $fixed = @()
        if ($body -match 'fixed\s*=\s*\{([^}]*)\}') { $fixed = @([regex]::Matches($Matches[1], '"([^"]*)"') | ForEach-Object { $_.Groups[1].Value }) }
        $choice = @()
        if ($body -match 'choice\s*=\s*\{([^}]*)\}') { $choice = @([regex]::Matches($Matches[1], '"([^"]*)"') | ForEach-Object { $_.Groups[1].Value }) }
        $open = 0
        if ($body -match 'open\s*=\s*(\d+)') { $open = [int]$Matches[1] }
        # How many slots come OUT of `choice`, absent meaning one (SPEC I17, B65). The
        # `choice\s*=\s*\{` pattern above cannot see this one: choiceN takes a number, not a
        # table, so the two never read each other's value.
        $choiceN = 0
        if ($body -match 'choiceN\s*=\s*(\d+)') { $choiceN = [int]$Matches[1] }
        if ($clanEntries.ContainsKey($key)) { $clanBad += "CLANS names '$key' twice - the second entry is the one Lua keeps, silently" }
        $clanEntries[$key] = @{ fixed = $fixed; choice = $choice; open = $open; choiceN = $choiceN }
    }
} else { $clanBad += "the root form declares no CLANS between its markers - Clan/Family would fill nothing (SPEC I17, I37)" }

$clanList = @()
if ($PICKER.ContainsKey('clan')) { $clanList = @(@($PICKER['clan']) + @($PICKER['family']) | Where-Object { $_ }) }
$discSet = @{}
if ($PICKER.ContainsKey('disc')) { foreach ($d in $PICKER['disc']) { if ($d) { $discSet[$d] = $true } } }
$discRows = 0
if ($rootTxt -match 'CLAN_DISC_ROWS\s*=\s*(\d+)') { $discRows = [int]$Matches[1] }
$released = 0

if ($clanList.Count -lt 84) { $clanBad += "the clan picker offers $($clanList.Count) entries, expected at least the 84 it carries (61 clans + 23 revenant families) - this check would be reading less than the sheet has (SPEC V209)" }
elseif ($discSet.Count -lt 36) { $clanBad += "the disc picker offers $($discSet.Count) names, expected at least 36 - the spelling leg below would pass on a short list" }
elseif ($discRows -lt 1) { $clanBad += "CLAN_DISC_ROWS is not declared - there is no slot count to measure against" }
else {
    foreach ($c in $clanList) {
        if (-not $clanEntries.ContainsKey($c)) { $clanBad += "'$c' is offered by the picker and has no CLANS entry - filling Clan/Family with it would do nothing (SPEC V236a)"; continue }
        $clanSeen++
        $en = $clanEntries[$c]
        if ($en.fixed.Count -gt $discRows) { $clanBad += "'$c' fixes $($en.fixed.Count) Disciplines for $discRows slots (SPEC V236c)" }
        foreach ($d in ($en.fixed + $en.choice)) {
            if (-not $discSet.ContainsKey($d)) { $clanBad += "'$c' names '$d', which the disc picker does not offer - the slot would open EMPTY with nothing raised (SPEC V236b)" }
        }
        if ($en.choice.Count -gt 0 -or $en.open -gt 0) { $released++ }
        elseif ($en.fixed.Count -eq 0) { $clanBad += "'$c' settles nothing and declares neither choice nor open - released has to be said in the data, not left to an empty entry (SPEC V236d)" }
    }
    foreach ($k in $clanEntries.Keys) {
        if ($clanList -notcontains $k) { $clanBad += "CLANS carries '$k', which the clan picker does not offer (SPEC V236a)" }
    }
}
if ($clanSeen -lt 84) { Fail "V236 only $clanSeen clan(s) were measured, expected 84 - this check is covering less than the picker offers (SPEC V209)" }
elseif ($clanBad) { foreach ($b in ($clanBad | Select-Object -First 12)) { Fail "V236 $b" } }
else { Pass "V236 CLANS answers all 84 clans and families in the picker's own spelling, $released of them released by choice or open" }

# ---- V295: an entry PROMISES a number of slots, and that number has to fit ------------
# B65: `choice` never said how MANY slots it opens, so renderClanDisc treated every slot
# without a `fixed` name as a slot to pick in - and Ventrue Antitribu, Salubri (Wu Zao / Watchers) and Angellis
# Ater handed out four clan Disciplines where the book gives three. The fourth slot exists
# for the Gargoyles, who fix four; the other three inherited it for free. V236 could not
# see it: it measures coverage, spelling, and the `fixed` ceiling ON ITS OWN - never the
# total an entry delivers. Prose in I17 said "one slot to choose" and prose is not a
# contract, so the number now lives in the table as `choiceN` and gets measured here.
$v295Bad = @()
$v295Choice = 0
foreach ($k in ($clanEntries.Keys | Sort-Object)) {
    $en = $clanEntries[$k]
    $picks = 0
    if ($en.choice.Count -gt 0) {
        $v295Choice++
        $picks = 1
        if ($en.choiceN -gt 0) { $picks = $en.choiceN }
        # (b) more picks than options is impossible to satisfy, and the slot sticks empty
        if ($picks -gt $en.choice.Count) {
            $v295Bad += "'$k' asks for $picks pick(s) out of a list of $($en.choice.Count) - a slot nobody can ever fill (SPEC V295b)"
        }
    } elseif ($en.choiceN -gt 0) {
        # (c) a count with no list to count is dead data, and the ABSENCE of the number is
        # what made B65 in the first place
        $v295Bad += "'$k' declares choiceN=$($en.choiceN) with no choice list to pick from (SPEC V295c)"
    }
    # (a) fixed + picked + open has to fit the slots that actually exist
    $total = $en.fixed.Count + $picks + $en.open
    if ($discRows -gt 0 -and $total -gt $discRows) {
        $v295Bad += "'$k' promises $total Disciplines ($($en.fixed.Count) fixed + $picks picked + $($en.open) open) for $discRows slots - the extra name falls on the floor with nothing raised (SPEC V295a)"
    }
}
if ($clanEntries.Count -eq 0) { Fail "V295 CLANS parsed to zero entries - there is no slot count to measure here (SPEC V209, B7)" }
elseif ($v295Choice -lt 4) { Fail "V295 only $v295Choice entr(y/ies) carrying a choice list were read, expected at least the 4 CLANS holds - the collector is missing the very shape this check exists for (SPEC V209)" }
elseif ($v295Bad) { foreach ($b in ($v295Bad | Select-Object -First 12)) { Fail "V295 $b" } }
else { Pass "V295 all $($clanEntries.Count) CLANS entries fit inside $discRows slots, $v295Choice of them handing out picks" }

# ---- V237: changing clan leaves no ORPHAN dot -----------------------------------------
# The slot whose NAME changes has its five dots cleared in the same pass that writes the
# name, and the slot whose name does not change is not touched at all. The two halves have
# to arrive together: clearing always would wipe somebody's purchase every time the sheet
# opened, and writing the name alone would leave dots bought under Auspex sitting in a slot
# that now reads Celerity - which the ledger would then price under the new name (SPEC I9,
# I10b), so the table finds out weeks later through an experience total nobody can account
# for. Silent, slow and expensive: the worst shape a bug takes on this sheet.
#
# Read STRUCTURALLY, by block depth, not by a regex over the text: a lazy .{0,N} match reads
# straight through the end; that closes the guard, and would go green on the one mutation
# that matters (SPEC V232 learned this the hard way, same session).
$orphanBad = @()
$cdFn = LuaFn $rootTxt 'renderClanDisc'
if (-not $cdFn) { $orphanBad += "renderClanDisc is gone from the root form - nothing fills the clan slots (SPEC I37)" }
else {
    if ($cdFn -notmatch 'CLANS\[') { $orphanBad += "renderClanDisc does not read CLANS - it has no source for the names (SPEC I17, I37)" }
    if ($cdFn -match 'setField\("clan"') { $orphanBad += "renderClanDisc writes `clan` - the dormant combo on the Main tab stays asleep, and clanFamily is what plays the part today (SPEC V173, B50)" }

    $cdLines = $cdFn -split "`n"
    $iGuard = -1
    for ($k = 0; $k -lt $cdLines.Count; $k++) { if ($cdLines[$k] -match 'if sheet\[field\] ~= name then') { $iGuard = $k; break } }
    if ($iGuard -lt 0) { $orphanBad += "renderClanDisc has no 'if sheet[field] ~= name then' guard - it would rewrite every slot on every render and clear dots that never changed hands (SPEC V237)" }
    else {
        $depth237 = 1
        $wroteName = $false
        $clearedDots = $false
        $endGuard = $cdLines.Count
        for ($k = $iGuard + 1; $k -lt $cdLines.Count -and $depth237 -gt 0; $k++) {
            if ($cdLines[$k] -match '^\s*end;\s*$') { $depth237--; if ($depth237 -eq 0) { $endGuard = $k }; continue }
            if ($cdLines[$k] -match 'setField\(field, name\)') { $wroteName = $true }
            if ($cdLines[$k] -match 'setField\(field \.\. "_" \.\. d, false\)') { $clearedDots = $true }
        }
        if (-not $wroteName) { $orphanBad += "the name is not written inside the changed-name guard (SPEC V237)" }
        if (-not $clearedDots) { $orphanBad += "the five dots are NOT cleared inside the changed-name guard - a dot bought under one Discipline would become a dot of another (SPEC V237)" }
        # and nothing clears dots outside it: that would cost the table its purchases on every open
        for ($k = 0; $k -lt $cdLines.Count; $k++) {
            if ($k -gt $iGuard -and $k -lt $endGuard) { continue }
            if ($cdLines[$k] -match 'setField\(field \.\. "_" \.\. d, false\)') { $orphanBad += "dots are cleared OUTSIDE the changed-name guard, at line $($k + 1) of renderClanDisc - reopening a sheet would wipe what the table bought (SPEC V237)" }
        }
    }
}
# the trigger cannot live on the tab that draws the slots: it is managed, and may never open
if ($rootTxt -notmatch '<dataLink fields="\{''language'', ''clanFamily''\}">') { $orphanBad += "the root form does not watch clanFamily - a tab that is never opened cannot be the thing that keeps the slots correct (SPEC V95, B26)" }
foreach ($nf in @('WoD20.11.lfm')) {
    if ((CodeOf (Join-Path $dir $nf)) -match 'renderClanDisc\(') { $orphanBad += "$nf calls renderClanDisc - it is a managed tab, and the sheet must be correct whether or not it is ever opened (SPEC V95)" }
}
if (-not $cdFn) { Fail "V237 renderClanDisc is not on the root form - this check has nothing to read (SPEC I37, V209)" }
elseif ($orphanBad) { foreach ($b in $orphanBad) { Fail "V237 $b" } }
else { Pass "V237 a clan change clears the dots of the slots whose name changed, and only those" }


# ---- V242: no bare PROSE inside Lua ---------------------------------------------------
# B54: a three-line comment written with `--` on the first line only. The other two became
# bare prose inside Lua, rdk -l exited 1 SILENTLY and DELETED the .rpk (SPEC B19, the fifth
# door to that signature), and the gate went green on a build that did not exist - it never
# runs Lua (HANDOFF section 8). Finding it cost a bisect of the root form's XML subtrees.
#
# The heuristic is deliberately NARROW: the line must fail all three tests at once - no `--`
# prefix, not one character of Lua punctuation, and reading like an English sentence of four
# words or more. Measured 0 false positives across the 15 files. A loose prose check fires on
# legitimate code, and a check people learn to ignore is worse than no check: it teaches that
# red is normal, which is how a real failure gets waved through.
#
# It does NOT replace running rdk -l before marking a task done (HANDOFF section 8). It
# catches THIS mistake - the one rdk refuses to explain - not syntax errors in general.
$proseBad = @()
$proseFiles = 0
$PROSE_PUNCT = '[=(){}\[\];:]'
$PROSE_WORDS = '^[A-Za-z][a-z]+( [A-Za-z`'',.\-]+){3,}$'
foreach ($f in $files) {
    $raw = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName))
    $proseFiles++
    $regions = @()
    foreach ($m in [regex]::Matches($raw, '(?s)<!\[CDATA\[(.*?)\]\]>')) { $regions += $m.Groups[1].Value }
    foreach ($m in [regex]::Matches($raw, '(?s)<event[^>]*>(.*?)</event>')) { $regions += ([regex]::Replace($m.Groups[1].Value, '(?s)<!--.*?-->', '')) }

    foreach ($r in $regions) {
        $n = 0
        foreach ($line in ($r -split "`n")) {
            $n++
            $t = $line.Trim()
            if (-not $t) { continue }
            if ($t.StartsWith('--')) { continue }
            if ($t -match $PROSE_PUNCT) { continue }
            if ($t -match $PROSE_WORDS) {
                $proseBad += "$($f.Name): '$($t.Substring(0, [Math]::Min(64, $t.Length)))' reads as a comment that lost its -- prefix"
            }
        }
    }
}
if ($proseFiles -lt 12) { Fail "V242 only $proseFiles .lfm were read, expected 12 - this check is covering less than the sheet has (SPEC V209). Was 15 until T909 (SPEC I136i)" }
elseif ($proseBad) { foreach ($p in $proseBad) { Fail "V242 $p" } }
else { Pass "V242 no bare prose in the Lua of $proseFiles files - every comment line carries its own -- (SPEC B54)" }


# ---- V241: a locked dropdown does not dim its own TEXT --------------------------------
# `opacity` is gui.Control.props["opacity"] (rrpgGUI.lua:239) and dims the whole control,
# the text with it. A comboBox has no text-only opacity to reach for - its own props are
# transparent / field / frameRegion / items / values / text / value, and of text it inherits
# only fontColor - so the ONLY way to keep a locked dropdown readable is not to carry the
# property at all (SPEC I41, user 2026-08-22).
#
# fontColor was rejected as the replacement: applyTheme repaints THEME_TEXTS, comboBox
# included, from the colour the XML authored (SPEC V57), so a colour written from Lua is
# overwritten on the next repaint or has to join the ledger - the same argument that made the
# tab strip light its active tab with `visible` instead of with a colour (SPEC I32).
#
# This guards what is in the SOURCE. It does not promise an appearance: what a disabled
# combo looks like is the host's, and only the Firecast can answer that (SPEC R98).
$dimComboBad = @()
$dimComboSeen = 0
foreach ($f in $files) {
    foreach ($n in (Doc $f.FullName).SelectNodes("//comboBox")) {
        $dimComboSeen++
        $op = $n.GetAttribute("opacity")
        if ($op -ne '') {
            $nm = $n.GetAttribute("name"); if (-not $nm) { $nm = $n.GetAttribute("field") }
            $dimComboBad += "$($f.Name) comboBox '$nm' is authored with opacity '$op' - it dims the text the player has to read (SPEC I41)"
        }
    }
}
foreach ($f in $files) {
    $code = CodeOf $f.FullName
    foreach ($m in [regex]::Matches($code, '(?m)^\s*(\w+)\.opacity\s*=')) {
        # only the combo painters are forbidden; edits, editors and dots still dim (V111)
        $fn = $m.Groups[1].Value
        if ($fn -eq 'c') { $dimComboBad += "$($f.Name) writes $($m.Groups[0].Value.Trim()) - the combo painters are the two that used to dim a dropdown (SPEC I41, V241)" }
    }
}
if ($dimComboSeen -lt 9) { Fail "V241 only $dimComboSeen comboBox(es) were read, expected the 9 the sheet declares - this check is covering less than the sheet has (SPEC V209). Was 17 until T872 took the four Vampire row combos away, 15 until T874 took the three Numina ones and 12 until its half B took cboHedgeAffiliation; the number is COVERAGE and the prohibition is unchanged" }
elseif ($dimComboBad) { foreach ($b in $dimComboBad) { Fail "V241 $b" } }
else { Pass "V241 none of the $dimComboSeen dropdowns dims itself - a locked one still reads (SPEC I41)" }


# ---- V239: a box is SYMMETRIC on the X axis -------------------------------------------
# The user asked for the same gap on both sides of every box (2026-08-22, item 1). The
# NUMBER is not fixed sheet-wide: 15/15 and 10/10 both live here and both are fine - what is
# measured is that the two sides agree. Forcing one number would mean rewriting V146, whose
# row width is "the box minus 20", for the sake of sixteen boxes that were already even.
#
# Two things are excluded, and neither is a loophole. The TITLE spans the box on purpose -
# left=0 with the box's own width is how V27 centres it - so counting it would read a gap of
# zero everywhere and the rule could never be met. A ROTATED child is excluded because
# `left` is its PRE-rotation box: the three attribute headings are authored at left=-40 and
# render inside the box at x 0..20, which is the same cut V27 already makes for them.
$symBad = @()
$symSeen = 0
foreach ($f in $files) {
    foreach ($box in (Doc $f.FullName).SelectNodes("//layout[@width]")) {
        $bw = -1
        if (-not [int]::TryParse($box.GetAttribute("width"), [ref]$bw)) { continue }
        if ($bw -le 0) { continue }

        # a BOX is a layout with a backdrop: align=client, or a rect covering it from 0,0
        $back = $null
        foreach ($k in $box.ChildNodes) {
            if ($k.NodeType -ne 'Element' -or $k.LocalName -ne 'rectangle') { continue }
            $kl = -1; $kt = -1; $kw = -1
            [void][int]::TryParse($k.GetAttribute("left"), [ref]$kl)
            [void][int]::TryParse($k.GetAttribute("top"), [ref]$kt)
            [void][int]::TryParse($k.GetAttribute("width"), [ref]$kw)
            if ($k.GetAttribute("align") -eq 'client' -or ($kl -eq 0 -and $kt -eq 0 -and $kw -eq $bw)) { $back = $k; break }
        }
        if ($null -eq $back) { continue }

        $titleNode = $null
        $titleTop = [int]::MaxValue
        foreach ($k in $box.ChildNodes) {
            if ($k.NodeType -ne 'Element' -or $k.LocalName -ne 'label') { continue }
            $tl = -1; $tw = -1; $tt = 0
            [void][int]::TryParse($k.GetAttribute("left"), [ref]$tl)
            [void][int]::TryParse($k.GetAttribute("width"), [ref]$tw)
            [void][int]::TryParse($k.GetAttribute("top"), [ref]$tt)
            if ($tl -eq 0 -and $tw -eq $bw -and $tt -lt $titleTop) { $titleTop = $tt; $titleNode = $k }
        }

        $lo = [int]::MaxValue
        $hi = [int]::MinValue
        foreach ($k in $box.ChildNodes) {
            if ($k.NodeType -ne 'Element') { continue }
            if ($k -eq $back) { continue }
            if ($k.LocalName -in @('dataLink', 'script', 'event', 'template')) { continue }
            if ($k.HasAttribute("rotationAngle")) { continue }
            $kl = 0; $kw = 0
            if (-not [int]::TryParse($k.GetAttribute("left"), [ref]$kl)) { continue }
            [void][int]::TryParse($k.GetAttribute("width"), [ref]$kw)
            # the section TITLE spans the box: V27 owns it, and counting it here would make
            # every box read a left gap of zero. Only the TOPMOST spanning label is the title -
            # the credits box is four of them stacked, and the three below the heading are
            # content that happens to span, whose 0/0 is symmetric anyway.
            if ($k -eq $titleNode) { continue }
            if ($kl -lt $lo) { $lo = $kl }
            if (($kl + $kw) -gt $hi) { $hi = $kl + $kw }
        }
        if ($lo -eq [int]::MaxValue) { continue }

        $symSeen++
        $gapR = $bw - $hi
        if ($lo -ne $gapR) {
            $ttl = '(untitled)'
            foreach ($k in $box.ChildNodes) {
                if ($k.NodeType -eq 'Element' -and $k.LocalName -eq 'label' -and $k.GetAttribute("horzTextAlign") -eq 'center' -and $k.GetAttribute("text") -and -not $k.HasAttribute("rotationAngle")) { $tw = 0; if ([int]::TryParse($k.GetAttribute("width"), [ref]$tw) -and $tw -ge ($bw * 0.8)) { $ttl = $k.GetAttribute("text"); break } }
            }
            $symBad += "$($f.Name) '$ttl' ($($bw)px wide) leaves $lo on the left and $gapR on the right - the two sides have to agree (SPEC I40)"
        }
    }
}
if ($symSeen -lt 70) { Fail "V239 only $symSeen box(es) were measured, expected the 70 the sheet draws - this check is covering less than the sheet has (SPEC V209)" }
elseif ($symBad) { foreach ($b in $symBad) { Fail "V239 $b" } }
else { Pass "V239 all $symSeen boxes leave the same gap on both sides" }

# ---- V400: the backdrop of a box covers the box EXACTLY --------------------------------
# SPEC V400, B132, I139d, user 2026-09-02 item 5. Nine boxes across three files authored a
# backdrop that was not the size of the box it sits in - five Notes boxes and three on Background
# ten pixels TALLER than their layout, so the rounded bottom corner was clipped off the screen,
# and one on Background ten pixels SHORTER, so the foot of the box carried no black at all.
#
# The gate was green over all nine for as long as they have existed, and that is the finding
# rather than the drift: V239, V240 and V280(c) all SKIP the backdrop on purpose - it IS the box,
# not a child of it - so three correct exclusions added up to a node with no owner. The two
# opposite signs are why this is an EQUALITY and not "the backdrop is at least the box": a number
# nobody compares walks both ways.
#
# The collector is V239's, to the letter. A box is a layout with a backdrop, and the backdrop is
# align=client or a rect from 0,0 as wide as the box - height deliberately NOT part of finding it,
# or a backdrop 10px off would stop being a backdrop and this rule would measure the sheet minus
# exactly the boxes it was written for (SPEC B7).
$v400Bad = @()
$v400Seen = 0
foreach ($f in $files) {
    foreach ($box in (Doc $f.FullName).SelectNodes("//layout[@width]")) {
        $bw400 = -1; $bh400 = -1
        if (-not [int]::TryParse($box.GetAttribute("width"), [ref]$bw400)) { continue }
        if (-not [int]::TryParse($box.GetAttribute("height"), [ref]$bh400)) { continue }
        if ($bw400 -le 0 -or $bh400 -le 0) { continue }
        foreach ($k in $box.ChildNodes) {
            if ($k.NodeType -ne 'Element' -or $k.LocalName -ne 'rectangle') { continue }
            if ($k.GetAttribute("align") -eq 'client') { $v400Seen++; break }
            $kl400 = -1; $kt400 = -1; $kw400 = -1; $kh400 = -1
            [void][int]::TryParse($k.GetAttribute("left"), [ref]$kl400)
            [void][int]::TryParse($k.GetAttribute("top"), [ref]$kt400)
            [void][int]::TryParse($k.GetAttribute("width"), [ref]$kw400)
            [void][int]::TryParse($k.GetAttribute("height"), [ref]$kh400)
            if ($kl400 -ne 0 -or $kt400 -ne 0 -or $kw400 -ne $bw400) { continue }
            $v400Seen++
            $ttl400 = '(untitled)'
            foreach ($lb400 in $box.ChildNodes) {
                if ($lb400.NodeType -eq 'Element' -and $lb400.LocalName -eq 'label' -and $lb400.GetAttribute("text")) { $ttl400 = $lb400.GetAttribute("text"); break }
            }
            if ($kh400 -ne $bh400) {
                $v400Bad += "$($f.Name) '$ttl400' is ${bw400}x${bh400} and its backdrop is ${kw400}x${kh400} - $([math]::Abs($kh400 - $bh400))px of the box is drawn without one, or of the backdrop outside the box where the parent clips it (SPEC V400, B132)"
            }
            break
        }
    }
}
if ($v400Seen -lt 69) { Fail "V400 only $v400Seen backdrop(s) were measured, expected at least the 69 boxes the sheet draws - this check is covering less than the sheet has (SPEC V209)" }
elseif ($v400Bad) { foreach ($b400 in $v400Bad) { Fail "V400 $b400" } }
else { Pass "V400 all $v400Seen box backdrops cover their box exactly - none clipped by the parent, none leaving a foot unpainted" }


# ---- V240: a box is SYMMETRIC on the Y axis, and the TITLE counts -----------------------
# Ten above the topmost child and ten under the lowest, on all sixty-six boxes (user
# 2026-08-22, item 1). The title counts as the first content because it is the first thing
# the eye meets, and because that is how the user measured it when the credits box was asked
# for "more room top and bottom" in the 82nd round - 14 above the heading, 14 below the last
# line. Measuring UNDER the title instead would read 25 over 15 and call that even.
#
# The two numbers are a FLOOR of 20 since the 104th round, and they were an equality of 10
# before it (user decision, SPEC T618). What forced the change is B63: HEALTH on the Main tab,
# HEALTH on Combat and SPECIALTIES are welded to the line V69 draws, and the 22px that line
# owes them was put INSIDE the box rather than in a gap above it, so all three close 42 under
# their last row on purpose. A floor is V280a's own wording - "no child closing less than 20
# from the border" - and it keeps the mutations that matter: a title back at top=4 and a box
# shrunk by 5px both still redden. What it gives up is the box that grows a hole under its
# last row and calls it a design, which is B38 by the other axis - V69, V49 and V193 are what
# hold those three heights now, and a box that answers to none of them has no reason to grow.
# They are NOT V231's numbers: that one guards the gap between the tab's CONTENT and the tab
# STRIP, is 12 against 8, and lives one level further out.
#
# The exclusions are V239's, for V239's reasons: the backdrop is the box, not something in
# it, and a rotated child's `top` is its PRE-rotation box. Both checks have to cut the same
# way or a box could pass one and fail the other for no reason a reader could see.
# The two boxes of the tabHedge band that are stretched to the height of HEDGE MAGIC beside
# them (SPEC I63, V267b). NAMED, and by title inside ONE file: WILLPOWER is also the name of a
# box on WoD20.1 and another on WoD20.3, and neither of those is stretched.
# TRUE FAITH replaced WILLPOWER in T830 (SPEC I117d, V361c) and inherits the exception with the
# rect: the box is the same 407x146 stretched to the band's base, so it pays CENTRING in place of
# the gap under its last row, exactly as the one beside it does.
$STRETCHED_BAND = @('QUINTESSENCE', 'TRUE FAITH')
$vpadBad = @()
$vpadSeen = 0
$stretchSeen = 0
foreach ($f in $files) {
    foreach ($box in (Doc $f.FullName).SelectNodes("//layout[@width][@height]")) {
        $bw = -1; $bh = -1
        if (-not [int]::TryParse($box.GetAttribute("width"), [ref]$bw)) { continue }
        if (-not [int]::TryParse($box.GetAttribute("height"), [ref]$bh)) { continue }
        if ($bw -le 0 -or $bh -le 0) { continue }

        $back = $null
        foreach ($k in $box.ChildNodes) {
            if ($k.NodeType -ne 'Element' -or $k.LocalName -ne 'rectangle') { continue }
            $kl = -1; $kt = -1; $kw = -1
            [void][int]::TryParse($k.GetAttribute("left"), [ref]$kl)
            [void][int]::TryParse($k.GetAttribute("top"), [ref]$kt)
            [void][int]::TryParse($k.GetAttribute("width"), [ref]$kw)
            if ($k.GetAttribute("align") -eq 'client' -or ($kl -eq 0 -and $kt -eq 0 -and $kw -eq $bw)) { $back = $k; break }
        }
        if ($null -eq $back) { continue }

        $hi = [int]::MaxValue
        $lo = [int]::MinValue
        foreach ($k in $box.ChildNodes) {
            if ($k.NodeType -ne 'Element') { continue }
            if ($k -eq $back) { continue }
            if ($k.LocalName -in @('dataLink', 'script', 'event', 'template')) { continue }
            if ($k.HasAttribute("rotationAngle")) { continue }
            if ($k.GetAttribute("align") -eq 'client') { continue }
            $kt = 0; $kh = 0
            if (-not [int]::TryParse($k.GetAttribute("top"), [ref]$kt)) { continue }
            [void][int]::TryParse($k.GetAttribute("height"), [ref]$kh)
            if ($kt -lt $hi) { $hi = $kt }
            if (($kt + $kh) -gt $lo) { $lo = $kt + $kh }
        }
        if ($hi -eq [int]::MaxValue) { continue }

        $vpadSeen++
        $gapB = $bh - $lo

        # The title is told the way V27 tells it, and for V27's reason (103rd round, SPEC T618):
        # since I73 gave every box 20 on all four sides the title CANNOT start at 0, so
        # "left=0 and width=box" now finds no title in any of the 73 boxes. It would not have
        # reddened anything here - the name would just read '(untitled)' - but the stretched-band
        # exception below is keyed on that name, so it would stop reaching its two boxes and go
        # on passing, which is the shape of B7 (SPEC V209).
        $ttl = '(untitled)'
        $ttlNode = $null
        foreach ($k in $box.ChildNodes) {
            if ($k.NodeType -ne 'Element' -or $k.LocalName -ne 'label' -or -not $k.GetAttribute("text")) { continue }
            if ($k.GetAttribute("horzTextAlign") -ne 'center' -or $k.HasAttribute("rotationAngle")) { continue }
            $tw = 0; if (-not [int]::TryParse($k.GetAttribute("width"), [ref]$tw)) { continue }
            if ($tw -lt ($bw * 0.8)) { continue }
            $ttl = $k.GetAttribute("text"); $ttlNode = $k; break
        }

        # The band across the top of tabHedge closes on ONE base (SPEC I63, user 2026-08-23),
        # so the two boxes on the right are STRETCHED past what their content needs and cannot
        # leave 10 under it. They are this check's one NAMED exception - by file and title, the
        # way $DESC_BRIGHT and bloodPool_* are named - because loosening the rule for every box
        # would let any box grow a hole under its last row and call it a design (SPEC B38).
        #
        # They do not escape a ruler, they SWAP one: the content below the title band has to be
        # CENTRED in the body, top gap equal to bottom gap, which is V239 turned 90 degrees
        # (SPEC V267b). The body opens at 41 - the title band plus the 10 V240 asks under it,
        # the same 41 the list boxes count with (466 = 41 + 25*17).
        if ($f.Name -eq 'WoD20.7.lfm' -and $STRETCHED_BAND -contains $ttl) {
            $stretchSeen++

            $hiC = [int]::MaxValue
            $loC = [int]::MinValue
            foreach ($k in $box.ChildNodes) {
                if ($k.NodeType -ne 'Element') { continue }
                if ($k -eq $back -or $k -eq $ttlNode) { continue }
                if ($k.LocalName -in @('dataLink', 'script', 'event', 'template')) { continue }
                if ($k.HasAttribute("rotationAngle")) { continue }
                if ($k.GetAttribute("align") -eq 'client') { continue }
                $kt = 0; $kh = 0
                if (-not [int]::TryParse($k.GetAttribute("top"), [ref]$kt)) { continue }
                [void][int]::TryParse($k.GetAttribute("height"), [ref]$kh)
                if ($kt -lt $hiC) { $hiC = $kt }
                if (($kt + $kh) -gt $loC) { $loC = $kt + $kh }
            }

            # Where the body opens, and the 114th round MOVED it (SPEC I86b, V267b, user
            # 2026-08-26). It used to be the hairline under the title band plus the margin I73
            # asks under it - title.bottom + 1 + 20, which read 41 while that margin was 10 and
            # 61 after. That charges the title's margin TWICE: it is a FLOOR for content stacked
            # under a title, not a band reserved from the box, and counting it as both pushed a
            # single centred row 21px low. The eye caught it on the Numina tab before any check
            # did, because both boxes were passing this arm the whole time.
            #
            # It opens on the BOX's own margin now - the same 20 V280a charges on four sides -
            # so the title takes no band of its own. Still DERIVED, not typed: 20 is read from
            # the one margin the sheet uses everywhere, and the title node is still what proves
            # this is the titled arm.
            $bodyTop = 20
            if ($hiC -eq [int]::MaxValue) { $vpadBad += "$($f.Name) '$ttl' is stretched and holds nothing under its title - the exception is for a box that pays centring, not for an empty one (SPEC V267b)" }
            elseif ($hi -ne $BOX_PAD_Y) { $vpadBad += "$($f.Name) '$ttl' opens with $hi above its title, not the $BOX_PAD_Y the head takes - stretched or not, the head is an equality (SPEC I40, V267b, I137c)" }
            # The content still clears the title band by the sheet's own margin (SPEC I86c). This
            # leg is what makes the 114th round's move a CEILING and not a direction: WILLPOWER,
            # the taller of the two, lands at exactly 20 under its title, so one more pixel up
            # reddens here. Without it the centring formula alone would happily walk the content
            # into the title as the box shrank.
            elseif (($hiC - ([int]$ttlNode.GetAttribute("top") + [int]$ttlNode.GetAttribute("height"))) -lt 20) { $vpadBad += "$($f.Name) '$ttl' leaves $($hiC - ([int]$ttlNode.GetAttribute('top') + [int]$ttlNode.GetAttribute('height')))px between the title and the content, under the 20 every box on this sheet keeps (SPEC I86c, V280a)" }
            # Centring is DERIVED and not a tolerance: the top gap is the floor of the halved
            # remainder, so the odd pixel always falls BELOW (SPEC I86c, V267b). Written as one
            # expected `top` rather than as two gaps compared loosely - a `-le 1` slack would
            # accept the odd pixel on either side and stop measuring which.
            elseif ($hiC -ne ($bodyTop + [math]::Floor(($bh - $bodyTop - ($loC - $hiC)) / 2))) { $vpadBad += "$($f.Name) '$ttl' ($($bh)px tall) opens its content at $hiC, not the $($bodyTop + [math]::Floor(($bh - $bodyTop - ($loC - $hiC)) / 2)) that centres it in the body with the odd pixel below - a stretched box pays CENTRING in place of the 20 (SPEC V267b, I73)" }
        }
        elseif ($hi -ne $BOX_PAD_Y -or $gapB -lt $BOX_PAD_Y) {
            $vpadBad += "$($f.Name) '$ttl' ($($bh)px tall) opens with $hi above its first child and leaves $gapB under its last - the head is exactly $BOX_PAD_Y and the foot never drops under it (SPEC I40, I73, V280a, I137c)"
        }
    }
}
if ($vpadSeen -lt 70) { Fail "V240 only $vpadSeen box(es) were measured, expected the 70 the sheet draws - this check is covering less than the sheet has (SPEC V209)" }
elseif ($stretchSeen -ne 2) { Fail "V240 $stretchSeen stretched box(es) took the centring rule, expected the 2 of the tabHedge band - an exception nothing reaches is an exception that stopped measuring (SPEC V209, V267b)" }
elseif ($vpadBad) { foreach ($b in $vpadBad) { Fail "V240 $b" } }
else { Pass "V240 all $vpadSeen boxes breathe at least 20 above their title and 20 under their last row, and the $stretchSeen stretched ones centre their content instead" }

# ---- V243: the storyteller box closes on a column, and the state sentence is gone --------
# (a) the box is checkboxes plus a short column of entry widgets - cmbHealthLevels, cmbSpecCost,
# cmbBgCost and btnSaveBaseline since the 88th round (SPEC I49, I51) - and either ALL of them make
# one column or the box has none (SPEC V26 - B12 and B13 are this same drift, found one round
# apart). The check counts DISTINCT x rather than naming a pair, so the next one cannot join
# off-column without saying so, which is what the pair-shaped version would have let the third do.
# (b) dynBaselineState is burned: the dead button is the message now (SPEC I42, V243b, user
# 2026-08-22), and the Experience tab still says "not saved yet" inside the log that sentence
# was about (SPEC I10, V33).
$stDocX  = (Doc (Join-Path $dir "WoD20.10.lfm"))
$btnSave = $stDocX.SelectSingleNode("//button[@name='btnSaveBaseline']")
$cmbHL   = $stDocX.SelectSingleNode("//comboBox[@name='cmbHealthLevels']")
$stateW  = 0
foreach ($f in $files) { $stateW += @((Doc $f.FullName).SelectNodes("//*[@name='dynBaselineState']")).Count }

$stColBox = BoxOf $stDocX "STORYTELLER SETTINGS"
$stColX = @()
if ($null -ne $stColBox) { $stColX = @($stColBox.SelectNodes("comboBox[@left] | button[@left] | edit[@left]") | ForEach-Object { $_.GetAttribute("left") } | Sort-Object -Unique) }

if ($null -eq $btnSave) { Fail "V243 btnSaveBaseline is not on WoD20.10 - the box lost the one action it exists to offer" }
elseif ($null -eq $cmbHL) { Fail "V243 cmbHealthLevels is not on WoD20.10 - the column the button lines up with is gone (SPEC V234)" }
elseif ($stColX.Count -eq 0) { Fail "V243 no entry widget was read inside STORYTELLER SETTINGS - the column check verifies nothing (SPEC V209, V20)" }
elseif ($stColX.Count -gt 1) { Fail "V243 the entry widgets of this box sit at x = $($stColX -join ', ') - all of them make one column or the box has none (SPEC I42, I49, V26)" }
elseif ($stateW -gt 0) { Fail "V243 a widget named dynBaselineState is back - that name is burned (SPEC I42, V243b)" }
else { Pass "V243 the box closes on one entry column at x=$($stColX[0]), Save last in it, and the state sentence is gone for good" }

# ---- V245/V246/V247: the storyteller can correct Spent, and it costs a LINE ---------------
# Spent stays the sum of the log (SPEC I11, V100). What the storyteller types is stored as a
# DIFFERENCE and shown as a row, so the figure in the box and the lines under it cannot come
# apart - an absolute override would have frozen the number while purchases kept moving the
# log (SPEC I44, V246).
$xp9Doc  = (Doc (Join-Path $dir "WoD20.9.lfm"))
$spentEd = $xp9Doc.SelectSingleNode("//edit[@name='edtSpentXP']")
$paintFn = LuaFn $rootTxt 'xpPaint'
$setSpFn = LuaFn $rootTxt 'xpSetSpent'
$ledgerFn = LuaFn $rootTxt 'xpLedgerRows'

if ($null -eq $spentEd) { Fail "V245 edtSpentXP is gone from WoD20.9 - there is nothing to unlock" }
elseif ($spentEd.GetAttribute("readOnly") -ne 'true') { Fail "V245 edtSpentXP is authored unlocked - the flag defaults OFF and the sheet has to read right before any Lua runs (SPEC V80)" }
elseif ($spentEd.GetAttribute("onUserChange") -notmatch 'xpSetSpent') { Fail "V245 edtSpentXP does not hand what is typed to xpSetSpent - the box would take a number and drop it" }
elseif ($spentEd.GetAttribute("field")) { Fail "V245 edtSpentXP owns a field - Spent is derived from the log and a stored copy could only drift (SPEC V100, V102)" }
elseif (-not $paintFn) { Fail "V245 xpPaint not found on the root form - nothing paints the three numbers" }
elseif ($paintFn -notmatch 'sheet\.stEditSpentXP == true') { Fail "V245 the lock does not read stEditSpentXP as an explicit true - nil would fall OPEN on a sheet saved before this round (SPEC V80, V89)" }
elseif ($paintFn -notmatch '(?m)^\s*box\.edtSpentXP\.readOnly = not stEdit; box\.edtSpentXP\.opacity = stEdit and 1 or 0\.75;') { Fail "V245 readOnly and opacity are not written on the same line for edtSpentXP - a box that looks free and refuses the key is the lie V112 exists to stop" }
elseif (-not $setSpFn) { Fail "V246 xpSetSpent not found on the root form" }
elseif ($setSpFn -notmatch 'if rows == nil then') { Fail "V246 xpSetSpent does not refuse when no baseline is saved - it would write a correction to a log that does not exist (SPEC V33)" }
elseif ($setSpFn -notmatch 'if n < 0 then') { Fail "V246 a negative Spent is not refused (SPEC V246d, the same call V131 makes for the balance)" }
elseif ($setSpFn -notmatch 'setField\("xpManual", n - logged\)') { Fail "V246 xpSetSpent does not store the DIFFERENCE - an absolute number here freezes Spent and lets the log disagree with it (SPEC V100)" }
elseif ($setSpFn -notmatch 'xpSum\(rows\) - \(tonumber\(sheet\.xpManual\) or 0\)') { Fail "V246 the difference is not worked out against the log WITHOUT the old correction - the box would drift by its own value on every second edit" }
elseif (([regex]::Match($setSpFn, '(?s)if n < 0 then.*?end;')).Value -match 'setField') { Fail "V246 something is written before the refusal returns - a refused number must leave the sheet untouched (SPEC V131)" }
elseif (-not $ledgerFn) { Fail "V246 xpLedgerRows not found on the root form" }
elseif ($ledgerFn -notmatch 'local manual = tonumber\(sheet\.xpManual\) or 0;') { Fail "V246 the ledger does not read xpManual - the correction would move no line" }
elseif ($ledgerFn -notmatch 'if manual ~= 0 then') { Fail "V246 the MANUAL row is not conditional - a zero correction would print a row saying nothing happened (SPEC V246b)" }
elseif ($ledgerFn -notmatch 'kind = "MANUAL", name = "MANUAL", level = "MANUAL", cost = manual') { Fail "V246 the MANUAL row does not carry the word in all three columns with the correction as its cost (SPEC I44)" }
elseif ($ledgerFn.IndexOf('local manual = tonumber') -lt $ledgerFn.IndexOf('"Specialty"')) { Fail "V246 the MANUAL row is pushed before Specialty - it has no category and no slot index, so it closes the order (SPEC I10)" }
elseif (-not $ptVal.ContainsKey('MANUAL')) { Fail "V246 MANUAL has no entry in the PT map - the ledger renders it through translateSheetText like every other column text (SPEC V22, V28)" }
else {
    Pass "V245 Spent unlocks only where the storyteller allowed it, fail-closed, with the look written beside the lock"
    Pass "V246 the correction is stored as a difference and shown as the last row of the log, keyed in both languages"
}

# V247: the two boxes of the Experience tab are the same width, and that width is derived
# from the rightmost column - a column that grows while its box stands still is a log poking
# out of its own frame (SPEC I44, the relation V193 and V171 use for their own boxes).
$xpBoxes = @($xp9Doc.SelectNodes("//layout[@width]") | Where-Object { $_.GetAttribute("width") -match '^\d+$' })
$logBox  = $xp9Doc.SelectSingleNode("//layout[@name='xpLogBox']")
$costCol = $xp9Doc.SelectSingleNode("//textEditor[@name='dynXpCost']")
$lvlCol  = $xp9Doc.SelectSingleNode("//textEditor[@name='dynXpLevel']")
$expBox  = $xpBoxes | Where-Object { $_.SelectSingleNode("label[@text='EXPERIENCE']") } | Select-Object -First 1
# The columns are read INSIDE the box that scrolls them, not against the frame (103rd round,
# SPEC T618): until I73 the scrollBox sat at left=0 and was as wide as the frame, so the two
# rulers were the same number and the check could use either. With a 20px margin they are 40
# apart, and the chain the invariant is about - column -> scrolling box -> frame - has to be
# said in two steps or the 16px bar reserve reads as 56.
$logScroll = $xp9Doc.SelectSingleNode("//scrollBox[@name='xpLogScroll']")

if ($null -eq $logBox -or $null -eq $costCol -or $null -eq $lvlCol) { Fail "V247 the log box or one of its columns is missing from WoD20.9 (SPEC V20 - this check would be reading nothing)" }
elseif ($null -eq $expBox) { Fail "V247 the EXPERIENCE box was not found on WoD20.9 - the width relation has only one side" }
elseif ($null -eq $logScroll) { Fail "V247 xpLogScroll is gone from WoD20.9 - the columns would be measured against the frame they no longer share an edge with (SPEC V209, I57)" }
else {
    $costEnd = [int]$costCol.GetAttribute("left") + [int]$costCol.GetAttribute("width")
    $logW    = [int]$logBox.GetAttribute("width")
    $expW    = [int]$expBox.GetAttribute("width")
    $scrL    = [int]$logScroll.GetAttribute("left")
    $scrW    = [int]$logScroll.GetAttribute("width")
    $needLvl = NeededPx 'MANUAL'

    if ($logW -ne $expW) { Fail "V247 the log box is $logW wide and the EXPERIENCE box $expW - the two boxes of this tab close on the same x or the tab reads crooked" }
    elseif ($scrW -ne ($costEnd + 16)) { Fail "V247 the scrolling box is $scrW wide and its last column ends at $costEnd - the columns are the Cost column plus the 16px the vertical bar comes out of, or a column grew and the box did not (SPEC I44, I57)" }
    elseif ($logW -ne (2 * $scrL + $scrW)) { Fail "V247 the log box is $logW wide around a scrolling box $scrW wide at left=$scrL - the frame is the columns plus the margin on both sides, or a column grew and the frame did not (SPEC I44, I73)" }
    elseif ([int]$lvlCol.GetAttribute("width") -lt $needLvl) { Fail "V247 the Level column is $($lvlCol.GetAttribute('width'))px and MANUAL needs about $needLvl - the one row whose Level is a word would come up cut (SPEC V16)" }
    else { Pass "V247 both Experience boxes are $logW wide, the last column closes 16 short of the box that scrolls it, and Level holds MANUAL" }
}

# ---- V249/V250: the typed rows' DESCRIPTION is the storyteller's to write -----------------
# Editable only where the marked row is typed AND the reader is the storyteller (SPEC I45,
# I8a). The XML stays readOnly and field-less in all three areas (SPEC V29): what the
# storyteller writes is put away by Lua, into the field of the row that is MARKED, read at
# the moment of the write - so leaving one typed row for another cannot spill into the one
# just left.
#
# RETIRED in the 159th round (SPEC V249/V250, T872). Every subject went out with wave 3:
# edtDiscDesc/edtPathDesc/edtRitualDesc, the three renderers, the three savers, NO_TEXT_YET,
# and the discSel/pathSel/ritualSel that said WHICH row. The RULE is not orphaned, and that is
# measured rather than assumed - it has two owners that already exist and already have checks:
#
#   * V266 for the Numina panes, which are the same shape. WARNING, and it cost the 157th a
#     round: saveNuminaDesc writes box.text, NOT form.<Edit>.text - following the subject there
#     is right, but the FORM has to be read from there too.
#   * V365(f) for the ? box, where savePopDesc does the four things this check charged -
#     descQuiet so the renderer's write does not come back as typing, isStoryteller() RE-asked
#     because readOnly on the control is a look and the rule is the rule, popRow read at the
#     MOMENT of the write, and the write itself through descFieldOf, the one owner (V135).
#
# The single leg that does NOT migrate is isTypedRow, and on purpose: the ? lives on PICKER
# rows (I102f, V365b), so a typed-row test there would refuse every write there is.
#
# What stays here is the retirement's own guard: a pane coming BACK in one of the three would
# be a second owner of the same text with no ruler on it (V135, B98).
$descAreas = @()
$typedBad = @()
$typedSeen = 0
foreach ($a in $descAreas) {
    $p = Join-Path $dir $a.File
    if (-not (Test-Path $p)) { $typedBad += "$($a.File) is missing"; continue }

    $txt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($p))
    $doc = Doc $p
    $ed  = $doc.SelectSingleNode("//textEditor[@name='$($a.Edit)']")
    $rFn = LuaFn $txt $a.Render
    $sFn = LuaFn $txt $a.Save
    $typedSeen++

    if ($null -eq $ed) { $typedBad += "$($a.File) has no $($a.Edit) - the block is gone"; continue }
    if ($ed.GetAttribute("readOnly") -ne 'true') { $typedBad += "$($a.File) $($a.Edit) is authored writable - the XML default is READ, and the lock opens from Lua for one reader only (SPEC V29)" }
    if ($ed.GetAttribute("field")) { $typedBad += "$($a.File) $($a.Edit) owns a field - book text must never be saved onto the character (SPEC V29)" }
    if ($ed.GetAttribute("onChange") -notmatch "$($a.Save)\(self\);") { $typedBad += "$($a.File) $($a.Edit) does not hand what is typed to $($a.Save) - the storyteller's text would live on screen and nowhere else" }

    if (-not $rFn) { $typedBad += "$($a.File) $($a.Render) not found"; continue }
    if ($rFn -notmatch 'isTypedRow\(sel\) and isStoryteller\(\)') { $typedBad += "$($a.File) $($a.Render) does not ask BOTH questions - typed row and storyteller, in that order (SPEC V249a)" }
    if ($rFn -notmatch "form\.$($a.Edit)\.readOnly = not write;") { $typedBad += "$($a.File) $($a.Render) does not write the lock beside the text - a block showing one row's text under another row's lock (SPEC V249c)" }
    if ($rFn -notmatch 'descQuiet = true;' -or $rFn -notmatch 'descQuiet = false;') { $typedBad += "$($a.File) $($a.Render) does not raise descQuiet around its own write - gui.TextEditor has onChange and no onUserChange, so the renderer's write comes back as if it were typing (SPEC V249d)" }

    if (-not $sFn) { $typedBad += "$($a.File) $($a.Save) not found"; continue }
    if ($sFn -notmatch 'if descQuiet or sheet == nil then return; end;') { $typedBad += "$($a.File) $($a.Save) does not stand down while the renderer writes (SPEC V249d)" }
    if ($sFn -notmatch 'if not isTypedRow\(sel\) or not isStoryteller\(\) then return; end;') { $typedBad += "$($a.File) $($a.Save) does not re-ask the rule - the readOnly on the control is a look, not the rule (SPEC V249a)" }
    if ($sFn -notmatch "setField\(descFieldOf\(sel\), form\.$($a.Edit)\.text\);") { $typedBad += "$($a.File) $($a.Save) does not write to the field of the MARKED row (SPEC V249c)" }
    if ($sFn -notmatch "local sel = sheet\.$($a.Sel);") { $typedBad += "$($a.File) $($a.Save) does not read the selection at the moment of the write - a stale row would take the text (SPEC V249c)" }

    # V250: the state sentence for a typed row nobody has written yet, in both languages, and
    # the same inline shape the other three states already use in these files (SPEC V33).
    if ($txt -notmatch '(?s)local NO_TEXT_YET = \{\s*en = "[^"]+",\s*pt = "[^"]+",') { $typedBad += "$($a.File) has no NO_TEXT_YET in both languages - an empty typed row would open a blank box for the player (SPEC V33, V250)" }
    if ($txt -notmatch 'return pick\(NO_TEXT_YET, lang\);') { $typedBad += "$($a.File) declares NO_TEXT_YET and never says it (SPEC V20 - a state text nothing reaches is not a state text)" }
    if ($txt -notmatch 'if isStoryteller\(\) then return ""; end;') { $typedBad += "$($a.File) shows the state sentence to the storyteller too - for him the empty box IS the input (SPEC V250)" }

    # The description fields are written by Lua alone and watched by nobody: a dataLink on one
    # would repaint the box under the cursor on every keystroke (SPEC V123, V249d).
    foreach ($dl in $doc.SelectNodes("//dataLink")) {
        $watch = "$($dl.GetAttribute('field')) $($dl.GetAttribute('fields'))"
        if ($watch -match 'Desc_\d') { $typedBad += "$($a.File) has a dataLink watching a description field - every keystroke would repaint the box the storyteller is typing in (SPEC V249d)" }
    }
}
# WoD20.7 joined the ghost list in T874: wave 4 took its four panes and saveNuminaDesc with them,
# so the storyteller's write on a typed row has ONE owner on the whole sheet now (SPEC V365f).
# Read with the COMMENTS STRIPPED, and that is not a loosening: these four files carry the note
# saying which pane left and why, and a rule that reddens on its own retirement note is the false
# positive V222 was written for - measured twice already, on 'denied' and on maxDiscLevel( (SPEC
# V222b). What is refused is the pane coming back in the XML or the saver in the Lua, both code.
foreach ($gf in @('WoD20.11.lfm', 'WoD20.7.lfm')) {
    $gt = NoComments ([System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir $gf))))
    foreach ($ghost in @('edtDiscDesc', 'edtPathDesc', 'edtRitualDesc', 'edtHedgeDesc', 'edtPsychicDesc', 'edtHedgeRitualDesc', 'edtFaithDesc', 'NO_TEXT_YET', 'saveNuminaDesc')) {
        if ($gt -match [regex]::Escape($ghost)) { $typedBad += "$gf names $ghost again - waves 3 and 4 moved the storyteller's write to savePopDesc (V365f) and a pane back here is a second owner of the same text with no ruler on it (SPEC V135, B98, T872, T874)" }
    }
}
# The owner the rule migrated to must still be standing, or the retirement quietly became a
# deletion. Named by function, not by check id: a check can be renumbered, a missing function
# cannot be argued with (SPEC V209, V20). It was two until T874 and it is ONE now, which is the
# whole point of V365(f) - one door, so there are no two halves left to disagree.
if (-not (LuaFn $rootTxt 'savePopDesc')) { $typedBad += "savePopDesc is gone from the root form - V249 retired INTO it, so the storyteller's write has no owner left anywhere (SPEC V365f, T872, T874)" }
if ($typedBad) { foreach ($b in $typedBad) { Fail "V249/V250 $b" } }
else { Pass "V249/V250 retired with the seven panes of T872 and T874 - not one of the four areas names a pane, NO_TEXT_YET or saveNuminaDesc, and the one owner the rule moved to (savePopDesc, behind the ?) is standing" }

# ---- V251: the five ritual rows that came off do not come back ---------------------------
# The names are burned like every other orphan (SPEC I3, V2): a sheet saved before the 85th
# round still carries what was in them, and a new field wearing one of those names would
# pour that stale value into an unrelated row.
#
# AMENDED for T880 (SPEC V251 amended, user 2026-09-01): the box grew back to 22 DRAWN rows and
# the five names stayed burned - which is why the two it gained are ritual_26 and ritual_27. The
# pattern below is unchanged and is the reason the round could not simply reuse _21 and _22: it
# is the only thing standing between a new row and a pre-85th sheet's stale value pouring into
# it. The COUNT is V204's (the constant is the highest index, 27, and 21..25 are its vacancies).
$ritGhost = @($files | Where-Object { (CodeOf $_.FullName) -match 'ritual_2[1-5]' })
if ($ritGhost) { foreach ($g in $ritGhost) { Fail "V251 $($g.Name) names one of the five ritual rows that came off in the 85th round - those names are burned, and a new row wearing one would be handed whatever an old sheet left in it (SPEC I46, V2, V251 amended)" } }
else { Pass "V251 not one of the five burned ritual names (ritual_21..ritual_25) is authored anywhere in the sheet - the two rows T880 added are _26 and _27" }

# ---- V252/V253: the list sorts itself, and what was just added flashes -------------------
# The fifteen PICKED rows are kept in level-then-name order and the five typed ones are left
# where the storyteller wrote them: the sort moves values between slots, and a typed ritual
# would leave its own description behind (SPEC I47, V252a, V249).
$r14 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20.11.lfm")))
$r14Doc = Doc (Join-Path $dir "WoD20.11.lfm")
$sortFn = LuaFn $r14 'sortRituals'
$flashFn = LuaFn $r14 'flashRitual'
$rankFn = LuaFn $r14 'ritualRank'
# Keyed on sortRituals and not on guardPick since T909: WoD20.11 carries the path guard AND the
# ritual guard now that the three column forms were dissolved into it, and "the first dataLink
# with a guardPick" stopped meaning "the ritual one" the moment they shared a file (SPEC I136j).
$guardLink = @($r14Doc.SelectNodes("//dataLink[contains(@onChange,'sortRituals')]") | ForEach-Object { $_.GetAttribute("onChange") })

if (-not $sortFn) { Fail "V252 sortRituals not found on WoD20.11 - nothing keeps the list in order" }
elseif (-not $rankFn) { Fail "V252 ritualRank not found - the sort key would be inline and the level read in a second place (SPEC V184)" }
elseif ($rankFn -notmatch '\^\(%d\+\)%\. ') { Fail "V252 the sort key does not read the level off the name prefix - a second table for the level is exactly what V184 forbids" }
elseif ($sortFn -notmatch 'local last = RITUAL_ROWS - RITUAL_FREE_ROWS;') { Fail "V252 the sort does not stop at the picked rows - it would move the typed ones and leave their descriptions behind (SPEC V252a, V249)" }
# The write-back reads list[k] and not list[i] since T880, and the two indices are the point: the
# rows are no longer contiguous (ritual_21..25 are burned, V251) so the walk runs over the ROW
# number while `list` stays dense, and the k-th value gathered belongs in the k-th row drawn.
# Indexing list by the row number would leave one row empty at the bottom for every vacancy passed
# on the way down - a ritual dropping off the sheet on a re-sort, with nothing said (SPEC V204
# amended, V263b). That the walk SKIPS the vacancies at all is V204's leg, not a second copy here.
elseif ($sortFn -notmatch 'setField\("ritual_" \.\. i, list\[k\] or ""\);') { Fail "V252 the sort does not write through setField with the dense cursor - a list already in order has to write nothing, or the link it hangs on bounces (SPEC V122), and with a hole in the numbering the value's place in `list` is not its row number (SPEC V204 amended)" }
# LEG (d) RETIRED in the 159th round (SPEC V252 amended, T872, user 2026-08-31 ask B). It
# charged the mark with following its value across a re-sort so the DESCRIPTION block kept
# showing the same ritual; wave 3 took the radio AND the block, so there is no mark to carry
# and no block to mislead - the damage (d) bought does not exist. Replaced by its own guard:
# a setField on ritualSel coming back would be writing a field nothing reads (V2, V8).
elseif ($sortFn -match 'ritualSel') { Fail "V252 the sort still writes ritualSel - the radio went out with the DESCRIPTION pane in T872, so this writes a field no widget owns and no reader reads (SPEC V252 amended, V2, V8)" }
elseif ($sortFn -notmatch 'if sortQuiet or sheet == nil then return; end;') { Fail "V252 the sort does not stand down for its own writes - each one comes back through the same dataLink and would sort a half-written list (SPEC V107)" }
elseif ($guardLink.Count -lt 1) { Fail "V252 the ritual guard dataLink is gone - the sort has nothing to hang on (SPEC V20)" }
elseif ($guardLink[0] -notmatch 'guardPick\(field, oldValue, newValue\); sortRituals\(self, field, newValue\);') { Fail "V252 sortRituals does not run right after guardPick on the same link - sorting a value the guard is about to revert is work thrown away (SPEC I47, V186)" }
else { Pass "V252 the fifteen picked rituals sort by level then name, through setField, standing down for their own writes - leg (d), the mark following its value, retired with the radio in T872" }

$hlTpl = $r14Doc.SelectSingleNode("//template[@name='RitualRow']")
$hlRect = $null
if ($hlTpl) { $hlRect = $hlTpl.SelectSingleNode("rectangle[@name='hl`$(field)']") }

if ($null -eq $hlTpl) { Fail "V253 RitualRow is gone from WoD20.11 (SPEC V20)" }
elseif ($null -eq $hlRect) { Fail "V253 the ritual row carries no highlight rectangle - there is nothing to flash (SPEC I47)" }
elseif ($hlRect.GetAttribute("visible") -ne 'false') { Fail "V253 the highlight is authored visible - every row would open lit" }
elseif ($hlRect.GetAttribute("hitTest") -ne 'false') { Fail "V253 the highlight takes the click - the row would be unusable for the second it is up" }
elseif ((@($hlTpl.ChildNodes | Where-Object { $_.NodeType -eq 'Element' })[-1]).GetAttribute("name") -ne 'hl$(field)') { Fail "V253 the highlight is not the LAST element of the row - the host paints in document order, so it would be painted OVER instead of over (SPEC I39, the reason dynXpEmpty closes the log box)" }
elseif ($hlRect.GetAttribute("color") -notmatch '^#[0-9A-Fa-f]{8}$') { Fail "V253 the highlight colour carries no alpha channel - a solid band would hide the name it is pointing at (SPEC I47)" }
elseif (-not $flashFn) { Fail "V253 flashRitual not found - nothing lights the row" }
elseif ($flashFn -notmatch 'rect\.visible = true;') { Fail "V253 flashRitual never lights the rectangle" }
elseif ($flashFn -notmatch 'setTimeout\(function\(\) rect\.visible = false; end, 1000\);') { Fail "V253 the flash is not put out after exactly 1000ms - a highlight with no timer is a highlight that stays (SPEC I47, R99)" }
elseif ($sortFn -notmatch 'flashRitual\(form, "ritual_" \.\. i\);') { Fail "V253 the flash is not fired from the sort - it has to land on the row that ended up with the value (SPEC V253d)" }
elseif ($sortFn.IndexOf('flashRitual') -lt $sortFn.IndexOf('setField("ritual_"')) { Fail "V253 the flash is looked up BEFORE the sort writes - it would light the row the value came from (SPEC V253d)" }
else { Pass "V253 the new ritual's row lights in the period's own colour, over the text, and goes out after 1000ms" }


# ---- V254: the specialty price is ONE value, and zero is a price -------------------------
# The two flags became one combo in the 87th round (SPEC I49). What this check owns is the
# part V154 cannot see from the Lua side: the list the combo OFFERS, the sentinel both halves
# have to agree on, the default written in two places, and the one item in that list that is
# prose. The prose item is the reason (d) exists at all - the traversal that enforces V17
# filters cbo* and this combo is cmb*, so nothing else on the sheet would ever ask whether
# `Purchase Forbidden` can be said in Portuguese (SPEC V209 leg a).
$specDoc  = Doc (Join-Path $dir "WoD20.10.lfm")
$specCbo  = $specDoc.SelectSingleNode("//comboBox[@name='cmbSpecCost']")
$specLink = $specDoc.SelectSingleNode("//dataLink[@field='stSpecCost']")
$deadFlag = @()
foreach ($f in $files) {
    $t = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName))
    if ($t -match 'stSpec[34]XP') { $deadFlag += $f.Name }
}
$specItems  = @()
$specValues = @()
if ($null -ne $specCbo) {
    $specItems  = @([regex]::Matches($specCbo.GetAttribute("items"),  "'([^']*)'") | ForEach-Object { $_.Groups[1].Value })
    $specValues = @([regex]::Matches($specCbo.GetAttribute("values"), "'([^']*)'") | ForEach-Object { $_.Groups[1].Value })
}
$specSentinel = [regex]::Match($rootTxt, 'local SPEC_FORBIDDEN = "([^"]+)";')
$wantPrices = @('0', '1', '2', '3', '4', '5')
$prose = @($specItems | Where-Object { $_ -notmatch '^\d+$' })
# The closed door is found by its ITEM being prose, not by sitting last: the invariant is
# about the set of prices and the sentinel under the door, and the order they are listed in
# is free to change (SPEC V254, the declared probe).
$proseIdx = -1
for ($i = 0; $i -lt $specItems.Count; $i++) { if ($specItems[$i] -notmatch '^\d+$') { $proseIdx = $i } }
$specSentinelValue = if ($proseIdx -ge 0 -and $proseIdx -lt $specValues.Count) { $specValues[$proseIdx] } else { '' }
$specPrices = @(for ($i = 0; $i -lt $specValues.Count; $i++) { if ($i -ne $proseIdx) { $specValues[$i] } })

if ($null -eq $specCbo) { Fail "V254 cmbSpecCost is not on WoD20.10 - the price the storyteller sets has no widget (SPEC I49)" }
elseif ($specItems.Count -eq 0 -or $specValues.Count -eq 0) { Fail "V254 cmbSpecCost offers nothing - this check would be measuring an empty list (SPEC V209, V20)" }
elseif ($specCbo.GetAttribute("field") -ne 'stSpecCost') { Fail "V254 cmbSpecCost writes '$($specCbo.GetAttribute('field'))', not stSpecCost - the price would be stored somewhere the Lua does not read" }
elseif ($deadFlag.Count -gt 0) { Fail "V254 $($deadFlag -join ', ') still names stSpec3XP/stSpec4XP - those two are ORPHANS and the name is burned (SPEC I3, V2, V254a)" }
elseif ($specItems.Count -ne $specValues.Count) { Fail "V254 cmbSpecCost shows $($specItems.Count) items for $($specValues.Count) values - one of them would resolve to nothing (SPEC V12)" }
elseif ($prose.Count -ne 1) { Fail "V254 cmbSpecCost carries $($prose.Count) prose item(s), expected exactly one closed-door entry (SPEC I49)" }
elseif (@(Compare-Object @($specPrices | Sort-Object) $wantPrices).Count -gt 0) { Fail "V254 cmbSpecCost prices are {$($specPrices -join ', ')}, expected {$($wantPrices -join ', ')} - zero is a PRICE and has to be offered (SPEC V254b)" }
elseif (-not $specSentinel.Success) { Fail "V254 SPEC_FORBIDDEN is not declared on the root form - the closed door would be a bare literal in whichever line asked last" }
elseif ($specSentinelValue -ne $specSentinel.Groups[1].Value) { Fail "V254 the combo writes '$specSentinelValue' under '$($prose[0])' and the Lua compares against '$($specSentinel.Groups[1].Value)' - the door would never read as shut (SPEC V12)" }
elseif ($null -eq $specLink) { Fail "V254 no dataLink declares a default for stSpecCost - a sheet that never touched the combo would open on nothing (SPEC V254c)" }
elseif ($specLink.GetAttribute("defaultValue") -ne '3') { Fail "V254 the dataLink defaults stSpecCost to '$($specLink.GetAttribute('defaultValue'))' while specCost falls back to 3 - the two places that answer for an empty field must not disagree (SPEC V254c)" }
elseif ($rootTxt -notmatch 'tonumber\(sheet\.stSpecCost\) or 3') { Fail "V254 specCost does not fall back to the same 3 the dataLink declares (SPEC V254c)" }
elseif (-not $ptK.Contains($prose[0]) -or -not $enK.Contains($prose[0])) { Fail "V254 '$($prose[0])' has no wod. key in both .lang blocks - that is the HOST's axis, and half of leg (d) (SPEC V254d, V10)" }
elseif (-not $embedded.ContainsKey($prose[0])) { Fail "V254 '$($prose[0])' has .lang keys but is absent from the PT map in WoD20.6 - the two are different AXES, not two copies: .lang answers to the host's language and the map answers to the sheet's own, which is the one the player switches. Keying only the first is exactly how B57 shipped (SPEC V254d, I52)" }
else { Pass "V254 the specialty price is one field: $($specPrices.Count) prices plus '$($prose[0])', sentinel '$specSentinelValue', default 3 in both places, keyed on both language axes" }

# ---- V257: a cmb* combo translates its OWN items, and never its own values ---------------
# B57 in one line: pickerItems opens with a cbo* test and returns, so no cmb* had ever been
# through the axis the PLAYER switches - the sheet's own `language` field - and `Purchase
# Forbidden` shipped in English on a sheet set to Portuguese with the gate green, because the
# only check that looked at the word measured the .lang file (the HOST's axis).
#
# Four legs. (a) the branch is reached and selects BY PREFIX - a nominal test would fix the
# combo of the day and leave the next one born mute, which is the shape B57 arrived in.
# (b) it writes `items` and never `values`: the item is what the player reads and the value is
# what the NDB keeps, so switching language cannot touch stored data and a second run lands in
# the same place (SPEC V22). (c) there is no second list - `items` and `values` are the same
# content for every cmb* the branch touches, which is what makes items REBUILDABLE from values
# and is why the prose value is the English text itself (SPEC V24, V208). (d) every prose item
# of a cmb* has a key in the PT map - V28 cannot reach these, its collector filters cbo*.
#
# Leg (c) carries a declared exception, held in BOTH directions: cmbDominatorGen shows `4th`
# and stores `4` because maxDiscLevel reads the number, so it is named in CMB_DISPLAY_MAP and
# skipped. A cmb* whose lists differ and is NOT named there fails, and a name there whose lists
# match fails as a dead exception - so the list cannot quietly grow into a way out.
$cmbFn    = LuaFn $hh6 'cmbItems'
$langFn6  = LuaFn $hh6 'applyLanguage'
$cmbSkip  = @()
$mapBlk   = [regex]::Match($hh6, 'local CMB_DISPLAY_MAP = \{([^}]*)\}')
if ($mapBlk.Success) { $cmbSkip = @([regex]::Matches($mapBlk.Groups[1].Value, '(\w+)\s*=\s*true') | ForEach-Object { $_.Groups[1].Value }) }

$cmbBad   = @()
$cmbSeen  = @()
$cmbProse = @()
foreach ($f in $files) {
    foreach ($cb in (Doc $f.FullName).SelectNodes("//comboBox[@name]")) {
        $nm = $cb.GetAttribute("name")
        if ($nm.Length -lt 3 -or $nm.Substring(0, 3) -cne 'cmb') { continue }
        $cmbSeen += "$($f.Name)/$nm"

        $it = @([regex]::Matches($cb.GetAttribute("items"),  "'([^']*)'") | ForEach-Object { $_.Groups[1].Value })
        $vl = @([regex]::Matches($cb.GetAttribute("values"), "'([^']*)'") | ForEach-Object { $_.Groups[1].Value })
        $same = ($it.Count -gt 0 -and $it.Count -eq $vl.Count)
        if ($same) { for ($i = 0; $i -lt $it.Count; $i++) { if ($it[$i] -cne $vl[$i]) { $same = $false } } }
        $declared = $cmbSkip -ccontains $nm

        if ($it.Count -eq 0 -or $vl.Count -eq 0) {
            $cmbBad += "$nm authors no items/values pair in $($f.Name) - leg (c) has nothing to compare (SPEC V209)"
        } elseif ($same -and $declared) {
            $cmbBad += "$nm is named in CMB_DISPLAY_MAP but its items ARE its values - a dead exception, and the next reader will take the list as permission (SPEC V257c)"
        } elseif (-not $same -and -not $declared) {
            $cmbBad += "$nm shows a list its values cannot rebuild and is not declared in CMB_DISPLAY_MAP - the branch would overwrite what the XML authored (SPEC V257c)"
        }

        # Only the combos the branch actually touches owe the PT map a key: a declared
        # exception is never rebuilt from its values, so its values are not what gets shown.
        if (-not $declared) {
            foreach ($v in $vl) {
                if ($v -ne '' -and $v -notmatch '^\d+$') {
                    $cmbProse += $v
                    if (-not $embedded.ContainsKey($v)) { $cmbBad += "$nm offers '$v' and the PT map has no key for it - it would show in English on a sheet set to Portuguese, which is B57 exactly (SPEC V257d)" }
                }
            }
        }
    }
}

if (-not $cmbFn) { $cmbBad += "cmbItems is gone from WoD20.6 - nothing rebuilds a value combo's items when the SHEET changes language (SPEC I52, B57)" }
else {
    if ($cmbFn -notmatch 'string\.sub\(nm, 1, 3\) ~= "cmb"') { $cmbBad += "cmbItems does not select its combos by PREFIX - a nominal test covers the combo of the day and lets the next one be born mute (SPEC V257a)" }
    if ($cmbFn -match 'nm == "cmb') { $cmbBad += "cmbItems compares the name against a literal combo - the branch is by prefix or it covers one widget (SPEC V257a)" }
    if ($cmbFn -notmatch 'c\.values') { $cmbBad += "cmbItems does not read c.values - the items would be rebuilt from something other than what the NDB stores (SPEC V257b)" }
    if ($cmbFn -notmatch 'c\.items\s*=') { $cmbBad += "cmbItems writes no items - the translation reaches nothing (SPEC V257b)" }
    if ($cmbFn -match 'c\.values\s*=') { $cmbBad += "cmbItems WRITES c.values - a language switch would rewrite saved data and running it twice would not land in the same place (SPEC V257b, V22)" }
    if ($cmbFn -notmatch 'PT\[') { $cmbBad += "cmbItems never reads the PT map - the item would come out in English on a sheet set to Portuguese (SPEC V257d)" }
}
if (-not $langFn6) { $cmbBad += "applyLanguage is gone from WoD20.6 - nothing walks the sheet on a language switch" }
elseif ($langFn6 -notmatch 'cmbItems\(') { $cmbBad += "the language traversal never calls cmbItems - the branch exists and nothing reaches it (SPEC V257a)" }

if ($cmbSeen.Count -eq 0) { Fail "V257 no cmb* combo was read across the sheet - every leg of this check is measuring an empty set (SPEC V209, V20)" }
elseif ($cmbProse.Count -eq 0) { Fail "V257 the $($cmbSeen.Count) cmb* combo(s) read carry no prose item at all - leg (d) is measuring nothing, and the prose item is the whole reason this check exists (SPEC V209, B57)" }
elseif ($cmbBad) { foreach ($b in $cmbBad) { Fail "V257 $b" } }
else { Pass "V257 all $($cmbSeen.Count) cmb* combos rebuild items from values by prefix, $($cmbSkip.Count) declared display-mapped, $($cmbProse.Count) prose item(s) keyed in the PT map" }


# ---- V255: SPECIALTIES and BACKGROUNDS end in rows the storyteller TYPES ------------------
# The counts and the "typed rows come last" leg ride on V204/V248, which walk the row spec and
# now cover both boxes. What is left here is the shape of the row itself and the reader that
# has to stop short of it: the gift a trait hands out is locked by the combo's `enabled`, and
# a typed row has no combo - so a granted specialty landing in one could be typed over with
# the stamp still on it (SPEC V151, V160, V255d).
$typedSpec = @(
    # The roster SHRINKS one wave at a time and does not empty out early. SpecialityFreeRow and
    # OpenAbilityFreeRow went in T870; DiscFreeRow, SecPathFreeRow and RitualFreeRow went in T873
    # (SPEC I130a/I130b, V378) - every discipline, secondary path and ritual row is a picker now,
    # and the way to put a name of your own in one is the Custom line (SPEC I129). What this rule
    # says - a typed row keeps the picker row's dots and entry x, and never grows a combo - still
    # has THREE subjects, the family whose wave has not run yet. When wave 4 lands (T875) the last
    # of them goes and this check goes with it; deleting it now would uncover three live templates
    # to save a line.
    # ONE subject left after T875, and it is the third of wave 4 that could NOT convert.
    # HedgeRitualPicker draws its dots as read-only ART - no field, no xpClick - because a PICKED
    # ritual's level is the "<n>. " prefix the book puts on its name (SPEC V269a). HedgeRitualFree's
    # dots ARE the level, bound to $(field)_1.._5 (SPEC V269b, V269d). Converting it MEASURED as
    # deleting 25 fields that hold data - hedgeRitual_13_1..17_5 - so the third stopped and became
    # a question for the spec instead of a silent loss (SPEC V362a, B122).
    # EMPTY since T892, and the roster stays DECLARED rather than deleted: it shrank one wave at a
    # time and the shape of the shrinking is the record. HedgeRitualFree was the last one left, and
    # it is the one that could not convert while its dots CARRIED the level in $(field)_1.._5 -
    # swapping the template would have eaten 25 fields holding data (SPEC B122). T892 gave the
    # picker pane a Ritual Level row, so the level is ASKED there and the five fields are written
    # by the confirm instead of by a dot the player clicks (SPEC V269b, V378a).
)

# The guard that replaces the walk above. An empty roster measures NOTHING, so what is held here
# is the other half of the promise: a retired template coming BACK would put an editable row on a
# sheet where every row is a picker, and the loop over an empty list cannot notice (SPEC V20, B7).
$V255_RETIRED = @('SpecialityFreeRow', 'OpenAbilityFreeRow', 'DiscFreeRow', 'SecPathFreeRow',
                  'RitualFreeRow', 'NuminaFree', 'PsychicFree', 'HedgeRitualFree')
$typedBad2 = @()
$typedSeen = 0
foreach ($ts in $typedSpec) {
    $doc = Doc (Join-Path $dir $ts.File)
    $tpl = $doc.SelectSingleNode("//template[@name='$($ts.Tpl)']")
    if ($null -eq $tpl) { $typedBad2 += "$($ts.File) declares no $($ts.Tpl) - the typed rows have no shape (SPEC I50)"; continue }
    $typedSeen++
    # The row's NAME, which is the first <edit> and not the first thing carrying a field: these
    # six lead with the radioButton that selects the row (groupName=*Sel), and reading that as
    # the owner reported six correct templates as broken the moment the roster moved onto them.
    # The two the roster carried before had no radio, so the distinction never came up.
    $owner = @($tpl.SelectNodes("edit[@field]"))[0]
    if ($null -eq $owner) { $typedBad2 += "$($ts.Tpl) hands its name to no <edit> at all - a typed row is an <edit> or it is a picker again (SPEC V255b)"; continue }
    if (@($tpl.SelectNodes(".//comboBox")).Count -gt 0) { $typedBad2 += "$($ts.Tpl) still carries a comboBox - the row would offer a list it is not supposed to have (SPEC V255b)" }
    $nm = $owner.GetAttribute("name")
    if ($nm -like 'cbo*') { $typedBad2 += "$($ts.Tpl) names its entry '$nm' - a cbo* name puts it back inside pickerItems and PICKER_LIST (SPEC V255c, V211a)" }
    # the dot column is the picker row's, unchanged: a typed row is bought at the same price
    $pick = $doc.SelectSingleNode("//template[@name='$($ts.Pick)']")
    if ($null -ne $pick) {
        $dotsT = @($tpl.SelectNodes("imageCheckBox") | ForEach-Object { $_.GetAttribute("left") })
        $dotsP = @($pick.SelectNodes("imageCheckBox") | ForEach-Object { $_.GetAttribute("left") })
        if (($dotsT -join ',') -ne ($dotsP -join ',')) { $typedBad2 += "$($ts.Tpl) puts its dots at {$($dotsT -join ', ')} and $($ts.Pick) at {$($dotsP -join ', ')} - the two read as one box or neither does (SPEC V26)" }
        $entT = @($tpl.SelectNodes("*[@field]"))[0].GetAttribute("left")
        $entP = @($pick.SelectNodes("*[@field]"))[0].GetAttribute("left")
        if ($entT -ne $entP) { $typedBad2 += "$($ts.Tpl) starts its entry at x=$entT and $($ts.Pick) at x=$entP (SPEC V26, B12)" }
    }
}
$renderSpecFn = LuaFn $rootTxt 'renderSpecialities'
$grantSpecFn  = LuaFn $rootTxt 'grantSpeciality'
$bound = 'for i = 1, SPECIALITY_ROWS - SPECIALITY_FREE_ROWS, 1 do'
foreach ($gt255 in $V255_RETIRED) {
    foreach ($f255 in $files) {
        if ($null -ne (Doc $f255.FullName).SelectSingleNode("//template[@name='$gt255']")) {
            $typedBad2 += "$($f255.Name) declares '$gt255' again - that template was retired when its family became pickers, and a typed row back on the sheet is one level with two owners (SPEC V378a, V269b, V135)"
        }
    }
}
if ($typedBad2.Count -gt 0) { foreach ($b in $typedBad2) { Fail "V255 $b" } }
elseif (-not $renderSpecFn) { Fail "V255 renderSpecialities is gone - nothing locks a granted specialty" }
elseif ($renderSpecFn -notmatch [regex]::Escape($bound)) { Fail "V255 renderSpecialities still walks every row - it looks for a combo the typed rows do not have (SPEC V255d)" }
elseif (-not $grantSpecFn) { Fail "V255 grantSpeciality is gone - the gift a trait hands out has nowhere to land" }
elseif ($grantSpecFn -notmatch [regex]::Escape($bound)) { Fail "V255 the gift can still land in a typed row - nothing there locks it, so the player could type over a granted specialty and keep the stamp (SPEC V255d, V151)" }
else { Pass "V255 none of the $($V255_RETIRED.Count) retired typed-row template(s) is authored anywhere, and the granted specialty stops at the picker rows" }

# ---- V256 + V417: the darker ground under the SIX XP numbers is a TRIO -------------------
# Rectangle behind, transparent edit, palette key: any one of the three missing and the effect
# is simply gone - rdk exits 0, the gate stays green and the field looks like every other one.
# That is the B6 shape, so the three are checked together. The palette leg is V53's (the colour
# is authored, so it is already inside that census); what is measured here is that the colour
# is NOT the box's own black, which would paint the ground the same shade as the box.
#
# SCOPE went from two to SIX on 2026-09-04 (SPEC V417, V256 as amended, T941). It used to read
# edtTotalXP and edtSpentXP only, and the prose forbade in writing the very thing the owner then
# asked for - the same border on Current, on every tab. So the dark ground stopped being a LOCK
# cue and became a FIELD frame, and the lock cue is opacity ALONE (SPEC V111a): the two locked
# ones keep 0.75, the four editable ones carry none. That last half is NOT re-measured here -
# V111(b) has always failed an editable widget carrying opacity, and a second owner for one fact
# is the no-op V20 forbids.
#
# The count is the V417 leg and it is separate on purpose: the trio legs are true by vacancy over
# an empty roster, so a name dropped from the list would take its field out of the rule without a
# sound (SPEC V209, B7). Fewer than six read = red on its own.
$V417_XP = @(
    [pscustomobject]@{ F = 'WoD20.9.lfm';  E = 'edtTotalXP' }
    [pscustomobject]@{ F = 'WoD20.9.lfm';  E = 'edtSpentXP' }
    [pscustomobject]@{ F = 'WoD20.9.lfm';  E = 'edtCurrentXP' }
    [pscustomobject]@{ F = 'WoD20.1.lfm';  E = 'edtCurrentXPMain' }
    [pscustomobject]@{ F = 'WoD20.7.lfm';  E = 'edtCurrentXPNumina' }
    [pscustomobject]@{ F = 'WoD20.11.lfm'; E = 'edtCurrentXPGhoul' }
)
$xpBad = @()
$xpSeen = 0
foreach ($row256 in $V417_XP) {
    $nm = $row256.E
    $ed = (Doc (Join-Path $dir $row256.F)).SelectSingleNode("//edit[@name='$nm']")
    if ($null -eq $ed) { $xpBad += "$nm is not on $($row256.F) - the roster outlived the widget, and a rule over a name with no widget makes no sound (SPEC V209, B111)"; continue }
    $xpSeen++
    if ($ed.GetAttribute("transparent") -ne 'true') { $xpBad += "$nm is not transparent - it paints its own ground over the rectangle and the darker shade never shows (SPEC V256, V417)" }
    $prev = $ed.PreviousSibling
    while ($null -ne $prev -and $prev.NodeType -ne 'Element') { $prev = $prev.PreviousSibling }
    if ($null -eq $prev -or $prev.LocalName -ne 'rectangle') { $xpBad += "$nm has no rectangle declared before it - a later sibling would paint OVER the number instead of behind it (SPEC V256, V417)" }
    else {
        foreach ($a in @('left', 'top', 'width', 'height')) {
            if ($prev.GetAttribute($a) -ne $ed.GetAttribute($a)) { $xpBad += "the ground behind $nm is $a=$($prev.GetAttribute($a)) against the field's $($ed.GetAttribute($a)) - it would show as a band, not as the field's own ground" }
        }
        if ($prev.GetAttribute("color") -eq 'black') { $xpBad += "the ground behind $nm is authored 'black' - that is the BOX's colour in every palette, so the field would not read as darker at all (SPEC V256)" }
        if ($prev.GetAttribute("hitTest") -ne 'false') { $xpBad += "the ground behind $nm takes the click - the field under it would stop answering" }
    }
}
if ($xpSeen -lt 6) { Fail "V417 only $xpSeen of the six experience numbers were read - this check is covering less than the sheet has, and the trio legs pass by vacancy over what it cannot see (SPEC V417, V209)" }
elseif ($xpBad.Count -gt 0) { foreach ($b in $xpBad) { Fail "V256 $b" } }
else { Pass "V256/V417 all $xpSeen experience numbers stand on a themed ground of their own - rectangle declared before, transparent edit, a colour that is not the box's black" }
# ---- V418: the Combat tab closes in ROWS, and the foot is what pays for it ---------------
# SPEC V418, I144b, I144c, user 2026-09-04 items 1 and 2. Same shape as V280(d) on the Ghoul
# and V402 on the Main: what the owner asked for is boxes ENDING on one line, and the only
# number free to move is the foot - so the feet (25 under COMBAT TRAITS, 24 under WILLPOWER)
# are DERIVED and deliberately live in no roster. Legs (a) and (b) are what charges them.
#
# Leg (c) is NOT V40 again. V40 lights only when two rectangles actually overlap; (c) lights
# on the 5px gap that overlaps nothing, which is why growing COMBAT TRAITS alone - the obvious
# way to do item 1 - has to move the HEALTH mirror down with it. The mirror cannot pay by
# SHRINKING: it is authored at the ten-row case and only the renderer shrinks it (SPEC V49),
# so the top is what moves and the tab closes 671 -> 681.
$V418_ROW1 = @('COMBAT', 'COMBAT TRAITS')
$V418_ROW2 = @('ARMOR', 'VIRTUES', 'HEALTH')
$V418_FEET = @('ARMOR', 'WILLPOWER')
$v418Box = @{}
foreach ($bx418 in (Doc (Join-Path $dir 'WoD20.3.lfm')).SelectNodes("//scrollBox/layout[@left and @top and @width and @height]")) {
    $lb418 = $bx418.SelectSingleNode("label[@text]")
    if ($null -eq $lb418) { continue }
    $v418Box[$lb418.GetAttribute("text")] = $bx418
}
$v418Bad = @()
if ($v418Box.Count -lt 6) {
    Fail "V418 only $($v418Box.Count) titled section box(es) were read on WoD20.3.lfm, expected the 6 the tab draws - a row rule over the boxes it cannot see is true by vacancy (SPEC V209, B7)"
} else {
    $missing418 = @(($V418_ROW1 + $V418_ROW2 + $V418_FEET) | Sort-Object -Unique | Where-Object { -not $v418Box.ContainsKey($_) })
    if ($missing418.Count -gt 0) {
        Fail "V418 the Combat tab no longer carries a box titled $($missing418 -join ', ') - the rows are named, so a retitled box leaves the rule without a word (SPEC V209)"
    } else {
        $bot418 = {
            param($ttl)
            [int]$v418Box[$ttl].GetAttribute("top") + [int]$v418Box[$ttl].GetAttribute("height")
        }
        # (a) the first row closes on one line
        $r1 = @{}
        foreach ($t418 in $V418_ROW1) { $r1[$t418] = & $bot418 $t418 }
        $r1v = @($r1.Values | Sort-Object -Unique)
        if ($r1v.Count -ne 1) {
            $v418Bad += "the top row does not close on one line - " + (($V418_ROW1 | ForEach-Object { "$_ at $($r1[$_])" }) -join ' against ') + " (SPEC V418a, I144b)"
        }
        # (b) ARMOR and WILLPOWER close on one line
        $r3 = @{}
        foreach ($t418 in $V418_FEET) { $r3[$t418] = & $bot418 $t418 }
        $r3v = @($r3.Values | Sort-Object -Unique)
        if ($r3v.Count -ne 1) {
            $v418Bad += "the left column and WILLPOWER do not close on one line - " + (($V418_FEET | ForEach-Object { "$_ at $($r3[$_])" }) -join ' against ') + " (SPEC V418b, I144c)"
        }
        # (c) the second row opens on one line, 5 under the close of the first (SPEC V298)
        $tops418 = @{}
        foreach ($t418 in $V418_ROW2) { $tops418[$t418] = [int]$v418Box[$t418].GetAttribute("top") }
        $t2v = @($tops418.Values | Sort-Object -Unique)
        if ($t2v.Count -ne 1) {
            $v418Bad += "the second row does not open on one line - " + (($V418_ROW2 | ForEach-Object { "$_ at $($tops418[$_])" }) -join ' against ') + " (SPEC V418c). V40 cannot see this: it lights when two rectangles OVERLAP, and a step in the gap overlaps nothing"
        } elseif ($r1v.Count -eq 1 -and $t2v[0] -ne ($r1v[0] + 5)) {
            $v418Bad += "the second row opens at $($t2v[0]) under a first row closing at $($r1v[0]) - that is a gap of $($t2v[0] - $r1v[0]) where every section gap on the sheet is 5 (SPEC V418c, V298)"
        }
    }
}
if ($v418Bad.Count -gt 0) { foreach ($b418 in $v418Bad) { Fail "V418 $b418" } }
elseif ($v418Box.Count -ge 6) { Pass "V418 the $($v418Box.Count) Combat boxes close in rows - the top row on y=$(([int]$v418Box['COMBAT'].GetAttribute('top')) + [int]$v418Box['COMBAT'].GetAttribute('height')), ARMOR and WILLPOWER on y=$(([int]$v418Box['ARMOR'].GetAttribute('top')) + [int]$v418Box['ARMOR'].GetAttribute('height')), and the second row opening together 5 below" }
# ---- V420: the domitor's generation is BORN 13th, and the seed is keyed on nil ----------
# SPEC V420, I144i, the owner's ask of 2026-09-04 (Ghoul item 3). Two things are measured and
# neither is the sentence: WHERE the seed lives and WHAT it writes.
#
# The value is read off the combo and never typed here. cmbDominatorGen authors items (what the
# box shows: '13th') beside values (what it stores: '13'), and a combo handed a value outside
# its own values list paints EMPTY without raising a thing (SPEC B91). Writing '13th' would be
# green against any literal this check could carry, so the check asks the WIDGET which value
# wears the label the owner asked for.
#
# Keyed on nil and never on "": the picker authors an empty item, so "" is a DECISION and
# re-seeding hands 13 back to a player who cleared it on purpose - the same reading V104 makes
# of appearance_1 and I29 of the ten blood dots.
#
# Mutation (SPEC V20, V222): key it on `== nil or == ""` -> red; seed "13th" -> red (the value
# is not in values); move the seed to the Ghoul tab -> red (the tab may never be opened, which
# is the reason written for the game seed being here). Probe: touch the appearance_1 seed
# beside it -> GREEN, that one belongs to V104.
$gen420 = (Doc (Join-Path $dir 'WoD20.11.lfm')).SelectSingleNode("//comboBox[@name='cmbDominatorGen']")
$ready420 = [regex]::Match($rootTxt, '(?s)<event name="onNodeReady">(.*?)</event>')
if ($null -eq $gen420) {
    Fail "V420 cmbDominatorGen is not on WoD20.11 - the seed would be writing a field no widget shows, and the value it has to write is read off that widget (SPEC V420, V209)"
} elseif (-not $ready420.Success) {
    Fail "V420 the root form has no onNodeReady - the three seeds beside this one have nowhere to live (SPEC V420, V107, V209)"
} else {
    $items420  = @([regex]::Matches($gen420.GetAttribute('items'),  "'([^']*)'")  | ForEach-Object { $_.Groups[1].Value })
    $values420 = @([regex]::Matches($gen420.GetAttribute('values'), "'([^']*)'") | ForEach-Object { $_.Groups[1].Value })
    $ix420 = [array]::IndexOf($items420, '13th')
    if ($items420.Count -eq 0 -or $values420.Count -ne $items420.Count) {
        Fail "V420 cmbDominatorGen read back $($items420.Count) item(s) against $($values420.Count) value(s) - the value the seed has to write cannot be derived, and a check that cannot find its subject and passes is B94 (SPEC V420, V209)"
    } elseif ($ix420 -lt 0) {
        Fail "V420 cmbDominatorGen no longer offers '13th' - the generation the owner asked to start on is not on the list any more, so the seed would write a value the box paints EMPTY (SPEC V420, B91)"
    } else {
        $want420 = $values420[$ix420]
        $seed420 = [regex]::Match($ready420.Groups[1].Value, 'if\s+sheet\s*~=\s*nil\s+and\s+sheet\.dominatorGen\s*([^\r\n]*?)\s+then\s+setField\("dominatorGen",\s*"([^"]*)"\)')
        if (-not $seed420.Success) {
            Fail "V420 the root onNodeReady does not seed dominatorGen - a new sheet opens with no generation, and renderMaxDisc with no ceiling (SPEC V420, I144i)"
        } elseif ($seed420.Groups[1].Value.Trim() -ne '== nil') {
            Fail "V420 the dominatorGen seed is keyed on '$($seed420.Groups[1].Value.Trim())' and not on nil alone - the picker authors an empty item, so `"`" is a DECISION and re-seeding hands 13 back to a player who cleared it on purpose (SPEC V420, V15, V104, I29)"
        } elseif ($seed420.Groups[2].Value -ne $want420) {
            Fail "V420 the seed writes `"$($seed420.Groups[2].Value)`" where cmbDominatorGen stores `"$want420`" for '13th' - items are what the box SHOWS and values are what it keeps, and a value outside the list paints EMPTY without an error (SPEC V420, B91, I144i)"
        } else {
            Pass "V420 dominatorGen is seeded `"$want420`" ('13th') on the root's onNodeReady, keyed on nil alone - beside the three seeds that are there for the same reason, because the Ghoul tab may never be opened"
        }
    }
}

# ---- V422: the TEXT has a declared anchor INSIDE the field ------------------------------
# SPEC V422, Q62, I144l, I144d. The hole this closes is one of CATEGORY, not of arithmetic:
# V399, V403, V27, V239 and V240 all measure the CONTROL's rectangle and not one of them looks
# INSIDE it, so a number pinned to the top of its own 25px field passes every one of them with
# the gate green and the box centred to the exact pixel - which is precisely what the owner was
# looking at while the measurement kept answering "it is already centred".
#
# vertTextAlign appears 15 times on this sheet and on <label> only: 0 of the 46 <edit>, against
# 8,606 <edit> carrying it in the third-party sheets in this same repo. The rest of the sheet
# stays unanchored on purpose and that is DECLARED DEBT rather than a silent hole - I144l has
# the census, and the day the owner asks for all of them this roster goes from 6 to 46 in one
# place. Being born charging 46 would redden code nobody asked to touch (SPEC B106).
#
# Mutation (SPEC V20, V222): drop vertTextAlign from one of the six -> red. Probe: drop it from
# one of the 15 <label> that carry it -> GREEN, the scope is the XP value field.
$v422Bad = @()
$v422Seen = 0
foreach ($row422 in $V417_XP) {
    $ed422 = (Doc (Join-Path $dir $row422.F)).SelectSingleNode("//edit[@name='$($row422.E)']")
    if ($null -eq $ed422) { $v422Bad += "$($row422.E) is not on $($row422.F) (SPEC V209, B111)"; continue }
    $v422Seen++
    foreach ($ax422 in @('vertTextAlign', 'horzTextAlign')) {
        if ($ed422.GetAttribute($ax422) -ne 'center') {
            $v422Bad += "$($row422.E) declares $ax422='$($ed422.GetAttribute($ax422))' - the number needs an anchor on BOTH axes inside its own field, and every geometry rule on this sheet stops at the outside of the control (SPEC V422, I144l)"
        }
    }
}
if ($v422Seen -lt 6) { Fail "V422 only $v422Seen of the six experience edits were read - an anchoring rule over the fields it cannot see is true by vacancy (SPEC V422, V209, B7)" }
elseif ($v422Bad.Count -gt 0) { foreach ($b422 in $v422Bad) { Fail "V422 $b422" } }
else { Pass "V422 all $v422Seen experience numbers are anchored INSIDE their own field - vertTextAlign and horzTextAlign both centre, which is the one thing the control-rectangle rules never look at" }
# ---- V421: a combo that authors no items= is FILLED when the form opens -----------------
# SPEC V421, B146, Q64 outcome (b), the owner's report of 2026-09-04. cboHedgeAttr had exactly
# one caller - the <dataLink> on hedgeAffiliation - so a reader who reached the Numina tab
# without touching the Affiliation found the Casting Attribute EMPTY. rdk exits 0, the gate was
# green, and the screen is INDISTINGUISHABLE from data that was lost: the owner reports "it did
# not save" for a bug that may have lost nothing at all, and the round that believes the report
# goes hunting for a bad write.
#
# The roster is READ, never typed: every cbo* that does not author items= in the XML, because
# the list moved to PICKER_LIST (SPEC I27) - and that the reader can SEE. cboClan (WoD20.1) is
# authored visible="false" enabled="false" as a value carrier for the `clan` field, and a combo
# nobody can see paints nothing at all: the failure mode this rule guards is a box on screen
# that reads EMPTY, so a hidden holder is outside it rather than exempted from it.
# Measured 2026-09-04 the roster is ONE. A roster of one is
# fragile from both sides and the spec says which: if the (a) exit of Q64 ever wins, the combo
# becomes a dyn* picker, the roster goes to ZERO and this rule loses its subject - so the
# zero-guard is RED and not green, and the day it fires this rule is retired in writing rather
# than left passing over nothing (SPEC V20, V209, B7).
#
# The filler is found and not named: a function that calls pickerItems( and mentions the combo -
# with the Lua line comments STRIPPED first. Without that the slice of renderHedgePickers ran on
# into the comment block behind it, which names cboClan in prose, and the check certified a
# filler that fills nothing: a rule that reads PROSE as code cannot tell the two apart.
# Mutation (SPEC V222): drop the call from onShow -> red. Probe: drop one of the other four
# renderers from that same onShow -> GREEN, their controls author their own text.
$v421Bad = @()
$v421Seen = 0
$src421 = @{}
foreach ($f421 in $files) { $src421[$f421.Name] = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f421.FullName)) }
$ready421 = [regex]::Match($rootTxt, '(?s)<event name="onNodeReady">(.*?)</event>').Groups[1].Value

$fill421 = @{}
foreach ($k421 in $src421.Keys) {
    $s421 = $src421[$k421]
    $defs421 = @([regex]::Matches($s421, '(?m)^[ \t]*(?:local\s+)?function\s+([A-Za-z_]\w*)\s*\('))
    for ($n421 = 0; $n421 -lt $defs421.Count; $n421++) {
        $a421 = $defs421[$n421].Index
        $b421 = if ($n421 + 1 -lt $defs421.Count) { $defs421[$n421 + 1].Index } else { $s421.Length }
        $body421 = $s421.Substring($a421, $b421 - $a421)
        $body421 = [regex]::Replace($body421, '--[^\r\n]*', '')
        if ($body421 -match 'pickerItems\(') { $fill421[$defs421[$n421].Groups[1].Value] = $body421 }
    }
}

foreach ($f421 in $files) {
    foreach ($cb421 in (Doc $f421.FullName).SelectNodes("//comboBox[@name]")) {
        $nm421 = $cb421.GetAttribute("name")
        if ($nm421 -notlike 'cbo*' -or $cb421.HasAttribute("items")) { continue }
        if ($cb421.GetAttribute("visible") -eq 'false') { continue }
        $v421Seen++
        $open421 = [regex]::Match($src421[$f421.Name], '(?s)<event name="onShow">(.*?)</event>').Groups[1].Value + $ready421
        $named421 = @($fill421.Keys | Where-Object { $fill421[$_] -match [regex]::Escape($nm421) })
        if ($named421.Count -eq 0) {
            $v421Bad += "$nm421 on $($f421.Name) authors no items= and no function on the sheet fills it - the list moved to PICKER_LIST and nothing brings it back, so the combo paints EMPTY with rdk exiting 0 (SPEC V421, I27, B146)"
        } elseif (@($named421 | Where-Object { $open421 -match ([regex]::Escape($_) + '\s*\(') }).Count -eq 0) {
            $v421Bad += "$nm421 on $($f421.Name) is filled by $($named421 -join ' / ') and NONE of them runs when the form opens - a renderer reachable only through a dataLink on ANOTHER field leaves the combo empty for a reader who touches nothing, and an empty combo looks exactly like data that was lost (SPEC V421, B146)"
        }
    }
}
if ($v421Seen -lt 1) { Fail "V421 no cbo* without items= was found on the sheet - this rule has lost its subject, and a rule that cannot fail is a no-op: retire it in writing rather than leave it green (SPEC V421, V20, V209, B7)" }
elseif ($v421Bad.Count -gt 0) { foreach ($b421 in $v421Bad) { Fail "V421 $b421" } }
else { Pass "V421 all $v421Seen combo(s) that author no items= are filled when their form opens, not only through a dataLink on another field" }
# ---- V424: the SELECTION comes back once the LIST exists ---------------------------------
# SPEC V424, B150, B151, the owner's measurement of 2026-09-04. The failure mode is a WINDOW
# and not lost data: the host binds field -> control when the control is CREATED, and a combo
# that authors no items= has ZERO of them at that instant, because the list moved to
# PICKER_LIST (SPEC I27) and arrives later on applyLanguage's walk at LOAD. A selection has
# nowhere to land in an empty list, so it is dropped - and nothing put it back, because
# `current` is READ from sheet[field] for V200's orphan rule and was never written back.
#
# The screen reads exactly like data thrown away, which is how one report sent TWO rounds
# hunting a paint bug (SPEC B146, B151). That is the whole reason this is a rule and not a
# one-line fix left to stand on its own.
#
# Both halves are measured. The write-back has to be THERE, and it has to be GUARDED: an
# unconditional `c.value = current` fires onChange on every theme walk and every language
# walk, which is the idiom cmbMfRevenant already avoids (WoD20th.lfm:4968).
#
# ONE site (SPEC V135): pickerItems is what already knows `current` and already writes the
# pair, so restoring it in renderHedgePickers instead would leave the language walk without it.
#
# Mutation (SPEC V222): drop the write-back -> red; drop the GUARD -> red. Probe: touch V200's
# `current` computation -> GREEN here, that one belongs to V200.
$pi424 = [regex]::Match($hh6, '(?s)local function pickerItems\(.*?\r?\n\t{3}end;')
if (-not $pi424.Success) {
    Fail "V424 pickerItems is gone from WoD20.6 - the one place that writes a picker's list has no owner left, and every leg below reads nothing (SPEC V424, V209)"
} else {
    $body424 = $pi424.Value
    $pair424 = [regex]::Match($body424, '(?m)^[ \t]*(if not sameList\(c\.values, kept\) then )?c\.values = kept;[^\r\n]*\r?\n[ \t]*(if not sameList\(c\.items, shown\) then )?c\.items = shown;')
    if (-not $pair424.Success) {
        Fail "V424 pickerItems no longer writes c.values and c.items as one pair - this rule hangs off that write, and a check that cannot find its subject and passes is B94 (SPEC V424, V202, V209)"
    } else {
        $tail424 = $body424.Substring($pair424.Index + $pair424.Length)
        $back424 = [regex]::Match($tail424, 'c\.value\s*=\s*current\s*;')
        $grd424  = [regex]::Match($tail424, 'if[^\r\n]*c\.value\s*~=\s*current[^\r\n]*then[^\r\n]*c\.value\s*=\s*current')
        if (-not $back424.Success) {
            Fail "V424 pickerItems writes the list and never gives the SELECTION back - the host bound the field while the combo still had zero items, so the value it is holding has nowhere to land and the box opens BLANK over data that is perfectly intact (SPEC V424, B150)"
        } elseif (-not $grd424.Success) {
            Fail "V424 the selection is written to c.value UNGUARDED - it has to be guarded on `"c.value ~= current`", or every theme walk and every language walk fires onChange on a value nobody touched (SPEC V424, B150, and the idiom cmbMfRevenant already uses)"
        } else {
            Pass "V424 pickerItems gives the selection back after it writes the list, and the write is guarded - the combo whose list arrives after the bind opens on the value it is actually holding"
        }
    }
}

# ---- V425: an id of SPEC.md does not repeat ----------------------------------------------
# SPEC V425, B152, T953. V384 already asks that a CITED id EXISTS; nothing asked that it is not
# written TWICE, and the two are the same pair every rule of this kind needs (it exists, and it
# is unique). The failure mode is the worst a spec has and it is silent: a reused id hands the
# OLD line the new one's rule, rdk exits 0, the gate stays green, and both lines sit there each
# believing it owns the number. Measured 2026-09-04: the 178th round was one command away from
# writing V423 and T950 a second time, having derived "next id" from the RANGE it had read.
#
# Three legs, and the union is not a luxury: V386 already reads SPEC-ARCHIVE.md to charge status
# x on what was archived, so an id that is RETIRED there and REBORN here is the case only the
# union catches - neither file alone sees it.
#
# The parser is V410's, by the same three shapes, so this births no second reader (SPEC V135).
# §Q are deliberately OUT and it is by FORM, not by oversight: they are BULLETS of §C and never
# a line head, which is exactly what made Q62 read as vacant in 2026-09-03 - B152 is that same
# mistake one round later on an id the parser DOES see.
#
# Mutation (SPEC V222): duplicate one `V<n>:` line -> red; duplicate one `T<n>|` row -> red; put
# an id in the live file that already lives in the archive -> red on the union leg. Probe:
# duplicate a §Q bullet -> GREEN, the scope is line heads.
$v425Bad = @()
$v425Seen = 0
$fam425 = @(
    [pscustomobject]@{ K = 'T'; Rx = '(?m)^(T[0-9]+)\|' }
    [pscustomobject]@{ K = 'V'; Rx = '(?m)^(V[0-9]+):' }
    [pscustomobject]@{ K = 'B'; Rx = '(?m)^(B[0-9]+)\|' }
)
$live425 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $PSScriptRoot 'SPEC.md')))
$arcPath425 = Join-Path $PSScriptRoot 'SPEC-ARCHIVE.md'
$arc425 = if (Test-Path -LiteralPath $arcPath425) { [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($arcPath425)) } else { '' }

foreach ($f425 in $fam425) {
    $inLive = @([regex]::Matches($live425, $f425.Rx) | ForEach-Object { $_.Groups[1].Value })
    $inArc  = @([regex]::Matches($arc425,  $f425.Rx) | ForEach-Object { $_.Groups[1].Value })
    $v425Seen += $inLive.Count

    foreach ($dup425 in @($inLive | Group-Object | Where-Object { $_.Count -gt 1 })) {
        $v425Bad += "$($dup425.Name) is a line head $($dup425.Count) times in SPEC.md - a reused id hands the OLDER line the NEWER one's rule and nothing anywhere says so (SPEC V425, B152)"
    }
    foreach ($dup425 in @($inArc | Group-Object | Where-Object { $_.Count -gt 1 })) {
        $v425Bad += "$($dup425.Name) is a line head $($dup425.Count) times in SPEC-ARCHIVE.md - the archive is where a closed id goes to STAY one thing (SPEC V425, V386)"
    }
    foreach ($both425 in @($inLive | Where-Object { $inArc -contains $_ } | Sort-Object -Unique)) {
        $v425Bad += "$both425 is a line head in BOTH SPEC.md and SPEC-ARCHIVE.md - an id that was retired and then reborn in the live file is the case neither file catches alone (SPEC V425, V386, B152)"
    }
}
if ($v425Seen -lt 1) { Fail "V425 not one T/V/B line head was read out of SPEC.md - the parser drifted, and a uniqueness rule that reads nothing certifies every duplicate there is (SPEC V425, V209, B7)" }
elseif ($v425Bad.Count -gt 0) { foreach ($b425 in ($v425Bad | Select-Object -First 8)) { Fail "V425 $b425" } }
else { Pass "V425 all $v425Seen T/V/B ids are written once - unique inside SPEC.md, inside SPEC-ARCHIVE.md, and across the two" }

# ---- V258: an opaque entry field may not sit in the corner's bite ---------------------
# cornerType="innerRound" curves the border INWARD, so the disc of radius r centred on the
# vertex is OUTSIDE the shape and at height y from the edge the border sits at
# x = sqrt(r*r - y*y) (SPEC R96). B53 was that arc eating the TITLE of a tab button; this is
# the same arc eating the FRAME of an <edit>, one level in - the field's own box crosses the
# curve and reads as spilling out of the black.
#
# SCOPE is the four controls that paint a box of their own: edit, textEditor, richEdit and
# comboBox, and only while they are not transparent="true". A label paints no ground (and the
# section title is left=0 by construction, which is how V27 centres it); a dot is ART, a
# circle inside its box, so its ink never reaches the vertex; and the four DESCRIPTION panes
# plus the three ledger columns are transparent, which is the DECLARED way out of this rule -
# with no ground there is no frame to lay over the curve. Measuring ink that does not exist
# would take this from 12 findings to 84.
#
# The ruler is the SQUARE r x r, not the arc. dx*dx + dy*dy >= r*r is the exact curve, and it
# lets a 10/10 box through by 0.1px - which is precisely the "too close, it touches" the user
# reported. V228 made the same call for the same reason, pricing the arc at its maximum (2r)
# rather than at the bite measured at glyph height.
#
# r is READ from the rectangle, never the literal 14: the rule is written in terms of it, so
# a radius left to drift would loosen the requirement along with the thing it measures. That
# is the lesson V228 already paid for.
$arcBad  = @()
$arcSeen = 0
$ARC_KINDS = @('edit', 'textEditor', 'richEdit', 'comboBox')
foreach ($f in $files) {
    $xml = Doc $f.FullName

    # templates are inlined at their call site, so a row's fields are only reachable through
    # the call. The adapter shadows .Name with the name= attribute, so LocalName is the only
    # safe test here (HANDOFF section 7).
    $arcTpl = @{}
    foreach ($t in $xml.SelectNodes("//template")) { $arcTpl[$t.GetAttribute("name")] = $t }

    foreach ($box in $xml.SelectNodes("//layout[@width][@height]")) {
        $bw = -1; $bh = -1
        if (-not [int]::TryParse($box.GetAttribute("width"), [ref]$bw)) { continue }
        if (-not [int]::TryParse($box.GetAttribute("height"), [ref]$bh)) { continue }
        if ($bw -le 0 -or $bh -le 0) { continue }

        # a BOX is a layout with a backdrop, read exactly as V239 and V240 read it
        $back = $null
        foreach ($k in $box.ChildNodes) {
            if ($k.NodeType -ne 'Element' -or $k.LocalName -ne 'rectangle') { continue }
            $kl = -1; $kt = -1; $kw = -1
            [void][int]::TryParse($k.GetAttribute("left"), [ref]$kl)
            [void][int]::TryParse($k.GetAttribute("top"), [ref]$kt)
            [void][int]::TryParse($k.GetAttribute("width"), [ref]$kw)
            if ($k.GetAttribute("align") -eq 'client' -or ($kl -eq 0 -and $kt -eq 0 -and $kw -eq $bw)) { $back = $k; break }
        }
        if ($null -eq $back) { continue }
        # The ruler is the LARGEST radius this box can WEAR, not the one the XML spells (SPEC
        # I89g, V316f). Since the 117th round a palette writes the corner at runtime, so Modern
        # Nights renders 18 on a rectangle whose XML still says 14 - reading the attribute alone
        # would quietly measure the SMALLER bite and this check would cover less than the sheet
        # draws, which is B7 arriving by radius. Both sides are READ: the attribute here, the
        # palette radii at the top of this file.
        #
        # The XML gate stays FIRST and is not folded into the max: a rectangle that authors no
        # radius has no bite to clear, and sectionBox refuses it too, so no palette can ever put
        # a corner on it. Folding it in would start measuring the square strip floor.
        $rXml = 0
        [void][int]::TryParse($back.GetAttribute("xradius"), [ref]$rXml)
        if ($rXml -le 0) { continue }   # a square box has no bite to clear
        $r = $rXml
        foreach ($pr258 in $boxCornerRadii) { if ($pr258 -gt $r) { $r = [int][Math]::Ceiling($pr258) } }

        $title = '(untitled)'
        foreach ($k in $box.ChildNodes) {
            if ($k.NodeType -eq 'Element' -and $k.LocalName -eq 'label' -and $k.GetAttribute("left") -eq '0' -and $k.GetAttribute("width") -eq "$bw" -and $k.GetAttribute("text")) { $title = $k.GetAttribute("text"); break }
        }

        # walk the subtree carrying the offset, and stop at a nested box: its children are
        # measured against ITS corners on its own pass, not against this one's.
        $stack = New-Object System.Collections.Stack
        foreach ($k in $box.ChildNodes) { if ($k.NodeType -eq 'Element') { [void]$stack.Push(@($k, 0, 0)) } }
        while ($stack.Count -gt 0) {
            $it = $stack.Pop(); $n = $it[0]; $ox = $it[1]; $oy = $it[2]
            if ($n -eq $back) { continue }
            if ($n.LocalName -in @('dataLink', 'script', 'event', 'template')) { continue }
            $nl = 0; $nt = 0; $nw = 0; $nh = 0
            [void][int]::TryParse($n.GetAttribute("left"), [ref]$nl)
            [void][int]::TryParse($n.GetAttribute("top"), [ref]$nt)
            [void][int]::TryParse($n.GetAttribute("width"), [ref]$nw)
            [void][int]::TryParse($n.GetAttribute("height"), [ref]$nh)
            $nl += $ox; $nt += $oy

            if ($n.LocalName -eq 'layout') {
                $nb = $null
                $iw = -1; [void][int]::TryParse($n.GetAttribute("width"), [ref]$iw)
                foreach ($k in $n.ChildNodes) {
                    if ($k.NodeType -ne 'Element' -or $k.LocalName -ne 'rectangle') { continue }
                    $kl = -1; $kt = -1; $kw = -1
                    [void][int]::TryParse($k.GetAttribute("left"), [ref]$kl)
                    [void][int]::TryParse($k.GetAttribute("top"), [ref]$kt)
                    [void][int]::TryParse($k.GetAttribute("width"), [ref]$kw)
                    if ($k.GetAttribute("align") -eq 'client' -or ($kl -eq 0 -and $kt -eq 0 -and $kw -eq $iw)) { $nb = $k; break }
                }
                $ir = 0; if ($null -ne $nb) { [void][int]::TryParse($nb.GetAttribute("xradius"), [ref]$ir) }
                if ($ir -gt 0) { continue }
                foreach ($k in $n.ChildNodes) { if ($k.NodeType -eq 'Element') { [void]$stack.Push(@($k, $nl, $nt)) } }
                continue
            }
            # a scrollBox carries fields the same way a plain layout does - it paints no ground
            # and bites no corner of its own, so its children are still measured against the BOX
            # they are drawn in. Walking past it instead would have dropped the four ledger
            # columns out of the census the moment the 90th round wrapped them (SPEC I57), and
            # the count guard below is what said so.
            if ($n.LocalName -eq 'scrollBox') {
                foreach ($k in $n.ChildNodes) { if ($k.NodeType -eq 'Element') { [void]$stack.Push(@($k, $nl, $nt)) } }
                continue
            }
            if ($arcTpl.ContainsKey($n.LocalName)) {
                foreach ($k in $arcTpl[$n.LocalName].ChildNodes) { if ($k.NodeType -eq 'Element') { [void]$stack.Push(@($k, $nl, $nt)) } }
                continue
            }
            if ($n.LocalName -notin $ARC_KINDS) { continue }
            if ($n.GetAttribute("transparent") -eq 'true') { continue }   # declared way out: no ground, no frame
            if ($nw -le 0 -or $nh -le 0) { continue }

            $arcSeen++
            $who = $n.GetAttribute("name"); if (-not $who) { $who = $n.GetAttribute("field") }; if (-not $who) { $who = "<$($n.LocalName)>" }
            foreach ($c in @(
                @('top-left',     $nl,             $nt),
                @('top-right',    ($bw - $nl - $nw), $nt),
                @('bottom-left',  $nl,             ($bh - $nt - $nh)),
                @('bottom-right', ($bw - $nl - $nw), ($bh - $nt - $nh)))) {
                $dx = [int]$c[1]; $dy = [int]$c[2]
                if ($dx -lt 0 -or $dy -lt 0) { continue }   # the field overruns the box: V37/V40's business, not this one's
                if ($dx -lt $r -and $dy -lt $r) {
                    $arcBad += "$($f.Name) '$title' ($($bw)x$($bh)): $who reaches its $($c[0]) corner at $dx/$dy, inside the $($r)x$($r) the concave border bites out - the field's frame lies over the curve (SPEC R96, B53)"
                }
            }
        }
    }
}
# The census is the guard, and it counts what the check actually WALKED: 328 opaque entry
# fields live inside a rounded box as of the 89th round, template calls inlined. Of those, 44
# sit near enough to a corner for the bite to be in play and 12 were inside it - but pinning
# the guard to 44 would let the walker lose 284 fields and still call itself covered, which
# is how B56 stayed green for eighteen rounds while reading 5 pickers of 17. Fewer than the
# sheet draws means the traversal broke, not that the sheet shrank; a round that deliberately
# removes rows moves this number in the same commit, the way T574 moved the three 60s.
if ($arcSeen -lt 328) { Fail "V258 only $arcSeen opaque entry field(s) were measured, expected the 328 the sheet draws inside a rounded box - this check is covering less than the sheet has (SPEC V209)" }
elseif ($arcBad) { foreach ($b in $arcBad) { Fail "V258 $b" } }
else { Pass "V258 all $arcSeen opaque entry fields clear the corner bite - none of them enters a radius square" }

# ---- V259: REVOKED - absorbed by V298 (SPEC B70, user 2026-08-25) --------------------
# It measured DOMINATOR -> BLOOD POOL, and that pair is box-to-box under one parent, so V298
# already walks it among the 73 boxes it collects - with a zero-guard of its own. V259 had ONE
# leg and that leg WAS the gap, so nothing is left standing once the gap has an owner.
#
# It is deleted rather than pointed at the new number, because carrying the literal here is
# what B70 IS: the gap was spelled in THREE checks - this one, V298, and an undeclared leg
# inside V262 - and two rounds of amendment moved only one of them. This copy also drifted
# from its own SPEC line, which still read 10 while the check read 15. V298 owns the number;
# anything measuring a box gap delegates to it instead of writing the number down again.

# ---- V260: the order of the log is the STAMP, and there is one stamp ------------------
# The rows are a DIFF of the frozen character against this one, and a diff carries no
# history: "newest on top" (user 2026-08-23) can only come from something saved, which is
# why the round that asked for it added a field. Six legs (SPEC I56).
#
# (b) is the one worth naming: xpOrder is keyed exactly the way xpFree is, trait#level. Two
# stamps that drift into two key shapes is what burned `freeDots` for good in the 38th round,
# and the sheet is still carrying that name as an orphan because of it.
$ordBad  = @()
$clickC  = if ($clickFn.Success) { NoComments $clickFn.Groups[1].Value } else { '' }
$rowsC   = NoComments (LuaFn $rootTxt 'xpLedgerRows')
$riseC   = NoComments (LuaFn $rootTxt 'pushRise')
$spentC  = NoComments (LuaFn $rootTxt 'xpSetSpent')
$ordWr   = @([regex]::Matches((NoComments $rootTxt), 'setField\("xpOrder"'))
$ordFns  = @(@('xpClick', $clickC), @('xpSetSpent', $spentC)) | Where-Object { $_[1] -match 'setField\("xpOrder"' }

# (a) written in exactly two functions, and in xpClick the price stamp and the order stamp
# stand together: written apart they drift, and drift here is a line on top with no price
# against it, or a price with no place in the order.
if ($ordWr.Count -ne 3) { $ordBad += "xpOrder is written in $($ordWr.Count) place(s) - exactly three: the stamp a purchase leaves, the one a sale takes away, and the storyteller's correction moving to the top (SPEC V260a)" }
if ($ordFns.Count -ne 2) { $ordBad += "xpOrder is stamped from $($ordFns.Count) function(s) - only xpClick and xpSetSpent move a point or a correction (SPEC V260a)" }
if ($clickC -match 'setField\("xpFree"' -and $clickC -match 'setField\("xpOrder"') {
    $iFree = $clickC.LastIndexOf('setField("xpFree"')
    $iOrd  = $clickC.IndexOf('setField("xpOrder"')
    if ($iOrd -lt $iFree) { $ordBad += "the order stamp is written BEFORE the price stamp in xpClick - the two move with one mark and are read in one place (SPEC V260a)" }
}

# (b) one key grammar for both stamps, and the row carries it
if ($clickC -notmatch 'local okey = trait \.\. "#" \.\. \(want and \(level \+ 1\) or level\)') { $ordBad += "xpClick does not build the order key as trait#level - a second key grammar is what made freeDots unrecoverable (SPEC V260b, V138)" }
if ($riseC -notmatch 'key = field \.\. "#" \.\. lvl') { $ordBad += "a ledger row carries no key - there would be nothing for the stamp to be looked up by (SPEC V260b)" }
if ($rowsC -notmatch 'key = "xpManual#0"') { $ordBad += "the MANUAL row carries no key - the storyteller's correction could never take its place in the order (SPEC I44, V260b)" }

# (c) a sale hands the stamp back, exactly like the price stamp does
if ($clickC -notmatch 'elseif sheet\.xpOrder ~= nil then') { $ordBad += "a sale never reaches the order stamp - a point sold would keep its place at the top of a log it is not in (SPEC V260c)" }
if ($clickC -notmatch 'string\.gsub\(sheet\.xpOrder, "\|" \.\. okey \.\. "\|", "\|"\)') { $ordBad += "the sale does not drop the order stamp of the level it gave up (SPEC V260c)" }

# (d) the click in flight is handed in, or the row just bought comes up at the BOTTOM until
# something else redraws the sheet - green build, green gate, and the player watching (B6)
if ($rowsC -notmatch 'clickOrder') { $ordBad += "xpLedgerRows cannot be handed the stamp of the click being priced - the row just bought would sort to the bottom until the next render (SPEC V260d)" }
if ($rowsC -notmatch 'local ord = \(sheet\.xpOrder or ""\) \.\. "\|" \.\. \(clickOrder or ""\) \.\. "\|"') { $ordBad += "the walk does not append the pending click LAST - that append is the whole of why the new line lands on top (SPEC V260d)" }
if ($clickC -notmatch 'xpLedgerRows\(field, want, key, want and okey or nil\)') { $ordBad += "xpClick prices without handing over the order stamp it is about to write (SPEC V260d)" }

# (e) one sorter. Summing does not depend on order (V100) and two sorters diverge in silence.
$sortAll = @([regex]::Matches((NoComments $rootTxt), 'table\.sort\(rows'))
if ($sortAll.Count -ne 1) { $ordBad += "rows are sorted in $($sortAll.Count) place(s) - the ledger orders them once, where it builds them (SPEC V260e)" }
if ($rowsC -notmatch 'table\.sort\(rows') { $ordBad += "xpLedgerRows does not order its own rows - whoever does would be a second place that knows about order (SPEC V260e)" }
if ((NoComments $progTxt) -match 'table\.sort') { $ordBad += "WoD20.9 sorts the rows it is given - the renderer formats, it does not reorder (SPEC V260e)" }

# (f) stable tie-break. On the day of the update EVERY line is unstamped, so an unstable sort
# is not a corner case here: it deals the whole log a different way round on every render.
if ($rowsC -notmatch 'rows\[i\]\.seq = i') { $ordBad += "the rows are not decorated with their build index - table.sort is not stable, and an unstamped log would shuffle on every render (SPEC V260f)" }
if ($rowsC -notmatch 'return a\.seq < b\.seq') { $ordBad += "the sort has no stable tie-break - two rows with no stamp would come back in whatever order the sort left them (SPEC V260f)" }

if (-not $clickFn.Success -or -not $rowsC) { Fail "V260 xpClick or xpLedgerRows is missing from the root form - this check is reading nothing (SPEC V209)" }
elseif ($ordBad) { foreach ($b in $ordBad) { Fail "V260 $b" } }
else { Pass "V260 one order stamp, keyed like the price stamp, dropped on a sale, handed to the click in flight and sorted once" }

# ---- V261: the log SCROLLS, the box does not grow --------------------------------------
# The box grew with its rows until the 90th round and the tab's own scrollBox did the
# scrolling, which is what kept line N reading across all four columns. The user asked for a
# fixed box with a bar of its own (2026-08-23), and the shared parent is what carries the
# alignment now: four columns with four bars scroll four different amounts (SPEC I57).
$scrBad  = @()
# named list of its own: $cols is reused further up the file and PowerShell leaks script
# scope, so borrowing it here reads whatever the last loop left behind - which is how this
# check first came back measuring nothing at all
$LEDGER_COLS = @('dynXpType', 'dynXpTrait', 'dynXpLevel', 'dynXpCost')
$logScr  = $logDoc.SelectSingleNode("//layout[@name='xpLogBox']/scrollBox")
$logCols = @($logDoc.SelectNodes("//textEditor") | Where-Object { $LEDGER_COLS -contains $_.GetAttribute("name") })

# (a) nothing writes the box height any more, and the grower is gone by name
if ($logCode -match 'xpLogBox\.height') { $scrBad += "xpLogBox.height is written from Lua - a box that grows with its rows is the request undone (SPEC V261a)" }
if ($logCode -match 'xpLogHeight') { $scrBad += "xpLogHeight is back - it was the routine that grew the frame, and the columns' height has a writer of its own now (SPEC V261a)" }

# (b) the four columns hang off ONE scrollBox
if ($null -eq $logScr) { $scrBad += "there is no scrollBox inside xpLogBox - the rows would have nowhere to scroll and the box would have to grow again (SPEC V261b)" }
else {
    $inScroll = @($logScr.SelectNodes("textEditor") | Where-Object { $LEDGER_COLS -contains $_.GetAttribute("name") })
    if ($inScroll.Count -ne 4) { $scrBad += "$($inScroll.Count) of the four ledger columns are children of the scrollBox - a column outside it does not scroll with its row and line N stops reading across (SPEC V261b)" }
}

# (c) the heading and the state sentence do NOT scroll
if ($null -ne $logScr) {
    foreach ($k in $logScr.ChildNodes) {
        if ($k.NodeType -ne 'Element') { continue }
        if ($k.LocalName -eq 'label') { $scrBad += "a label sits inside the scrollBox ('$($k.GetAttribute("name"))$($k.GetAttribute("text"))') - the column headings and the state sentence belong to the box, not to the rows (SPEC V261c)" }
    }
}

# (d) one height, written once, for all four
$colHCalls = @([regex]::Matches($logCode, '(?m)^\s*xpColsHeight\('))
$colHWrite = @([regex]::Matches($logCode, '(?m)form\.dynXp\w+\.height\s*='))
if ($logCode -notmatch 'local function xpColsHeight\(form, h\)') { $scrBad += "there is no single writer for the column height - four heights written apart scroll four different amounts (SPEC V261d)" }
if ($colHCalls.Count -lt 2) { $scrBad += "xpColsHeight is called $($colHCalls.Count) time(s) - the empty path and the populated path must BOTH set a height, or one of them keeps a stale scroll range (SPEC V261d)" }
if ($colHWrite.Count -ne 4) { $scrBad += "$($colHWrite.Count) column height(s) are written, expected the four inside xpColsHeight - a height written anywhere else is a column scrolling on its own (SPEC V261d)" }

# (e) zero-guard: if the traversal breaks, say so instead of passing on nothing
if ($logCols.Count -ne 4) { Fail "V261 found $($logCols.Count) ledger column(s) on WoD20.9, expected 4 - this check is reading a tab that is not there (SPEC V209)" }
elseif ($scrBad) { foreach ($b in $scrBad) { Fail "V261 $b" } }
else { Pass "V261 the four columns scroll together inside a fixed box, and nothing grows the frame" }

# ---- V262: the Numina tab tiles, and there is no strip left to clear (SPEC I58, I117) ------
# Four legs until T830, and three of them had the STRIP and the three stacked PANES as their
# subject: (a) numStrip carries no align and the panes share one exact rect below it, (b) each
# pane's content fits the pane on both axes, (d) V40's pane exemption is measured by shared rect
# rather than proxied by <import>. T830 took the strip and the panes - the three lists stand side
# by side now and all three are visible (SPEC I117, V361a) - so all three legs lost their subject
# in one round. Kept as loops over an empty set they would report PASS over nothing, which is B7
# three times under one check id.
#
# Leg (c) SURVIVES and is the one that grew: it is what V193 retired into for the Ghoul areas and
# what V296 retired into for this tab, so it is now the only thing holding a row of boxes to its
# ruler on either tab. What moved is WHERE the boxes are looked for - the panes were the hosts and
# the form is the host now - and nothing about what is measured moved at all.
$numPath = Join-Path $dir "WoD20.7.lfm"
$v262Bad = @()
if (-not (Test-Path $numPath)) { Fail "V262 WoD20.7.lfm is missing - the Numina tab is gone" }
else {
    $numDoc   = Doc $numPath
    $numHost  = $numDoc.SelectSingleNode("//scrollBox")

    # The retirement's own guard, and it is the zero the old legs used to Fail on read the other
    # way round: a strip or a stacked pane coming BACK would come back UNMEASURED (SPEC B98).
    $numStrip = $numDoc.SelectSingleNode("//layout[@name='numStrip'] | //layout[@name='hedgeStrip']")
    $numPanes = @($numDoc.SelectNodes("//scrollBox/layout[starts-with(@name,'tab')]"))
    if ($null -ne $numStrip) { $v262Bad += "WoD20.7 declares '$($numStrip.GetAttribute('name'))' again - T830 took both sub-tab strips off this tab and legs (a), (b) and (d) retired with them, so a bar that comes back comes back with no ruler on it (SPEC I117, V268 retired, T830)" }
    if ($numPanes.Count -gt 0) { $v262Bad += "WoD20.7 holds $($numPanes.Count) stacked pane(s) again - the three lists are columns since T830 and exactly one pane visible is the shape that went with the strip (SPEC I117, V361a, T830)" }
    if ($null -eq $numHost) { Fail "V262 WoD20.7 has no scrollBox - leg (c) has no host to read boxes from (SPEC V209)" }
    else {
        # (c) One ruler, measured on the ROW and not on the box (SPEC I63): the top band is three
        # boxes across, so HEDGE MAGIC closes at 586 with TRUE FAITH carrying the line to 1410 -
        # and "every right-hand box ends on the same x" would redden code that is right (SPEC B7).
        #
        # What the leg protects is that no strip of dead space opens anywhere across a row: every
        # row of boxes has to TILE - open at 0 and close on the ruler the widest row sets. The
        # gutter between neighbours is NOT read here (SPEC B70); V298 owns that number. Shrinking
        # one box of a pair passes a "closes on the line" test through its sibling and fails V298.
        $numRows = @{}
        foreach ($bx in $numHost.SelectNodes("layout[rectangle]")) {
            $bl = 0; $bw = 0; $bt = 0
            if (-not ([int]::TryParse($bx.GetAttribute("left"), [ref]$bl) -and [int]::TryParse($bx.GetAttribute("width"), [ref]$bw) -and
                      [int]::TryParse($bx.GetAttribute("top"), [ref]$bt))) { continue }

            # A BOX, told from a strip the way V239 and V240 tell them apart: the box wears a
            # background rectangle, a strip wears loose pills. The test is kept although no strip
            # is left to catch - it is what makes "box" mean something here rather than "layout".
            $hasBack = $false
            foreach ($k in $bx.ChildNodes) {
                if ($k.NodeType -ne 'Element' -or $k.LocalName -ne 'rectangle') { continue }
                $kl = -1; $kt = -1; $kw = -1
                [void][int]::TryParse($k.GetAttribute("left"), [ref]$kl)
                [void][int]::TryParse($k.GetAttribute("top"), [ref]$kt)
                [void][int]::TryParse($k.GetAttribute("width"), [ref]$kw)
                if ($k.GetAttribute("align") -eq 'client' -or ($kl -eq 0 -and $kt -eq 0 -and $kw -eq $bw)) { $hasBack = $true; break }
            }
            if (-not $hasBack) { continue }
            $t = $bx.SelectSingleNode("label")
            $nm = if ($t) { $t.GetAttribute("text") } else { "box" }
            if (-not $numRows.ContainsKey($bt)) { $numRows[$bt] = @() }
            $numRows[$bt] += [pscustomobject]@{ L = $bl; R = $bl + $bw; N = $nm }
        }
        if ($numRows.Count -lt 2) { $v262Bad += "only $($numRows.Count) row(s) of boxes were measured, expected the 2 the tab draws - the band and the three columns - and a ruler struck over one row is true for any width ever written (SPEC V209, V20)" }
        else {
            # The ruler is the WIDEST row of the tab, never a number written here (SPEC V262c,
            # T618): 1270 was the literal that kept this invariant and V267a red for four rounds
            # when T615 moved the panes, which is B7 arriving through the ruler.
            $numRuler = 0
            foreach ($k in $numRows.Keys) {
                $r = (@($numRows[$k] | Sort-Object L))[-1].R
                if ($r -gt $numRuler) { $numRuler = $r }
            }
            foreach ($k in $numRows.Keys) {
                $row = @($numRows[$k] | Sort-Object L)
                $shape = ($row | ForEach-Object { "$($_.N) $($_.L)..$($_.R)" }) -join ' | '
                if ($row[0].L -ne 0) { $v262Bad += "row @$k opens at x=$($row[0].L), not at 0 - a row that starts short leaves dead space no other check reads: $shape" }
                if ($row[$row.Count - 1].R -ne $numRuler) { $v262Bad += "row @$k closes at x=$($row[$row.Count - 1].R), not on the $numRuler the widest row of the tab sets: $shape" }
            }
        }
    }

    if ($v262Bad) { foreach ($b in $v262Bad) { Fail "V262 $b" } }
    else { Pass "V262 no strip and no stacked pane are left on the Numina tab, and its $($numRows.Count) rows of boxes tile from 0 to $numRuler" }
}

# ---- V263: the two numina lists count their rows in ONE place (SPEC I59, I60) ---------
# Same weld V145 puts on BACKGROUND_ROWS and V147 on SPECIALITY_ROWS, and for the reason B46
# spelled out: RITUAL_ROWS said 20 while the XML drew 25 for three green rounds, because the
# constant had no reader to disagree with it.
#
#   (a) constant == template calls in the .lfm that draws the box
#   (b) the typed rows are the LAST indices, so every reader is 'picker = total - typed'
#   (c) no count LITERAL survives in the Lua that walks them
$v263Bad = @()
$numTxt263 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($numPath))
$rootFor263 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($rootPath))
$rowSpec263 = @(
    @{ Const = 'NUMINA_ROWS';  Free = 'NUMINA_FREE_ROWS';  Picker = 'HedgePicker';   FreeTpl = 'NuminaFree';  Base = 'numina' },
    # psychic declares a VACANT set from T830 on and numina does not, which is the honest asymmetry:
    # one family had four ids burned by decision and the other has none (SPEC Q32, V362b, V204).
    @{ Const = 'PSYCHIC_ROWS'; Free = 'PSYCHIC_FREE_ROWS'; Picker = 'PsychicPicker'; FreeTpl = 'PsychicFree'; Base = 'psychic'; Vacant = 'PSYCHIC_VACANT' }
)
$v263Seen = 0
foreach ($rs in $rowSpec263) {
    $cm = [regex]::Match($rootFor263, "(?m)^\s*$($rs.Const)\s*=\s*(\d+);")
    $fm = [regex]::Match($rootFor263, "(?m)^\s*$($rs.Free)\s*=\s*(\d+);")
    if (-not $cm.Success) { $v263Bad += "$($rs.Const) is not declared on the root form - the row count would be a literal in every loop"; continue }
    if (-not $fm.Success) { $v263Bad += "$($rs.Free) is not declared on the root form - the typed rows would be a list of indices to forget"; continue }
    $v263Seen++
    $total = [int]$cm.Groups[1].Value
    $typed = [int]$fm.Groups[1].Value

    $pickIdx = @([regex]::Matches($numTxt263, "<$($rs.Picker)\s+field=`"$($rs.Base)_(\d+)`"") | ForEach-Object { [int]$_.Groups[1].Value })
    $freeIdx = @([regex]::Matches($numTxt263, "<$($rs.FreeTpl)\s+field=`"$($rs.Base)_(\d+)`"") | ForEach-Object { [int]$_.Groups[1].Value })

    # (a) AMENDED in T830, and this is the leg the psychic hole was always going to reach: the
    # constant is the HIGHEST INDEX and what the XML draws is that minus the VACANT ids. Every
    # vacancy has to be NAMED in one of I3's two lists - that last test is what stops a hole from
    # becoming the excuse for a loose index, which is B116 (SPEC V204 amended, V263a, Q32).
    $vac263 = @{}
    if ($rs.Vacant) {
        $vm263 = [regex]::Match($rootFor263, '(?m)^\s*' + $rs.Vacant + '\s*=\s*\{([^}]*)\}')
        if (-not $vm263.Success) { $v263Bad += "$($rs.Vacant) is not declared on the root form - $($rs.Base) carries a hole and no reader can be told WHICH index is missing (SPEC V204, V263a)" }
        else {
            foreach ($m263 in [regex]::Matches($vm263.Groups[1].Value, '\[\s*(\d+)\s*\]')) {
                $iv263 = [int]$m263.Groups[1].Value
                $vac263[$iv263] = $true
                $nm263 = "$($rs.Base)_$iv263"
                if ($I3_ORPHANS -notcontains $nm263 -and $I3_DELETED -notcontains $nm263) { $v263Bad += "$($rs.Vacant) burns index $iv263 and '$nm263' is in NEITHER list of I3 - a hole nobody declared is a loose index wearing a hole's clothes (SPEC V204, V362b, B116)" }
                if ($pickIdx -contains $iv263 -or $freeIdx -contains $iv263) { $v263Bad += "$($rs.Vacant) burns index $iv263 and the XML draws $($rs.Base)_$iv263 anyway - the id is burned, and a row wearing it gives the old field the new row's data in silence (SPEC V362c, V251)" }
            }
        }
    }
    if (($pickIdx.Count + $freeIdx.Count) -ne ($total - $vac263.Count)) { $v263Bad += "$($rs.Const) says $total with $($vac263.Count) vacant, so the XML should draw $($total - $vac263.Count) $($rs.Base) row(s) and it draws $($pickIdx.Count + $freeIdx.Count) - a row no loop walks is a dot that costs nothing and never reaches the log (SPEC B46, V263a)" }
    if ($freeIdx.Count -ne $typed) { $v263Bad += "$($rs.Free) says $typed but the XML draws $($freeIdx.Count) typed $($rs.Base) row(s)" }

    # (b)
    if ($freeIdx.Count -gt 0 -and $pickIdx.Count -gt 0) {
        $firstFree = ($freeIdx | Measure-Object -Minimum).Minimum
        $lastPick  = ($pickIdx | Measure-Object -Maximum).Maximum
        if ($firstFree -lt $lastPick) { $v263Bad += "typed row $($rs.Base)_$firstFree sits above picker row $($rs.Base)_$lastPick - the typed rows are the LAST ones, or 'picker = total - typed' stops being true for every reader (SPEC V255a)" }
    }
}
# (c)
foreach ($m in [regex]::Matches($rootFor263, 'numinaTypeInUse\s*\(([^)]*)\)')) {
    if ($m.Groups[1].Value -match ',\s*\d+\s*$') { $v263Bad += "numinaTypeInUse is called with a literal count ($($m.Groups[1].Value.Trim())) - that is the number B46 got wrong with nobody to notice" }
}
if ($rootFor263 -notmatch 'count\s*=\s*NUMINA_ROWS' -or $rootFor263 -notmatch 'count\s*=\s*PSYCHIC_ROWS') { $v263Bad += "XP_NUMINA does not read the two constants - declareTrait and the ledger would walk a count of their own" }

if ($v263Seen -ne 2) { Fail "V263 only $v263Seen of the two numina row counts were read - this check is covering less than the tab has (SPEC V209)" }
elseif ($v263Bad) { foreach ($b in $v263Bad) { Fail "V263 $b" } }
else { Pass "V263 both numina lists declare their row count once, the typed rows are the last of each, and no count literal survives" }

# ---- V264: the HEDGE MAGIC box and the quintessence dots (SPEC I60) -------------------
#   (a) hedgeAttr is the nine of XP_ATTRS, compared in BOTH directions (the shape of V148)
#   (b) quint_* are FREE dots, a NOMINAL exception beside bloodPool_* (V219), never a loosening
#   (c) the ten sit on one row with a constant pitch
$v264Bad = @()
$attrDecl = [regex]::Match($rootFor263, '(?s)local XP_ATTRS = \{(.*?)\};')
$attrNames = @()
if ($attrDecl.Success) { $attrNames = @([regex]::Matches($attrDecl.Groups[1].Value, '\{"([^"]+)",') | ForEach-Object { $_.Groups[1].Value }) }
$hedgeList = @()
if ($PICKER.ContainsKey('hedgeAttr')) { $hedgeList = @($PICKER['hedgeAttr'] | Where-Object { $_ -ne '' }) }

if ($attrNames.Count -ne 9) { Fail "V264 XP_ATTRS parsed to $($attrNames.Count) name(s), expected 9 - leg (a) has nothing to compare against (SPEC V209)" }
elseif ($hedgeList.Count -eq 0) { Fail "V264 PICKER_LIST has no 'hedgeAttr' list - the casting-attribute picker would open blank with the gate green (SPEC B6, V211a)" }
else {
    foreach ($a in @($attrNames | Where-Object { $hedgeList -notcontains $_ })) { $v264Bad += "'$a' is an XP_ATTRS attribute the casting picker does not offer" }
    foreach ($h in @($hedgeList | Where-Object { $attrNames -notcontains $_ })) { $v264Bad += "the casting picker offers '$h', which is not one of the nine attributes" }
    if ($PICKER['hedgeAttr'][0] -ne '') { $v264Bad += "the casting picker does not start empty - there would be no way to clear it (SPEC V15)" }
}

$quintDots = @($numDoc.SelectNodes("//imageCheckBox") | Where-Object { $_.GetAttribute("field") -match '^quint_\d+$' })
if ($quintDots.Count -ne 10) { Fail "V264 $($quintDots.Count) quintessence dot(s) were read, expected 10 - this check is covering less than the box draws (SPEC V209)" }
else {
    foreach ($q in $quintDots) {
        $qf = $q.GetAttribute("field")
        if ($q.GetAttribute("onClick")) { $v264Bad += "$qf carries an onClick - quintessence is a RESOURCE and reaches xpClick through nothing (SPEC V219, I9)" }
        if ($q.GetAttribute("autoChange") -eq 'false') { $v264Bad += "$qf is autoChange='false' - a free dot marks itself, which is what makes it free" }
    }
    if ($rootFor263 -match 'declareTrait\("quint') { $v264Bad += "quint_* is declared as a trait - it would be priced, refused and logged like a rating (SPEC V219)" }

    # (c) one row, one pitch: a resource row split across two tops is V37's defect in a shape
    # nothing measures, because V37 weighs a row against its NEIGHBOUR and this is one row.
    $qTops = @($quintDots | ForEach-Object { [int]$_.GetAttribute("top") } | Sort-Object -Unique)
    if ($qTops.Count -ne 1) { $v264Bad += "the ten dots sit on $($qTops.Count) different tops ($($qTops -join ', ')) - they are one row" }
    $qLefts = @($quintDots | ForEach-Object { [int]$_.GetAttribute("left") } | Sort-Object)
    $pitches = @()
    for ($i = 1; $i -lt $qLefts.Count; $i++) { $pitches += ($qLefts[$i] - $qLefts[$i - 1]) }
    if (@($pitches | Sort-Object -Unique).Count -ne 1) { $v264Bad += "the dots are spaced $((@($pitches | Sort-Object -Unique)) -join ', ') apart - one pitch, or the row reads as two groups" }
}
if ($v264Bad) { foreach ($b in $v264Bad) { Fail "V264 $b" } }
else { Pass "V264 the casting picker is exactly the nine attributes and the ten quintessence dots are free, evenly spaced and priced by nothing" }

# ---- V265: the True Faith pane has no picker (SPEC I61) ------------------------------
# This REPLACES V50, which measured a mechanism the 90th round removed: it asked the renderer
# to special-case the faith ROW so any of four religions resolved to the one entry. There is no
# row to select any more, so what is worth measuring is the opposite - that nothing drives that
# block but the fixed key.
#
# T874 moved the SURFACE and left the rule: the pane went with wave 4 (SPEC V365d) and the block
# is a ? on the row now. It is the one ? on the sheet that names its key as a LITERAL, and that is
# what leg (b) measures here - everywhere else a literal key would open one description from every
# row, which V333(d) refuses; here there is exactly one row and exactly one entry, so the literal
# IS the fixed key V50 always described. It also hands popOpen no row, so there is no override to
# write and savePopDesc stands down (SPEC V365f).
$v265Bad = @()
# The pane became the third BOX of the top band in T830, where WILLPOWER stood (SPEC I117d,
# V361c): found by TITLE and not by name, the way V296 found its boxes, because a pane name was
# something the strip needed and there is no strip left to need it (SPEC V188, V18).
$faithPane = $numDoc.SelectSingleNode("//layout[label/@text='TRUE FAITH']")
$faithQ = $numDoc.SelectSingleNode("//layout[label/@text='TRUE FAITH']//button[@name='btnQFaith']")
if ($null -eq $faithPane) { Fail "V265 there is no TRUE FAITH box in WoD20.7 - this check reads nothing (SPEC V209)" }
elseif ($null -eq $faithQ) { Fail "V265 btnQFaith is gone - the True Faith block has no door to its text at all, and the pane that used to be that door left in T874 (SPEC V209, V365d)" }
else {
    # (a)
    $faithCombos = @($faithPane.SelectNodes(".//comboBox"))
    if ($faithCombos.Count -ne 0) { $v265Bad += "the True Faith pane holds $($faithCombos.Count) comboBox(es) - religion is TYPED, and a list there would be a picker whose choice nothing reads (SPEC I61)" }
    $faithOwner = @($faithPane.SelectNodes(".//*[@field='faith']"))
    if ($faithOwner.Count -ne 1) { $v265Bad += "the 'faith' field has $($faithOwner.Count) owner(s) in the pane, expected exactly one (SPEC V1)" }
    elseif ($faithOwner[0].LocalName -ne 'edit') { $v265Bad += "'faith' is owned by a <$($faithOwner[0].LocalName)> - the row is typed since the 90th round (SPEC I61)" }
    $faithRadios = @($faithPane.SelectNodes(".//radioButton"))
    if ($faithRadios.Count -ne 0) { $v265Bad += "the True Faith pane holds $($faithRadios.Count) radio(s) - one Path means there is nothing to select (SPEC I61, V30)" }

    # (b) the ? opens on the FIXED key, through the one opener, with no row behind it
    if ($numTxt263 -match 'selected\s*==\s*"faith"') { $v265Bad += "WoD20.7 still special-cases the faith row - dead branch on a selection that cannot happen (SPEC V50, retired)" }
    $fq265 = $faithQ.GetAttribute("onClick")
    if ($fq265 -notmatch "^\s*popOpen\(self,\s*'Numina',\s*'True Faith',\s*nil,\s*nil\)") { $v265Bad += "btnQFaith opens with '$fq265' - the True Faith block reads ONE fixed key out of the Numina module and hands popOpen no row, because there is no slot behind it to override (SPEC I61, V50, V365f)" }
    if ($faithQ.GetAttribute("text") -ne '?') { $v265Bad += "btnQFaith reads '$($faithQ.GetAttribute('text'))' and not '?' - the door to a description is the ? everywhere on this sheet (SPEC I102h)" }
    if ($faithQ.HasAttribute("field")) { $v265Bad += "btnQFaith owns field '$($faithQ.GetAttribute('field'))' - the door to book text writes nothing onto the character (SPEC V29, R12)" }

    # (c) the dots did not move with the widget swap
    $faithDots = @($faithPane.SelectNodes(".//imageCheckBox") | Where-Object { $_.GetAttribute("field") -match '^faith_\d+$' })
    if ($faithDots.Count -ne 5) { $v265Bad += "$($faithDots.Count) True Faith dot(s) sit beside the row, expected 5 - they moved here from the Main tab and a saved rating follows the widget (SPEC V35, V5)" }
}
if ($v265Bad) { foreach ($b in $v265Bad) { Fail "V265 $b" } }
else { Pass "V265 the True Faith box types its religion, selects nothing, and its ? opens the one fixed entry with no row behind it" }

# ---- V266: the typed rows of Numina hand their DESCRIPTION to the storyteller ------------
# SPEC I62 - the design the three Ghoul areas got in the 85th round (V249/V250), on the two
# Numina panes and on the ritual pane the 91st round added beside them (SPEC I65).
#
# RETIRED in T874, one wave after its sister V249/V250 and for the identical reason: every
# subject went out with the panes. edtHedgeDesc, edtPsychicDesc, edtHedgeRitualDesc and
# edtFaithDesc are gone, and with them renderNuminaDesc, paintNuminaPane, saveNuminaDesc,
# numinaText, faithText and the four state tables (SPEC V365d, I118d).
#
# The RULE is not orphaned, and that is measured rather than assumed - it moved to owners that
# already exist and already have checks of their own:
#
#   * V365(f) for the storyteller's write. savePopDesc does the four things this check charged -
#     descQuiet so the renderer's write does not come back as typing, isStoryteller() RE-asked
#     because readOnly on the control is a look, popRow read at the MOMENT of the write, and the
#     write itself through descFieldOf, the one owner (V135, I45).
#   * V365(e) for the DATA. The window went, the fields did not: every descFieldOf(<row>) that
#     existed before exists after, which is the leg that separates "moved" from "eaten".
#   * V265 for True Faith, where the fixed key is now read by a ? that hands popOpen no row -
#     leg (e) here charged the same thing of a pane that no longer exists.
#   * V360c/V364b for the sentence. Three pane states collapsed into one, said in one place.
#
# The one leg that does NOT migrate is isTypedRow, and on purpose, exactly as V249 recorded it:
# the ? lives on PICKER rows (I102f, V365b), so a typed-row test there would refuse every write
# there is. The typed rows keep their <edit> until T875 converts them (SPEC I130b, V378d).
#
# What stays here is the retirement's own guard. It is deliberately NOT a copy of the ghost list
# V249/V250 already walks over the same file - that one refuses the WIDGETS and the saver coming
# back. This one refuses the second half, the READERS: a text helper reappearing on this tab is a
# second owner of a text that has exactly one (SPEC V135, B98).
$v266Bad = @()
$numPath266 = Join-Path $dir 'WoD20.7.lfm'
$numTxt266 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($numPath266))
$numDoc266 = Doc $numPath266

foreach ($ghost266 in @('renderNuminaDesc', 'paintNuminaPane', 'numinaText', 'faithText')) {
    if ($numTxt266 -match ('\b' + [regex]::Escape($ghost266) + '\s*\(')) {
        $v266Bad += "WoD20.7 declares or calls $ghost266 again - the panes it served left in T874 and this ruler retired with them, so a reader that comes back is a second owner of one text with nothing measuring it (SPEC V365d, V135, B98, T874)"
    }
}

# The owner the rule migrated to has to be standing, or the retirement was a deletion. Named by
# FUNCTION and not by check id: a check can be renumbered, a missing function cannot be argued
# with (SPEC V209, V20).
if (-not (LuaFn $rootTxt 'savePopDesc')) { $v266Bad += "savePopDesc is gone from the root form - V266 retired INTO it, so what the storyteller types on a typed row has no owner left anywhere (SPEC V365f, T874)" }
if (-not (LuaFn $rootTxt 'descFor')) { $v266Bad += "descFor is gone from the root form - it is the ONE reader the ? and the picker pane share, and V266's read half retired into it (SPEC V381, V135, T874)" }

# A dataLink on a description field would repaint the box under the cursor on every keystroke.
# The rule outlives the panes because the ? writes into the same fields (SPEC V123, V249d).
foreach ($dl266 in $numDoc266.SelectNodes("//dataLink")) {
    $watch266 = "$($dl266.GetAttribute('field')) $($dl266.GetAttribute('fields'))"
    if ($watch266 -match 'Desc_\d') { $v266Bad += "a dataLink on WoD20.7 watches a description field - every keystroke would repaint the box the storyteller is typing in (SPEC V123, V249d)" }
}

if ($v266Bad) { foreach ($b in $v266Bad) { Fail "V266 $b" } }
else { Pass "V266 retired with the four Numina panes in T874 - not one of the pane readers is back on the tab, nothing watches a description field, and the two owners the rule moved to (savePopDesc for the write, descFor for the read) are standing" }

# ---- V267: the top band of tabHedge closes on ONE base -----------------------------------
# SPEC I63, user 2026-08-23. Leg (b) is not here: it is the named exception inside V240 above,
# where the rule it replaces lives, with its own guard on the count of stretched boxes.
$v267Bad = @()
$numDoc267 = Doc (Join-Path $dir 'WoD20.7.lfm')
$numTxt267 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir 'WoD20.7.lfm')))
# The band lost its pane in T830: tabHedge went with the sub-tabs and the three boxes are direct
# children of the scrollBox now (SPEC I117, V361a). What the leg measures did not move an inch -
# one top, one height, and the band closing on the ruler the columns below it use.
$hedgePane267 = $numDoc267.SelectSingleNode("//scrollBox")

# (a) FOUR boxes since T920 - the fourth is EXPERIENCE, and it is the count that moved and not the rule: one top, one height, and the band closes where the panes close (SPEC I139g).
if ($null -eq $hedgePane267) { Fail "V267 WoD20.7 has no scrollBox - this check reads nothing (SPEC V209)" }
else {
    $bandTop267 = BandTop $hedgePane267
    $band267 = @()
    foreach ($bx in $hedgePane267.SelectNodes("layout[rectangle]")) {
        $bt = -1; $bl = -1; $bw = -1; $bh = -1
        if (-not ([int]::TryParse($bx.GetAttribute("top"), [ref]$bt) -and [int]::TryParse($bx.GetAttribute("left"), [ref]$bl) -and
                  [int]::TryParse($bx.GetAttribute("width"), [ref]$bw) -and [int]::TryParse($bx.GetAttribute("height"), [ref]$bh))) { continue }
        if ($bt -ne $bandTop267) { continue }
        $t267 = $bx.SelectSingleNode("label")
        $band267 += [pscustomobject]@{ N = $(if ($t267) { $t267.GetAttribute("text") } else { 'box' }); L = $bl; R = $bl + $bw; H = $bh }
    }
    if ($band267.Count -ne 4) { $v267Bad += "the band across the top of tabHedge holds $($band267.Count) box(es), expected 4 - HEDGE MAGIC, QUINTESSENCE, TRUE FAITH where WILLPOWER stood since T830, and EXPERIENCE, which the owner asked for beside them on 2026-09-02 and which pays the band ruler like the other three (SPEC I63, I117d, V209)" }
    else {
        $heights267 = @($band267 | ForEach-Object { $_.H } | Sort-Object -Unique)
        if ($heights267.Count -ne 1) { $v267Bad += "the band closes on $($heights267.Count) different bases ($($heights267 -join ', ')) - 'the same height as the box beside it' is the request in letter (SPEC I63, V267a)" }
        # The same ruler V262c just measured, taken from it rather than typed again (103rd
        # round, SPEC V267a, T618): 1270 was a literal in two checks, and T615 moved both. If
        # V262c never got as far as setting one, saying so is the answer - a band measured
        # against nothing would pass while measuring nothing (SPEC V209, B7).
        $right267 = ($band267 | Measure-Object -Property R -Maximum).Maximum
        if (-not $numRuler) { $v267Bad += "V262c set no ruler for the panes - the band has nothing to close on and this leg would pass on an empty comparison (SPEC V209, V262c)" }
        elseif ($right267 -ne $numRuler) { $v267Bad += "the band closes at x=$right267, not on the $numRuler ruler the panes below it use (SPEC V262c, V267a)" }
    }
}

# (c) RETIRED in T830. The willpower block left the Numina tab whole (SPEC I117d, V361c): the ten
# permanent dots, the ten temporary boxes and the painter that drove them. It was a THIRD copy of
# a rating the Main tab owns (SPEC I9), and TRUE FAITH took its rect. Not one FIELD went with it -
# willpower_c1..c10 are still authored in WoD20.3, which is what V362(a) asks and the only reason
# this is a move and not a cut; the ten dots above them carried no field at all.
#
# What stays is the retirement's own guard: the block coming BACK here would be that third copy
# again, with the ruler that used to charge it gone (SPEC V135, B98).
$wpDots267 = @($numDoc267.SelectNodes("//imageCheckBox[starts-with(@name,'wpNum_')]"))
$wpBoxes267 = @($numDoc267.SelectNodes("//checkBox[starts-with(@field,'willpower_c')]"))
if ($wpDots267.Count -gt 0 -or $wpBoxes267.Count -gt 0) { $v267Bad += "WoD20.7 draws $($wpDots267.Count) permanent willpower dot(s) and $($wpBoxes267.Count) temporary box(es) again - the block left the tab in T830 and this leg retired with it, so it comes back UNMEASURED and as a third copy of what the Main tab owns (SPEC I9, I117d, T830)" }
if ($numTxt267 -match 'form\["wpNum_" \.\. d\]') { $v267Bad += "WoD20.7 still paints wpNum_* - the painter went with the block in T830 (SPEC I117d, B55)" }
# The rating did not move house: the mirror V36 declares has to be standing somewhere else, or
# the retirement above quietly ate ten fields (SPEC V362a).
$wp3rd267 = @((Doc (Join-Path $dir 'WoD20.3.lfm')).SelectNodes("//checkBox[starts-with(@field,'willpower_c')]"))
if ($wp3rd267.Count -ne 10) { $v267Bad += "WoD20.3 carries $($wp3rd267.Count) willpower_c box(es), expected 10 - T830 took the Numina copy on the promise that this one is the owner, and without it the field is gone rather than moved (SPEC V362a, V36, I9)" }

# (d) a <template> name is module-global: the duplicate makes rdk exit 1 with NO message and
# take the .rpk with it (SPEC B19). Measured across the whole sheet, not just this pair.
$tplHome267 = @{}
$tplDup267 = @()
foreach ($ff267 in $files) {
    foreach ($t267b in (Doc $ff267.FullName).SelectNodes("//template[@name]")) {
        $tn267 = $t267b.GetAttribute("name")
        if ($tplHome267.ContainsKey($tn267)) { $tplDup267 += "'$tn267' is declared in both $($tplHome267[$tn267]) and $($ff267.Name)" }
        else { $tplHome267[$tn267] = $ff267.Name }
    }
}
if ($tplHome267.Count -lt 26) { Fail "V267 only $($tplHome267.Count) template(s) were read across the sheet - this leg is covering less than the sheet has (SPEC V209). 27 until T892 deleted HedgeRitualFree, the last typed-row template on the sheet" }
foreach ($d267b in $tplDup267) { $v267Bad += "$d267b - a template name is module-global, and the duplicate makes rdk exit 1 with no message at all, deleting the .rpk on its way out (SPEC B19, V267d)" }

if ($v267Bad) { foreach ($b in $v267Bad) { Fail "V267 $b" } }
else { Pass "V267 the three boxes of the band close on one base on the ruler the columns use, the willpower block that was a third copy of the Main tab's rating is gone with its ten fields still authored in WoD20.3, and no template name is declared twice" }

# ---- V268: the sub-tab strips, RETIRED (SPEC I58, I64, I117) ------------------------------
# It measured the second and third bars of the sheet: that no two SUB_TABS groups shared a pane
# name (renderSubTabs keyed one map by NAME, so a duplicate crossed the two strips with nothing in
# the gate or in rdk to say so), that hedgeStrip cleared its two panes, and that the affinity note
# rode in it. T830 took BOTH strips - the Numina tab is three columns now, the way the Ghoul tab
# became three in T832 - so SUB_TABS is empty, renderSubTabs is deleted and selectTab has one job
# left. Every leg here compared two things where only one is left.
#
# What the rule bought did not evaporate; it moved to where the subject moved:
#   * the affinity note is INSIDE the box it explains now, which is what V88 charges.
#   * the panes it cleared are columns, and V262(c) holds every row of boxes to one ruler.
#   * the name collision it refused cannot happen: there is no map keyed by pane name any more.
#
# The guard is the retirement's own, and it is the one leg that still has meaning: a group or a
# strip coming BACK would come back with none of the above measuring it (SPEC V135, B98).
$v268Bad = @()
if ($rootTxt -match '(?m)^\s*SUB_TABS = ') { $v268Bad += "SUB_TABS is declared again on the root form - T830 took the last sub-tab off the sheet and this rule retired with it, so a group here is state for a strip nothing draws (SPEC I117, B55, T830)" }
if (LuaFn $rootTxt 'renderSubTabs') { $v268Bad += "renderSubTabs is back on the root form - it painted pills for a strip that no longer exists, and the map it keyed by pane NAME is exactly the collision this rule used to refuse (SPEC I117, V268 retired, T830)" }
foreach ($f268 in $files) {
    foreach ($s268 in (Doc $f268.FullName).SelectNodes("//layout[@name='numStrip'] | //layout[@name='hedgeStrip'] | //layout[@name='vampStrip']")) {
        $v268Bad += "$($f268.Name) declares '$($s268.GetAttribute('name'))' again - the sub-tab bars went in T832 and T830 and every leg that measured them retired with them (SPEC I117, I118, B98)"
    }
}
# The one bar that is LEFT has to still be there, or "one strip" is a sentence about a sheet with
# none and every count above it is passing on an empty set (SPEC V209, V20).
if ($null -eq (Doc (Join-Path $dir 'WoD20th.lfm')).SelectSingleNode("//layout[@name='tabStrip']")) { $v268Bad += "tabStrip is gone from the root form - the eleven top tabs are the only bar left and V226, V281, V301, V304, V306, V307, V315, V319, V320 and V323 all read it (SPEC V209)" }
if ($v268Bad) { foreach ($b in $v268Bad) { Fail "V268 $b" } }
else { Pass "V268 retired with the last two sub-tab strips in T830 - no SUB_TABS, no renderSubTabs and no bar but the eleven-tab one, so there is no second map keyed by pane name left to collide" }

# ---- V269: a ritual's level is READ, and it costs one per level ---------------------------
# SPEC I65, user 2026-08-23.
$v269Bad = @()

# (a) the twelve PICKED rows wear the book's level in art: no field, no click, dimmed.
$ritPick269 = $numDoc267.SelectSingleNode("//template[@name='HedgeRitualPicker']")
if ($null -eq $ritPick269) { Fail "V269 the ritual row template is missing from WoD20.7 - this check reads nothing (SPEC V209)" }
else {
    $pickDots269 = @($ritPick269.SelectNodes("imageCheckBox"))
    if ($pickDots269.Count -ne 5) { $v269Bad += "a picked ritual row draws $($pickDots269.Count) dot(s), expected 5 - the level it shows runs 1 to 5 (SPEC I65)" }
    foreach ($d269 in $pickDots269) {
        if ($d269.GetAttribute("field")) { $v269Bad += "a dot of HedgeRitualPicker owns field '$($d269.GetAttribute('field'))' - the level lives in the item text, and a field here is a second place to keep it, free to disagree (SPEC V269a)" }
        if ($d269.GetAttribute("onClick")) { $v269Bad += "a dot of HedgeRitualPicker is clickable - the book sets the level of a ritual that came off the list (SPEC V269a)" }
        if ($d269.GetAttribute("opacity") -ne '0.40') { $v269Bad += "a dot of HedgeRitualPicker reads live at opacity '$($d269.GetAttribute('opacity'))' - read-only art is dimmed on this sheet (SPEC V111, V269a)" }
    }

    # (b) REWRITTEN in T892 (user 2026-09-02). It used to read HedgeRitualFree, whose dots were
    # BOUND to $(field)_1.._5 and clickable. That template is gone: every ritual row is a picker
    # and the level of a homebrew one is chosen in the picker pane instead (SPEC V378a).
    #
    # The five fields did NOT go anywhere - that is the whole reason this shape was chosen over
    # binding the picker's dots, which would have left every saved sheet's book rituals drawing
    # blank. They are written by the confirm and read by hedgeRitualLevel, and nothing AUTHORS
    # them any more, so their existence is held HERE rather than by a field sweep (SPEC B122).
    if ($null -ne $numDoc267.SelectSingleNode("//template[@name='HedgeRitualFree']")) {
        $v269Bad += "HedgeRitualFree is back on WoD20.7 - the level of a homebrew ritual is asked for in the picker pane since T892, and an editable dot beside it is a second owner for one number (SPEC V269b, V135)"
    }
    $conf269 = LuaFn $rootTxt 'mfConfirm'
    if (-not $conf269) { $v269Bad += "mfConfirm is gone - nothing carries the pane's level out to the row (SPEC V209)" }
    elseif ($conf269 -notmatch 'setField\(MF\.field \.\. "_" \.\. d, d <= \(MF\.level or 0\)\)') {
        $v269Bad += "the picker's confirm does not write the level to the row's five fields - a homebrew ritual would be saved with a name and no level, and the dots beside it would never light again (SPEC V269b, T892)"
    }
    elseif ($conf269.IndexOf('setField(MF.field .. "_" .. d') -gt $conf269.IndexOf('setField(MF.field, nm)')) {
        $v269Bad += "the confirm writes the level AFTER the name - writing the name is what wakes the dot painter, so the row would draw one repaint behind the level it was just given (SPEC V269b, T892)"
    }
}
if ($rootTxt -notmatch 'for i = TYPED_ROW_FROM\.hedgeRitual, HEDGE_RITUAL_ROWS, 1 do declareTrait\("hedgeRitual_" \.\. i, 1, 5, 0\); end;') {
    $v269Bad += "declareTrait does not start at the first TYPED ritual row - the twelve picked ones carry no dot field at all, and declaring them hands xpClick a range of widgets that do not exist (SPEC V269b)"
}

# (c) ONE line per ritual, and it is the only aggregated category on the sheet.
$rowsFn269 = LuaFn $rootTxt 'xpLedgerRows'
if (-not $rowsFn269) { $v269Bad += "xpLedgerRows not found" }
else {
    if ($rowsFn269 -notmatch 'kind = "Ritual", name = slotName\(field\), level = to, cost = cost') { $v269Bad += "the ledger does not write ONE aggregated line per ritual - the request is that the change stays in the same record, because a ritual has one level (SPEC I9, V269c)" }
    if ($rowsFn269 -match 'pushRise\(rows, "Ritual"') { $v269Bad += "the ledger sends rituals through pushRise - that is one line per POINT, which is the shape every rated trait uses and the one thing a ritual is not (SPEC V269c)" }
    if ($rowsFn269 -notmatch 'if base\[field\] == live then from = hedgeRitualLevel\(field, live, base\); end;') { $v269Bad += "the ledger does not price a SWAPPED ritual from zero - a level one traded for a level three would be charged as a two-point rise (SPEC I65, V269d)" }
    if ($rowsFn269 -notmatch 'string\.find\(ctx\.free, "\|" \.\. field \.\. "#" \.\. lvl \.\. "\|", 1, true\)') { $v269Bad += "the ritual line ignores the free stamp - a level the storyteller gave away would still be charged (SPEC V139, V269c)" }
}
if ($rootTxt -notmatch 'if kind == "Ritual" then return 1; end;') { $v269Bad += "xpCost has no flat price for a ritual - one experience per level is the rule the user gave (SPEC I9)" }

# (d) ONE reader of the level. The painter on WoD20.7 calls the root's, so the art on screen
# and the price in the log cannot tell two stories.
if ($rootTxt -notmatch '(?m)^\s*function hedgeRitualLevel\(field, value, node\)') { $v269Bad += "hedgeRitualLevel is not a global on the root form - the painter on another .lfm could not reach it and would grow a copy of the grammar (SPEC V223, V269d)" }
# T892: the ORDER inside it carries the rule. The book's prefix answers first and the five fields
# answer only when there is no prefix to read - reversed, a ritual taken off the list would be
# priced by whatever those fields happen to still hold from an earlier pick (SPEC V269d).
$lvl269 = LuaFn $rootTxt 'hedgeRitualLevel'
if (-not $lvl269) { $v269Bad += "hedgeRitualLevel has no body to read - the level of every ritual on the sheet comes through it (SPEC V209, V269d)" }
else {
    $pre269 = $lvl269.IndexOf('%d+')
    $dot269 = $lvl269.IndexOf('liveLevel(')
    if ($pre269 -lt 0)      { $v269Bad += "hedgeRitualLevel no longer reads the '<n>. ' prefix - a ritual off the book would lose the level printed on its own name (SPEC V269a, V269d)" }
    elseif ($dot269 -lt 0)  { $v269Bad += "hedgeRitualLevel never falls back to the row's dot fields - a homebrew ritual has no prefix, so its level would read zero for good (SPEC V269b, T892)" }
    elseif ($pre269 -gt $dot269) { $v269Bad += "hedgeRitualLevel asks the dot fields BEFORE the prefix - a ritual off the list would be priced by whatever those five fields still hold (SPEC V269d, T892)" }
    if ($lvl269 -match 'isTypedRow') { $v269Bad += "hedgeRitualLevel still asks isTypedRow - HEDGE_RITUAL_FREE_ROWS is zero since T892, so that branch can never be taken and the level it holds is unreachable (SPEC T892, B7)" }
}
if ($rootTxt -notmatch '(?m)^\s*HEDGE_RITUAL_FREE_ROWS = 0;') { $v269Bad += "HEDGE_RITUAL_FREE_ROWS is not zero - T892 converted the last five typed ritual rows, and a non-zero count here would stop the dot painter short of the rows it now owns (SPEC V378a, V383b)" }
$paintRit269 = LuaFn $numTxt267 'paintHedgeRitualDots'
if (-not $paintRit269) { $v269Bad += "paintHedgeRitualDots is gone - the picked rows own no dot field, so nothing would ever light them (SPEC V269a)" }
elseif ($paintRit269 -notmatch 'hedgeRitualLevel\(field, sheet\[field\]\)') { $v269Bad += "the dot painter does not read the level through hedgeRitualLevel - a second copy of the '<n>. ' grammar drifts from the one the log prices with (SPEC V269d)" }
if ($numTxt267 -match '\^\(%d\+\)%\. ') { $v269Bad += "WoD20.7 parses the level prefix itself - that grammar is written once, on the root form (SPEC V269d)" }

# (e) the key exists and opens empty: without it pickerItems falls back to c.values and the box
# leaves the axis that translates it, which is B57 through another door.
$ritList269 = [regex]::Match($rootTxt, '(?s)\["hedgeRitual"\] = \{(.*?)\},')
if (-not $ritList269.Success) { $v269Bad += "PICKER_LIST has no 'hedgeRitual' key - the twelve picked rows would read c.values and never see the sheet's language (SPEC V269e, B57)" }
else {
    $items269 = @([regex]::Matches($ritList269.Groups[1].Value, '"([^"]*)"') | ForEach-Object { $_.Groups[1].Value })
    if ($items269.Count -lt 1 -or $items269[0] -ne '') { $v269Bad += "PICKER_LIST['hedgeRitual'] does not open with the empty item every picker opens with (SPEC V15, V269e)" }
}

if ($v269Bad) { foreach ($b in $v269Bad) { Fail "V269 $b" } }
else { Pass "V269 the picked ritual rows show the book's level in art, the typed ones buy theirs a point at a time, and the log carries one line per ritual" }


# ---- V270: Affiliation is a PICKER and its list lives in one place ------------------------
# SPEC I66, user 2026-08-23. The failure this guards is not a wrong pixel: a picker whose key
# the map does not hold is not a compile error - it falls through to `or c.values`, which T493
# emptied, and the dropdown opens BLANK with rdk exiting 0 (SPEC V211a, B6).
$v270Bad = @()

$affCbo270 = @()
$affEdt270 = @()
foreach ($f270 in $files) {
    $d270 = Doc $f270.FullName
    # The ENTRY POINT, whatever shape the I113e wave left it wearing (SPEC V270a amended, T874
    # half B): a comboBox binding the field, or the picker button that opens the list for it.
    # The leg always meant ONE, never "one combo" - and after the conversion the button is the
    # entry and the only field= on the name belongs to the hidden twin.
    foreach ($n270 in $d270.SelectNodes("//comboBox[@field='hedgeAffiliation']")) { $affCbo270 += $n270 }
    foreach ($n270 in $d270.SelectNodes("//button[@name='dynhedgeAffiliation']")) { $affCbo270 += $n270 }
    foreach ($n270 in $d270.SelectNodes("//edit[@field='hedgeAffiliation']")) { $affEdt270 += "$($f270.Name) $($n270.GetAttribute('name'))" }
}

# (a) ONE combo owns the field, and the only <edit> allowed on it is the declared mirror twin
# of I71 - which is authored hidden. An edit that is VISIBLE is the field going back to free
# text, which is exactly what the 93rd round replaced.
if ($affCbo270.Count -ne 1) { $v270Bad += "$($affCbo270.Count) entry point(s) reach hedgeAffiliation, expected exactly 1 - two ways into one field is the duplicate V1 refuses, and which SHAPE the one wears is what the I113e wave decides (SPEC V270a amended)" }
elseif ($affCbo270[0].GetAttribute("name") -ne 'dynhedgeAffiliation') { $v270Bad += "the affiliation entry is named '$($affCbo270[0].GetAttribute('name'))', not dynhedgeAffiliation - T874 half B converted it and the name is what renderManualEntry swaps (SPEC V270a amended, V274d)" }
else {
    # (d) the key is the ARGUMENT of mfOpen now, not a sum over the control's name: the button is
    # dyn* and fieldRoot's "drop the cbo" has nothing to drop. Read off the onClick, because a key
    # the map does not hold is not a compile error - the list opens EMPTY with rdk exiting 0
    # (SPEC V270d amended, V211a, B6).
    $oc270 = $affCbo270[0].GetAttribute("onClick")
    if ($oc270 -notmatch "mfOpen\(\s*self\s*,\s*'hedgeAffiliation'\s*,\s*'hedgeAffiliation'\s*,") { $v270Bad += "the affiliation button opens with '$oc270' - it has to hand mfOpen the field AND the list key 'hedgeAffiliation', and a key PICKER_LIST does not hold opens an empty box with rdk exiting 0 (SPEC V270d amended, V211a, B6)" }
    if ($oc270 -notmatch ",\s*'Numina'\s*\)") { $v270Bad += "the affiliation button declares no 'Numina' module - the two sides of one row name the SAME module or the ? and the list read different books (SPEC V373a, B110)" }
}
if ($affEdt270.Count -gt 1) { $v270Bad += "$($affEdt270.Count) <edit> controls bind hedgeAffiliation ($($affEdt270 -join ', ')) - the only one allowed is the declared mirror twin of SPEC I71 (SPEC V270a)" }
elseif ($affEdt270.Count -eq 1 -and $affEdt270[0] -notmatch 'edtHedgeAffiliation') { $v270Bad += "an <edit> binds hedgeAffiliation and is not the declared mirror ($($affEdt270[0])) - the field went back to free text (SPEC V270a)" }

# (b) the list is inside the MARKED region V211b finds, and it is 26 distinct names plus the
# empty item. A duplicate in a picker is the item that can never be chosen.
$region270 = [regex]::Match($rootTxt, '(?s)>>> PICKER_LIST_BEGIN(.*?)<<< PICKER_LIST_END')
if (-not $region270.Success) { $v270Bad += "the PICKER_LIST markers are gone from the root form - nothing here can find the list (SPEC V211b, V209)" }
else {
    $affList270 = [regex]::Match($region270.Groups[1].Value, '(?s)\["hedgeAffiliation"\] = \{(.*?)\},\s*\n')
    if (-not $affList270.Success) { $v270Bad += "PICKER_LIST['hedgeAffiliation'] is not inside the marked region - a key outside it is a list V211 cannot reach (SPEC V270b)" }
    else {
        $affItems270 = @([regex]::Matches($affList270.Groups[1].Value, '"([^"]*)"') | ForEach-Object { $_.Groups[1].Value })
        if ($affItems270.Count -eq 0) { $v270Bad += "PICKER_LIST['hedgeAffiliation'] parsed to zero items - this check is reading nothing (SPEC V209)" }
        else {
            if ($affItems270[0] -ne '') { $v270Bad += "the affiliation list does not open with the empty item every picker on this sheet opens with (SPEC V15, V270b)" }
            $named270 = @($affItems270 | Where-Object { $_ -ne '' })
            if ($named270.Count -ne 26) { $v270Bad += "the affiliation list carries $($named270.Count) names, expected 26 - twenty from M20 Sorcerer, one from HH2 and four from the Companion, plus Lone Practitioner (SPEC I66, R105, R107, R108)" }
            $dupAff270 = @($named270 | Group-Object | Where-Object { $_.Count -gt 1 })
            foreach ($d in $dupAff270) { $v270Bad += "'$($d.Name)' is listed $($d.Count) times - a duplicate in a picker is the item nobody can ever choose (SPEC V270b)" }

            # (c) the book's own term for the unaffiliated, and the negative leg that stops
            # somebody 'improving' it into a word the books never printed (SPEC R105 p. 79).
            if ($named270 -notcontains 'Lone Practitioner') { $v270Bad += "'Lone Practitioner' is not on the list - it is the book's term for the unaffiliated and a choice of its own, distinct from the unanswered empty item (SPEC R105, V270c)" }
            foreach ($bad270 in @('None', 'No Affiliation', 'Unaligned', 'Unaffiliated')) {
                if ($named270 -contains $bad270) { $v270Bad += "'$bad270' is on the list - the book says Lone Practitioner and this one was invented (SPEC R105, V270c)" }
            }

            # (d) used to COMPUTE the key from the control name the way fieldRoot does - drop
            # `cbo`, lowercase the first letter, cut a trailing _<n>. That sum died with the
            # comboBox in T874 half B: the control is `dyn*` now and there is no `cbo` to drop
            # (SPEC V270d amended). The key is read off the mfOpen ARGUMENT instead, up where the
            # entry point is resolved - one owner, and it is the one the running code actually
            # passes rather than a name the code no longer consults (SPEC V135).
        }
    }
}
if ($v270Bad) { foreach ($b in $v270Bad) { Fail "V270 $b" } }
else { Pass "V270 Affiliation is one comboBox on one marked list of 26 names, Lone Practitioner among them, and the control name resolves to the key that holds it" }


# ---- V271: AFFINITY and CASTING_ATTR name nothing outside the canon -----------------------
# SPEC I67, I68, user 2026-08-23. A filter value the picker does not carry does not fail loudly:
# it silently empties the row it filters.
$v271Bad = @()

function TableOf271($txt, $name) {
    $m = [regex]::Match($txt, "(?s)(?m)^\s*$name = \{\r?\n(.*?)^\s*\};")
    if (-not $m.Success) { return $null }
    $out = [ordered]@{}
    foreach ($row in [regex]::Matches($m.Groups[1].Value, '\["([^"]+)"\]\s*=\s*\{([^}]*)\}')) {
        $vals = @([regex]::Matches($row.Groups[2].Value, '"([^"]*)"') | ForEach-Object { $_.Groups[1].Value })
        $out[$row.Groups[1].Value] = $vals
    }
    return $out
}

$affinity271 = TableOf271 $rootTxt 'AFFINITY'
$casting271 = TableOf271 $rootTxt 'CASTING_ATTR'
$numina271 = @()
$hattr271 = @()
$affNames271 = @()
if ($region270.Success) {
    $nl271 = [regex]::Match($region270.Groups[1].Value, '(?s)\["numina"\] = \{(.*?)\},\s*\n')
    if ($nl271.Success) { $numina271 = @([regex]::Matches($nl271.Groups[1].Value, '"([^"]*)"') | ForEach-Object { $_.Groups[1].Value } | Where-Object { $_ -ne '' }) }
    $hl271 = [regex]::Match($region270.Groups[1].Value, '(?s)\["hedgeAttr"\] = \{(.*?)\},\s*\n')
    if ($hl271.Success) { $hattr271 = @([regex]::Matches($hl271.Groups[1].Value, '"([^"]*)"') | ForEach-Object { $_.Groups[1].Value } | Where-Object { $_ -ne '' }) }
    $al271 = [regex]::Match($region270.Groups[1].Value, '(?s)\["hedgeAffiliation"\] = \{(.*?)\},\s*\n')
    if ($al271.Success) { $affNames271 = @([regex]::Matches($al271.Groups[1].Value, '"([^"]*)"') | ForEach-Object { $_.Groups[1].Value } | Where-Object { $_ -ne '' }) }
}
$xpAttrs271 = @()
$xp271 = [regex]::Match($rootTxt, '(?s)(?m)^\s*local XP_ATTRS = \{(.*?)\};')
if ($xp271.Success) { $xpAttrs271 = @([regex]::Matches($xp271.Groups[1].Value, '"([^"]*)"') | ForEach-Object { $_.Groups[1].Value } | Where-Object { $_ -ne '' }) }

if ($null -eq $affinity271 -or $affinity271.Count -eq 0) { $v271Bad += "AFFINITY is not declared on the root form, or parsed to zero keys - the first hedge row would filter by nothing and this check would read nothing (SPEC I67, V209)" }
elseif ($null -eq $casting271 -or $casting271.Count -eq 0) { $v271Bad += "CASTING_ATTR is not declared on the root form, or parsed to zero keys (SPEC I68, V209)" }
elseif ($numina271.Count -eq 0 -or $hattr271.Count -eq 0 -or $affNames271.Count -eq 0 -or $xpAttrs271.Count -eq 0) { $v271Bad += "one of the four lists this check compares against parsed EMPTY (numina $($numina271.Count), hedgeAttr $($hattr271.Count), affiliations $($affNames271.Count), XP_ATTRS $($xpAttrs271.Count)) - every comparison below would pass by default (SPEC V209)" }
else {
    # (a) every key of both tables is an affiliation the picker offers.
    foreach ($k in @($affinity271.Keys) + @($casting271.Keys)) {
        if ($affNames271 -notcontains $k) { $v271Bad += "'$k' is keyed in a filter table and is not an affiliation the picker offers - nobody can ever select it, so the table entry is dead (SPEC V271a, B55)" }
    }
    # (b) every path an affiliation favours is a path the numina picker carries.
    foreach ($k in $affinity271.Keys) {
        foreach ($v in $affinity271[$k]) {
            if ($numina271 -notcontains $v) { $v271Bad += "AFFINITY['$k'] favours '$v', which PICKER_LIST['numina'] does not carry - a filter value outside the list empties the whole row (SPEC V271b, I67)" }
        }
    }
    # (c) every favoured attribute is one of the nine, in the sheet's own spelling, and the
    # union of them stays inside XP_ATTRS - the same bijection V264a measures both ways.
    foreach ($k in $casting271.Keys) {
        foreach ($v in $casting271[$k]) {
            if ($hattr271 -notcontains $v) { $v271Bad += "CASTING_ATTR['$k'] favours '$v', which PICKER_LIST['hedgeAttr'] does not carry - `Inteligence` is this sheet's spelling and correcting it in one place only breaks the bijection (SPEC V271c, I3)" }
            if ($xpAttrs271 -notcontains $v) { $v271Bad += "CASTING_ATTR['$k'] favours '$v', which XP_ATTRS does not carry - an attribute that entered one side only passes every count check on the sheet (SPEC V271c, V264a)" }
        }
    }
    # (d) ABSENCE is the rule 'does not filter'. A key with an empty table is B6 by another door.
    foreach ($t in @(@('AFFINITY', $affinity271), @('CASTING_ATTR', $casting271))) {
        if ($t[1].Contains('Lone Practitioner')) { $v271Bad += "$($t[0]) has a key for 'Lone Practitioner' - the unaligned magician picks from everything, and the way this sheet says that is by having NO key (SPEC V271d, I67)" }
        foreach ($k in $t[1].Keys) {
            if ($t[1][$k].Count -eq 0) { $v271Bad += "$($t[0])['$k'] is an EMPTY table - that is a dropdown with nothing in it, which is B6 arriving through another door (SPEC V271d)" }
        }
    }
    # (e) the two counts are DIRECTED, not an oversight: only the twenty M20 Sorcerer entries
    # print a `Favored Attributes:` line, so five affiliations have paths and no attribute.
    if ($affinity271.Count -ne 25) { $v271Bad += "AFFINITY holds $($affinity271.Count) keys, expected 25 - the twenty-six affiliations less Lone Practitioner (SPEC V271e, I67)" }
    if ($casting271.Count -ne 20) { $v271Bad += "CASTING_ATTR holds $($casting271.Count) keys, expected 20 - only the M20 Sorcerer entries declare a favoured attribute (SPEC V271e, R107, R108)" }
    foreach ($k in $casting271.Keys) {
        if (-not $affinity271.Contains($k)) { $v271Bad += "'$k' favours an attribute and no path - the difference between the two tables runs one way only, and this one goes the wrong way (SPEC V271e)" }
    }
}
if ($v271Bad) { foreach ($b in $v271Bad) { Fail "V271 $b" } }
else { Pass "V271 the two filter tables name only what the three pickers carry - $($affinity271.Count) affiliations with paths, $($casting271.Count) with attributes, and Lone Practitioner in neither" }


# ---- V272: the two affiliation filters reach two controls, and the memo sees them ----------
# SPEC I67, I68, user 2026-08-23. Legs (b) and (c) are the ones that show NO symptom when they
# are wrong: the combo serves a stale list, or sheet[field] comes back nil and the stored value
# is quietly dropped - both with rdk exiting 0 and this gate green (SPEC B48, V200).
$v272Bad = @()

$pickFn272 = [regex]::Match($hh6, '(?s)local function pickerItems\(c, lang, era, levels\)(.*?)\n\t\t\tend;')
$filterName272 = [regex]::Match($hh6, '(?s)local FILTER_NAME = \{(.*?)\};')
$filterRow272 = [regex]::Match($hh6, '(?s)local FILTER_ROW = \{(.*?)\};')

if (-not $filterName272.Success) { $v272Bad += "FILTER_NAME is not declared in WoD20.6 - the two singleton filters have nothing keying them and this check reads nothing (SPEC I67, V209)" }
elseif (-not $pickFn272.Success) { $v272Bad += "pickerItems not found in WoD20.6 - every leg below would pass by default (SPEC V209)" }
else {
    # (a) exactly the two names, and the ROOT table did not grow one of them: FILTER_ROW keyed
    # by `numina` would filter all seventeen hedge rows, and only the FIRST is the Affinity Path.
    $fnNames272 = @([regex]::Matches($filterName272.Groups[1].Value, '([A-Za-z_][A-Za-z0-9_]*)\s*=\s*true') | ForEach-Object { $_.Groups[1].Value } | Sort-Object)
    if ($fnNames272.Count -eq 0) { $v272Bad += "FILTER_NAME parsed to zero names - nothing is filtered and nothing here measures it (SPEC V209)" }
    elseif (($fnNames272 -join ',') -ne 'cboHedgeAttr') { $v272Bad += "FILTER_NAME holds $($fnNames272 -join ', '), expected cboHedgeAttr alone - cbonumina_1 left in T874 with wave 4, and the Affinity Path's filter is asked in mfOpen now, where the pool is built (SPEC V272a, I67, I113c, V205)" }
    if ($filterRow272.Success) {
        foreach ($bad272 in @('numina', 'psychic', 'hedgeAttr')) {
            if ($filterRow272.Groups[1].Value -match "(?m)^\s*$bad272\s*=") { $v272Bad += "FILTER_ROW gained '$bad272' - filtering by ROOT takes all seventeen rows of the box instead of the first (SPEC V272a, V203)" }
        }
    }

    $body272 = NoComments $pickFn272.Groups[1].Value

    # (b) the memo stamp names the affiliation. Without it the stamp holds across a change of
    # affiliation and every combo is served the list built under the old one.
    if ($body272 -notmatch 'local stamp = .*hedgeAffiliation') { $v272Bad += "the pickerItems memo is not stamped with the affiliation - switching it leaves lang and era untouched, the stamp holds, and the old list is served with rdk exiting 0 (SPEC V272b, B48)" }

    # (c) the field a SINGLETON writes. string.sub(nm,4) hands back `HedgeAttr` with the capital
    # still on it, sheet[field] is nil, and V200 lets go of the stored value in silence.
    # T793 re-spelled the question and this literal moved with it. The OLD `_%d+$` asked only
    # about the underscored rows and answered `no` for cboSpeciality1, which HAS a number and
    # writes speciality_1 - the field came out `speciality` and the value was dropped in silence
    # (SPEC V346d). `_?(%d+)$` is the same question asked once for both spellings, and accepting
    # the old literal here would be letting that hole back in.
    if ($body272 -notmatch 'string\.match\(nm, "_\?\(%d\+\)\$"\)') { $v272Bad += "pickerItems never asks whether the control name ends in a row number - that question is what tells a ROW from a SINGLETON, and without it cboHedgeAttr resolves to 'HedgeAttr' with the capital still on, sheet[field] comes back nil, and the row's own value is dropped with no symptom anywhere (SPEC V272c, V346d, I68, V200)" }
    elseif ($body272 -notmatch 'fieldRoot\(nm\)\s*;') { $v272Bad += "the singleton branch of the field line does not end at fieldRoot - nothing lowercases the first letter and sheet[field] is nil (SPEC V272c, I68)" }
    if ($body272 -match 'local field = filter and string\.sub\(nm, 4\) or nil;') { $v272Bad += "the pre-94th field line is back - it serves ROWS and lies to every SINGLETON picker (SPEC V272c)" }
}

# (d) what the filter READS is what the link WATCHES, and the renderer asks for the same two
# names leg (a) holds (SPEC V123).
$dl272 = [regex]::Match($numTxt267, '<dataLink[^>]*field="hedgeAffiliation"[^>]*>')
if (-not $dl272.Success) { $v272Bad += "no dataLink on WoD20.7 watches hedgeAffiliation - the two lists would only rebuild when something else happened to repaint them (SPEC V272d, V123)" }
elseif ($dl272.Value -notmatch 'renderHedgePickers\(') { $v272Bad += "the hedgeAffiliation dataLink does not call renderHedgePickers (SPEC V272d)" }
$hedgeFn272 = LuaFn $hh6 'renderHedgePickers'
if (-not $hedgeFn272) { $v272Bad += "renderHedgePickers is not declared in WoD20.6 - the dataLink calls a global that finds nil at runtime, and rdk exits 0 on that (SPEC V272d, V223, B51)" }
else {
    $askFor272 = @([regex]::Matches((NoComments $hedgeFn272), '([A-Za-z_][A-Za-z0-9_]*)\s*=\s*true') | ForEach-Object { $_.Groups[1].Value } | Sort-Object)
    if (($askFor272 -join ',') -ne 'cboHedgeAttr') { $v272Bad += "renderHedgePickers asks xpFind for $($askFor272 -join ', '), which is not the set FILTER_NAME filters - the two are one list and they shrink together, or a control is filtered and never re-rendered and keeps the list it was born with (SPEC V272d, T874)" }
    if ((NoComments $hedgeFn272) -notmatch 'discLevels\(\)') { $v272Bad += "renderHedgePickers hands pickerItems no level map - `filter` is `isFilterRow(nm) and levels ~= nil`, so a nil there turns BOTH filters off and the round does nothing (SPEC V272d, V205)" }
}

# (e) the two branches, and both of them answer YES when the table has no key: a branch that
# said no would open two empty dropdowns for Lone Practitioner.
$allowFn272 = LuaFn $rootTxt 'pickAllowed'
if (-not $allowFn272) { $v272Bad += "pickAllowed not found on the root form - the filter has no door to the rule (SPEC V209, V199)" }
else {
    $ab272 = NoComments $allowFn272
    foreach ($pair272 in @(@('numina_1', 'AFFINITY'), @('hedgeAttr', 'CASTING_ATTR'))) {
        if ($ab272 -notmatch "field == `"$($pair272[0])`"") { $v272Bad += "pickAllowed has no branch for '$($pair272[0])' - the table is data nobody reads (SPEC V272e, B55)" }
        elseif ($ab272 -notmatch $pair272[1]) { $v272Bad += "the '$($pair272[0])' branch of pickAllowed does not read $($pair272[1]) (SPEC V272e)" }
    }
}
$allows272 = LuaFn $rootTxt 'hedgeAllows'
if (-not $allows272) {
    $allows272m = [regex]::Match($rootTxt, '(?s)local function hedgeAllows\(t, value\)(.*?)\n\t\t\tend;')
    if ($allows272m.Success) { $allows272 = $allows272m.Value }
}
if (-not $allows272) { $v272Bad += "the shared body the two branches call is gone - each branch would grow its own copy of 'no key means no filter' (SPEC V272e)" }
elseif ((NoComments $allows272) -notmatch 'if ok == nil then return true; end;') { $v272Bad += "an affiliation with no entry does not open the whole list - Lone Practitioner and the unanswered empty item would face two empty dropdowns (SPEC V272e, V271d)" }

if ($v272Bad) { foreach ($b in $v272Bad) { Fail "V272 $b" } }
else { Pass "V272 the affiliation filter reaches cboHedgeAttr - the one combo left after wave 4 - the memo is stamped with it, pickAllowed still answers for numina_1 through AFFINITY where mfOpen asks it, the singleton field is computed through fieldRoot, and no key means no filter" }


# ---- V273: every numina description carries its source, in both languages ------------------
# SPEC I70, I21, user 2026-08-23. This is V192 in the other module: the numina text lives in
# descNumina.lua and the collector is a different one, so the rule is a SISTER and not a wider
# regex (SPEC V203, I24). V177 measures EXISTENCE and V32 the picker-to-DESC cover; this one
# measures FORMAT.
$v273Bad = @()
# The three titles are ASCII in this file on purpose: PowerShell 5.1 reads a .ps1 with no BOM
# as ANSI, so an accented or curly-quote literal typed here would arrive mojibaked and the check
# redden on correct text. Built from code points, it compares the same bytes descNumina.lua
# holds (SPEC R109).
$PAG273  = "p" + [char]0xE1 + "g."
$COMP273 = "Sorcerer" + [char]0x2019 + "s Companion"
$BOOK273 = @{
    "M20 Sorcerer"          = @(18, 95)
    "The Hunters Hunted II" = @(69, 142)
}
$BOOK273[$COMP273] = @(14, 31)
$numEn273 = DescEntries (Join-Path $plugin "descNumina_en.lua") 'DESC' 'en'
$numPt273 = DescEntries (Join-Path $plugin "descNumina_pt.lua") 'DESC' 'pt'
if ($null -eq $numEn273) { $v273Bad += "descNumina_en.lua is missing from the plugin root, or its DESC markers are gone - there is no [en] text to measure (SPEC I24, V209, V210)" }
else {
    if ($null -eq $numPt273) { $v273Bad += "descNumina_pt.lua is missing from the plugin root, or its DESC markers are gone - there is no [pt] text to measure (SPEC I24, V209, V210)" }
    else {
        # T495 put the two languages in two files, so the pair leg (d) compares is a join by
        # key. A key with no [pt] side is V210's fail, and it is named here too: without it
        # leg (d) would pass on the half it never saw (SPEC V210a).
        $ptMap273 = DescMap $numPt273
        $seen273 = 0
        foreach ($e273 in $numEn273) {
            $seen273++
            $key273 = $e273.Key
            $cite273 = @{}
            $sides273 = @(, @('en', $e273.Body, 'p.'))
            if ($ptMap273.ContainsKey($key273)) { $sides273 += , @("pt", $ptMap273[$key273], $PAG273) }
            else { $v273Bad += "'$key273' is in descNumina_en.lua and nowhere in descNumina_pt.lua - the [pt] side has no source to check (SPEC V210a)" }
            foreach ($side273 in $sides273) {
                $lang273 = $side273[0]
                $blocks273 = $side273[1] -split "`n`n`n"
                if ($blocks273.Count -lt 3) { $v273Bad += "'$key273' [$lang273] is $($blocks273.Count) block(s), expected the three of SPEC I21 - source, name, then the book text (SPEC V273)"; continue }
                # block 2 is the KEY itself. `Shadow` and `Shadows` are two different numina one
                # letter apart, so swapping them passes V177 and has to die here (SPEC V273).
                if ($blocks273[1] -ne $key273) { $v273Bad += "'$key273' [$lang273] names itself '$($blocks273[1])' in the second block - the two are different numina when they differ by one letter (SPEC V273, V177)"; continue }
                # block 1 is `<title>, p. <n>` and its pt label; the label is the ONLY thing
                # that translates, the title is a proper name (SPEC I21).
                $lbl273 = [regex]::Escape($side273[2])
                $head273 = [regex]::Match($blocks273[0], "^(.+), $lbl273 (\d+)$")
                if (-not $head273.Success) { $v273Bad += "'$key273' [$lang273] opens with '$($blocks273[0])', not '<book>, $($side273[2]) <page>' (SPEC V273, I21)"; continue }
                $bk273 = $head273.Groups[1].Value
                $pg273 = [int]$head273.Groups[2].Value
                # (b) three titles and no fourth: a book joins this sheet through SPEC R, not through
                # a typing slip in one entry.
                if (-not $BOOK273.ContainsKey($bk273)) { $v273Bad += "'$key273' [$lang273] cites '$bk273', which is not one of the three books SPEC R109 names (SPEC V273b)"; continue }
                # (c) the page is a number inside the range of the book it names.
                $range273 = $BOOK273[$bk273]
                if ($pg273 -lt $range273[0] -or $pg273 -gt $range273[1]) { $v273Bad += "'$key273' [$lang273] cites $bk273 p. $pg273, outside the $($range273[0])-$($range273[1]) the numina, hedge rituals and affiliations of that book occupy (SPEC V273c, R107, R108, R109)"; continue }
                $cite273[$lang273] = "$bk273|$pg273"
            }
            # (d) one entry is one source: a translation that changed book or page is drift that
            # nobody would ever see on screen, because only one language is open at a time.
            if ($cite273.ContainsKey('en') -and $cite273.ContainsKey('pt') -and $cite273['en'] -ne $cite273['pt']) {
                $v273Bad += "'$key273' cites $($cite273['en'] -replace '\|', ' p. ') in English and $($cite273['pt'] -replace '\|', ' p. ') in Portuguese - one entry is one source (SPEC V273d, I69)"
            }
        }
        if ($seen273 -eq 0) { $v273Bad += "the DESC region parsed to zero entries - the parser or the generated shape drifted and every leg above passed by default (SPEC V209)" }
        elseif ($seen273 -ne 128) { $v273Bad += "descNumina_en.lua holds $seen273 entries, expected 128 - the 49 the two numina pickers offer, plus True Faith, which has no picker and is an entry all the same, plus the 52 hedge rituals of T928 and the 26 affiliations of T929 (SPEC I70, I61, V265a, I140l, I140m)" }
        elseif ($numPt273.Count -ne 128) { $v273Bad += "descNumina_pt.lua holds $($numPt273.Count) entries, expected 128 - the halves are read one at a time now, and a short [pt] side is text no reader of that language can reach (SPEC V210a, I70)" }
    }
}
if ($v273Bad) { foreach ($b in $v273Bad) { Fail "V273 $b" } }
else { Pass "V273 all $seen273 numina descriptions open with book and page in both languages, name themselves in the second block, and cite one source per entry" }


# ---- V274: the two manual-entry flags are fail-closed, and the mirror is declared ----------
# SPEC I71, user 2026-08-23. What the flag swaps is which of two widgets on ONE field the reader
# can reach - so the NDB stays the single source of truth and nothing has to be migrated.
$v274Bad = @()
$stDoc274 = Doc (Join-Path $dir "WoD20.10.lfm")
$stBox274 = $stDoc274.SelectSingleNode("//layout[label/@text='STORYTELLER SETTINGS']")
if ($null -eq $stBox274) { $v274Bad += "the STORYTELLER SETTINGS box is gone from WoD20.10 - this check reads nothing (SPEC V209)" }
else {
    # (a) the two flags exist, and NEITHER declares a default: nil is already false, and a
    # declared default would be a second answer to the same question (SPEC V80, V89, V175).
    foreach ($chk274 in @(@('chkManualAffiliation', 'stManualAffiliation'), @('chkManualClanFamily', 'stManualClanFamily'))) {
        $node274 = $stBox274.SelectSingleNode("checkBox[@name='$($chk274[0])']")
        if ($null -eq $node274) { $v274Bad += "$($chk274[0]) is not in the STORYTELLER SETTINGS box - the flag the storyteller flips is gone (SPEC V274a)" }
        elseif ($node274.GetAttribute("field") -ne $chk274[1]) { $v274Bad += "$($chk274[0]) binds '$($node274.GetAttribute('field'))', not $($chk274[1]) (SPEC V274a, I3)" }
        if ($stDoc274.SelectSingleNode("//dataLink[@field='$($chk274[1])'][@defaultValue]")) { $v274Bad += "$($chk274[1]) declares a defaultValue - the flag must be fail-closed on nil, and a default is a second place for that answer to live (SPEC V274a, V80, V89)" }
    }

    # (e) the box grew 60 and stayed symmetric: the head above the title, the same under the last
    # row, and Save is still the last input in it (SPEC V240, V238b).
    #
    # DERIVED off the margin since T913 rather than carrying 419 as a literal: this was the THIRD
    # place the sheet wrote the same number down, and the margin change is what made the three
    # disagree out loud (SPEC B70, B130, I137c). Content closes at 419 - 20 = 399 whatever the
    # margin is; the box is that plus one head and one foot.
    $h274 = [int]$stBox274.GetAttribute("height")
    $want274 = 399 - 20 + (2 * $BOX_PAD_Y)
    if ($h274 -ne $want274) { $v274Bad += "the box is $h274 tall, expected $want274 - two flags of 25 at a pitch of 30 moved everything under them by 60, and the box carries one head and one foot on top of that (SPEC V274e, I71, I73, I137c)" }
    $kids274 = @($stBox274.ChildNodes | Where-Object { $_.NodeType -eq "Element" -and $_.LocalName -ne "rectangle" -and $_.HasAttribute("top") })
    if ($kids274.Count -eq 0) { $v274Bad += "the box holds no placed control - the two gaps below would be measured against nothing (SPEC V209)" }
    else {
        $minTop274 = ($kids274 | ForEach-Object { [int]$_.GetAttribute("top") } | Measure-Object -Minimum).Minimum
        $maxBot274 = ($kids274 | ForEach-Object { [int]$_.GetAttribute("top") + [int]$_.GetAttribute("height") } | Measure-Object -Maximum).Maximum
        if ($minTop274 -ne $BOX_PAD_Y) { $v274Bad += "the box breathes $minTop274 above its title, expected $BOX_PAD_Y (SPEC V240, V274e, I137c)" }
        if (($h274 - $maxBot274) -ne $BOX_PAD_Y) { $v274Bad += "the box breathes $($h274 - $maxBot274) under its last row, expected $BOX_PAD_Y (SPEC V240, V274e, I137c)" }
        $lowest274 = @($kids274 | Where-Object { $_.GetAttribute("name") } | Sort-Object { [int]$_.GetAttribute("top") })[-1]
        if ($lowest274.GetAttribute("name") -ne 'btnSaveBaseline') { $v274Bad += "the last named control in the box is '$($lowest274.GetAttribute('name'))' - Save Initial Character is the one irreversible action here and comes after everything (SPEC V238b, V274e)" }
    }
}

# (b) the two pairs are DECLARED mirrors, so V36 knows them and V1 does not call them duplicates.
# Only clanFamily is a mirror now. hedgeAffiliation stopped being one in T874 half B and NOT by
# losing anything: a picker button carries no field=, so the pair is a button and an edit and one
# single widget owns the name. Asking for it here would demand the stale declaration V36 refuses
# (SPEC V274b amended, B121). The leg returns the day T876 converts clanFamily.
foreach ($fld274 in @('hedgeAffiliation', 'clanFamily')) {
    if ($mirrors.Contains($fld274)) { $v274Bad += "'$fld274' is in the mirror declaration - it is a picker since wave 5 of I113e and a picker button binds no field, so the name has ONE owner and the declaration would be the stale one V36 refuses (SPEC V274b, V36, B121)" }
}

# (c) the AUTHORED state is the flag OFF: the picker visible by default, the typed twin hidden
# AND disabled. A sheet that never opened the storyteller's tab paints right with no Lua.
# The visible half is a picker BUTTON on both sides since T874 half B and T876, so the roster
# carries the element name too - one reader, two shapes, because the shape is what the I113e
# wave changes and the RULE is not about the shape (SPEC V274c amended).
# The clanFamily side is a PAIR again since T972 (SPEC V274 amended, I149b, V433): the twin sits
# on dynclanFamily's rectangle, the second row of the DOMINATOR box.
foreach ($pair274 in @(@('WoD20.7.lfm', 'dynhedgeAffiliation', 'edtHedgeAffiliation', 'button'), @('WoD20.11.lfm', 'dynclanFamily', 'edtClanFamily', 'button'))) {
    $doc274 = Doc (Join-Path $dir $pair274[0])
    $cbo274 = $doc274.SelectSingleNode("//$($pair274[3])[@name='$($pair274[1])']")
    $edt274 = $doc274.SelectSingleNode("//edit[@name='$($pair274[2])']")
    if ($null -eq $cbo274) { $v274Bad += "$($pair274[1]) is gone from $($pair274[0]) - half the mirror is missing (SPEC V274c)"; continue }
    if ($null -eq $edt274) { $v274Bad += "$($pair274[2]) is not in $($pair274[0]) - the flag would have nothing to swap the picker for (SPEC V274c)"; continue }
    if ($cbo274.GetAttribute("visible") -ne '') { $v274Bad += "$($pair274[1]) authors visible='$($cbo274.GetAttribute('visible'))' - the picker is the DEFAULT side and default is not written down (SPEC V274c, V56)" }
    if ($edt274.GetAttribute("visible") -ne 'false' -or $edt274.GetAttribute("enabled") -ne 'false') { $v274Bad += "$($pair274[2]) is not authored visible='false' enabled='false' - hidden and still enabled takes Tab focus, which shows up in no screenshot (SPEC V274c, V274d)" }
    foreach ($att274 in @('left', 'top', 'width', 'height')) {
        if ($cbo274.GetAttribute($att274) -ne $edt274.GetAttribute($att274)) { $v274Bad += "$($pair274[2]) sits at $att274=$($edt274.GetAttribute($att274)) and $($pair274[1]) at $($cbo274.GetAttribute($att274)) - one field, one rectangle, or the swap moves the row (SPEC V274c, I71)" }
    }
}
# (d) BOTH properties for ALL FOUR controls, and the four are named in one place.
$manFn274 = LuaFn $hh6 'renderManualEntry'
if (-not $manFn274) { $v274Bad += "renderManualEntry is not declared in WoD20.6 - the two dataLinks call a global that finds nil, and rdk exits 0 on that (SPEC V274d, V223, B51)" }
else {
    $mb274 = NoComments $manFn274
    foreach ($nm274 in @('dynhedgeAffiliation', 'edtHedgeAffiliation', 'dynclanFamily', 'edtClanFamily')) {
        # WORD BOUNDARY and case-SENSITIVE, both, and a mutation is what asked for them: renaming
        # the entry to `dynhedgeAffiliationX` left this leg GREEN, because a bare -notmatch is a
        # SUBSTRING test and the longer name contains the shorter one. That is the anchor V222
        # names in letter - "padrao de check que casa identificador ! ter fronteira de palavra" -
        # and it had been sitting here since the pair was authored (SPEC V222, V205, B121).
        if ($mb274 -cnotmatch "\b$nm274\b") { $v274Bad += "renderManualEntry never names $nm274 - a control the renderer does not reach keeps whatever state the last event left it in (SPEC V274d)" }
    }
    if ($mb274 -notmatch 'c\.visible = ' -or $mb274 -notmatch 'c\.enabled = ') { $v274Bad += "renderManualEntry does not write BOTH visible and enabled - writing only visible leaves an invisible control taking Tab focus (SPEC V274d)" }
    if ($mb274 -notmatch 'stManualAffiliation' -or $mb274 -notmatch 'stManualClanFamily') { $v274Bad += "renderManualEntry does not read both flags - either entry point has to leave all four controls agreeing with them (SPEC V274d)" }
    if ($mb274 -notmatch 'rootOf\(from\)') { $v274Bad += "renderManualEntry does not walk from the ROOT - the two pairs live two tabs apart and a tab-scoped find reaches only one of them (SPEC V143, V274d)" }
}
# the flags are watched, or the swap only happens when something else repaints (SPEC V123).
foreach ($trig274 in @(@('WoD20.7.lfm', 'stManualAffiliation'), @('WoD20.11.lfm', 'stManualClanFamily'))) {
    $t274 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir $trig274[0])))
    $lnk274 = [regex]::Match($t274, "<dataLink[^>]*field=`"$($trig274[1])`"[^>]*>")
    if (-not $lnk274.Success) { $v274Bad += "nothing in $($trig274[0]) watches $($trig274[1]) - flipping the flag would change nothing until the tab was reopened (SPEC V274d, V123)" }
    elseif ($lnk274.Value -notmatch 'renderManualEntry\(') { $v274Bad += "the $($trig274[1]) dataLink does not call renderManualEntry (SPEC V274d)" }
    if ($t274 -notmatch 'renderManualEntry\(self\);\s*\n') { $v274Bad += "$($trig274[0]) never calls renderManualEntry from its own event - the storyteller may have flipped the flag with the tab closed (SPEC V274d)" }
}

if ($v274Bad) { foreach ($b in $v274Bad) { Fail "V274 $b" } }
else { Pass "V274 both manual-entry flags are fail-closed and declared mirrors, the typed twins are authored hidden on the picker's own rectangle, and one renderer writes visible and enabled for all four" }

# ---- V275: the picker memo is keyed by what CHANGES the list, not by the field root -------
# SPEC V275, the backprop of B58. pickerItems memoises the list it just built. Key that memo by
# fieldRoot and every row of one root shares one entry - so when ONE row of a root is filtered
# and its siblings are not (cbonumina_1 is the Affinity Path and answers to the affiliation,
# cbonumina_2..17 answer to nothing - SPEC I67), whichever row paints FIRST decides the list for
# all of them: row 1 first and the other sixteen inherit its pruned list; rows 2..17 first and
# row 1 is served the whole one with its filter silently doing nothing. Neither order errors,
# both come out of rdk -l at exit 0, and nothing on screen says which one happened.
#
# V272(b) measures the STAMP - what INVALIDATES the memo. This measures the KEY - what SEPARATES
# it. Two halves of one memo, failing for different reasons, and they only work as a pair, the
# same way V198 and V206 do.
#
# Anchored on the CONSTRUCTION and never on the identifier (SPEC V222): the key is whatever
# INDEXES memo.lists, found by reading that index and then the local it is assigned from. So
# renaming the local is invisible here - that is the neighbour probe - while keying on the root
# alone goes red. Comments come off first: this file explains the OLD shape in prose two lines
# above the fix, and a check that reddens on prose is the false positive V222 exists for.
$v275Bad = @()
$code275 = CodeOf (Join-Path $dir 'WoD20.6.lfm')
$fn275 = LuaFn $code275 'pickerItems'
if (-not $fn275) { $v275Bad += "pickerItems is gone from WoD20.6 - the memo this reads has no builder left (SPEC V209)" }
else {
    # The names that filter ONE row out of a root, read from the table and never listed here: a
    # name added to FILTER_NAME has to reach the key on the same round, and a check carrying its
    # own copy of the roster would not notice (SPEC V20).
    $tbl275 = [regex]::Match($code275, '(?s)local FILTER_NAME = \{(.*?)\};')
    $names275 = @()
    if ($tbl275.Success) { $names275 = @([regex]::Matches($tbl275.Groups[1].Value, '(\w+)\s*=\s*true') | ForEach-Object { $_.Groups[1].Value }) }
    if ($names275.Count -eq 0) { $v275Bad += "FILTER_NAME declares no name - every leg below is true by default, and a filtered row keying by root would pass unseen (SPEC V209, B58)" }

    # Whatever indexes memo.lists IS the key, under any name.
    $idx275 = [regex]::Match($fn275, 'memo\.lists\[\s*(\w+)\s*\]')
    if (-not $idx275.Success) { $v275Bad += "nothing indexes memo.lists inside pickerItems - this check found no key to measure (SPEC V209)" }
    else {
        $kn275 = $idx275.Groups[1].Value
        $asg275 = [regex]::Match($fn275, "(?m)^\s*local\s+$kn275\s*=\s*(.+?);\s*$")
        if (-not $asg275.Success) { $v275Bad += "memo.lists is indexed by '$kn275' and nothing local assigns it - the key is built somewhere this cannot read (SPEC V209)" }
        else {
            $rhs275 = $asg275.Groups[1].Value.Trim()
            if ($rhs275 -notmatch 'FILTER_NAME\[') {
                $v275Bad += "the memo key is built as '$rhs275' and never asks FILTER_NAME - the filtered row and its unfiltered siblings share one entry, and whichever paints first decides the list for the rest (SPEC V275, B58)"
            }
            elseif ($rhs275 -notmatch 'FILTER_NAME\[\s*nm\s*\]\s*==\s*true\s+and\s+nm\b') {
                $v275Bad += "the memo key asks FILTER_NAME and does not answer with the NAME - '$rhs275'. Asking is not enough: the filtered row has to key by nm, or it lands back on the root it shares with its siblings (SPEC V275)"
            }
        }
    }
}
if ($v275Bad) { foreach ($b275 in $v275Bad) { Fail "V275 $b275" } }

else { Pass "V275 the picker memo keys its $($names275.Count) filtered name(s) by name and everything else by root - the filtered row cannot inherit a sibling's list" }

# ---- V276..V279: the Victorian filigree ---------------------------------------------
# SPEC I72, the 98th round. The ornament is built by Lua into one <path> per section box, so
# NOTHING above sees it: without this block the palette key, the generator and the call could
# all be deleted and every other check would stay green - which is exactly the state the
# feature shipped in, and the reason item (1) of the request was to spec it at all.
$ornBad = @()
$ornMake = LuaFn $hh6 'ornament'
$ornGeo = LuaFn $hh6 'ornPath'
$ornArmFn = LuaFn $hh6 'ornArm'
$ornFiligFn = LuaFn $hh6 'ornFiligree'
$ornFileteFn = LuaFn $hh6 'ornFilete'

if (-not $ornMake) { $ornBad += "ornament() is gone from WoD20.6 - the filigree has no painter (SPEC I72c)" }
elseif (-not $ornGeo) { $ornBad += "ornPath() is gone from WoD20.6 - there is no geometry to paint (SPEC I72)" }
elseif (-not $ornArmFn) { $ornBad += "ornArm() is gone - the corner cannot shrink to fit and a short box goes back to being skipped (SPEC V279, B59)" }
elseif (-not $ornFiligFn) { $ornBad += "ornFiligree() is gone from WoD20.6 - the DEFAULT drawing is missing, so three eras of four would have no frame to fall back to (SPEC I87b, V313b)" }
else {
    $ornBody = NoComments $ornMake

    # V276: the path is made at RUNTIME and placed by align, never by geometry. A left/top/
    # width/height written here would put a theme back in the business V57 took it out of.
    #
    # What the path is parented TO is no longer asked here (103rd round, SPEC T621, T623). The
    # leg that used to sit on this line demanded `setParent(c)` and cited I72c, and I72c is
    # REVOKED: the user asked for the filigree on TOP of the labels and fields, so the argument
    # is now the <layout> the rectangle lives in. Two checks cannot both be the truth about one
    # argument, and V276's own sentence never mentioned parenting - it is about a path made at
    # runtime and placed by align. V283a owns the question now, in the opposite direction.
    if ($ornBody -notmatch 'gui\.newPath\(\)') { $ornBad += "V276 nothing creates a <path> - the filigree is declared and never drawn (SPEC I72)" }
    elseif ($ornBody -notmatch '(?m)^\s*\w+\.align\s*=\s*"client";') { $ornBad += "V276 the path is not align=client - it would need a left and a size, which is the geometry V57 forbids" }
    elseif ($ornBody -notmatch '(?m)^\s*\w+\.hitTest\s*=\s*false;') { $ornBad += "V276 the path is hit-testable - it would eat every click meant for the box under it" }
    elseif ([regex]::Matches($ornBody, '"(left|top|width|height)"|\.(left|top|width|height)\s*=').Count -gt 0) {
        $ornBad += "V276 the painter WRITES geometry - V37/V40/V49 only ever measure the static XML, so a size written here is a collision nothing can see (SPEC V57)"
    }

    # V277: a plainer era hides the filigree and never destroys it - the V56 promise, for the
    # frame instead of the backdrop. Destroying would also strand the handle in the ledger.
    if ($ornBody -notmatch 'visible\s*=\s*false;') { $ornBad += "V277 the filigree is never hidden - Modern would ship with a gold frame on every box (SPEC V56)" }
    if ($ornBody -match 'destroy') { $ornBad += "V277 the filigree is DESTROYED rather than hidden - the ledger would keep a dead handle and switching back would redraw 73 paths (SPEC V277)" }

    # V278: who gets one is decided by CONSTRUCTION - authored black plus a radius of its own.
    # Anchored on what the code READS, not on a roster of names spelled here (SPEC V222).
    #
    # RE-POINTED in the 117th round and NOT renumbered (SPEC V316d): the test moved out of
    # ornament() into sectionBox, so the frame and the CORNER of I89 ask it in ONE place. Left
    # grepping ornament() this check would find nothing, report nothing and pass by vacancy -
    # the no-op V20 refuses, and B7 in the letter.
    $sbFn278 = LuaFn $hh6 'sectionBox'
    $sbBody278 = NoComments $sbFn278
    if (-not $sbFn278) { $ornBad += "V278 sectionBox is gone - the one place that says which rectangles ARE section boxes does not exist, so neither caller is filtered (SPEC V209, V316d)" }
    elseif ($ornBody -notmatch 'sectionBox\(\s*c\s*,\s*fill\s*\)') { $ornBad += "V278 ornament() no longer asks sectionBox - a second copy of the filter is exactly what V67 pays not to have (SPEC V316d)" }
    elseif ($sbBody278 -notmatch 'normColor\(\s*fill\s*\)') { $ornBad += "V278 the filter does not read the AUTHORED fill - by the time this runs the box is mahogany, so reading the current colour matches nothing (SPEC V62)" }
    else {
        # The radius has to REFUSE, not merely be read. Anchored on the construction (SPEC
        # V222): find the local assigned from c.xradius, then demand a return guarded by it.
        # Asking only that the word appears passes a version that reads the radius and then
        # frames the box anyway - which is how the tab-strip floor would get a gold frame.
        $radL = [regex]::Match($sbBody278, '(?m)^\s*local\s+(\w+)\s*=\s*c\.xradius;')
        if (-not $radL.Success) { $ornBad += "V278 nothing reads the box own corner radius - the tab-strip floor is black and square and would be framed like a section box (SPEC V229, V68)" }
        else {
            $rn278 = [regex]::Escape($radL.Groups[1].Value)
            if ($sbBody278 -notmatch "(?m)^\s*if\s+[^\r\n]*\b$rn278\b[^\r\n]*then return;") {
                $ornBad += "V278 the corner radius is read and never REFUSES - a black square would be framed exactly like a section box (SPEC V229, V68)"
            }
        }
    }

    # V279: the corner SHRINKS to fit; no box is silently skipped. The floor is read out of the
    # Lua and measured against the smallest box the XML actually authors, so the two can never
    # drift: a shorter box added tomorrow reddens this instead of quietly coming out plain.
    # This is the backprop of B59 - TRUE FAITH, 1270 wide and 76 tall, fell through the old
    # fixed guard and nothing said so.
    # $ornBody and not $ornMake: the comment above explains the OLD guard in prose, and a
    # check that reddens on prose is the false positive V222 exists for.
    if ($ornBody -match '2\s*\*\s*ORN_ARM') {
        $ornBad += "V279 the painter still refuses a box by size - that is the guard that skipped TRUE FAITH in silence (SPEC B59)"
    }
    $armCap = [regex]::Match($hh6, '(?m)^\s*local ORN_ARM\s*=\s*(\d+);')
    $armSlack = [regex]::Match($ornArmFn, 'math\.floor\(math\.min\(w, h\) / 2\) - (\d+)')
    # 115th round: the floor is read PER STYLE (SPEC I87b, T706). ornPath is a dispatcher now
    # and carries no geometry of its own, so the filigree floor comes out of ornFiligree -
    # pointing this at the dispatcher would match nothing and turn the leg into the no-op V20
    # exists to refuse. The filete's own floor is measured below, and it floors the CROSS.
    $armFloor = [regex]::Match($ornFiligFn, '(?m)^\s*if a < (\d+) then return "";')
    if (-not $armCap.Success -or -not $armSlack.Success -or -not $armFloor.Success) {
        $ornBad += "V279 ORN_ARM, the shrink or the floor could not be read - the guard this measures is unreadable, so the check is a no-op (SPEC V20)"
    } else {
        $slack = [int]$armSlack.Groups[1].Value
        $floor = [int]$armFloor.Groups[1].Value
        $tiny = 0
        $tinyName = ''
        foreach ($f in $files) {
            foreach ($r in (Doc $f.FullName).SelectNodes("//rectangle[@color='black'][@xradius]")) {
                $p = $r.ParentNode
                $bw = 0; $bh = 0
                if (-not [int]::TryParse($r.GetAttribute("width"), [ref]$bw)) { [void][int]::TryParse($p.GetAttribute("width"), [ref]$bw) }
                if (-not [int]::TryParse($r.GetAttribute("height"), [ref]$bh)) { [void][int]::TryParse($p.GetAttribute("height"), [ref]$bh) }
                if ($bw -le 0 -or $bh -le 0) { continue }
                $side = [Math]::Min($bw, $bh)
                if ($tiny -eq 0 -or $side -lt $tiny) { $tiny = $side; $tinyName = "$($f.Name) $($bw)x$($bh)" }
            }
        }
        if ($tiny -eq 0) { $ornBad += "V279 no section box was found - this check measured nothing (SPEC V209)" }
        else {
            $arm = [Math]::Min([int]$armCap.Groups[1].Value, [Math]::Floor($tiny / 2) - $slack)
            if ($arm -lt $floor) { $ornBad += "V279 the smallest section box ($tinyName) resolves to an arm of $arm, under the floor of $floor - it would come out with NO filigree while its neighbours carry it (SPEC B59)" }
            else { Pass "V279 the shortest section box ($tinyName) resolves to an arm of $arm, clear of the floor of $floor - no box is skipped" }
        }
    }

    # V283: the filigree draws OVER the content of the box (item 1 of the 2026-08-24 request,
    # SPEC I72c REVOKED, V283). Four legs, and each is about a line of Lua this gate cannot RUN
    # (SPEC B30, B34) - so what it cannot count, it FORBIDS, the way V198 and V275 already do.
    #
    # (a) the argument handed to setParent is the <layout>, not the <rectangle>. A child draws
    # above its parent and below the parent's LATER siblings, so setParent(c) - what shipped
    # until this round - put the path under every label and field the box carries. That is the
    # report that opened the round: the boxes you type in were covering the detail.
    $sp283 = @([regex]::Matches($ornBody, 'setParent\(\s*([^()]*(?:\([^()]*\))?[^()]*?)\s*\)') | ForEach-Object { $_.Groups[1].Value.Trim() })
    if ($sp283.Count -eq 0) { $ornBad += "V283 ornament() never parents the path it creates - a path on no parent is on no form, and where it sits is the whole of this round (SPEC V209, V283a)" }
    else {
        foreach ($a283 in $sp283) {
            # (b) getParent() as the argument is the trap, not a style. setParent returns early
            # when the parent is unchanged (rrpgObjs.lua:637, SPEC R112): it compiles, runs, exits
            # 0 and moves NOTHING. No error to grep for, no symptom but the effect that never
            # arrives - B6 through a new door. Detaching with nil first is the one legal path.
            if ($a283 -match 'getParent\s*\(') {
                $ornBad += "V283 setParent is handed '$a283' - handing back the parent it already has returns early and does nothing at all (rrpgObjs.lua:637, SPEC R112, V283b)"
            }
            elseif ($a283 -eq 'nil') { continue }
            elseif ($a283 -notmatch '^\w+\.parent$') {
                $ornBad += "V283 the path is parented to '$a283' - it has to take the <layout> that holds the box (c.parent), or it draws UNDER the labels and fields, which is the bug the user reported (SPEC V283a, I72c REVOKED)"
            }
        }
    }

    # (c) hitTest false is the ONLY thing between "ornament on top" and "ornament that eats every
    # click on the box". V276 asks for it on its own account; here it is a DECLARED dependency of
    # (a) - the moment the path went over the fields, that line stopped being hygiene.
    if ($ornBody -notmatch '(?m)^\s*\w+\.hitTest\s*=\s*false;') {
        $ornBad += "V283 the path now draws OVER the content and is still hit-testable - it would swallow every click meant for the dots, combos and fields under it (SPEC V283c, V276)"
    }

    # (d) V280 is a PREREQUISITE of this, and it is measured rather than only written down: the
    # inner rule sits ORN_IN in from the border, so a box whose children start closer than that
    # gets its own content struck through. With I73's 20 there is room; at the 10 this sheet had
    # before T615 the 9px rule crossed the full-width children of 68 of the 73 boxes, which is
    # why raising the path before the margin lands is a visible regression and not a fix.
    $ornIn283 = [regex]::Match($hh6, '(?m)^\s*local ORN_IN\s*=\s*(\d+);')
    if (-not $ornIn283.Success) { $ornBad += "V283 ORN_IN could not be read out of WoD20.6 - the rule this leg measures against the margin is unreadable, so the leg is a no-op (SPEC V209, V20)" }
    elseif ([int]$ornIn283.Groups[1].Value -ge 20) {
        $ornBad += "V283 the inner rule sits $($ornIn283.Groups[1].Value)px in, at or past the 20px margin I73 gives every box - a path drawn on TOP at that width strikes through the children that fill the box (SPEC V283d, V280a, I73)"
    }
}
if ($ornBad) { foreach ($b in $ornBad) { Fail $b } }
else { Pass "V276/V277/V278/V283 the filigree is a runtime path, placed by align, hidden and never destroyed, filtered by construction, and it draws OVER the content without taking a click" }

# ---- V313: the DRAWING comes out of the PALETTE, and the memo stamps all three axes ----
# SPEC I87, the 115th round. Until now there was one frame and one era wearing it, so `which
# drawing` was not a question anybody could get wrong. With two, it is - and the way to get it
# wrong is silent in both directions: a dispatch that defaults to the new drawing repaints 73
# boxes in an era nobody asked, and a memo that does not stamp the STYLE hands back the frame
# it built for the era before. Neither raises anything: rdk -l exits 0 and the sheet is wrong
# on screen only. This block is the only thing above either.
$v313Bad = @()
$ornMake313 = LuaFn $hh6 'ornament'
$styleVar313 = [regex]::Match($hh6, '(?m)^\s*local (ornStyle\w+) = nil;')

# (a) a style with no colour beside it is a DEAD key: what hides the path is the colour (V277),
# so the style would never be reached and nothing would say so. Read out of the THEMES block at
# its own indent, the same way V63 reads the eight required keys - not from a roster here.
if (-not $themesBlock) { $v313Bad += "V313 the THEMES block could not be read - leg (a) measured nothing (SPEC V209, V20)" }
else {
    $styled313 = 0
    foreach ($k in $themeKeys) {
        $pal313 = [regex]::Match($themesBlock, "(?ms)^\t{4}\[""$([regex]::Escape($k))""\] = \{(.*?)^\t{4}\},")
        if (-not $pal313.Success) { $v313Bad += "V313 palette '$k' could not be read - its ornament keys are unchecked (SPEC V209)"; continue }
        $hasStyle313 = $pal313.Groups[1].Value -match "(?m)^\t{5}ornStyle\s*="
        $hasCol313 = $pal313.Groups[1].Value -match "(?m)^\t{5}ornament\s*="
        if ($hasStyle313) {
            $styled313++
            if (-not $hasCol313) {
                $v313Bad += "V313 palette '$k' declares ornStyle and no ornament - the colour is what hides the path (V277), so the style is a key that can never draw and never complain (SPEC V313a, I87a)"
            }
        }
    }
    if ($styled313 -eq 0) { $v313Bad += "V313 no palette declares ornStyle - leg (a) matched nothing, which is B7 waiting (SPEC V209, V20)" }
    else { Pass "V313 $styled313 palette(s) declare ornStyle and every one of them declares an ornament colour too" }
}

# (b) the DEFAULT is the filigree. An era that declares no style has to fall to the drawing
# that was here first - a dispatch whose fallthrough is the filete changes 73 boxes in three
# eras that never asked, and the symptom lands an era away from the line that caused it.
if (-not $ornGeo) { $v313Bad += "V313 ornPath is gone from WoD20.6 - there is no dispatch to measure (SPEC V209)" }
else {
    $disp313 = NoComments $ornGeo
    if ($disp313 -notmatch 'if\s+style\s*==\s*"filete"') {
        $v313Bad += "V313 ornPath does not branch on the style it is handed - the palette cannot pick a drawing, and the key T703 added would do nothing at all (SPEC I87b)"
    }
    if ($disp313 -notmatch 'ornFilete\s*\(') { $v313Bad += "V313 ornPath never reaches ornFilete - the Dark Ages frame is written and never called (SPEC I87b, V209)" }
    $tail313 = [regex]::Match($disp313, '(?m)^\s*return\s+(\w+)\(w, h\);\s*$')
    if (-not $tail313.Success) { $v313Bad += "V313 ornPath has no unguarded final return - there is no default drawing, so an era with no style gets NO frame instead of the filigree (SPEC V313b)" }
    elseif ($tail313.Groups[1].Value -ne 'ornFiligree') {
        $v313Bad += "V313 ornPath falls through to $($tail313.Groups[1].Value) - the default has to be the filigree, or three eras of four silently change drawing (SPEC V313b, I87b)"
    }
    else { Pass "V313 ornPath dispatches on the palette's style and falls through to the filigree" }
}

# (c) the memo stamps the STYLE. Switching era on an open sheet moves no box, so the size
# comparison V284 already owns answers `same` and the entry is handed straight back - wearing
# the drawing of the era before, repainted in the new era's colour. B58 and B62 on a third
# axis, and the only one of the three with no measurement to notice it.
if (-not $ornMake313) { $v313Bad += "V313 ornament() is gone from WoD20.6 - there is no memo to measure (SPEC V209)" }
elseif (-not $styleVar313.Success) {
    $v313Bad += "V313 no module-level style is kept beside ornColour - refreshOrnament runs outside the theme walk with no palette in hand, so it would have nothing to redraw FROM (SPEC I87e)"
}
else {
    $sv313 = [regex]::Escape($styleVar313.Groups[1].Value)
    $ornB313 = NoComments $ornMake313

    if ($hh6 -notmatch "(?m)^\s*$sv313 = t\.ornStyle;") {
        $v313Bad += "V313 the style global is never written from the palette - it would stay nil forever and every era would draw the filigree (SPEC I87e)"
    }

    $carries313 = $false
    foreach ($t313 in [regex]::Matches($ornB313, '\{[^{}]*\}')) {
        if ($t313.Value -match ('\b' + $sv313 + '\b')) { $carries313 = $true; break }
    }

    if (-not $carries313) {
        $v313Bad += "V313 the memo entry does not carry the style - it can tell 'already drawn' and 'drawn at THIS size' and not 'drawn in THIS drawing', so changing era keeps the old frame (SPEC I87d, V313c, B58)"
    }
    elseif ($ornB313 -notmatch ("~=\s*" + $sv313 + '|' + $sv313 + "\s*~=")) {
        $v313Bad += "V313 the stored style is written and never COMPARED - the third door never opens for a change of era, and the sheet keeps the frame it had (SPEC V313c)"
    }
    else {
        $calls313 = [regex]::Matches($ornB313, 'ornPath\s*\(([^)]*)\)')
        $bare313 = @($calls313 | Where-Object { $_.Groups[1].Value -notmatch ('\b' + $sv313 + '\b') })

        if ($calls313.Count -eq 0) { $v313Bad += "V313 ornament() never calls ornPath - there is no drawing to hand a style to (SPEC V209)" }
        elseif ($bare313.Count -gt 0) {
            $v313Bad += "V313 $($bare313.Count) of $($calls313.Count) ornPath call(s) in ornament() carry no style - every one has to, or the one that does not rebuilds the box in the default drawing with nothing to say so (SPEC I87b, V313c)"
        }
        else { Pass "V313 the memo stamps size AND style, and all $($calls313.Count) ornPath calls carry it" }
    }
}

# (d) one painter, one generator. Two copies is what V67 pays not to have with THEME_STROKE and
# V308 with the marker's three motifs; here the cost of the second copy is 73 boxes wearing two
# frames at once, with nothing in rdk -l or above to say which one is stale.
$nOrn313 = [regex]::Matches($hh6, '(?m)^\s*local function ornament\s*\(').Count
$nPath313 = [regex]::Matches($hh6, '(?m)^\s*local function ornPath\s*\(').Count
if ($nOrn313 -ne 1) { $v313Bad += "V313 WoD20.6 declares $nOrn313 ornament() - one painter per style is two memos and two creation paths for one <path> (SPEC V313d, V67)" }
elseif ($nPath313 -ne 1) { $v313Bad += "V313 WoD20.6 declares $nPath313 ornPath() - the dispatch is the single door, and a second one is a second answer to which drawing is in force (SPEC V313d)" }
else { Pass "V313 one painter and one generator serve both drawings" }

if ($v313Bad) { foreach ($b in $v313Bad) { Fail $b } }

# ---- V314: the filete's geometry is a RELATION, not five literals -----------------------
# SPEC I87c, the 115th round. Every number in this drawing has the other side of a relation
# sitting in the same file, so the gate reads BOTH and never spells one here: a literal spelled
# on this side is the second truth V49 pays not to have, and the failure it hides is visual -
# a cross sitting on the corner curve, or touching the 3px outline, exits 0 and merely looks wrong.
$v314Bad = @()
$c314 = @{}
foreach ($n314 in @('ORN_BOXR', 'ORN_IN', 'ORN_CROSS', 'ORN_CROSS2', 'ORN_CROSS_AT', 'THEME_STROKE')) {
    $m314 = [regex]::Match($hh6, "(?m)^\s*local $n314\s*=\s*([0-9.]+);")
    if ($m314.Success) { $c314[$n314] = [double]$m314.Groups[1].Value }
}

if ($c314.Count -ne 6) {
    $v314Bad += "V314 only $($c314.Count) of the 6 constants this measures could be read out of WoD20.6 - the relations are unreadable, so every leg below is a no-op (SPEC V209, V20)"
} else {
    $rIn314 = $c314['ORN_BOXR'] + $c314['ORN_IN']
    $straight314 = [Math]::Sqrt(($rIn314 * $rIn314) - ($c314['ORN_IN'] * $c314['ORN_IN']))

    # (a) the cross lands on the STRAIGHT run. The corner arc gives out where the rule leaves
    # the box's own concave radius; inside that the cross sits on the curve and comes out
    # crooked. Both sides read from the Lua: move ORN_BOXR and the floor moves with it.
    if ($c314['ORN_CROSS_AT'] -le $straight314) {
        $v314Bad += "V314 the corner cross sits at $($c314['ORN_CROSS_AT']) and the rule is still on its arc until $([Math]::Round($straight314, 2)) - it would be drawn on the CURVE, crooked, with nothing to say so (SPEC V314a, I87c)"
    } else { Pass "V314 the corner cross at $($c314['ORN_CROSS_AT']) clears the corner arc, which gives out at $([Math]::Round($straight314, 2))" }

    # (b) the centre cross does not touch the frame. Centred on a rule ORN_IN in, a half-arm of
    # h reaches ORN_IN - h outward against the box's own outline, which applyTheme writes at
    # THEME_STROKE on every rectangle it repaints (V67). This is a CEILING, not a taste.
    $cap314 = $c314['ORN_IN'] - $c314['THEME_STROKE']
    if ($c314['ORN_CROSS2'] -ge $cap314) {
        $v314Bad += "V314 the centre cross has a half-arm of $($c314['ORN_CROSS2']) and reaches $($c314['ORN_IN'] - $c314['ORN_CROSS2']) out, into the $($c314['THEME_STROKE'])px outline the theme paints on every box - the ceiling is $cap314 (SPEC V314b, V67)"
    } else { Pass "V314 the centre cross half-arm of $($c314['ORN_CROSS2']) clears the $($c314['THEME_STROKE'])px outline, under the ceiling of $cap314" }

    # (c) and it does not reach the CONTENT. The filete draws on TOP of everything the box
    # carries (I72c), so what keeps it off the children is the 20px margin every box has - the
    # same number V283d measures the rule itself against, one motif further in.
    $reach314 = $c314['ORN_IN'] + $c314['ORN_CROSS2']
    if ($reach314 -gt 20) {
        $v314Bad += "V314 the centre cross reaches ${reach314}px into the box, at or past the 20px margin I73 gives every one - drawn on TOP it would strike through the children that fill the box (SPEC V314c, V280a, V283d)"
    } else { Pass "V314 the deepest the filete reaches is ${reach314}px, clear of the 20px content margin" }

    # (d) the hierarchy IS the drawing. "One slightly larger in the middle" is what the plate
    # says; equal arms lose the centre and no check above this one would notice.
    if ($c314['ORN_CROSS'] -ge $c314['ORN_CROSS2']) {
        $v314Bad += "V314 the corner cross ($($c314['ORN_CROSS'])) is not smaller than the centre one ($($c314['ORN_CROSS2'])) - the plate's whole hierarchy is that one of them is larger (SPEC V314d)"
    } else { Pass "V314 the corner cross ($($c314['ORN_CROSS'])) stays under the centre one ($($c314['ORN_CROSS2']))" }

    # (e) what a small box drops is the CROSS, never the rule - V279's doctrine in the letter
    # T613 wrote it for the filigree's arm. The floor is READ out of the Lua and measured
    # against the smallest box the XML actually authors, so neither can drift from the other.
    $roomM314 = [regex]::Match($hh6, '(?m)^\s*local ORN_CROSS_ROOM\s*=\s*2 \* \(ORN_CROSS_AT \+ ORN_CROSS \+ ORN_CROSS2\);')
    $fileteFam314 = ''
    foreach ($fn314 in @('ornFilete', 'ornFileteCorner', 'ornFileteEdge')) { $fileteFam314 += (LuaFn $hh6 $fn314) }
    $famNC314 = NoComments $fileteFam314
    $emitted314 = [regex]::Matches($famNC314, 'ornCross\s*\(').Count
    $floored314 = [regex]::Matches($famNC314, 'ORN_CROSS_ROOM').Count

    if (-not $fileteFam314) { $v314Bad += "V314 the filete family is gone from WoD20.6 - leg (e) measured nothing (SPEC V209)" }
    elseif (-not $roomM314.Success) { $v314Bad += "V314 ORN_CROSS_ROOM is not the sum of the three constants it floors - the floor and the motif could drift apart with nothing between them (SPEC V314e, V49)" }
    elseif ($famNC314 -match 'return "";') {
        $v314Bad += "V314 the filete refuses a box by size - what a small box drops is the CROSS, never the rule, and a box skipped in silence between ornamented neighbours is exactly B59 (SPEC V314e, T613)"
    }
    elseif ($emitted314 -eq 0) {
        $v314Bad += "V314 the filete emits no cross at all - the motif this leg floors does not exist (SPEC V209)"
    }
    elseif ($floored314 -lt $emitted314) {
        $v314Bad += "V314 the filete emits $emitted314 cross(es) and consults ORN_CROSS_ROOM $floored314 time(s) - a cross drawn without the test overlaps its neighbour on a short run, and counting the family ONCE passed while two of three drew unguarded (SPEC V314e)"
    }
    else {
        $room314 = 2 * ($c314['ORN_CROSS_AT'] + $c314['ORN_CROSS'] + $c314['ORN_CROSS2'])
        $tiny314 = 0
        $tinyN314 = ''
        foreach ($f in $files) {
            foreach ($r in (Doc $f.FullName).SelectNodes("//rectangle[@color='black'][@xradius]")) {
                $p314 = $r.ParentNode
                $bw314 = 0; $bh314 = 0
                if (-not [int]::TryParse($r.GetAttribute("width"), [ref]$bw314)) { [void][int]::TryParse($p314.GetAttribute("width"), [ref]$bw314) }
                if (-not [int]::TryParse($r.GetAttribute("height"), [ref]$bh314)) { [void][int]::TryParse($p314.GetAttribute("height"), [ref]$bh314) }
                if ($bw314 -le 0 -or $bh314 -le 0) { continue }
                $side314 = [Math]::Min($bw314, $bh314)
                if ($tiny314 -eq 0 -or $side314 -lt $tiny314) { $tiny314 = $side314; $tinyN314 = "$($f.Name) $($bw314)x$($bh314)" }
            }
        }
        if ($tiny314 -eq 0) { $v314Bad += "V314 no section box was found - leg (e) measured nothing (SPEC V209)" }
        elseif ($tiny314 -lt $room314) {
            $v314Bad += "V314 the smallest section box ($tinyN314) is under the ${room314}px a run needs to hold its crosses - it would come out with the bare rule while its neighbours carry the motif (SPEC V314e, B59)"
        }
        else { Pass "V314 the smallest section box ($tinyN314) clears the ${room314}px the crosses need - no box loses the motif in silence" }
    }
}

if ($v314Bad) { foreach ($b in $v314Bad) { Fail $b } }

# ---- V315: the BAR's drawing comes out of the same palette the BOX's does (SPEC I88) -----
# The 116th round. I87 gave the era its own BOX frame; this gives it the BAR, through the SAME
# palette key and the SAME module global, so no era can wear one drawing on its boxes and
# another on its tabs. Six legs, and (c) is the one that keeps the new drawing off the words.
$v315Bad = @()
$fil315  = LuaFn $hh6 'markFilete'
$path315 = LuaFn $hh6 'markPath'
$rule315 = LuaFn $hh6 'markRule'
$mkC315 = [regex]::Match($hh6, '(?m)^\s*local ORN_FIL_MARK\s*=\s*([\d.]+);')
$crC315 = [regex]::Match($hh6, '(?m)^\s*local ORN_FIL_CROSS\s*=\s*([\d.]+);')
$spC315 = [regex]::Match($hh6, '(?m)^\s*local ORN_FIL_SEP\s*=\s*([\d.]+);')
$m2C315 = [regex]::Match($hh6, '(?m)^\s*local ORN_MARK2\s*=\s*([\d.]+);')
$plC315 = [regex]::Match($hh6, '(?m)^\s*local ORN_PILLR\s*=\s*([\d.]+);')
if (-not ($fil315 -and $path315 -and $rule315)) {
    Fail "V315 markFilete / markPath / markRule could not all be read out of WoD20.6 - every leg below measures one of them, so all six would be no-ops (SPEC V209, V20)"
}
elseif (-not ($mkC315.Success -and $crC315.Success -and $spC315.Success -and $m2C315.Success -and $plC315.Success)) {
    Fail "V315 ORN_FIL_MARK / ORN_FIL_CROSS / ORN_FIL_SEP / ORN_MARK2 / ORN_PILLR are not all readable in WoD20.6 - the Lua side of these relations is gone, and a relation with one side is a no-op (SPEC V209, V20)"
}
else {
    $filBody315  = NoComments $fil315
    $pathBody315 = NoComments $path315
    $ruleBody315 = NoComments $rule315
    $filMark315  = [double]$mkC315.Groups[1].Value
    $filCross315 = [double]$crC315.Groups[1].Value
    $filSep315   = [double]$spC315.Groups[1].Value
    $mark2_315   = [double]$m2C315.Groups[1].Value
    $pillr315    = [double]$plC315.Groups[1].Value

    # (a) the style is DISPATCHED and it arrives as an ARGUMENT. markPath is declared ABOVE the
    # local that holds the era's style, so reading it there compiles to a GETGLOBAL and finds
    # nil - rdk -l exits 0 and there is no symptom to grep, which is B51 (SPEC I88a, V223).
    if ($pathBody315 -notmatch 'function markPath\(w, h, kind, style\)') { $v315Bad += "markPath does not take the style as a fourth argument - the palette has no way to reach the bar (SPEC I88a)" }
    if ($pathBody315 -notmatch 'if style == "filete" then') { $v315Bad += "markPath does not dispatch on the filete style - every era falls to the default drawing and the palette key is dead (SPEC I88a)" }
    if ($pathBody315 -match '\bornStyleNow\b') { $v315Bad += "markPath reads ornStyleNow directly, and that local is declared BELOW it - the read compiles to a GETGLOBAL and calls nil, with exit 0 and nothing to grep (SPEC I88a, V223, B51)" }
    if ($ruleBody315 -notmatch 'markPath\(bw, bh, kind, ornStyleNow\)') { $v315Bad += "markRule does not pass ornStyleNow into markPath - the style arrives nil and the era's own drawing is never reached (SPEC I88a)" }
    # EMENDED by the 118th round and NOT renumbered (SPEC I90h). The count was "exactly 1
    # palette declares ornStyle", which the chain makes false: two eras carry a style now. It
    # becomes a RELATION with both sides READ - as many palettes declaring a style as ornPath has
    # branches to answer them - so a fifth era with a sixth drawing cannot slip past a number
    # spelled here.
    #
    # And the TYPO test changes painter. It used to demand markPath dispatch on the declared
    # value, which was right while ornStyle meant one thing; `corrente` changes the BOX and
    # deliberately leaves the bar alone, so that demand would redden a product decision. What
    # still has to hold is that the BOX painter answers every style a palette declares - it is
    # the painter the key exists for, and a style ornPath does not know falls to the filigree
    # with exit 0, the gate green and only the screen left to tell. A style the BAR does not
    # dispatch is a declared subset and stays GREEN.
    $decl315 = [regex]::Matches($hh6, '(?m)^\s*ornStyle\s*=\s*"([^"]+)"')
    $pathFn315 = NoComments (LuaFn $hh6 'ornPath')
    $branch315 = @([regex]::Matches($pathFn315, 'if style == "([^"]+)" then') | ForEach-Object { $_.Groups[1].Value })
    if ($decl315.Count -eq 0) { $v315Bad += "no palette declares ornStyle - leg (a) matched nothing, which is B7 waiting (SPEC V209, V20)" }
    elseif ($branch315.Count -ne $decl315.Count) {
        $v315Bad += "$($decl315.Count) palette(s) declare a style and ornPath branches on $($branch315.Count) - a style with no branch draws the filigree instead, and a branch with no palette is a drawing nobody can reach (SPEC I90h, V315a)"
    }
    else {
        foreach ($d315 in $decl315) {
            $val315 = $d315.Groups[1].Value
            if ($branch315 -notcontains $val315) { $v315Bad += "the palette declares ornStyle = $val315 and ornPath dispatches on no such style - a mistyped style falls to the default drawing with exit 0, gate green, and only the screen left to tell (SPEC I90h, V285)" }
        }
    }

    # (b) a STYLE's constant does not cross into ANOTHER style's drawing, the way V308 keeps a
    # MOTIF's constant out of another motif (B69). EMENDED by the 119th round and NOT
    # renumbered (SPEC I91i): it enumerated the two styles that existed, and a third drawing
    # arrived - so it is a table now, and a fourth style is one row rather than a rewrite.
    #
    # THREE exceptions, and they are DECLARED rather than accidental: ORN_PILLR, the arc V228
    # authors on all nineteen buttons; ORN_SUB_MARK, the sub-tab height the user approved in
    # the 109th round, which is one fact in any style; and ORN_KAPPA, which is not a drawing
    # choice at all but the constant of the arc, so it carries none of B69's risk.
    $cor315 = LuaFn $hh6 'markCorrente'
    if (-not $cor315) { $v315Bad += "markCorrente is gone from WoD20.6 - the corrente style's drawing cannot be read, so the row this leg holds for it would measure nothing (SPEC I91a, V209, V20)" }
    else {
        $corBody315 = NoComments $cor315
        $defOwn315 = @('ORN_MARK', 'ORN_MARK2', 'ORN_MARK_RX', 'ORN_MARK_RY', 'ORN_MARK_GAP', 'ORN_SUB', 'ORN_SEP_RX', 'ORN_SEP_RY')
        $filOwn315 = @('ORN_FIL_MARK', 'ORN_FIL_CROSS', 'ORN_FIL_SEP')
        $corOwn315 = @('ORN_COR_MARK', 'ORN_COR_MARK2', 'ORN_COR_IN', 'ORN_COR_SPAN', 'ORN_COR_SUB2', 'ORN_COR_SUB_SPAN', 'ORN_COR_DOT')
        $fam315 = @(
            @{ fn = 'markPath (the default drawing)'; body = $pathBody315; forbid = ($filOwn315 + $corOwn315); sub = $false },
            @{ fn = 'markFilete';                     body = $filBody315;  forbid = ($defOwn315 + $corOwn315); sub = $true  },
            @{ fn = 'markCorrente';                   body = $corBody315;  forbid = ($defOwn315 + $filOwn315); sub = $true  }
        )
        foreach ($f315 in $fam315) {
            foreach ($bad315 in $f315.forbid) {
                if ($f315.body -match ("\b" + [regex]::Escape($bad315) + "\b(?!_|\d)")) { $v315Bad += "$($f315.fn) reads $bad315, which belongs to ANOTHER style - one constant serving two drawings is how the 110th round moved three levels when it was asked to move one (SPEC I88e, I91i, V308, B69)" }
            }
            if ($f315.sub -and $f315.body -notmatch '\bORN_SUB_MARK\b') { $v315Bad += "$($f315.fn) has no ORN_SUB_MARK - its sub-tab rule has come off the height the user saw and approved in the 109th round (SPEC I88e, I91c)" }
        }
    }

    # (c) THE relation, and the one that keeps the cross off the words. In the default style the
    # topmost ink is the upper rule, at h - ORN_MARK2 = 21, which is the state the user approved
    # on screen in the 112th round. In filete the topmost ink is the cross's UPPER ARM, so the
    # two constants have to add to the same number or the drawing climbs into the label box,
    # which closes at 20 (SPEC I88d, B68). Both sides read from Lua, neither spelled here.
    if (($filMark315 + $filCross315) -ne $mark2_315) { $v315Bad += "ORN_FIL_MARK ($filMark315) + ORN_FIL_CROSS ($filCross315) is $($filMark315 + $filCross315) against ORN_MARK2 = $mark2_315 - the two styles no longer put their topmost ink on the same line, so one of them is nearer the words than was ever approved (SPEC I88d, V307a, B68)" }

    # (d) the tab's cross is BIGGER than the separator's: the level that says least says it in
    # less, and equal arms lose the hierarchy with nothing anywhere to say so (SPEC I88f).
    if ($filCross315 -le $filSep315) { $v315Bad += "ORN_FIL_CROSS ($filCross315) is not greater than ORN_FIL_SEP ($filSep315) - the separator stops being the smaller mark and the three levels stop being told apart by quantity (SPEC I88f)" }

    # (e) the floor is READ, never spelled. What falls out for want of room is the CROSS, never
    # the rule (V279) - and a guard that CANNOT fire is B59's hole, so the narrowest of the
    # nineteen is measured against a floor built from the Lua's own numbers (SPEC I88h).
    $wid315 = @()
    $sep315 = 0
    foreach ($pr315 in @(,@("WoD20th.lfm", "tabStrip"))) {
        $st315 = (Doc (Join-Path $dir $pr315[0])).SelectSingleNode("//layout[@name='$($pr315[1])']")
        if ($null -eq $st315) { $v315Bad += "$($pr315[1]) is gone from $($pr315[0]) - the bar this invariant measures does not exist (SPEC I32, I58, V209)"; continue }
        foreach ($r315 in $st315.SelectNodes("rectangle[starts-with(@name,'tabOn')]")) { $wid315 += [double]$r315.GetAttribute("width") }
        $sep315 += @($st315.SelectNodes("rectangle[starts-with(@name,'sep')]")).Count
    }
    $foot315  = [Math]::Sqrt(($pillr315 + $filMark315) * ($pillr315 + $filMark315) - $filMark315 * $filMark315)
    $floor315 = 2 * $foot315 + 2 * $filCross315
    if ($wid315.Count -ne 11) { $v315Bad += "read $($wid315.Count) marker(s) across the one bar left, expected 11 - this check is covering less than the bars hold (SPEC V209)" }
    elseif ((($wid315 | Measure-Object -Minimum).Minimum) -le $floor315) { $v315Bad += "the narrowest marker is $(($wid315 | Measure-Object -Minimum).Minimum) against a floor of $([Math]::Round($floor315, 2)) - at that width the tab drops its crosses, and a drawing that quietly loses half its motif is not the one anybody chose (SPEC I88h, V279, B59)" }
    if ($sep315 -ne 0) { $v315Bad += "read $sep315 separator carrier(s) - sep<X> only ever stood between two SUB-tab names and T830 took the last sub-tab off the sheet, so a carrier back on the bar is a separator between names that do not exist (SPEC I78d, V306b, T830)" }

    # (f) the marker memo stamps the DRAWING. The nineteen never resize, so measure is not what
    # this memo has to tell apart - and without the stamp, swapping era on an OPEN sheet leaves
    # the bar wearing the previous era's drawing repainted in the new colour (SPEC I88g, B62).
    # The closing brace is what makes this the CREATION stamp and not the rewrite below: the
    # first draft of this leg matched a bare "s = ornStyleNow", which the restyle branch also
    # contains, so deleting the stamp left the check green - caught by the mutation V20 asks
    # for, which is the whole reason it is run before the check is believed (SPEC V20, B7).
    if ($ruleBody315 -notmatch 's = ornStyleNow\s*\}') { $v315Bad += "markRule never stamps the style into the marker memo at CREATION - swapping era on an OPEN sheet leaves the bar in the previous era's drawing repainted in the new colour, with exit 0 and the gate green (SPEC I88g, B58, B62)" }
    if ($ruleBody315 -notmatch 'e\.s = ornStyleNow') { $v315Bad += "markRule never writes the new style back into the memo after redrawing - the entry would say the OLD style for ever and every later repaint would rebuild the same path (SPEC I88g, V284)" }
    if ($ruleBody315 -notmatch 'e\.s ~= ornStyleNow') { $v315Bad += "markRule never compares the stamped style against the era in force - the memo answers already-drawn to a question about a DIFFERENT drawing (SPEC I88g, V284)" }

    if ($v315Bad) { foreach ($b in $v315Bad) { Fail "V315 $b" } }
    else { Pass "V315 the bar's drawing comes out of the palette that draws the boxes, each style keeps its own constants, and both styles put their topmost ink at $mark2_315 from the marker's foot" }
}

# ---- V316: the PALETTE may write the CORNER, and ONE selector says in whom ----------------
# SPEC I89, the 117th round. The coffin bevel is the first thing a palette writes that is
# GEOMETRY and not colour, so it is the first that can deform a rectangle rather than recolour
# it. Every leg below has its other side read out of the Lua: a number spelled on this side is
# the second truth V49 pays not to have, and the failure each one hides is visual - exit 0,
# rdk -l 0, gate green, wrong on screen.
$v316Bad = @()

$sbFn316   = LuaFn $hh6 'sectionBox'
$ornFn316  = LuaFn $hh6 'ornament'
$themeM316 = [regex]::Match($hh6, 'local function applyTheme\(v, from\)(.*?)\n\t\t\tend;', 'Singleline')
$body316   = if ($themeM316.Success) { NoComments $themeM316.Groups[1].Value } else { '' }
$boxr316   = [regex]::Match($hh6, '(?m)^\s*local ORN_BOXR\s*=\s*([0-9.]+);')
$props316  = @('cornerType', 'xradius', 'yradius')

# Zero-guard FIRST: a leg that reads nothing passes for the wrong reason, and the whole point of
# this block is that nothing above it sees the feature at all (SPEC V209, V20).
if (-not $sbFn316)              { $v316Bad += "sectionBox is not in WoD20.6 - every leg below measures a selector that does not exist (SPEC V209)" }
if (-not $themeM316.Success)    { $v316Bad += "applyTheme could not be read - the corner write is unmeasurable, so legs (c) and (d) are no-ops (SPEC V209, V20)" }
if (-not $boxr316.Success)      { $v316Bad += "ORN_BOXR could not be read out of the Lua - leg (b) has no other side and states an opinion instead of a relation (SPEC V209)" }
if ($boxCornerAny -eq 0)        { $v316Bad += "no palette declares boxCorner - the coffin bevel of I89 is not in the sheet and this whole block is measuring nothing (SPEC V209)" }

# (a) the key is a TABLE with BOTH fields, and it cannot exist by halves. `type` has to be a
# value the SDK's enum actually answers to (SPEC R117) - anything else is a write the SDK
# ignores in silence - and `radius` has to be POSITIVE. A radius of 0 is not "a square corner":
# it UNMAKES the selector, because sectionBox reads c.xradius LIVE and a 0 refuses the box, so
# one number in Modern Nights would wipe the filigree off the other three eras on an OPEN sheet.
$enum316 = @('round', 'bevel', 'innerRound', 'innerLine')
if ($boxCornerAny -ne $boxCornerDecl.Count) {
    $v316Bad += "$boxCornerAny palette key(s) named boxCorner but only $($boxCornerDecl.Count) is the { type = ..., radius = ... } table this reads - a half-declared key is a corner nobody can measure (SPEC V316a, I89a)"
}
foreach ($ty316 in $boxCornerTypes) {
    if ($enum316 -notcontains $ty316) { $v316Bad += "a palette asks for cornerType '$ty316', which is not one of the $($enum316.Count) the SDK enum answers to ($($enum316 -join ', ')) - the setter would ignore it in silence and the box would keep the concave bite (SPEC V316a, R117)" }
}
foreach ($rd316 in $boxCornerRadii) {
    if ($rd316 -le 0) { $v316Bad += "a palette asks for radius $rd316 - a radius at or under 0 makes sectionBox REFUSE the box, which strips the filigree off every OTHER era rather than squaring one corner (SPEC V316a, V278)" }
}

# (b) REVOKED by the 118th round and moved to V317a (SPEC I90d). What this leg forbade was the
# PAIRING - boxCorner beside ornament - on the grounds that the filigree and the filete arc
# CONCAVE at ORN_BOXR + ORN_IN and would cross a cut corner. That reasoning is intact; the ban
# built on it is not, because `corrente` is a drawing made FOR the bevel. The rule became "the
# style and the corner AGREE", which is strictly larger: it still refuses a concave drawing on a
# cut corner AND it refuses the direction this ban let straight through - a bevelled era asking
# for `filete`. Deleting the leg without V317a landing in the same commit would leave that whole
# axis unmeasured, which is why T715 and T716 ship together.
#
# (e) stays here and rides the same walk: exactly ONE palette declares the corner and the other
# three fall to the XML. A second one is a new shape in an era nobody asked for, and the symptom
# shows up one era away from whoever caused it - the family of B6.
$declCorner316 = @()
foreach ($k316 in $themeKeys) {
    $pal316 = [regex]::Match($themesBlock, "(?ms)^\t{4}\[""$([regex]::Escape($k316))""\] = \{(.*?)^\t{4}\},")
    if (-not $pal316.Success) { $v316Bad += "palette '$k316' could not be read - leg (e) cannot see what it declares (SPEC V209)"; continue }
    $pb316 = [regex]::Replace($pal316.Groups[1].Value, '(?m)^\s*--.*$', '')
    if ($pb316 -match '(?m)^\s*boxCorner\s*=') { $declCorner316 += $k316 }
}
if ($declCorner316.Count -ne 2) {
    $v316Bad += "$($declCorner316.Count) palette(s) declare boxCorner ($($declCorner316 -join ', ')), expected exactly 2 since the 123rd round - Modern Nights cuts and Classical Age steps; the other two fall to the corner the XML authored, and a third changes the shape of an era nobody asked about (SPEC V316e, V324a, B6)"
}

# (c) the WAY BACK OUT is written down. paint() returns early on a nil value (V61), so a write
# of t.boxCorner ALONE would leave all 73 boxes beveled FOR EVER once Modern Nights had been on:
# rdk -l exits 0, this gate stays green, and the defect exists only on screen - B58 and B62 on a
# fourth axis. The value handed to paint has to mention the palette AND the authored value, and
# the authored one has to be the SAME expression paint records as the original: an `or` onto
# some other local would restore a corner the box never had.
#
# Anchored on the CONSTRUCTION and not on the spelling (SPEC V222): find the local the palette's
# table is read INTO, then demand each painted value mention that local. Grepping for the literal
# `t.boxCorner` at the three paint sites would redden the version that reads the table once - the
# one the code is written in - and green-light the one that reads it three times.
if ($body316) {
    $bcL316 = [regex]::Match($body316, '(?m)^\s*local\s+(\w+)\s*=\s*t\.boxCorner;')
    if (-not $bcL316.Success) { $v316Bad += "applyTheme never reads t.boxCorner - the palette cannot reach the corner it declares (SPEC I89c, V209)" }
    else {
        $bcN316 = [regex]::Escape($bcL316.Groups[1].Value)
        foreach ($pr316 in $props316) {
            $pm316 = [regex]::Match($body316, "paint\(c, ""$pr316"",\s*(.+?),\s*([A-Za-z_]\w*)\);")
            if (-not $pm316.Success) { $v316Bad += "applyTheme never paints ``$pr316`` - one of the three properties the coffin bevel is made of is not written at all (SPEC V209, I89c)"; continue }
            $val316 = $pm316.Groups[1].Value
            $org316 = $pm316.Groups[2].Value
            if ($val316 -notmatch "\b$bcN316\b") { $v316Bad += "the value painted onto ``$pr316`` never reads the palette's corner - that property would keep the authored value in every era and the bevel would come out half drawn (SPEC I89c)" }
            elseif ($val316 -notmatch "\bor\s+$([regex]::Escape($org316))\b") { $v316Bad += "the value painted onto ``$pr316`` does not fall back to the AUTHORED ``$org316`` - paint() returns early on nil (V61), so leaving Modern Nights would leave all 73 boxes beveled for ever, with exit 0 and this gate green (SPEC V316c, B58, B62)" }
        }
    }
}

# (d) the selector is ONE. sectionBox exists, BOTH callers ask it, and the test itself is not
# copied anywhere else - a second copy of "which rectangles are the boxes" is what V67 pays not
# to have, and the cost of the two drifting apart here is the 19 pills beveled with a width
# priced for the concave arc (V228). Counted on the construction rather than on a roster: the
# authored-black test is the line that would have to be duplicated.
if ($sbFn316 -and $body316 -notmatch 'sectionBox\(\s*c\s*,\s*fill\s*\)') {
    $v316Bad += "applyTheme never asks sectionBox - the corner write would run on every rectangle the sheet has (SPEC V316d, V228)"
}
if ($sbFn316 -and (NoComments $ornFn316) -notmatch 'sectionBox\(\s*c\s*,\s*fill\s*\)') {
    $v316Bad += "ornament() never asks sectionBox - the two callers would each carry their own idea of which rectangles are boxes (SPEC V316d, V67)"
}
$dupe316 = [regex]::Matches((NoComments $hh6), 'normColor\(\s*fill\s*\)\s*~=\s*"#FF000000"').Count
if ($dupe316 -ne 1) {
    $v316Bad += "the authored-black test appears $dupe316 time(s) in WoD20.6, expected exactly 1 - two copies of the selector drift apart in silence and the pills are what pays (SPEC V316d, V67)"
}

# (f) and V258's ruler READS the palettes rather than the XML alone. This is the one leg whose
# other side is in THIS file, so it is read out of this file - the same trick V312 uses. Modern
# Nights renders 18 on a rectangle whose XML says 14: a ruler that consults only the attribute
# measures the SMALLER bite and quietly covers less than the sheet draws, which is B7 by radius.
$gateSrc316 = [System.IO.File]::ReadAllText($PSCommandPath)
$v258Blk316 = [regex]::Match($gateSrc316, '(?ms)^# ---- V258:.*?^if \(\$arcSeen')
if (-not $v258Blk316.Success) { $v316Bad += "the V258 block could not be read out of this gate - leg (f) is a no-op (SPEC V209, V20)" }
elseif ($v258Blk316.Value -notmatch '\$boxCornerRadii') { $v316Bad += "V258 measures the corner bite from the XML attribute alone - it would price the 14 the XML spells while Modern Nights draws 18, and cover less than the sheet has (SPEC V316f, I89g, B7)" }

if ($v316Bad) { foreach ($b in $v316Bad) { Fail "V316 $b" } }
else { Pass "V316 one palette ($($declCorner316 -join ', ')) writes the corner as $($boxCornerTypes -join ', ')/$($boxCornerRadii -join ', '), all $($props316.Count) properties fall back to the authored value, and one sectionBox answers for both callers" }

# ---- V427: the six XP frames wear the palette's corner, at the SMALLER radius -------------
# SPEC V427, I146e, T960, user 2026-09-04 item 9. The six #0A0A0A frames under the XP numbers
# (V417) never pass sectionBox - it says yes to the box black only - so the corner write of
# V316 walks past them and they stayed round in every era. A NAMED roster carries them, the
# palette's TYPE is painted, and the radius is the smaller of the palette's and the authored
# one: 18 on a 25px frame would eat the frame. Every value falls back to the authored one the
# way V316(c) demands, or the bevel outlives Modern Nights on an open sheet.
$v427Bad = @()
$roster427 = [regex]::Match($hh6, '(?s)local XP_FRAME\s*=\s*\{(.*?)\};')
$names427 = @()
if ($roster427.Success) { $names427 = @([regex]::Matches($roster427.Groups[1].Value, '(\w+)\s*=\s*true') | ForEach-Object { $_.Groups[1].Value } | Sort-Object) }
$want427 = @($V417_XP | ForEach-Object { $_.E -replace '^edt', 'bg' } | Sort-Object)
if (-not $roster427.Success) { $v427Bad += "(a) XP_FRAME is not declared in WoD20.6 - the six frames have no roster and the corner never reaches them (SPEC V427a, V209)" }
elseif (Compare-Object $names427 $want427) { $v427Bad += "(a) XP_FRAME names [$($names427 -join ', ')] and the six frames of V417 are [$($want427 -join ', ')] - a frame off the roster stays round in one era only, the shape of B6 (SPEC V427a)" }
$blk427 = [regex]::Match($body316, '(?s)if XP_FRAME\[nm\] then(.*?)\n\s*end;')
if (-not $body316) { $v427Bad += "(b) applyTheme could not be read - legs (b) and (c) are no-ops (SPEC V209, V20)" }
elseif (-not $blk427.Success) { $v427Bad += "(b) applyTheme has no 'if XP_FRAME[nm] then' block - the roster is declared and nothing paints from it (SPEC V427b, V209)" }
else {
    $fb427 = $blk427.Groups[1].Value
    $bcL427 = [regex]::Match($fb427, 'local\s+(\w+)\s*=\s*t\.boxCorner;')
    if (-not $bcL427.Success) { $v427Bad += "(b) the XP_FRAME block never reads t.boxCorner - the palette cannot reach the frames it is meant to shape (SPEC V427b)" }
    else {
        $bcN427 = [regex]::Escape($bcL427.Groups[1].Value)
        foreach ($pr427 in $props316) {
            $pm427 = [regex]::Match($fb427, "paint\(c, ""$pr427"",\s*(.+?),\s*([A-Za-z_]\w*)\);")
            if (-not $pm427.Success) { $v427Bad += "(b) the XP_FRAME block never paints '$pr427' (SPEC V427b, V316c)"; continue }
            $val427 = $pm427.Groups[1].Value; $org427 = $pm427.Groups[2].Value
            if ($val427 -notmatch "\b$bcN427\b") { $v427Bad += "(b) the value painted onto '$pr427' never reads the palette's corner (SPEC V427b)" }
            elseif ($val427 -notmatch "\bor\s+$([regex]::Escape($org427))\b") { $v427Bad += "(b) the value painted onto '$pr427' does not fall back to the authored '$org427' - paint() returns early on nil (V61) and the bevel would outlive Modern Nights (SPEC V427b, V316c)" }
            elseif ($pr427 -ne 'cornerType' -and $val427 -notmatch "math\.min\(\s*$bcN427\.radius\s*,\s*$([regex]::Escape($org427))\s*\)") { $v427Bad += "(c) '$pr427' is painted from the palette's bare radius - 18 on a 25px frame eats the frame; the frame takes the SMALLER of the two (SPEC V427c)" }
        }
    }
}
# (d) the box selector is untouched: the authored-black test is written once, and V316(d)
# measures that count - this rule adds a roster by NAME and nothing by fill.
if ($v427Bad) { foreach ($b427 in $v427Bad) { Fail "V427 $b427" } }
else { Pass "V427 the six XP frames are a named roster of $($names427.Count), applyTheme paints their corner from the palette at the smaller radius and falls back to the authored value on all $($props316.Count) properties" }

# ---- V317: the DRAWING and the CORNER agree, and the radius has ONE owner ------------------
# SPEC I90, the 118th round. This is the heir of V316b: that leg forbade boxCorner beside
# ornament, which was the only thing measurable while every drawing was built for the concave
# corner. `corrente` is built FOR the bevel, so the ban becomes an AGREEMENT - and the agreement
# catches both directions where the ban caught one.
$v317Bad = @()

$pathFn317 = LuaFn $hh6 'ornPath'
$pathNC317 = NoComments $pathFn317
$sig317    = [regex]::Match($pathNC317, 'function\s+ornPath\s*\(([^)]*)\)')
$boxr317   = [regex]::Match($hh6, '(?m)^\s*local ORN_BOXR\s*=\s*([0-9.]+);')
$in317     = [regex]::Match($hh6, '(?m)^\s*local ORN_IN\s*=\s*([0-9.]+);')
$link317   = [regex]::Match($hh6, '(?m)^\s*local ORN_LINK\s*=\s*([0-9.]+);')
$stroke317 = [regex]::Match($hh6, '(?m)^\s*local THEME_STROKE = ([0-9.]+);')

$fam317 = ''
foreach ($fn317 in @('ornCorrenteRun', 'ornCorrenteCorner', 'ornCorrenteEdge', 'ornCorrente')) { $fam317 += (LuaFn $hh6 $fn317) }
$famNC317 = NoComments $fam317

# Zero-guard FIRST - a leg that reads nothing passes for the wrong reason (SPEC V209, V20).
if (-not $pathFn317)        { $v317Bad += "ornPath is gone from WoD20.6 - there is no dispatch to measure and every leg below is a no-op (SPEC V209)" }
if (-not $famNC317)         { $v317Bad += "the corrente family is not in WoD20.6 - the drawing this whole block measures does not exist (SPEC V209)" }
if (-not $link317.Success)  { $v317Bad += "ORN_LINK could not be read out of the Lua - leg (f) has no other side and states an opinion instead of a relation (SPEC V209)" }
if (-not $boxr317.Success)  { $v317Bad += "ORN_BOXR could not be read out of the Lua - leg (b) is a no-op (SPEC V209)" }
if (-not $in317.Success)    { $v317Bad += "ORN_IN could not be read out of the Lua - legs (e) and (f) are no-ops (SPEC V209)" }
if ($boxCornerAny -eq 0)    { $v317Bad += "no palette declares boxCorner - leg (a) has nothing to pair a style with (SPEC V209)" }

# (a) the STYLE and the CORNER agree, and WHICH styles are corner-aware is READ rather than
# spelled: a branch of ornPath that forwards the radius parameter is drawn to the box's OWN
# corner, and a branch that does not is drawn to ORN_BOXR, the concave one. So the equivalence
# the gate measures is
#
#     palette declares boxCorner  <=>  its style's branch forwards the radius
#
# with both sides out of the Lua. That refuses a concave drawing on a cut corner - what V316b
# bought - AND the direction V316b let through, a bevelled era asking for `filete`. The default
# branch must NOT forward it: an era with no style has no corner either.
#
# `bevel` is the one value spelled here, and deliberately: the chain's arithmetic is a 45 degree
# CUT, and `round` or `innerRound` would keep every relation below arithmetically valid while
# drawing the links across a curve. The SDK enum is already spelled in V316a for the same
# reason - there is no Lua-side declaration of "which corners are cuts" to read instead.
if ($pathFn317 -and $sig317.Success) {
    $args317 = @($sig317.Groups[1].Value -split ',' | ForEach-Object { $_.Trim() })
    $rad317  = if ($args317.Count -ge 4) { $args317[3] } else { '' }
    if (-not $rad317) { $v317Bad += "ornPath takes $($args317.Count) parameter(s) and no radius - the chain would have to find the bevel in a constant of its own, which is a second owner for the 18 the palette declares (SPEC V317d, B69)" }
    else {
        $aware317 = @()
        foreach ($m317 in [regex]::Matches($pathNC317, 'if style == "([^"]+)" then return ([^;]+); end;')) {
            if ($m317.Groups[2].Value -match ('\b' + [regex]::Escape($rad317) + '\b')) { $aware317 += $m317.Groups[1].Value }
        }
        $tail317 = [regex]::Match($pathNC317, '(?m)^\s*return\s+(\w+)\(([^)]*)\);')
        if ($tail317.Success -and $tail317.Groups[2].Value -match ('\b' + [regex]::Escape($rad317) + '\b')) {
            $v317Bad += "the DEFAULT branch of ornPath forwards the radius - an era that declares no style has no boxCorner either, so it would be drawn to a corner it does not have (SPEC V317a)"
        }
        foreach ($k317 in $themeKeys) {
            $pal317 = [regex]::Match($themesBlock, "(?ms)^\t{4}\[""$([regex]::Escape($k317))""\] = \{(.*?)^\t{4}\},")
            if (-not $pal317.Success) { $v317Bad += "palette '$k317' could not be read - leg (a) cannot see what it declares (SPEC V209)"; continue }
            $pb317 = [regex]::Replace($pal317.Groups[1].Value, '(?m)^\s*--.*$', '')
            $st317 = [regex]::Match($pb317, '(?m)^\s*ornStyle\s*=\s*"([^"]+)"')
            $bc317 = [regex]::Match($pb317, '(?m)^\s*boxCorner\s*=\s*\{\s*type\s*=\s*"([^"]*)"')
            $wants317 = $st317.Success -and ($aware317 -contains $st317.Groups[1].Value)

            if ($wants317 -and -not $bc317.Success) {
                $v317Bad += "palette '$k317' draws '$($st317.Groups[1].Value)', whose branch is built from the box's OWN corner, and declares no boxCorner - it would be drawn to the corner the XML authored, which is not the one it was designed for (SPEC V317a, I90d)"
            }
            elseif (-not $wants317 -and $bc317.Success) {
                $v317Bad += "palette '$k317' cuts the corner and draws $(if ($st317.Success) { "'$($st317.Groups[1].Value)'" } else { 'the default filigree' }), which is built to the CONCAVE ORN_BOXR ($($boxr317.Groups[1].Value)) - the rule would arc across the very outline it decorates, with nothing to say so (SPEC V317a, I90d)"
            }
            elseif ($wants317) {
                # Which SHAPE each radius-taking style was drawn for. Two entries since the 123rd
                # round, and the pair is NAMED here rather than derived: the geometry that proves
                # each one is measured by its own invariant - V317e for the chain across a 45
                # degree cut, V324c and V324d for the step - and this leg only has to say which
                # corner the palette is then allowed to declare. A style absent from the map is a
                # style nobody wrote an agreement for, and that is the loudest failure of the two.
                $shape317 = @{ 'corrente' = 'bevel'; 'corona-muralis' = 'innerLine' }
                $st317v = $st317.Groups[1].Value
                $bc317v = $bc317.Groups[1].Value
                if (-not $shape317.ContainsKey($st317v)) {
                    $v317Bad += "palette '$k317' draws '$st317v', which takes the radius and so declares a corner, but no agreement is written for it - the pair style/corner is read from both sides and a style with no side to read is B7 waiting (SPEC V317a, V209)"
                }
                elseif ($bc317v -ne $shape317[$st317v]) {
                    $v317Bad += "palette '$k317' cuts its corner as '$bc317v' and draws '$st317v', which was built for '$($shape317[$st317v])' - every relation below stays arithmetically valid and the drawing comes out across a corner that is not its own, with rdk -l exiting 0 (SPEC V317a, I90d, I97a)"
                }
            }
        }
    }
}

# (b) ORN_BOXR is the Lua's COPY of the radius the XML authors, and until this round nothing
# soldered the two - the 117th round declared the hole and this closes it. Move the XML and
# leave the constant and the filigree arcs at the old radius over the new corner, with rdk -l
# exiting 0 and the gate green. Both sides read: the constant here, the attribute off the boxes
# V68 already counts.
if ($boxr317.Success) {
    $xr317 = @{}
    $seen317 = 0
    foreach ($f in $files) {
        foreach ($r317 in (Doc $f.FullName).SelectNodes("//rectangle[@color='black'][@xradius]")) {
            $seen317++
            $xr317[$r317.GetAttribute("xradius")] = $true
        }
    }
    if ($seen317 -lt 60) { $v317Bad += "only $seen317 section box(es) were read for leg (b), expected at least 60 - it is measuring less than the sheet has (SPEC V209)" }
    elseif ($xr317.Keys.Count -ne 1) { $v317Bad += "the boxes author $($xr317.Keys.Count) different radii ($($xr317.Keys -join ', ')) - V68 should already have caught that, and leg (b) has no single number to solder to" }
    elseif ([double]($xr317.Keys | Select-Object -First 1) -ne [double]$boxr317.Groups[1].Value) {
        $v317Bad += "ORN_BOXR is $($boxr317.Groups[1].Value) and the XML authors $($xr317.Keys | Select-Object -First 1) on all $seen317 boxes - the filigree would arc to a radius the boxes no longer have (SPEC V317b, I90i)"
    }
    else { Pass "V317 ORN_BOXR ($($boxr317.Groups[1].Value)) is soldered to the radius all $seen317 boxes author" }
}

# (c) one style, one palette. The memo stamps the STYLE (V313c) and the drawing now depends on
# the RADIUS too - two palettes sharing a style with different radii would make the memo answer
# "already drawn" about a different drawing, on an OPEN sheet, with nothing to grep. The fix is
# this rule rather than a fifth field in ornPainted: state nobody has to maintain (SPEC I90g).
$styleSeen317 = @{}
foreach ($d317 in [regex]::Matches($hh6, '(?m)^\s*ornStyle\s*=\s*"([^"]+)"')) {
    $sv317 = $d317.Groups[1].Value
    if ($styleSeen317.ContainsKey($sv317)) { $v317Bad += "'$sv317' is declared by more than one palette - the memo's style stamp would no longer identify the radius, and switching between those two eras on an OPEN sheet keeps the first one's drawing (SPEC V317c, B58, B62)" }
    $styleSeen317[$sv317] = $true
}

# (d) every call to ornPath carries all four arguments. A call that drops the radius reaches the
# chain with nil and every coordinate below becomes a Lua error inside a path nobody reads.
if ($pathFn317 -and $sig317.Success) {
    $want317 = @($sig317.Groups[1].Value -split ',').Count
    foreach ($c317 in [regex]::Matches((NoComments $hh6), '(?<!function )\bornPath\s*\(([^)]*)\)')) {
        $got317 = @($c317.Groups[1].Value -split ',').Count
        if ($got317 -ne $want317) { $v317Bad += "an ornPath call passes $got317 argument(s) against a signature of $want317 - the missing one is the radius and the chain would be drawn from nil (SPEC V317d)" }
    }
}

# (e) the chain is RELATION, not literals. R, the cut and the pitch all fall out of the box's
# radius and ORN_IN, so the gate computes what they come to and demands those numbers do NOT
# appear spelled in the family. A literal on one side is the second truth V49 pays not to have,
# and what it hides is a link sitting off the cut - exit 0 everywhere, only the screen to tell.
if ($famNC317 -and $in317.Success -and $boxCornerRadii.Count -ge 1) {
    # Distinct names on purpose: PowerShell is case-INSENSITIVE, so $r317v and $R317v are one
    # variable, and a scalar times an array is array REPETITION rather than arithmetic - the
    # first draft of this leg built an Object[] and died on the division.
    $insetV = [double]($in317.Groups[1].Value)
    $radV   = [double](@($boxCornerRadii)[0])
    foreach ($rr317 in @($boxCornerRadii)) { if ([double]$rr317 -gt $radV) { $radV = [double]$rr317 } }
    $k45317 = [Math]::Sqrt(2) - 1
    $runV   = $radV + ($insetV * $k45317)
    $cutV   = ([Math]::Sqrt(2) * $radV) - (2 * $insetV * $k45317)
    $pitchV = $cutV / 3
    foreach ($lit317 in @($runV, $cutV, $pitchV)) {
        $rx317 = '(?<![\w.])' + [regex]::Escape([Math]::Round($lit317, 1).ToString([Globalization.CultureInfo]::InvariantCulture)) + '(?![\w.])'
        $rx317b = '(?<![\w.])' + [regex]::Escape([Math]::Round($lit317).ToString()) + '(?![\w.])'
        if ($famNC317 -match $rx317 -or ($lit317 -eq [Math]::Round($lit317) -and $famNC317 -match $rx317b)) {
            $v317Bad += "the corrente family spells $([Math]::Round($lit317, 2)), which is what the radius and ORN_IN already come to - a literal on one side stops moving when the other does (SPEC V317e, V49)"
        }
    }
    $run317 = NoComments (LuaFn $hh6 'ornCorrenteRun')
    if ($run317 -notmatch 'math\.sqrt\s*\(\s*2\s*\)') { $v317Bad += "ornCorrenteRun does not compute the 45 degree offset from sqrt(2) - the inset of a CUT corner is not the inset of a curve (SPEC V317e)" }
    if ($run317 -notmatch '\bORN_IN\b') { $v317Bad += "ornCorrenteRun does not read ORN_IN - the run would sit at an inset of its own and drift from the one the user approved (SPEC V317e, V315b)" }
    if ($famNC317 -notmatch '/\s*3\b') { $v317Bad += "the corrente family never divides the cut in three - the three links are what the plate is, and a hard-coded pitch stops following the cut (SPEC V317e)" }
    if ($famNC317 -match 'return "";') { $v317Bad += "the corrente refuses a box by size - the cut is the same on every box (I90e), so a refusal here can only ever be the silent skip of B59 (SPEC V317e, V314e)" }
}

# (f) and it borrows V314's two ceilings rather than inventing clearances. Outward the link may
# not reach the 3px outline the theme paints on every box; inward it may not reach the 20px
# margin every box gives its children. Both are the numbers the CROSS already answers to.
if ($link317.Success -and $in317.Success -and $stroke317.Success) {
    $lk317 = [double]$link317.Groups[1].Value
    $dd317 = [double]$in317.Groups[1].Value
    $ss317 = [double]$stroke317.Groups[1].Value
    if ($lk317 -ge ($dd317 - $ss317)) { $v317Bad += "ORN_LINK is $lk317 and reaches $($dd317 - $lk317) out, into the $($ss317)px outline the theme paints on every box - the ceiling is $($dd317 - $ss317) (SPEC V317f, V314b, V67)" }
    if (($dd317 + $lk317) -gt 20) { $v317Bad += "the link reaches $($dd317 + $lk317)px into the box, at or past the 20px margin I73 gives every one - drawn on TOP it would strike through the children (SPEC V317f, V314c)" }
}

if ($v317Bad) { foreach ($b in $v317Bad) { Fail "V317 $b" } }
else { Pass "V317 the cut corner and the chain agree - the style whose branch reads the box's own radius is the one whose palette cuts it - the run stops $([Math]::Round($runV, 2)) in, the cut it leaves is $([Math]::Round($cutV, 2)) long and each of the three links spans $([Math]::Round($pitchV, 2)), none of them spelled in the Lua, and the link clears both of V314's ceilings" }
# ---- V304: the open tab is marked in all FOUR eras, and the line is thin (SPEC I78a, I78e)
# This replaces V282 whole. V282 tied the bar's line work to `ornament`, which only the Victorian
# palette declares - so the marker existed in ONE era of four and the sheet opened with no tab
# looking open in the other three. That is V229's failure shape, arriving through the door the
# check meant to guard it.
#
# The carrier is transparent BOTH ways and authors no sides: a rectangle that draws anything at
# all draws it 3px thick, because applyTheme writes THEME_STROKE onto every rectangle it repaints
# (SPEC V67), and the drawing the user picked is a hairline. So the rectangle carries and the
# path draws - and the path is fed the era's stroke accent, which all four palettes have.
$v304Bad = @()
$carriers304 = 0
foreach ($pr304 in @(,@("WoD20th.lfm", "tabStrip"))) {
    $st304 = (Doc (Join-Path $dir $pr304[0])).SelectSingleNode("//layout[@name='$($pr304[1])']")
    if ($null -eq $st304) { $v304Bad += "$($pr304[1]) is gone from $($pr304[0]) (SPEC I32, I58, V209)"; continue }
    foreach ($on304 in $st304.SelectNodes("rectangle[starts-with(@name,'tabOn')]")) {
        $carriers304++
        $nm304 = $on304.GetAttribute("name")

        # (a) transparent both ways, and no sides - a CARRIER, not a drawing.
        if ($on304.GetAttribute("color") -ne '#00000000') {
            $v304Bad += "$nm304 is filled '$($on304.GetAttribute('color'))' - the marker carries a path now and paints nothing itself (SPEC I78a)"
        }
        if ($on304.GetAttribute("strokeColor") -ne '#00000000') {
            $v304Bad += "$nm304 authors strokeColor='$($on304.GetAttribute('strokeColor'))' - applyTheme would hand it the 3px rule of V67 and the drawing is a 1px hairline (SPEC I78a, V67)"
        }
        if ($on304.GetAttribute("sides")) {
            $v304Bad += "$nm304 authors sides='$($on304.GetAttribute('sides'))' - that was the 108th round's foot rule, and it comes back at 3px over the path that replaced it (SPEC I78a)"
        }

        # (c) authored hidden, because applyTabVisibility and renderSubTabs are what light it -
        # and the path rides that same visible, which is why it needs no state of its own.
        if ($on304.GetAttribute("visible") -ne 'false') {
            $v304Bad += "$nm304 is authored visible='$($on304.GetAttribute('visible'))' - the sheet would open with every tab marked at once, until the first render (SPEC V94, I78a)"
        }
    }
}

# (b) the colour the painter is handed comes from the STROKE map, which all four palettes key,
# and never from t.ornament, which one of them declares. This is the whole of why V282 died.
if ($hh6 -notmatch '(?m)^\s*local accent = t\.stroke\[normColor\("#FFFFFF"\)\];') {
    $v304Bad += "applyTheme does not read the era's accent out of t.stroke - without it the bar has no colour that exists in all four palettes (SPEC I78e, V304b)"
}
if ($hh6 -match '(?m)^\s*markRule\([^\r\n]*t\.ornament') {
    $v304Bad += "markRule is handed t.ornament - only the Victorian palette declares one, so the open tab would vanish in three eras of four with nothing red anywhere (SPEC I78e, V229, B66)"
}

if ($carriers304 -lt 11) { Fail "V304 only $carriers304 marker carrier(s) were read, expected 11 - this check is covering less than the one bar holds (SPEC V209)" }
elseif ($v304Bad) { foreach ($b in $v304Bad) { Fail "V304 $b" } }
else { Pass "V304 all $carriers304 markers are transparent carriers authored hidden, and the painter is handed the era's stroke accent - so the open tab shows in every era, at 1px" }


# ---- V306: one painter, three motifs, each picked by construction (SPEC I78b, I78d) -----
# The old filter - transparent fill plus the house radius - died when the button lost its box:
# since I77a both halves of every pair match it. What tells them apart now is the NAME, and the
# level comes from the bar the control sits in. That is one name read (tabStrip) and two family
# prefixes, which is the idiom note<X> and lbl<X> already established - not a roster of 24.
#
# (c) is why nothing here knows which tab is open: the path is a CHILD of the carrier, and the
# carrier's visible is already written by applyTabVisibility and renderSubTabs. A second opinion
# on that is exactly what V227 keeps off the sheet.
$v306Bad = @()
$markFn = LuaFn $hh6 'markRule'
$markKindFn = LuaFn $hh6 'markKind'
$markGeo = LuaFn $hh6 'markPath'
if (-not $markFn) { Fail "V306 markRule is gone from WoD20.6 - the bar has no painter to measure (SPEC I78e, V209)" }
elseif (-not $markKindFn) { Fail "V306 markKind is gone - nothing picks the motif and (b) has nothing to read (SPEC I78b, V209)" }
elseif (-not $markGeo) { Fail "V306 markPath is gone - the marker has no geometry of its own and only ornPath is left, which is the frame R111 refused (SPEC R111, V306)" }
else {
    $markBody = NoComments $markFn
    $kindBody = NoComments $markKindFn
    $geoBody = NoComments $markGeo

    # (a) it runs off the SAME traversal the boxes do, and it is handed the era's accent. A
    # painter nothing calls is the state I72 shipped in until the 98th round specced it.
    if ($hh6 -notmatch '(?m)^\s*markRule\(\s*c\s*,\s*nm\s*,\s*accent\s*\);') {
        $v306Bad += "nothing calls markRule with the name and the era's accent - the bar would never be drawn and no other check would notice (SPEC I78e, V306a)"
    }

    # (b) three motifs, and the two families are CLOSED - every control the painter accepts is
    # named tabOn* or sep*, and the level is read off the parent bar.
    foreach ($lit306 in @('"sep"', '"tabOn"', '"tabStrip"', '"sub"', '"tab"')) {
        if ($kindBody -notmatch [regex]::Escape($lit306)) {
            $v306Bad += "markKind never mentions $lit306 - one of the three motifs cannot be reached, and an unreachable branch is a drawing that silently never appears (SPEC I78b, V306b)"
        }
    }
    if ($kindBody -notmatch '\.parent') {
        $v306Bad += "markKind does not read the parent bar - without the level the top bar and a sub-bar get the same motif and the hierarchy the user asked for is gone (SPEC I78b, V306b)"
    }
    foreach ($k306 in @('"sep"', '"sub"')) {
        if ($geoBody -notmatch [regex]::Escape($k306)) {
            $v306Bad += "markPath has no branch for kind $k306 - markKind hands it a motif the geometry does not know and the path comes out empty (SPEC I78b, V306b)"
        }
    }

    # The "every sep<X> names a real sub-tab" leg went with SUB_TABS in T830: there is no roster of
    # sub-tab names left to match a separator against, and no separator left to match. What replaced
    # it is stronger and needs no roster - a sep<X> ANYWHERE on the sheet is now the failure, because
    # the thing it stands between cannot exist (SPEC I78d, V268 retired, T830).
    $seps306 = 0
    $carr306 = 0
    foreach ($f in $files) {
        foreach ($s306 in (Doc $f.FullName).SelectNodes("//rectangle[starts-with(@name,'sep')]")) {
            $seps306++
            $sn306 = $s306.GetAttribute("name").Substring(3)
            if ($s306.GetAttribute("hitTest") -ne 'false') { $v306Bad += "sep$sn306 authors hitTest='$($s306.GetAttribute('hitTest'))' - it sits over the gap between two buttons and would eat the click meant for one of them (SPEC I78d)" }
            if ($s306.GetAttribute("color") -ne '#00000000' -or $s306.GetAttribute("strokeColor") -ne '#00000000') { $v306Bad += "sep$sn306 is not transparent both ways - it carries a path and paints nothing itself, exactly like the markers (SPEC I78d, I78a)" }
        }
        foreach ($c306 in (Doc $f.FullName).SelectNodes("//rectangle[starts-with(@name,'tabOn')]")) { $carr306++ }
    }
    if ($seps306 -ne 0) { $v306Bad += "$seps306 separator(s) on the sheet - the last two went with numStrip and hedgeStrip in T830, the way the vampStrip pair went in T832, and sep<X> has never stood anywhere but between two SUB-tab names (SPEC I78d, V306b, T830)" }

    # (c) created once, parented to the carrier, hidden and never destroyed (SPEC V277).
    if ($markBody -notmatch 'setParent\(\s*c\s*\)') {
        $v306Bad += "the path is not parented to the control the painter picked - on the strip itself an align='client' child fills the whole band, and the carrier's visible would stop hiding it (SPEC I78b, V306c, V283a)"
    }
    if ($markBody -notmatch 'visible\s*=\s*false;') { $v306Bad += "the line work is never hidden - the painter must be able to take it back, the way V277 promises for the filigree (SPEC V277, V306c)" }
    if ($markBody -match 'destroy') { $v306Bad += "the line work is DESTROYED rather than hidden - the memo would keep a dead handle (SPEC V277, V306c)" }
    if ($markBody -notmatch '\bmarkPath\s*\(') { $v306Bad += "markRule does not build its path from markPath - the marker's motif is its own (SPEC R111)" }
    if ($markBody -match '\bornPath\s*\(') { $v306Bad += "markRule reaches for ornPath - 2*ORN_ARM is 80 and a marker is 30 tall, which is the frame R111 ruled out (SPEC R111)" }

    $targets306 = $carr306 + $seps306
    if ($targets306 -lt 11) { Fail "V306 the painter has $targets306 target(s) on the sheet, expected 11 - the eleven markers of the one bar left, and no separator since T830; a painter with nothing to paint passes forever (SPEC V209, B7)" }
    elseif ($v306Bad) { foreach ($b in $v306Bad) { Fail "V306 $b" } }
    else { Pass "V306 one painter reaches $targets306 targets - $carr306 markers and $seps306 separators - picks its motif from the name and the bar, and hangs the path on the carrier that already knows which tab is open" }
}

# ---- V280: what a section box is spaced BY (SPEC I73, V280, user 2026-08-24) -----------
# TWO legs since the 107th round - (b), the gap between two boxes, left for V298 when the user
# took it from 20 to 15 to 10 to 5 (SPEC I76a). What is left here is the INTERNAL margin,
# still 20 - the two numbers diverge by 4x now, and V298 is the single owner of the gap (B70).
# What they replace is the thing that made I73 necessary in the first place: a
# mixture of 10, 15 and 20 measured across the sheet on 2026-08-24, every one of them derived
# box by box because nothing here held a single number for either question.
#
# The box is told by CONSTRUCTION, the sentence V278 already uses on the Lua side - a <layout>
# wearing a black rectangle with a corner radius of its own. Naming 73 boxes here instead
# would go stale the first time one is added, and the count below is what says so out loud.
#
# (c) is a rule about this CHECK, not about the sheet: no leg reads the left or top of a child
# carrying rotationAngle. For a rotated label those are the PRE-rotation box - the three
# ATTRIBUTES headings author left=-20 and render inside the box - so reading them as margin
# reddens code that is right. V27, V239 and V240 all cut the same way already, and this one
# INHERITS the cut rather than inventing another: reading their silence as a hole is B61.
$v280Bad = @()
$v280Boxes = @()
$v280Rot = 0
$v280Overlay = 0
foreach ($f in $files) {
    foreach ($box in (Doc $f.FullName).SelectNodes("//layout[rectangle[@color='black'][@xradius]]")) {
        $bl = 0; $bt = 0; $bw = 0; $bh = 0
        if (-not ([int]::TryParse($box.GetAttribute("width"), [ref]$bw) -and [int]::TryParse($box.GetAttribute("height"), [ref]$bh))) { continue }
        if ($OVERLAY_BOXES -contains $box.GetAttribute("name")) { $v280Overlay++; continue }
        [void][int]::TryParse($box.GetAttribute("left"), [ref]$bl)
        [void][int]::TryParse($box.GetAttribute("top"), [ref]$bt)
        $t280 = $box.SelectSingleNode("label[@horzTextAlign='center']")
        $v280Boxes += [pscustomobject]@{
            F = $f.Name; N = $(if ($t280) { $t280.GetAttribute("text") } else { '(untitled)' })
            L = $bl; T = $bt; W = $bw; H = $bh; Node = $box; P = $box.ParentNode }
    }
}
if ($v280Boxes.Count -ne 68) { Fail "V280 $($v280Boxes.Count) section box(es) were collected, expected the 68 I73 measures - the construction filter stopped matching and both legs below would be reading a fraction of the sheet (SPEC V209, I73). Was 73 until T872 took the three Ghoul DESCRIPTION boxes away and 70 until T874 took the four Numina ones (SPEC V365d)" }
else {
    # (a) TWO numbers since T913: 20 on the X sides, 15 on the Y ones (SPEC I137c, user
    # 2026-09-02). The X pair is a FLOOR and always was. The Y pair splits: the FOOT is a floor,
    # for V240's old reason - boxes welded to a closing line carry the slack INSIDE (SPEC B63) -
    # and the HEAD is an EQUALITY, because it has no freedom at all and a floor there would let a
    # new box keep 20 and never be told (SPEC I137c).
    foreach ($b in $v280Boxes) {
        $ml = [int]::MaxValue; $mr = [int]::MaxValue; $mt = [int]::MaxValue; $mb = [int]::MaxValue
        foreach ($k in $b.Node.ChildNodes) {
            if ($k.NodeType -ne 'Element') { continue }
            if ($k.LocalName -in @('dataLink', 'script', 'event', 'template')) { continue }
            if ($k.GetAttribute("align") -eq 'client') { continue }              # the backdrop, and anything else filling the box
            if ($k.LocalName -eq 'rectangle' -and $k.GetAttribute("left") -eq '0' -and $k.GetAttribute("top") -eq '0') { continue }
            if ($k.HasAttribute("rotationAngle")) { $v280Rot++; continue }       # (c)
            $kl = 0; $kt = 0; $kw = 0; $kh = 0
            if (-not ([int]::TryParse($k.GetAttribute("left"), [ref]$kl) -and [int]::TryParse($k.GetAttribute("top"), [ref]$kt))) { continue }
            [void][int]::TryParse($k.GetAttribute("width"), [ref]$kw)
            [void][int]::TryParse($k.GetAttribute("height"), [ref]$kh)
            if ($kl -lt $ml) { $ml = $kl }
            if ($kt -lt $mt) { $mt = $kt }
            if (($b.W - $kl - $kw) -lt $mr) { $mr = $b.W - $kl - $kw }
            if (($b.H - $kt - $kh) -lt $mb) { $mb = $b.H - $kt - $kh }
        }
        if ($ml -eq [int]::MaxValue) { $v280Bad += "$($b.F) '$($b.N)' holds nothing this leg can measure - a box with no placed child is a box with no margin to read (SPEC V209)"; continue }
        if ($ml -lt $BOX_PAD_X -or $mr -lt $BOX_PAD_X -or $mt -lt $BOX_PAD_Y -or $mb -lt $BOX_PAD_Y) {
            $v280Bad += "$($b.F) '$($b.N)' ($($b.W)x$($b.H)) leaves L=$ml R=$mr T=$mt B=$mb - a section box clears $BOX_PAD_X on the X sides and $BOX_PAD_Y on the Y ones (SPEC I73, V280a, I137c)"
        }
        elseif ($mt -ne $BOX_PAD_Y) {
            $v280Bad += "$($b.F) '$($b.N)' opens $mt above its first child where the head is exactly $BOX_PAD_Y - the head is the one margin with no freedom, so a floor there would let a box keep the old number and never be told (SPEC I137c, V280a)"
        }
    }
    # (c) zero-guard: the exclusion has to FIRE. If the three rotated headings ever stop being
    # matched - renamed attribute, a different way of turning them - this leg goes quiet and
    # (a) starts reading a pre-rotation box as a margin, which is B61 arriving a second time.
    if ($v280Overlay -ne $OVERLAY_BOXES.Count) { $v280Bad += "$v280Overlay overlay box(es) were skipped, expected $($OVERLAY_BOXES.Count) - the exclusion is reading a name nothing carries, and a floating box would be measured for a margin and a neighbour it does not have (SPEC V20, B7)" }
    if ($v280Rot -eq 0) { $v280Bad += "no rotated child was skipped by (a) - the cut V27/V239/V240 make is not firing here, and the next rotated label would be read as a margin it is not (SPEC V209, V280c, B61)" }

    if ($v280Bad) { foreach ($b in $v280Bad) { Fail "V280 $b" } }
    else { Pass "V280 all $($v280Boxes.Count) section boxes clear 20 on four sides, and $v280Rot rotated child(ren) were cut out of the margin" }
}

# ---- V280(d): the FOOT of a section box has a REASON (SPEC T881, I118f, I73) ------------
# Leg (a) charges 20 on four sides and stops there, so the foot - what is left between the last
# row and the bottom border - has been free since I73 was written. Measured on 2026-09-01 it held
# 20 in three boxes and 35 in two, both green, and nothing could say which was right; T880 made it
# three values by putting 26 under RITUALS. A number with no owner is how every layout round picks
# one by hand, which is the drift I73 was opened to stop, arriving through the bottom of the box.
#
# The relation is the one I118f found and T879/T880 paid for: THE FOOT IS WHAT MAKES A COLUMN
# CLOSE. A box ends at 61 + 25*rows + foot and the rows are the content, so on a column that has
# to end on a given line the foot is the only free number - pinning where the columns close pins
# every foot on them. That is why this is a relation and not a list of legal feet: a list is three
# literals with no reason and the next round writes a fourth. 26 is not a magic number, it is what
# 22 ritual rows need in order to land where the other two columns already land.
#
# The Y axis and not the X: V225(b) tried the same sentence on the right edge and was RETIRED as
# impossible, the three widths being different on purpose (SPEC B106, T832). Heights are the axis
# where the alignment is real, and this round is what made it real.
#
# The three columns are READ off the tab, never named here - a roster typed into the gate is
# the fourth literal this leg exists to refuse (SPEC V221, V225a). Until T909 they were read
# through the <import> each column held; the three forms are gone (SPEC I136i, V397), so a
# column is the direct <layout> child below the band and its boxes are the ones the V280 census
# already collected under it. Same three columns, one document (SPEC V20).
$v280dBad = @()
$colBottoms = @{}
$bandTop881 = BandTop ((Doc (Join-Path $dir "WoD20.11.lfm")).SelectSingleNode("//scrollBox"))
foreach ($col881 in ((Doc (Join-Path $dir "WoD20.11.lfm")).SelectNodes("//scrollBox/layout") | Where-Object { [int]$_.GetAttribute("top") -gt $bandTop881 })) {
    $sub881 = ColId361 $col881
    if (-not $sub881) { $sub881 = "the column at x=$($col881.GetAttribute('left'))" }
    $in881 = @($v280Boxes | Where-Object { $_.F -eq "WoD20.11.lfm" -and $_.P.LocalName -eq "layout" -and $_.P.GetAttribute("left") -eq $col881.GetAttribute("left") -and $_.P.GetAttribute("top") -eq $col881.GetAttribute("top") })
    if ($in881.Count -eq 0) { $v280dBad += "(d) the column $sub881 holds no section box the V280 census can see - its foot goes unmeasured and the equality below would be struck between the columns that are left (SPEC V20, B7)"; continue }
    $feet881 = @()
    foreach ($b881 in $in881) {
        # The foot is what the box has under its LAST child, which is the same subtraction leg
        # (a) makes on the other three sides. Children with no numbers of their own - the black
        # rectangle that tells a box from a layout - carry no bottom to measure.
        $low881 = [int]::MinValue
        foreach ($k881 in $b881.Node.ChildNodes) {
            if ($k881.NodeType -ne 'Element' -or $k881.HasAttribute("rotationAngle") -or $k881.GetAttribute("align") -eq 'client') { continue }
            $kt881 = 0; $kh881 = 0
            if (-not [int]::TryParse($k881.GetAttribute("top"), [ref]$kt881)) { continue }
            [void][int]::TryParse($k881.GetAttribute("height"), [ref]$kh881)
            if (($kt881 + $kh881) -gt $low881) { $low881 = $kt881 + $kh881 }
        }
        if ($low881 -eq [int]::MinValue) { $v280dBad += "(d) '$($b881.N)' in the $sub881 column has no positioned child - its foot is unmeasurable and the box would pass this leg by being empty (SPEC V20)"; continue }
        $feet881 += "$($b881.N) $($b881.H - $low881)"
    }
    $colBottoms[$sub881] = [pscustomobject]@{
        Bottom = (($in881 | ForEach-Object { $_.T + $_.H }) | Measure-Object -Maximum).Maximum
        Feet   = $feet881
    }
}
# Zero-guard, and it is TWO questions: that columns were found at all, and that more than one was
# - an equality struck over a single column is true for every foot ever written (SPEC V20, B7).
if ($colBottoms.Count -lt 2) { $v280dBad += "(d) $($colBottoms.Count) column(s) of the Ghoul tab were read, and an equality needs at least two - with one the rule passes over any foot at all (SPEC V20, B7, V209)" }
else {
    $ends881 = @($colBottoms.Values | ForEach-Object { $_.Bottom } | Sort-Object -Unique)
    if ($ends881.Count -ne 1) {
        $v280dBad += "(d) the $($colBottoms.Count) columns of the Ghoul tab close at $((($colBottoms.Keys | Sort-Object | ForEach-Object { "$_ at $($colBottoms[$_].Bottom)" }) -join ', ')) - they close on ONE line, and the foot of a box is the only free number in 61 + 25*rows + foot, so a column ending anywhere else is a foot somebody chose by hand (SPEC I118f, T879, T880, T881, I73)"
    }
}
if ($v280dBad) { foreach ($b in $v280dBad) { Fail "V280 $b" } }
else { Pass "V280 (d) the $($colBottoms.Count) Ghoul columns all close at $(@($colBottoms.Values)[0].Bottom), which is what gives each foot its reason - $((($colBottoms.Keys | Sort-Object | ForEach-Object { $colBottoms[$_].Feet }) -join ', '))" }

# ---- V298: the gap BETWEEN two section boxes (SPEC I76a, V298, user 2026-08-25) --------
# This IS V280b, moved out and re-numbered when it split off; the NUMBER since then is amended in
# place - 20 on the 98th, 15 on the 108th, 10 on the 110th, 5 here - only the literal moves, and a third
# name for one rule is what T664 chose against. The old value is gone rather than tolerated: two
# rules over one gap is the shape B7 warns about, and a check that accepts either number accepts
# the drift it exists to catch - so the mutation this check owes is 10, the value of the round
# just gone, not the 20 nobody would author by accident any more.
#
# 5 and the INTERNAL margin of 20 are DIFFERENT numbers, on purpose (SPEC I76a), and they now
# diverge by 2x: the margin pays for the ornament's palmette, which reaches 23 on the diagonal,
# and a gap pays for nothing at all - at 5 it no longer even parts the two 3px strokes it sits
# between, it BUTTS them (SPEC I76a: centred stroke leaves ~2px of air, inset leaves 5 - the
# screen answers that, T678 asks it). V280a keeps the 20, this keeps the 5, and they are 4x apart.
#
# BOTH axes, because B52 cost a whole round proving a mutation on X says nothing about Y
# (SPEC V222), and Y is the axis that carried the 5px of WoD20.4 when I73 first measured the
# sheet, while X had nothing below 10 even then.
#
# Neighbours, not every pair: two boxes under one parent that face each other with no third
# box standing between them. Scope is box-to-box ONLY - button-to-button (4) and bar-to-pane
# (12 and 4) belong to V281/V299 and V232, and I76a names them as staying out, so reddening
# on them would be a false alarm on numbers this round agreed not to touch.
if ($v280Boxes.Count -ne 68) { Fail "V298 $($v280Boxes.Count) section box(es) were collected, expected the 68 I73 measures - with the collector broken this leg reads a fraction of the sheet (SPEC V209, I73). One collector serves both this and V280 (B70), so the number moves once" }
else {
    # The declared HOLE is GONE with T908 and the 5px rule is whole again. T904 had left the
    # 680..1010 band of the Main grid with no bottom box, so two boxes faced each other a whole
    # band apart and 675..1015 was named here BY INTERVAL (SPEC I134m, Q44). The owner filled
    # that band with SPECIALTIES on 2026-09-02, so the exception lived exactly one round - which
    # is what naming a hole by interval is good for: it dies the day its reason dies, instead of
    # sitting here ready to excuse the NEXT 340px gap (SPEC I136c, V209, B7).
    $v298Bad = @()
    $gapsX = 0; $gapsY = 0
    foreach ($grp in ($v280Boxes | Group-Object { $_.F + '|' + $_.P.GetHashCode() })) {
        $arr = @($grp.Group)
        foreach ($a in $arr) { foreach ($c in $arr) {
            if ([object]::ReferenceEquals($a, $c)) { continue }
            if (($a.T -lt ($c.T + $c.H)) -and ($c.T -lt ($a.T + $a.H)) -and ($c.L -ge ($a.L + $a.W))) {
                $between = $false
                foreach ($m in $arr) {
                    if ([object]::ReferenceEquals($m, $a) -or [object]::ReferenceEquals($m, $c)) { continue }
                    if (($m.T -lt ($c.T + $c.H)) -and ($c.T -lt ($m.T + $m.H)) -and ($m.L -ge ($a.L + $a.W)) -and (($m.L + $m.W) -le $c.L)) { $between = $true; break }
                }
                if (-not $between) {
                    $gapsX++
                    $g = $c.L - ($a.L + $a.W)
                    if ($g -ne 5) { $v298Bad += "$($a.F) '$($a.N)' closes at x=$($a.L + $a.W) and '$($c.N)' opens at x=$($c.L) - $($g)px between two section boxes where the house leaves 5 (SPEC I76a, V298)" }
                }
            }
            if (($a.L -lt ($c.L + $c.W)) -and ($c.L -lt ($a.L + $a.W)) -and ($c.T -ge ($a.T + $a.H))) {
                $between = $false
                foreach ($m in $arr) {
                    if ([object]::ReferenceEquals($m, $a) -or [object]::ReferenceEquals($m, $c)) { continue }
                    if (($m.L -lt ($c.L + $c.W)) -and ($c.L -lt ($m.L + $m.W)) -and ($m.T -ge ($a.T + $a.H)) -and (($m.T + $m.H) -le $c.T)) { $between = $true; break }
                }
                if (-not $between) {
                    $gapsY++
                    $g = $c.T - ($a.T + $a.H)
                    if ($g -ne 5) { $v298Bad += "$($a.F) '$($a.N)' closes at y=$($a.T + $a.H) and '$($c.N)' opens at y=$($c.T) - $($g)px between two section boxes where the house leaves 5 (SPEC I76a, V298)" }
                }
            }
        } }
    }
    if ($gapsX -eq 0 -or $gapsY -eq 0) { $v298Bad += "measured $gapsX gap(s) on X and $gapsY on Y - an axis with no gap read is an axis with no rule, which is exactly the half of B52 that shipped green (SPEC V209, V222)" }
    if ($v298Bad) { foreach ($b in $v298Bad) { Fail "V298 $b" } }
    else { Pass "V298 $gapsX gaps on X and $gapsY on Y between section boxes are all 5 apart" }
}

# ---- V401: the three Ghoul columns open 5 under the band -------------------------------
# SPEC V401, B133, I139e, V298, user 2026-09-02 item 4. DOMINATOR and BLOOD POOL close at 128 and
# the first box of each column opened at 160 - a 32px gap where the sheet's own is 5, visible to
# the naked eye and green in the gate for as long as the columns have been there.
#
# V298 never saw it because it groups by PARENT: the band boxes are children of the scrollBox and
# the first box of a column is a child of the COLUMN layout, so the two ends of that gap are not
# siblings and the pair was never in the comparison at all. This rule measures it ABSOLUTE, and it
# NAMES the file instead of teaching V298 to resolve nesting for every pair on the sheet - that
# would be a second V298 rather than an amendment of it (SPEC V328d).
#
# Nothing is typed: the band bottom is read off the band boxes, the 5 is the gap V298 charges
# everywhere else, and the column tops are read off the columns.
$v401Bad = @()
$doc401 = Doc (Join-Path $dir "WoD20.11.lfm")
$sb401 = $doc401.SelectSingleNode("//scrollBox")
$bandTop401 = BandTop $sb401
$band401 = -1
$cols401 = @()
foreach ($n401 in $doc401.SelectNodes("//scrollBox/layout")) {
    $t401 = -1; $h401 = -1
    if (-not [int]::TryParse($n401.GetAttribute("top"), [ref]$t401)) { continue }
    if (-not [int]::TryParse($n401.GetAttribute("height"), [ref]$h401)) { continue }
    # the partition V361 already uses on this tab: a direct child of the scrollBox at top=0 is the
    # BAND, anything below it is a COLUMN. No second shape, and no roster of names to keep in step.
    if ($t401 -eq $bandTop401) { if (($t401 + $h401) -gt $band401) { $band401 = $t401 + $h401 } }
    else { $cols401 += $n401 }
}
if ($band401 -lt 0) { $v401Bad += "no band box was found at top=0 on WoD20.11 - the y this rule measures from cannot be read (SPEC V20, V209)" }
elseif ($cols401.Count -ne 3) { $v401Bad += "$($cols401.Count) column(s) were found under the band, expected 3 - the sweep broke and the gaps below would be struck over nothing (SPEC V209, V221)" }
else {
    foreach ($c401 in $cols401) {
        $ct401 = [int]$c401.GetAttribute("top")
        $first401 = [int]::MaxValue
        $name401 = "(untitled column)"
        foreach ($bx401 in $c401.SelectNodes("layout")) {
            $bt401 = -1
            if (-not [int]::TryParse($bx401.GetAttribute("top"), [ref]$bt401)) { continue }
            $hasBack401 = $false
            foreach ($k401 in $bx401.ChildNodes) {
                if ($k401.NodeType -eq 'Element' -and $k401.LocalName -eq 'rectangle' -and $k401.GetAttribute("align") -eq 'client') { $hasBack401 = $true; break }
            }
            if (-not $hasBack401) { continue }
            if ($bt401 -lt $first401) {
                $first401 = $bt401
                $lb401 = $bx401.SelectSingleNode("label[@text]")
                if ($null -ne $lb401) { $name401 = $lb401.GetAttribute("text") }
            }
        }
        if ($first401 -eq [int]::MaxValue) { $v401Bad += "the column at left=$($c401.GetAttribute('left')) holds no box with a backdrop - the gap would be measured against nothing (SPEC V209)"; continue }
        $abs401 = $ct401 + $first401
        if ($abs401 -ne ($band401 + 5)) {
            $v401Bad += "'$name401' opens at y=$abs401 absolute and the band closes at $band401 - $($abs401 - $band401)px where every other pair of section boxes on the sheet is 5 apart (SPEC V298, B133)"
        }
    }
}
if ($v401Bad) { foreach ($b401 in $v401Bad) { Fail "V401 $b401" } }
else { Pass "V401 the 3 Ghoul columns each open 5px under the band that closes at y=$band401 - the gap V298 cannot see, because the two ends of it are not siblings" }

# ---- V404: a transparent FILL declares its own contour ---------------------------------
# SPEC V404, B136, I140a, I140b, user 2026-09-02 item 1. applyTheme walks every THEME_SHAPES
# rectangle and writes `t.stroke[normColor(authored(c,"strokeColor"))]` plus THEME_STROKE
# (WoD20.6.lfm). With NOTHING authored, the era hands the rectangle its default rule - and on a
# rectangle whose FILL is transparent, that rule is a contour with no box inside it.
#
# popScrim was the one of twenty-seven that did not declare, and it is 1595x936: the era drew a
# frame around the whole sheet, and it lit every time the picker or the ? opened, because mfOpen
# and popOpen raise the SAME scrim. One cause, the two symptoms the owner reported (SPEC B136).
#
# This is NOT V287 said twice. V287 owns WHICH controls may carry a transparent contour - a
# roster, guarding the idiom from leaking onto a section box and killing its gold fillet. This
# owns which controls must SAY something about their contour at all, and its subject is the FILL.
# A rectangle painted black and left silent is correct here and always was: it is asking for the
# era's rule, and it has a box for the rule to sit on.
#
# The floor is what the sheet draws, not a taste: twenty-six of twenty-seven already authored it
# before this rule existed, so it charges nobody for a new idiom.
$v404Bad = @()
$v404Seen = 0
foreach ($f in $files) {
    foreach ($r404 in (Doc $f.FullName).SelectNodes("//rectangle[@color='#00000000']")) {
        $v404Seen++
        if (-not $r404.HasAttribute("strokeColor")) {
            $nm404 = $r404.GetAttribute("name")
            if ($nm404 -eq '') { $nm404 = "(unnamed) at left=$($r404.GetAttribute('left')) top=$($r404.GetAttribute('top'))" }
            $v404Bad += "$($f.Name) <rectangle name='$nm404'> fills transparent and authors no strokeColor - applyTheme hands it the era's rule, and a contour with no box behind it is what B136 drew over the whole sheet"
        }
    }
}
if ($v404Seen -lt 27) { Fail "V404 only $v404Seen transparent-fill rectangle(s) were measured, expected the 27 the sheet draws - this check is covering less than the sheet has (SPEC V209)" }
elseif ($v404Bad) { foreach ($b404 in $v404Bad) { Fail "V404 $b404" } }
else { Pass "V404 all $v404Seen transparent-fill rectangles declare their own contour - none is left to take the era's rule with no box behind it" }

# ---- V403: the BLOOD POOL dots are one block, centred under the title ------------------
# SPEC V403, I140f, user 2026-09-02 item 4. Twenty dots in two rows, and the owner asked for the
# bottom row to rise, the block to sit centred, and the rows to close up a little. All three are
# one statement: ONE pitch, and the block centred in the body below the title.
#
# NOTHING here is a literal top. The two rows are derived from the title's foot, the box height
# and the pitch, so the day the title or the box moves the dots move with them - and the day
# somebody nudges one row by hand, the other one no longer answers and this reddens.
#
# V240's normal arm was the only thing measuring this box before, and it charges a 15px head and
# a foot of at least 15: it accepted the bottom row anywhere from 35 to 88. That is why 53/88 -
# eighteen above the block and fifteen below it - sat there in plain sight (SPEC I140f).
$v403Bad = @()
$doc403 = Doc (Join-Path $dir "WoD20.11.lfm")
$box403 = @($doc403.SelectNodes("//layout[label/@text='BLOOD POOL']"))[0]
if ($null -eq $box403) { $v403Bad += "the BLOOD POOL box is gone from WoD20.11 - this rule measured nothing (SPEC V20, V209)" }
else {
    $bh403 = [int]$box403.GetAttribute("height")
    $ttl403 = $box403.SelectSingleNode("label[@text='BLOOD POOL']")
    $tb403 = [int]$ttl403.GetAttribute("top") + [int]$ttl403.GetAttribute("height")
    $rows403 = @{}
    $dh403 = 0
    foreach ($k403 in $box403.ChildNodes) {
        if ($k403.NodeType -ne 'Element' -or $k403.LocalName -ne 'imageCheckBox') { continue }
        if ($k403.GetAttribute("field") -notlike 'bloodPool_*') { continue }
        $kt403 = [int]$k403.GetAttribute("top")
        $rows403[$kt403] = ($rows403[$kt403] + 1)
        $dh403 = [int]$k403.GetAttribute("height")
    }
    $tops403 = @($rows403.Keys | Sort-Object)
    if ($tops403.Count -ne 2) { $v403Bad += "the dots sit on $($tops403.Count) row(s), expected 2 - twenty dots in two rows is the shape the box has drawn since it was written (SPEC V219, V209)" }
    elseif ($rows403[$tops403[0]] -ne $rows403[$tops403[1]]) { $v403Bad += "the two rows hold $($rows403[$tops403[0]]) and $($rows403[$tops403[1]]) dots - a block is two EQUAL rows, or the centring below is measuring a staircase" }
    else {
        $pitch403 = $tops403[1] - $tops403[0]
        $blk403 = $pitch403 + $dh403
        # The datum below is the INNER RULE of the filete and not the box edge (SPEC V403 as amended
        # 2026-09-04, T961, I146f): three of the four palettes draw ORN_IN px inward and the owner's
        # screenshot showed the second row sitting on that rule. ORN_IN is READ off WoD20.6 by V317
        # ($in317) - a 9 typed here would be its second owner (SPEC B70).
        $in403 = if ($in317.Success) { [int]$in317.Groups[1].Value } else { -1 }
        $want403 = $tb403 + [math]::Floor(($bh403 - $in403 - $tb403 - $blk403) / 2)
        if ($in403 -lt 0) { $v403Bad += "ORN_IN could not be read out of WoD20.6 - the datum of the centring is unmeasurable (SPEC V403, B70, V209)" }
        elseif ($pitch403 -ne 30) {
            $v403Bad += "the two rows are ${pitch403}px apart and the owner set the pitch at 30 - it was 35 until T924, and 'a little less space between them' is the whole of the ask (SPEC I140f)"
        }
        elseif ($tops403[0] -ne $want403) {
            $v403Bad += "the block opens at $($tops403[0]) in a ${bh403}px box whose title closes at $tb403 and whose inner rule sits ORN_IN=$in403 up from the edge - centred between the two a ${blk403}px block opens at $want403 (SPEC V403 as amended, I146f)"
        }
    }
}
if ($v403Bad) { foreach ($b403 in $v403Bad) { Fail "V403 $b403" } }
else { Pass "V403 the 20 BLOOD POOL dots are two rows on one pitch, centred between the title and the inner rule of the filete (ORN_IN=$in403)" }

# ---- V407: clanFamily is the NAMED exception to the picker's ? ------------------------
# SPEC V407, I140g, I140h, V365a, V333g, user 2026-09-02 item 5 - his own word, "e uma excecao".
#
# It cuts BOTH ways, the way $V365_EXEMPT and $BARE_Q333 already do: the button is gone from the
# XML and the name is gone from the bare-? roster, and either one coming back reddens. An
# exception that only forbids is a door (SPEC V328d).
#
# What the exception PAYS is the second leg, and it is why this one is cheap: with no ? leading
# it, dynclanFamily and edtClanFamily open where <edit field="dominator"> opens and close
# where it closes, so the rows of the DOMINATOR box share ONE left and ONE right and are the grid
# V224 asks for. The ? was the only reason they were not (SPEC I140g).
#
# ONE button again since T972 (SPEC V407 amended twice, V433a): T954 had split the line in two
# and the user undid it on 2026-09-05. The exception names the one button, and the two names of
# the split stay on the refusal roster so a ? cannot come back under them either.
$v407Bad = @()
$doc407 = Doc (Join-Path $dir "WoD20.11.lfm")
foreach ($q407 in @('btnQclanFamily', 'btnQclan', 'btnQfamily')) {
    if ($null -ne $doc407.SelectSingleNode("//button[@name='$q407']")) {
        $v407Bad += "$q407 is back on WoD20.11 - clan and family are the named exception to V365(a) and the exception cuts both ways (SPEC V407, V328d)"
    }
}
$dom407 = @($doc407.SelectNodes("//edit[@field='dominator']"))[0]
$pick407 = @($doc407.SelectNodes("//button[@name='dynclanFamily']"))[0]
$twin407 = @($doc407.SelectNodes("//edit[@name='edtClanFamily']"))[0]
if ($null -eq $dom407 -or $null -eq $pick407 -or $null -eq $twin407) {
    $v407Bad += "one of dominator / dynclanFamily / edtClanFamily is gone from WoD20.11 - the grid this exception pays for cannot be measured (SPEC V20, V209)"
} else {
    $dl407 = [int]$dom407.GetAttribute("left"); $dr407 = $dl407 + [int]$dom407.GetAttribute("width")
    foreach ($n407 in @($pick407, $twin407)) {
        $l407 = [int]$n407.GetAttribute("left"); $r407 = $l407 + [int]$n407.GetAttribute("width")
        if ($l407 -ne $dl407 -or $r407 -ne $dr407) {
            $v407Bad += "'$($n407.GetAttribute('name'))' spans $l407..$r407 and the dominator entry spans $dl407..$dr407 - with the ? gone the rows of DOMINATOR are one grid, which is what the exception buys (SPEC V224, I140g)"
        }
    }
}
if ($v407Bad) { foreach ($b407 in $v407Bad) { Fail "V407 $b407" } }
else { Pass "V407 dynclanFamily carries no ? and its entry shares both x edges with the dominator row - the exception cuts both ways and pays for itself in the V224 grid" }

# ---- V408: a list is covered by the module its picker names ---------------------------
# SPEC V408, B137, I140i, I140j, I140k, I140m, user 2026-09-02 item 6. Every picker on the sheet
# authors the pair itself - mfOpen(self, field, LIST, KIND) - and KIND is what descText turns into
# `require("desc<KIND>_<lang>.lua")`. Nothing was checking that the module it names holds the
# names the list offers, so 78 of them pointed at descNumina, which holds fifty OTHER names, and
# the player read `No description available` on a ritual the book describes (SPEC B137).
#
# V366 asked EXACTLY this question and asked it for `nature` alone, roster of exemptions and all -
# the right rule applied to one list of twelve. It KEEPS nature: that pair is authored with a
# template variable for the list, so it never reaches this sweep, and two owners for one fact is
# the no-op V20 forbids (SPEC B70).
#
# A kind with NO module on disk is not a failure here: descText pcalls the require and descFor
# falls to the sentence, which is V364a working, and `Road` and `Clan` live there on purpose.
# The roster, and it has TWO species on purpose - one of them is what keeps it from becoming the
# bin this rule was written to prevent (SPEC I140k, I140m).
#
#   NO TEXT - the book does not describe that name. It is a statement about the SOURCE.
#   DEBT    - the book DOES describe it and nobody has extracted it yet. It is a statement about
#             US, so it has to name an OPEN §T, and this check reads SPEC.md to confirm the §T is
#             still open. Close the task without the text and the line stops excusing anything.
#
# The roster opened with TWO lines, both DEBT, and both were PAID in the same round: T928
# extracted the fifty-two hedge rituals and T929 the twenty-six affiliations, so both lists are
# covered by descNumina like any other. That is what a debt naming a task is FOR - closing the
# task retires the line, instead of the line outliving the gap it was written for.
#
# It carried ONE line from T848 to T954, DEBT of the same shape, and T954 retired it without
# paying it: `Clan` stopped being a kind with no module when descClan_en.lua landed, but the
# button that opened it named the list `clanFamily` - clan + the twenty three revenant families -
# and descClan holds the sixty-one CLANS only, so the line excused the twenty three by NAME
# against T850. Since T954 the Ghoul tab has one button per list (SPEC I145a, V426a): the pair
# (clan, Clan) is covered 61 for 61 and needs no excuse, and (family, Family) has no module on
# disk yet, which is the V364a exemption two paragraphs up and is legitimate until T850 lands
# descFamily - the day it does, this sweep measures it like any other with nothing to excuse.
# That is precisely what Q65 was opened to get: two clean lines where one partial line stood.
#
# NAMES, and not a bare list, and this is what T848 asked for when it said the coverage rule is
# born with the missing ones NAMED, the shape of V366 and not a count (SPEC T848, V366c, B105).
# MEASURED 2026-09-03 before the field existed: with a bare `@{List; Task}` line on clanFamily,
# deleting the `Nosferatu` entry from BOTH halves of descClan left the WHOLE gate green - a debt
# written for twenty three families was excusing all eighty four names of the list. A per-list
# excuse is only honest when the whole list is owed, which is how the two 2026-09-02 lines were
# born and why nobody noticed. So a roster line excuses the names it lists and nothing else, and
# it has the two legs V366(b) has: a name here that the module DOES hold is a FAIL, because the
# line would be lying about text that exists, and that is what retires a line automatically the
# round the debt gets paid. A line with no Names is a FAIL too - an optional field with a silent
# fallback is the bin coming back through the door. What it must never become is a bin
# (SPEC I140m, V362b). Shape of a line: @{ List = '<list>'; Task = 'T<n>'; Names = @('...') }.
$V408_DEBT = @()
$v408Bad = @()
$v408Pairs = @{}
foreach ($f in $files) {
    $t408 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f.FullName))
    foreach ($m408 in [regex]::Matches($t408, "mfOpen\(self,\s*'[^']*',\s*'([^']*)',\s*'([^']*)'")) {
        $l408 = $m408.Groups[1].Value
        # a LIST named by a template variable cannot be resolved from source, and saying so is the
        # answer: nature and merit are those two, and V366 and V210 own them where they live.
        if ($l408 -like '*$(*') { continue }
        $v408Pairs["$l408|$($m408.Groups[2].Value)"] = $true
    }
}
$v408Seen = 0
$v408Debt = @{}
$v408Excused = @{}
foreach ($r408 in $V408_DEBT) {
    $v408Debt[$r408.List] = $r408.Task
    if ($null -eq $r408.Names -or @($r408.Names).Count -eq 0) {
        $v408Bad += "'$($r408.List)' is on the debt roster with no Names - a line that excuses a whole list is a line that excuses the next gap too, and the names are what T848 measured (SPEC V366c, I140m)"
        continue
    }
    $set408 = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
    foreach ($n408 in @($r408.Names)) { [void]$set408.Add($n408) }
    $v408Excused[$r408.List] = $set408
}
foreach ($k408 in ($v408Pairs.Keys | Sort-Object)) {
    $parts408 = $k408 -split '\|'
    $list408 = $parts408[0]; $kind408 = $parts408[1]
    if (-not $PICKER.ContainsKey($list408)) { $v408Bad += "a picker offers PICKER_LIST['$list408'] and the map has no such list - the roster and the map drifted (SPEC V211, V209)"; continue }
    $modPath408 = Join-Path $plugin "desc${kind408}_en.lua"
    if (-not (Test-Path -LiteralPath $modPath408)) { continue }
    if (-not $DESC_MARKER.ContainsKey($kind408)) { $v408Bad += "'$kind408' has a module on disk and no marker here - the sweep would skip it in silence (SPEC V209, V20)"; continue }
    $ent408 = DescEntries $modPath408 $DESC_MARKER[$kind408] 'en'
    if ($null -eq $ent408 -or @($ent408).Count -eq 0) { $v408Bad += "desc${kind408}_en.lua parsed to zero entries - the coverage below would be struck over nothing (SPEC V209, V20)"; continue }
    $have408 = @{}
    foreach ($e408 in $ent408) { $have408[$e408.Key] = $true }
    $miss408 = @(@($PICKER[$list408]) | Where-Object { $_ -ne '' -and -not $have408.ContainsKey($_) })
    # V433(e): the union is ONE list read by TWO modules, by pertinence - a family name is owed
    # to descFamily and everything else to the kind the button names (SPEC V433c, I149a).
    if ($list408 -eq 'clanFamily' -and $PICKER.ContainsKey('family')) {
        $famMod408 = Join-Path $plugin 'descFamily_en.lua'
        $haveF408 = @{}
        if ((Test-Path -LiteralPath $famMod408) -and $DESC_MARKER.ContainsKey('Family')) { $famEnt408 = DescEntries $famMod408 $DESC_MARKER['Family'] 'en'; foreach ($e408 in $famEnt408) { $haveF408[$e408.Key] = $true } }
        $famSet408 = @{}
        foreach ($n408 in @($PICKER['family'])) { $famSet408[$n408] = $true }
        $miss408 = @(@($PICKER[$list408]) | Where-Object { $_ -ne '' -and -not $(if ($famSet408.ContainsKey($_)) { $haveF408.ContainsKey($_) } else { $have408.ContainsKey($_) }) })
    }
    $v408Seen++
    # the excuse is by NAME: a name on the roster that the module now HOLDS is a line lying about
    # text that exists, and striking it is what retires the line the round the debt gets paid
    if ($v408Excused.ContainsKey($list408)) {
        foreach ($n408 in $v408Excused[$list408]) {
            if ($have408.ContainsKey($n408)) { $v408Bad += "'$list408' excuses '$n408' on the debt roster and desc${kind408} HOLDS it - the line outlived the gap it was written for (SPEC I140m, V362b)" }
            elseif (@($PICKER[$list408]) -notcontains $n408) { $v408Bad += "'$list408' excuses '$n408' on the debt roster and the list does not offer it - a stale name excuses nothing and hides the next gap (SPEC V20, I140k)" }
        }
        $miss408 = @($miss408 | Where-Object { -not $v408Excused[$list408].Contains($_) })
    }
    # the §T leg runs whether or not anything is left over: a debt that is doing its whole job is
    # exactly the debt that must still name an OPEN task, and hanging this off the leftovers is
    # how the leg would go quiet on the day the roster started working (SPEC V20, B7)
    if ($v408Debt.ContainsKey($list408)) {
        $owed408 = if ($v408Excused.ContainsKey($list408)) { $v408Excused[$list408].Count } else { $miss408.Count }
        $task408 = $v408Debt[$list408]
        $row408 = @(Select-String -LiteralPath (Join-Path $PSScriptRoot 'SPEC.md') -Pattern "^$task408\|" -Encoding utf8)
        if ($row408.Count -eq 0) { $v408Bad += "'$list408' is declared a DEBT against $task408 and SPEC.md has no such task - a debt with no creditor excuses everything (SPEC V20, I140k)" }
        elseif ($row408[0].Line -match "^$task408\|x\|") { $v408Bad += "'$list408' is declared a DEBT against $task408 and $task408 is CLOSED, with $owed408 name(s) still uncovered - the task that owes the text cannot be marked done while the text is missing (SPEC I140k, V209)" }
    }
    if ($miss408.Count -eq 0) { continue }
    $show408 = @($miss408 | Select-Object -First 3) -join ', '
    if (-not $v408Debt.ContainsKey($list408)) {
        $v408Bad += "PICKER_LIST['$list408'] offers $($miss408.Count) name(s) that desc${kind408} does not hold - e.g. $show408 - and the list is on no roster. The ? opens on 'No description available' for a name the book describes (SPEC B137)"
        continue
    }
    # a name the roster does NOT name is not excused by the roster line standing next to it
    $v408Bad += "PICKER_LIST['$list408'] offers $($miss408.Count) name(s) that desc${kind408} does not hold and the debt roster does not name - e.g. $show408. A debt excuses the names it lists and no others (SPEC V366c, B137)"
}
foreach ($r408 in $V408_DEBT) {
    if (-not $PICKER.ContainsKey($r408.List)) { $v408Bad += "'$($r408.List)' is on the debt roster and PICKER_LIST has no such list - a stale line excuses the next gap (SPEC V20)" }
}
if ($v408Seen -lt 6) { Fail "V408 only $v408Seen (list, kind) pair(s) with a module on disk were measured, expected at least 6 - the sweep broke and this rule would certify a sheet with no descriptions at all (SPEC V209, V20)" }
elseif ($v408Bad) { foreach ($b408 in $v408Bad) { Fail "V408 $b408" } }
else { Pass "V408 all $v408Seen (list, kind) pairs with a module on disk are covered by it, bar $($V408_DEBT.Count) declared debt(s), each naming an OPEN task" }

# ---- V406: the band has ONE definition, and the four rules read it --------------------
# SPEC V406, I140q, Q53, B70, B112. Not a geometry rule - a rule about who owns a sentence.
#
# What makes it worth a check rather than a comment: while the band sat at zero, a reader that
# still compared to the literal was INDISTINGUISHABLE from one that asked BandTop. It went quiet
# for as long as the number happened to be 0, and spoke only on the day somebody moved it - which
# is the shape of every no-op this file has ever grown (SPEC V20, B7).
#
# The four retired idioms are named as DEAD STRINGS. That is deliberate: a literal that proves an
# exception died stays in the gate rather than being deleted, because a rule with no literal left
# is a rule that can no longer fail (SPEC V376, V20).
$v406Bad = @()
$gate406 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $PSScriptRoot 'verify-hunters-hunted.ps1')))
$code406 = [regex]::Replace($gate406, '(?m)^\s*#.*$', '')
if ($code406 -notmatch '(?m)^function BandTop\(') { $v406Bad += "BandTop is gone - the band would go back to being four sentences that agree only while it sits at zero (SPEC B70, B112)" }
$calls406 = @([regex]::Matches($code406, '=\s*BandTop\s+[\$\(]'))
if ($calls406.Count -lt 7) { $v406Bad += "$($calls406.Count) rule(s) read BandTop, expected at least the 7 that partition a tab - V361, V267, V390, V401, V221, V225 and V280(d) (SPEC V406, I140q)" }
# The seven retired idioms are BUILT from their parts and never spelled out whole: this check
# reads the gate's OWN source, so a roster carrying the literal would find itself and redden on
# the day it was written. What identifies a reader is its variable and its operator.
$DEAD406 = @(@('ly361','-eq'), @('bt','-ne'), @('t401','-eq'), @('bt390','-eq'), @('_.GetAttribute("top")','-gt'))
foreach ($dead406 in $DEAD406) {
    $needle406 = '$' + $dead406[0] + ' ' + $dead406[1] + ' ' + '0'
    if ($code406.Contains($needle406)) { $v406Bad += "the retired idiom ``$needle406`` is back in the gate - it agrees with BandTop only while the band sits at zero, and says nothing at all the day it moves (SPEC V406, Q53)" }
}
# and the function is not a stub: the two tabs the owner moved really do answer 10
foreach ($pair406 in @(@('WoD20.7.lfm', 10), @('WoD20.11.lfm', 10))) {
    $sb406 = ContentRoot (Doc (Join-Path $dir $pair406[0]))
    $got406 = BandTop $sb406
    if ($got406 -ne $pair406[1]) { $v406Bad += "BandTop reads $got406 on $($pair406[0]) and the owner set that band at $($pair406[1]) - a definition that answers the wrong number is worse than four that disagree (SPEC V286a, I140p)" }
}
if ($v406Bad) { foreach ($b406 in $v406Bad) { Fail "V406 $b406" } }
else { Pass "V406 the band is defined once, in BandTop, and $($calls406.Count) rules read it - none of the four retired `top == 0` idioms is left" }

# ---- V399: the EXPERIENCE boxes carry ONE edit and centre it ---------------------------
# SPEC V399, I139h, I139k, user 2026-09-02 item 6. The owner asked for the "Current Experience"
# name to leave the box so the typed part gets the room, and for what is left to be centred. Both
# halves are measured here because NOTHING else measures either: V240's normal arm charges 15
# above the title and at least 15 under the last child, which the edit satisfies anywhere between
# 35 and 90, and V240's centring arm is NAMED and reaches two boxes of WoD20.7 only.
#
# The body opens at the FOOT OF THE TITLE and not at the box margin. Main is 105px tall: centring
# from 20 would put the edit 15px under its own title, and V280(a) charges 20 there. Derived from
# the title node, so a box whose title grows moves its number with it.
#
# The odd pixel falls BELOW, which is the rule V240's own centring arm already uses - written as
# one expected `top` and not as two gaps compared loosely, so a slack of one cannot accept the odd
# pixel on either side and stop measuring which.
#
# Progress (WoD20.9) is NOT here and that is not an oversight: it draws three numbers with three
# labels and is the full VIEW of I11. This rule is about the boxes that show the balance alone.
$XP_ONE_BOX = @(
    [pscustomobject]@{ F = 'WoD20.1.lfm';  E = 'edtCurrentXPMain' }
    [pscustomobject]@{ F = 'WoD20.11.lfm'; E = 'edtCurrentXPGhoul' }
    [pscustomobject]@{ F = 'WoD20.7.lfm';  E = 'edtCurrentXPNumina' }
)
$v399Bad = @()
foreach ($row399 in $XP_ONE_BOX) {
    $doc399 = Doc (Join-Path $dir $row399.F)
    $ed399 = @($doc399.SelectNodes("//edit[@name='$($row399.E)']"))[0]
    if ($null -eq $ed399) { $v399Bad += "$($row399.E) is not on $($row399.F) - the roster outlived the widget, which is the orphan of B111 one floor up (SPEC V20, V209)"; continue }
    $box399 = $ed399.ParentNode
    $bh399 = -1
    if (-not [int]::TryParse($box399.GetAttribute("height"), [ref]$bh399)) { $v399Bad += "the box holding $($row399.E) on $($row399.F) carries no height - the centring would be struck over nothing (SPEC V209)"; continue }
    $lbls399 = @($box399.SelectNodes("label"))
    if ($lbls399.Count -ne 1) { $v399Bad += "the box holding $($row399.E) on $($row399.F) carries $($lbls399.Count) label(s), expected 1 - the title. The 'Current Experience' name is what the owner asked out of these boxes (SPEC I139h)" ; continue }
    $eds399 = @($box399.SelectNodes("edit"))
    if ($eds399.Count -ne 1) { $v399Bad += "the box holding $($row399.E) on $($row399.F) carries $($eds399.Count) edit(s), expected 1 - a second number here is a second owner of a computed value (SPEC V1, I11)"; continue }
    $tb399 = [int]$lbls399[0].GetAttribute("top") + [int]$lbls399[0].GetAttribute("height")
    $eh399 = [int]$ed399.GetAttribute("height")
    # The datum below is the INNER RULE of the filete, the same one V403 reads for the blood pool
    # (SPEC V399 as amended 2026-09-04, T965, Q67): ORN_IN is READ off WoD20.6 by V317 ($in317)
    # and never typed here (SPEC B70).
    $in399 = if ($in317.Success) { [int]$in317.Groups[1].Value } else { -1 }
    if ($in399 -lt 0) { $v399Bad += "ORN_IN could not be read out of WoD20.6 - the datum of the centring is unmeasurable (SPEC V399, B70, V209)"; continue }
    $want399 = $tb399 + [math]::Floor(($bh399 - $in399 - $tb399 - $eh399) / 2)
    $got399 = [int]$ed399.GetAttribute("top")
    if ($got399 -ne $want399) {
        $v399Bad += "$($row399.E) opens at top=$got399 in a ${bh399}px box whose title closes at $tb399 and whose inner rule sits ORN_IN=$in399 up from the edge - centred between the two it opens at $want399 (SPEC V399 as amended, Q67)"
    }
}
if ($XP_ONE_BOX.Count -lt 2) { Fail "V399 the roster names $($XP_ONE_BOX.Count) box(es) - a centring rule over one box is true of every box ever drawn (SPEC V20, B7)" }
elseif ($v399Bad) { foreach ($b399 in $v399Bad) { Fail "V399 $b399" } }
else { Pass "V399 the $($XP_ONE_BOX.Count) EXPERIENCE boxes each carry a title and ONE edit, with the number centred between the title and the inner rule of the filete" }

# ---- V390: the COLUMN boxes of Numina and Ghoul are one width -------------------------
# SPEC V390, I134a, I134h, I139n, B134, B112. The user asked for every box of the two rearranged
# tabs to take the width of PSYCHIC PHENOMENA, and the number is READ off that box - a literal
# here would be its second owner and the first to age.
#
# This rule existed as PROSE ONLY until T920, which is B134: the sentence "nunca um literal no
# check" asserts a check, and there was none - `PSYCHIC PHENOMENA` appeared three times in this
# file and not one of them swept a width. A rule nobody can mutate is a rule nobody can accept.
#
# The SUBJECT is the COLUMN boxes and not the band, and that is the 2026-09-02 amendment rather
# than a pile of exceptions. DOMINATOR and BLOOD POOL were never really exceptions - they were
# BAND - and once QUINTESSENCE and TRUE FAITH shrank to their content so EXPERIENCE could stand
# beside them, a named list would have held four exceptions over three measured boxes per tab.
# The partition is the one V361 already uses on both tabs and needs no second shape: a direct
# <layout> child of the scrollBox at top=0 is BAND, every other box is COLUMN. The band has its
# own ruler and it is V361, which freezes each of those rects by name (SPEC V361a, V328d).
$v390Bad = @()
$doc390 = Doc (Join-Path $dir "WoD20.7.lfm")
$ref390 = @($doc390.SelectNodes("//layout[label/@text='PSYCHIC PHENOMENA']"))[0]
if ($null -eq $ref390) { $v390Bad += "the PSYCHIC PHENOMENA box is gone from WoD20.7 - the one width every column is held to cannot be read, and a literal here would be its second owner (SPEC V209, B112)" }
else {
    $w390 = [int]$ref390.GetAttribute("width")
    $seen390 = 0
    foreach ($fn390 in @("WoD20.7.lfm", "WoD20.11.lfm")) {
        $d390 = Doc (Join-Path $dir $fn390)
        # the BAND: direct children of the scrollBox at top=0. Read once per file, by identity.
        $sb390 = $d390.SelectSingleNode("//scrollBox")
        $bandTop390 = BandTop $sb390
        $band390 = @{}
        foreach ($n390 in $d390.SelectNodes("//scrollBox/layout")) {
            $bt390 = -1
            if (-not [int]::TryParse($n390.GetAttribute("top"), [ref]$bt390)) { continue }
            if ($bt390 -eq $bandTop390) { $band390[$n390.GetHashCode()] = $true }
        }
        foreach ($bx390 in $d390.SelectNodes("//layout[@width]")) {
            if ($band390.ContainsKey($bx390.GetHashCode())) { continue }
            $back390 = $false
            foreach ($k390 in $bx390.ChildNodes) {
                if ($k390.NodeType -eq 'Element' -and $k390.LocalName -eq 'rectangle' -and $k390.GetAttribute("align") -eq 'client' -and $k390.GetAttribute("color") -eq 'black') { $back390 = $true; break }
            }
            if (-not $back390) { continue }
            $bw390 = -1
            if (-not [int]::TryParse($bx390.GetAttribute("width"), [ref]$bw390)) { continue }
            $seen390++
            if ($bw390 -ne $w390) {
                $ttl390 = '(untitled)'
                $lb390 = $bx390.SelectSingleNode("label[@text]")
                if ($null -ne $lb390) { $ttl390 = $lb390.GetAttribute("text") }
                $v390Bad += "$fn390 '$ttl390' is ${bw390}px and PSYCHIC PHENOMENA sets ${w390} - every COLUMN box of these two tabs takes that one width (SPEC I134a, I139n)"
            }
        }
    }
    if ($seen390 -lt 8) { $v390Bad += "only $seen390 column box(es) were measured across the two tabs, expected at least the 8 they draw - this rule is covering less than the sheet has (SPEC V209, V20)" }
}
if ($v390Bad) { foreach ($b390 in $v390Bad) { Fail "V390 $b390" } }
else { Pass "V390 every COLUMN box of Numina and Ghoul is the width PSYCHIC PHENOMENA sets - the band is V361's and carries its own rects" }

# ---- V281: what a tab BUTTON is spaced by, HORIZONTALLY (SPEC I73, V281, T616) ---------
# The same request as V280, one level out: 30 in from the end of the bar it opens, and the 4
# between two buttons left alone because it was the one number the four bars already agreed on.
#
# The VERTICAL half - 15 above and below each pill - left for V299 in the 107th round, when the
# user took it to 5 and the four bars went 60 -> 40 (SPEC I76b). Nothing here loosened: the leg
# moved whole, and what stayed is the axis the request did not touch.
#
# Leg (c) is NOT the sentence V281 was written with. "The last button closes 30 before the end"
# describes a bar whose buttons fill it edge to edge, and none of the four is: a pill is as wide
# as its own text (SPEC V228), so the last one closes wherever the words stop - measured on
# 2026-08-24, 450px short on vampStrip, 949 on numStrip, 1191 on hedgeStrip. Asking for slack of
# exactly 30 would redden three bars that are right. What it becomes instead (user decision, the
# 104th round) is the thing that sentence was protecting: the bar FITS its buttons - the last one
# closes inside it with the 30 margin still to spare, so adding a tab without widening the bar
# reddens here. tabStrip authors no width at all - it is align-driven and the XML reads 0 - so it
# is counted OUT of (c) and the count is printed, rather than skipped in a silence that would read
# as coverage (SPEC B7, V209).
$v281Bad = @()
$v281Pills = 0; $v281Bars = 0; $v281Fit = 0; $v281NoWidth = 0
foreach ($f in $files) {
    foreach ($bar in (Doc $f.FullName).SelectNodes("//layout[rectangle[starts-with(@name,'btnTab')]]")) {
        $barW = 0; $barH = 0
        if (-not [int]::TryParse($bar.GetAttribute("height"), [ref]$barH)) { continue }
        [void][int]::TryParse($bar.GetAttribute("width"), [ref]$barW)
        $pills = @($bar.SelectNodes("rectangle[starts-with(@name,'btnTab')]") | ForEach-Object {
            $pl = 0; $pt = 0; $pw = 0; $ph = 0
            [void][int]::TryParse($_.GetAttribute("left"), [ref]$pl); [void][int]::TryParse($_.GetAttribute("top"), [ref]$pt)
            [void][int]::TryParse($_.GetAttribute("width"), [ref]$pw); [void][int]::TryParse($_.GetAttribute("height"), [ref]$ph)
            [pscustomobject]@{ L = $pl; T = $pt; W = $pw; H = $ph; N = $_.GetAttribute("name") } } | Sort-Object L)
        if ($pills.Count -eq 0) { continue }
        $v281Bars++; $v281Pills += $pills.Count
        $bn = $bar.GetAttribute("name")

        # (b) the first button opens 30 in, the horizontal half of the same margin.
        if ($pills[0].L -ne 30) { $v281Bad += "$($f.Name) $bn opens its first button at left=$($pills[0].L) - the bar hands over 30 before the first pill (SPEC I73, V281b)" }
        # (c) the bar FITS: see the note above for why this is not "closes at exactly 30".
        if ($barW -le 0) { $v281NoWidth++ }
        else {
            $v281Fit++
            $end = $pills[-1].L + $pills[-1].W
            if (($end + 30) -gt $barW) { $v281Bad += "$($f.Name) $bn closes its last button at $end in a bar $($barW)px wide - the bar has to hold its buttons and the 30 margin after them, or a tab added tomorrow runs off the end (SPEC V281c, V228)" }
        }
        # (d) 4 between two buttons - frozen on purpose, and frozen is not unmeasured.
        for ($i = 1; $i -lt $pills.Count; $i++) {
            $pg = $pills[$i].L - ($pills[$i - 1].L + $pills[$i - 1].W)
            if ($pg -ne 4) { $v281Bad += "$($f.Name) $($pills[$i].N) sits $($pg)px from the button before it - the gap between two pills is 4 and T616 left it alone deliberately (SPEC I73, V281d)" }
        }
    }
}
if ($v281Bars -ne 1 -or $v281Pills -ne 11) { Fail "V281 $v281Pills button(s) were read across $v281Bars bar(s), expected the 11 on the one bar I73 measures - this check is covering less than the sheet has (SPEC V209, I73)" }
elseif ($v281Fit -ne 0) { Fail "V281 (c) measured $v281Fit bar(s) for fit - the one bar left after T830 is align-driven and authors NO width, so a bar with one is a bar that stopped sizing itself from its buttons (SPEC I73, V281c, T830)" }
elseif ($v281Bad) { foreach ($b in $v281Bad) { Fail "V281 $b" } }
else { Pass "V281 all $v281Pills tab buttons open 30 in and sit 4 apart; $v281Fit of $v281Bars bars hold their buttons plus the margin ($v281NoWidth author no width and is align-driven)" }

# ---- V299: the bar is 40 tall and its panes came down with it (SPEC I76b, V299) --------
# This is V281a, moved out and re-numbered because the number changed: 15 above and below a
# 30px pill became 5, so the four bars went 60 -> 40 (user 2026-08-25, "diminuir bastante a
# altura"). The old leg is gone rather than loosened, for V298's reason: two rules over one
# gap is B7. The HORIZONTAL half of I73 - 30 in from the end, 4 between two pills - did not
# move and stays in V281, which is why that check keeps three legs and loses one.
#
# (a) is a RELATION, not a literal: the bar measures twice the pill's top plus the pill. A
# bar shrunk with one pill left behind reddens, and so does a pill moved inside a bar that
# stayed - both sides of one equation, which is what V222 asks of a relation.
#
# (b) is LITERAL and says so. The three ABSOLUTE bars carry their panes; tabStrip is
# align="top" and the scrollBox under it reflows on its own, which is what I74a bought when
# it zeroed the top gap. Nothing else would catch a bar that shrank while its panes stayed:
# V262a asks only that a pane open at or below the end of the bar, so a 20px hole reads
# GREEN there. The two numbers are I73's, frozen on purpose, and this is their only reader
# in the gate. V293 could measure hedgeStrip as a relation because that bar has content on
# both sides of it; numStrip and vampStrip have one side, and inventing a relation where the
# second side does not exist is how B64 was born.
$v299Bad = @()
$v299Bars = 0; $v299Pills = 0
$v299Panes = @{
    'numStrip'   = @{ File = 'WoD20.7.lfm';  Gap = 12; Panes = @('tabHedge', 'tabPsychic', 'tabFaith') }
    'hedgeStrip' = @{ File = 'WoD20.7.lfm';  Gap = 12; Panes = @('tabHedgePaths', 'tabHedgeRituals') }
}
$v299Seen = @{}
foreach ($f in $files) {
    $doc299 = Doc $f.FullName
    foreach ($bar in $doc299.SelectNodes("//layout[rectangle[starts-with(@name,'btnTab')]]")) {
        $barH = 0
        if (-not [int]::TryParse($bar.GetAttribute("height"), [ref]$barH)) { continue }
        $pills = @($bar.SelectNodes("rectangle[starts-with(@name,'btnTab')]") | ForEach-Object {
            $pt = 0; $ph = 0
            [void][int]::TryParse($_.GetAttribute("top"), [ref]$pt); [void][int]::TryParse($_.GetAttribute("height"), [ref]$ph)
            [pscustomobject]@{ T = $pt; H = $ph; N = $_.GetAttribute("name") } })
        if ($pills.Count -eq 0) { continue }
        $v299Bars++; $v299Pills += $pills.Count
        $bn = $bar.GetAttribute("name")

        # (a) height = 2 * pill top + pill height, and every pill in the bar agrees on both.
        foreach ($p in $pills) {
            if ((2 * $p.T + $p.H) -ne $barH) {
                $v299Bad += "$($f.Name) $($p.N) sits $($p.T) from the top of a $($barH)px bar around a $($p.H)px pill - the bar is twice the pill's top plus the pill, which is 40 around the 30 the house draws (SPEC I76b, V299a)"
            }
            if (($barH - $p.T - $p.H) -ne $p.T) {
                $v299Bad += "$($f.Name) $($p.N) leaves $($barH - $p.T - $p.H) under it and $($p.T) over it - a tab button breathes the same on both (SPEC I76b, V299a)"
            }
        }

        # (b) the panes of an ABSOLUTE bar open the frozen gap under it.
        if ($v299Panes.ContainsKey($bn)) {
            $spec = $v299Panes[$bn]
            $v299Seen[$bn] = $true
            $barT = 0
            if (-not [int]::TryParse($bar.GetAttribute("top"), [ref]$barT)) {
                $v299Bad += "$($f.Name) $bn authors no top - an absolute bar whose own position cannot be read cannot be measured against the panes it carries (SPEC V209, V299b)"
            } else {
                foreach ($pn in $spec.Panes) {
                    $pane = $doc299.SelectSingleNode("//layout[@name='$pn']")
                    if ($pane -eq $null) { $v299Bad += "$($f.Name) pane '$pn' was not found under $bn - V299b is measuring nothing for it (SPEC V209)"; continue }
                    $paneT = 0
                    if (-not [int]::TryParse($pane.GetAttribute("top"), [ref]$paneT)) { $v299Bad += "$($f.Name) pane '$pn' authors no top (SPEC V209, V299b)"; continue }
                    $g299 = $paneT - ($barT + $barH)
                    if ($g299 -ne $spec.Gap) {
                        $v299Bad += "$($f.Name) '$pn' opens $($g299)px under $bn, which ends at $($barT + $barH) - I73 froze that gap at $($spec.Gap), and a bar that shrank without its panes leaves exactly this hole with V262a still green (SPEC I76b, V299b)"
                    }
                }
            }
        }
    }
}
if ($v299Bars -ne 1 -or $v299Pills -ne 11) { Fail "V299 $v299Pills button(s) were read across $v299Bars bar(s), expected the 11 on the one bar I73 measures - this check is covering less than the sheet has (SPEC V209, I73)" }
elseif ($v299Seen.Count -ne 0) { Fail "V299 (b) reached $($v299Seen.Count) absolute bar(s) - the bars with panes under them were the SUB-tab ones, vampStrip went in T832 and the last two in T830, and the one bar left is align-driven with no pane of its own (SPEC I76b, I117, T830)" }
elseif ($v299Bad) { foreach ($b in $v299Bad) { Fail "V299 $b" } }
else { Pass "V299 all $v299Pills tab buttons breathe the same above and below across $v299Bars bar, and there is no absolute bar left with a pane under it to open a gap (SPEC I117, T830)" }


# ---- V300: the caption lives and dies with the pane it explains, RETIRED (SPEC I76c, I117) --
# note<X> was a caption that belonged to pane tab<X> and could NOT live inside it: the Hedge panes
# had no free band, so the 91st round parked the affinity note on hedgeStrip and renderSubTabs
# hid it in the same line that lit the pill, so the two could not come apart. T830 gave the column
# a foot and the note moved INSIDE the box it explains - which is where a caption always belonged,
# and the reason this rule existed was that it could not get there.
#
# With no pane to pair against and no renderSubTabs to hide it in step with, all three legs lost
# their subject. What still charges the note: V88, which asks for its exact words on this tab.
$v300Bad = @()
foreach ($f300 in $files) {
    foreach ($n300 in (Doc $f300.FullName).SelectNodes("//*[starts-with(@name,'note')]")) {
        $nn300 = $n300.GetAttribute("name")
        if ($nn300 -eq 'noteHedgePaths') { continue }
        $v300Bad += "$($f300.Name) declares '$nn300' - note<X> paired a caption with a PANE, T830 took the last pane, and a caption named for one is named for something that does not exist (SPEC I76c, I117, B98)"
    }
}
# The one note there is has to be never on a BAR and always ATTACHED to the list it captions,
# which is the whole of what the retirement bought. WHERE it attaches moved once more in T898:
# the user asked for it OUTSIDE the box and directly under it (SPEC I134d), because inside the
# foot it read as a twenty-first row of a list of twenty - and the 25px of foot it was costing
# is what made that column 586 against the 561 of PSYCHIC PHENOMENA beside it.
#
# So the leg stops asking WHOSE CHILD the note is and asks what that was ever a proxy for: does
# the caption sit on the list it describes. It reads GEOMETRY now - directly under the box and
# inside its x span - which is what a caption standing over the wrong list actually breaks.
# Parentage only answered the question while the answer was "inside".
$doc300  = Doc (Join-Path $dir 'WoD20.7.lfm')
$note300 = $doc300.SelectSingleNode("//label[@name='noteHedgePaths']")
$box300  = $doc300.SelectSingleNode("//scrollBox/layout[label/@text='HEDGE MAGIC PATHS']")
if ($null -eq $note300) { $v300Bad += "the affinity note is gone from WoD20.7 - V88 asks for its words and this leg asks where it stands, and neither has anything to read (SPEC V209, V88)" }
elseif ($null -eq $box300) { $v300Bad += "the HEDGE MAGIC PATHS box is gone from WoD20.7 - the caption is measured against the list it captions and there is no list (SPEC V209, I117)" }
elseif ($null -ne $box300.SelectSingleNode("label[@name='noteHedgePaths']")) {
    $v300Bad += "the affinity note is back INSIDE the HEDGE MAGIC PATHS box - the user asked for it under the box and outside it, and in the foot it reads as one more row of the list (SPEC I134d, T898)"
} else {
    $bL300 = [int]$box300.GetAttribute('left');  $bT300 = [int]$box300.GetAttribute('top')
    $bW300 = [int]$box300.GetAttribute('width'); $bH300 = [int]$box300.GetAttribute('height')
    $nL300 = [int]$note300.GetAttribute('left');  $nT300 = [int]$note300.GetAttribute('top')
    $nW300 = [int]$note300.GetAttribute('width')
    if ($nT300 -lt ($bT300 + $bH300)) { $v300Bad += "the affinity note opens at y=$nT300 and the HEDGE MAGIC PATHS box closes at $($bT300 + $bH300) - a caption that overlaps the list it captions is not under it (SPEC I134d)" }
    elseif ($nT300 -gt ($bT300 + $bH300 + 20)) { $v300Bad += "the affinity note opens at y=$nT300, $($nT300 - $bT300 - $bH300)px under the box it explains - a caption floating that far off belongs to neither box (SPEC I134d, V298)" }
    if ($nL300 -lt $bL300 -or ($nL300 + $nW300) -gt ($bL300 + $bW300)) { $v300Bad += "the affinity note runs $nL300..$($nL300 + $nW300) and its box runs $bL300..$($bL300 + $bW300) - a caption outside the x span of its list stands over the column beside it (SPEC I134d, V88)" }
}
if ($v300Bad) { foreach ($b in $v300Bad) { Fail "V300 $b" } }
else { Pass "V300 retired with the panes in T830 - the one caption on the sheet stands directly under the box it explains and inside its x span, and no note<X> is left pointing at a pane" }

# ---- V301: the bar wears no box, and the marker is a rule (SPEC I77a, I77b) -----------
# The 108th round took the pill away. Eleven gold outlines side by side divided the eye evenly
# and the OPEN tab was told apart by its fill alone - the weakest signal on the band. So the
# button keeps every number it had and loses its contour, and the marker keeps every number it
# had and becomes a foot rule: transparent fill, an authored contour, sides="bottom". The 3px
# THEME_STROKE applyTheme already writes lands as a 3px rule in the era's own accent, which is
# why no palette was touched for any of this (SPEC V53, V67).
#
# (b) is the leg with teeth. The three attributes are ONE state: a transparent fill with no
# sides draws NOTHING, in every era, and the sheet opens with no tab looking open. That is the
# silent and total failure V229 was written to stop, inherited here now that the pill it used
# to measure is gone.
$v301Bad = @()
$pairs301 = 0
foreach ($pr301 in @(,@("WoD20th.lfm", "tabStrip"))) {
    $st301 = (Doc (Join-Path $dir $pr301[0])).SelectSingleNode("//layout[@name='$($pr301[1])']")
    if ($null -eq $st301) { $v301Bad += "$($pr301[1]) is gone from $($pr301[0]) - the bar it names has no buttons to measure (SPEC I32, I58, V209)"; continue }
    foreach ($on301 in $st301.SelectNodes("rectangle[starts-with(@name,'tabOn')]")) {
        $nm301 = $on301.GetAttribute("name")
        $sfx301 = $nm301.Substring(5)
        $btn301 = $st301.SelectSingleNode("rectangle[@name='btnTab$sfx301']")
        if ($null -eq $btn301) { $v301Bad += "'$nm301' has no 'btnTab$sfx301' beside it - the PAIR is what (c) measures (SPEC I32, V209)"; continue }
        $pairs301++

        # (a) the button is a click target and nothing else.
        if ($btn301.GetAttribute("color") -ne '#00000000') {
            $v301Bad += "btnTab$sfx301 is filled '$($btn301.GetAttribute('color'))' - the button is the target of a click and paints nothing (SPEC I77a)"
        }
        if ($btn301.GetAttribute("strokeColor") -ne '#00000000') {
            $v301Bad += "btnTab$sfx301 authors strokeColor='$($btn301.GetAttribute('strokeColor'))' - the box is what the 108th round removed, and applyTheme hands a 3px rule to every contour it knows (SPEC I77a, V67)"
        }

        # (b) LEFT for V304 in the 109th round. The marker stopped drawing anything of its own -
        # it carries a path now (SPEC I78a) - so what used to be measured here is measured there,
        # in the shape the marker has. What stays HERE is the button and the pair.

        # (c) the pair is one control drawn twice: same four numbers, so the rule is exactly as
        # wide as the word it underlines. V232a already welds the width on tabStrip; this is the
        # same weld on four bars and both axes - measuring one axis is how B52 cost a round.
        foreach ($a301 in @('left', 'top', 'width', 'height')) {
            $va301 = $on301.GetAttribute($a301)
            $vb301 = $btn301.GetAttribute($a301)
            if ($va301 -ne $vb301) {
                $v301Bad += "$nm301 has $a301='$va301' and btnTab$sfx301 has $a301='$vb301' - the rule and the word it marks have come apart (SPEC I77b, V301c)"
            }
        }
    }
}
if ($pairs301 -lt 11) { Fail "V301 only $pairs301 button/marker pair(s) were read, expected 11 - this check is covering less than the one bar holds (SPEC V209)" }
elseif ($v301Bad) { foreach ($b in $v301Bad) { Fail "V301 $b" } }
else { Pass "V301 all $pairs301 tab buttons author no contour and sit on the same four numbers as the marker they pair with - no box on the bar, and the rule is as wide as the word" }

# ---- V305: the sub-tab says its state in WEIGHT and in COLOUR, RETIRED (SPEC I78c, I117) ----
# lbl<X> was the sub-tab's word CLOSED - italic, in the theme's text colour - and lblOn<X> the same
# word OPEN, roman and in the era's accent: two labels and not one restyled, because fontColor
# belongs to applyTheme and a Lua write there is undone on the next repaint (SPEC V57). The state
# rode on `visible`, lit in renderSubTabs' own loop so word, pill and pane could not come apart.
#
# T830 took the last sub-tab (SPEC I117): SUB_TABS is empty, renderSubTabs is deleted, and there is
# no word on the sheet that has an OPEN and a CLOSED state. The eleven top-bar words keep their own
# pair and their own ruler, which is V307's and has always been (SPEC I78c, V307).
#
# The guard is the retirement's own: a lbl/lblOn pair on a SUB-bar would be state with nothing
# painting it, since the painter it depended on is gone (SPEC B55, B98).
$v305Bad = @()
if (LuaFn $rootTxt 'renderSubTabs') { $v305Bad += "renderSubTabs is back - it is what lit lblOn<X> in the same line as the pill, and a painter for words nothing declares is dead work (SPEC I78c, B55)" }
foreach ($f305 in $files) {
    if ($f305.Name -eq 'WoD20th.lfm') { continue }
    foreach ($l305 in (Doc $f305.FullName).SelectNodes("//*[starts-with(@name,'lblOn')]")) {
        $v305Bad += "$($f305.Name) declares '$($l305.GetAttribute('name'))' - lblOn<X> is the OPEN half of a SUB-tab word, and T830 took the last sub-tab off the sheet (SPEC I78c, I117, B98)"
    }
}
# The top bar keeps its pair, and it has to still be there or the retirement above is describing a
# sheet with no words at all (SPEC V209).
$top305 = @((Doc (Join-Path $dir 'WoD20th.lfm')).SelectNodes("//layout[@name='tabStrip']//label")).Count
if ($top305 -ne 11) { $v305Bad += "the top bar carries $top305 word(s), expected 11 - MEASURED 2026-09-01, that bar never carried the lbl/lblOn PAIR at all: it draws one plain label inside each button and lights the pill instead, so the two-label shape this rule held was the sub-tab's alone (SPEC I78c, V307)" }
if ($v305Bad) { foreach ($b in $v305Bad) { Fail "V305 $b" } }
else { Pass "V305 retired with the last sub-tab strip in T830 - the eleven top-bar words are single labels under their own pill and V307 measures them, and no lblOn<X> is left anywhere for a painter that no longer exists" }

# ---- V307: the line work runs where the LEVEL says (SPEC I79, I80) --------------------
# Amended in the 111th round, not renumbered. It was born measuring the 19 buttons as ONE
# population, and the user treats the two levels as different things: the 110th round's drop was
# asked for the ELEVEN of the top bar, and it reached the eight sub-tabs too (B69). A single
# ruler over populations the user keeps apart is a product decision written as geometry.
#
# (a) is the relation the 110th round bought: the word ends, then 3px, then the drawing - with
# the three constants READ out of WoD20.6 and never spelled here, so moving either side reddens.
#
# (b) is the OPPOSITE, and deliberately so: on a sub-bar the label covers the whole button and
# the short rule runs inside it. That is the state the user saw on screen and approved in the
# 109th round. The leg measures no clearance - it exists so that undoing this takes a REQUEST,
# and so the next reader does not "fix" an approved drawing back into the shape B69 produced.
$v307Bad = @()
$top307 = 0
$sub307 = 0
$mk307 = [regex]::Match($hh6, '(?m)^\s*local ORN_MARK\s*=\s*([\d.]+);')
$mk2_307 = [regex]::Match($hh6, '(?m)^\s*local ORN_MARK2\s*=\s*([\d.]+);')
$ry307 = [regex]::Match($hh6, '(?m)^\s*local ORN_MARK_RY\s*=\s*([\d.]+);')
$sm307 = [regex]::Match($hh6, '(?m)^\s*local ORN_SUB_MARK\s*=\s*([\d.]+);')
if (-not ($mk307.Success -and $mk2_307.Success -and $ry307.Success -and $sm307.Success)) {
    Fail "V307 ORN_MARK / ORN_MARK2 / ORN_MARK_RY / ORN_SUB_MARK could not all be read out of WoD20.6 - the Lua side of these relations is unreadable, so the legs are no-ops (SPEC V209, V20)"
}
else {
    $artTop307 = [Math]::Max([double]$mk2_307.Groups[1].Value, [double]$mk307.Groups[1].Value + [double]$ry307.Groups[1].Value)
    # 3 until the 112th round, when the user asked the drawing closer to the word (SPEC I81). It
    # is measured on the label BOX, and the gate does not know the height of a GLYPH - PX_PER_CHAR
    # measures width and nothing measures height (SPEC R102) - so at 1 the four eras, on four
    # different fonts, are what decide whether it touches. That is a screen question, not a static
    # one, and T681 is where it gets answered.
    $clear307 = 1
    foreach ($pr307 in @(,@("WoD20th.lfm", "tabStrip"))) {
        $st307 = (Doc (Join-Path $dir $pr307[0])).SelectSingleNode("//layout[@name='$($pr307[1])']")
        if ($null -eq $st307) { $v307Bad += "$($pr307[1]) is gone from $($pr307[0]) (SPEC I32, I58, V209)"; continue }
        $isTop307 = ($pr307[1] -eq 'tabStrip')
        foreach ($b307 in $st307.SelectNodes("rectangle[starts-with(@name,'btnTab')]")) {
            $bn307 = $b307.GetAttribute("name")
            $bh307 = [double]$b307.GetAttribute("height")
            $seen307 = @()
            if ($isTop307) { $top307++ } else { $sub307++ }
            foreach ($l307 in $b307.SelectNodes("label")) {
                $lt307 = [double]$l307.GetAttribute("top")
                $lh307 = [double]$l307.GetAttribute("height")
                $seen307 += "$lt307/$lh307"
                if ($isTop307) {
                    # (a) the word ends, the clearance, then the drawing.
                    if (($lt307 + $lh307) -gt ($bh307 - $artTop307 - $clear307)) {
                        $v307Bad += "$bn307's word ends at $($lt307 + $lh307) and the line work starts at $($bh307 - $artTop307) - on the TOP bar the drawing runs below the word (SPEC I79, V307a, B68)"
                    }
                }
                else {
                    # (b) the approved state: the word covers the button, the short rule runs in it.
                    if ($lh307 -ne $bh307 -or $lt307 -ne 0) {
                        $v307Bad += "$bn307's word is $($lh307)px at top $lt307 inside a $($bh307)px button - on a SUB-bar the word covers the button, which is the state the user approved in the 109th round; shrinking it here is B69 coming back (SPEC I80b, V307b)"
                    }
                    if (($bh307 - [double]$sm307.Groups[1].Value) -ge ($lt307 + $lh307)) {
                        $v307Bad += "$bn307's short rule runs at $($bh307 - [double]$sm307.Groups[1].Value), at or below the word's box ending at $($lt307 + $lh307) - the sub-bar rule sits INSIDE the label box by decision, and moving it out is the top bar's ruler leaking down again (SPEC I80b, V307b, B69)"
                    }
                }
            }
            # (c) both words of a pair on the same vertical geometry, or the sub-tab jumps as it opens.
            if (($seen307 | Sort-Object -Unique).Count -gt 1) {
                $v307Bad += "$bn307 carries words on different vertical geometry ($($seen307 -join ', ')) - the pair would open at a different height than it closed (SPEC V307c, V305a)"
            }
        }
    }
    if ($top307 -ne 11) { Fail "V307 read $top307 top-bar button(s), expected 11 - this check is covering less than the one bar holds (SPEC V209)" }
    elseif ($sub307 -ne 0) { Fail "V307 read $sub307 sub-bar button(s) - T830 took the last sub-tab strip off the sheet, so a second level of words is a level with no ruler on it (SPEC I117, V305 retired, T830)" }
    elseif ($v307Bad) { foreach ($b in $v307Bad) { Fail "V307 $b" } }
    else { Pass "V307 the $top307 top-bar words clear the line work by $clear307, and there is no second level left to keep a second ruler for - the sub-bars went with T830" }
}

# ---- V308: a motif's constant does not cross into another motif (SPEC I80c) ------------
# This measures the CODE, not the drawing, and it is the invariant B69 asks for. markPath grew
# three motifs in the 109th round and the constants stayed in one heap, so ORN_MARK was read by
# `tab` AND `sub`, and ORN_MARK_RY by `tab` AND `sep`. The 110th round then moved the top bar
# and moved two other levels with it - rdk -l exit 0, gate green, and the damage one level away
# from the edit that caused it, which is the B6 family.
#
# ORN_PILLR is the declared exception: it is not a drawing choice but the arc V228 authors on
# every button, so it is one fact with one owner (SPEC V49) and every motif may read it.
$v308Bad = @()
$geo308 = LuaFn $hh6 'markPath'
if (-not $geo308) { Fail "V308 markPath is gone from WoD20.6 - there are no motifs to read (SPEC I78b, V209)" }
else {
    $body308 = NoComments $geo308
    $iSep308 = $body308.IndexOf('if kind == "sep" then')
    $iSub308 = $body308.IndexOf('if kind == "sub" then')
    if ($iSep308 -lt 0 -or $iSub308 -lt 0 -or $iSub308 -le $iSep308) {
        Fail "V308 markPath does not hold the sep and sub branches in that order - the slices this check reads cannot be cut (SPEC I78b, V209)"
    }
    else {
        # Three slices: sep, sub, and whatever follows them - which is the top bar's motif.
        $slices308 = @(
            @{ name = 'sep'; text = $body308.Substring($iSep308, $iSub308 - $iSep308); forbid = @('ORN_MARK', 'ORN_MARK2', 'ORN_MARK_RX', 'ORN_MARK_RY', 'ORN_MARK_GAP', 'ORN_SUB', 'ORN_SUB_MARK') },
            @{ name = 'sub'; text = $body308.Substring($iSub308, $body308.IndexOf('return table.concat', $iSub308) - $iSub308); forbid = @('ORN_MARK', 'ORN_MARK2', 'ORN_MARK_RX', 'ORN_MARK_RY', 'ORN_MARK_GAP', 'ORN_SEP_RX', 'ORN_SEP_RY') },
            @{ name = 'tab'; text = $body308.Substring($body308.IndexOf('return table.concat', $iSub308)); forbid = @('ORN_SUB', 'ORN_SUB_MARK', 'ORN_SEP_RX', 'ORN_SEP_RY') }
        )
        foreach ($sl308 in $slices308) {
            foreach ($bad308 in $sl308.forbid) {
                # ORN_MARK is a prefix of ORN_MARK2 and ORN_MARK_RX; ORN_SUB of ORN_SUB_MARK.
                if ($sl308.text -match ("\b" + [regex]::Escape($bad308) + "\b(?!_|\d)")) {
                    $v308Bad += "the `"$($sl308.name)`" motif reads $bad308, which belongs to another motif - that sharing is exactly how the 110th round moved three levels when it was asked to move one (SPEC I80c, B69)"
                }
            }
        }
        if ($body308 -notmatch '\bORN_SUB_MARK\b') { $v308Bad += "the sub motif has no ORN_SUB_MARK of its own - without it the short rule is back on the top bar's constant (SPEC I80b, I80c)" }
        if ($body308 -notmatch '\bORN_SEP_RY\b') { $v308Bad += "the sep motif has no ORN_SEP_RY of its own - the separator's fleuron is back on the top bar's constant (SPEC I80c)" }
        if ($v308Bad) { foreach ($b in $v308Bad) { Fail "V308 $b" } }
        else { Pass "V308 the three motifs of markPath each read their own family of constants - only ORN_PILLR, the arc V228 authors, is shared" }
    }
}

# ---- V284: the filigree of a box that RESIZES is redrawn, not inherited ---------------
# SPEC I72d, the 100th round, item 2 of the request. HEALTH is the one section box whose size
# moves at runtime - renderHealthTrack writes its height from the storyteller's track length
# (SPEC V49) - and the ornament data is built ONCE, from what the box measured at creation.
# Nothing above sees this: a frame drawn for ten rows sitting on a four-row box compiles,
# runs, exits 0 and merely looks wrong, which is the B6 shape on a new door.
$hbBad = @()
$ornMake2 = LuaFn $hh6 'ornament'
$refreshFn = LuaFn $hh6 'refreshOrnament'

if (-not $ornMake2) { $hbBad += "V284 ornament() is gone from WoD20.6 - there is no painter to refresh, so this check measured nothing (SPEC V209)" }
elseif (-not $rendFn) { $hbBad += "V284 renderHealthTrack() is gone from the root form - the box that resizes has no renderer, so this check measured nothing (SPEC V209)" }
else {
    $ornBody2 = NoComments $ornMake2
    $rendBody = NoComments $rendFn

    # (a) the memo carries the MEASUREMENTS, not just the path. Anchored on the CONSTRUCTION
    # and not on field names (SPEC V222): what has to hold is that the stored entry is a table
    # with the size beside the path, because a memo that only knows "already drawn" cannot
    # know "drawn at THIS size" - B58 one axis over, the carimbo instead of the key.
    $memoW = [regex]::Match($ornBody2, '(?m)^\s*local\s+(\w+)\s*=\s*ornPainted\[c\.handle\];')

    # The pair the box is measured into. Everything below is anchored on THESE names rather
    # than on the shape of the store, because "= {" is incidental: the entry may be built into
    # a local and handed over on the next line and still be the same construction (SPEC V222).
    $szL = [regex]::Match($ornBody2, '(?m)^\s*local\s+(\w+)\s*,\s*(\w+)\s*=\s*c\.width\s*,\s*c\.height;')
    $memoStored = $ornBody2 -match 'ornPainted\[c\.handle\]\s*='
    $carries = $false

    if ($szL.Success) {
        $wN = [regex]::Escape($szL.Groups[1].Value)
        $hN = [regex]::Escape($szL.Groups[2].Value)

        foreach ($t in [regex]::Matches($ornBody2, '\{[^{}]*\}')) {
            if ($t.Value -match ('\b' + $wN + '\b') -and $t.Value -match ('\b' + $hN + '\b')) { $carries = $true; break }
        }
    }

    if (-not $memoW.Success) { $hbBad += "V284 nothing reads ornPainted[c.handle] - the memo this check measures is gone (SPEC V209)" }
    elseif (-not $szL.Success) { $hbBad += "V284 the box is never measured into a pair - there is nothing for the memo to carry (SPEC I72d, V209)" }
    elseif (-not $memoStored) { $hbBad += "V284 nothing is stored in ornPainted[c.handle] - the memo is read and never written (SPEC V209)" }
    elseif (-not $carries) {
        $hbBad += "V284 the memo stores the path ALONE - with no measurement beside it the painter cannot tell 'already drawn' from 'drawn at this size', and a resized box keeps the frame it was born with (SPEC I72d, B58)"
    }
    else {
        $mn = [regex]::Escape($memoW.Groups[1].Value)

        # Storing the size and never reading it back is decoration. Demand the CURRENT size and
        # a comparison against what was stored - that pair IS the third door.
        if ($ornBody2 -notmatch 'c\.width' -or $ornBody2 -notmatch 'c\.height') {
            $hbBad += "V284 the painter never reads the box's current size - it has nothing to notice a resize with (SPEC I72d)"
        }

        $cmpPat = '(?m)~=\s*' + $mn + '\.\w+|' + $mn + '\.\w+\s*~='
        if ($ornBody2 -notmatch $cmpPat) {
            $hbBad += "V284 the stored measurement is written and never COMPARED - the third door never opens and the resize goes unnoticed (SPEC I72d)"
        }

        if ($ornBody2 -notmatch '\.data\s*=') {
            $hbBad += "V284 nothing rewrites the path data - the frame can only ever be built once (SPEC R113a)"
        }
    }

    # (b) ORDER, and it is the leg with no symptom: refreshing on the near side of the height
    # write rebuilds the frame to the measure the box is LEAVING, and exits 0 doing it.
    $hIdx = $rendBody.IndexOf('.height')
    $rIdx = $rendBody.IndexOf('refreshOrnament')

    if ($hIdx -lt 0) { $hbBad += "V284 renderHealthTrack no longer writes the box height - the resize this check guards is gone (SPEC V49, V209)" }
    elseif ($rIdx -lt 0) { $hbBad += "V284 renderHealthTrack never refreshes the filigree - the box resizes and the frame stays the size it was born (SPEC I72d)" }
    elseif ($rIdx -lt $hIdx) { $hbBad += "V284 the filigree is refreshed BEFORE the height is written - it would be rebuilt to the measure the box is leaving, with no error to show for it (SPEC V284b)" }

    # (c) BOTH health boxes, through the prefix the renderer already carries. A literal name
    # here fixes the Main tab and leaves the Combat twin crooked, in silence.
    if ($rendBody -match 'dynHealth3?_box') {
        $hbBad += "V284 the refresh names a health box LITERALLY - renderHealthTrack serves both of them through its prefix, so a literal repairs one and abandons the other (SPEC V284c)"
    }

    # (d) the NEGATIVE leg. `stretch` exists in the SDK and would do all of this with no code
    # at all, at the price of scaling the volutes and beads with the box - the very shearing
    # that made the ornament vector instead of a PNG. Spelled out so a later round cannot
    # "simplify" it back in and undo the reason the thing is drawn per box (SPEC R113b).
    if ($ornBody2 -match '\.mode\s*=\s*"(?!original")') {
        $hbBad += "V284 the path mode is no longer 'original' - stretch scales the volutes and beads with the box, which is the shearing the per-box vector exists to avoid (SPEC R113b)"
    }

    # ... and it has to BE there. MEASURED by mutation on 2026-09-03 (T622): deleting
    # `p.mode = "original";` outright left the leg above GREEN, because a negative leg finds
    # nothing to object to in an ABSENCE - the same no-op door B7 is named for, on the one leg
    # written to survive a future round's tidying. WrapMode's default is NATIVE and nobody has
    # measured it: R113 reads the enum out of rrpgGUI.lua:1119-1120 and stops at the four values,
    # so a deleted line leaves the motif riding on a default this repo cannot name. "Delete the
    # redundant-looking line" IS the simplification this leg exists to refuse (SPEC V20, B7).
    # Two independent ifs and not an elseif: "stretch" trips the first alone and an absence the
    # second alone, so each mutation names the door it came through.
    if ($ornBody2 -notmatch '\.mode\s*=') {
        $hbBad += "V284 ornament() never sets the path mode at all - the leg above has nothing to object to in an absence, and the motif rides on WrapMode's native default, which no measurement in this repo names (SPEC V284d, R113b, V20)"
    }

    if (-not $refreshFn) {
        $hbBad += "V284 refreshOrnament() is gone from WoD20.6 - renderHealthTrack lives on the root form and has no other reach into the painter's chunk (SPEC I72d)"
    }
}

if ($hbBad) { foreach ($b in $hbBad) { Fail $b } }
else { Pass "V284 the filigree carries its measurements, notices a resize, is refreshed AFTER the height is written, and reaches both health boxes by prefix" }

# ---- V285: what the gate cannot RUN, the filigree may not DEPEND on ------------------
# SPEC B62, the 102nd round. V284 above proved the SHAPE of the refresh - the memo carries a
# measurement, the calls are in the right order, the mode is original - and the feature was
# broken anyway, because the two RUNTIME facts it rested on were never checkable here: the
# gate does not execute Lua (SPEC B30, B34). This block is V198/V275's own doctrine applied
# to the READS instead of to the memo: what cannot be counted gets PROHIBITED.
$v285Bad = @()
$refFn = LuaFn $hh6 'refreshOrnament'
$ornFn285 = LuaFn $hh6 'ornament'

if (-not $refFn) { $v285Bad += "V285 refreshOrnament() is gone from WoD20.6 - the refresh path this check measures does not exist (SPEC V209)" }
elseif (-not $ornFn285) { $v285Bad += "V285 ornament() is gone from WoD20.6 - there is no painter to measure (SPEC V209)" }
else {
    $refBody = NoComments $refFn
    $ornBody285 = NoComments $ornFn285

    # (a) the ornamented control is found by the MEMO. A finder that matches nothing returns
    # quietly and exits 0 - there is no error and no check to catch it, which is half of B62.
    if ($refBody -notmatch 'ornPainted\[') {
        $v285Bad += "V285 refreshOrnament does not find its target through ornPainted - a finder built on a property read returns empty in silence when the host answers in a shape nobody checked (SPEC V285a, B62)"
    }

    # (b) the NEGATIVE leg, and the one that keeps (a) from being undone. getClassName is proven
    # by the theme walk that repaints every round; `align` was proven by nothing, and the read
    # this replaces was the only one in the sheet.
    if ($refBody -match '\.align\s*==' -or $ornBody285 -match '\.align\s*==') {
        $v285Bad += "V285 the filigree READS .align - it has no precedent as a runtime read anywhere in the sheet, and if the host answers with an enum index instead of the string the branch silently never runs (SPEC V285b, B62)"
    }

    # (c) the redraw measurement is HANDED OVER, not read back. Anchored on the signature and on
    # the refresh branch actually naming those parameters - a fifth argument that is accepted and
    # then ignored is the same bug wearing the fix's clothes.
    $sig = [regex]::Match($ornBody285, 'function\s+ornament\s*\(([^)]*)\)')
    if (-not $sig.Success) { $v285Bad += "V285 ornament's signature could not be read - this check is a no-op (SPEC V209, V20)" }
    else {
        $prms = @($sig.Groups[1].Value -split ',' | ForEach-Object { $_.Trim() } | Where-Object { $_ })
        if ($prms.Count -lt 5) {
            $v285Bad += "V285 ornament takes $($prms.Count) parameter(s) - it cannot be HANDED a measurement, so the redraw can only read one back off a control whose layout may not have resolved (SPEC V285c, B62)"
        } else {
            $reIdx = $ornBody285.IndexOf('if e ~= nil then')
            $crIdx = $ornBody285.IndexOf('if e == nil then')
            if ($reIdx -lt 0 -or $crIdx -le $reIdx) {
                $v285Bad += "V285 the refresh branch and the creation branch could not be told apart in ornament - this check is a no-op (SPEC V209, V20)"
            } else {
                $refBranch = $ornBody285.Substring($reIdx, $crIdx - $reIdx)
                foreach ($p in @($prms[3], $prms[4])) {
                    if ($refBranch -notmatch ('(?<![\w.])' + [regex]::Escape($p) + '(?![\w])')) {
                        $v285Bad += "V285 the refresh branch never uses the handed-over '$p' - the measurement is accepted and then thrown away, which redraws from the size the box is LEAVING (SPEC V285c, B62)"
                    }
                }
            }
        }
        # and the caller has to actually hand something over
        if ($refBody -notmatch 'ornament\s*\([^)]*,[^)]*,[^)]*,[^)]*,[^)]*\)') {
            $v285Bad += "V285 refreshOrnament calls the painter without a measurement - the parameters exist and nobody fills them (SPEC V285c)"
        }
    }

    # (d) the general rule (b) is one case of: nothing else is read off the child. `handle` is
    # the key of a table the sheet owns, not a measurement the host has to resolve.
    $loopVar = [regex]::Match($refBody, '(?m)^\s*local\s+(\w+)\s*=\s*\w+\[\s*i\s*\]\s*;')
    if ($loopVar.Success) {
        $lv = [regex]::Escape($loopVar.Groups[1].Value)
        foreach ($m in [regex]::Matches($refBody, '(?<![\w.])' + $lv + '\.(\w+)')) {
            if ($m.Groups[1].Value -ne 'handle') {
                $v285Bad += "V285 refreshOrnament reads .$($m.Groups[1].Value) off the child - every property beyond `handle` is a runtime answer this gate cannot verify, and B62 was two of them (SPEC V285d)"
            }
        }
    }
}

if ($v285Bad) { foreach ($b in ($v285Bad | Sort-Object -Unique)) { Fail $b } }
else { Pass "V285 the filigree finds its target by memo, reads no .align, and is redrawn from the measurement it is handed - not from one read back" }

if ($Build) {
    $rdk = "$env:LOCALAPPDATA\FirecastSDK3\rdk.exe"
    if (-not (Test-Path $rdk)) { Fail "V6 rdk.exe not found at $rdk" }
    else {
        $b = Get-Item $rpk
        $beforeStamp, $beforeSize = $b.LastWriteTime, $b.Length
        Push-Location $plugin
        & $rdk -l | Out-Host
        $code = $LASTEXITCODE
        Pop-Location
        if ($code -eq 0) { Pass "V6 rdk -l exit 0" } else { Fail "V6 rdk -l exit $code" }
        $a = Get-Item $rpk
        if ($a.LastWriteTime -ne $beforeStamp -or $a.Length -ne $beforeSize) {
            Pass "V7 .rpk changed ($beforeSize -> $($a.Length) bytes)"
        } else { Fail "V7 .rpk unchanged after build - exit 0 was not proof (see SPEC B.1)" }
    }
}


# ---- V310: a hedge ritual is offered only while its PATH reaches its level ---------------
# SPEC I83, V310, user 2026-08-26. Six legs, and (e) and (f) are the two that give no symptom
# at all when they are wrong: the filter simply stops pruning, rdk exits 0 and every other
# check on this sheet stays green.
$v310Bad = @()

# The table maps ONE path per ritual - a string, not a list (SPEC I83b). It parses to zero rows
# today and that is legal while PICKER_LIST["hedgeRitual"] is empty too; leg (a) below is what
# ties the two together the moment either one fills.
$hrpDecl310 = [regex]::Match($rootTxt, '(?s)(?m)^\s*HEDGE_RITUAL_PATH = \{\r?\n(.*?)^\s*\};')
$hrp310 = [ordered]@{}
if ($hrpDecl310.Success) {
    foreach ($row in [regex]::Matches($hrpDecl310.Groups[1].Value, '\["([^"]+)"\]\s*=\s*"([^"]*)"')) {
        $hrp310[$row.Groups[1].Value] = $row.Groups[2].Value
    }
}

$numina310 = @()
$hritual310 = @()
if ($region270.Success) {
    $nl310 = [regex]::Match($region270.Groups[1].Value, '(?s)\["numina"\] = \{(.*?)\},\s*\n')
    if ($nl310.Success) { $numina310 = @([regex]::Matches($nl310.Groups[1].Value, '"([^"]*)"') | ForEach-Object { $_.Groups[1].Value } | Where-Object { $_ -ne '' }) }
    $rl310 = [regex]::Match($region270.Groups[1].Value, '(?s)\["hedgeRitual"\] = \{(.*?)\},\s*\n')
    if ($rl310.Success) { $hritual310 = @([regex]::Matches($rl310.Groups[1].Value, '"([^"]*)"') | ForEach-Object { $_.Groups[1].Value } | Where-Object { $_ -ne '' }) }
}

$rootNoC310 = NoComments $rootTxt
$hh6NoC310  = NoComments $hh6

# Zero-guard: the three things this check reads have to BE there, or it measures nothing and
# passes green forever (SPEC V209, V20).
if (-not $hrpDecl310.Success) { $v310Bad += "HEDGE_RITUAL_PATH is not declared on the root form - the ritual filter has no table to read and this check measured nothing (SPEC I83b, V209)" }
elseif ($rootNoC310 -notmatch '(?m)^\s*function hedgePathLevels\(\)') { $v310Bad += "hedgePathLevels is gone from the root form - there is no map of path levels and this check measured nothing (SPEC I83d, V209)" }
elseif (-not $region270.Success) { $v310Bad += "the PICKER_LIST region could not be read - the two lists this check compares against are unreachable (SPEC V209)" }
else {
    # (a) both ends of the table stay inside the two lists, and the coverage rule switches on
    # the moment the ritual list fills. An empty table beside an empty list is the state the
    # 112th round ships; an empty table beside a FULL list is a filter that prunes nothing.
    foreach ($k in $hrp310.Keys) {
        if ($hritual310 -notcontains $k) { $v310Bad += "HEDGE_RITUAL_PATH is keyed on '$k', which PICKER_LIST['hedgeRitual'] does not carry - nobody can ever pick it, so the entry is dead (SPEC V310a, B55)" }
        if ($numina310 -notcontains $hrp310[$k]) { $v310Bad += "HEDGE_RITUAL_PATH['$k'] names the path '$($hrp310[$k])', which PICKER_LIST['numina'] does not carry - a path no row can hold answers zero, so the ritual is offered to nobody (SPEC V310a)" }
    }
    if ($hritual310.Count -gt 0) {
        $uncovered310 = @($hritual310 | Where-Object { -not $hrp310.Contains($_) })
        if ($uncovered310.Count -gt 0) { $v310Bad += "$($uncovered310.Count) ritual(s) are on the picker list with no path in HEDGE_RITUAL_PATH, e.g. '$($uncovered310[0])' - an unknown value prunes nothing, so those would be offered at every level of every path (SPEC V310a, I83b)" }
        $noPrefix310 = @($hritual310 | Where-Object { $_ -notmatch '^\d+\. ' })
        if ($noPrefix310.Count -gt 0) { $v310Bad += "$($noPrefix310.Count) ritual(s) carry no '<n>. ' prefix, e.g. '$($noPrefix310[0])' - the level is read off the item text and nowhere else, so a missing prefix reads as level 0 and passes for free (SPEC V310a, V310c, I83c)" }
    }

    # (b) the branch answers in pickAllowed, BEFORE pickRefusal, and an unknown value is left
    # alone. A `false` on the unknown key would open twelve blank dropdowns while the table is
    # still empty - V271d/V272e in a third place.
    # Anchored on the function's OWN indent, never on `^\s*end;`: pickAllowed's branches each
    # close with an `end;` of their own, so a loose anchor would cut the body off before the
    # pickRefusal call and the ordering leg below would quietly measure nothing (SPEC V20).
    $pa310 = [regex]::Match($rootNoC310, "(?s)\n\t\t\tfunction pickAllowed\(([^)]*)\)(.*?)\n\t\t\tend;")
    if (-not $pa310.Success) { $v310Bad += "pickAllowed could not be read off the root form - the one door to the rule is gone (SPEC V199, V209)" }
    else {
        if ($pa310.Groups[1].Value -notmatch 'hedge') { $v310Bad += "pickAllowed does not take the hedge path map - its fourth parameter is what the ritual branch measures against (SPEC I83f, V310e)" }
        $paBody310 = $pa310.Groups[2].Value
        $iRit310 = $paBody310.IndexOf('hedgeRitual_%d+$')
        $iRef310 = $paBody310.IndexOf('pickRefusal')
        if ($iRit310 -lt 0) { $v310Bad += "pickAllowed has no ritual-row branch - the twelve picked ritual rows would fall through to pickRefusal, which knows nothing about hedge magic (SPEC V310b)" }
        elseif ($iRef310 -ge 0 -and $iRit310 -gt $iRef310) { $v310Bad += "the ritual branch sits AFTER the pickRefusal call - pickRefusal answers nil for a field it does not know, so the branch would never be reached (SPEC V310b)" }
    }
    $hra310 = [regex]::Match($rootNoC310, "(?s)\n\t\t\tlocal function hedgeRitualAllows\(([^)]*)\)(.*?)\n\t\t\tend;")
    if (-not $hra310.Success) { $v310Bad += "hedgeRitualAllows is gone from the root form - the rule the branch delegates to is unreadable (SPEC V310b, V209)" }
    else {
        $hraBody310 = $hra310.Groups[2].Value
        if ($hraBody310 -notmatch 'if path == nil then return true') { $v310Bad += "hedgeRitualAllows does not answer TRUE for a value HEDGE_RITUAL_PATH has never heard of - while the table is empty that is every value, so the twelve dropdowns would open blank (SPEC V310b, V271d)" }
        # (c) the level comes off the item text and from nowhere else. A number in the table
        # would be the second source V269d already closed for the price.
        if ($hraBody310 -notmatch '\^\(%d\+\)%\. ') { $v310Bad += "hedgeRitualAllows does not read the level off the item-text prefix - a second source for one number is what lets the dots on screen say three while the filter measures four (SPEC V310c, V269d)" }
        if ($hraBody310 -match 'HEDGE_RITUAL_LEVEL') { $v310Bad += "hedgeRitualAllows reads a level from a table instead of the item text (SPEC V310c)" }
    }
    foreach ($v in $hrp310.Values) {
        if ($v -match '^\d+$') { $v310Bad += "HEDGE_RITUAL_PATH holds the number '$v' as a path - the level is the item's prefix and the table carries the PATH only (SPEC V310c)" }
    }

    # (d) the path map is its OWN, over all SEVENTEEN rows, and the ritual branch never reads
    # discLevels: `Necromancy` and `Weather Control` are a hedge path AND a vampire
    # Discipline/path, so one shared map lets the wrong one open the rituals (SPEC B15, V34).
    $hpl310 = [regex]::Match($rootNoC310, "(?s)\n\t\t\tfunction hedgePathLevels\(\)(.*?)\n\t\t\tend;")
    if (-not $hpl310.Success) { $v310Bad += "hedgePathLevels could not be read - this leg measured nothing (SPEC V209)" }
    else {
        $hplBody310 = $hpl310.Groups[1].Value
        if ($hplBody310 -notmatch 'NUMINA_ROWS') { $v310Bad += "hedgePathLevels does not walk NUMINA_ROWS - a literal count here goes stale the next time a row is added, and the five TYPED rows hold dots like any other (SPEC V310d, I83d)" }
        if ($hplBody310 -notmatch 'traitLevel') { $v310Bad += "hedgePathLevels never reads a dot - it would answer zero for every path and prune the whole list (SPEC V310d)" }
        if ($hplBody310 -notmatch '>') { $v310Bad += "hedgePathLevels does not keep the HIGHER of two rows naming one path - answering with the lower takes away a level the player owns (SPEC V310d)" }
    }
    if ($hra310.Success -and $hra310.Groups[2].Value -match 'discLevels') { $v310Bad += "the ritual rule reads discLevels - Necromancy the DISCIPLINE at five would open the rituals of Necromancy the hedge PATH (SPEC V310d, B15)" }

    # (e) the map rides the memo's own generation. Outside that block it is rebuilt once per
    # CONTROL; read from anywhere the reset cannot reach it serves last render's levels with
    # rdk exiting 0 and the gate green - B48 through a third door.
    $reset310 = [regex]::Match($hh6NoC310, '(?s)if memo\.stamp ~= stamp or memo\.levels ~= levels then(.*?)end;')
    if (-not $reset310.Success) { $v310Bad += "the picker memo no longer resets on a new generation - there is nothing for the hedge map to ride (SPEC V310e, V209)" }
    elseif ($reset310.Groups[1].Value -notmatch 'memo\.hedge\s*=\s*hedgePathLevels\(\)') { $v310Bad += "memo.hedge is not built INSIDE the memo reset - built outside it, the map is rebuilt once per control instead of once per render (SPEC V205, V310e)" }
    $hedgeBuilds310 = @([regex]::Matches($hh6NoC310, 'hedgePathLevels\(\)'))
    if ($hedgeBuilds310.Count -gt 1) { $v310Bad += "hedgePathLevels() is called $($hedgeBuilds310.Count) times in WoD20.6 - one build per render is the whole point, and a second caller is a second generation nothing invalidates (SPEC V205, V310e)" }

    # (f) reach and trigger, and the two halves fail for different reasons: a link without the
    # DOTS never wakes on a level, and a renderer without the twelve NAMES reaches no combo.
    $fr310 = [regex]::Match($hh6, '(?ms)local FILTER_ROW = \{(.*?)\}')
    if (-not $fr310.Success) { $v310Bad += "FILTER_ROW is not declared - the ritual rows would not be filtered at all (SPEC V310f, V209)" }
    elseif ($fr310.Groups[1].Value -notmatch 'hedgeRitual\s*=\s*true') { $v310Bad += "FILTER_ROW does not carry hedgeRitual - pickerItems asks isFilterRow first, so the twelve combos would be handed the whole list (SPEC V310f, I83g)" }
    $fn310 = [regex]::Match($hh6, '(?ms)local FILTER_NAME = \{(.*?)\}')
    if ($fn310.Success -and $fn310.Groups[1].Value -match 'hedgeRitual') { $v310Bad += "FILTER_NAME carries a hedgeRitual control - all twelve rows answer to the SAME question, so this scope holds by ROOT; by name only the one named row would filter (SPEC V310f, I83g, V203)" }

    # REACH and TRIGGER, and T874 moved BOTH to the same place. While the twelve ritual rows were
    # comboBoxes the filter had to be pushed at them - renderHedgePickers had to NAME them, and a
    # dataLink had to watch the seventeen path names AND their eighty-five dots so that buying a
    # level woke the push. Wave 4 made them pickers: there is no list to keep up to date any more,
    # because the pool is built at the moment the box OPENS and hedgePathLevels() is read there,
    # once per opening (SPEC I113c, V205, V354c). A dataLink watching 102 fields to refresh a
    # control that no longer exists is B55's dead data, not a guard.
    #
    # So the two legs become one question asked of mfOpen, which is where the answer now lives -
    # and it is a STRONGER question than the old pair: the old one woke on a change, this one
    # cannot be stale at all, because nothing is cached between openings.
    $rhp310 = [regex]::Match($hh6NoC310, "(?s)\n\t\t\tfunction renderHedgePickers\(from\)(.*?)\n\t\t\tend;")
    if (-not $rhp310.Success) { $v310Bad += "renderHedgePickers is gone from WoD20.6 - nothing re-filters the one hedge combo wave 2 has not reached (SPEC V310f, V209)" }
    elseif ($rhp310.Groups[1].Value -match 'cbohedgeRitual_') { $v310Bad += "renderHedgePickers names a cbohedgeRitual control again - wave 4 turned the twelve picked ritual rows into pickers, so that name reaches nothing and the walk is looking for a control that cannot exist (SPEC V248c, V310f, T874)" }

    $doc310 = Doc (Join-Path $dir "WoD20.7.lfm")
    foreach ($dl in $doc310.SelectNodes("//dataLink")) {
        if ($dl.GetAttribute("onChange") -match 'renderHedgePickers' -and $dl.GetAttribute("fields") -match "'numina_1_1'") {
            $v310Bad += "a dataLink on WoD20.7 still watches the numina DOTS to push renderHedgePickers - the twelve combos it fed left in T874 and the ritual filter is asked inside mfOpen now, so this is 102 fields waking a walk that writes nothing (SPEC V310f, B55, T874)"
        }
    }
    $open310 = LuaFn $rootTxt 'mfOpen'
    if (-not $open310) { $v310Bad += "mfOpen is gone - the ritual filter retired INTO it and there is nothing left asking hedgePathLevels at all (SPEC V310f, V209, T874)" }
    else {
        $ob310 = NoComments $open310
        if ($ob310 -notmatch 'local hedge\s*=\s*hedgePathLevels\(\);') { $v310Bad += "mfOpen does not build the hedge path map while it builds the pool - the ritual rows would be offered every ritual in the book and guardPick would throw the choice back on the next click, which is the list and the guard disagreeing (SPEC V199, V310f, I83f)" }
        if ($ob310 -notmatch 'pickAllowed\(MF\.field, vals\[i\], levels, hedge\)') { $v310Bad += "mfOpen does not hand the hedge map to pickAllowed - the fourth argument is what the ritual branch measures against, and a nil there opens the whole list (SPEC V310b, I83f)" }
    }
}

if ($v310Bad) { foreach ($b in $v310Bad) { Fail "V310 $b" } }
else { Pass "V310 the ritual filter reads one table of $($hrp310.Count) ritual(s) against a map of all 17 numina rows, answers inside pickAllowed before pickRefusal, rides the memo's own generation, and is woken by 17 names and 85 dots" }

# ---- V312: the width ruler knows the FONT SIZE, and is still a ceiling -------------------
# SPEC I86f, V312, R116, user 2026-08-26. Leg (c) is the one that gives B43 back if it falls:
# a ruler that loosens is not a licence to re-tighten, and a comboBox CLIPS rather than wraps.
$v312Bad = @()

$gateSrc312 = [System.IO.File]::ReadAllText($PSCommandPath)
$body312    = [regex]::Match($gateSrc312, '(?s)function NeededPx\(([^)]*)\)\s*\{(.*?)\n\}')

# (a) TWO constants, and the 12pt one is picked by the ATTRIBUTE. A ruler chosen by control
# NAME is a nominal exception wearing a formula's clothes (SPEC V312a).
$def312 = [regex]::Match($gateSrc312, '(?m)^\$PX_PER_CHAR\s*=\s*([\d.]+)\s*$')
$s12312 = [regex]::Match($gateSrc312, '(?m)^\$PX_PER_CHAR_12\s*=\s*([\d.]+)\s*$')

if (-not $def312.Success) { $v312Bad += "`$PX_PER_CHAR is gone - the body ruler every label is measured against no longer exists and this check measured nothing (SPEC V209, V16)" }
elseif (-not $s12312.Success) { $v312Bad += "`$PX_PER_CHAR_12 is gone - the fontSize axis collapsed back to one literal and the four fields of I86d are illegal again (SPEC V312a, V209)" }
elseif (-not $body312.Success) { $v312Bad += "NeededPx could not be read - the one place that turns characters into pixels is unreadable (SPEC V312d, V209)" }
else {
    $defV312 = [double]$def312.Groups[1].Value
    $s12V312 = [double]$s12312.Groups[1].Value

    if ($defV312 -ne 6.5) { $v312Bad += "the body ruler is $defV312, not the 6.5 it has been calibrated at since the first round - the 114th round added an axis, it did not move this one (SPEC V312a)" }
    # (b) the 12pt ruler stays ABOVE what the screen draws. R116 measured >1cm of slack beside
    # a 55-character item in a 362px interior, which puts the truth at 5.89 or less.
    if ($s12V312 -gt $defV312) { $v312Bad += "the 12pt ruler ($s12V312) is WIDER than the body ruler ($defV312) - smaller glyphs cannot need more pixels, so one of the two is upside down (SPEC V312b)" }
    if ($s12V312 -lt 5.9) { $v312Bad += "the 12pt ruler is $s12V312, under the 5.9 R116's measurement bounds it at - below that it stops being a CEILING and becomes a guess, and what waits there is a comboBox that clips (SPEC V312b, B43, R116)" }

    $nbody312 = $body312.Groups[2].Value
    if ($nbody312 -notmatch '\$PX_PER_CHAR_12' -or $nbody312 -notmatch '\$PX_PER_CHAR\b') { $v312Bad += "NeededPx does not read BOTH rulers - whichever it dropped is a constant nothing consults (SPEC V312d)" }
    if ($nbody312 -notmatch "12") { $v312Bad += "NeededPx never tests for the size 12 - the second ruler exists and no control can ever reach it (SPEC V312a)" }
    if ($body312.Groups[1].Value -notmatch 'fontSize') { $v312Bad += "NeededPx does not take a fontSize - the ruler cannot be chosen by the attribute, only by the caller's opinion (SPEC V312a)" }
    # The negative half of (a): the choice is made on the SIZE, never on who is asking.
    if ($nbody312 -match 'cbo[A-Z]|GetAttribute\("name"\)|hedgeAffiliation') { $v312Bad += "NeededPx picks its ruler from a control NAME - that is a nominal exception, which is B38 arriving through the width door (SPEC V312a)" }

    # (d) ONE place reads the rulers. Two owners for one sum is B70 on another axis - and the
    # test is where they are READ, not how they are multiplied: the product is written against
    # the chosen ruler, so counting `*` would measure the spelling of one line.
    #
    # Comment lines are cut first, or this check's own prose about $PX_PER_CHAR would count as
    # a second reader and the leg would redden on itself.
    $codeOnly312 = ($gateSrc312 -split "`n" | Where-Object { $_ -notmatch '^\s*#' }) -join "`n"
    $outside312 = 0
    foreach ($m312 in [regex]::Matches($codeOnly312, '\$PX_PER_CHAR(_12)?')) {
        $lineAt312 = $codeOnly312.Substring(0, $m312.Index)
        $lineAt312 = $lineAt312.Substring($lineAt312.LastIndexOf("`n") + 1)
        if ($lineAt312 -match '^\$PX_PER_CHAR') { continue }          # the two definitions
        if ($nbody312.Contains($m312.Value)) { continue }             # inside NeededPx
        $outside312++
    }
    if ($outside312 -gt 0) { $v312Bad += "a ruler is read outside NeededPx and outside its own declaration - two owners for one sum is what B70 cost a round to find (SPEC V312d)" }

    # (c) the ruler only LOOSENED, and the three entries of the HEDGE MAGIC grid are held EQUAL
    # to each other - which is the promise - without any of them being held to a NUMBER, which
    # is what T896 had to take out.
    #
    # The three carried literals (335/335/360) from the round the ? split the affiliation entry.
    # The user then asked for every box on the Numina tab to take the width of PSYCHIC PHENOMENA
    # and said in writing that pickers, typed fields and dropdowns are what may shrink to pay for
    # it (SPEC I134e, I134f). A literal here charges the tab for obeying its owner - B106 - so the
    # width is DERIVED from the box the entries live in: interior right minus where the entry
    # column opens. Same answer V390 gives the box widths one door over, and the grid promise is
    # stronger for it: three controls equal to a computed number cannot drift into a staircase,
    # while three equal literals only cannot drift until somebody edits one.
    $doc312 = Doc (Join-Path $dir "WoD20.7.lfm")
    $hm312  = @($doc312.SelectNodes("//scrollBox/layout[label/@text='HEDGE MAGIC']"))[0]
    if ($null -eq $hm312) { $v312Bad += "the HEDGE MAGIC box is gone from WoD20.7 - the entry width is derived from it and this leg has nothing to derive from (SPEC V209, I134f)" }
    $entryL312 = 0
    $aff312 = if ($null -ne $hm312) { @($hm312.SelectNodes("button[@name='dynhedgeAffiliation']"))[0] } else { $null }
    if ($null -ne $aff312) { $entryL312 = [int]$aff312.GetAttribute("left") }
    # 20 is the box margin V280(a) floors, and it is read off the TITLE rather than assumed: the
    # title label opens on the margin in every box on this sheet.
    $marg312 = 20
    $ttl312 = if ($null -ne $hm312) { $hm312.SelectSingleNode("label[@text='HEDGE MAGIC']") } else { $null }
    if ($null -ne $ttl312) { $marg312 = [int]$ttl312.GetAttribute("left") }
    $entryW312 = if ($null -ne $hm312 -and $entryL312 -gt 0) { [int]$hm312.GetAttribute("width") - $marg312 - $entryL312 } else { 0 }
    if ($entryW312 -le 0) { $v312Bad += "the entry column of HEDGE MAGIC derived to $entryW312 px - this leg would certify any width at all (SPEC V209, V20)" }
    $WIDTH312 = @{
        'dynhedgeAffiliation' = $entryW312
        'edtHedgeAffiliation' = $entryW312
        'cboHedgeAttr'        = $entryW312
    }
    $seen312 = 0
    foreach ($nm312 in $WIDTH312.Keys) {
        $node312 = @($doc312.SelectNodes("//*[@name='$nm312']"))[0]
        if ($null -eq $node312) { $v312Bad += "'$nm312' is not on WoD20.7 - one of the controls I86d narrows cannot be found, so this leg measured less than it claims (SPEC V209)"; continue }
        $seen312++
        $w312 = [int]$node312.GetAttribute("width")
        if ($entryW312 -gt 0 -and $w312 -ne $WIDTH312[$nm312]) { $v312Bad += "'$nm312' is ${w312}px and the HEDGE MAGIC box derives $($WIDTH312[$nm312]) for its entry column - the column is a GRID and one control leaving it reopens the ladder the user had removed (SPEC I86d, V297b, I134f)" }
    }
    if ($seen312 -ne 3) { $v312Bad += "only $seen312 of the 3 named entry controls were measured (SPEC V209)" }

    # The Essence edit carries no name, so it is reached by its field - and it has to land on
    # the same width as its two neighbours or V297b is the only thing left holding the grid.
    $ess312 = @($doc312.SelectNodes("//edit[@field='hedgeEssence']"))[0]
    if ($null -eq $ess312) { $v312Bad += "the hedgeEssence entry is gone from WoD20.7 - the middle row of the HEDGE MAGIC grid is unmeasured (SPEC V209, I86d)" }
    elseif ($entryW312 -gt 0 -and [int]$ess312.GetAttribute("width") -ne $entryW312) { $v312Bad += "the hedgeEssence entry is $([int]$ess312.GetAttribute('width'))px against the $entryW312 its box derives - it is a free-text field with no list to price it, so nothing but this leg keeps it on the grid (SPEC I86d, I134f)" }

    # No OTHER 12pt picker SPENT the slack, and the test says exactly that rather than guessing
    # at a width: every one of them still clears the OLD 6.5 ruler, which each of them cleared
    # before this round (the gate was green). The four of I86d are excluded because failing 6.5
    # is the whole point of them.
    #
    # A bare width threshold cannot express this - the sheet has 12pt pickers at 150px holding
    # three-word lists, and they are correct. The ruler gaining slack is not permission to spend
    # it: the vampire ritual and path rows carry the longest strings the sheet owns, and they are
    # exactly where B43 was found.
    foreach ($f312 in $files) {
        foreach ($cb312 in (Doc $f312.FullName).SelectNodes("//comboBox[@fontSize='12'][@width]")) {
            $cn312 = $cb312.GetAttribute("name")
            if ($WIDTH312.ContainsKey($cn312)) { continue }
            $cw312 = 0; if (-not [int]::TryParse($cb312.GetAttribute("width"), [ref]$cw312)) { continue }
            $tpl312 = TplOf $cb312
            $need312 = 0; $worst312 = ""
            foreach ($it312 in (ListOf $cb312 $tpl312)) {
                if ($it312 -eq '') { continue }
                $n312 = NeededPx $it312
                if ($n312 -gt $need312) { $need312 = $n312; $worst312 = $it312 }
            }
            if ($need312 -eq 0) { continue }
            if (($need312 + $ARROW) -gt $cw312 -and $NARROW_OK -notcontains $cn312) { $v312Bad += "$($f312.Name): the 12pt picker '$cn312' is ${cw312}px and would not clear the OLD 6.5 ruler ('$worst312' wants $($need312 + $ARROW)) - it spent slack the 114th round opened for four named controls and nobody else (SPEC V312c, R116)" }
        }
    }
}

if ($v312Bad) { foreach ($b in $v312Bad) { Fail "V312 $b" } }
else { Pass "V312 the ruler is a pair - 6.5 for the body and $($s12312.Groups[1].Value) for 12pt, chosen by the attribute in the one place that multiplies - and the 3 named entries plus hedgeEssence all sit on the ${entryW312}px their own box derives, spent by nobody else" }

# ---- V338: defaultValue lives on <dataLink>, and nowhere else ----------------------------
# SPEC I99b, V338, B80. This one exists because the XML parser ACCEPTS the attribute anywhere
# and rdk refuses it silently: exit 1, no message, .rpk deleted - the B19 mode. It cost a
# three-step bisection to find, and the first two steps blamed innocent Lua written that same
# round. A wrong-home attribute has no symptom until the build dies, so the check is the symptom.
$v338Bad = @()
$dv338 = @()
foreach ($f338 in $files) {
    foreach ($n338 in (Doc $f338.FullName).SelectNodes("//*[@defaultValue]")) {
        $dv338 += [pscustomobject]@{ File = $f338.Name; Tag = $n338.LocalName; Field = $n338.GetAttribute("field") }
    }
}
# (c) zero-guard FIRST: the attribute exists in seven places, so an empty sweep means the
# collector broke, not that the sheet got tidy (SPEC V20, B7).
if ($dv338.Count -eq 0) { $v338Bad += "no defaultValue was found anywhere on the sheet - it is authored in several places, so this is the collector failing, not the sheet being clean (SPEC V20)" }
else {
    # (a) every one of them is on a dataLink.
    foreach ($d338 in $dv338) {
        if ($d338.Tag -ne 'dataLink') { $v338Bad += "$($d338.File): defaultValue is authored on a <$($d338.Tag)> for field '$($d338.Field)' - it belongs to <dataLink>, and anywhere else rdk exits 1 with an EMPTY message and deletes the .rpk (SPEC B80, B19)" }
    }
    # (b) the road default is the pair of the cboRoad that no longer carries it (SPEC I99b).
    $road338 = @($dv338 | Where-Object { $_.Field -eq 'road' })
    if ($road338.Count -ne 1) { $v338Bad += "the road default sits on $($road338.Count) dataLink(s), not the one I99b declares - without it a sheet saved before the 125th round opens with no road at all" }
    $cbo338 = @((Doc (Join-Path $dir "WoD20.1.lfm")).SelectNodes("//*[@name='dynroad'][@defaultValue] | //*[@name='edtRoad'][@defaultValue]"))
    if ($cbo338.Count -ne 0) { $v338Bad += "the road control carries defaultValue again - this is the exact authoring that killed the build in the 125th round (SPEC B80)" }
}

if ($v338Bad) { foreach ($b in $v338Bad) { Fail "V338 $b" } }
else { Pass "V338 all $($dv338.Count) defaultValue attributes sit on a dataLink, the road default among them, and cboRoad carries none" }
# ---- V329 / V330: the bearing ladder and the one writer that owns the road line ----------
# SPEC I99g, I99h, V329, V330, the 125th round. The ladder is the half a screen test cannot
# reach cheaply - five rungs and a road roster of 55 - so it is measured here instead.
$v329Bad = @()
$rootLua = [System.IO.File]::ReadAllText((Join-Path $dir "WoD20th.lfm"))

# (a) five rungs, DESCENDING, with the four cuts of core p.310 / da p.114 spelled literally,
# and a bare `return` for the last so no rating falls through (SPEC R120).
$mod329 = [regex]::Match($rootLua, '(?s)function bearingMod\(n\)(.*?)\n\t{3}end;')
if (-not $mod329.Success) { $v329Bad += "bearingMod is gone - the ladder that turns a dot count into a modifier does not exist and this check measured nothing (SPEC V20, V209)" }
else {
    $body329 = $mod329.Groups[1].Value
    $cuts329 = @([regex]::Matches($body329, 'n >= (\d+)') | ForEach-Object { [int]$_.Groups[1].Value })
    if (($cuts329 -join ',') -ne '10,8,4,2') { $v329Bad += "bearingMod cuts at [$($cuts329 -join ',')], not the 10,8,4,2 both books print - the rungs moved without a round behind them (SPEC R120, V329a)" }
    foreach ($sig329 in @('"-2"','"-1"','"0"','"+1"','"+2"')) {
        if (-not $body329.Contains($sig329)) { $v329Bad += "bearingMod never returns $sig329 - one of the five rungs is unreachable (SPEC V329b)" }
    }
    if ($body329 -notmatch '(?m)^\s*return "\+2";') { $v329Bad += "the last rung is not a bare return - a fifth test would leave a rating with no answer, and a blank modifier is the silent kind (SPEC V329a)" }
}

# (b)/(c)/(d) BEARING is TOTAL over the road list and carries nothing the list does not offer.
$roadItems329 = @()
$rl329 = [regex]::Match($rootLua, '(?s)\["road"\] = \{(.*?)\n\t{4}\},')
if (-not $rl329.Success) { $v329Bad += "PICKER_LIST[""road""] could not be read - the roster this leg compares against is gone (SPEC V209)" }
else { $roadItems329 = @([regex]::Matches($rl329.Groups[1].Value, '"([^"]+)"') | ForEach-Object { $_.Groups[1].Value }) }

$bearKeys329 = @{}
$bt329 = [regex]::Match($rootLua, '(?s)\n\t{4}BEARING = \{(.*?)\n\t{4}\};')
if (-not $bt329.Success) { $v329Bad += "BEARING could not be read - the name half of the line has no table (SPEC V209)" }
else { foreach ($m in [regex]::Matches($bt329.Groups[1].Value, '\["([^"]+)"\]\s*=\s*"([^"]*)"')) { $bearKeys329[$m.Groups[1].Value] = $m.Groups[2].Value } }

# The DEFAULT road, and it has ONE owner in this file since T906. It was spelled out three times
# - in the BEARING leg, in the era-filter leg and in the dataLink leg - and T906 renamed the value
# (Humanity -> Path of Humanity, SPEC I135b). Three copies of one fact is three chances for the
# next rename to leave one of them behind, green, describing a road nobody is on (SPEC B70, B112).
#
# It stays a LITERAL here rather than being read off the sheet: the dataLink leg below EXISTS to
# catch the value drifting, so deriving it from the dataLink would make that leg agree with
# whatever it found. One literal, three readers, and the drift check keeps its teeth.
$ROAD_DEFAULT = 'Path of Humanity'
# The names a saved sheet may still be holding that the road list no longer offers. They are the
# subject of V395 and each one goes the day nobody can be holding it any more - a decision with a
# date on it, not a tidy-up (SPEC I135d, V2). 'Humanity' is the pre-T906 default; 'Road of Heaven'
# left in T932 because it is not a Road at all, it is the umbrella over three Abrahamic paths and
# what the player picks is the path (SPEC Q58, I141d). It is a ROSTER and not one name because the
# second retirement landed one round after the first: a second scalar would have been a second
# copy of every leg below, which is the shape B70 cured on the default itself.
$ROAD_RETIRED = @('Humanity', 'Road of Heaven')
if ($roadItems329.Count -eq 0 -or $bearKeys329.Count -eq 0) { $v329Bad += "the road roster or BEARING came back EMPTY - both legs below would pass on nothing (SPEC V20)" }
else {
    foreach ($r329 in $roadItems329) { if (-not $bearKeys329.ContainsKey($r329)) { $v329Bad += "'$r329' is offered by the picker and has no BEARING row - the line would open blank, which is the failure only an eye catches (SPEC V329c)" } }
    foreach ($k329 in $bearKeys329.Keys) { if ($roadItems329 -notcontains $k329) { $v329Bad += "BEARING carries '$k329', which the picker never offers - a name nobody can reach (SPEC V329d)" } }
    foreach ($k329 in $bearKeys329.Keys) { if ($bearKeys329[$k329] -eq '') { $v329Bad += "BEARING['$k329'] is EMPTY - the five the book leaves unnamed were given names by the user on 2026-08-28, so a blank one is a row that got lost (SPEC I99c5)" } }
    if ($bearKeys329[$ROAD_DEFAULT] -ne 'Normalcy') { $v329Bad += "BEARING['$ROAD_DEFAULT'] is '$($bearKeys329[$ROAD_DEFAULT])', not the Normalcy core p.310 prints - it is the default road, so this is the row every new sheet shows (SPEC V329e, R121)" }
}

# (e) ROAD_ERA answers for the same 55 and nothing else, and Humanity is the one row that is
# not era-bound: an era whose list dropped it would leave the field holding no legal value.
$eraKeys329 = @{}
$et329 = [regex]::Match($rootLua, '(?s)\n\t{4}ROAD_ERA = \{(.*?)\n\t{4}\};')
if (-not $et329.Success) { $v329Bad += "ROAD_ERA could not be read - the era axis I99c2 declares has no table (SPEC V209)" }
else {
    foreach ($m in [regex]::Matches($et329.Groups[1].Value, '\["([^"]+)"\]\s*=\s*"([^"]*)"')) { $eraKeys329[$m.Groups[1].Value] = $m.Groups[2].Value }
    foreach ($r329 in $roadItems329) { if (-not $eraKeys329.ContainsKey($r329)) { $v329Bad += "'$r329' has no ROAD_ERA row - it would be filtered out of every era at once (SPEC I99c2)" } }
    foreach ($k329 in $eraKeys329.Keys) { if ($eraKeys329[$k329] -ne 'modern' -and $eraKeys329[$k329] -ne 'medieval') { $v329Bad += "ROAD_ERA['$k329'] is '$($eraKeys329[$k329])', which is neither of the two eras the .tsv measures (SPEC I99c2)" } }
}

# (f) the era axis is APPLIED, not merely declared. ROAD_ERA existing and nothing reading it is
# the born-dead shape of B75, and it would look exactly like a working feature: the dropdown
# just shows all 55 in every era and only an eye catches it.
$pi329 = [System.IO.File]::ReadAllText((Join-Path $dir "WoD20.6.lfm"))
if ($pi329 -notmatch 'root == "road" and raw ~= current') { $v329Bad += "pickerItems has no road branch - ROAD_ERA is declared and never read, so every era offers all 55 roads (SPEC I99c2, B75)" }
if ($pi329 -notmatch 'local roadEra = \(era == "Dark Ages" or era == "Classical Age"\)') { $v329Bad += "the two medieval themes are not named where the road list is filtered - a fifth theme would fall into one era by default instead of being decided (SPEC I99c2)" }
if ($pi329 -notmatch "raw == ""$ROAD_DEFAULT""") { $v329Bad += "$ROAD_DEFAULT is not exempted from the era filter - an era whose list drops it leaves the field holding a value its own list does not offer (SPEC I99c3)" }
# V330: ONE writer for the three states, and the predicate is `clanFamily` FILLED - not the
# revenant set, which Q1 retired. A leg that still looked for REVENANT would be measuring code
# that no longer exists, which is B7 wearing a green tick.
$rb330 = [regex]::Match($rootLua, '(?s)function renderBearing\(from\)(.*?)\n\t{3}end;')
if (-not $rb330.Success) { $v329Bad += "renderBearing is gone - nothing writes the bearing line and the two labels are born dead, which is exactly B75 (SPEC V330a)" }
else {
    $body330 = $rb330.Groups[1].Value
    foreach ($c330 in @('dynroad','dynBearingName')) {
        if (-not $body330.Contains($c330)) { $v329Bad += "renderBearing never touches '$c330' - one of the three states it is supposed to own has another writer or none (SPEC V330a)" }
    }
    if ($body330 -notmatch 'fam ~= nil and fam ~= ""') { $v329Bad += "renderBearing does not gate on clanFamily being FILLED - Q1 opened the road to clans, and a leg testing anything else is measuring the retired rule (SPEC I99d, V330b)" }
    if ($body330 -match 'REVENANT') { $v329Bad += "renderBearing still reads a REVENANT set - Q1 retired it as the gate, so this is the old predicate surviving next to the new one (SPEC I99d)" }
    if ($body330 -notmatch 'sheet\["humanity_" \.\. i\]') { $v329Bad += "renderBearing does not count the dots off the NDB - reading widgets scores a silent zero for a row not built yet (SPEC I99g)" }
}
# (c) the three controls have NO other toucher anywhere on the sheet. The write shape here is
# `found["cboRoad"].enabled` plus two local aliases, so a regex hunting `.visible` beside the
# name finds zero writes even INSIDE renderBearing and passes on the empty set - which is what
# the loop that stood here did: it counted into $writers330 and then asserted nothing at all
# (SPEC B7, V20). What is measured instead is every mention that is not the XML declaration,
# because reaching one of these controls means naming it first, and the one place allowed to
# name them is renderBearing (SPEC V330c, V199, B12).
$rootCode330 = CodeOf (Join-Path $dir "WoD20th.lfm")
$rbCode330   = [regex]::Match($rootCode330, '(?s)function renderBearing\(from\)(.*?)\n\t{3}end;')
# TWO owners since the 157th round, and they are NAMED - the list is closed, not a licence
# (SPEC V380f, I128f, T864). renderBearing owns what cboRoad SHOWS; renderRoadPair owns where
# it SITS, because the pair is centred as a group and the group's width moves with the
# language. That is the same split I128f already draws on the LABEL - the WoD20.6 traversal
# owns its text, Lua owns its geometry - and V330c's reason survives it: a second toucher is
# a second ruler only when the two decide the SAME thing. Content and position are not the
# same thing, and no third name is allowed in here without a round behind it.
$rpCode330   = [regex]::Match($rootCode330, '(?s)function renderRoadPair\(from\)(.*?)\n\t{3}end;')
$reach330    = 0
$own330      = 0
$where330    = @()
foreach ($nm330 in @('dynroad','dynBearingName')) {
    foreach ($lf330 in $files) {
        # the pattern goes to a VARIABLE first: `-replace 'a' + $x, ''` binds as one argument
        # and the replacement silently does not happen, with no error to show for it.
        $decl330 = 'name="' + $nm330 + '"'
        $k330 = ([regex]::Matches(((CodeOf $lf330.FullName) -replace $decl330, ''), [regex]::Escape($nm330))).Count
        if ($k330 -gt 0) { $reach330 += $k330; $where330 += "$($lf330.Name) names $nm330 $k330 time(s)" }
    }
    if ($rbCode330.Success) { $own330 += ([regex]::Matches($rbCode330.Groups[1].Value, [regex]::Escape($nm330))).Count }
    if ($rpCode330.Success) { $own330 += ([regex]::Matches($rpCode330.Groups[1].Value, [regex]::Escape($nm330))).Count }
}
if ($reach330 -eq 0) { $v329Bad += "not one of the three bearing control names survives into the Lua - either the comment stripper ate the code or all three were renamed, and every count here would agree on zero (SPEC V20, B7)" }
elseif ($reach330 -ne $own330) { $v329Bad += "the three bearing controls are named $reach330 time(s) in the sheet's Lua and only $own330 of those sit inside renderBearing ($($where330 -join '; ')) - a second toucher is a second ruler for one rule, and the two disagree on screen with nothing to say so (SPEC V330c, V199, B12)" }
$cnt330 = ([regex]::Matches($rootLua, 'renderBearing\(self\);')).Count
if ($cnt330 -lt 2) { $v329Bad += "renderBearing is called from $cnt330 dataLink(s) - clanFamily, road and the ten dots all have to reach it or a rating can move without the line following (SPEC V330d)" }
# V328b: the default is on a dataLink AND the value is `Humanity`. Two failures, and they read
# nothing alike - the wrong HOME kills the build with an empty message and no .rpk (SPEC B80),
# the wrong VALUE builds clean and opens every pre-125th sheet on a road nobody chose. One test
# for both answered "not on a dataLink" to a value drift and sent the reader after a dead build
# that had not happened (SPEC I99b, V328b).
$dl328 = [regex]::Match($rootLua, 'dataLink field="road" defaultValue="([^"]*)"')
if (-not $dl328.Success) { $v329Bad += "the road default is not on a dataLink - authored on the comboBox it is accepted by the XML and REJECTED by rdk, silently, with the .rpk deleted (SPEC I99b, B80)" }
elseif ($dl328.Groups[1].Value -ne $ROAD_DEFAULT) { $v329Bad += "the road dataLink defaults to '$($dl328.Groups[1].Value)', not the $ROAD_DEFAULT I99b declares - a character with no road IS on Humanity, and a sheet saved before the 125th round would open on a road it never walked (SPEC V328b)" }

if ($v329Bad) { foreach ($b in $v329Bad) { Fail "V329/V330 $b" } }
else { Pass "V329/V330 the ladder cuts at 10,8,4,2 with a bare last rung; BEARING and ROAD_ERA both answer for all $($roadItems329.Count) roads and nothing else; renderBearing owns all three states off one clanFamily test, is the only place on the sheet that names the three, and the road default sits on its dataLink at Humanity" }

# ---- V318: the line ending is CONTENT, and it is MEASURED --------------------------------
# SPEC B74, the 118th round. The Git Bash text tools rewrite a file WITHOUT its carriage
# returns - not on the line that matched, on the whole file - and four one-line edits turned a
# 3027-line CRLF SPEC.md into bare LF in silence. Three things hid it at once: git diff
# normalises under core.autocrlf=true and showed 11 changed lines rather than 3027, the Git Bash
# `cat -A` does not print ^M either, and rdk -l compiles LF without a word. There was no symptom
# at all, and the commit would have carried it.
#
# So the only signal that exists is the MEASUREMENT, and it is two counts per file. The 116th
# round already wrote this warning into HANDOFF.md for awk and for .lfm only; the real reach is
# every text tool in the shell and every CRLF file in the repo, which is why it is a check now
# and not a note.
$eolBad = @()
$eolSeen = 0
# HANDOFF.md joined the roster on 2026-09-02, and it joined because it BROKE: the head of it was
# rewritten from a here-string in LF while the tail stayed CRLF, and the file came out 6138 CR
# against 6214 LF with the gate green - B74 happening again in the one CRLF file at the root this
# check did not look at. It is read at the start of every round, so a diff that lies about it is
# the same cost as one lying about the spec (SPEC B74, I133).
foreach ($f318 in @($files.FullName) + @((Join-Path $PSScriptRoot 'verify-hunters-hunted.ps1'), (Join-Path $PSScriptRoot 'SPEC.md'), (Join-Path $PSScriptRoot 'HANDOFF.md'))) {
    if (-not (Test-Path $f318)) { $eolBad += "$(Split-Path $f318 -Leaf) is not where this check looks for it - it cannot be measured (SPEC V209)"; continue }
    $t318 = [IO.File]::ReadAllText($f318)
    $cr318 = ([regex]::Matches($t318, "`r")).Count
    $lf318 = ([regex]::Matches($t318, "`n")).Count
    $eolSeen++
    if ($lf318 -eq 0) { $eolBad += "$(Split-Path $f318 -Leaf) has no line break at all - it is not the file this check was written for" }
    elseif ($cr318 -ne $lf318) { $eolBad += "$(Split-Path $f318 -Leaf) carries $cr318 CR against $lf318 LF - a text tool rewrote it and ate the carriage returns, which git diff hides under autocrlf and rdk -l compiles without a word (SPEC B74)" }
}
if ($eolSeen -lt 15) { Fail "V318 only $eolSeen file(s) were measured, expected the 12 .lfm plus the gate, the spec and the handoff - this check is covering less than the repo has (SPEC V209). The .lfm were 15 until T909 (SPEC I136i)" }
elseif ($eolBad) { foreach ($b in $eolBad) { Fail "V318 $b" } }
else { Pass "V318 all $eolSeen source files are CRLF end to end - no text tool has eaten a carriage return" }

# ---- V319: the corrente BAR is the THIRD drawing family, and each style pays its own -------
# The 119th round. I88 gave the Dark Ages its bar through the same palette key that already
# chose its box frame; this does it for Modern Nights, and REVOKES I90h, which one round ago
# left that era's bar on the default drawing BY DECISION. Six legs, and (e) and (f) are the two
# that keep the sheet's first FILLED ink from surviving into an era that never asked for it.
$v319Bad  = @()
$path319  = LuaFn $hh6 'markPath'
$cor319   = LuaFn $hh6 'markCorrente'
$fil319   = LuaFn $hh6 'markFilete'
$rule319  = LuaFn $hh6 'markRule'
$corOwn319 = @('ORN_COR_MARK', 'ORN_COR_IN', 'ORN_COR_MARK2', 'ORN_COR_SPAN', 'ORN_COR_SUB2', 'ORN_COR_SUB_SPAN', 'ORN_COR_DOT')
$num319 = @{}
foreach ($k319 in ($corOwn319 + @('ORN_MARK2', 'ORN_PILLR', 'ORN_SUB_MARK'))) {
    $m319 = [regex]::Match($hh6, '(?m)^\s*local ' + $k319 + '\s*=\s*([\d.]+);')
    if ($m319.Success) { $num319[$k319] = [double]$m319.Groups[1].Value }
}
if (-not ($path319 -and $cor319 -and $fil319 -and $rule319)) {
    Fail "V319 markPath / markCorrente / markFilete / markRule could not all be read out of WoD20.6 - every leg below measures one of them, so all six would be no-ops (SPEC V209, V20)"
}
elseif ($num319.Count -ne 10) {
    Fail "V319 read $($num319.Count) of the 10 constants this invariant relates, expected 10 - the Lua side of these relations is gone, and a relation with one side is a no-op (SPEC V209, V20)"
}
else {
    $pathBody319 = NoComments $path319
    $corBody319  = NoComments $cor319
    $filBody319  = NoComments $fil319
    $ruleBody319 = NoComments $rule319

    # (a) the style is DISPATCHED to a drawing of its own, and it arrives as an ARGUMENT.
    # markCorrente is declared ABOVE the local that holds the era's style, so reading it there
    # compiles to a GETGLOBAL and finds nil - exit 0 and nothing to grep, which is B51.
    if ($pathBody319 -notmatch 'if style == "corrente" then return markCorrente') { $v319Bad += "markPath does not dispatch corrente to a drawing of its own - the era falls back to the default bar, which is exactly what I90h said and the 119th round revoked (SPEC I91a)" }
    if ($corBody319 -match '\bornStyleNow\b') { $v319Bad += "markCorrente reads ornStyleNow directly, and that local is declared BELOW it - the read compiles to a GETGLOBAL and answers nil, with exit 0 and nothing to grep (SPEC I91a, V223, B51)" }

    # (b) the topmost ink does not RISE. In the default style it is the upper rule, at
    # h - ORN_MARK2 = 21, which is the state the user approved on screen in the 112th round.
    # Here the main rule IS the topmost ink, so one side is enough - and unlike V315c there is
    # no second owner of the 21 left to age (SPEC I91h, B70, B68).
    if ($num319['ORN_COR_MARK'] -gt $num319['ORN_MARK2']) { $v319Bad += "ORN_COR_MARK ($($num319['ORN_COR_MARK'])) is above ORN_MARK2 ($($num319['ORN_MARK2'])) - the corrente bar has climbed nearer the words than the drawing the user approved in the 112th round ever was (SPEC I91h, V307a, B68)" }

    # (c) the hierarchy holds on BOTH axes: the tab's main rule runs longer than the sub-tab's
    # and its second rule is wider. One axis alone leaves the two levels telling themselves
    # apart by a single number, which is I78b's hierarchy on one leg (SPEC I91b, I91c).
    $foot319 = [Math]::Sqrt(($num319['ORN_PILLR'] + $num319['ORN_SUB_MARK']) * ($num319['ORN_PILLR'] + $num319['ORN_SUB_MARK']) - $num319['ORN_SUB_MARK'] * $num319['ORN_SUB_MARK'])
    if ($num319['ORN_COR_IN'] -ge $foot319) { $v319Bad += "ORN_COR_IN ($($num319['ORN_COR_IN'])) is not inside the sub-tab's foot ($([Math]::Round($foot319, 2))) - the tab's main rule has stopped being the longer of the two and the levels read the same (SPEC I91b, V319c)" }
    if ($num319['ORN_COR_SPAN'] -ge $num319['ORN_COR_SUB_SPAN']) { $v319Bad += "ORN_COR_SPAN ($($num319['ORN_COR_SPAN'])) is not less than ORN_COR_SUB_SPAN ($($num319['ORN_COR_SUB_SPAN'])) - the tab's second rule has stopped being the wider one, and equal spans lose the hierarchy with nothing anywhere to say so (SPEC I91c, V315d)" }

    # (d) the floor is READ, never spelled, and there is ONE per motif - the main rule's. The
    # second rule is PROPORTIONAL, so it cannot run out of room and has no floor to write: a
    # guard that CANNOT fire is B59's hole (SPEC I91g, V314e, V315e).
    $wTop319 = @(); $wSub319 = @(); $sepN319 = 0
    foreach ($pr319 in @(,@("WoD20th.lfm", "tabStrip", $true))) {
        $st319 = (Doc (Join-Path $dir $pr319[0])).SelectSingleNode("//layout[@name='$($pr319[1])']")
        if ($null -eq $st319) { $v319Bad += "$($pr319[1]) is gone from $($pr319[0]) - the bar this invariant measures does not exist (SPEC I32, I58, V209)"; continue }
        foreach ($r319 in $st319.SelectNodes("rectangle[starts-with(@name,'tabOn')]")) {
            if ($pr319[2]) { $wTop319 += [double]$r319.GetAttribute("width") } else { $wSub319 += [double]$r319.GetAttribute("width") }
        }
        $sepN319 += @($st319.SelectNodes("rectangle[starts-with(@name,'sep')]")).Count
    }
    if (($wTop319.Count + $wSub319.Count) -ne 11) { $v319Bad += "read $($wTop319.Count + $wSub319.Count) marker(s) across the one bar left, expected 11 - this check is covering less than the bars hold (SPEC V209)" }
    else {
        $minAll319 = (($wTop319 + $wSub319) | Measure-Object -Minimum).Minimum
        $minSub319 = ($wSub319 | Measure-Object -Minimum).Minimum
        if ($minAll319 -le (2 * $num319['ORN_COR_IN'])) { $v319Bad += "the narrowest marker is $minAll319 against a floor of $(2 * $num319['ORN_COR_IN']) - at that width the corrente tab draws nothing at all, and a bar that quietly disappears is not the one anybody chose (SPEC I91g, V279, B59)" }
        if ($wSub319.Count -gt 0 -and $minSub319 -le (2 * $foot319)) { $v319Bad += "the narrowest sub-tab is $minSub319 against a floor of $([Math]::Round(2 * $foot319, 2)) - its main rule would be drawn end before start, so the marker refuses and the level loses its mark (SPEC I91g, V279, B59)" }
    }
    if ($sepN319 -ne 0) { $v319Bad += "read $sepN319 separator carrier(s) - sep<X> retired with the sub-tab strips in T830 and a carrier back on the bar is a separator between names that do not exist (SPEC I78d, I91d, V209)" }

    # (e) the FILL has ONE owner and it is the DRAWING. markPath and markFilete return the path
    # alone, markCorrente returns a second value in its `sep` branch and nowhere else, and
    # markRule spells neither a style nor a motif of its own - it only applies what it is
    # handed. Two owners of "who is filled" is B69 in a third place (SPEC I91e).
    if ($ruleBody319 -match '"corrente"') { $v319Bad += "markRule spells the corrente style - the painter has taken a second opinion on a question the drawing already answers, which is the sharing B69 is made of (SPEC I91e)" }
    if ($ruleBody319 -match '"sep"') { $v319Bad += "markRule spells the sep motif - it is markKind that resolves the level and the drawing that decides the fill, and a third reader of either is one that can disagree (SPEC I91e)" }
    if ($filBody319 -match ', true') { $v319Bad += "markFilete returns a second value - the filete style has taken up the fill flag, and the Dark Ages cross would be painted solid (SPEC I91e)" }
    if ($pathBody319 -match ', true') { $v319Bad += "markPath returns a second value of its own - the fill would arrive from the dispatcher instead of the drawing, and every era would carry it (SPEC I91e)" }
    $iSep319 = $corBody319.IndexOf('if kind == "sep" then')
    $iSub319 = $corBody319.IndexOf('if kind == "sub" then')
    $iTab319 = $corBody319.IndexOf('local s = w * ORN_COR_SPAN')
    if ($iSep319 -lt 0 -or $iSub319 -le $iSep319 -or $iTab319 -le $iSub319) {
        $v319Bad += "markCorrente does not hold the sep, sub and tab motifs in that order - the slices this leg reads cannot be cut, so the per-motif half of it would measure nothing (SPEC I91b, V209, V20)"
    }
    else {
        $sl319 = @(
            @{ name = 'sep'; text = $corBody319.Substring($iSep319, $iSub319 - $iSep319); forbid = @('ORN_COR_MARK', 'ORN_COR_MARK2', 'ORN_COR_IN', 'ORN_COR_SPAN', 'ORN_COR_SUB2', 'ORN_COR_SUB_SPAN', 'ORN_SUB_MARK') },
            @{ name = 'sub'; text = $corBody319.Substring($iSub319, $iTab319 - $iSub319); forbid = @('ORN_COR_MARK', 'ORN_COR_MARK2', 'ORN_COR_IN', 'ORN_COR_SPAN', 'ORN_COR_DOT') },
            @{ name = 'tab'; text = $corBody319.Substring($iTab319); forbid = @('ORN_COR_SUB2', 'ORN_COR_SUB_SPAN', 'ORN_COR_DOT', 'ORN_SUB_MARK') }
        )
        foreach ($s319 in $sl319) {
            foreach ($b319 in $s319.forbid) {
                if ($s319.text -match ("\b" + [regex]::Escape($b319) + "\b(?!_|\d)")) { $v319Bad += "the corrente $($s319.name) motif reads $b319, which belongs to another motif - that sharing is how the 110th round moved three levels when it was asked to move one (SPEC I91c, V308, B69)" }
            }
        }
        if ($sl319[0].text -notmatch ', true') { $v319Bad += "the corrente sep motif does not return the fill flag - the dot comes out as an outline and the user's choice of a SOLID one is undone with exit 0 and the gate green (SPEC I91d, I91e)" }
        if ($sl319[1].text -match ', true') { $v319Bad += "the corrente sub motif returns the fill flag - a sub-tab's rules would be filled, and filling an open run is ink nobody asked for (SPEC I91e)" }
        if ($sl319[2].text -match ', true') { $v319Bad += "the corrente tab motif returns the fill flag - the top bar's rules would be filled, and filling an open run is ink nobody asked for (SPEC I91e)" }
    }

    # (f) the memo CARRIES the fill and markRule writes the colour on every repaint. Nailed at
    # creation, `color` survives a swap of era on an OPEN sheet and leaves the next era's cross
    # filled - rdk -l exit 0, gate green, and only the screen left to tell (SPEC I91f, B58, B62).
    if ($ruleBody319 -notmatch 'local d, f = markPath\(bw, bh, kind, ornStyleNow\)') { $v319Bad += "markRule does not take the fill flag back from markPath at CREATION - the memo would carry nil and the dot would never be filled (SPEC I91e, I91f)" }
    if ($ruleBody319 -notmatch 'local d, f = markPath\(c\.width, c\.height, e\.k, ornStyleNow\)') { $v319Bad += "markRule does not take the fill flag back from markPath when the era CHANGES - the memo would keep the previous era's answer for ever (SPEC I91f)" }
    if ($ruleBody319 -notmatch 'f = f, s = ornStyleNow') { $v319Bad += "the creation entry does not stamp the fill flag with s still LAST - either the memo has lost the flag, or the stamp has moved off the brace V315f tells the creation by (SPEC I91f, V315f)" }
    if ($ruleBody319 -notmatch 'e\.f = f') { $v319Bad += "markRule never writes the new fill flag back into the memo after redrawing - the entry would answer for the PREVIOUS era's drawing (SPEC I91f, V284)" }
    if ($ruleBody319 -notmatch 'e\.p\.color = colour') { $v319Bad += "markRule never paints the fill - the dot is drawn and left hollow, which is the drawing the user did NOT pick (SPEC I91d, I91f)" }
    if ($ruleBody319 -match '(?<![\w.])p\.color') { $v319Bad += "markRule nails p.color shut at creation - swapping era on an OPEN sheet then leaves the next era's separator FILLED, with exit 0, the gate green and only the screen to tell (SPEC I91f, B58, B62)" }

    if ($v319Bad) { foreach ($b in $v319Bad) { Fail "V319 $b" } }
    else { Pass "V319 the corrente bar is a drawing of its own, its seven constants stay inside their motifs, its ink tops out no higher than $($num319['ORN_MARK2']) from the foot, and the fill comes from the drawing and is repainted every time" }
}
# ---- V320: the AVATAR wears the era's shape by a DECLARED roster, and ONE question says
# who DRAWS (SPEC I92, the 120th round) ------------------------------------------------------
# The avatar is the one thing on the sheet that is shaped like a section box and is not one:
# its frame is DimGray and its carrier is transparent, so the construction test of V278 refuses
# both and Modern Nights left it with the concave bite while the 73 wore the cut corner. The arm
# that lets it in cannot be a rule about SHAPE - by construction the carrier IS the nineteen
# tabOn<X> pills - so it is a roster of two names, and every leg below reads its other side out
# of the Lua or out of the XML rather than spelling it here.
$v320Bad = @()

$sbFn320    = LuaFn $hh6 'sectionBox'
$ornFn320   = LuaFn $hh6 'ornament'
$sbBody320  = if ($sbFn320)  { NoComments $sbFn320 }  else { '' }
$ornBody320 = if ($ornFn320) { NoComments $ornFn320 } else { '' }

$themeM320  = [regex]::Match($hh6, 'local function applyTheme\(v, from\)(.*?)\n\t\t\tend;', 'Singleline')
$themeB320  = if ($themeM320.Success) { NoComments $themeM320.Groups[1].Value } else { '' }

$roster320  = @('avatarFrame', 'ornAvatar')
$frame320   = $mainDocV69.SelectSingleNode("//rectangle[@name='avatarFrame']")
$carrier320 = $mainDocV69.SelectSingleNode("//rectangle[@name='ornAvatar']")
$img320     = $mainDocV69.SelectSingleNode("//image[@field='avatar']")

$allLfm320  = (Get-ChildItem (Join-Path $dir '*.lfm') | ForEach-Object { [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($_.FullName)) }) -join "`n"
$tabOn320   = ([regex]::Matches($allLfm320, 'name="tabOn')).Count

# Zero-guard FIRST: every leg under this reads the selector, the two rectangles or the pills,
# and a leg that reads nothing passes for the wrong reason (SPEC V209, V20).
if (-not $sbFn320)          { $v320Bad += "sectionBox is not in WoD20.6 - the selector this whole block measures does not exist (SPEC V209)" }
if (-not $ornFn320)         { $v320Bad += "ornament() could not be read - leg (c) cannot tell which answer it takes and is a no-op (SPEC V209, V20)" }
if (-not $themeM320.Success){ $v320Bad += "applyTheme could not be read - the corner write is unmeasurable and leg (c) is half blind (SPEC V209, V20)" }
if ($null -eq $frame320)    { $v320Bad += "no rectangle named avatarFrame in WoD20.1 - the roster points at a control that is gone, which is a check that goes quiet on the thing it guards (SPEC V209, V320a, B7)" }
if ($null -eq $carrier320)  { $v320Bad += "no rectangle named ornAvatar in WoD20.1 - same hole, and this is the one that DRAWS (SPEC V209, V320a, B7)" }
if ($null -eq $img320)      { $v320Bad += "the avatar image is gone from WoD20.1 - leg (d) has nothing to sit the two rectangles around (SPEC V209)" }
if ($tabOn320 -lt 11)       { $v320Bad += "only $tabOn320 tabOn<X> carriers were read and there are 11 - leg (b) measures the collision this roster exists to avoid, so reading fewer is measuring less than the sheet has (SPEC V209, V20)" }

# (a) the arm cites EXACTLY the two names, and both are in the XML above. A roster that names a
# control nobody authored is green over a hole; a roster that grew a third name quietly is a
# selector nobody declared.
$cited320 = @([regex]::Matches($sbBody320, 'nm\s*==\s*"([^"]+)"') | ForEach-Object { $_.Groups[1].Value })
if ((($cited320 | Sort-Object) -join ',') -ne (($roster320 | Sort-Object) -join ',')) {
    $v320Bad += "sectionBox's name arm cites [$($cited320 -join ', ')] and the declared roster is [$($roster320 -join ', ')] - the two sides have to be the same list, or the selector answers for a control the spec never let in (SPEC V320a, I92a)"
}

# (b) by NAME and never by CONSTRUCTION. The carrier is transparent both ways with its own
# radius and hitTest false - the four properties of every tabOn<X> - so a fill-based arm bevels
# the nineteen pills, whose width is priced for the concave arc (SPEC V228, V316d). This is the
# leg the "swap the roster for a shape test" mutation has to turn red.
if ($sbBody320 -match '#00000000') {
    $v320Bad += "sectionBox tests the TRANSPARENT fill - that is construction, not a roster, and it lets in all $tabOn320 tabOn<X> carriers along with the avatar's, bevelling pills whose width was priced for the arc (SPEC V320b, V228, V316d)"
}
if ($sbBody320 -match 'tabOn') {
    $v320Bad += "sectionBox mentions tabOn - the selector is reasoning about the tab pills, which means the roster stopped being about the avatar (SPEC V320b)"
}
if ($sbBody320 -notmatch 'getName\(\)') {
    $v320Bad += "sectionBox never asks the control its NAME, so whatever arm it grew is not the declared roster of I92a (SPEC V320b)"
}

# (c) ONE selector, TWO answers. The corner write takes the FIRST, ornament() takes the SECOND,
# and neither of them may reason about a name on its own - a second copy of "which rectangles
# are the boxes" is what V67 pays not to have (SPEC I89b, I92b).
if (([regex]::Matches($hh6, 'local function sectionBox')).Count -ne 1) {
    $v320Bad += "sectionBox is declared $((([regex]::Matches($hh6, 'local function sectionBox')).Count)) times - there is exactly one question about who the boxes are (SPEC V320c, V67, I89b)"
}
if ($sbBody320 -notmatch 'return true, true') {
    $v320Bad += "sectionBox has no `"return true, true`" - the pair of answers I92b is built on is not there, so nothing distinguishes wearing the shape from receiving the drawing (SPEC V320c)"
}
if ($sbBody320 -notmatch 'return true, false') {
    $v320Bad += "sectionBox never answers `"wears but does not draw`" - the frame would take a path of its own, and it sits UNDER the image, so that path lands behind the photo (SPEC V320c, I92c, B75)"
}
if ($ornBody320 -notmatch 'local _, draws = sectionBox\(c, fill\)') {
    $v320Bad += "ornament() does not take the SECOND answer from sectionBox - reading the first would hang a path on the avatar frame, behind the photo, which is B75 all over again (SPEC V320c, I92b)"
}
if ($ornBody320 -notmatch 'if not draws then return; end;') {
    $v320Bad += "ornament() does not refuse on the second answer - whatever it refuses on now, it is not the one that says who draws (SPEC V320c)"
}
if ($themeB320 -notmatch 'if sectionBox\(c, fill\) then') {
    $v320Bad += "the corner write no longer reads sectionBox's FIRST answer - either it stopped asking, or it started asking a second question of its own (SPEC V320c, V316d)"
}
foreach ($nm320 in $roster320) {
    if ($ornBody320 -match [regex]::Escape($nm320)) { $v320Bad += "ornament() names $nm320 itself - the roster has a second copy, and two lists of who the avatar is will drift (SPEC V320c, V67)" }
    if ($themeB320 -match [regex]::Escape($nm320)) { $v320Bad += "the theme walk names $nm320 itself - same second copy, on the corner side this time (SPEC V320c, V67)" }
}

# (d) the one that DRAWS is the one on TOP, and that is POSITION IN THE FILE rather than a name:
# the frame is declared BEFORE the image and the carrier AFTER it, so the path the carrier gets
# lands over the photo. Inverted, the drawing goes behind the picture with exit 0, a green gate
# and only the screen to tell - which is exactly B75.
$posFrame320 = $mainRawForMap.IndexOf('name="avatarFrame"')
$posImg320   = $mainRawForMap.IndexOf('field="avatar"')
$posCarr320  = $mainRawForMap.IndexOf('name="ornAvatar"')
if ($posFrame320 -lt 0 -or $posImg320 -lt 0 -or $posCarr320 -lt 0) {
    $v320Bad += "one of the avatar's three controls could not be found in the raw XML, so the stacking order is unmeasurable (SPEC V209, V320d)"
} else {
    if ($posFrame320 -gt $posImg320) { $v320Bad += "avatarFrame is declared AFTER the image - the grey letterbox would paint over the photo (SPEC V320d, I92c)" }
    if ($posCarr320 -lt $posImg320)  { $v320Bad += "ornAvatar is declared BEFORE the image - its path would land BEHIND the photo, drawn and invisible, which is B75 in one line (SPEC V320d, B75, I92c)" }
}

# (e) the frame keeps the shape V309 collects it by. The name is new; the DimGray fill is what
# V309's collector matches on, and losing it would take the avatar out of the check that exists
# because the frame spent three rounds thirty pixels behind the photo (SPEC B71).
if ($null -ne $frame320 -and $frame320.GetAttribute('color') -ne 'DimGray') {
    $v320Bad += "avatarFrame no longer authors color='DimGray' - V309 collects it by that fill, so renaming it out of the collector is B71 waiting to happen again (SPEC V320e, V309, V53)"
}

# (f) FOUR palettes declare ornament. AMENDED by the 122nd round (SPEC V322e): it read THREE,
# and the Classical Age was the one staying clean. It now declares an ornament of its own, so the
# count moves and the leg does NOT go away: what it measures is that the number of ornamented eras
# is DECLARED and never accidental, which is what stops a fifth from arriving unannounced. The
# avatar itself draws in NO era since V321 - this count is now about the 73 boxes.
# The original reason still stands: this leg exists so that "the era did not change" cannot
# Classical Age" cannot become a bug report in a later round - which is precisely what T695(4)
# became once Modern Nights grew an ornament of its own.
$ornDecl320 = ([regex]::Matches($hh6, '(?m)^\s*ornament\s*=\s*"#[0-9A-Fa-f]{6,8}"')).Count
if ($ornDecl320 -ne 4) {
    $v320Bad += "$ornDecl320 palette(s) declare ornament and the sheet is built around 4 - every era declares its own frame since the 122nd round, so a fifth is a drawing in an era nobody asked for and a third is one era losing it (SPEC V320f, V322e, V277)"
}

if ($v320Bad) { foreach ($b in $v320Bad) { Fail "V320 $b" } }
else { Pass "V320 the avatar wears the era's shape by a roster of $($roster320.Count) declared names, one selector answers both who wears and who draws, the carrier is the control on top, and $ornDecl320 palettes declare ornament" }

# ---- V321: NO avatar control receives a path, and the PAIR of answers survives (SPEC I93,
# the 121st round) ---------------------------------------------------------------------------
# The 120th round let the carrier draw, and the drawing came out over the whole tab: ornament()
# hangs its path on the control's PARENT with align="client" (SPEC I72c), so the geometry comes
# from the parent and not from the control. The 73 boxes fill their layout; the carrier is a
# loose rectangle in the tab's scrollBox and fills nothing. That is B76, and it is invisible to
# rdk -l and to every check above - the position only exists at runtime. What CAN be measured is
# the answer the selector gives, so that is what this pins.
$v321Bad = @()

$sbFn321    = LuaFn $hh6 'sectionBox'
$ornFn321   = LuaFn $hh6 'ornament'
$sbBody321  = if ($sbFn321)  { NoComments $sbFn321 }  else { '' }
$ornBody321 = if ($ornFn321) { NoComments $ornFn321 } else { '' }
$roster321  = @('avatarFrame', 'ornAvatar')

# Zero-guard FIRST (SPEC V209, V20).
if (-not $sbFn321)  { $v321Bad += "sectionBox is not in WoD20.6 - every leg here measures its answers and there are none (SPEC V209)" }
if (-not $ornFn321) { $v321Bad += "ornament() could not be read - leg (c) counts its parent writes and would count zero for the wrong reason (SPEC V209, V20)" }

# (a) each roster name is cited on a line that answers WEARS BUT DOES NOT DRAW. Both sides are
# read: the name has to be in the arm, and the arm it is in has to say `return true, false`.
foreach ($nm321 in $roster321) {
    $arm321 = [regex]::Match($sbBody321, '(?m)^.*nm\s*==\s*"' + [regex]::Escape($nm321) + '".*$')
    if (-not $arm321.Success) {
        $v321Bad += "the roster name $nm321 is not cited in sectionBox at all - the arm this leg measures is gone, and a check that goes quiet on a missing target is B7 waiting (SPEC V209, V321a)"
    } elseif ($arm321.Value -notmatch 'return true, false') {
        $v321Bad += "sectionBox answers DRAWS for $nm321 - ornament() would hang a path on it, and because the path takes its geometry from the PARENT that path covers the whole tab, not the control (SPEC V321a, I93b, B76)"
    }
}

# (b) the PAIR survives the removal. The question "who draws" still exists - today the 73 are
# the whole answer - and collapsing sectionBox back to one value leaves a selector that cannot
# say no the next time a loose control joins the roster.
if ($sbBody321 -notmatch 'return true, true') {
    $v321Bad += "sectionBox no longer answers `"wears and draws`" for anyone - the 73 boxes are that answer, so this is the section boxes losing their own frame (SPEC V321b, I93a)"
}
if ($ornBody321 -notmatch 'local _, draws = sectionBox\(c, fill\)') {
    $v321Bad += "ornament() stopped taking the SECOND answer - with the pair gone it would draw for whoever merely WEARS the shape, which is B76 by another door (SPEC V321b, V320c)"
}

# (c) ONE creation path in ornament(), and it is the parent. markRule has its own
# (p:setParent(c)) and is another painter; copying it in here is the second branch I93b refuses
# and the second creation path V67 pays not to have.
$setP321 = [regex]::Matches($ornBody321, 'p:setParent\(')
if ($setP321.Count -ne 1) {
    $v321Bad += "ornament() has $($setP321.Count) p:setParent( calls and there is exactly one - a second creation path is how the avatar gets its drawing back through the door I93b closed (SPEC V321c, V67)"
}
elseif ($ornBody321 -notmatch 'p:setParent\(c\.parent\)') {
    $v321Bad += "ornament() no longer parents the path to c.parent - the rule of I72c is what puts the drawing above the box's own fields, and changing it silently moves every one of the 73 frames (SPEC V321c, I72c)"
}

if ($v321Bad) { foreach ($b in $v321Bad) { Fail "V321 $b" } }
else { Pass "V321 neither of the $($roster321.Count) avatar controls receives a path, the pair of answers still separates wearing from drawing, and ornament() keeps its single parent write" }


# ---- V322: the CORONA MURALIS draws on the 73 and the Classical era stops being the empty
# one (SPEC I94, the 122nd round) -------------------------------------------------------------
# Of the four palettes the Classical was the ONLY one declaring no ornament, so V277 hid its
# path and its boxes were bare outline. Five legs: the palette declares both keys and the colour
# is one it already carried; the fourth branch exists and does NOT take the radius; the size
# guard drops the MERLON and never the rule; the constants stay inside their own family; and
# exactly four palettes now declare an ornament.
$v322Bad = @()

$muralFam322 = ""
foreach ($fn322 in @("ornCoronaMuralis", "ornMuralCorner", "ornMuralEdge", "ornMuralFoot")) { $muralFam322 += (LuaFn $hh6 $fn322) }
$famNC322  = NoComments $muralFam322
$pathNC322 = NoComments (LuaFn $hh6 "ornPath")

# zero-guard: a leg that reads nothing is a no-op that passes by vacancy (SPEC V20, V209, B7)
if (-not $muralFam322) { $v322Bad += "the corona muralis family is gone from WoD20.6 - there is no drawing left to measure (SPEC V209, V322)" }
if (-not $pathNC322)   { $v322Bad += "ornPath is gone from WoD20.6 - nothing dispatches a style at all (SPEC V209)" }

# (a) the palette declares BOTH keys, and the colour is one the palette ALREADY carried. A
# style with no colour is a key that can never draw and never complain (V313a, V277); a colour
# from outside the palette is the loose colour V53 exists to refuse, arriving by the one door
# nobody watches - the ornament.
$pal322 = [regex]::Match($themesBlock, "(?ms)^\t{4}\[""Classical Age""\] = \{(.*?)^\t{4}\},")
if (-not $pal322.Success) { $v322Bad += "the Classical Age palette could not be read - leg (a) measured nothing (SPEC V209, V322a)" }
else {
    $pb322 = NoComments $pal322.Groups[1].Value
    $st322 = [regex]::Match($pb322, "(?m)^\t{5}ornStyle\s*=\s*""([^""]+)""")
    $or322 = [regex]::Match($pb322, "(?m)^\t{5}ornament\s*=\s*""(#[0-9A-Fa-f]{6,8})""")
    $sb322 = [regex]::Match($pb322, "(?ms)^\t{5}stroke = \{(.*?)^\t{5}\},")
    $sw322 = [regex]::Match($sb322.Groups[1].Value, "\[""white""\]\s*=\s*""(#[0-9A-Fa-f]{6,8})""")

    if (-not $st322.Success) { $v322Bad += "the Classical Age palette declares no ornStyle - the era falls back to the default filigree and the round drew nothing (SPEC V322a, I94a)" }
    elseif ($st322.Groups[1].Value -ne "corona-muralis") { $v322Bad += "the Classical Age palette draws $($st322.Groups[1].Value) and the round is built on corona-muralis (SPEC V322a, I94a)" }
    if (-not $or322.Success) { $v322Bad += "the Classical Age palette declares ornStyle and no ornament - the colour is what hides the path (V277), so the style is a key that can never draw and never complain (SPEC V322a, V313a)" }
    elseif (-not $sw322.Success) { $v322Bad += "the Classical Age stroke has no white key - leg (a) has nothing to compare the ornament colour against (SPEC V209, V322a)" }
    elseif ($or322.Groups[1].Value -ne $sw322.Groups[1].Value) {
        $v322Bad += "the Classical ornament is $($or322.Groups[1].Value) and the palette outline is $($sw322.Groups[1].Value) - a colour the palette did not already carry is a loose colour arriving through the ornament, which is the door V53 exists to shut (SPEC V322a, V53)"
    }
}

# (b) the FOURTH branch, and it does NOT forward the radius. The V317a check derives from the
# CODE which style wants a cut corner, by reading which branch carries ornPath radius - hand it
# over here and the gate starts DEMANDING a boxCorner of the Classical palette, which by V317a
# it cannot have. The failure would land one level away from whoever caused it, which is B6.
$sig322 = [regex]::Match($hh6, "local function ornPath\(([^)]*)\)")
$rad322 = ""
if ($sig322.Success) { $sigA322 = $sig322.Groups[1].Value -split "\s*,\s*"; $rad322 = $sigA322[$sigA322.Count - 1] }
$br322 = [regex]::Match($pathNC322, "if style == ""corona-muralis"" then return (\w+)\(([^)]*)\); end;")

if (-not $br322.Success) { $v322Bad += "ornPath has no corona-muralis branch - the palette names a style the dispatcher never heard of, so the era silently draws the default filigree (SPEC V322b, I94b)" }
elseif ($br322.Groups[1].Value -ne "ornCoronaMuralis") { $v322Bad += "the corona-muralis branch calls $($br322.Groups[1].Value) and the drawing is ornCoronaMuralis (SPEC V322b)" }
elseif ($rad322 -and $br322.Groups[2].Value -notmatch ("\b" + [regex]::Escape($rad322) + "\b")) {
    # INVERTED by the 123rd round (SPEC V324b). It used to REFUSE the radius here, and that was
    # what held the Classical era without a corner of its own. Now the palette declares one, so
    # the branch has to carry the radius: drop it and the boxCorner above goes orphan, V317a own
    # derivation stops seeing this style as one that wants a corner, and the failure lands one
    # level away from whoever caused it - the B6 family.
    $v322Bad += "the corona-muralis branch does not forward $rad322 - the palette declares a boxCorner and the drawing would be built to the corner the XML authored instead of the one the era writes, with rdk -l exiting 0 (SPEC V324b, V322b, I96c)"
}

# (c) what a box without room drops is the MERLON, never the RULE. A box skipped in silence
# between ornamented neighbours is B59 in one line - that is how TRUE FAITH (1270x76) fell. The
# floor is READ out of the Lua and measured against the smallest box the XML actually authors,
# so neither can drift from the other (same shape as V314e and V315e).
$c322 = @{}
foreach ($k322 in @("ORN_MUR_OUT", "ORN_MUR_IN", "ORN_MUR_STEP", "ORN_MUR_RISE")) {
    $m322 = [regex]::Match($hh6, "(?m)^\s*local $k322\s*=\s*([\d.]+);")
    if ($m322.Success) { $c322[$k322] = [double]$m322.Groups[1].Value }
}
$boxrM322 = [regex]::Match($hh6, "(?m)^\s*local ORN_BOXR\s*=\s*(\d+);")

if ($c322.Count -ne 4) { $v322Bad += "only $($c322.Count) of the 4 ORN_MUR_* constants could be read - leg (c) has no floor to compute (SPEC V209, V322c)" }
elseif (-not $boxrM322.Success) { $v322Bad += "ORN_BOXR could not be read - the merlon foot cannot be computed (SPEC V209, V322c)" }
elseif ($famNC322 -match "return """";") {
    $v322Bad += "the corona muralis refuses a whole box by size - the RULE is what never drops, so a refusal at that level can only ever be the silent skip of B59 (SPEC V322c, V314e)"
}
elseif ($famNC322 -notmatch "if n < 1 then return; end;") {
    $v322Bad += "the merlon has no size refusal at all - a guard that CANNOT fire is the hole B59 came through, and the crenellation would be drawn end before start (SPEC V322c, V20, V279)"
}
else {
    $ftM322 = [Math]::Sqrt([Math]::Pow([double]$boxrM322.Groups[1].Value + $c322["ORN_MUR_OUT"], 2) - [Math]::Pow($c322["ORN_MUR_OUT"], 2))
    $flr322 = 2 * $ftM322 + $c322["ORN_MUR_STEP"]
    # The dimensions are the BOXES' own, read the way V239 and V400 read a box - a layout with a
    # backdrop - and no longer the backdrop RECTANGLE's width and height. Those were the same
    # number only while nine boxes authored a SIZED backdrop instead of align=client, and T917
    # gave the last of them align=client: this leg then read ZERO (SPEC B135, B132, V400).
    #
    # It was never measuring what its own message claims. Nine boxes out of sixty-seven carried a
    # sized backdrop, and the sentence says "the smallest box side the XML authors" - the other
    # fifty-eight were never in the minimum, so the merlon floor was cleared by a sample nobody
    # chose. Reading the boxes is what the rule always meant (SPEC V209).
    $dim322 = @()
    foreach ($f in $files) {
        foreach ($bx322 in (Doc $f.FullName).SelectNodes("//layout[@width][@height]")) {
            $bw322 = -1; $bh322 = -1
            if (-not [int]::TryParse($bx322.GetAttribute("width"), [ref]$bw322)) { continue }
            if (-not [int]::TryParse($bx322.GetAttribute("height"), [ref]$bh322)) { continue }
            if ($bw322 -le 0 -or $bh322 -le 0) { continue }
            $back322 = $false
            foreach ($k322 in $bx322.ChildNodes) {
                if ($k322.NodeType -ne 'Element' -or $k322.LocalName -ne 'rectangle') { continue }
                if ($k322.GetAttribute("color") -ne 'black' -or -not $k322.HasAttribute("xradius")) { continue }
                $kl322 = -1; $kt322 = -1
                [void][int]::TryParse($k322.GetAttribute("left"), [ref]$kl322)
                [void][int]::TryParse($k322.GetAttribute("top"), [ref]$kt322)
                if ($k322.GetAttribute("align") -eq 'client' -or ($kl322 -eq 0 -and $kt322 -eq 0)) { $back322 = $true; break }
            }
            if (-not $back322) { continue }
            $dim322 += [double]$bw322
            $dim322 += [double]$bh322
        }
    }
    if ($dim322.Count -lt 100) { $v322Bad += "only $($dim322.Count) box dimension(s) were read for leg (c), expected at least the 100 the boxes of the sheet carry - it is measuring less than the sheet has (SPEC V209, V20)" }
    else {
        $minD322 = ($dim322 | Measure-Object -Minimum).Minimum
        if ($minD322 -le $flr322) {
            $v322Bad += "the smallest box side the XML authors is $minD322 against a merlon floor of $([Math]::Round($flr322, 2)) - at that size the crenellation drops and a box wearing only half the crown is not the one anybody chose (SPEC V322c, V279, B59)"
        }
    }
}

# (d) constants stay inside their own family (SPEC V308, B69): a number serving two drawings
# moves both when only one of them was asked for. ORN_OUT and ORN_IN (5 and 9) belong to the
# filigree and the filete, and the merlon has four of its own.
$oldFam322 = ""
foreach ($fn322 in @("ornFiligree", "ornFilete", "ornFileteCorner", "ornFileteEdge", "ornCorrente")) { $oldFam322 += (LuaFn $hh6 $fn322) }
$oldNC322 = NoComments $oldFam322

if ($famNC322 -match "\bORN_OUT\b|\bORN_IN\b") { $v322Bad += "the corona muralis reads ORN_OUT or ORN_IN - those are the filigree and filete rules, and one rule serving two drawings moves both when only one was asked for (SPEC V322d, V308, B69)" }
if ($oldNC322 -match "\bORN_MUR_") { $v322Bad += "the filigree or filete family reads an ORN_MUR_* constant - the merlon owns those four, and a shared owner is B69 in the other direction (SPEC V322d, V308)" }
if (-not $oldNC322) { $v322Bad += "the filigree and filete families could not be read - the second half of leg (d) measured nothing (SPEC V209, V20)" }

# (e) exactly FOUR palettes declare an ornament. This is the amended V320f seen from the other
# side: every era carries a frame now, and the count being DECLARED is what stops a fifth from
# arriving without a round behind it.
$ornDecl322 = ([regex]::Matches($hh6, "(?m)^\s*ornament\s*=\s*""#[0-9A-Fa-f]{6,8}""")).Count
$style322 = ([regex]::Matches($hh6, "(?m)^\t{5}ornStyle\s*=\s*""[^""]+""")).Count
if ($ornDecl322 -ne 4) { $v322Bad += "$ornDecl322 palette(s) declare ornament and every one of the 4 eras is meant to carry a frame since this round (SPEC V322e, V320f)" }
if ($style322 -lt 3) { $v322Bad += "only $style322 palette(s) declare an ornStyle - the corona muralis was the third, and fewer means an era quietly fell back to the default (SPEC V322e, V313a)" }

if ($v322Bad) { foreach ($b in $v322Bad) { Fail "V322 $b" } }
else { Pass "V322 the Classical Age draws corona-muralis in the gold it already carried, the fourth branch takes no radius, the merlon floor ($([Math]::Round($flr322, 2))) clears the smallest box side ($minD322), the four ORN_MUR_* stay in their own family, and $ornDecl322 palettes declare an ornament" }


# ---- V325: the CHUNK fits under the compiler ceiling for locals, and the ceiling is
# MEASURED (SPEC B77, the 122nd round) ---------------------------------------------------------
# What breaks does NOT announce itself: rdk -l exits 1, deletes the .rpk and names neither file
# nor line, so the round that overflows finds out by bisecting six builds unless the gate says it
# first. Three legs: the count, and where the two newest families live.
$v325Bad = @()

# (a) locals at CHUNK scope - three tabs, the level THEMES and the ORN_* families sit at.
# Ceiling measured at 105; the leg keeps 5 of margin.
$chunk325 = [regex]::Matches($hh6, "(?m)^\t{3}local ").Count
if ($chunk325 -eq 0) { $v325Bad += "counted 0 locals at chunk scope in WoD20.6 - the indentation this leg keys on moved, so it is measuring nothing (SPEC V209, V20, V325a)" }
elseif ($chunk325 -gt 100) {
    $v325Bad += "WoD20.6 declares $chunk325 locals at chunk scope against a ceiling of 100 (real limit 105, five kept as margin) - over it rdk -l exits 1, deletes the .rpk and names no file or line (SPEC V325a, B77, B19)"
}

# (c) POSITION, not count, and by PREFIX rather than by a list of names: what this leg measures
# is WHERE a family lives, never WHO is in it. A constant can be renamed inside its painter and
# stay correct; the moment one is hoisted to chunk scope the ceiling is back in play, and (a)
# alone would only catch that once the ceiling was touched - one round late. Same reasoning as
# V325b, which counts instead of naming owners.
$colFn325 = LuaFn $hh6 "markCoronaMuralis"
$murFn325 = LuaFn $hh6 "ornCoronaMuralis"

if (-not $colFn325) { $v325Bad += "markCoronaMuralis is gone from WoD20.6 - leg (c) has no painter to look inside (SPEC V209, V325c)" }
elseif (-not $murFn325) { $v325Bad += "ornCoronaMuralis is gone from WoD20.6 - leg (c) has no painter to look inside (SPEC V209, V325c)" }
else {
    foreach ($px325 in @("ORN_COL_", "ORN_GUT_", "ORN_MUR_")) {
        $hoist325 = [regex]::Matches($hh6, ("(?m)^\t{3}local " + $px325))
        if ($hoist325.Count -gt 0) {
            $v325Bad += "$($hoist325.Count) $px325* constant(s) sit at CHUNK scope - that family lives inside its own painter precisely so the chunk stays under the ceiling, and hoisting one is B77 repeating with a build that fails without naming a line (SPEC V325c, I95b, I94d)"
        }
    }
    foreach ($f325 in @("ornMuralFoot", "ornMuralCorner", "ornMuralEdge")) {
        if ($hh6 -match ("(?m)^\t{3}local function " + $f325 + "\b")) {
            $v325Bad += "$f325 is declared at CHUNK scope - the three mural helpers live inside ornCoronaMuralis, and each one hoisted out spends a chunk local the ceiling has no room for (SPEC V325c, B77)"
        }
    }

    # zero-guard for the leg itself: with the families gone from their painters there would be
    # nothing left to place and the loops above would pass by vacancy (SPEC V20, B7).
    $inCol325 = [regex]::Matches($colFn325, "ORN_COL_\w+|ORN_GUT_\w+").Count
    $inMur325 = [regex]::Matches($murFn325, "ORN_MUR_\w+").Count
    if ($inCol325 -lt 8) { $v325Bad += "markCoronaMuralis mentions $inCol325 column/gutta constant(s) and the family is 8 - leg (c) would be placing constants that are not there (SPEC V209, V20, V325c)" }
    if ($inMur325 -lt 4) { $v325Bad += "ornCoronaMuralis mentions $inMur325 mural constant(s) and the family is 4 - same vacancy on the box side (SPEC V209, V20, V325c)" }
}

if ($v325Bad) { foreach ($b in $v325Bad) { Fail "V325 $b" } }
else { Pass "V325 WoD20.6 declares $chunk325 local(s) at chunk scope against a ceiling of 100, and no ORN_COL_/ORN_GUT_/ORN_MUR_ constant and none of the three mural helpers has been hoisted out of its painter" }

# ---- V347: the ROOT chunk has a ceiling too, and it is a DIFFERENT one (SPEC B93, T789) ---
# V325 has guarded WoD20.6 since B77, and the number there is 105. That number is NOT a
# property of the SDK: the rdk compiler emits locals of its own into the same chunk, so the
# visible ceiling is what is LEFT of Lua's 200 after the file's own code has taken its share -
# which makes it a per-FILE number. The root form is four times the size of WoD20.6, and its
# share is correspondingly bigger: MEASURED on 2026-08-29 by adding plain unused locals one at
# a time, +4 compiles and +5 exits 1, so the root's visible ceiling is 53 against the 49 it
# declares. Four slots in the whole file, and the T789 slice wanted six.
#
# The failure mode is B19's, which is why this is worth a check rather than a comment: rdk -l
# exits 1, prints NOTHING, deletes the .rpk and names no file and no line. It cost a six-build
# bisection here, exactly as B77 says it cost one there.
$v347Bad = @()
$chunk347 = [regex]::Matches($rootTxt, "(?m)^\t{3}local ").Count

# zero-guard FIRST: the indentation this keys on is the whole measurement, and a chunk that
# counts zero means the level moved, not that the form got tidy (SPEC V20, B7).
if ($chunk347 -eq 0) { $v347Bad += "counted 0 locals at chunk scope in WoD20th.lfm - the indentation this leg keys on moved, so it is measuring nothing (SPEC V209, V20)" }
elseif ($chunk347 -gt 50) {
    $v347Bad += "WoD20th.lfm declares $chunk347 local(s) at chunk scope against a ceiling of 50 (real limit 53, three kept as margin) - over it rdk -l exits 1, deletes the .rpk and names no file or line (SPEC B93, B77, B19)"
}

# A `local function` at chunk scope spends a slot exactly like a variable does, and that is the
# half a reader gets wrong: the mf* helpers of T789 were written as local functions first and
# cost four slots before they cost anything else. Named so the next round reads it here rather
# than rediscovering it in a bisection (SPEC B93).
$fn347 = [regex]::Matches($rootTxt, "(?m)^\t{3}local function ").Count
if ($chunk347 -gt 0 -and $fn347 -eq 0) { $v347Bad += "no local function at chunk scope in WoD20th.lfm - the form declares them by the dozen, so this count is reading the wrong level (SPEC V209, V20)" }

if ($v347Bad) { foreach ($b in $v347Bad) { Fail "V347 $b" } }
else { Pass "V347 WoD20th.lfm declares $chunk347 local(s) at chunk scope - $fn347 of them local functions - against a ceiling of 50, measured at 53" }

# ---- V339: the merit/flaw search box (SPEC I107, Q16, T785) -----------------------------
# Q16 killed the dropdown for these twelve rows and put a search box in its place. Most of what
# can go wrong here is SILENT: a row short cuts results with no symptom, a require outside the
# timer just never paints the loading, a find without its fourth argument only throws on the
# names carrying a parenthesis. None of it reddens a build.
$v339Bad = @()
$doc339 = Doc (Join-Path $dir "WoD20.2.lfm")
# mfSearch left this file for the root form when B107 moved the floating overlays off the
# tab (V367): MeritPicked is still a Traits template and the box is not, so the two halves
# of this check read two files on purpose.
$root339 = Doc (Join-Path $dir "WoD20th.lfm")
$traits339 = [System.IO.File]::ReadAllText((Join-Path $dir "WoD20.2.lfm"))

# (a) the twelve picked rows carry the BUTTON and its hidden twin, and no comboBox. V13 still
# rules the other combos on the sheet - the revocation is NAMED here, not a general loosening.
$btn339 = @($doc339.SelectNodes("//template[@name='MeritPicked']/button[starts-with(@name,'dynMerit_')]"))
$twin339 = @($doc339.SelectNodes("//template[@name='MeritPicked']/edit[@visible='false'][starts-with(@field,'merit_')]"))
$picked339 = @([regex]::Matches($traits339, '<MeritPicked '))
if ($btn339.Count -ne 1) { $v339Bad += "MeritPicked declares $($btn339.Count) dynMerit_ button(s), expected 1 - the name column of a picked row IS the opener (SPEC I107a, V339a)" }
if ($twin339.Count -ne 1) { $v339Bad += "MeritPicked declares $($twin339.Count) hidden merit_ edit(s), expected 1 - a button has no field, so the data lives in the twin behind it (SPEC I107a1, V339a)" }
if ($picked339.Count -ne 22) { $v339Bad += "$($picked339.Count) MeritPicked row(s) authored, expected 14 - m0..m6 and f0..f6 are picked and the other eight are typed (SPEC I100a, V339a)" }
$cbo339 = @($doc339.SelectNodes("//template[@name='MeritPicked']/comboBox"))
if ($cbo339.Count -gt 0) { $v339Bad += "$($cbo339.Count) comboBox survives in the merit templates - Q16 retired the dropdown for these rows (SPEC V339a, V13)" }

# (b) one box, and EXACTLY twenty rows: the page size of I107k. One short cuts a result with no
# symptom anywhere; one over is a control nothing ever paints.
$box339 = @($root339.SelectNodes("//layout[@name='mfSearch']"))
if ($box339.Count -ne 1) { $v339Bad += "mfSearch is authored $($box339.Count) time(s), expected 1 - one box per sheet is what makes opening another mean overwriting this one (SPEC I107c, V339b)" }
$rows339 = @($root339.SelectNodes("//layout[@name='mfSearch']/button[starts-with(@name,'dynMfRow_')]"))
if ($rows339.Count -ne 20) { $v339Bad += "the box carries $($rows339.Count) dynMfRow_ button(s), expected the 20 the page size of I107k declares - one short cuts a result silently, one over is a dead control (SPEC V339b)" }
foreach ($i339 in 1..20) {
    if (-not ($root339.SelectSingleNode("//layout[@name='mfSearch']/button[@name='dynMfRow_$i339']"))) {
        $v339Bad += "dynMfRow_$i339 is missing - the rows are read by index, so a gap is a page that stops early (SPEC V339b)"
    }
}

# (f) the box owns no field: book text may not be written onto the character (SPEC V29).
if ($root339.SelectSingleNode("//layout[@name='mfSearch'][@field]")) { $v339Bad += "mfSearch owns a field - the box is furniture and V29 keeps furniture out of the data contract (SPEC V339f)" }
$okBorn339 = $root339.SelectSingleNode("//button[@name='btnMfOk']")
if ($null -eq $okBorn339) { $v339Bad += "btnMfOk is gone - nothing confirms a marking (SPEC V209)" }
elseif ($okBorn339.GetAttribute("enabled") -ne "false") { $v339Bad += "btnMfOk is not authored enabled=false - it would confirm before anything was marked (SPEC I107m, V339)" }

# (c) the require lives INSIDE the timer. Outside it the loading never paints, because require
# is synchronous and the screen repaints only after the turn ends (SPEC I107f, R44, R47).
$openFn339 = LuaFn $rootTxt 'mfOpen'
if (-not $openFn339) { $v339Bad += "mfOpen is gone - legs (c) and (d) measured nothing (SPEC V209)" }
else {
    $reqAll339 = @([regex]::Matches($rootTxt, 'require\("meritData\.lua"\)'))
    if ($reqAll339.Count -ne 1) { $v339Bad += "meritData is required $($reqAll339.Count) time(s) in the root chunk, expected exactly 1 - a second require is a second parse of 60KB (SPEC I107e, V135)" }
    $timer339 = [regex]::Match($openFn339, '(?s)setTimeout\(function\(\)(.*?)end, 1\);')
    if (-not $timer339.Success) { $v339Bad += "mfOpen schedules no setTimeout - the loading text would never reach the screen before the parse (SPEC I107f, V339c)" }
    elseif ($timer339.Groups[1].Value -notmatch 'require\("meritData\.lua"\)') { $v339Bad += "the require sits OUTSIDE the mfOpen timer - require is synchronous, so the box would appear already full and the loading would never paint (SPEC I107f, V339c)" }
}

# (d) the animation STOPS - on close and when the parse ends. A timer that outlives its box has
# nothing left to stop it, and there is no clearTimeout to cancel with (SPEC I107g).
$closeFn339 = LuaFn $rootTxt 'mfClose'
if (-not $closeFn339) { $v339Bad += "mfClose is gone - leg (d) measured nothing (SPEC V209)" }
elseif ($closeFn339 -notmatch 'MF\.load = MF\.load \+ 1') { $v339Bad += "mfClose does not bump MF.load - the loading animation would go on ticking into a closed box, which is the leak I107g names (SPEC V339d)" }
$tickFn339 = LuaFn $rootTxt 'mfTick'
if (-not $tickFn339) { $v339Bad += "mfTick is gone - there is no animation left to stop (SPEC I107g, V209)" }
elseif ($tickFn339 -notmatch 'MF\.load ~= mine') { $v339Bad += "mfTick does not compare its captured generation against MF.load - without that test nothing can stop it (SPEC I107g, V339d)" }
elseif ($tickFn339 -notmatch 'end, 300\);') { $v339Bad += "mfTick does not reschedule on 300ms - that is the interval I107g declares (SPEC V339d)" }

# (e) the find is LITERAL. Without the fourth argument a name carrying a parenthesis - and the
# lists carry plenty - is read as a PATTERN and the search throws (SPEC I107h).
$filterFn339 = LuaFn $rootTxt 'mfFilter'
if (-not $filterFn339) { $v339Bad += "mfFilter is gone - leg (e) measured nothing (SPEC V209)" }
else {
    $finds339 = @([regex]::Matches($filterFn339, 'string\.find\('))
    $literal339 = @([regex]::Matches($filterFn339, 'string\.find\((?:[^()]|\([^()]*\))*, 1, true\)'))
    if ($finds339.Count -eq 0) { $v339Bad += "mfFilter calls string.find nowhere - the filter is not filtering and every leg here would pass on it (SPEC V209, V20)" }
    elseif ($literal339.Count -ne $finds339.Count) { $v339Bad += "$($finds339.Count - $literal339.Count) of $($finds339.Count) string.find call(s) in mfFilter omit the fourth argument - without it a name carrying a parenthesis is read as a PATTERN and the search throws (SPEC I107h, V339e)" }
}

# ONE point of write, three doors into it (SPEC I107i, I107l as amended, V135, B38).
$writes339 = @([regex]::Matches((NoComments $rootTxt), 'setField\(MF\.field, MF\.picked\)'))
if ($writes339.Count -ne 1) { $v339Bad += "the picked row is written in $($writes339.Count) place(s), expected exactly 1 - btnMfOk, the double click and ENTER are three DOORS into one writer, not three writers (SPEC I107i, V135, B38)" }
$confirmFn339 = LuaFn $rootTxt 'mfConfirm'
if ($confirmFn339 -and $confirmFn339 -notmatch 'setField\(MF\.field, MF\.picked\)') { $v339Bad += "the write does not live in mfConfirm - that is the one function the three doors open onto (SPEC I107i, V339)" }

# (g) zero-guard: with the box gone every leg above passes by vacancy (SPEC V20, B7).
if ($box339.Count -eq 0 -and $rows339.Count -eq 0 -and $picked339.Count -eq 0) {
    $v339Bad += "the search box, its rows and the picked rows all count zero - the collector is reading the wrong file and this check verified nothing (SPEC V209, V20, B7)"
}

if ($v339Bad) { foreach ($b in $v339Bad) { Fail "V339 $b" } }
else { Pass "V339 the search box stands once with its $($rows339.Count) rows, the $($picked339.Count) picked rows carry button plus hidden twin and no combo, the require sits inside the timer, the animation stops on close, every find is literal, and the row is written in one place behind three doors" }

# ---- V323: the COLUMN and the GUTTA draw on the Classical bar (SPEC I95, the 122nd round) -----
# The era stops wearing a new box with the default marker. Six legs.
$v323Bad = @()
$markPathNC323 = NoComments (LuaFn $hh6 "markPath")
$colNC323 = NoComments (LuaFn $hh6 "markCoronaMuralis")
$murNC323 = NoComments (LuaFn $hh6 "ornCoronaMuralis")

# zero-guard
if (-not $colNC323)      { $v323Bad += "markCoronaMuralis is gone from WoD20.6 - there is no bar drawing to measure (SPEC V209, V323)" }
if (-not $markPathNC323) { $v323Bad += "markPath is gone from WoD20.6 - nothing dispatches a marker style (SPEC V209)" }
else {
    $branch323 = [regex]::Matches($markPathNC323, "if style == ""[^""]+"" then return \w+\(").Count
    if ($branch323 -lt 3) { $v323Bad += "markPath has $branch323 style branch(es) and the sheet is built around 3 - the Classical era would fall back to the default marker with nothing to say so (SPEC V209, V323a)" }
}

# (a) the THIRD branch, and it forwards kind. A branch with no function is nil called on the
# theme switch; a function with no branch is code that never runs, with rdk -l 0 and the gate
# green - the B6 family, from both ends.
$br323 = [regex]::Match($markPathNC323, "if style == ""corona-muralis"" then return (\w+)\(([^)]*)\); end;")
$sig323 = [regex]::Match($hh6, "local function markCoronaMuralis\(([^)]*)\)")
if (-not $br323.Success) { $v323Bad += "markPath has no corona-muralis branch - the palette names a style the marker dispatcher never heard of (SPEC V323a, I95a)" }
elseif ($br323.Groups[1].Value -ne "markCoronaMuralis") { $v323Bad += "the corona-muralis marker branch calls $($br323.Groups[1].Value) (SPEC V323a)" }
elseif ($br323.Groups[2].Value -notmatch "\bkind\b") { $v323Bad += "the corona-muralis marker branch does not forward kind - all three levels would draw the same motif (SPEC V323a, I95a)" }
if ($sig323.Success) {
    $nArg323 = @($sig323.Groups[1].Value -split "\s*,\s*").Count
    if ($nArg323 -ne 3) { $v323Bad += "markCoronaMuralis takes $nArg323 parameter(s) and the contract is 3 (w, h, kind) - the level it is drawing for arrives nil (SPEC V323a)" }
} else { $v323Bad += "markCoronaMuralis has no readable signature - leg (a) measured nothing (SPEC V209)" }

# (b) THREE motifs, and three DIFFERENT drawings. A motif that returns "" for one kind is a level
# gone mute on the bar, and there is no rdk -l that says so - only the screen.
if ($colNC323) {
    if ($colNC323 -notmatch 'kind == "sep"') { $v323Bad += "markCoronaMuralis never tests kind == sep - the separator falls through to whatever the tab draws (SPEC V323b, I95d)" }
    if ($colNC323 -notmatch 'kind == "sub"') { $v323Bad += "markCoronaMuralis never tests kind == sub - the sub-tab and the tab become the same drawing, which is I78b hierarchy lost (SPEC V323b)" }
}

# (c) the FILL answers per MOTIF, never per style (SPEC I95e): two return values, true on sep and
# on sub, and NOT true on tab. Without this the column comes out FILLED - markRule writes
# color = colour when the second value is true - and the Tuscan pair becomes one block of gold.
if ($colNC323) {
    $sepRet323 = [regex]::Match($colNC323, '(?s)kind == "sep".{0,400}?return [^;]*?,\s*true')
    $subRet323 = [regex]::Match($colNC323, '(?s)kind == "sub".{0,1400}?return [^;]*?,\s*true')
    if (-not $sepRet323.Success) { $v323Bad += "the sep motif does not return a second value of true - the separator gutta would be traced instead of filled, and it is a peg, not an outline (SPEC V323c, I95e)" }
    if (-not $subRet323.Success) { $v323Bad += "the sub motif does not return a second value of true - its guttae would be traced (SPEC V323c, I95e)" }
    $tabTail323 = $colNC323.Substring([Math]::Max(0, $colNC323.Length - 700))
    if ($tabTail323 -match 'return [^;]*?,\s*true') { $v323Bad += "the tab motif returns true for the fill - the Tuscan column would be painted solid instead of traced, which is the one thing I95e exists to keep apart (SPEC V323c)" }
}

# (d) TWO guards, and what each drops is the SMALLEST thing: on tab the capital and base go and
# the shaft survives; on sub the gutta goes and the rule survives. Floors READ out of the Lua and
# measured against the narrowest button of each LEVEL - a floor with two owners is B69.
$c323 = @{}
foreach ($k323 in @("ORN_COL_W", "ORN_COL_SHAFT", "ORN_COL_ROOM", "ORN_GUT_STEP", "ORN_SUB_MARK", "ORN_PILLR")) {
    $m323 = [regex]::Match($hh6, "(?m)^\s*local $k323\s*=\s*([\d.]+);")
    if ($m323.Success) { $c323[$k323] = [double]$m323.Groups[1].Value }
}
if ($c323.Count -ne 6) { $v323Bad += "only $($c323.Count) of the 6 constants leg (d) needs could be read - it has no floor to compute (SPEC V209, V323d)" }
else {
    $wTop323 = @(); $wSub323 = @()
    foreach ($pr323 in @(,@("WoD20th.lfm","tabStrip",$true))) {
        $st323 = (Doc (Join-Path $dir $pr323[0])).SelectSingleNode("//layout[@name='$($pr323[1])']")
        if ($null -eq $st323) { continue }
        foreach ($r323 in $st323.SelectNodes("rectangle[starts-with(@name,'tabOn')]")) {
            if ($pr323[2]) { $wTop323 += [double]$r323.GetAttribute("width") } else { $wSub323 += [double]$r323.GetAttribute("width") }
        }
    }
    if (($wTop323.Count + $wSub323.Count) -ne 11) { $v323Bad += "read $($wTop323.Count + $wSub323.Count) marker(s) across the one bar left, expected 11 - leg (d) is covering less than the bars hold (SPEC V209)" }
    else {
        $minTop323 = ($wTop323 | Measure-Object -Minimum).Minimum
        $minSub323 = ($wSub323 | Measure-Object -Minimum).Minimum
        $fCap323 = 2 * $c323["ORN_COL_W"] + $c323["ORN_COL_ROOM"]
        $fSha323 = 2 * $c323["ORN_COL_SHAFT"] + $c323["ORN_COL_ROOM"]
        $iSub323 = [Math]::Sqrt(($c323["ORN_PILLR"] + $c323["ORN_SUB_MARK"]) * ($c323["ORN_PILLR"] + $c323["ORN_SUB_MARK"]) - $c323["ORN_SUB_MARK"] * $c323["ORN_SUB_MARK"])
        $fGut323 = 2 * $iSub323 + $c323["ORN_GUT_STEP"]
        if ($fSha323 -ge $fCap323) { $v323Bad += "the shaft floor ($fSha323) is not below the capital floor ($fCap323) - the two levels of degradation collapse into one and the marker goes straight from full to gone (SPEC V323d, I95c)" }
        if ($minTop323 -le $fSha323) { $v323Bad += "the narrowest top marker is $minTop323 against a shaft floor of $fSha323 - at that width the tab draws NOTHING, and a marker that quietly disappears is not the one anybody chose (SPEC V323d, V279, B59)" }
        if ($wSub323.Count -gt 0 -and $minSub323 -le $fGut323) { $v323Bad += "the narrowest sub-tab is $minSub323 against a gutta floor of $([Math]::Round($fGut323, 2)) - at that width the pegs drop and the sub-tab keeps only its rule, which is the level losing half its mark (SPEC V323d, V279)" }
        if ($colNC323 -match 'return "";' -and $colNC323 -notmatch 'ORN_COL_SHAFT') { $v323Bad += "the bar refuses by size without reading the shaft constant - the refusal is not the one the two-step degradation describes (SPEC V323d)" }
    }
}

# (e) constants stay inside their own family (SPEC V308, B69). ORN_SUB_MARK is the DECLARED
# exception and is read by both markers - the sub-tab height the user approved is one fact.
if ($colNC323) {
    if ($colNC323 -match "\bORN_MUR_") { $v323Bad += "markCoronaMuralis reads an ORN_MUR_* constant - those belong to the BOX frame, and one number serving the box and the bar moves both when only one was asked for (SPEC V323e, V308, B69)" }
    if ($colNC323 -match "\bORN_FIL_|\bORN_COR_") { $v323Bad += "markCoronaMuralis reads a filete or corrente constant - it is drawing another era with this one numbers (SPEC V323e, V308)" }
}
if ($murNC323 -and $murNC323 -match "\bORN_COL_|\bORN_GUT_") { $v323Bad += "ornCoronaMuralis reads a column or gutta constant - the box frame is reading the bar family, which is B69 in the other direction (SPEC V323e, V308)" }

# (f) the palette keeps its TWO keys and gains no third: the marker reads ornStyle, which already
# exists, and its colour is t.stroke on #FFFFFF, as I78e set for all four eras. ornament belongs
# to the BOX. A new key here would be a loose colour arriving through the one door nobody watches.
$pal323 = [regex]::Match($themesBlock, "(?ms)^\t{4}\[""Classical Age""\] = \{(.*?)^\t{4}\},")
if (-not $pal323.Success) { $v323Bad += "the Classical Age palette could not be read - leg (f) measured nothing (SPEC V209, V323f)" }
else {
    $pb323 = NoComments $pal323.Groups[1].Value
    $nOrn323 = [regex]::Matches($pb323, "(?m)^\t{5}ornament\s*=").Count
    $nSty323 = [regex]::Matches($pb323, "(?m)^\t{5}ornStyle\s*=").Count
    if ($nOrn323 -ne 1) { $v323Bad += "the Classical Age palette declares $nOrn323 ornament key(s) and the contract is exactly 1 - a second is a loose colour entering through the ornament door (SPEC V323f, V53)" }
    if ($nSty323 -ne 1) { $v323Bad += "the Classical Age palette declares $nSty323 ornStyle key(s) and the contract is exactly 1 (SPEC V323f)" }
}

if ($v323Bad) { foreach ($b in $v323Bad) { Fail "V323 $b" } }
else { Pass "V323 the Classical bar draws the column and the gutta through markPath third branch, the three motifs are three drawings, only sep and sub fill, the narrowest marker ($minTop323) clears the shaft floor ($fSha323), the eight constants stay in their family, and the palette keeps its two keys" }


# ---- V324: the CLASSICAL corner holds on the 73 AND on the avatar, and the drawing was REMADE
# for it (SPEC I96, I97, the 122nd and 123rd rounds) -------------------------------------------
# Five legs. The era declares a step, the painter takes the radius, the foot stopped arcing, the
# corner stopped curving, and the avatar rides the FIRST answer of sectionBox.
$v324Bad = @()
$murNC324 = NoComments (LuaFn $hh6 "ornCoronaMuralis")
$secNC324 = NoComments (LuaFn $hh6 "sectionBox")
$ornNC324 = NoComments (LuaFn $hh6 "ornament")

if (-not $murNC324) { $v324Bad += "ornCoronaMuralis is gone from WoD20.6 - there is no drawing to measure (SPEC V209, V324)" }
if (-not $secNC324) { $v324Bad += "sectionBox is gone from WoD20.6 - leg (e) cannot ask who wears the shape (SPEC V209, V324e)" }

# (a) the palette declares BOTH fields, and the TYPE is read by VALUE. innerLine and bevel are
# the two members of the enum that do not arc, and swapping one for the other leaves the foot of
# I97a correct for the wrong shape, with rdk -l exiting 0.
$pal324 = [regex]::Match($themesBlock, "(?ms)^\t{4}\[""Classical Age""\] = \{(.*?)^\t{4}\},")
if (-not $pal324.Success) { $v324Bad += "the Classical Age palette could not be read - leg (a) measured nothing (SPEC V209, V324a)" }
else {
    $pb324 = NoComments $pal324.Groups[1].Value
    $bc324 = [regex]::Match($pb324, "(?m)^\s*boxCorner\s*=\s*\{\s*type\s*=\s*""([^""]*)""\s*,\s*radius\s*=\s*([\d.]+)\s*\}")
    if (-not $bc324.Success) { $v324Bad += "the Classical Age palette declares no complete boxCorner - half a table is a write the SDK ignores in silence, and the symptom would be a ROUND box under a merlon drawn for a step (SPEC V324a, V316a, I96d)" }
    else {
        if ($bc324.Groups[1].Value -ne "innerLine") { $v324Bad += "the Classical Age corner is '$($bc324.Groups[1].Value)' and the era was drawn for innerLine - the two non-arcing members of the enum are not interchangeable, and the foot of I97a would be right for the wrong shape (SPEC V324a, I96a)" }
        if ([double]$bc324.Groups[2].Value -le 0) { $v324Bad += "the Classical Age corner radius is $($bc324.Groups[2].Value) - a radius of zero or less UNDOES the selector of V278, because ornament() reads c.xradius live and a 0 refuses the box (SPEC V324a, V316a)" }
    }
}

# (b) THREE parameters and the branch forwards the radius. Both ends read: V322b now demands
# exactly what it used to refuse, and dropping either end orphans the boxCorner above.
$sig324 = [regex]::Match($hh6, "local function ornCoronaMuralis\(([^)]*)\)")
if (-not $sig324.Success) { $v324Bad += "ornCoronaMuralis has no readable signature (SPEC V209, V324b)" }
else {
    $n324 = @($sig324.Groups[1].Value -split "\s*,\s*").Count
    if ($n324 -ne 3) { $v324Bad += "ornCoronaMuralis takes $n324 parameter(s) and the contract is 3 (w, h, radius) - the era corner would arrive nil and the frame would be drawn to whatever the XML authored (SPEC V324b, I96c)" }
}

# (c) the FOOT steps: it reads neither ORN_BOXR, nor a root of a difference of squares, nor a
# sqrt(2) factor. Three refusals, one per shape the sheet carries - a rule of the wrong shape
# over this corner crosses the outline it decorates, and there is no rdk -l that sees it.
# Nested since B77: these live INSIDE ornCoronaMuralis at four tabs, so LuaFn - which is
# written for chunk-scope functions - hands back everything down to the next chunk-level end,
# and the run count below would add ornMuralEdge to ornMuralCorner. Cut at the OWN indentation.
function NestedFn($txt, $name) {
    $m = [regex]::Match($txt, "(?s)\t{4}local function " + [regex]::Escape($name) + "\(.*?\r?\n\t{4}end;")
    if ($m.Success) { return $m.Value }
    return ""
}
$footNC324 = NoComments (NestedFn $hh6 "ornMuralFoot")
if (-not $footNC324) { $v324Bad += "ornMuralFoot is gone from WoD20.6 - leg (c) measured nothing (SPEC V209, V324c)" }
else {
    if ($footNC324 -match "\bORN_BOXR\b") { $v324Bad += "ornMuralFoot reads ORN_BOXR - that constant is the Lua copy of the radius the XML authors, and this era writes its own through boxCorner, so the frame would be drawn to another era corner (SPEC V324c, V317d, B69)" }
    if ($footNC324 -match "math\.sqrt") { $v324Bad += "ornMuralFoot still takes a square root - that is what a CONCAVE ARC costs, and the era stopped arcing when it started stepping (SPEC V324c, I97a)" }
    if ($footNC324 -notmatch "radius") { $v324Bad += "ornMuralFoot does not read the radius - it would step by a number that is not the one the palette declares (SPEC V324c, I97a)" }
}

# (d) the CORNER stops curving AND emits TWO runs. Two legs and not one: the foot can be right
# while the curve stays in the corner, which is the PARTIAL defect - half the frame stepped and
# half arced on the same box - and it would pass (c) with only the screen to tell.
$cornNC324 = NoComments (NestedFn $hh6 "ornMuralCorner")
if (-not $cornNC324) { $v324Bad += "ornMuralCorner is gone from WoD20.6 - leg (d) measured nothing (SPEC V209, V324d)" }
else {
    if ($cornNC324 -match "\bornArc\b") { $v324Bad += "ornMuralCorner still calls ornArc - the corner would arc across a step, which is exactly the crossing V316b existed to forbid and V317a inherited (SPEC V324d, I96d)" }
    $runs324 = [regex]::Matches($cornNC324, "\bornLine\s*\(").Count
    if ($runs324 -ne 2) { $v324Bad += "ornMuralCorner emits $runs324 straight run(s) and a step closes with 2 - ONE is what a 45 degree CUT draws between the same two feet, so it passes every measurement of the pair and is wrong only on screen (SPEC V324d, I97b)" }
}

# (e) the AVATAR rides along, and it is MEASURED: the corner write sits under the FIRST value of
# sectionBox, the one that says WEARS, not the second, which says DRAWS. Move it to the second
# and the avatar loses the corner while the 73 keep it - the user request undone, rdk -l 0, gate
# green, and only the screen counting (SPEC I92a, I93a).
if ($secNC324) {
    foreach ($nm324 in @("avatarFrame", "ornAvatar")) {
        if ($secNC324 -notmatch [regex]::Escape($nm324)) { $v324Bad += "sectionBox no longer names $nm324 - the roster the avatar rides on is gone, so it stops wearing the era shape (SPEC V324e, V320a)" }
    }
}
$themeB324 = NoComments $themeFn.Groups[1].Value
if (-not $themeB324) { $v324Bad += "the theme walk could not be read - leg (e) cannot see which answer the corner write sits under (SPEC V209, V324e)" }
elseif ($themeB324 -notmatch "if sectionBox\(c, fill\) then") { $v324Bad += "the corner write no longer sits under sectionBox FIRST answer - whatever it asks now, it is not the question that puts the avatar and the 73 on the same branch (SPEC V324e, I96b)" }

if ($v324Bad) { foreach ($b in $v324Bad) { Fail "V324 $b" } }
else { Pass "V324 the Classical Age steps its corner at the radius it declares, the painter takes it as a parameter, the foot is a plain sum, the corner closes with two runs and no arc, and the avatar rides the same first answer the 73 boxes do" }

# ---- V326: the Classical corner goes PAST the ceiling, and the number is DECLARED rather than
# fallen out of some d (SPEC I97b, the 123rd round) ---------------------------------------------
# It exists because the step is the first drawing on the sheet to pass 23. The leg does NOT forbid
# the overshoot - it stops it from MOVING without a round behind it.
$v326Bad = @()
$murIn326 = [regex]::Match($hh6, "(?m)^\s*local ORN_MUR_IN\s*=\s*([\d.]+);")
$murOut326 = [regex]::Match($hh6, "(?m)^\s*local ORN_MUR_OUT\s*=\s*([\d.]+);")
$pal326 = [regex]::Match($themesBlock, "(?ms)^\t{4}\[""Classical Age""\] = \{(.*?)^\t{4}\},")

if (-not $murIn326.Success -or -not $murOut326.Success) { $v326Bad += "ORN_MUR_IN or ORN_MUR_OUT could not be read - there is no depth to compute (SPEC V209, V326a)" }
elseif (-not $pal326.Success) { $v326Bad += "the Classical Age palette could not be read (SPEC V209, V326a)" }
else {
    $r326 = [regex]::Match((NoComments $pal326.Groups[1].Value), "(?m)^\s*boxCorner\s*=\s*\{[^}]*radius\s*=\s*([\d.]+)")
    if (-not $r326.Success) { $v326Bad += "the Classical Age boxCorner declares no radius - leg (a) has nothing to add the rule inset to (SPEC V209, V326a)" }
    else {
        # (a) computed from BOTH sides - the radius off the palette, the inset off the Lua.
        $depth326 = [double]$r326.Groups[1].Value + [double]$murIn326.Groups[1].Value
        if ($depth326 -ne 12) {
            $v326Bad += "the Classical rule reaches $depth326 into the box and the round declared 12 - AMENDED 2026-09-06, when the era's outline went from the shared 3px to its own 1px (V67) and the crown closed up on the edge behind it: the two EMPTY gaps are 2 each (outline to dente, ameia to rule), which is what sets ORN_MUR_OUT and ORN_MUR_IN, and the rule now stops 9 short of the 20px content margin instead of 5. It was 16 from the 124th round, which shrank the step to 1 and killed the overshoot; at radius 14 it was 29 and at radius 20 it would be 35, with only the screen to warn (SPEC V326a, I97b, I73)"
        }
        # (c) the merlon stays INSIDE the rule that frames it.
        if ([double]$murOut326.Groups[1].Value -ge [double]$murIn326.Groups[1].Value) {
            $v326Bad += "ORN_MUR_OUT ($($murOut326.Groups[1].Value)) is not inside ORN_MUR_IN ($($murIn326.Groups[1].Value)) - the crenellation would be drawn on or past the rule it hangs under, and the frame would read as two rules instead of a wall (SPEC V326c)"
        }
    }
}

# (b) every OTHER era stays under 23: the two concave ones arc at ORN_BOXR + ORN_IN and Modern
# Nights cuts. Without this, "the Classical may pass 23" quietly becomes "anyone may", which is
# how a limit dies with nobody deciding it.
$deep326 = @()
foreach ($k326 in $themeKeys) {
    if ($k326 -eq "Classical Age") { continue }
    $p326 = [regex]::Match($themesBlock, "(?ms)^\t{4}\[""$([regex]::Escape($k326))""\] = \{(.*?)^\t{4}\},")
    if (-not $p326.Success) { continue }
    $b326 = [regex]::Match((NoComments $p326.Groups[1].Value), "(?m)^\s*boxCorner\s*=\s*\{\s*type\s*=\s*""([^""]*)""\s*,\s*radius\s*=\s*([\d.]+)")
    if ($b326.Success -and $b326.Groups[1].Value -eq "innerLine" -and ([double]$b326.Groups[2].Value + [double]$murIn326.Groups[1].Value) -gt 23) {
        $deep326 += $k326
    }
}
if ($deep326.Count -gt 0) { $v326Bad += "$($deep326 -join ', ') also step past the 23px ceiling - the overshoot was decided for ONE era at ONE corner, and a second one is the limit dying without anybody deciding it (SPEC V326b)" }

if ($v326Bad) { foreach ($b in $v326Bad) { Fail "V326 $b" } }
else { Pass "V326 the Classical rule reaches a declared $($depth326)px at the corner - inside the 20px margin since the 124th round - the merlon stays inside it, and no other era steps past the 23px ceiling" }

# ---- V333: the ? box is ONE, and the scrim is painted under it -------------------------
# SPEC I102, V333, T760. The box is a floating overlay on the Traits tab, so two of the rules
# the rest of the sheet lives by do not apply to it and it is cut out of V40 and V280/V298 by
# name up top. What replaces them is here: one box, one scrim, the scrim first, both born
# hidden, and one closer behind both doors.
$v333Bad = @()
$ovl333 = Doc (Join-Path $dir "WoD20th.lfm")
$ovlRaw333 = [System.IO.File]::ReadAllText((Join-Path $dir "WoD20th.lfm"))
$rootLua333 = [System.IO.File]::ReadAllText((Join-Path $dir "WoD20th.lfm"))

# (a) ONE of each on the WHOLE sheet, not just on this tab. A second box opens under the first
# and nothing closes it, which is the state I102d exists to make impossible.
$pop333   = 0
$scrim333 = 0
foreach ($f333 in $files) {
    $d333 = Doc $f333.FullName
    $pop333   += @($d333.SelectNodes("//layout[@name='popDesc']")).Count
    $scrim333 += @($d333.SelectNodes("//rectangle[@name='popScrim']")).Count
}
if ($pop333 -ne 1)   { $v333Bad += "popDesc is authored $pop333 time(s), expected exactly 1 - one box per sheet is what makes 'open another' mean 'overwrite this one' (SPEC I102d, V333a)" }
if ($scrim333 -ne 1) { $v333Bad += "popScrim is authored $scrim333 time(s), expected exactly 1 - a second scrim is a second click-outside, and each would only close half of what is open (SPEC I102a, V333a)" }

# (b) file order IS paint order, the sentence V320 already measures for ornament. The scrim has
# to sit BETWEEN the grid and the box: authored after it, it paints over the box and the X
# becomes unclickable while the grid underneath stays live.
$iScrim = $ovlRaw333.IndexOf('name="popScrim"')
$iPop   = $ovlRaw333.IndexOf('name="popDesc"')
if ($iScrim -lt 0 -or $iPop -lt 0) { $v333Bad += "the root form carries no popScrim/popDesc pair - leg (b) measured nothing and would pass on the absence (SPEC V20, V209)" }
elseif ($iScrim -gt $iPop) { $v333Bad += "popScrim is authored AFTER popDesc - it would paint over the box it is meant to sit under, and every click inside the box would land on the scrim and close it (SPEC I102a, V333b)" }

# (c) both born hidden. A box that ships visible covers the grid from the first paint.
foreach ($n333 in @('popScrim', 'popDesc')) {
    $c333 = $ovl333.SelectSingleNode("//*[@name='$n333']")
    if ($null -eq $c333) { $v333Bad += "$n333 is not on the root form - an overlay parented to a TAB is invisible from every other tab (SPEC V367, B107, V209)" }
    elseif ($c333.GetAttribute("visible") -ne 'false') { $v333Bad += "$n333 is authored visible - it would cover the grid from the moment the tab opens (SPEC I102a, V333c)" }
}

# (e) ONE closer, reached from BOTH doors and from nowhere else. Two closers is how the scrim
# ends up hidden with the box still on screen - the V135 disease in its geometry form.
$close333 = @($ovl333.SelectNodes("//*[@onClick]") | Where-Object { $_.GetAttribute("onClick") -match 'popClose\(' })
$closeNames333 = @($close333 | ForEach-Object { $_.GetAttribute("name") })
if ($closeNames333 -notcontains 'btnPopClose') { $v333Bad += "btnPopClose does not call popClose - the X is one of the two doors I102c declares (SPEC V333e)" }
if ($closeNames333 -notcontains 'popScrim')    { $v333Bad += "popScrim does not call popClose - a click outside would leave the box standing (SPEC V333e)" }
if ($close333.Count -ne 2) { $v333Bad += "$($close333.Count) control(s) call popClose, expected the X and the scrim - a third door is a third place the two visibles can disagree (SPEC V135, V333e)" }

# (f) wordWrap is the ONLY thing between this box and the horizontal scrollbar the user ruled
# out: SDK3 has no scroll property on textEditor, so the fix is to give it nothing to scroll.
$edt333 = $ovl333.SelectSingleNode("//textEditor[@name='edtPopDesc']")
if ($null -eq $edt333) { $v333Bad += "edtPopDesc is gone - the box has nothing to write into (SPEC V209)" }
else {
    if ($edt333.GetAttribute("wordWrap") -ne 'true') { $v333Bad += "edtPopDesc is authored without wordWrap - one long line puts a horizontal scrollbar in the box, which is the single thing the user ruled out (SPEC I102c, V333f)" }
    if ($edt333.GetAttribute("readOnly") -ne 'true') { $v333Bad += "edtPopDesc is writable - it shows book text, and what is typed there has no reader (SPEC V29, R12)" }
    if ($edt333.HasAttribute("field")) { $v333Bad += "edtPopDesc owns field '$($edt333.GetAttribute('field'))' - book text would be saved onto the character (SPEC V29)" }
}

# (d) and (g) landed with T761. Both are counts of CALL SITES and not of authored nodes: the
# twenty-eight ? buttons are two <button> elements, one inside MeritPicked and one inside
# OpenAbility, and a grep for btnQ* over the XML finds exactly those two. A leg written against
# the authored nodes would read 2, never move again, and pass forever on nothing - B7 arriving
# through the template door.
#
# The roster is CLOSED and named, the way V109 names cboGame's three: a third template growing
# a ? has to redden here rather than be absorbed into a total that still says 28.
# template -> rows it draws the ? on. The four Vampire ones joined in T872 (wave 3 of I113e):
# DiscRow serves BOTH boxes of WoD20.12 - 12 open slots and 4 clan slots - which is one
# template over two roots, exactly the arrangement V178 asks for so the two lists cannot fork.
# The three Numina ones joined in T874 (wave 4): 12 hedge paths, 19 psychic phenomena and 12
# picked hedge rituals, the PICKER rows of each box and never the typed ones under them - a ? on
# a typed row is what I102f refuses and V365(b) charges by form.
$V333_SRC   = @{ MeritPicked = 22; OpenAbility = 21; HeaderPicker = 2; DiscRow = 19; MainPathRow = 4; SecPathRow = 15; RitualRow = 22; HedgePicker = 20; PsychicPicker = 20; HedgeRitualPicker = 20 }
$q333       = @{}
$qCalls333  = 0
$qOpen333   = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
foreach ($f333b in $files) {
    $doc333b = Doc $f333b.FullName
    $raw333b = [System.IO.File]::ReadAllText($f333b.FullName)
    foreach ($tpl333 in $doc333b.SelectNodes("//template[@name]")) {
        $tn333 = $tpl333.GetAttribute("name")
        foreach ($b333 in $tpl333.SelectNodes(".//button")) {
            if ($b333.GetAttribute("name") -notlike 'btnQ*') { continue }
            # the call sites of the template that draws it, in the file that declares it
            $calls333 = @([regex]::Matches($raw333b, '<' + [regex]::Escape($tn333) + '[ />]')).Count
            if (-not $q333.ContainsKey($tn333)) { $q333[$tn333] = 0 }
            $q333[$tn333] += $calls333
            $qCalls333 += $calls333

            # (d) ONE opener for all twenty-eight, and each one hands it the value of ITS OWN
            # row. A literal key would compile, look right and open the same description from
            # every button on the sheet - the failure that has no symptom until someone reads.
            $oc333 = $b333.GetAttribute("onClick")
            $call333 = [regex]::Match($oc333, '^\s*([A-Za-z_]\w*)\s*\(')
            if (-not $call333.Success) { $v333Bad += "$($f333b.Name) $($b333.GetAttribute('name')) calls nothing on click - the ? would open no box at all (SPEC V333d)" }
            else { [void]$qOpen333.Add($call333.Groups[1].Value) }
            if ($oc333 -notmatch 'sheet\[') { $v333Bad += "$($f333b.Name) $($b333.GetAttribute('name')) passes no row value to the opener - a literal key opens one description from every row (SPEC V333d, I102b)" }
        }
    }
}
# (g) zero-guard FIRST: everything under it is a count, and a count over an empty set is the
# check that measures nothing and says PASS (SPEC V20, B7, B92).
if ($qCalls333 -eq 0) { $v333Bad += "no ? button was reached at all - legs (d) and (g) are measuring an empty set (SPEC V209, V20, B92)" }
else {
    foreach ($k333 in $q333.Keys) {
        if (-not $V333_SRC.ContainsKey($k333)) { $v333Bad += "template '$k333' draws a ? button and is not on the named roster - the total below would swallow it and still read 28 (SPEC V333g, V109)" }
        elseif ($q333[$k333] -ne $V333_SRC[$k333]) { $v333Bad += "'$k333' draws the ? on $($q333[$k333]) row(s), expected $($V333_SRC[$k333]) - a row short is a description the player cannot reach, a row over is a button on a TYPED row, which I102f refuses (SPEC V333g)" }
    }
    foreach ($k333 in $V333_SRC.Keys) {
        if (-not $q333.ContainsKey($k333)) { $v333Bad += "'$k333' carries no ? button - the $($V333_SRC[$k333]) rows it draws lost their description door (SPEC I102b, V333g)" }
    }
    if ($qCalls333 -ne 165) { $v333Bad += "$qCalls333 ? button(s) in a TEMPLATE on the sheet, expected the 165 of I102b/I102h - 22 on the merit/flaw rows, 21 on the background rows, 2 on the nature/demeanor headers, the 60 the Ghoul tab carries after T879 and T880 (19 discipline, 4 main path, 15 secondary path, 22 ritual) and the 52 the Numina tab carries after T875 (20 hedge path, 20 psychic, and 12 hedge ritual - seventeen after T892 converted the last five and TWENTY since T912 squared the box with HEDGE MAGIC PATHS, SPEC V269b, I136m) (SPEC V333g, T846, T872, T873, T874, T830, T875)" }
    # The ? buttons authored OUTSIDE a template, which the walk above cannot see: it reads
    # templates because that is where 148 buttons come from two dozen <button> elements. T874 made
    # this a real category for the first time - btnQFaith is a ? on a row that is not drawn by a
    # template and whose key is a LITERAL, because True Faith is ONE Path with one entry and no
    # slot behind it (SPEC I61, V50, V265). Left unlisted it would escape leg (d) entirely, which
    # is the vacancy V354(i) was written about, one door over. NAMED, never a skip: a second bare
    # ? has to redden here rather than be absorbed (SPEC V328d, I114f).
    # btnQhedgeAffiliation joined in T874 half B: the affiliation row is a label+entry GRID row
    # and not a template-drawn list row, so its ? is authored inline like the True Faith one
    # (SPEC I107a1, V297).
    $BARE_Q333 = @('btnQFaith', 'btnQroad')   # btnQclanFamily left in T925 - clanFamily is V407's named exception
    $bareSeen333 = @()
    foreach ($f333c in $files) {
        foreach ($b333c in (Doc $f333c.FullName).SelectNodes("//button[@text='?']")) {
            if ($null -ne (TplOf $b333c)) { continue }
            $bn333 = $b333c.GetAttribute("name")
            $bareSeen333 += $bn333
            if ($BARE_Q333 -notcontains $bn333) { $v333Bad += "$($f333c.Name) draws a ? named '$bn333' outside any template and it is on no roster - the template walk cannot see it, so leg (d) would never ask what it opens (SPEC V333g, V354i, V328d)" }
            else {
                $oc333c = $b333c.GetAttribute("onClick")
                if ($oc333c -notmatch '^\s*popOpen\(') { $v333Bad += "$bn333 opens with '$oc333c' - one opener owns the box, bare ? or not (SPEC V333d, V135)" }
            }
        }
    }
    foreach ($bq333 in $BARE_Q333) {
        if ($bareSeen333 -notcontains $bq333) { $v333Bad += "'$bq333' is on the bare-? roster and is authored nowhere - a stale name excuses the next bare ? that appears (SPEC V333g, V20)" }
    }

    if ($qOpen333.Count -gt 1) { $v333Bad += "the ? buttons call $($qOpen333.Count) different functions ($(($qOpen333 | Sort-Object) -join ', ')) - one opener owns the box, or the two disagree about what is open (SPEC V135, V333d)" }
    elseif ($qOpen333.Count -eq 1 -and -not $qOpen333.Contains('popOpen')) { $v333Bad += "the ? buttons call '$(@($qOpen333)[0])' and not popOpen - the opener I102d declares is the one that also raises the scrim (SPEC V333d)" }
}

# The opener itself still has to exist and keep its shape, because every btnQ names it.
# `sub` joined the signature in T791 and `row` in T845. `sub` is OPTIONAL - it names the
# list ("merit"/"flaw") and only matters where one name lives in both (SPEC I102g); `row` names
# the FIELD of the line that opened the box, which is what the storyteller's write needs to know
# to land anywhere (SPEC I102i, V365f). The first three stay pinned in order because every btnQ
# names them, and `row` joins them pinned: BOTH call sites pass it, and a popOpen that quietly
# loses it still compiles while the storyteller types into a box that saves nowhere. A sixth
# parameter is allowed after it, the way this line was extended twice already.
if ($rootLua333 -notmatch 'function popOpen\(from, kind, key, sub, row[,)]') { $v333Bad += "popOpen is gone or changed shape - it is the ONE opener every btnQ will name, with the row's key as an argument (SPEC I102d, V333d, I102g)" }
if ($rootLua333 -notmatch 'function popClose\(from\)') { $v333Bad += "popClose is gone - the X and the scrim both name it (SPEC V333e)" }

if ($v333Bad) { foreach ($b in $v333Bad) { Fail "V333 $b" } }
else { Pass "V333 one ? box and one scrim, the scrim painted under it, both born hidden, one closer behind both doors, edtPopDesc wrapping so no horizontal bar can appear, and $qCalls333 ? buttons - $(($V333_SRC.Keys | Sort-Object | ForEach-Object { "$($q333[$_]) on $_" }) -join ', ') - all reaching popOpen with their own row's value" }

# ---- V334: the zoom has ONE ceiling and ONE floor --------------------------------------
# SPEC I103, V334, T762, and the user's answer to Q7 ("Salvar"). The size is ONE field for all
# every pane and not one field per pane: the user asked for it saved, not saved per pane, and
# nine keys in the data contract cannot be taken back later (SPEC V2).
$v334Bad = @()
# REWRITTEN by T847: the roster is DERIVED from the code, never typed. This line was written out
# as a literal three times and aged three times - eight panes, then nine, then I103 promised two -
# and a typed number is what made each of those a separate edit somebody had to remember. Now the
# XML is the source, the Lua table and the button pairs are compared AGAINST it, and a pane that
# arrives or leaves moves all three together or the gate names which one lagged (SPEC V334b, B102).
$PANES334 = @()
foreach ($f334 in $files) {
    foreach ($te334 in @((Doc $f334.FullName).SelectNodes("//textEditor[@name]"))) {
        $nm334 = $te334.GetAttribute("name")
        if ($nm334 -match '^edt.*Desc$') { $PANES334 += $nm334 }
    }
}
$PANES334 = @($PANES334 | Sort-Object -Unique)

# zero-guard FIRST: everything under this compares against the derived set, and a set that comes
# back empty makes every one of those comparisons pass on nothing (SPEC V20, B7, B92).
if ($PANES334.Count -eq 0) { $v334Bad += "no edt*Desc textEditor was found in any .lfm - the roster this leg derives is empty, so every count below measures nothing (SPEC V209, V20)" }

# The Lua half has to name the SAME set: DESC_PANES is what applyDescFont walks, so a pane the
# XML carries and the table does not is a pane the zoom skips in silence (SPEC V334e, V135).
$luaPanes334 = @()
if ($rootTxt -match '(?s)local DESC_PANES = \{(.*?)\};') {
    $luaPanes334 = @([regex]::Matches($Matches[1], 'edt[A-Za-z]*Desc') | ForEach-Object { $_.Value } | Sort-Object -Unique)
}
if ($luaPanes334.Count -eq 0) { $v334Bad += "DESC_PANES is gone or unreadable in WoD20th.lfm - it is the list applyDescFont walks, and without it the Lua half of this roster measures nothing (SPEC V334e, V209)" }
else {
    foreach ($p334x in $PANES334) {
        if ($luaPanes334 -notcontains $p334x) { $v334Bad += "'$p334x' is authored in the XML and absent from DESC_PANES - the zoom would skip it in silence (SPEC V334b, V135)" }
    }
    foreach ($l334x in $luaPanes334) {
        if ($PANES334 -notcontains $l334x) { $v334Bad += "'$l334x' is named in DESC_PANES and authored in no .lfm - the table is carrying a pane that does not exist (SPEC V334b, V209)" }
    }
}

# (b) every derived pane with its pair, found BY NAME off the pane's own suffix - so a button
# that drifts onto another box is a miss here and not a silent pass on a total that still adds up.
$btn334 = @()
foreach ($f334 in $files) {
    foreach ($b334 in (Doc $f334.FullName).SelectNodes("//button[starts-with(@name,'btnFont')]")) {
        $btn334 += [pscustomobject]@{ File = $f334.Name; Name = $b334.GetAttribute("name"); Click = $b334.GetAttribute("onClick") }
    }
}
if ($btn334.Count -eq 0) { $v334Bad += "no btnFont* button was collected - the zoom has no controls at all and every leg below would pass on an empty set (SPEC V20, B7)" }
else {
    foreach ($p334 in $PANES334) {
        $sfx334 = ($p334 -replace '^edt', '') -replace 'Desc$', ''
        foreach ($way334 in @('Up', 'Down')) {
            $want334 = "btnFont$way334$sfx334"
            if (@($btn334 | Where-Object { $_.Name -eq $want334 }).Count -ne 1) {
                $v334Bad += "$want334 is missing or authored twice - each derived pane carries exactly one + and one - (SPEC I103b, V334b)"
            }
        }
    }
    if ($btn334.Count -ne ($PANES334.Count * 2)) { $v334Bad += "$($btn334.Count) btnFont* button(s) on the sheet against $($PANES334.Count) derived pane(s) - the sheet carries a pair per pane, and the expected number comes off the panes rather than out of a literal that ages (SPEC I112c, V334b, B102)" }

    # (c) every one of them goes through fontStep with the step and nothing else. A button that
    # writes .fontSize itself is a second place the ceiling lives (SPEC V135, V334a, V334c).
    foreach ($b334 in $btn334) {
        $wantClick334 = if ($b334.Name -like 'btnFontUp*') { 'fontStep(self, 2);' } else { 'fontStep(self, -2);' }
        if ($b334.Click -ne $wantClick334) { $v334Bad += "$($b334.File) $($b334.Name) calls '$($b334.Click)', expected '$wantClick334' - one function owns the step and both limits (SPEC V334c)" }
    }
}

# (a) the floor, the ceiling and the step are written ONCE, inside fontStep.
$fs334 = [regex]::Match($rootLua333, '(?s)function fontStep\(from, delta\)(.*?)\n\t{3}end;')
if (-not $fs334.Success) { $v334Bad += "fontStep is gone - it is the one place the 10, the 32 and the 2 are allowed to live, and without it this leg measured nothing (SPEC I103a, V334a, V209)" }
else {
    $body334 = $fs334.Groups[1].Value
    if ($body334 -notmatch 'if s < 10 then s = 10; end;') { $v334Bad += "fontStep has no floor at 10 - the minus button would run the text down to nothing (SPEC I103a, V334a)" }
    if ($body334 -notmatch 'if s > 32 then s = 32; end;') { $v334Bad += "fontStep has no ceiling at 32 - the plus button would run past what the box can hold (SPEC I103a, V334a)" }
    if ($body334 -notmatch 'setField\("descFontSize", s\)') { $v334Bad += "fontStep does not write descFontSize - the size would die with the session, and the user asked for it SAVED (SPEC I103c, Q7)" }

    # and nowhere else in the chunk: a hardcoded size on any pane is the second copy of the
    # number, which is how two buttons end up disagreeing about where the zoom stops.
    $outside334 = $rootLua333.Replace($fs334.Value, '')
    $hard334 = @([regex]::Matches($outside334, '\.fontSize\s*=\s*\d+'))
    if ($hard334.Count -gt 0) { $v334Bad += "$($hard334.Count) fontSize assignment(s) to a literal number outside fontStep ($(($hard334 | ForEach-Object { $_.Value.Trim() }) -join ', ')) - the size has one writer and it reads the field (SPEC V334a, V135)" }
}

# (e) the way back, which is the half V316c asks of theme and Q7 asks of this: the field is READ
# on open and applied to all eight, and a sheet that never had one opens at 12 and not at nil.
$ad334 = [regex]::Match($rootLua333, '(?s)function applyDescFont\(from\)(.*?)\n\t{3}end;')
if (-not $ad334.Success) { $v334Bad += "applyDescFont is gone - nothing would put the saved size back, and descFontSize would be a field written and never read (SPEC V334e)" }
else {
    $adBody334 = $ad334.Groups[1].Value
    if ($adBody334 -notmatch 'local size = 12;') { $v334Bad += "applyDescFont has no 12 to fall back on - a sheet saved before this round would open at nil (SPEC V334e)" }
    if ($adBody334 -notmatch 'DESC_PANES') { $v334Bad += "applyDescFont does not walk DESC_PANES - it would put the size back on fewer than the nine (SPEC I103c, I112c)" }
}
# and the walk has to REACH the nine: naming DESC_PANES inside applyDescFont says nothing about
# what DESC_PANES HOLDS, and a pane left out of the table wears its + and - and ignores them -
# a zoom that does nothing is the silent half B7 describes (SPEC I103c, I112c, T806).
$dp334 = [regex]::Match($rootLua333, '(?s)local DESC_PANES = \{(.*?)\};')
if (-not $dp334.Success) { $v334Bad += "DESC_PANES is gone from WoD20th.lfm - applyDescFont would walk nothing and every pane would keep the authored 12 (SPEC I103c, V209)" }
else {
    $inPanes334 = @([regex]::Matches($dp334.Groups[1].Value, '(\w+)\s*=\s*true') | ForEach-Object { $_.Groups[1].Value })
    if ($inPanes334.Count -eq 0) { $v334Bad += "DESC_PANES parsed to 0 pane(s) - the table's shape moved and this leg is measuring nothing (SPEC V20, B7)" }
    else {
        foreach ($want in $PANES334) { if ($inPanes334 -notcontains $want) { $v334Bad += "$want is not in DESC_PANES - it carries a zoom pair the walk never reaches, so its + and - do nothing (SPEC I103c, I112c)" } }
        foreach ($got in $inPanes334) { if ($PANES334 -notcontains $got) { $v334Bad += "DESC_PANES carries '$got', which is not one of the $($PANES334.Count) description panes - the walk would resize a control that is not one (SPEC I103c)" } }
    }
}
$ready334 = [regex]::Match($rootLua333, '(?s)<event name="onNodeReady">(.*?)</event>')
if (-not $ready334.Success) { $v334Bad += "the root form has no onNodeReady - there is nowhere for the way back to run (SPEC V209)" }
elseif ($ready334.Groups[1].Value -notmatch 'applyDescFont\(self\);') { $v334Bad += "the root onNodeReady never calls applyDescFont - the size would be written and never read, which is V316c's half-loop (SPEC V334e)" }

# (d) the panes still own no field. What leaks to the NDB on purpose is the SIZE; the TEXT does
# not, and V29 is untouched by this round.
foreach ($f334 in $files) {
    foreach ($n334 in (Doc $f334.FullName).SelectNodes("//textEditor[@name]")) {
        if ($PANES334 -notcontains $n334.GetAttribute("name")) { continue }
        if ($n334.HasAttribute("field")) { $v334Bad += "$($f334.Name) $($n334.GetAttribute('name')) owns a field - book text would be written onto the character (SPEC V29, V334d)" }
    }
}

# (f) descFontSize has no widget of its own, so it has to be DECLARED Lua-owned - otherwise the
# exception V8 grants would be a generic escape hatch instead of a closed list (SPEC I3, B25).
if ($luaOwned -notcontains 'descFontSize') { $v334Bad += "descFontSize is not in the declared Lua-owned list - a field with no widget owner needs the declaration, or V8's exception stops being a closed list (SPEC I3, V334f, B25)" }

if ($v334Bad) { foreach ($b in $v334Bad) { Fail "V334 $b" } }
else { Pass "V334 $($PANES334.Count) description panes - counted off the XML, matched against DESC_PANES - carry $($btn334.Count) zoom buttons, all of them through the one fontStep that owns the 10, the 32 and the 2, with descFontSize written on the click and read back on open" }

# ---- V337: a family ceiling refuses, and never tidies up (SPEC I106, I108c, T781) ---------
# The table is SPARSE by design: only the family a book actually limits has a row, and having no
# row is the normal case for twenty-one of the twenty-three. What this measures is that the rows
# that DO exist can fire, fire before anything is written, and never reach back to take away a
# dot the player had already marked.
$v337Bad = @()
$cap337 = @{}
$capBlock337 = [regex]::Match($rootTxt, '(?s)\n\t{3}FAMILY_CAP = \{(.*?)\n\t{3}\};')
if (-not $capBlock337.Success) {
    $v337Bad += "FAMILY_CAP is not declared on the root form - the ceilings I106 measured out of the books are not on the sheet at all (SPEC I106a, V209)"
} else {
    foreach ($m337 in [regex]::Matches($capBlock337.Groups[1].Value, '\["([^"]+)"\]\s*=\s*\{([^}]*)\}')) {
        $row337 = @{}
        foreach ($kv337 in [regex]::Matches($m337.Groups[2].Value, '(\w+)\s*=\s*(\d+)')) { $row337[$kv337.Groups[1].Value] = [int]$kv337.Groups[2].Value }
        $cap337[$m337.Groups[1].Value] = $row337
    }
}

# (g) zero-guard FIRST: two families are capped today, so an empty table is the parser breaking
# and not the sheet getting tidy - and every leg below would pass on the empty set (SPEC B7).
if ($cap337.Count -eq 0) { $v337Bad += "FAMILY_CAP parsed to no rows at all - two families carry a ceiling, so this is the collector failing and every leg below would pass on nothing (SPEC V20, B7)" }
else {
    # (a) a ceiling on a family the dropdown does not offer can never fire, which V337a calls
    # worse than absent: it reads like the rule is enforced when nothing enforces it.
    $famFor337 = @()
    if ($PICKER.ContainsKey('family')) { $famFor337 = @($PICKER['family'] | Where-Object { $_ }) }
    foreach ($k337 in $cap337.Keys) {
        if ($famFor337 -notcontains $k337) { $v337Bad += "FAMILY_CAP caps '$k337', which PICKER_LIST['family'] does not offer - a ceiling nobody can select never fires (SPEC V337a)" }
    }

    # (b) the Enrathi numbers, LITERAL, off gr p.95 - "may not have a Humanity rating higher
    # than 3" and a Conscience of two. Asserted rather than merely counted, because a cap that
    # drifts by one is a cap that reads correct and refuses the wrong dot (SPEC R127, V337b).
    if (-not $cap337.ContainsKey('Enrathi')) { $v337Bad += "FAMILY_CAP has no Enrathi row - it is the one cap R127 measured before this round and the one V337b names (SPEC V209)" }
    else {
        if ($cap337['Enrathi']['humanity'] -ne 3) { $v337Bad += "Enrathi caps humanity at '$($cap337['Enrathi']['humanity'])', expected 3 (SPEC R127, V337b)" }
        if ($cap337['Enrathi']['conscience'] -ne 2) { $v337Bad += "Enrathi caps conscience at '$($cap337['Enrathi']['conscience'])', expected 2 (SPEC R127, V337b)" }
    }
}

# (c) the NUMBER is read in one place. The entry points HAVE to be two - a blood pool dot may not
# reach xpClick (SPEC V219) and a rating dot may not skip it - but a second reader of the table
# is a second place the ceiling can be got wrong, which is what V135 is really about.
$fc337 = LuaFn $rootTxt 'familyCap'
if (-not $fc337) { $v337Bad += "familyCap is gone - each refuser would have to find the ceiling for itself, which is the two-owners disease V135 exists for (SPEC V337c)" }
$readers337 = @([regex]::Matches($rootTxt, 'FAMILY_CAP\['))
if ($readers337.Count -ne 1) { $v337Bad += "FAMILY_CAP is indexed $($readers337.Count) time(s) in the root chunk, expected exactly once, inside familyCap (SPEC V337c, V135)" }

# (d) BOTH refusers say no BEFORE they write. A guard that lets the mark land and argues
# afterwards cost three rounds once already (SPEC B38, V135, V337d).
$click337 = NoComments (LuaFn $rootTxt 'xpClick')
if (-not $click337) { $v337Bad += "xpClick not found - leg (d) measured nothing on the rating side (SPEC V209)" }
else {
    $capAt337 = $click337.IndexOf('familyCap(trait)')
    $markAt337 = $click337.IndexOf('markDot(')
    if ($capAt337 -lt 0) { $v337Bad += "xpClick never asks familyCap - humanity and conscience would take the dot with no ceiling in the way (SPEC I106b, I106c)" }
    elseif ($markAt337 -ge 0 -and $capAt337 -gt $markAt337) { $v337Bad += "xpClick asks familyCap AFTER it marks - the dot would land and the refusal would arrive too late to mean anything (SPEC V337d, B38)" }
}
$blood337 = NoComments (LuaFn $rootTxt 'bloodClick')
if (-not $blood337) { $v337Bad += "bloodClick not found - leg (d) measured nothing on the blood pool side (SPEC V209)" }
else {
    $bCap337 = $blood337.IndexOf('familyCap("bloodPool")')
    $bSet337 = $blood337.IndexOf('setField(field, true)')
    if ($bCap337 -lt 0) { $v337Bad += "bloodClick never asks familyCap - the blood ceiling would be data nobody reads (SPEC V337c)" }
    elseif ($bSet337 -lt 0) { $v337Bad += "bloodClick never turns a dot ON - the twenty dots would be one-way (SPEC V209)" }
    elseif ($bCap337 -gt $bSet337) { $v337Bad += "bloodClick asks familyCap AFTER it writes - that is the write-then-undo the whole amendment of V219 was made to avoid (SPEC V337d, Q20)" }
}

# (e) nothing REACHES BACK. Choosing a capped family with a rating already above it must not
# clear a single dot: an aged value stays, exactly as it does under the generation ceiling
# (SPEC I106d, V187, Q18). This is the leg that catches the tidy-up that looks like kindness.
$reach337 = @([regex]::Matches($rootTxt, 'setField\("(humanity_\d+|conscience[_\d]*|bloodPool_\d+)"\s*,\s*false'))
if ($reach337.Count -gt 0) { $v337Bad += "$($reach337.Count) place(s) write a capped trait to false - a cap stops the NEXT dot and never takes back one the player marked (SPEC I106d, V187, B75)" }

# (f) the refusal SPEAKS, and in both languages (SPEC I106f, V10, V130).
foreach ($side337 in @(@('pt', $ptK), @('en', $enK))) {
    if (-not $side337[1].Contains('That family caps this trait')) { $v337Bad += "the refusal has no [$($side337[0])] key - it would show the raw English to a player reading Portuguese, and V130 exists because a string living only in Lua escapes the XML checks (SPEC I106f)" }
}

if ($v337Bad) { foreach ($b in $v337Bad) { Fail "V337 $b" } }
else { Pass "V337 $($cap337.Count) family ceiling(s), all on families the dropdown offers, the number read in one place, both refusers saying no before they write, nothing reaching back to unmark a dot, and the refusal keyed in both languages" }

# ---- V341: Grimaldi is TWO families, split on the weakness (SPEC I108b, R129, Q14) --------
# R93 put Grimaldi on one line because the Discipline TRIO matches in both books. It does -
# Celerity, Dominate, Fortitude in da p.454 and in gr p.98 alike - and that was the whole
# comparison. The WEAKNESS is the second axis and it does NOT match: the medieval family is
# capped at eight blood points, the modern one carries a behavioural flaw with no number in it.
# One line was an INCOMPLETE decision rather than a wrong one, and this is what stops it from
# quietly closing back up.
$v341Bad = @()
$fam341 = @()
if ($PICKER.ContainsKey('family')) { $fam341 = @($PICKER['family'] | Where-Object { $_ }) }

# zero-guard FIRST: an empty list means the parser broke, and every leg below would pass on it.
if ($fam341.Count -eq 0) { $v341Bad += "PICKER_LIST['family'] parsed to nothing - the collector broke and every leg below would pass on the empty set (SPEC V20, B7)" }
else {
    # (a) 23 entries, both era names present, and the bare name GONE. A bare Grimaldi surviving
    # beside the two would be a third family nobody decided on, and CLANS would answer for it.
    if ($fam341.Count -ne 23) { $v341Bad += "PICKER_LIST['family'] holds $($fam341.Count) entries, expected 23 - the 22 R93 froze, with Grimaldi split by era and Obertus by line (SPEC I108b, V341a, V342a)" }
    foreach ($want341 in @('Grimaldi (Dark Ages)', 'Grimaldi (Modern)')) {
        if ($fam341 -notcontains $want341) { $v341Bad += "'$want341' is not in the family list - the split R129 measured is not on the sheet (SPEC V341a)" }
    }
    if ($fam341 -contains 'Grimaldi') { $v341Bad += "the bare 'Grimaldi' is still in the family list beside the two era entries - a third family nobody decided on, and a sheet saved on it would go on answering (SPEC V341a)" }

    # (b) both carry the SAME trio, because the trio is exactly what did NOT diverge. A check
    # that only asserted "two entries exist" would pass on two entries disagreeing about the
    # Disciplines, which is the opposite of what R129 found.
    $trio341 = @('Celerity', 'Dominate', 'Fortitude')
    foreach ($k341 in @('Grimaldi (Dark Ages)', 'Grimaldi (Modern)')) {
        if (-not $clanEntries.ContainsKey($k341)) { $v341Bad += "CLANS has no entry for '$k341' - the dropdown would offer a family that fills no Discipline slot (SPEC V341b, V236a)"; continue }
        $got341 = @($clanEntries[$k341].fixed)
        if (($got341 -join '|') -ne ($trio341 -join '|')) { $v341Bad += "CLANS gives '$k341' the trio '$($got341 -join ', ')', expected '$($trio341 -join ', ')' - the trio is what MATCHED in both books, and the weakness is what split them (SPEC R129, V341b)" }
    }

    # (d) RETIRED in T954: it counted the clan+family union at 85, and the union left with its
    # only reader when the Ghoul tab got one button per list (SPEC V426d, V341 amended). What
    # the leg guarded - the list growing with the split - leg (a) already reads off the family
    # list itself.
}

# (c) the medieval family is capped at eight blood points and the modern one is NOT. The user
# settled how on 2026-08-28 (Q20): V219 was amended so the dots stop marking themselves, which
# is what gave the cap a BEFORE to stand in. The pair matters more than either half - a cap on
# both would be the sheet outvoting gr p.98, which gives the modern family a behavioural flaw
# with no number in it at all (SPEC R129, I108c, V341c).
if ($cap337.Count -gt 0) {
    if (-not $cap337.ContainsKey('Grimaldi (Dark Ages)')) { $v341Bad += "FAMILY_CAP has no 'Grimaldi (Dark Ages)' row - da p.454 caps the family at eight blood points and the split exists to carry that (SPEC I108c, V341c)" }
    elseif ($cap337['Grimaldi (Dark Ages)']['bloodPool'] -ne 8) { $v341Bad += "the medieval Grimaldi caps bloodPool at '$($cap337['Grimaldi (Dark Ages)']['bloodPool'])', expected 8 (SPEC R129, V341c)" }
    if ($cap337.ContainsKey('Grimaldi (Modern)')) { $v341Bad += "the MODERN Grimaldi carries a cap - gr p.98 gives it a behavioural flaw with no number in it, and a ceiling invented here would be the sheet outvoting the book (SPEC R129, V341c)" }
}

if ($v341Bad) { foreach ($b in $v341Bad) { Fail "V341 $b" } }
else { Pass "V341 Grimaldi stands as two families on the 23-entry list, the bare name gone, both carrying the one trio that did not diverge" }
# ---- V342: the Obertus Narov LINE is its own entry, not a choice slot (SPEC T786, Q21) ----
# The number V342 is RESERVED here and /ck:spec still owes its SPEC text - the check is written
# first so the decision cannot regress while the spec catches up (the same order V333's deferred
# legs are recorded in).
#
# gr p.102: "Obertus revenants, who display signs of the Narov line, should swap Obfuscate for
# Dominate." That is a CONDITION, not a free pick, and the difference is the whole reason this
# is two entries instead of one entry with an open third slot: a choice slot would hand Dominate
# to ANY Obertus, which is permission the book does not give. Splitting by era would have been
# wrong for the other reason - the era does NOT diverge here (SPEC T786): the trio matches in
# both books and the weakness differs only in which Derangement, which this sheet never models.
$v342Bad = @()
$OBERTUS342 = @{ 'Obertus' = @('Auspex', 'Obfuscate', 'Vicissitude'); 'Obertus (Narov)' = @('Auspex', 'Dominate', 'Vicissitude') }
$fam342 = @()
if ($PICKER.ContainsKey('family')) { $fam342 = @($PICKER['family'] | Where-Object { $_ }) }

# zero-guard FIRST - an empty roster passes every leg below on nothing (SPEC V20, B7).
if ($fam342.Count -eq 0) { $v342Bad += "PICKER_LIST['family'] parsed to nothing - every leg below would pass on the empty set (SPEC V20, B7)" }
else {
    foreach ($k342 in $OBERTUS342.Keys) {
        if ($fam342 -notcontains $k342) { $v342Bad += "'$k342' is not in the family list - the pair Q21 settled is not on the sheet (SPEC V342a)" ; continue }
        if (-not $clanEntries.ContainsKey($k342)) { $v342Bad += "CLANS has no entry for '$k342' - the dropdown would offer a family that fills no Discipline slot (SPEC V236a)"; continue }
        $got342 = @($clanEntries[$k342].fixed)
        $want342 = $OBERTUS342[$k342]
        if (($got342 -join '|') -ne ($want342 -join '|')) { $v342Bad += "CLANS gives '$k342' the trio '$($got342 -join ', ')', expected '$($want342 -join ', ')' (SPEC R129, T786, V342b)" }
        # (d) the swap is a CONDITION: a fixed trio each, never an open slot. An open slot would
        # hand Dominate to any Obertus, which gr p.102 does not allow.
        if ($clanEntries[$k342].choice.Count -gt 0 -or $clanEntries[$k342].open -gt 0) {
            $v342Bad += "'$k342' is released by choice or open - the Narov swap is a condition the book ties to the LINE, and an open slot would offer Dominate to any Obertus (SPEC V342d, Q21)"
        }
    }
    # the two must actually DIFFER, and by exactly the one Discipline the book swaps. A check
    # that only asserted "two entries exist" would pass on two identical ones, which is the
    # split quietly closing back up.
    if ($clanEntries.ContainsKey('Obertus') -and $clanEntries.ContainsKey('Obertus (Narov)')) {
        $a342 = @($clanEntries['Obertus'].fixed)
        $b342 = @($clanEntries['Obertus (Narov)'].fixed)
        $diff342 = @(Compare-Object $a342 $b342 | ForEach-Object { $_.InputObject })
        $dl342 = ($diff342 | Sort-Object) -join '|'
        if ($dl342 -ne 'Dominate|Obfuscate') {
            $v342Bad += "the two Obertus trios differ by '$($diff342 -join ', ')', expected exactly Obfuscate for Dominate - that one swap IS the line (SPEC R129, V342b)"
        }
    }
    # (c) neither era gives Obertus a NUMBER, so neither entry may carry a ceiling. This is the
    # leg that stops T786's reading from being re-litigated as a cap nobody measured.
    foreach ($k342 in $OBERTUS342.Keys) {
        if ($cap337.ContainsKey($k342)) { $v342Bad += "FAMILY_CAP caps '$k342' - neither da p.454 nor gr p.102 puts a NUMBER on this family's weakness, and a ceiling invented here is the sheet outvoting the book (SPEC T774, V342c)" }
    }
}

if ($v342Bad) { foreach ($b in $v342Bad) { Fail "V342 $b" } }
else { Pass "V342 Obertus stands as two entries, one per line, differing by exactly the Obfuscate/Dominate swap gr p.102 names, each with a FIXED trio and neither carrying a ceiling" }


# ---- V340: the two merit/flaw tables ARE the list now (SPEC I100d, T750, B82) ----
# SPEC I100d asked for ONE table and said "no name is shared between merits and flaws -
# CONFIRMED before it holds". The confirmation ran in T750 and found `Cannibal`: a 1pt Merit
# in Lore of the Clans p.106 and a 3pt Flaw in Ghouls & Revenants p.136, opposite mechanics
# the dedupe keeps apart. So the module ships TWO tables and (b) below is per-table, with the
# crossing names NAMED here - an unnamed crossing is a dedupe that never happened (SPEC B82).
$v340Bad = @()
$V340_BOTH    = @('Cannibal')                     # name allowed in BOTH tables
$V340_NO_COST = @('Construct', 'Stormwarden')     # M20 Victorian Age p.122 prints no cost at all
$V340_N       = @{ MERIT_DATA = 380; FLAW_DATA = 493 }   # Q24 split them: 311/336 = 647 -> 380/493 = 873 (SPEC T796, V348c)
$V340_PICK    = @{ MERIT_DATA = 'merit'; FLAW_DATA = 'flaw' }
$mdPath = Join-Path $plugin "meritData.lua"
$mdTbl  = @{}
if (-not (Test-Path $mdPath)) {
    $v340Bad += "meritData.lua is not in the plugin folder - every leg below would pass on nothing (SPEC V20, B7)"
} else {
    $mdTxt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($mdPath))
    foreach ($tn in @('MERIT_DATA', 'FLAW_DATA')) {
        $blk = [regex]::Match($mdTxt, "(?s)local $tn = \{(.*?)\r?\n\};")
        if (-not $blk.Success) { $v340Bad += "table $tn is not in meritData.lua (SPEC I100d)"; continue }
        $d = New-Object 'System.Collections.Generic.Dictionary[string,object]' ([StringComparer]::Ordinal)
        foreach ($m in [regex]::Matches($blk.Groups[1].Value, '\["((?:[^"\\]|\\.)*)"\]\s*=\s*\{\s*book\s*=\s*"((?:[^"\\]|\\.)*)"\s*,\s*page\s*=\s*"([^"]*)"\s*,\s*cost\s*=\s*"([^"]*)"\s*\}')) {
            $k = $m.Groups[1].Value -replace '\\"', '"' -replace '\\\\', '\'
            # (b) no duplicate key INSIDE a table - Lua would keep the last one silently
            if ($d.ContainsKey($k)) { $v340Bad += "(b) '$k' appears twice in $tn - Lua keeps the last one and the first is lost without a word" ; continue }
            $d[$k] = [pscustomobject]@{
                Book = ($m.Groups[2].Value -replace '\\"', '"' -replace '\\\\', '\')
                Page = $m.Groups[3].Value
                Cost = $m.Groups[4].Value
            }
        }
        $mdTbl[$tn] = $d
    }
}
# (g) zero-guard FIRST - an empty table passes every leg below on nothing (SPEC V20, B7)
foreach ($tn in @('MERIT_DATA', 'FLAW_DATA')) {
    if (-not $mdTbl.ContainsKey($tn) -or $mdTbl[$tn].Count -eq 0) { $v340Bad += "(g) $tn parsed to nothing" }
}
if ($v340Bad.Count -eq 0) {
    foreach ($tn in @('MERIT_DATA', 'FLAW_DATA')) {
        $d = $mdTbl[$tn]
        # (f) the count T750 measured, and the picker list that stands beside it
        if ($d.Count -ne $V340_N[$tn]) { $v340Bad += "(f) $tn holds $($d.Count) entries, expected $($V340_N[$tn]) - a smaller table is an entry the regeneration dropped without a word (SPEC T750)" }
        $pk = @()
        if ($PICKER.ContainsKey($V340_PICK[$tn])) { $pk = @($PICKER[$V340_PICK[$tn]] | Where-Object { $_ -ne '' }) }
        if ($pk.Count -eq 0) { $v340Bad += "(f) PICKER_LIST['$($V340_PICK[$tn])'] parsed to nothing - the dropdown and the table cannot be compared" }
        else {
            $only = @($pk | Where-Object { -not $d.ContainsKey($_) })
            $miss = @($d.Keys | Where-Object { $pk -notcontains $_ })
            if ($only.Count) { $v340Bad += "(f) PICKER_LIST['$($V340_PICK[$tn])'] offers $($only.Count) item(s) $tn has no data for, first '$($only[0])' - picking it would write an empty book, page and cost" }
            if ($miss.Count) { $v340Bad += "(f) $tn carries $($miss.Count) key(s) the list never offers, first '$($miss[0])' - data nobody can reach" }
        }
        foreach ($k in $d.Keys) {
            $e = $d[$k]
            # (a) book and page are never empty; cost MAY be, in the two the book prints bare
            if ($e.Book -eq '') { $v340Bad += "(a) '$k' in $tn has no book title (SPEC I100e)" }
            if ($e.Page -eq '') { $v340Bad += "(a) '$k' in $tn has no page" }
            if ($e.Cost -eq '' -and $V340_NO_COST -notcontains $k) {
                $v340Bad += "(a) '$k' in $tn has an empty cost and is not one of the two M20 Victorian Age p.122 entries the book prints without one - an empty cost anywhere else means the parser lost it, and widening this exception turns it into a door (SPEC B82)"
            }
            # (c) every key is keyed in both languages AND in the PT map of WoD20.6
            if (-not $ptK.Contains($k)) { $v340Bad += "(c) '$k' has no [pt] key in localization.lang (SPEC V17)" }
            if (-not $enK.Contains($k)) { $v340Bad += "(c) '$k' has no [en] key in localization.lang (SPEC V17)" }
            if (-not $embedded.ContainsKey($k)) { $v340Bad += "(c) '$k' is not in the PT map of WoD20.6.lfm - it has a [pt] key nothing ever reads (SPEC V28, B14)" }
            # (d) a parenthesis without a collision is noise: it must have a sibling
            $mm = [regex]::Match($k, '^(.+) \([^)]+\)$')
            if ($mm.Success) {
                $stem = $mm.Groups[1].Value
                # o irmao pode ser outro sufixo OU o nome PELADO: o dedupe deixou `Oathbreaker`
                # sem sufixo (bh/bos/da, 4pt) ao lado de `Oathbreaker (Ravnos)` (lotc, 2pt). O que
                # V336d proibe e parentese SEM colisao, e essa colisao existe.
                $sib = 0
                foreach ($tn2 in @('MERIT_DATA', 'FLAW_DATA')) {
                    foreach ($k2 in $mdTbl[$tn2].Keys) { if ($k2 -ne $k -and ($k2 -like "$stem (*)" -or $k2 -eq $stem)) { $sib++ } }
                }
                if ($sib -eq 0) { $v340Bad += "(d) '$k' wears an origin suffix with no sibling '$stem' or '$stem (...)' anywhere - a parenthesis without a collision is noise (SPEC V336d)" }
            }
        }
    }
    # (b) a name in BOTH tables has to be one the dedupe NAMED
    $inBoth = @($mdTbl['MERIT_DATA'].Keys | Where-Object { $mdTbl['FLAW_DATA'].ContainsKey($_) })
    foreach ($k in $inBoth) {
        if ($V340_BOTH -notcontains $k) { $v340Bad += "(b) '$k' is in BOTH tables and is not on the named list - two mechanics under one name is the silent loss rule 0 of the dedupe exists to stop (SPEC B82)" }
    }
    foreach ($k in $V340_BOTH) {
        if (-not ($mdTbl['MERIT_DATA'].ContainsKey($k) -and $mdTbl['FLAW_DATA'].ContainsKey($k))) { $v340Bad += "(b) '$k' is named as crossing both tables but no longer does - the exception outlived the reason for it" }
    }
    # (e) neither table may be read from a .lfm <script>: the CDATA is copied per open sheet
    foreach ($f340 in (Get-ChildItem -LiteralPath $dir -Filter *.lfm)) {
        $t340 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f340.FullName))
        foreach ($tn in @('MERIT_DATA', 'FLAW_DATA')) {
            if ($t340 -match "(?<![A-Za-z_])$tn(?![A-Za-z_])") { $v340Bad += "(e) $($f340.Name) names $tn inside its own script - the CDATA is copied raw into constructNew_*, so every open sheet would parse its own copy (SPEC I24, I100g, T479)" }
        }
    }
}
if ($v340Bad) { foreach ($b in $v340Bad) { Fail "V340 $b" } }
else { Pass "V340 meritData.lua ships $($mdTbl['MERIT_DATA'].Count) merits and $($mdTbl['FLAW_DATA'].Count) flaws, each with a book and a page, cost empty only in the two the book prints bare, every key matching its picker list and keyed in [pt], [en] and the PT map, every origin suffix with a sibling, `Cannibal` the one name in both, and neither table read from a .lfm script" }

# ---- V343: the SOURCE of those tables, measured (SPEC I100d, T750, B83, B84) ----
# The .lua above is derived. research/merit_flaw.tsv is where the data actually lives, and it
# is the file that rotted quietly: 81 `da` rows sat with no TYPE from T749 until T750 went to
# build the lists, because nothing read them in between (SPEC B83). Data nobody reads has no
# gate, so it rots - this is the gate.
$v343Bad = @()
$V343_BOOKS = @('core','lotc','gr','hh','bh','anarch','rob','lob','da','tos','dac','bos','sorc','sorcc','m20va')
$V343_ROWS  = 782
# 641 e ORDINAL de proposito. `Sleeping With the Enemy` (core p.492) e
# `Sleeping with the Enemy` (bos p.66) diferem so no W e sao 2 linhas de livros diferentes -
# uma hashtable comum do PowerShell e case-insensitive e funde as duas, que e o aviso que
# merit_flaw_pt.tsv ja carrega no cabecalho. O gerador FUNDE por nome (o core vence a precedencia).
$V343_NAMES = 641
$tsv343 = Join-Path $PSScriptRoot "research\merit_flaw.tsv"

$pt343  = Join-Path $PSScriptRoot "research\merit_flaw_pt.tsv"

if (-not (Test-Path $tsv343)) { $v343Bad += "research/merit_flaw.tsv not found at '$tsv343' - every leg below would pass on nothing (SPEC V20, B7)" }
elseif (-not (Test-Path $pt343)) { $v343Bad += "research/merit_flaw_pt.tsv not found at '$pt343'" }
else {
    $rows343 = @([System.IO.File]::ReadAllLines($tsv343, [System.Text.Encoding]::UTF8) | Where-Object { $_ -notmatch '^#' -and $_.Trim() -ne '' })
    $tr343 = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
    foreach ($l in [System.IO.File]::ReadAllLines($pt343, [System.Text.Encoding]::UTF8)) {
        if ($l -match '^#' -or $l.Trim() -eq '') { continue }
        $c = $l -split "`t"
        if ($c.Count -ge 2 -and $c[1].Trim() -ne '') { [void]$tr343.Add($c[0].Trim()) }
    }
    # (e) zero-guard FIRST
    if ($rows343.Count -eq 0) { $v343Bad += "(e) merit_flaw.tsv is empty or all comment" }
    elseif ($tr343.Count -eq 0) { $v343Bad += "(e) merit_flaw_pt.tsv is empty or all comment" }
    else {
        $names343 = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
        foreach ($l in $rows343) {
            $c = $l -split "`t"
            if ($c.Count -lt 6) { $v343Bad += "(b) a row has $($c.Count) columns, expected 6: '$($l.Substring(0, [Math]::Min(50, $l.Length)))'" ; continue }
            [void]$names343.Add($c[0].Trim())
            # (a) the column that rotted
            if ($c[1].Trim() -eq '') { $v343Bad += "(a) '$($c[0].Trim())' ($($c[4].Trim()) p.$($c[5].Trim())) has no TYPE - it has no list on the sheet to enter, and nothing downstream would say so (SPEC B83)" }
            elseif ($c[1].Trim() -notin @('Merit', 'Flaw')) { $v343Bad += "(a) '$($c[0].Trim())' has type '$($c[1].Trim())', which is neither Merit nor Flaw" }
            if ($V343_BOOKS -notcontains $c[4].Trim()) { $v343Bad += "(b) '$($c[0].Trim())' names book '$($c[4].Trim())', which is not one of the 15 in research/README.md" }
            if ($c[5].Trim() -notmatch '^\d+$') { $v343Bad += "(b) '$($c[0].Trim())' has page '$($c[5].Trim())', which is not a number" }
        }
        # (c) no orphan in EITHER direction - an orphan is the trail of a name changed on one side
        foreach ($n in $names343) { if (-not $tr343.Contains($n)) { $v343Bad += "(c) '$n' has no translation in merit_flaw_pt.tsv" } }
        foreach ($n in $tr343)   { if (-not $names343.Contains($n)) { $v343Bad += "(c) merit_flaw_pt.tsv translates '$n', which merit_flaw.tsv no longer carries" } }
        # (d) the count - smaller is an entry the re-extraction lost without a word (SPEC B84)
        if ($rows343.Count -ne $V343_ROWS)   { $v343Bad += "(d) merit_flaw.tsv holds $($rows343.Count) rows, expected $V343_ROWS - a re-extraction that loses entries exits 0 and says nothing (SPEC B84)" }
        if ($names343.Count -ne $V343_NAMES) { $v343Bad += "(d) merit_flaw.tsv holds $($names343.Count) distinct names, expected $V343_NAMES" }
    }
}
if ($v343Bad) { foreach ($b in $v343Bad) { Fail "V343 $b" } }
else { Pass "V343 research/merit_flaw.tsv holds $V343_ROWS rows and $V343_NAMES names, every one typed Merit or Flaw, every book among the 15 and every page a number, and merit_flaw_pt.tsv matches it with no orphan in either direction" }


# ---- V336: the background list - Artifacts in place, the two translations changed, and a
# parenthesis ONLY where a collision exists (SPEC V336, I105, T764, T766, T767) ------------
# Six legs:
#   (a) `Artifacts` is in PICKER_LIST["background"], between `Armory` and `Avatar` (I105a);
#   (b) [pt] wod.Node = Nodo and wod.Pure Breed = Raca Pura, LITERAL, and [en] still reads
#       the English word on both (I105c);
#   (c) no item appears twice - V14 covers the general case, this leg is the one the
#       parenthesis opened: `Status (Mage)` written twice reads as two games and is one;
#   (d) every `Name (Origin)` item has at least one SIBLING `Name (OtherOrigin)`. A
#       parenthesis with no collision behind it is noise, and it is also the shape a
#       half-applied split leaves (I105b, measured in research/bg_essence.tsv);
#   (e) every item has a key in descBackground_en AND _pt, so the ? button never opens an
#       empty box (I102e). T765 has not shipped those modules yet, and this leg says so OUT
#       LOUD in the Pass line rather than passing quietly on nothing (SPEC B7, V209);
#   (f) zero-guard on all of it: an empty list, or a list with no parenthesised item at all,
#       makes (c) and (d) true by vacuum.
#
# ASCII source on purpose (SPEC B84): PowerShell 5.1 reads a .ps1 without BOM as ANSI, so a
# literal `Raca Pura` with the cedilla would arrive mojibake and the comparison would fail
# SILENTLY. The accented string is built from its code point, the way V273 already does.
$V336_PURE_PT = "Ra" + [char]0xE7 + "a Pura"
$v336Bad = @()
$bg336 = @($PICKER['background'])
$v336Paren = @()
$v336DescNote = ''
if ($bg336.Count -lt 2) {
    $v336Bad += "(f) PICKER_LIST[`"background`"] read $($bg336.Count) item(s) - every leg below would be true by vacuum (SPEC V209, B7)"
} else {
    # (a) position, not mere presence: I105a named the two neighbours
    $i336 = [Array]::IndexOf($bg336, 'Artifacts')
    if ($i336 -lt 0) { $v336Bad += "(a) 'Artifacts' is not in the background list at all (SPEC I105a)" }
    elseif ($i336 -eq 0 -or $bg336[$i336 - 1] -cne 'Armory' -or $i336 -eq ($bg336.Count - 1) -or $bg336[$i336 + 1] -cne 'Avatar') {
        $v336Bad += "(a) 'Artifacts' sits between '$(if ($i336 -gt 0) { $bg336[$i336 - 1] } else { '<start>' })' and '$(if ($i336 -lt ($bg336.Count - 1)) { $bg336[$i336 + 1] } else { '<end>' })', not between 'Armory' and 'Avatar' (SPEC I105a)"
    }

    # (b) the two translations the user changed, LITERAL on the [pt] side and untouched on [en]
    $v336LangTxt = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($langFile))
    $v336En = [regex]::Match($v336LangTxt, '(?s)\[en\](.*?)(\n\[|$)')
    $v336EnTxt = $(if ($v336En.Success) { $v336En.Groups[1].Value } else { '' })
    if ($v336EnTxt.Length -eq 0) { $v336Bad += "(f) the [en] section of localization.lang read empty - the [en] half of leg (b) would pass on nothing (SPEC V209)" }
    foreach ($p336 in @(@('Node', 'Nodo'), @('Pure Breed', $V336_PURE_PT))) {
        if (-not $ptVal.ContainsKey($p336[0])) { $v336Bad += "(b) '$($p336[0])' has no [pt] key in localization.lang (SPEC I105c, V17)" }
        elseif ($ptVal[$p336[0]] -cne $p336[1]) { $v336Bad += "(b) [pt] wod.$($p336[0]) reads '$($ptVal[$p336[0]])', and I105c fixed it at '$($p336[1])'" }
        if ($v336EnTxt.IndexOf("wod.$($p336[0])=$($p336[0])") -lt 0) { $v336Bad += "(b) the [en] key wod.$($p336[0])=$($p336[0]) is gone - I105c changed the [pt] side only" }
    }

    # (c) and (d) - the two the parenthesis opened
    $seen336 = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
    foreach ($it336 in $bg336) {
        if ($it336 -eq '') { continue }
        if (-not $seen336.Add($it336)) { $v336Bad += "(c) '$it336' is in the background list twice - the second one is a row the player can pick that means the same as the first" }
        if ($it336 -match '^(.+?) \(([^()]+)\)$') { $v336Paren += ,@($it336, $Matches[1], $Matches[2]) }
    }
    foreach ($p336 in $v336Paren) {
        $sib336 = 0
        foreach ($q336 in $v336Paren) { if ($q336[1] -ceq $p336[1] -and $q336[2] -cne $p336[2]) { $sib336++ } }
        if ($sib336 -eq 0) { $v336Bad += "(d) '$($p336[0])' wears an origin suffix with no sibling '$($p336[1]) (...)' in the list - a parenthesis without a collision is noise, and it is what a half-applied split looks like (SPEC I105b)" }
    }
    if ($v336Paren.Count -eq 0) { $v336Bad += "(f) no background item carries an origin suffix - leg (d) measured nothing, and I105b put at least the Status pair in this list (SPEC V209, B7)" }

    # (e) the description modules T765 owes. Absent is reported, never assumed away.
    $en336 = DescEntries (Join-Path $plugin "descBackground_en.lua") 'BACKGROUND_DESC' 'en'
    $pt336 = DescEntries (Join-Path $plugin "descBackground_pt.lua") 'BACKGROUND_DESC' 'pt'
    if ($null -eq $en336 -and $null -eq $pt336) {
        $v336DescNote = ", and leg (e) MEASURED NOTHING because descBackground_en.lua and _pt.lua are not at the plugin root yet (SPEC T765)"
    } elseif ($null -eq $en336 -or $null -eq $pt336) {
        $v336Bad += "(e) only one half of descBackground is at the plugin root, or its BACKGROUND_DESC markers are gone - one language would open an empty box (SPEC I102e, V210)"
    } else {
        $k336 = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
        foreach ($e336 in $en336) { [void]$k336.Add($e336.Key) }
        $j336 = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
        foreach ($e336 in $pt336) { [void]$j336.Add($e336.Key) }
        foreach ($it336 in $bg336) {
            if ($it336 -eq '') { continue }
            if (-not $k336.Contains($it336)) { $v336Bad += "(e) '$it336' has no entry in descBackground_en.lua - its ? button opens an empty box (SPEC I102e)" }
            if (-not $j336.Contains($it336)) { $v336Bad += "(e) '$it336' has no entry in descBackground_pt.lua - its ? button opens an empty box (SPEC I102e)" }
        }
        $v336DescNote = ", and all $($bg336.Count - 1) carry a description on both sides"
    }
}
if ($v336Bad) { foreach ($b in $v336Bad) { Fail "V336 $b" } }
else { Pass "V336 the background list holds $($bg336.Count - 1) items with 'Artifacts' between 'Armory' and 'Avatar', Node/Pure Breed translated on the [pt] side only, no item twice, and each of the $($v336Paren.Count) origin suffixes with a sibling$v336DescNote" }


# ---- V344: the DESCRIPTION module of merits and flaws has no key that eats another --------
# (SPEC V344, I102g, B89, T791/T792)
# V340b already forbids a duplicate INSIDE each table of meritData.lua, and nothing looked at
# descMerit_* at all - that gap is how `Cannibal` got in twice (Merit in Lore of the Clans
# p.106, Flaw in Ghouls & Revenants p.136) and the second entry ate the first in silence, so
# one of the two opened the other's text. Five legs:
#   (a) no key appears twice in either half;
#   (b) every name V340 marks as living in BOTH lists exists ONLY in composite form
#       (`merit|<name>` AND `flaw|<name>`), and the bare form of that name is gone;
#   (c) a name from one list only carries NO prefix - a prefix without a collision is noise,
#       the same rule leg (d) of V336 applies to the origin suffix;
#   (d) popOpen builds the key by CONCATENATION, never as a literal, or the renderer ends up
#       nailed to one of the two halves the same way V210b describes;
#   (e) zero-guard: nothing read, or no name in both lists, means (b) measured nothing.
$v344Bad = @()
$half344 = @{}
foreach ($lang344 in @('en', 'pt')) {
    $half344[$lang344] = DescEntries (Join-Path $plugin "descMerit_$lang344.lua") 'MERIT_DESC' $lang344
    if ($null -eq $half344[$lang344]) { $v344Bad += "descMerit_$lang344.lua is not at the plugin root, or its MERIT_DESC markers are gone (SPEC I102e, V209)" }
}
# the names that live in both lists come from meritData.lua, which is the list's real artifact
$both344 = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
$md344 = Join-Path $plugin 'meritData.lua'
if (-not (Test-Path $md344)) { $v344Bad += "meritData.lua is not at the plugin root - there is nothing to say which names live in both lists (SPEC V209)" }
else {
    $mdTxt344 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($md344))
    $tbl344 = @{}
    foreach ($t344 in @('MERIT_DATA', 'FLAW_DATA')) {
        $r344 = [regex]::Match($mdTxt344, '(?s)' + $t344 + '\s*=\s*\{(.*?)\n\};')
        $s344 = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
        if ($r344.Success) { foreach ($m344 in [regex]::Matches($r344.Groups[1].Value, '\["([^"]+)"\]\s*=')) { [void]$s344.Add($m344.Groups[1].Value) } }
        $tbl344[$t344] = $s344
    }
    if ($tbl344['MERIT_DATA'].Count -eq 0 -or $tbl344['FLAW_DATA'].Count -eq 0) {
        $v344Bad += "(e) meritData.lua parsed to $($tbl344['MERIT_DATA'].Count) merit(s) and $($tbl344['FLAW_DATA'].Count) flaw(s) - leg (b) would have no name to check (SPEC V209, B7)"
    }
    foreach ($n344 in $tbl344['MERIT_DATA']) { if ($tbl344['FLAW_DATA'].Contains($n344)) { [void]$both344.Add($n344) } }
}
if ($both344.Count -eq 0 -and $v344Bad.Count -eq 0) {
    $v344Bad += "(e) no name lives in both lists - leg (b) measured nothing, and `Cannibal` has been in both since T750 (SPEC V209, B7, B89)"
}
foreach ($lang344 in @('en', 'pt')) {
    if ($null -eq $half344[$lang344]) { continue }
    $seen344 = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
    foreach ($e344 in $half344[$lang344]) {
        if (-not $seen344.Add($e344.Key)) { $v344Bad += "(a) descMerit_$lang344.lua carries '$($e344.Key)' twice - Lua keeps the LAST one, so the first entry's text is unreachable and its row opens the other one's description (SPEC B89)" }
    }
    foreach ($n344 in $both344) {
        foreach ($p344 in @('merit', 'flaw')) {
            if (-not $seen344.Contains("$p344|$n344")) { $v344Bad += "(b) '$n344' is in both lists but descMerit_$lang344.lua has no '$p344|$n344' - the row on the $p344 side has no text of its own (SPEC I102g)" }
        }
        if ($seen344.Contains($n344)) { $v344Bad += "(b) descMerit_$lang344.lua still carries the BARE key '$n344' next to the composite pair - the bare one is what the flat table used to eat (SPEC I102g, B89)" }
    }
    foreach ($k344 in $seen344) {
        if ($k344 -cmatch '^(merit|flaw)\|(.+)$') {
            if (-not $both344.Contains($Matches[2])) { $v344Bad += "(c) descMerit_$lang344.lua prefixes '$k344', but '$($Matches[2])' lives in one list only - a prefix without a collision is noise (SPEC I102g)" }
        }
    }
}
# (d) the key is BUILT, never a literal - read off the code with comments stripped (SPEC V222)
$v344Cat = 0
foreach ($f344 in $files) {
    $code344 = CodeOf $f344.FullName
    $v344Cat += ([regex]::Matches($code344, 'DESC\[\s*sub\s*\.\.')).Count
    foreach ($m344 in [regex]::Matches($code344, 'DESC\[\s*"(?:merit|flaw)\|')) {
        $v344Bad += "$($f344.Name) reads DESC with a LITERAL composite key - the side is then hard-wired and the other row can never reach its text (SPEC I102g, V210b)"
    }
}
if ($v344Cat -eq 0) { $v344Bad += "(d) no .lfm builds the description key out of the list name - leg (d) read nothing and would pass on anything (SPEC V209, B7)" }
if ($v344Bad) { foreach ($b in $v344Bad) { Fail "V344 $b" } }
else { Pass "V344 descMerit_en/pt carry no key twice, the $($both344.Count) name(s) in both lists ship only as merit|/flaw| pairs, no one-list name wears a prefix, and popOpen builds the key by concatenation" }

# ---- V348: the cost is a NUMBER, and the split of Q24 lost nothing --------------------
# SPEC C Q24, I109, T796. A range is not a cost, it is N costs printed short; `1 or 3` ships as
# `Acute Senses (1)` and `Acute Senses (3)`, each with its own number and the same book, page
# and text. 87 entries became 313 and the two tables went 311/336 = 647 -> 380/493 = 873.
#
# What is NOT here, on purpose (SPEC V135, the T776 lesson): the cost being non-empty except in
# the two the book prints bare, every key being in PICKER_LIST both ways, every key carrying a
# [pt]/[en]/PT-map entry, and a parenthesis needing a sibling. V340 owns all four and measures
# them over the same table - a second copy is a second ruler to age. What is left is what only
# Q24 can break.
$v348Bad = @()
$V348_N = 873
if ($mdTbl.Count -ne 2) {
    $v348Bad += "meritData.lua did not parse into two tables - every leg below would read an empty set (SPEC V209, V20)"
} else {
    $all348 = 0
    $sfx348 = 0
    $stem348 = @{}
    foreach ($tn348 in @('MERIT_DATA', 'FLAW_DATA')) {
        foreach ($k348 in $mdTbl[$tn348].Keys) {
            $e348 = $mdTbl[$tn348][$k348]
            $all348++
            # (a) THE Q24 rule, and nothing else measures it: V340a only asks that the cost is
            # not empty, so `1-5` would sail through it exactly as it did for six rounds.
            if ($e348.Cost -notmatch '^\d+$' -and $V340_NO_COST -notcontains $k348) {
                $v348Bad += "(a) '$k348' in $tn348 costs '$($e348.Cost)', which is not a fixed number - a range is N entries printed short and Q24 splits them (SPEC I109a)"
            }
            # (d) the N in the parenthesis and the cost are the SAME number. V340d already asks
            # that a suffix has a sibling; it never ties the suffix to the price, so `Boon (3)`
            # costing 5 passes there - and on screen the player buys a number nobody printed.
            $m348 = [regex]::Match($k348, '^(.+) \((\d+)\)$')
            if ($m348.Success) {
                $sfx348++
                if ($e348.Cost -ne $m348.Groups[2].Value) {
                    $v348Bad += "(d) '$k348' in $tn348 costs '$($e348.Cost)' and its suffix says $($m348.Groups[2].Value) - the number in the name IS the price (SPEC I109b)"
                }
                $key348 = $tn348 + '|' + $m348.Groups[1].Value
                if (-not $stem348.ContainsKey($key348)) { $stem348[$key348] = New-Object 'System.Collections.Generic.List[object]' }
                $stem348[$key348].Add([pscustomobject]@{ K = $k348; Book = $e348.Book; Page = $e348.Page })
            }
        }
    }
    # (c) the count, EXACT. Smaller is an entry the regeneration dropped without a word.
    if ($all348 -ne $V348_N) { $v348Bad += "(c) the two tables hold $all348 entries, expected the $V348_N of Q24 - 380 merits and 493 flaws (SPEC T796)" }

    # (f) the N sisters come from ONE entry of ONE book: same book, same page. A sister with a
    # page of her own means the split ran over two different entries that share a name.
    foreach ($s348 in $stem348.Keys) {
        $g348 = $stem348[$s348]
        $b348 = @($g348 | ForEach-Object { $_.Book } | Sort-Object -Unique)
        $p348 = @($g348 | ForEach-Object { $_.Page } | Sort-Object -Unique)
        if ($b348.Count -gt 1) { $v348Bad += "(f) the sisters of '$s348' name $($b348.Count) different books - one entry of one book was split, so they share it (SPEC I109b)" }
        if ($p348.Count -gt 1) { $v348Bad += "(f) the sisters of '$s348' name pages $($p348 -join ', ') - the book prints one entry (SPEC I109b, I109d)" }
    }

    # (e) every key opens a description, on BOTH sides. V344 guards the composite keys and the
    # duplicates; it never asks that a key HAS an entry, which is the leg V336e keeps for the
    # backgrounds. A merit with no body opens an empty ? box and nothing says so.
    $desc348 = @{}
    foreach ($lang348 in @('en', 'pt')) {
        $d348 = DescEntries (Join-Path $plugin "descMerit_$lang348.lua") 'MERIT_DESC' $lang348
        $s = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
        if ($null -ne $d348) { foreach ($e in $d348) { [void]$s.Add($e.Key) } }
        $desc348[$lang348] = $s
    }
    if ($desc348['en'].Count -eq 0 -or $desc348['pt'].Count -eq 0) {
        $v348Bad += "(e) descMerit_en.lua or descMerit_pt.lua parsed to nothing - leg (e) would pass on the empty set (SPEC V209, V20)"
    } else {
        $miss348 = 0
        $first348 = ''
        foreach ($tn348 in @('MERIT_DATA', 'FLAW_DATA')) {
            $kind348 = if ($tn348 -eq 'MERIT_DATA') { 'merit' } else { 'flaw' }
            foreach ($k348 in $mdTbl[$tn348].Keys) {
                # composite where the name lives in BOTH lists, bare otherwise - the rule I102g
                # sets and V344 polices; here it only decides WHICH key to look for.
                $want348 = if ($mdTbl['MERIT_DATA'].ContainsKey($k348) -and $mdTbl['FLAW_DATA'].ContainsKey($k348)) { "$kind348|$k348" } else { $k348 }
                foreach ($lang348 in @('en', 'pt')) {
                    if (-not $desc348[$lang348].Contains($want348)) {
                        $miss348++
                        if ($first348 -eq '') { $first348 = "$want348 (descMerit_$lang348)" }
                    }
                }
            }
        }
        if ($miss348 -gt 0) { $v348Bad += "(e) $miss348 key(s) of meritData.lua open an EMPTY ? box, first '$first348' - the split has to carry the description with it (SPEC I109c, I102e)" }
    }

    # (g) zero-guard: no suffixed key at all means legs (d) and (f) read nothing, and Q24 put
    # 313 of them in (SPEC V20, B7, B92).
    if ($all348 -eq 0) { $v348Bad += "(g) no entry was read at all" }
    if ($sfx348 -eq 0) { $v348Bad += "(g) no key carries a ( N ) suffix - legs (d) and (f) measured an empty set, and Q24 split 87 entries into 313 (SPEC V209, V20)" }
}
if ($v348Bad) { foreach ($b in $v348Bad) { Fail "V348 $b" } }
else { Pass "V348 all 873 merit/flaw entries cost one fixed number, the 313 split keys price exactly what their suffix says, each family of sisters shares one book and page, and every key opens a description on both sides" }

# ---- V349: the four columns, and the ruler the NAME never had -------------------------
# SPEC I107a5, C Q25, T797, and B94 for why leg (c) is new. The user asked for the Page column
# to shrink to its label and the room to go to Book; the ( N ) of Q24 pushed the widest [pt]
# name from 240 to 264, so 18 of the 55 had to go to the name instead - and NOTHING measured
# that column, because V196 reads comboBox and T783 turned the name into a <button>.
$v349Bad = @()
$doc349 = Doc (Join-Path $dir "WoD20.2.lfm")
# left;width of every column, per template. The ? is in the gutter and is not a column (I102f).
$V349_COLS = @{
    'MeritPicked' = @{ 'merit_' = '20;264'; 'book_' = '284;117'; 'type_' = '401;45'; 'costy_' = '446;50' }
    'MeritFree'   = @{ 'merit_' = '20;264'; 'book_' = '284;117'; 'type_' = '401;45'; 'costy_' = '446;50' }
}
$seen349 = 0
foreach ($tn349 in @('MeritPicked')) {
    $t349 = $doc349.SelectSingleNode("//template[@name='$tn349']")
    if ($null -eq $t349) { $v349Bad += "the $tn349 template is gone (SPEC V209)"; continue }
    foreach ($c349 in $t349.SelectNodes("edit|button")) {
        $f349 = $c349.GetAttribute("field")
        if ($f349 -eq '') { continue }
        $root349 = $f349 -replace '\$\(num\)$', ''
        if (-not $V349_COLS[$tn349].ContainsKey($root349)) { continue }
        $got349 = "$($c349.GetAttribute('left'));$($c349.GetAttribute('width'))"
        $seen349++
        if ($got349 -ne $V349_COLS[$tn349][$root349]) {
            $v349Bad += "(a) $tn349 draws '$root349' at $got349, expected $($V349_COLS[$tn349][$root349]) - the four columns tile 20..496 and a seam is what the eye catches first (SPEC I107a5)"
        }
    }
}
# the visible name of the picked half is the BUTTON, and it moves with the hidden twin or the
# text sits over a field of another width (SPEC I107a1).
$btn349 = $doc349.SelectSingleNode("//template[@name='MeritPicked']/button[starts-with(@name,'dynMerit_')]")
if ($null -eq $btn349) { $v349Bad += "MeritPicked has no dynMerit_ button - the name column has no widget (SPEC I107a)" }
else {
    $w349 = 0
    [void][int]::TryParse($btn349.GetAttribute("width"), [ref]$w349)
    if ("$($btn349.GetAttribute('left'));$w349" -ne '20;264') { $v349Bad += "(a) the dynMerit_ button is at $($btn349.GetAttribute('left'));$w349, expected 20;264 - the button and its hidden twin are one column (SPEC I107a1, I107a5)" }

    # (c) THE RULER. 6.0 px/char and not 6.5: T790 measured this column at 6.0 and R116 found
    # real rendering at 5.89 or less, so 6.0 is a measured ceiling. NeededPx takes the longer of
    # the [en] and [pt] halves, which is the whole point - the [pt] names are the long ones.
    $worst349 = 0
    $who349 = ''
    $n349 = 0
    foreach ($lst349 in @('merit', 'flaw')) {
        if (-not $PICKER.ContainsKey($lst349)) { continue }
        foreach ($it349 in $PICKER[$lst349]) {
            if ($it349 -eq '') { continue }
            $n349++
            $px349 = NeededPx $it349 '12'
            if ($px349 -gt $worst349) { $worst349 = $px349; $who349 = $it349 }
        }
    }
    if ($n349 -eq 0) { $v349Bad += "(d) neither merit list reached the ruler - leg (c) measured nothing, which is the shape B94 already cost once (SPEC V209, V20)" }
    elseif ($worst349 -gt $w349) {
        $v349Bad += "(c) the name column is $w349 wide and '$who349' needs $worst349 at 6.0 px/char - a button CLIPS, and T790 closed this exact gap once already (SPEC B94, I107a4)"
    }
}
# (b) the Page column still has to hold its own heading. Shrinking it to the label was the
# request; shrinking it PAST the label is the way that request goes wrong.
$pg349 = NeededPx 'PAGE'
$hdr349 = @($doc349.SelectNodes("//label[@text='PAGE']"))
if ($hdr349.Count -eq 0) { $v349Bad += "(b) no Page heading was found - the column check read nothing (SPEC V209)" }
foreach ($h349 in $hdr349) {
    $hw349 = 0
    [void][int]::TryParse($h349.GetAttribute("width"), [ref]$hw349)
    if ($hw349 -lt $pg349) { $v349Bad += "(b) the PAGE heading is $hw349 wide and 'PAGE'/'PAGINA' needs $pg349 - the column was shrunk past its own label (SPEC C Q25, V16)" }
}
if ($seen349 -lt 4) { $v349Bad += "(d) only $seen349 of the 4 field columns were measured - this check is covering less than MeritPicked draws (SPEC V209)" }
if ($v349Bad) { foreach ($b in $v349Bad) { Fail "V349 $b" } }
else { Pass "V349 the four merit/flaw columns tile 20;264 284;117 401;45 446;50 in both templates, the Page heading still holds its own label, and the name column fits all $n349 items in both languages at 6.0 px/char" }

# ---- V350: the dropdown comes out in the order it is READ ------------------------------
# SPEC I110, C Q26, T799. The gate cannot RUN Lua (SPEC B30, B34), so every leg here measures
# the SOURCE - and the source is where each of these can go wrong silently. None of them raises
# an error at runtime: an unsorted pair renders a list that lies about which value it stores, a
# sort before the orphan leaves the one visible item at the bottom, and a byte comparison looks
# perfectly sorted right up to the first accent.
$v350Bad = @()
$w6txt350 = [System.IO.File]::ReadAllText((Join-Path $dir "WoD20.6.lfm"))
# comment lines OUT before any of this is read: a call that is commented out is not a call, and
# IndexOf cannot tell the difference. That is how the zero-guard below first shipped GREEN over
# a dead sort (SPEC V330c, B92, V20).
$w6code350 = [regex]::Replace($w6txt350, '(?m)^\s*--.*$', '')
$pk350 = LuaFn $w6code350 'pickerItems'
$fold350 = LuaFn $w6code350 'foldKey'
$cmb350 = LuaFn $w6code350 'cmbItems'

if (-not $pk350) { $v350Bad += "pickerItems is gone from WoD20.6 - there is nothing left that builds a dropdown (SPEC V209)" }
else {
    # (f) zero-guard FIRST: no sort at all and every leg below is measuring an absence.
    $iSort350 = $pk350.IndexOf('table.sort(ord')
    if ($iSort350 -lt 0) { $v350Bad += "(f) pickerItems never calls table.sort - Q26 asked for alphabetical order and the list would come out in the order the constant is authored (SPEC I110a, V20)" }
    else {
        # (a) ONE permutation, both halves. The pair is what V202 guarantees - item N shows value
        # N - so a sort that reordered `shown` alone would leave every dropdown showing one name
        # and storing another, with no error anywhere (SPEC V24, V202).
        $kLine350 = [regex]::Match($pk350, 'k2\[#k2 \+ 1\]\s*=\s*kept\[(.+?)\];')
        $sLine350 = [regex]::Match($pk350, 's2\[#s2 \+ 1\]\s*=\s*shown\[(.+?)\];')
        if (-not $kLine350.Success -or -not $sLine350.Success) {
            $v350Bad += "(a) the rebuild of the sorted pair is gone - one of `kept` and `shown` is no longer rebuilt from the permutation (SPEC I110a, V202)"
        } elseif ($kLine350.Groups[1].Value -ne $sLine350.Groups[1].Value) {
            $v350Bad += "(a) the pair is rebuilt from TWO different indexes ('$($kLine350.Groups[1].Value)' and '$($sLine350.Groups[1].Value)') - item N would stop showing value N (SPEC V202, V24)"
        }
        if ($pk350 -notmatch 'kept\s*=\s*k2;' -or $pk350 -notmatch 'shown\s*=\s*s2;') {
            $v350Bad += "(a) the sorted arrays are built and never assigned back - the sort would run and change nothing, which is the shape B7 describes (SPEC I110a)"
        }

        # (b) AFTER the orphan. V200/V346b appends the value the row is holding when the list no
        # longer offers it; sorted before that append, the one item the player can actually see
        # is the one stuck at the bottom out of order (SPEC I110a).
        # the source ALIGNS this assignment with the one under it, so the landmark is matched by
        # pattern and not by a literal with one space in it (that literal is what this leg first
        # shipped with, and it reddened on correct code).
        $mOrphan350 = [regex]::Match($pk350, 'kept\[#kept \+ 1\]\s*=\s*current;')
        $iOrphan350 = if ($mOrphan350.Success) { $mOrphan350.Index } else { -1 }
        if ($iOrphan350 -lt 0) { $v350Bad += "(b) the orphan append of V200 is gone from pickerItems - this leg has no landmark to sit after (SPEC V209, V346b)" }
        elseif ($iSort350 -lt $iOrphan350) { $v350Bad += "(b) the sort runs BEFORE the orphan is appended - the saved value would land at the end of the list, out of order (SPEC I110a, V200)" }

        # (c) the blank row stays first. It is not a name (SPEC V15, I110b).
        if ($pk350 -notmatch 'if kept\[1\] == "" then first = 2; end;') {
            $v350Bad += "(c) the empty item is not pinned at index 1 - V15's blank row would sort in among the names (SPEC I110b)"
        }

        # (d) the comparison goes through the FOLD and not through the raw bytes.
        if ($pk350 -notmatch 'foldKey\(') { $v350Bad += "(d) pickerItems sorts without foldKey - table.sort compares bytes, so in [pt] `Acido` would land after `Zebra` and the list would look sorted until the first accent (SPEC I110c)" }
    }
    # (e) ONE place sorts. cmbItems is the value combo - cmbHealthLevels is 7, 8, 9, 10 and the
    # order IS the data - so it is the named exception, not an oversight (SPEC I110d).
    if (-not $cmb350) { $v350Bad += "(e) cmbItems is gone from WoD20.6 - the other half of the pair this leg compares (SPEC V209)" }
    elseif ($cmb350 -match 'table\.sort') { $v350Bad += "(e) cmbItems sorts its items - a value combo's order is its meaning, and cmbHealthLevels would come out 10, 7, 8, 9 (SPEC I110d)" }
}

# the two cbo* whose order IS the answer, named and counted: a third name here is a combo
# quietly opting out of Q26, and a wildcard would take the whole rule with it.
$ns350 = [regex]::Match($w6code350, '(?m)^\s*PICKER_NOSORT\s*=\s*\{(.*?)\};')
if (-not $ns350.Success) { $v350Bad += "(e) PICKER_NOSORT is gone - every cbo* would be sorted, including the roster V109 pins (SPEC I110d)" }
else {
    $names350 = @([regex]::Matches($ns350.Groups[1].Value, '(\w+)\s*=\s*true') | ForEach-Object { $_.Groups[1].Value } | Sort-Object)
    $want350 = @('cboGame', 'cboSheetTheme')
    if (($names350 -join ',') -ne ($want350 -join ',')) {
        $v350Bad += "(e) PICKER_NOSORT names $($names350.Count) combo(s) [$($names350 -join ', ')], expected exactly cboGame and cboSheetTheme - the opt-out is a named list, not a door (SPEC I110d, V15)"
    }
}

# (d)/(f) the fold itself. An ASCII-only map is the failure that passes: gsub finds nothing,
# every accented name sorts by its 0xC3 lead byte, and the check above still sees foldKey called.
if (-not $fold350) { $v350Bad += "(d) foldKey is gone from WoD20.6 - the sort would fall back to byte order (SPEC I110c)" }
else {
    if ($fold350 -notmatch 'string\.lower') { $v350Bad += "(d) foldKey does not lowercase - `Zebra` would sort before `abelha`, which is byte order wearing a fold's name (SPEC I110c)" }
    if ($fold350 -notmatch 'FOLD_MAP\[') { $v350Bad += "(d) foldKey never reads FOLD_MAP - the accents would survive the fold (SPEC I110c)" }
}
$fm350 = [regex]::Match($w6code350, '(?s)FOLD_MAP\s*=\s*\{(.*?)\n\t+\};')
if (-not $fm350.Success) { $v350Bad += "(f) FOLD_MAP is gone - foldKey would map nothing and leg (d) would pass on an empty table (SPEC V20, B7)" }
else {
    $pairs350 = @([regex]::Matches($fm350.Groups[1].Value, '\["([^"]+)"\]\s*=\s*"([^"]*)"'))
    $accent350 = @($pairs350 | Where-Object { $_.Groups[1].Value -match '[^\x00-\x7F]' })
    if ($accent350.Count -eq 0) {
        $v350Bad += "(f) FOLD_MAP carries $($pairs350.Count) pair(s) and NOT ONE of them is an accented letter - the fold would be a no-op and the list would sort by byte with every check above still green (SPEC I110c, V20, B92)"
    }
    foreach ($p350 in $accent350) {
        if ($p350.Groups[2].Value -match '[^\x00-\x7F]') { $v350Bad += "(f) FOLD_MAP maps '$($p350.Groups[1].Value)' onto '$($p350.Groups[2].Value)', which is itself accented - the fold has to land on ASCII or it sorts by byte again (SPEC I110c)" }
    }
}
if ($v350Bad) { foreach ($b in $v350Bad) { Fail "V350 $b" } }
else { Pass "V350 pickerItems sorts the value/item pair by one permutation, after the orphan and with the blank row pinned, through a fold of $($accent350.Count) accented letters, and only cboGame and cboSheetTheme opt out" }

# ---- V351: the ROAD label rides on the picker's own line -------------------------------
# SPEC C Q27, I99k-m, T801. The user asked for the label beside the dropdown; what it BUYS is
# 25px of height off three boxes, and what it COSTS is 51px of picker width - measured, one
# road name of fifty-five clips, and the next longest wants 204 against 239.
#
# Leg (b) of the spec is NOT here and that is deliberate (SPEC I99j): "the three bottom boxes
# close on ONE y" is V69's ruler, it reads the y off the tab's own map comment, and it already
# reddened on this very change until the map was rewritten. A second copy would be the same
# rule with two owners, which is the defect V199/B12 are about.
$v351Bad = @()
$m351 = Doc (Join-Path $dir "WoD20.1.lfm")
$lbl351 = $null
foreach ($n351 in $m351.SelectNodes("//label[@text='ROAD']")) { $lbl351 = $n351; break }
# The road is a picker BUTTON since T876 (SPEC I131d, I113e wave 5), and its ? leads the entry
# the way the affiliation's does. So the pair this leg measures is three controls, and the one
# the LABEL must not run into is the ?, not the button behind it.
$cbo351 = $m351.SelectSingleNode("//button[@name='dynroad']")
$q351   = $m351.SelectSingleNode("//button[@name='btnQroad']")
if ($null -eq $lbl351) { $v351Bad += "(e) the ROAD label is gone from WoD20.1 - every leg here measured nothing (SPEC V209, V20)" }
if ($null -eq $cbo351) { $v351Bad += "(e) dynroad is gone from WoD20.1 (SPEC V209)" }
elseif ($null -eq $q351) { $v351Bad += "(e) btnQroad is gone from WoD20.1 - the picker family that got a ? in T876 lost it, and V365(a) asks every picker for one (SPEC V365a, V209)" }
if ($null -ne $lbl351 -and $null -ne $cbo351) {
    $lt351 = 0; $ll351 = 0; $lw351 = 0; $ct351 = 0; $cl351 = 0
    [void][int]::TryParse($lbl351.GetAttribute("top"), [ref]$lt351)
    [void][int]::TryParse($lbl351.GetAttribute("left"), [ref]$ll351)
    [void][int]::TryParse($lbl351.GetAttribute("width"), [ref]$lw351)
    [void][int]::TryParse($cbo351.GetAttribute("top"), [ref]$ct351)
    [void][int]::TryParse($cbo351.GetAttribute("left"), [ref]$cl351)

    # (a) ONE line, and the two do not sit on top of each other. Same top is the request; the
    # non-overlap is what keeps "same line" from meaning "same pixels" (SPEC I99k).
    if ($lt351 -ne $ct351) { $v351Bad += "(a) the ROAD label is at top=$lt351 and cboRoad at top=$ct351 - Q27 asked for the two on ONE line, and a label back on its own row puts the 25px back into the box (SPEC I99k)" }
    if (($ll351 + $lw351) -gt [int]$q351.GetAttribute("left")) { $v351Bad += "(a) the ROAD label runs to x=$($ll351 + $lw351) and the ? opens at x=$([int]$q351.GetAttribute('left')) - they would draw over each other (SPEC I99k, V37)" }

    # (c) the label still has to hold its own word. `CAMINHO` is the long half.
    $need351 = NeededPx 'ROAD'
    if ($lw351 -lt $need351) { $v351Bad += "(c) the ROAD label is $lw351 wide and 'ROAD'/'CAMINHO' needs $need351 - the label was squeezed to buy the picker room, which trades one clip for another (SPEC V16, I99k)" }
}
# (e) the picker gave 14px to the label in the 143rd, so the NAME has to be able to shrink into
# what is left. renderBearing owns cboRoad (V330) and is the one place allowed to size it; the
# ruler is fitSize, the same one mfLabel uses, so the sheet and this gate measure text alike.
$rb351 = LuaFn $rootTxt 'renderBearing'
if (-not $rb351) { $v351Bad += "(e) renderBearing is gone - nothing sizes the road name to the picker it has to fit (SPEC V209, V330)" }
elseif ((NoComments $rb351) -notmatch 'mfLabel\(found\["dynroad"\]') { $v351Bad += "(e) renderBearing does not paint dynroad through mfLabel - mfLabel is what translates the road, writes it and FITS it to the button, and the fifteen lines that did it by hand went with the combo in T876 (SPEC V196, V312, I131d)" }
if (-not (LuaFn $rootTxt 'fitSize')) { $v351Bad += "(e) fitSize is gone - it is the ruler leg (e) and mfLabel both size text with (SPEC V16, V312a)" }
# (d) the width exception is a LIST OF ONE, and it is read by V196 and V312c both. A second
# name is how a named exception becomes a general loosening one line at a time - the door B7
# describes, and the reason V15's own exception is spelled out combo by combo.
# (d) the width exception is EMPTY since T876, and empty is the answer rather than the leg
# retiring: NARROW_OK existed for cboRoad alone, cboRoad stopped being a comboBox, and V196 and
# V312c walk comboBoxes. So no picker on the sheet needs the exception any more - which is the
# STRONGER state, because a narrow combo appearing tomorrow now reddens those two directly with
# no door to go through. What is guarded is the door staying shut: a name added here has to be
# argued for the way the one that was here was (SPEC I99m, B7).
if ($null -eq $NARROW_OK) { $v351Bad += "(d) NARROW_OK is gone as a variable - V196 and V312c read it, and a missing roster is not the same as an empty one (SPEC I99m, V209)" }
elseif (@($NARROW_OK).Count -ne 0) {
    $v351Bad += "(d) the width exception names [$(@($NARROW_OK) -join ', ')] - it emptied in T876 when its one name stopped being a comboBox, and a picker narrower than its longest item is a CLIP, not an exemption (SPEC I99m, B7, V196)"
}
if ($v351Bad) { foreach ($b in $v351Bad) { Fail "V351 $b" } }
else { Pass "V351 the ROAD label, the ? and dynroad share one line without overlapping, the label still holds 'CAMINHO', cboRoad is the only picker allowed to be narrow, and its name is sized to fit what is left" }

# ---- V378: a family that has a picker has no TYPED row left ---------------------------
# SPEC V378, I130, T870/T871, user 2026-08-31 asks 7-11. Every background, merit, flaw and
# speciality row is a picker now; the way to put a name of your own in one is the Custom line
# (SPEC I129), which is why this could land without TAKING anything.
#
# The roster of dead templates GROWS BY NAME, one wave at a time, and that is deliberate: a leg
# that named all nine today would be red against correct code until waves 3 and 4 run, which is
# B106 exactly - a rule written for a world that does not exist yet.
$v378Bad = @()
$V378_DEAD = @('MeritFree', 'OpenAbilityFreeRow', 'SpecialityFreeRow',
               # T873 adds wave 3's three, BY NAME and in the same commit that deletes them
               # (SPEC V378a, V378d, I130b).
               'DiscFreeRow', 'SecPathFreeRow', 'RitualFreeRow',
               # T875 adds TWO of wave 4's three. HedgeRitualFree is NOT here and its absence is
               # the finding, not an oversight: HedgeRitualPicker draws read-only dots because a
               # picked ritual's level is the book's "<n>. " prefix (SPEC V269a), while the typed
               # row keeps its level in dots BOUND to $(field)_1.._5 (SPEC V269b). Converting it
               # measured as deleting 25 fields that hold data, so eight of the nine are closed
               # and the ninth is a question for the spec (SPEC V362a, B122).
               'NuminaFree', 'PsychicFree')
# NAMED and closed. "Anything that looks like free text" would let the next typed row in
# unnoticed, which is the whole failure mode this rule exists for (SPEC I130g, B7).
$V378_KEEP = @('CustomAbility', 'HeaderNarrow')
$V378_KEEP_FIELD = @('specialityName_')
# The CONVERTED templates, measured as templates and not by field prefix: OpenAbility binds
# `field="$(field)"`, so a prefix test on `background_` reads right past it and leg (b) went
# from covering four families to covering two without saying so.
$V378_PICKED = @('MeritPicked', 'OpenAbility', 'SpecialityRow', 'HeaderPicker')

$deadFound378 = @()
$visEdit378 = @()
$seen378 = 0
foreach ($f378 in $files) {
    $raw378 = [System.IO.File]::ReadAllText($f378.FullName)
    $doc378 = Doc $f378.FullName

    # (a) gone as a declaration AND gone as a call site. A template nobody instantiates still
    # compiles and still tells the next round the sheet offers a typed row (SPEC I130e).
    foreach ($d378 in $V378_DEAD) {
        if ($null -ne $doc378.SelectSingleNode("//template[@name='$d378']")) { $deadFound378 += "$($f378.Name) still declares <template name='$d378'>" }
        if ($raw378 -match "<$d378\b") { $deadFound378 += "$($f378.Name) still calls <$d378" }
    }

    # (b) a converted row is a VISIBLE button over a HIDDEN twin that owns the field (SPEC
    # I107a1). Every edit the template draws is either that hidden twin or a sibling column the
    # exception names - never one the player can type the row's own name into.
    foreach ($pk378 in $V378_PICKED) {
        $tn378 = $doc378.SelectSingleNode("//template[@name='$pk378']")
        if ($null -eq $tn378) { continue }
        $seen378++

        if (@($tn378.SelectNodes("button[not(starts-with(@name,'btnQ'))]")).Count -eq 0) {
            $visEdit378 += "$pk378 draws no picker button - the row would have no way to open the box (SPEC I107a1)"
        }
        foreach ($e378 in $tn378.SelectNodes("edit[@field]")) {
            $fld378 = $e378.GetAttribute("field")
            $keepIt = $false
            foreach ($k378 in $V378_KEEP_FIELD) { if ($fld378 -like "$k378*") { $keepIt = $true } }
            # The merit table's book/page/cost columns are readOnly by V331b, not hidden - they
            # are siblings of the row, not the row's name.
            if ($e378.GetAttribute("readOnly") -eq "true") { $keepIt = $true }
            if ($keepIt) { continue }
            if ($e378.GetAttribute("visible") -ne "false") { $visEdit378 += "$pk378 lets '$fld378' be typed into" }
        }
    }
}
if ($deadFound378.Count -gt 0) { $v378Bad += "$($deadFound378 -join '; ') - a typed row in a family that has a picker (SPEC I130, V378a)" }
# (d) zero-guard: with no field of a converted family read at all, leg (b) passes on silence.
if ($seen378 -lt $V378_PICKED.Count) { $v378Bad += "only $seen378 of the $($V378_PICKED.Count) converted template(s) were read - leg (b) is covering less than the sheet has (SPEC V20, B7)" }
if ($visEdit378.Count -gt 0) { $v378Bad += "$($visEdit378 -join '; ') - the field belongs on the hidden twin of I107a1, behind the button (SPEC V378b)" }
if ($v378Bad) { foreach ($b in $v378Bad) { Fail "V378 $b" } }
else { Pass "V378 none of the $($V378_DEAD.Count) retired typed template(s) is declared or called, and all $seen378 converted template(s) put their field on a hidden twin behind a button - with $($V378_KEEP.Count) template(s) and $($V378_KEEP_FIELD.Count) field root(s) named as the closed exception" }

# ---- V379: every picker offers `-- Custom --`, and it costs the page nothing -----------
# SPEC V379, I129a/I129b/I129c, T868/T869, user 2026-08-31 ask 12. It is what let the typed
# rows go (SPEC I130): without it, turning a free row into a picker would TAKE the ability to
# put a name of your own in a slot.
$v379Bad = @()
$rootDoc379 = Doc (Join-Path $dir "WoD20th.lfm")
$rootLua379 = CodeOf (Join-Path $dir "WoD20th.lfm")
$btnC379 = @($rootDoc379.SelectNodes("//layout[@name='mfSearch']/button[@name='btnMfCustom']"))[0]
$btnR379 = @($rootDoc379.SelectNodes("//layout[@name='mfSearch']/button[@name='btnMfRemove']"))[0]
$rows379 = @($rootDoc379.SelectNodes("//layout[@name='mfSearch']/button[starts-with(@name,'dynMfRow_')]"))

# (a) a control of its OWN, sibling to the remove line and not one of the paged results.
if ($null -eq $btnC379) { $v379Bad += "(a) btnMfCustom is not a child of mfSearch - the only way to put a name of your own in a slot is gone (SPEC I129a, V20)" }
elseif ($null -eq $btnR379) { $v379Bad += "(a) btnMfRemove is gone - the custom line is defined as its sibling and the check has nothing to sit it beside (SPEC I121, V20)" }
elseif ($rows379.Count -eq 0) { $v379Bad += "(a) no dynMfRow_* under mfSearch - the page count leg would measure nothing (SPEC V20, B7)" }
else {
    # (b) the page still offers TWENTY names. The remove line rode INSIDE the pool once and ate
    # one of them (SPEC I121b); a second fixed line added the same way would take another.
    $mfRows379 = [regex]::Match($rootLua379, 'rows\s*=\s*(\d+)')
    if ($rows379.Count -ne 20) { $v379Bad += "(b) mfSearch draws $($rows379.Count) result row(s), not the 20 I107k promises - the fixed lines are eating the page" }
    elseif (-not $mfRows379.Success -or [int]$mfRows379.Groups[1].Value -ne $rows379.Count) {
        $v379Bad += "(b) MF.rows and the drawn rows disagree - one of them is counting the fixed lines (SPEC I107k, I129a)"
    }
    # and neither fixed line may be reached through the pool or the hit list.
    foreach ($fx379 in @('btnMfCustom', 'btnMfRemove')) {
        if ($rootLua379 -match ("MF\.(pool|hits)\[[^\]]*\]\s*=\s*[^\n]*" + $fx379)) {
            $v379Bad += "(b) $fx379 is written into MF.pool or MF.hits - it would sort, page and vanish past page one, which is the defect I121 fixed for remove (SPEC V379b)"
        }
    }

    # (c) ALWAYS visible, filled row or empty - and that is where it parts from remove, whose
    # visibility IS gated on the row holding something (SPEC I121a vs I129b).
    if ($btnC379.GetAttribute("visible") -eq "false") { $v379Bad += "(c) btnMfCustom is authored hidden - customising needs nothing to already be there (SPEC I129b)" }
    $custFn379 = [regex]::Match($rootLua379, '(?s)btnMfCustom"\]\s*\.visible\s*=\s*([^\n;]+)')
    if ($custFn379.Success -and $custFn379.Groups[1].Value -match 'canRemove|MF\.canRemove|has') {
        $v379Bad += "(c) btnMfCustom's visibility is gated the way btnMfRemove's is - remove needs something to remove, custom does not (SPEC I129b, V379c)"
    }

    # (d) the label is a string of its own in BOTH halves of the .lang - remove could borrow the
    # blank value's label, and there is no blank here to borrow from (SPEC I129c, V369e, V10).
    $langRaw379 = [System.IO.File]::ReadAllText((Join-Path (Split-Path $dir -Parent) "localization.lang"))
    foreach ($k379 in @('-- Custom --', 'custom description')) {
        $hits379 = ([regex]::Matches($langRaw379, '(?m)^wod\.' + [regex]::Escape($k379) + '=')).Count
        if ($hits379 -lt 2) { $v379Bad += "(d) `wod.$k379` appears $hits379 time(s) in localization.lang - a visible string needs both halves (SPEC V10, I129c)" }
    }
}
if ($v379Bad) { foreach ($b in $v379Bad) { Fail "V379 $b" } }
else { Pass "V379 the Custom line is a control of its own beside the remove line, always visible, outside MF.pool and MF.hits so the page still offers $($rows379.Count) names, and its label is keyed in both languages" }

# ---- V381: ONE reader for a row's description, and the order lives in it ---------------
# SPEC V381, I129k, T868/T869. popOpen carried "override, else book" and mfDesc carried "book";
# two copies of an order that has to agree is the second copy that ages quietly (SPEC V135).
# descFor is now the only place it is written, and it grew the custom step in front without
# either caller learning about it.
$v381Bad = @()
$descForFn381 = [regex]::Match($rootLua379, '(?s)function descFor\(slot, kind, key, sub, lang\)(.*?)\n\t{3}end;')

# (a) exactly ONE descFor, and the two readers reach the text through it.
if (-not $descForFn381.Success) { $v381Bad += "(a) descFor is gone - the order would be back in the callers, one copy each (SPEC I129k, V135)" }
else {
    $body381 = NoComments $descForFn381.Groups[1].Value
    foreach ($rd381 in @('popOpen', 'mfDesc')) {
        $fn381 = [regex]::Match($rootLua379, '(?s)function ' + $rd381 + '\([^)]*\)(.*?)\n\t{3}end;')
        if (-not $fn381.Success) { $v381Bad += "(a) $rd381 was not found - one of the two readers is gone (SPEC V209)"; continue }
        if ((NoComments $fn381.Groups[1].Value) -notmatch 'descFor\(') { $v381Bad += "(a) $rd381 does not read through descFor - it is carrying its own copy of the order (SPEC V381a, V135)" }
    }

    # (b) and NOBODY reads the three sources outside it. Writes are fine - savePopDesc and
    # mfConfirm both write - so what is looked for is the READ, sheet[<derivation>].
    #
    # ONE exception, NAMED and closed: mfCustom. It reads the pair to FILL the editor the player
    # is about to type in - he clicked Custom, so there is no order to apply and no book to fall
    # back to. What V381 forbids is a second place DECIDING which of the four texts a row shows;
    # loading a form is not deciding. A generic "unless it looks like a form" would kill the leg.
    $others381 = @()
    foreach ($fnM381 in [regex]::Matches($rootLua379, '(?s)function (\w+)\(([^)]*)\)(.*?)\n\t{3}end;')) {
        $nm381 = $fnM381.Groups[1].Value
        if ($nm381 -eq 'descFor' -or $nm381 -eq 'descText' -or $nm381 -eq 'mfCustom') { continue }
        $bd381 = NoComments $fnM381.Groups[3].Value
        if ($bd381 -match 'sheet\[\s*(desc|custom)(Desc)?FieldOf\(' -or $bd381 -match 'descText\(') { $others381 += $nm381 }
    }
    if ($others381.Count -gt 0) { $v381Bad += "(b) $($others381 -join ', ') read a row's description outside descFor - a second copy of the order is what V135 refuses (SPEC V381b)" }

    # (c) the order itself, in order, and the sentence of V360c last.
    $iCustom381 = $body381.IndexOf('customDescFieldOf')
    $iOwn381    = $body381.IndexOf('descFieldOf')
    $iBook381   = $body381.IndexOf('descText(')
    $iSent381   = $body381.IndexOf('No description available')
    if ($iCustom381 -lt 0 -or $iOwn381 -lt 0 -or $iBook381 -lt 0 -or $iSent381 -lt 0) {
        $v381Bad += "(c) descFor is missing one of its four steps - custom, the storyteller's override, the book and the sentence of V360c (SPEC I129k)"
    }
    elseif (-not ($iCustom381 -lt $iOwn381 -and $iOwn381 -lt $iBook381 -and $iBook381 -lt $iSent381)) {
        $v381Bad += "(c) descFor's four steps are out of order - custom, then the override, then the book, then the sentence (SPEC I129k, V381c)"
    }
    # Both of the first two hang on `key` and not on the slot's current value: an order written
    # against sheet[slot] would show the row's own text under every candidate in the picker.
    elseif ($body381 -notmatch 'sheet\[cf\]\s*==\s*key') {
        $v381Bad += "(c) descFor's custom step does not test the CANDIDATE - written against the slot's value it would leak the row's custom text under every name in the picker (SPEC I129k)"
    }

    # (d) the two derivations exist and are built ON descFieldOf, so there is one shape to keep
    # in step and not three (SPEC I129f, V381d).
    foreach ($d381 in @('customFieldOf', 'customDescFieldOf')) {
        $df381 = [regex]::Match($rootLua379, '(?s)function ' + $d381 + '\(field\)(.*?)\n\t{3}end;')
        if (-not $df381.Success) { $v381Bad += "(d) $d381 is gone - the custom would have nowhere per-slot to live (SPEC I129f)" }
        elseif ((NoComments $df381.Groups[1].Value) -notmatch '(descFieldOf|customFieldOf)\(') {
            $v381Bad += "(d) $d381 does not derive off the existing derivation - three shapes to keep in step instead of one (SPEC I129f, V135)"
        }
    }
}
if ($v381Bad) { foreach ($b in $v381Bad) { Fail "V381 $b" } }
else { Pass "V381 descFor is the one reader, popOpen and mfDesc both go through it, nothing else reads a description, the four steps are in order and keyed on the candidate, and both custom derivations are built on descFieldOf" }

# ---- V377: every picker slot has a description field that RESOLVES ---------------------
# SPEC V377, B111, I129g, T867. descFieldOf is the ONE derivation of where a row keeps its
# text, and it answered nil for twenty-eight of the rows that own a ? - the twenty-two merits
# and flaws, whose num is `m0`..`f10`, and the standalone pickers with no underscore at all.
# Nil reaches savePopDesc, which returns on it without a word: the storyteller typed and
# nothing was written, on the rows nobody happened to test.
#
# The pattern is read from the SOURCE and not copied here (leg c): a gate carrying its own
# copy goes green the day the Lua's own pattern changes under it (SPEC V135).
$v377Bad = @()
$rootCode377 = CodeOf (Join-Path $dir "WoD20th.lfm")
$dfo377 = [regex]::Match($rootCode377, '(?s)function descFieldOf\(field\)(.*?)\n\t{3}end;')
$luaPats377 = @()
if ($dfo377.Success) {
    foreach ($mp in [regex]::Matches($dfo377.Groups[1].Value, 'string\.match\(field,\s*"([^"]+)"')) {
        # Lua patterns -> .NET regex. Only the classes descFieldOf actually uses.
        $luaPats377 += ($mp.Groups[1].Value -replace '%a', '[a-zA-Z]' -replace '%w', '[a-zA-Z0-9]' -replace '%d', '[0-9]')
    }
}

# (a) the fields come from the CODE on both sides: every template that opens a picker or a ?,
# expanded through its own instances, so what is measured is the name the sheet really writes.
$slots377 = @()
$tplZero377 = @()
$tplSeen377 = 0
foreach ($f377 in $files) {
    $doc377 = Doc $f377.FullName
    foreach ($tpl377 in $doc377.SelectNodes("//template")) {
        $raw377 = $tpl377.OuterXml
        if ($raw377 -notmatch 'mfOpen\(|popOpen\(') { continue }
        $tname377 = $tpl377.GetAttribute("name")
        $tplSeen377++
        # The field the template binds - the hidden twin of I107a1, or the row's own edit.
        $fattr377 = @($tpl377.SelectNodes(".//*[@field]") | ForEach-Object { $_.GetAttribute("field") } | Select-Object -First 1)
        if ($fattr377.Count -eq 0) { continue }
        $before377 = $slots377.Count
        foreach ($doc2 in @($files | ForEach-Object { Doc $_.FullName })) {
            foreach ($inst377 in $doc2.SelectNodes("//$tname377")) {
                $nm377 = $fattr377[0]
                foreach ($at377 in $inst377.Attributes) { $nm377 = $nm377.Replace('$(' + $at377.Name + ')', $at377.Value) }
                if ($nm377 -notmatch '\$\(') { $slots377 += $nm377 }
            }
        }
        if ($slots377.Count -eq $before377) { $tplZero377 += $tname377 }
    }
}
$slots377 = @($slots377 | Sort-Object -Unique)

# (b) every one of them resolves. (d) zero-guard: a template that opens a picker and yields no
# slot means the expansion broke, not that the sheet shrank - and it is measured per TEMPLATE
# rather than against a total, so the number does not have to be hand-raised by every wave.
$unresolved377 = @()
foreach ($s377 in $slots377) {
    $ok377 = $false
    foreach ($p377 in $luaPats377) { if ($s377 -match $p377) { $ok377 = $true; break } }
    if (-not $ok377) { $unresolved377 += $s377 }
}
if (-not $dfo377.Success) { $v377Bad += "descFieldOf is gone - it is the ONE derivation of where a row keeps its text (SPEC I45, V365e)" }
elseif ($luaPats377.Count -eq 0) { $v377Bad += "no string.match pattern was read out of descFieldOf - the check carries no ruler and would pass anything (SPEC V20, V377c)" }
elseif ($slots377.Count -eq 0) { $v377Bad += "not one picker slot was expanded out of the templates - the sweep broke and every name would resolve vacuously (SPEC V20, B7)" }
elseif ($tplZero377.Count -gt 0) { $v377Bad += "$($tplZero377 -join ', ') open a picker but expanded to NO slot - the instances were not found and their rows went unmeasured (SPEC V20)" }
elseif ($unresolved377.Count -gt 0) { $v377Bad += "$($unresolved377.Count) picker slot(s) have no description field - descFieldOf answers nil for [$($unresolved377 -join ', ')] and savePopDesc returns on nil without a word (SPEC B111, I129g)" }
if ($v377Bad) { foreach ($b in $v377Bad) { Fail "V377 $b" } }
else { Pass "V377 all $($slots377.Count) picker slot(s) across $tplSeen377 template(s) derive a description field, through the $($luaPats377.Count) pattern(s) read out of descFieldOf itself" }

# ---- V380: the ROAD pair is CENTRED, and centred in EVERY language ---------------------
# SPEC V380, I128, T864/T866, user 2026-08-31 ask 6. The pair used to be a label pinned at
# left=20 with width=80 beside a combo at 105: the word hugged x=100 with forty pixels of
# nothing to its left, which is what the user read as "shifted right". There was nothing to
# straighten - there was no centring at all.
#
# What is checked is the RELATION and the FORMULA, not the two numbers the XML happens to
# carry: the lefts are written at runtime from ROAD_LABEL_W, so a gate that only read the XML
# would be measuring one language out of two (leg c) and would go green on the day pt drifted.
$v380Bad = @()
$rpSrc380 = [regex]::Match((CodeOf (Join-Path $dir "WoD20th.lfm")), '(?s)function renderRoadPair\(from\)(.*?)\n\t{3}end;')
$tbl380   = [regex]::Match((CodeOf (Join-Path $dir "WoD20th.lfm")), 'ROAD_LABEL_W\s*=\s*\{([^}]*)\}')
$lbl380   = @($mainDoc.SelectNodes("//label[@name='lblRoad']"))[0]
# (e) the CONTROL's type comes from the SOURCE and is not assumed to be a comboBox: wave 5 of
# I113e turns this into a picker button, and B94 is exactly a width promise that lost its
# guard when the widget under it changed type.
$ctl380   = @($mainDoc.SelectNodes("//*[@name='dynroad']"))[0]
# The ? is part of the GROUP being centred since T876, so its width is a term in the arithmetic
# below and it is READ, never assumed: a ? that changed size would slide the pair off centre and
# nothing else would say so (SPEC I131d, V380b).
$q380     = @($mainDoc.SelectNodes("//*[@name='btnQroad']"))[0]

if (-not $rpSrc380.Success) { $v380Bad += "renderRoadPair is gone - the pair would sit wherever the XML pinned it and the pt case would go unmeasured (SPEC V20)" }
elseif (-not $tbl380.Success) { $v380Bad += "ROAD_LABEL_W is gone - the label width per language is the ONE number the two lefts derive from (SPEC I128d)" }
elseif ($null -eq $lbl380) { $v380Bad += "lblRoad is not on WoD20.1 - the check measured nothing (SPEC V20)" }
elseif ($null -eq $ctl380) { $v380Bad += "dynroad is not on WoD20.1 - the check measured nothing (SPEC V20)" }
elseif ($null -eq $q380) { $v380Bad += "btnQroad is not on WoD20.1 - the ? is a term in the centring and this check would measure a group that is not the one on screen (SPEC V20)" }
else {
    $ctlType380 = $ctl380.Name
    $cw380 = [int]$ctl380.GetAttribute("width")

    # (d) the table's keys are exactly the languages the sheet offers. A language with no entry
    # would leave the pair wherever the last one put it, silently.
    # The languages come from the SETTINGS combo that sets `language`, not from a pair of
    # literals here: a third language would have to be added in two places, and the second one
    # to be forgotten would be this check going green on a pair nobody centred.
    $langCbo380 = @((Doc (Join-Path $dir "WoD20.6.lfm")).SelectNodes("//comboBox[@field='language']"))[0]
    $keys380 = @([regex]::Matches($tbl380.Groups[1].Value, '(\w+)\s*=') | ForEach-Object { $_.Groups[1].Value } | Sort-Object)
    $langs380 = @()
    if ($null -ne $langCbo380) {
        $langs380 = @([regex]::Matches($langCbo380.GetAttribute("values"), "'([^']+)'") | ForEach-Object { $_.Groups[1].Value } | Sort-Object)
    }
    if ($langs380.Count -eq 0) { $v380Bad += "(d) the language combo on WoD20.6 has no values to read - the key check measured nothing (SPEC V20)" }
    elseif (Compare-Object $keys380 $langs380) {
        $v380Bad += "(d) ROAD_LABEL_W names [$($keys380 -join ', ')] and the sheet speaks [$($langs380 -join ', ')] - a language with no entry leaves the pair where the last one put it (SPEC V380d)"
    }

    # The width the Lua uses for the control has to be the control's REAL width, or the pair
    # centres around a number that is not on screen (leg e, SPEC B94, I128g).
    $cwLua380 = [regex]::Match($rpSrc380.Groups[1].Value, 'local\s+cw\s*=\s*(\d+)')
    if (-not $cwLua380.Success) { $v380Bad += "(e) renderRoadPair no longer states the control width it centres around (SPEC I128g)" }
    elseif ([int]$cwLua380.Groups[1].Value -ne $cw380) {
        $v380Bad += "(e) renderRoadPair centres around a ${$cwLua380.Groups[1].Value}px control and <$ctlType380 name='dynroad'> is ${cw380}px - the pair would sit off centre by the difference (SPEC I128g, B94)"
    }

    # (a)+(b)+(c): run the Lua's own arithmetic for EVERY language and check the two relations.
    $widest380 = -1; $widestL380 = 0; $widestC380 = 0
    foreach ($m380 in [regex]::Matches($tbl380.Groups[1].Value, '(\w+)\s*=\s*(\d+)')) {
        $lg = $m380.Groups[1].Value; $w = [int]$m380.Groups[2].Value
        $qw380 = [int]$q380.GetAttribute("width")
        $left = 20 + [math]::Floor((290 - ($w + 5 + $qw380 + 5 + $cw380)) / 2)
        $qLeft = $left + $w + 5
        $ctlLeft = $qLeft + $qw380 + 5
        if ($qLeft -ne $left + $w + 5) { $v380Bad += "(a) [$lg] the gap between label and ? is not 5" }
        $slackL = $left - 20
        $slackR = 310 - ($ctlLeft + $cw380)
        if ([math]::Abs($slackL - $slackR) -gt 1) {
            $v380Bad += "(b) [$lg] the pair leaves ${slackL}px on the left and ${slackR}px on the right - it is not centred in the box interior (SPEC V380b)"
        }
        if ($w -gt $widest380) { $widest380 = $w; $widestL380 = $left; $widestC380 = $ctlLeft }
    }

    # The XML authors the WORST case and lets the renderer shrink it, the way HEALTH authors
    # its ten-row height (SPEC V49, I128e): a label authored at the English width would sit in
    # the file as a clipped CAMINHO waiting for someone to open the sheet in pt.
    $xw380 = [int]$lbl380.GetAttribute("width"); $xl380 = [int]$lbl380.GetAttribute("left")
    $xc380 = [int]$ctl380.GetAttribute("left")
    if ($xw380 -ne $widest380 -or $xl380 -ne $widestL380 -or $xc380 -ne $widestC380) {
        $v380Bad += "(c) the XML authors the label at left=$xl380 width=$xw380 with the control at $xc380, and the WIDEST language wants left=$widestL380 width=$widest380 control=$widestC380 - the file has to declare the worst case (SPEC V49, I128e)"
    }
}
if ($v380Bad) { foreach ($b in $v380Bad) { Fail "V380 $b" } }
else { Pass "V380 the ROAD pair centres in the box interior in all $($langs380.Count) language(s) off one ROAD_LABEL_W number each, with a 5px gap, the XML authoring the widest case, and the control read as a <$($ctl380.Name)> from source rather than assumed" }

# ---- V331: the picked rows and the typed rows do not mix -------------------------------
# SPEC V331 as REWRITTEN in the 141st (user 2026-08-29). Five of the seven legs the first
# writing asked for are NOT here, and each absence is measured, not lazy:
#   (c)(d)(f) - every list item having table data, every key being offered, and book/page/cost
#              being filled - are V340f and V340a, over the same two tables (verify:12234, :12242).
#   (e)       - "the two lists are disjoint" - is REVOKED BY FACT: `Cannibal` is in both on
#              purpose (B82, I102g, B89), V340b names the exception, and the user confirmed the
#              rule. Written as asked, this leg would redden correct code.
# What is left is what nothing else measures.
$v331Bad = @()
$doc331 = Doc (Join-Path $dir "WoD20.2.lfm")
$raw331 = [System.IO.File]::ReadAllText((Join-Path $dir "WoD20.2.lfm"))

# (a) WHICH rows, not how many. V339a already counts twelve <MeritPicked and never looks at the
# nums, so swapping m3 for m9 sails through it - and a picked row on m9 is a search box writing
# into a row the player is also typing in.
$want331 = @{
    'MeritPicked' = @(0..10 | ForEach-Object { "m$_" }) + @(0..10 | ForEach-Object { "f$_" })
}
foreach ($tpl331 in @('MeritPicked')) {
    $got331 = @([regex]::Matches($raw331, "<$tpl331\b[^>]*\bnum=""([^""]+)""") | ForEach-Object { $_.Groups[1].Value } | Sort-Object)
    $exp331 = @($want331[$tpl331] | Sort-Object)
    if ($got331.Count -eq 0) { $v331Bad += "(c) no <$tpl331 call was read at all - every leg on this template measured nothing (SPEC V209, V20)"; continue }
    if (($got331 -join ',') -ne ($exp331 -join ',')) {
        $v331Bad += "(a) $tpl331 is called on [$($got331 -join ', ')], expected [$($exp331 -join ', ')] - the picked half and the typed half are decided by the ROW, and a swap here gives one row two behaviours (SPEC I100a, V339a)"
    }
}

# (b) the three book columns of a PICKED row are readOnly and the typed row's are not. mfConfirm
# writes all three in one step (I107i); a keyboard in them is exactly what the search box exists
# to stop, and it is item (3) of T787. The typed row is the opposite by design.
$cols331 = @('book_$(num)', 'type_$(num)', 'costy_$(num)')
foreach ($pair331 in @(,@('MeritPicked', $true))) {
    $t331 = $doc331.SelectSingleNode("//template[@name='$($pair331[0])']")
    if ($null -eq $t331) { $v331Bad += "(c) the $($pair331[0]) template is gone (SPEC V209)"; continue }
    foreach ($c331 in $cols331) {
        $e331 = $t331.SelectSingleNode("edit[@field='$c331']")
        if ($null -eq $e331) { $v331Bad += "(c) $($pair331[0]) has no edit on '$c331' (SPEC I100f, V209)"; continue }
        $ro331 = ($e331.GetAttribute("readOnly") -eq 'true')
        if ($pair331[1] -and -not $ro331) {
            $v331Bad += "(b) $($pair331[0]) leaves '$c331' writable - the player could type over what mfConfirm wrote and the row would disagree with the book it names (SPEC I107i, T787)"
        } elseif (-not $pair331[1] -and $ro331) {
            $v331Bad += "(b) $($pair331[0]) locks '$c331' - a TYPED row is the storyteller's own entry and nothing else fills it (SPEC I50, V255)"
        }
    }
}
if ($v331Bad) { foreach ($b in $v331Bad) { Fail "V331 $b" } }
else { Pass "V331 m0-m6 and f0-f6 are picked, m7-m10 and f7-f10 are typed, and the three book columns are locked on the picked half and open on the typed one" }

# ---- V332: the Traits map is what the XML HAS, measured --------------------------------
# SPEC V332 as REWRITTEN in the 141st. The first writing measured against I101a-e and T790 (x)
# had already redrawn the whole tab: it asked for 1580 where the tab closes on 1595 and for
# 235x155 boxes that are 225x153 and 225x186. Written as asked it would redden correct code.
# Leg (f) - "the map comment at the top agrees" - is gone for the same reason: T790 decided the
# opposite in as many words, keeping B73 as PRACTICE, and nothing in the gate reads the .2 map.
$v332Bad = @()
# the 14 are the UNNAMED children of the scrollBox; popDesc and mfSearch are named overlays and
# float over the grid by design (I102a, I107c), which is why V40 and V280 cut them out too.
$boxes332 = @()
foreach ($b332 in $doc331.SelectNodes("//scrollBox/layout")) {
    if ($b332.HasAttribute("name")) { continue }
    $l = 0; $t = 0; $w = 0; $h = 0
    [void][int]::TryParse($b332.GetAttribute("left"), [ref]$l)
    [void][int]::TryParse($b332.GetAttribute("top"), [ref]$t)
    [void][int]::TryParse($b332.GetAttribute("width"), [ref]$w)
    [void][int]::TryParse($b332.GetAttribute("height"), [ref]$h)
    $boxes332 += [pscustomobject]@{ L = $l; T = $t; W = $w; H = $h }
}
# (f) zero-guard first: fewer than fourteen and every count below is reading a short list.
if ($boxes332.Count -ne 14) {
    $v332Bad += "(f) $($boxes332.Count) top-level box(es) on the Traits tab, expected the 14 of I101h - the map and the tab have to be the same thing (SPEC V209, V20)"
} else {
    # (a) the map of I101h, LITERAL. Measured off the XML on 2026-08-29 and written down there
    # so the next round reads a number instead of re-deriving one (SPEC B73).
    # REMEASURED 2026-09-02 off the XML that T901 and T902 left (SPEC I134j, I134k). Every box
    # on the tab now closes on ONE of three lines - 345, 520 and 695 - and 695 is where
    # BACKGROUNDS closes, which is what the user asked FLAWS and the bottom row to meet.
    #
    # The tab floor went 693 -> 695 and that is arithmetic, not taste: MERITS = FLAWS closing on
    # BACKGROUNDS wants 2h+5 = floor, and the ten right-hand boxes on one height want 4h+15 =
    # floor. At 693 the second gives 169.5. 695 gives 345 and 170, both whole (SPEC I134j).
    $want332 = @(
        '0;0;364;679', '369;0;536;337', '369;342;536;337', '1370;0;225;337',
        '910;0;225;166', '1140;0;225;166', '910;171;225;166', '1140;171;225;166',
        '910;342;225;166', '1140;342;225;166', '1370;342;225;166',
        '910;513;225;166', '1140;513;225;166', '1370;513;225;166'
    )
    $got332 = @($boxes332 | ForEach-Object { "$($_.L);$($_.T);$($_.W);$($_.H)" } | Sort-Object)
    $extra332 = @($got332 | Where-Object { $want332 -notcontains $_ })
    $miss332 = @($want332 | Where-Object { $got332 -notcontains $_ })
    if ($extra332.Count -or $miss332.Count) {
        $v332Bad += "(a) the tab draws [$($extra332 -join ' ')] where I101h says [$($miss332 -join ' ')] - left;top;width;height, and the map is the one T790 left (SPEC I101h)"
    }

    # (b) no two of the fourteen overlap. O(n^2) over fourteen is nothing, and a box drawn over
    # another is the failure that looks fine in the XML and hides half a table on screen.
    for ($i = 0; $i -lt $boxes332.Count; $i++) {
        for ($j = $i + 1; $j -lt $boxes332.Count; $j++) {
            $a = $boxes332[$i]; $b = $boxes332[$j]
            if ($a.L -lt ($b.L + $b.W) -and $b.L -lt ($a.L + $a.W) -and $a.T -lt ($b.T + $b.H) -and $b.T -lt ($a.T + $a.H)) {
                $v332Bad += "(b) the boxes at $($a.L),$($a.T) and $($b.L),$($b.T) overlap - one is drawing over the other (SPEC V37)"
            }
        }
    }

    # (c) the tab closes on 1595 and the columns keep the 5px gutter V298 measures elsewhere.
    $right332 = ($boxes332 | ForEach-Object { $_.L + $_.W } | Measure-Object -Maximum).Maximum
    if ($right332 -ne 1595) { $v332Bad += "(c) the rightmost box closes at $right332, not the 1595 T790's arithmetic lands on (364+5+536+5+225+5+225+5+225) - this tab reaches for a horizontal scrollbar first (SPEC I101h)" }
    $colL332 = @($boxes332 | ForEach-Object { $_.L } | Sort-Object -Unique)
    foreach ($cl332 in $colL332) {
        if ($cl332 -eq 0) { continue }
        $prev332 = @($boxes332 | Where-Object { ($_.L + $_.W) -eq ($cl332 - 5) })
        if ($prev332.Count -eq 0) { $v332Bad += "(c) the column at x=$cl332 does not open 5px after any box's right edge - the gutters on this tab are all 5 (SPEC V298)" }
    }

    # (d) RETIRED into V392 on 2026-09-02. It said the ten background boxes come in two bands,
    # 153 and 186, with DERANGEMENTS told apart by its 311 - three literals for a shape the user
    # has now replaced: one height for all ten, DERANGEMENTS the single named exception, and the
    # bottom row closing with BACKGROUNDS (SPEC I134k). Rewriting the three numbers here would
    # have made this the SECOND owner of that rule beside V392, which is B70 (SPEC V135).
    #
    # What survives here is (a), which pins every rect including these ten, and it is a snapshot:
    # it catches drift, while V392 states the RULE the drift would break.
}
# (e) the rename was TEXT only: the field kept its old spelling, because renaming it drops what
# saved sheets already hold (SPEC V2, I101g).
$art332 = @($doc331.SelectNodes("//label[@text='ARTIFACTS']"))
$sta332 = @($doc331.SelectNodes("//label[@text='STATUS']"))
if ($sta332.Count -ne 0) { $v332Bad += "(e) the label STATUS is back on the Traits tab - the rename of I101g was undone in the text (SPEC I101g)" }
if ($art332.Count -ne 1) { $v332Bad += "(e) the ARTIFACTS label appears $($art332.Count) time(s), expected 1 (SPEC I101g)" }
if (@($doc331.SelectNodes("//*[@field='status']")).Count -ne 1) { $v332Bad += "(e) the field 'status' is no longer owned exactly once on this tab - renaming it drops what saved sheets hold (SPEC V2)" }
if ($v332Bad) { foreach ($b in $v332Bad) { Fail "V332 $b" } }
else { Pass "V332 the Traits tab draws the 14 boxes of I101h with no overlap, closes on 1595 with 5px gutters, and still owns 'status' once under the ARTIFACTS label - its ten background boxes are V392's now" }

# ---- V345: every description module breaks lines the SAME way, and that way is LF ---------
# (SPEC V345, B87) The ten modules are written by script, and a module coming out with the
# other kind of line break changes not one character of text yet makes the file differ from
# the shipped one on thousands of lines - descMerit_en.lua showed up with 6486 changed lines
# and zero changed words, which is a diff that lies about what a round did.
# The house style is LF, MEASURED byte by byte: descDisc/descNumina/descPath/descRitual carry
# 0 CR between them. The two descMerit_* were the odd ones out and were regenerated to match.
# Counting with `grep -c $'\r'` is what made the first reading say the opposite - the byte
# count below is the one that answers.
$v345Bad = @()
$v345Files = @(Get-ChildItem -LiteralPath $plugin -Filter 'desc*.lua' | Sort-Object Name)
if ($v345Files.Count -eq 0) { $v345Bad += "no desc*.lua at the plugin root - this check read nothing (SPEC V209, B7)" }
foreach ($f345 in $v345Files) {
    $b345 = [System.IO.File]::ReadAllBytes($f345.FullName)
    $lf345 = 0; $crlf345 = 0
    for ($i = 0; $i -lt $b345.Length; $i++) {
        if ($b345[$i] -eq 10) { $lf345++; if ($i -gt 0 -and $b345[$i - 1] -eq 13) { $crlf345++ } }
    }
    if ($lf345 -eq 0) { $v345Bad += "$($f345.Name) has no line break at all - it was not read as text (SPEC V209)" }
    elseif ($crlf345 -ne 0) { $v345Bad += "$($f345.Name) has $crlf345 CRLF among $lf345 line breaks - the other description modules are LF throughout, and a module that disagrees makes its own diff lie about what changed (SPEC B87)" }
}
if ($v345Bad) { foreach ($b in $v345Bad) { Fail "V345 $b" } }
else { Pass "V345 all $($v345Files.Count) desc*.lua at the plugin root break lines the same way - LF throughout, inside the [==[ ]==] literal too" }

# ---- V352: the description lives INSIDE the search box, and dies with it ------------------
# SPEC I112, V352, Q29, T806/T807. The user asked for the description "inside the window", and
# that is not decoration: a pane authored as a SIBLING of mfSearch would be a second closing
# point, and the text would stand on screen after the box that explains it is gone (I112g).
$v352Bad = @()
$doc352 = Doc (Join-Path $dir "WoD20th.lfm")

# (g) zero-guard FIRST: with the pane gone every leg below passes by vacancy (SPEC V20, B7).
$pane352 = @($doc352.SelectNodes("//textEditor[@name='edtMfDesc']"))
if ($pane352.Count -ne 1) {
    $v352Bad += "(g) edtMfDesc is authored $($pane352.Count) time(s) on the root form, expected 1 - without it legs (a) and (b) read an empty set (SPEC I112b, V20, B7)"
} else {
    # (a) CHILD of mfSearch and not a sibling - the whole of I112g in one XPath.
    if (@($doc352.SelectNodes("//layout[@name='mfSearch']/textEditor[@name='edtMfDesc']")).Count -ne 1) {
        $v352Bad += "(a) edtMfDesc is not a child of mfSearch - a sibling would be a second closing point and the text would outlive the box mfClose hides (SPEC I112g, V135)"
    }
    # (b) read-only, wrapping, no field. wordWrap is the only thing keeping the horizontal bar
    # off - there is no scroll property in SDK3 - and a field would write book text onto the
    # character, which is the line V29 draws and V334d keeps for the other eight panes.
    $p352 = $pane352[0]
    if ($p352.GetAttribute("readOnly") -ne 'true') { $v352Bad += "(b) edtMfDesc is not readOnly - book text typed over would look to the player like it saved (SPEC I112b)" }
    if ($p352.GetAttribute("wordWrap") -ne 'true') { $v352Bad += "(b) edtMfDesc has no wordWrap - with no scroll property in SDK3 the only way to have no horizontal bar is to give it nothing to scroll (SPEC I112b, V333f)" }
    if ($p352.HasAttribute("field")) { $v352Bad += "(b) edtMfDesc owns a field - book text would be written onto the character (SPEC V29, V334d)" }
}

# (c) the box grew to the RIGHT and the result column did not move one pixel. Absolute: 300+1000
# closes at 1300 of the 1595 the sheet has, 80+620 at 700 of the 733 the body has (SPEC I112a).
#
# top is 80 and not the 40 it was until B107. The box hangs off the body layout now, whose
# origin sits ABOVE the 40px tabStrip, while a tab layout's origin sits below it - so 80 here
# is the same place on screen 40 used to be. The number moved because the PARENT did; reading
# it as a resize and "fixing" it back to 40 would slide the box up under the strip (V367).
$box352 = $doc352.SelectSingleNode("//layout[@name='mfSearch']")
if ($null -eq $box352) { $v352Bad += "(c) mfSearch is gone - there is no box for the pane to live in (SPEC I107c, V209)" }
else {
    foreach ($want352 in @(@('left','300'), @('top','80'), @('width','1000'), @('height','635'))) {
        $got352 = $box352.GetAttribute($want352[0])
        if ($got352 -ne $want352[1]) { $v352Bad += "(c) mfSearch $($want352[0])='$got352', expected $($want352[1]) - the box closes at 1300 of 1595 and at 700 of 733, and it grew to the RIGHT (SPEC I112a, V367)" }
    }
    foreach ($nm352 in @('edtMfQuery','lblMfLoading','dynMfRow_1','dynMfRow_20')) {
        $c352 = $doc352.SelectSingleNode("//layout[@name='mfSearch']/*[@name='$nm352']")
        if ($null -eq $c352) { $v352Bad += "(c) $nm352 is gone from the box (SPEC I107c, V209)" }
        elseif ($c352.GetAttribute("left") -ne '20' -or $c352.GetAttribute("width") -ne '480') {
            $v352Bad += "(c) $nm352 sits at left=$($c352.GetAttribute('left')) width=$($c352.GetAttribute('width')), expected 20/480 - the result column does not move, the pane took the ground the box GREW (SPEC I112a)"
        }
    }
}

# (d) the pane is CLEARED wherever the marking is dropped. Text left standing with no marking
# under it is precisely the lie I107m exists to stop.
# mfDesc and not mfSelect since T857: the pane painting was pulled OUT of mfSelect so the
# opening (pre-marked, I122a) and the remove line (I122c) could share it. Reading mfSelect
# here would fail correct code, and mfDesc is the STRONGER target - it is now the one and
# only writer of the pane, where before it was one caller among the ones that could be.
$sel352 = LuaFn $rootTxt 'mfDesc'
foreach ($fn352 in @('mfFilter', 'mfPage')) {
    $body352 = LuaFn $rootTxt $fn352
    if (-not $body352) { $v352Bad += "(d) $fn352 is gone - the pane would keep the text of a row that is no longer marked (SPEC V209)" }
    elseif ($body352 -notmatch '\["edtMfDesc"\]\.text = ""') { $v352Bad += "(d) $fn352 does not clear edtMfDesc - the marking drops and the description stays, which is what I107m refuses (SPEC I112f)" }
}

# (e) the 563KB module is parsed in mfOpen's TIMER and nowhere else. Inside mfSelect it would
# freeze the tab on the first click of a row, which is I107f coming in the other door.
$open352 = LuaFn $rootTxt 'mfOpen'
if (-not $open352) { $v352Bad += "(e) mfOpen is gone - leg (e) measured nothing (SPEC V209)" }
else {
    $timer352 = [regex]::Match($open352, '(?s)setTimeout\(function\(\)(.*?)end, 1\);')
    if (-not $timer352.Success) { $v352Bad += "(e) mfOpen schedules no setTimeout - there is nowhere for the parse to go (SPEC I107f)" }
    elseif ($timer352.Groups[1].Value -notmatch 'require[,(]\s*"desc" \.\. ') { $v352Bad += "(e) the description-module require is not inside mfOpen's timer - either package.loaded is never warmed and the first marking parses 563KB, or the parse lands in the turn the box is drawn (SPEC I112e, R92, T479)" }
}
if (-not $sel352) { $v352Bad += "(e) mfSelect is gone - nothing paints the pane (SPEC V209)" }
elseif ($sel352 -match '\brequire\s*[,(]') { $v352Bad += "(e) mfSelect requires a module - 563KB parsed on the first click of a row freezes the tab in the middle of the choice (SPEC I112e, I107f, T479). The pattern reads the GUARDED form too: pcall(require, ...) is still a parse, and V364's guard changed the call shape without changing what this leg is for" }

# (f) ONE function returns the text and BOTH readers come through it. Two readers are two
# rulers for the composite key of I102g, and the second one drifts in silence (SPEC V344d).
$readers352 = @([regex]::Matches((CodeOf (Join-Path $dir "WoD20th.lfm")), 'DESC\[\s*sub\s*\.\.'))
if ($readers352.Count -ne 1) { $v352Bad += "(f) the composite key is built in $($readers352.Count) place(s), expected exactly 1 - one ruler for the key, and the ? box and the pane both come through it (SPEC I112d, V344d, V135)" }
if (-not (LuaFn $rootTxt 'descText')) { $v352Bad += "(f) descText is gone - it is the one reader I112d declares (SPEC V209)" }
$pop352 = LuaFn $rootTxt 'popOpen'
if (-not $pop352) { $v352Bad += "(f) popOpen is gone - leg (f) measured nothing (SPEC V209)" }
elseif ($pop352 -notmatch 'descFor\(') { $v352Bad += "(f) popOpen does not read through descFor - the ? box would be a second reader carrying its own copy of the key rule (SPEC I112d, V135)" }
if ($sel352 -and $sel352 -notmatch 'descFor\(') { $v352Bad += "(f) mfDesc does not read through descFor - the pane would be the second reader (SPEC I112d, V135)" }

if ($v352Bad) { foreach ($b in $v352Bad) { Fail "V352 $b" } }
else { Pass "V352 edtMfDesc is a child of mfSearch, read-only and wrapping with no field, the box closes at 1300/700 with the result column still at 20/480, mfFilter and mfPage clear the pane, the 563KB module is parsed in mfOpen's timer and never in mfSelect, and one descText serves both readers" }
# ---- V354: the generic box loses nothing the dropdown it replaces had ---------------------
# SPEC I113, V354, Q30, T808/T809, and REWRITTEN by T841 after B100. Legs are written per WAVE,
# but a leg is the FORM that wave adds - never the rule of the FAMILY. V354 was born when ONE
# wave stood on disk and the two collapsed into each other, so the PILOT's shape - numbered
# rows, a description module on disk - became a requirement of every wave and forbade exactly
# what V364/I116c made legal in the same round (SPEC B100).
#
# Per wave: Tpl/File are the template and the file that draws it. Mod is the description module
# and is OPTIONAL - a wave with NO module is legal, its call site names none, and its pane falls
# to V360c's sentence through the path V364b already opens. RootArg is the third argument of
# mfOpen AS AUTHORED: a literal root, or the $(field) indirection when one template serves more
# than one root. Roots lists the PICKER_LIST keys the wave really opens, each with the FORM its
# rows are drawn in and how many of them there are:
#
#   numbered  <Tpl field="<root>_N">   N rows of ONE root          (the pilot: 16 background_N)
#   bare      <Tpl field="<root>">     no number, >1 root per tpl  (HeaderPicker: nature/demeanor)
#   indirect  <Tpl num="N">            the call site names NO root - the field is built INSIDE
#                                      the template (SpecialityRow: field="speciality_$(num)")
#
# The roster is NAMED and not pattern-matched: a wildcard would swallow the next wave the day
# someone writes it and report it as measured (SPEC V328d). Leg (i) is what keeps a named roster
# honest - it asks the CODE which templates open the box and refuses any nobody declared.
$V354_WAVE = @(
    @{ Tpl = 'OpenAbility'; File = 'WoD20.2.lfm'; Mod = 'Background'; FieldAttr = '$(field)'; RootArg = 'background';
       Roots = @( @{ Root = 'background'; Form = 'numbered'; Rows = 21 } ) },
    # Wave 2, and the two shapes that B100 was opened over. HeaderPicker draws its rows BARE -
    # field="nature" and field="demeanor", no number - and one template carries TWO roots, whose
    # PICKER_LIST entries are the same authored list by identity (V211c). SpecialityRow draws
    # them INDIRECT: the call site is <SpecialityRow num="N"> and names no root at all, the
    # field being built inside the template.
    #
    # HeaderPicker DECLARES 'Nature' since T860. It declared no module while T838 was still
    # extracting the text, and that was correct THEN - descNature_en/_pt did not exist. Leaving
    # the declaration behind once they did is what B110 cost: the ? named the module and the
    # picker for the SAME row named nothing, so one showed the book and the other said the book
    # carries nothing. One authored list serves both roots by identity, so one module does too.
    # SpecialityRow still declares none, and never will: Q33 settled it (user 2026-08-30).
    @{ Tpl = 'HeaderPicker'; File = 'WoD20.1.lfm'; Mod = 'Nature'; FieldAttr = '$(field)'; RootArg = '$(field)';
       Roots = @( @{ Root = 'nature'; Form = 'bare'; Rows = 1 }, @{ Root = 'demeanor'; Form = 'bare'; Rows = 1 } ) },
    @{ Tpl = 'SpecialityRow'; File = 'WoD20.1.lfm'; FieldAttr = 'speciality_$(num)'; RootArg = 'speciality';
       Roots = @( @{ Root = 'speciality'; Form = 'indirect'; Rows = 10 } ) },
    # Wave 3 (T872): the three families the picker filter actually prunes. DiscRow is the first
    # template on the roster to carry two NUMBERED roots - one template serves both boxes of
    # WoD20.12 so the clan slots and the open slots cannot offer different lists (V178), and
    # PICKER_LIST["clanDisc"] IS PICKER_LIST["disc"] by identity (V211c), so leg (h) reads one
    # authored list twice rather than two copies.
    #
    # The module name is the FILE's, not the family's: descText builds desc<Mod>_<lang>.lua, so
    # it is Disc and never Discipline, and all four halves are on disk.
    #
    # MainPathRow and SecPathRow are two roots on two templates whose comboBox line was BYTE
    # FOR BYTE identical. A textual replace converts both and leaves the second opening the
    # FIRST one's list - it happened in the 157th - so leg (b) reading RootArg per template is
    # what catches that, and it is why they are two entries and not one.
    @{ Tpl = 'DiscRow'; File = 'WoD20.11.lfm'; Mod = 'Disc'; FieldAttr = '$(field)'; RootArg = 'disc';
    # Row counts went 12/12/15 -> 15/15/20 in T873, when the eleven typed rows of these three
    # boxes became pickers too and their three templates were deleted (SPEC I130b, V378).
       Roots = @( @{ Root = 'disc'; Form = 'numbered'; Rows = 15 }, @{ Root = 'clanDisc'; Form = 'numbered'; Rows = 4 } ) },
    @{ Tpl = 'MainPathRow'; File = 'WoD20.11.lfm'; Mod = 'Path'; FieldAttr = '$(field)'; RootArg = 'mainPath';
       Roots = @( @{ Root = 'mainPath'; Form = 'numbered'; Rows = 4 } ) },
    @{ Tpl = 'SecPathRow'; File = 'WoD20.11.lfm'; Mod = 'Path'; FieldAttr = '$(field)'; RootArg = 'secPath';
       Roots = @( @{ Root = 'secPath'; Form = 'numbered'; Rows = 15 } ) },
    @{ Tpl = 'RitualRow'; File = 'WoD20.11.lfm'; Mod = 'Ritual'; FieldAttr = '$(field)'; RootArg = 'ritual';
    # Rows is what the box DRAWS (22 since T880), never RITUAL_ROWS - that constant is the highest
    # index (27) and carries the five burned vacancies inside it (SPEC V204 amended, V251).
       Roots = @( @{ Root = 'ritual'; Form = 'numbered'; Rows = 22 } ) },
    # Wave 4 (T874): the three families the AFFILIATION prunes, and the one tab where all three
    # description panes left in one round (SPEC I131b, V365d). All three declare the SAME module,
    # Numina, because there is one book behind the tab - descNumina_en/_pt carried the text for
    # the three panes that just went, and the ? reads the same halves through descFor.
    #
    # hedgeRitual names it too, and knowingly: the 53 rituals have no entries in descNumina yet,
    # so the ? and the picker pane fall to V360c's sentence TOGETHER. That is the point of V373a -
    # the two doors of one row name one module - and declaring nothing here would put the ? and
    # the pane on two different answers the day the first ritual text is written.
    #
    # Rows are the PICKER rows. T875 converted the five typed rows of numina and of psychic, so
    # for those two 'picker = total - typed' resolves to the whole box. hedgeRitual still ends in
    # five <edit>s: its picker draws read-only dots and the typed rows keep their level in BOUND
    # ones, so converting would have eaten 25 fields with data (SPEC V269a, V269b, B122).
    @{ Tpl = 'HedgePicker'; File = 'WoD20.7.lfm'; Mod = 'Numina'; FieldAttr = '$(field)'; RootArg = 'numina';
    # 12 -> 15 in T830, then 15 -> 20 in T875 when the five typed rows converted: every numina row
    # is a picker now and NUMINA_FREE_ROWS is 0, so 'picker = total - typed' resolves to the whole
    # box (SPEC I117b, I117c, I130b, V263).
       Roots = @( @{ Root = 'numina'; Form = 'numbered'; Rows = 20 } ) },
    @{ Tpl = 'PsychicPicker'; File = 'WoD20.7.lfm'; Mod = 'Numina'; FieldAttr = '$(field)'; RootArg = 'psychic';
    # 19 -> 15 in T830, and NOT because a row was added: psychic_16.._19 are DELETED BY DECISION
    # (SPEC Q32, V362b) and the four ids are burned. PSYCHIC_ROWS stays 24 - it is the highest
    # index. T875 then converted the five typed rows at _20.._24, so the box draws TWENTY pickers
    # over a span of 24 indices with four burned inside it (SPEC V263a, V204).
       Roots = @( @{ Root = 'psychic'; Form = 'numbered'; Rows = 20 } ) },
    @{ Tpl = 'HedgeRitualPicker'; File = 'WoD20.7.lfm'; Mod = 'Numina'; FieldAttr = '$(field)'; RootArg = 'hedgeRitual';
    # 12 -> 17 in T892: the last five typed rows converted, HEDGE_RITUAL_FREE_ROWS went to zero and
    # 'picker = total - typed' resolves to the whole box, the way numina and psychic already did.
    # 17 -> 20 in T912 (SPEC I136m, user 2026-09-02 item 7): three slots more, so the rituals hold
    # as many as HEDGE MAGIC PATHS beside them. FREE_ROWS was already zero, so the three were born
    # pickers and their fields came off the declareTrait loop that was already there.
    # What held it back was the level - the typed rows kept it in BOUND dots, so converting ate 25
    # fields with data (SPEC B122) - and the Ritual Level row of the picker pane is where it went.
       Roots = @( @{ Root = 'hedgeRitual'; Form = 'numbered'; Rows = 20 } ) }
)

# Templates allowed to open the box WITHOUT being a wave of I113e. NAMED, never a generic skip:
# a whitelist that took any template would kill leg (i) for every wave (SPEC V328d, B7).
# MeritPicked is the merit/flaw box, older than I113 - its root is $(sub), resolved at runtime,
# so no static reader can follow it, and it already has owners (SPEC I107a, V331, V332).
$V354_EXEMPT = @('MeritPicked')

$v354Bad = @()
$v354Seen = 0
$v354Rooted = 0
foreach ($w354 in $V354_WAVE) {
    $doc354 = Doc (Join-Path $dir $w354.File)
    $tpl354 = $doc354.SelectSingleNode("//template[@name='$($w354.Tpl)']")

    # (g) zero-guard for THIS wave: with the template gone every leg under it reads an empty set.
    if ($null -eq $tpl354) { $v354Bad += "(g) $($w354.File) declares no $($w354.Tpl) - the wave's rows have no shape and legs (a) to (h) would measure nothing (SPEC V209, V20, B7)"; continue }
    $v354Seen++

    # (a) the I107a1 pair that REPLACES V13: one visible button that opens the box, one twin
    # <edit> carrying the field, and the twin cannot be typed into. Hidden AND disabled is a
    # stronger guarantee than readOnly, and it is the form MeritPicked already ships.
    $cbo354 = @($tpl354.SelectNodes(".//comboBox"))
    if ($cbo354.Count -ne 0) { $v354Bad += "(a) $($w354.Tpl) still carries $($cbo354.Count) comboBox - the row would keep the dropdown Q30 replaced (SPEC I107a1, V13)" }
    $btn354 = @($tpl354.SelectNodes("button[contains(@onClick,'mfOpen(')]"))
    if ($btn354.Count -ne 1) { $v354Bad += "(a) $($w354.Tpl) has $($btn354.Count) button(s) opening the box, expected 1 - two openers are two doors onto one row (SPEC I107a1, V135)" }
    $twin354 = @($tpl354.SelectNodes("edit[@field='" + $w354.FieldAttr + "']"))
    if ($twin354.Count -ne 1) { $v354Bad += "(a) $($w354.Tpl) hands its field to $($twin354.Count) edit(s), expected the one hidden twin (SPEC I107a1)" }
    else {
        if ($twin354[0].GetAttribute("visible") -ne 'false') { $v354Bad += "(a) the twin edit of $($w354.Tpl) is not authored visible=false - two entries would stand on the same pixels and the button's text would be the one nobody sees (SPEC I107a1)" }
        if ($twin354[0].GetAttribute("enabled") -ne 'false') { $v354Bad += "(a) the twin edit of $($w354.Tpl) is not authored enabled=false - the field would take the keyboard again, and 'only a choice from the list writes it' is exactly what V13 used to guarantee (SPEC I107a1, V13)" }
        if ($btn354.Count -eq 1 -and $twin354[0].GetAttribute("left") -ne $btn354[0].GetAttribute("left")) { $v354Bad += "(a) the twin sits at x=$($twin354[0].GetAttribute('left')) and the button at x=$($btn354[0].GetAttribute('left')) - one field, two widgets, one place (SPEC I107a1)" }
    }

    # (b) REWRITTEN by T841 (SPEC B100): the LIST is mandatory, the description MODULE is NOT.
    # The list half is unchanged - the box reads PICKER_LIST and no second copy of a list is
    # born (V208, V211). The module half went from EXISTENCE to COHERENCE: the wave DECLARES
    # whether it has one, and BOTH outcomes are legal. Demanding the file either way forbids
    # converting a list with no module, which is what I116c/V364 made legal and what Q33
    # OBLIGES - speciality will never have one, by the user's decision of 2026-08-30.
    # B113: the tail after the list is OPEN. This matched the WHOLE signature and so it went
    # red the moment I115b gave mfOpen a fifth argument for the box size - a ruler AHEAD of
    # the code is B106, and this was the same mistake running backwards. What is mandatory is
    # the field and the PICKER_LIST root; mod and size and whatever I115b adds next are not
    # this leg's business, so the shape now closes on [,)] instead of on ) alone.
    if ($btn354.Count -eq 1) {
        $click354 = $btn354[0].GetAttribute("onClick")
        $rootPat354 = [regex]::Escape($w354.RootArg)
        if ($w354.Mod) {
            $want354 = "mfOpen\(self,\s*'[^']+',\s*'" + $rootPat354 + "',\s*'" + [regex]::Escape($w354.Mod) + "'\s*[,)]"
            $shape354 = "the FIELD, the PICKER_LIST root '$($w354.RootArg)' and the description module '$($w354.Mod)'"
        }
        else {
            $want354 = "mfOpen\(self,\s*'[^']+',\s*'" + $rootPat354 + "'\s*[,)]"
            $shape354 = "the FIELD and the PICKER_LIST root '$($w354.RootArg)' - and NO module, because this wave declares none"
        }
        if ($click354 -notmatch $want354) {
            $v354Bad += "(b) $($w354.Tpl) opens with '$click354' - the arguments of I113a are $shape354 (SPEC I113a, B100)"
        }
    }
    if ($w354.Mod) {
        foreach ($half354 in @('en', 'pt')) {
            if (-not (Test-Path -LiteralPath (Join-Path $plugin "desc$($w354.Mod)_$half354.lua"))) {
                $v354Bad += "(b) $($w354.Tpl) DECLARES the module '$($w354.Mod)' and desc$($w354.Mod)_$half354.lua is not at the plugin root - a declared module that is not on disk is the pane opening empty on a wave that promised text (SPEC I102e, V210, V215)"
            }
        }
    }
    else {
        foreach ($half354 in @('en', 'pt')) {
            if (Test-Path -LiteralPath (Join-Path $plugin "desc$($w354.Tpl)_$half354.lua")) {
                $v354Bad += "(b) $($w354.Tpl) declares NO module and desc$($w354.Tpl)_$half354.lua is on disk - text nobody opens is text nobody maintains; either the wave names it or the file goes (SPEC B100, V135)"
            }
        }
    }

    # (e) REWRITTEN by T841 (SPEC B100): there are THREE forms of row and the ruler reads all
    # three. A row short is a picker orphaned without a list; a row over is a row that lost its
    # combo and never got the box. Counting only the NUMBERED form let the whole nature/demeanor
    # wave through by VACANCY - 924 measured strings fell to 856 and the gate stayed green.
    $raw354 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir $w354.File)))
    foreach ($r354 in @($w354.Roots)) {
        $v354Rooted++
        $pat354 = $null
        if     ($r354.Form -eq 'numbered') { $pat354 = "<$($w354.Tpl)\s+field=`"" + [regex]::Escape($r354.Root) + "_\d+`"" }
        elseif ($r354.Form -eq 'bare')     { $pat354 = "<$($w354.Tpl)\s+field=`"" + [regex]::Escape($r354.Root) + "`"" }
        elseif ($r354.Form -eq 'indirect') { $pat354 = "<$($w354.Tpl)\s+num=`"\d+`"" }
        else { $v354Bad += "(e) wave $($w354.Tpl) declares form '$($r354.Form)' for root '$($r354.Root)', which is none of numbered/bare/indirect - a form the ruler cannot read is a wave that passes by vacancy (SPEC V354e, B100)" }

        if ($null -ne $pat354) {
            $n354 = @([regex]::Matches($raw354, $pat354)).Count
            if ($n354 -ne $r354.Rows) { $v354Bad += "(e) $($w354.File) draws $n354 $($w354.Tpl) row(s) for root '$($r354.Root)' in the $($r354.Form) form and this wave converts $($r354.Rows) (SPEC I113e, V354e)" }
            # (g) per ROOT, and not per wave: with more than one root on one template the
            # whole-wave guard above cannot see the root that vanished (SPEC B100).
            if ($n354 -eq 0) { $v354Bad += "(g) root '$($r354.Root)' of $($w354.Tpl) has not one row in $($w354.File) - every leg reading that root measures nothing (SPEC V20, B7, B100)" }
        }

        # (h) NAMED by T841 (SPEC B100). This lived INSIDE leg (d) since T809 and a leg with no
        # name cannot be charged wave by wave. While the combo stood, V9/V22/V28 swept these
        # names into the visible census through ListOf; taking the combo away took the whole
        # family out of that census without a word - 68 archetypes on the wave that measured it.
        if (-not $PICKER.ContainsKey($r354.Root)) { $v354Bad += "(b) PICKER_LIST has no '$($r354.Root)' key - the box would open on nothing at all (SPEC I113b, V211)" }
        elseif (@($PICKER[$r354.Root]).Count -eq 0) { $v354Bad += "(b) PICKER_LIST['$($r354.Root)'] is empty - every leg reading it measures nothing (SPEC V211, V20)" }
        else {
            $noPt354 = @(); $noMap354 = @()
            foreach ($it354 in @($PICKER[$r354.Root])) {
                if ($it354 -eq '') { continue }
                if (-not $ptK.Contains($it354)) { $noPt354 += $it354 }
                if (-not $embedded.ContainsKey($it354)) { $noMap354 += $it354 }
            }
            if ($noPt354.Count -gt 0) { $v354Bad += "(h) $($noPt354.Count) item(s) of PICKER_LIST['$($r354.Root)'] carry no [pt] key ($((@($noPt354) | Select-Object -First 3) -join ', ')) - with the combo gone nothing else sweeps them into the visible set (SPEC V10, V28, B7, B100)" }
            if ($noMap354.Count -gt 0) { $v354Bad += "(h) $($noMap354.Count) item(s) of PICKER_LIST['$($r354.Root)'] are absent from the PT map ($((@($noMap354) | Select-Object -First 3) -join ', ')) - they would never translate (SPEC V28, B14, B100)" }
        }
    }

    # (i, per ROOT) MEASURED by the T842 bench: leg (i) below charges completeness of the
    # TEMPLATE roster, and that is not enough for the BARE form. Dropping 'demeanor' from this
    # wave's Roots left the gate GREEN - HeaderPicker was still declared, so (i) was satisfied,
    # and the second root simply stopped being measured. That is B100's hole surviving inside
    # the fix for B100, and the cure is the same one: ask the CODE. A bare row NAMES its root in
    # the .lfm, so the set of roots drawn has to EQUAL the set declared - no more, no less.
    #
    # Only the bare form: numbered and indirect carry ONE root each and the count in (e) already
    # pins it. Asking this of a numbered wave would read background_1..16 as sixteen roots.
    $bare354 = @(@($w354.Roots) | Where-Object { $_.Form -eq 'bare' } | ForEach-Object { $_.Root })
    if ($bare354.Count -gt 0) {
        $drawn354 = @([regex]::Matches($raw354, "<$($w354.Tpl)\s+field=`"([^`"]+)`"") | ForEach-Object { $_.Groups[1].Value } | Sort-Object -Unique)
        foreach ($d354 in $drawn354) {
            if ($bare354 -notcontains $d354) { $v354Bad += "(i) $($w354.File) draws a $($w354.Tpl) on root '$d354' and this wave does not declare it - an undeclared root is measured by nothing at all, which is how 68 archetypes lost their guard with the gate green (SPEC V354i, B100)" }
        }
        foreach ($b354 in $bare354) {
            if ($drawn354 -notcontains $b354) { $v354Bad += "(i) this wave declares the bare root '$b354' and $($w354.File) draws no $($w354.Tpl) on it - a declared root with no row excuses nothing (SPEC V354i, V20)" }
        }
    }
}

# (d, first half) the two things the dropdown guaranteed and the box has to keep. Measured on
# the BOX and not per wave: one box serves every family (SPEC I113f).
$open354 = LuaFn $rootTxt 'mfOpen'
if (-not $open354) { $v354Bad += "(d) mfOpen is gone - there is no box left to measure (SPEC V209)" }
else {
    $timer354 = [regex]::Match($open354, '(?s)setTimeout\(function\(\)(.*?)end, 1\);')
    if (-not $timer354.Success) { $v354Bad += "(d) mfOpen schedules no setTimeout - the pool is built nowhere (SPEC I107f)" }
    else {
        $body354 = NoComments $timer354.Groups[1].Value
        if ($body354 -notmatch 'PICKER_LIST\[MF\.list\]') { $v354Bad += "(d) the box does not read PICKER_LIST[MF.list] - it is carrying a list of its own, and a second copy of a list is what V208 exists to stop (SPEC I113b, V211)" }
        if ($body354 -notmatch 'MF\.pool\[#MF\.pool \+ 1\] = cur;') { $v354Bad += "(d) the box never appends the value the row is HOLDING - a renamed item would vanish from the only door the field has left, which is B91 walking back in (SPEC V200, V346b)" }
        else {
            $iOrph354 = $body354.IndexOf('MF.pool[#MF.pool + 1] = cur;')
            $iPool354 = $body354.IndexOf('MF.pool = {};')
            if ($iPool354 -ge 0 -and $iOrph354 -lt $iPool354) { $v354Bad += "(d) the orphan is appended BEFORE the pool is built, so the append is thrown away one line later - the same order V350b pins on the dropdown (SPEC V200)" }
        }
    }
}
$filt354 = LuaFn $rootTxt 'mfFilter'
if (-not $filt354) { $v354Bad += "(d) mfFilter is gone - nothing orders what the box shows (SPEC V209)" }
else {
    $fb354 = NoComments $filt354
    if ($fb354 -notmatch 'table\.sort\(hits, function') { $v354Bad += "(d) mfFilter does not sort its hits - the box would come out in whatever order the pool happens to hold, and for a merit map that is hash order (SPEC V350, I110)" }
    if ($fb354 -notmatch 'for i = 1, #MF\.pool, 1 do') { $v354Bad += "(d) mfFilter does not walk MF.pool - it is reading a source the generic opener no longer fills (SPEC I113b)" }
}

# (i) NEW with T841 (SPEC B100). The roster is NAMED on purpose, and a named roster only stays
# honest if the CODE is asked which templates open the box: a wave converted and never added to
# it passes by VACANCY - every leg above reads an empty set and the gate stays green, which is
# the third lesson B100 wrote. The cure is not a wildcard, it is completeness charged from the
# other side (SPEC V354i, V328d).
$v354Opens = @()
foreach ($f354 in $files) {
    $d354 = Doc $f354.FullName
    foreach ($t354 in @($d354.SelectNodes("//template[@name]"))) {
        if (@($t354.SelectNodes(".//button[contains(@onClick,'mfOpen(')]")).Count -gt 0) { $v354Opens += $t354.GetAttribute("name") }
    }
}
$v354Opens = @($v354Opens | Sort-Object -Unique)
$v354Known = @(@($V354_WAVE | ForEach-Object { $_.Tpl }) + $V354_EXEMPT)
foreach ($o354 in $v354Opens) {
    if ($v354Known -notcontains $o354) { $v354Bad += "(i) template '$o354' opens the box with mfOpen( and is in NEITHER the wave roster nor the named exemption list - a converted wave nobody declared passes by vacancy, which is exactly what B100 measured (SPEC V354i, V328d)" }
}
foreach ($e354 in $V354_EXEMPT) {
    if ($v354Opens -notcontains $e354) { $v354Bad += "(i) '$e354' is on the exemption list and opens no box - an exemption for a template that no longer opens one excuses nothing, and a stale line here is how a real wave slips in later (SPEC V354i, V20)" }
}

# (g) the roster itself: no wave read means every leg above passed by vacancy.
if ($v354Seen -eq 0) { $v354Bad += "(g) not one wave of I113e was read - the roster is empty or its templates are gone (SPEC V20, B7)" }
if ($v354Rooted -eq 0) { $v354Bad += "(g) not one ROOT was read across the roster - a wave with no roots declared measures nothing at all (SPEC V20, B100)" }

if ($v354Bad) { foreach ($b in $v354Bad) { Fail "V354 $b" } }
else { Pass "V354 $v354Seen wave(s) of I113e converted over $v354Rooted root(s) - $((@($V354_WAVE | ForEach-Object { $w = $_; (@($w.Roots | ForEach-Object { "$($_.Rows) $($_.Root) row(s) [$($_.Form)]" })) -join ', ' })) -join ' | ') - each on the button-plus-hidden-twin of I107a1 with no comboBox left, opening on its own PICKER_LIST root, coherent with the description module it declares or with declaring none, every root drawing the rows it says and every item still covered by the PT map, and the $($v354Opens.Count) template(s) that open the box all accounted for" }

# ---- V355: the picker says what it HAS and what it has NOT, in the language in force -------
# SPEC I114, V355, T813/T815, user 2026-08-29. Every leg below is the SHAPE of code and is
# measurable NOW. I114i - whether a Lua-written fontStyle and a Lua-read ctrl.width actually
# land on screen - is the tab test of T816 and is deliberately NOT asked here: a check that
# guesses at runtime is a check that is green for the wrong reason.
$shownFn355   = LuaFn $rootTxt 'mfShown'
$labelFn355   = LuaFn $rootTxt 'mfLabel'
$paintFn355   = LuaFn $rootTxt 'mfPaint'
$filterFn355  = LuaFn $rootTxt 'mfFilter'
$openFn355    = LuaFn $rootTxt 'mfOpen'
$confirmFn355 = LuaFn $rootTxt 'mfConfirm'
$fitFn355     = LuaFn $rootTxt 'fitSize'
$rootNC355    = NoComments $rootTxt
$v355Bad = @()

# (i) the zero-guard runs FIRST and on purpose: with mfShown gone, or nothing dimmed, or
# nothing shrunk, every leg under it reads an empty set and passes by VACANCY, which is the
# whole of B7 and the reason V20 asks for a mutation before a check is believed.
$dim355 = @([regex]::Matches($rootNC355, '\.opacity\s*=\s*0\.80\b'))
$fitCalls355 = @([regex]::Matches($rootNC355, '=\s*fitSize\('))
if (-not $shownFn355) { $v355Bad += "(i) mfShown was not found on the root form - every leg of the display contract below would measure nothing (SPEC V209, V20, B7)" }
if (-not $fitFn355) { $v355Bad += "(i) fitSize was not found on the root form - leg (g) and the callers that shrink text would measure nothing (SPEC V209, V20, B7)" }
if ($dim355.Count -eq 0) { $v355Bad += "(i) not one write of opacity 0.80 is left on the root form - the 'not filled in' cue of I114e is gone and leg (b) would pass by vacancy (SPEC V244, V20, B7)" }
if ($fitCalls355.Count -eq 0) { $v355Bad += "(i) fitSize is called nowhere - the ruler exists and nothing shrinks, which is leg (g) green with the feature absent (SPEC I114d, V20, B7)" }

# (a) ONE function says what a pool entry READS as, and BOTH readers go through it - the
# painter of the box and the sort key of the filter. A second place deciding the shown text is
# V135 walking in through the door translation opened: the day one learns a name and the other
# does not, the row the player clicks is not the row the box sorted.
if ($shownFn355) {
    $sb355 = NoComments $shownFn355
    if ($sb355 -notmatch 'translateSheetText\(') { $v355Bad += "(a) mfShown never translates - the box would show the canonical English V24 stores while the sheet is in pt (SPEC I114a, V28)" }
    if ($sb355 -notmatch ([regex]::Escape('"-- " .. t .. " --"'))) { $v355Bad += "(a) mfShown does not build the dashed form of the blank entry - the way OUT of the row would read as an empty line, and V15's blank is not a name (SPEC I114b)" }
}
foreach ($rd355 in @(@{ N = 'mfPaint'; B = $paintFn355 }, @{ N = 'mfFilter'; B = $filterFn355 })) {
    if (-not $rd355.B) { $v355Bad += "(a) $($rd355.N) was not found - one of the two readers of the display contract is gone (SPEC V209)"; continue }
    $rb355 = NoComments $rd355.B
    if ($rb355 -notmatch 'mfShown\(') { $v355Bad += "(a) $($rd355.N) does not call mfShown - it is deciding on its own what the player reads, and two deciders disagree the day one is translated and the other is not (SPEC I114a, V135)" }
    if ($rb355 -match 'translateSheetText\(') { $v355Bad += "(a) $($rd355.N) calls translateSheetText itself - that is the SECOND place building the shown text, which is exactly what routing both readers through mfShown was for (SPEC I114a, V135)" }
}

# (b) the two states are painted in ONE place. Two writers are two states that DRIFT: the day
# one learns a new dimming number the other keeps the old one, and the sheet then says "empty"
# in two different voices with the gate green over both.
if (-not $labelFn355) { $v355Bad += "(b) mfLabel was not found - nothing paints either state of a picker row (SPEC V209)" }
else {
    $lb355 = NoComments $labelFn355
    foreach ($mk355 in @(
        @{ Rx = '\.opacity\s*=\s*1\b';                     W = 'the FILLED state at opacity 1' },
        @{ Rx = '\.opacity\s*=\s*0\.80\b';                 W = 'the EMPTY state at opacity 0.80 (SPEC V244 as amended on 2026-08-29 and again on 2026-09-04, T962)' },
        @{ Rx = [regex]::Escape('"-- " .. t .. " --"');    W = 'the dashed empty label of I114e' },
        @{ Rx = '\.fontStyle\s*=\s*"italic"';              W = 'the italic of the EMPTY state' },
        @{ Rx = '\.fontStyle\s*=\s*""';                    W = 'the roman of the FILLED state' }
    )) {
        if ($lb355 -notmatch $mk355.Rx) { $v355Bad += "(b) mfLabel does not paint $($mk355.W) - one of the two states is missing and a row would keep the look of the other (SPEC I114e)" }
    }
    # The dimming number is mfLabel's ALONE. V244 was amended to carry TWO legal weights in Lua
    # - 0.75 for read-only and 0.80 for not-filled-in (0.60 until T962) - precisely so the two cues stay two
    # sentences; a second writer of 0.80 collapses them back into one.
    $dimIn355 = @([regex]::Matches($lb355, '\.opacity\s*=\s*0\.80\b')).Count
    if ($dim355.Count -ne $dimIn355) { $v355Bad += "(b) the root form writes opacity 0.80 $($dim355.Count) time(s) and only $dimIn355 of them are inside mfLabel - 'not filled in' has grown a second voice, and V244 gave it exactly one (SPEC I114e, V244, V135)" }
}

# (c) the SAME count V305c already charges, read off V305c's OWN numbers rather than measured a
# second time. Two owners for one ruler is V135, and the two DIVERGING is B70 and B73 in the
# letter - there the spec said one number and the check said another for rounds on end. What
# this leg ADDS is the two VALUES: V305c counts writers, I114e names what they write.
# The count MIGRATED here in T830, and that is the opposite of a second owner: V305 retired with
# the last sub-tab strip and its (c) leg - one writer of fontStyle on the root form, inside mfLabel
# - had no home left. A rule whose owner retires either moves or dies, and this one has a live
# subject (SPEC V305 retired, V135, I114e).
$fsRoot355 = @([regex]::Matches((CodeOf (Join-Path $dir "WoD20th.lfm")), '\.fontStyle\s*='))
$fsFn355   = LuaFn $rootTxt 'mfLabel'
$fsIn355   = if ($fsFn355) { @([regex]::Matches((NoComments $fsFn355), '\.fontStyle\s*=')).Count } else { 0 }
if (-not $fsFn355) { $v355Bad += "(c) mfLabel is gone - the ONE writer of fontStyle the exception is for does not exist, so the count below is struck over nothing (SPEC V209, B7)" }
elseif ($fsRoot355.Count -ne $fsIn355) { $v355Bad += "(c) the root form writes fontStyle $($fsRoot355.Count) time(s) and $fsIn355 of them are inside mfLabel - the exception I114e bought is for ONE writer, and a second is the blanket ban coming back with a reason (SPEC I114e, V135)" }

# (d) the way OUT of a row is offered only when there is something to get out of, and mfConfirm
# lets the blank THROUGH. The old guard refused "" as if it were "nothing marked": nil is
# unmarked, "" is a real choice, and one guard for the two hands the player an item that does
# nothing.
if (-not $openFn355) { $v355Bad += "(d) mfOpen was not found - nothing builds the pool (SPEC V209)" }
else {
    $tm355 = [regex]::Match($openFn355, '(?s)setTimeout\(function\(\)(.*?)end, 1\);')
    if (-not $tm355.Success) { $v355Bad += "(d) mfOpen schedules no setTimeout - the pool is built nowhere (SPEC I107f)" }
    else {
        $ob355 = NoComments $tm355.Groups[1].Value
        foreach ($cd355 in @(
            @{ Rx = 'local has = cur ~= nil and cur ~= "";';        W = 'does not read whether the row is HOLDING something before it builds the pool - both the blank entry and the orphan append hang off that one answer (SPEC I114c)' },
            @{ Rx = 'MF.canRemove = has;';                          W = 'does not gate the REMOVE offer on the same condition - T856 took the blank out of the pool and made it btnMfRemove, so this is where "an empty row is offered a Remove that removes nothing" is stopped now (SPEC I121a, V369d)' },
            # The BLANK TEST and not the whole line: T872 put pickAllowed beside it in the same
            # condition (V205 amended), and a pattern that owned the tail would have reddened
            # correct code the way V354(b) did in B113. What is mandatory is that the blank is
            # excluded; what else the condition asks is that other invariant's business.
            @{ Rx = 'vals[i] ~= ""';                                W = 'still copies the blank first entry into the pool - it is btnMfRemove now, a control of its own outside the paging, and a copy in the pool would put it back on page 1 only (SPEC I121b, V369a)' }
        )) {
            if ($ob355 -notmatch ([regex]::Escape($cd355.Rx))) { $v355Bad += "(d) the opener $($cd355.W)" }
        }
        # The blank going BACK into the pool is the regression this leg exists to catch
        # after T856: it would sort with the names, page with them and vanish past page 1 -
        # on the very row whose only way out it is (SPEC V369a, I121b).
        if ($ob355 -match ([regex]::Escape('MF.pool[#MF.pool + 1] = ""'))) {
            $v355Bad += "(d) the opener pushes the blank back into MF.pool - it is btnMfRemove now and has to stay out of the pool, or it pages away from the row it empties (SPEC I121b, V369a, V369b)"
        }
    }
}
if (-not $confirmFn355) { $v355Bad += "(d) mfConfirm was not found - nothing writes what the box chose (SPEC V209)" }
else {
    $cb355 = NoComments $confirmFn355
    if ($cb355 -notmatch ([regex]::Escape('if MF.picked == nil or MF.field == nil then return; end;'))) { $v355Bad += "(d) mfConfirm does not guard on nil alone - nil is UNMARKED and the empty string is a real CHOICE, and one guard for the two refuses the blank at the door (SPEC I114b, V15)" }
    if ($cb355 -match 'MF\.picked\s*==\s*""[^\r\n]*then return') { $v355Bad += "(d) mfConfirm still returns on MF.picked == the empty string - that is the OLD guard, and with it the box offers an item that cannot be chosen (SPEC I114b, V15)" }
}

# (e) I107i by the other door: the four fields that land TOGETHER leave together. A row holding
# a book, a page and a cost for a merit it no longer names is exactly the half-filled row that
# writing all four in one step exists to avoid.
if ($confirmFn355) {
    $ce355 = NoComments $confirmFn355
    $br355 = 'elseif MF.picked == "" and num ~= nil and MF.data ~= nil then'
    if ($ce355 -notmatch ([regex]::Escape($br355))) { $v355Bad += "(e) mfConfirm has no branch for the blank being CHOSEN - the name would empty and its book, page and cost would stay behind (SPEC I114, I107i)" }
    else {
        $tail355 = $ce355.Substring($ce355.IndexOf($br355))
        foreach ($col355 in @('book_', 'type_', 'costy_')) {
            if ($tail355 -notmatch ([regex]::Escape('setField("' + $col355 + '" .. num, "");'))) { $v355Bad += "(e) clearing the name does not clear $col355 - one of the three columns that came WITH it is left behind, and the row then reads a page for a merit it does not name (SPEC I114, I107i)" }
        }
    }
}

# (f) the title is DERIVED and never looked up. A roster here ages one row per wave of I113e
# and ages SILENTLY - the wave that forgets its row gets the title of nothing, and no static
# reader can tell that from a wave that has not been written yet.
if ($openFn355) {
    $of355 = NoComments $openFn355
    if ($of355 -notmatch ([regex]::Escape('MF.title = "Select " .. string.upper(string.sub(list, 1, 1)) .. string.sub(list, 2);'))) { $v355Bad += "(f) mfOpen does not DERIVE its title from the list - I114f says the three strings the .lang already carries come out of the list NAME, so a new wave adds a .lang key and not a roster row (SPEC I114f)" }
    foreach ($ttl355 in @('Select Merit', 'Select Flaw', 'Select Background')) {
        if ($of355 -match ([regex]::Escape('"' + $ttl355 + '"'))) { $v355Bad += "(f) mfOpen names the literal '$ttl355' - that is the roster the derivation replaced, back under another name (SPEC I114f)" }
    }
}
$ttlW355 = @([regex]::Matches($rootNC355, 'MF\.title\s*=[^=]'))
if ($ttlW355.Count -ne 1) { $v355Bad += "(f) MF.title is written $($ttlW355.Count) time(s) on the root form, expected the one derivation - a second writer is the label table coming back (SPEC I114f, V135)" }

# (g) the GATE's own ruler ported into the Lua (V16, V312a), and the only one there. It only
# ever DIMINISHES: there is no text-measuring call in SDK3, which is why every width check
# lives in this file and not in the sheet, and a ruler that claimed a fit would be answering a
# question it cannot answer (I114d).
if ($fitFn355) {
    $gb355 = NoComments $fitFn355
    if ($gb355 -notmatch 'while size > 7\b') { $v355Bad += "(g) fitSize has no floor of 7 - a long enough name shrinks until the row reads as blank, which is worse than the clipping it was written to stop (SPEC I114d)" }
    if ($gb355 -notmatch ([regex]::Escape('string.gsub(txt, "[\128-\191]", "")'))) { $v355Bad += "(g) fitSize does not fold UTF-8 continuation bytes away before it counts - a pt accent would count twice and the name would shrink for nothing (SPEC I114d)" }
    if ($gb355 -notmatch ([regex]::Escape('local size = 12;'))) { $v355Bad += "(g) fitSize does not start at the authored 12 - a ruler that only diminishes has to start where the XML left off (SPEC I114d)" }
    if ($gb355 -match 'size = size \+ ') { $v355Bad += "(g) fitSize GROWS the size - it is an estimate and may only shrink, because claiming a fit is what V196 and V312 are for (SPEC I114d)" }
}
$rulers355 = @([regex]::Matches($rootNC355, '\*\s*0\.5\b'))
$rulersIn355 = if ($fitFn355) { @([regex]::Matches((NoComments $fitFn355), '\*\s*0\.5\b')).Count } else { 0 }
if ($rulers355.Count -ne $rulersIn355) { $v355Bad += "(g) the root form carries $($rulers355.Count) px-per-character ruler(s) and only $rulersIn355 of them are inside fitSize - a second ruler is two answers to 'does this fit', and B86 is what one ruler with two OWNERS already cost (SPEC I114d, V135)" }

# (h) the order is the order of what is READ. Sorting on the canonical English while the box
# shows Portuguese is an order the eye cannot follow - and it is a GREEN failure, because every
# name is present and only the sequence is wrong.
if ($filterFn355) {
    $hb355 = NoComments $filterFn355
    if ($hb355 -notmatch ([regex]::Escape('key[name] = (foldKey ~= nil) and foldKey(shown)'))) { $v355Bad += "(h) mfFilter does not build its sort key from foldKey over the SHOWN name - comparing accented bytes raw puts a name in A after one in Z (SPEC I114g, I110c, V350)" }
    if ($hb355 -notmatch 'table\.sort\(hits, function\(a, b\)') { $v355Bad += "(h) mfFilter sorts with no comparator - the box comes out in whatever order the pool happens to hold, and for a merit map that is hash order (SPEC I114g, V350)" }
}
if ($rootNC355 -match '(?m)^\s*(local\s+)?function\s+foldKey\b' -or $rootNC355 -match '(?m)^\s*(local\s+)?FOLD_MAP\s*=') { $v355Bad += "(h) the root form declares a fold of its OWN - foldKey and FOLD_MAP are WoD20.6's and already GLOBAL, and a second fold is two orderings of one list (SPEC I114g, I110c, V135)" }

if ($v355Bad) { foreach ($b in $v355Bad) { Fail "V355 $b" } }
else { Pass "V355 one mfShown decides what both readers show, mfLabel paints the two states alone - roman at opacity 1, dashed italic at 0.80 - its fontStyle count agrees with V305c's, the blank joins the pool only for a row that HOLDS a value and mfConfirm lets it through and clears book, page and cost with it, the title is derived from the list with no roster, fitSize is the one width ruler with a floor of 7 counting characters and never growing, and the order is foldKey's over what is read" }
# ---- V356: the combos of VALUE stay dropdowns ---------------------------------------------
# SPEC C 144th, V356, I113e as widened, T817, T843, user 2026-08-30. EIGHT combos, and the rule
# is no longer "the two settings tabs" - Q31 broke that framing on 2026-08-30 by ruling that
# dominatorGen (4..14) and hedgeAttr STAY dropdowns, and neither of those lives on WoD20.6 or
# WoD20.10. What separates a combo of VALUE from a picker of PROSE is what the leg already
# measured and not where it sits: an authored items/values pair on a field that is a setting,
# and nothing here reading PICKER_LIST.
#
# Widening it was not optional. The tab framing had the two new ones outside the foreach, and
# B95 already measured what that costs: cmbBgCost and language carried no TYPE guard at all
# while four others were covered by ACCIDENT, through checks resolving //comboBox[@name=...] to
# ask something else entirely. That is the cover B94 showed evaporates the moment the target
# changes type, and here it was never anyone's intention in the first place.
#
# I113e's waves convert PROSE pickers off PICKER_LIST and touch none of these files, so this
# leg is not narrowing that programme - it is closing the door B94 left open, where a future
# ruler reading "no comboBox anywhere on the sheet" eats these eight without a word.
#
# This REPLACES V354(f), which said the same thing about two of the eight and never became a
# check at all (SPEC B95): its legs are written per WAVE, and (f) is the NEGATION of a wave,
# so the foreach had nowhere to hang it.
#
# The roster is the kind that GUARDS, not the kind that AGES. I114f forbids a roster that must
# GROW a row per wave, because the wave that forgets its row passes silently. This one lists
# what must NOT change, so losing an entry LIGHTS it - the opposite failure direction. Do not
# "fix" it into a derivation on the grounds that rosters age.
#
# Anchored on the FIELD and not on coordinates: WoD20.7 and WoD20.11 are being restructured by
# T830/T832, and a left/top anchor here would break the day those land. Not on the NAME either,
# and that is MEASURED rather than assumed - the language combo authors no name attribute at
# all, which is half of why B95 found it unguarded.
$V356_KEEP = @(
    @{ File = 'WoD20.6.lfm';  Where = 'Settings';    Field = 'language' },
    @{ File = 'WoD20.6.lfm';  Where = 'Settings';    Field = 'game' },
    @{ File = 'WoD20.6.lfm';  Where = 'Settings';    Field = 'sheetTheme' },
    @{ File = 'WoD20.10.lfm'; Where = 'Storyteller'; Field = 'healthLevels' },
    @{ File = 'WoD20.10.lfm'; Where = 'Storyteller'; Field = 'stSpecCost' },
    @{ File = 'WoD20.10.lfm'; Where = 'Storyteller'; Field = 'stBackgroundCost' },
    # SEVEN, not the eight Q31 seemed to name, and the difference was MEASURED by T843 rather
    # than assumed. Q31 ruled on 2026-08-30 that dominatorGen and hedgeAttr both STAY dropdowns
    # "because they are combos of VALUE like the other six - authored items/values, reading no
    # PICKER_LIST". That is true of dominatorGen and FALSE of hedgeAttr: WoD20.7 authors no
    # items and no values on it and says so in its own comment - "cbo, not cmb: this is a picker
    # of PROSE, so its list lives in PICKER_LIST and translates with the sheet" (SPEC I27, B57).
    # Listing it here would demand an authored pair, and authoring one is a SECOND copy of a
    # list that V208 exists to forbid - and it would stop translating.
    #
    # So hedgeAttr is NOT guarded here and was NOT silently converted either. The user's call came
    # on 2026-09-01, on a premise that had by then actually been read: it STAYS a dropdown, and
    # T844 was cancelled rather than built (SPEC V356 amended, T844 cancelled). That makes it a
    # THIRD category and not an omission - prose that stays a dropdown by the owner's choice. It
    # still does not belong in this roster, because listing it would demand an authored pair and
    # authoring one is the second copy of a list V208 forbids, which would stop it translating.
    # It is NAMED in V356 so a future ruler reading "no comboBox anywhere" cannot eat it in
    # silence, which is B94 waiting its turn.
    @{ File = 'WoD20.11.lfm'; Where = 'Ghoul';       Field = 'dominatorGen' }
)
$v356Bad = @()
$v356Seen = 0
foreach ($k356 in $V356_KEEP) {
    $doc356 = Doc (Join-Path $dir $k356.File)

    # (a) EXACTLY one comboBox on the field, in the file its tab imports. Zero is the dropdown
    # having become something else; two is an undeclared mirror on a field that is a single
    # setting, which is a second place to change one answer from.
    $cb356 = @($doc356.SelectNodes("//comboBox[@field='$($k356.Field)']"))
    if ($cb356.Count -eq 0) { $v356Bad += "(a) the $($k356.Where) tab has no comboBox on '$($k356.Field)' in $($k356.File) - the dropdown the user asked on 2026-08-30 to KEEP has become something else, and the button of I107a1 was never called for here (SPEC C 144th, I113e)"; continue }
    if ($cb356.Count -gt 1) { $v356Bad += "(a) the $($k356.Where) tab carries $($cb356.Count) comboBox on '$($k356.Field)' in $($k356.File) - one setting with two widgets is an undeclared mirror (SPEC V1, V36)"; continue }
    $v356Seen++

    # (b) what makes it a SETTING and not prose, and it is the REASON the leg names it rather
    # than the name itself: the pair is AUTHORED in the XML and nothing here reads PICKER_LIST.
    # Losing the authored items is the combo turning into a picker by the back door.
    foreach ($at356 in @('items', 'values')) {
        if ([string]::IsNullOrEmpty($cb356[0].GetAttribute($at356))) { $v356Bad += "(b) the '$($k356.Field)' combo of the $($k356.Where) tab authors no $at356 - the authored pair is what separates a VALUE combo from a prose picker, and prose is the only thing I113e converts (SPEC C 144th, V15, I110d)" }
    }
}

# (c) the zero-guard, and it is written to catch the SILENT shortfall: a leg that skipped its
# file and reported nothing is a leg passing by vacancy (B7), which is worse than one that
# fails loudly.
if ($v356Seen -lt $V356_KEEP.Count -and $v356Bad.Count -eq 0) { $v356Bad += "(c) only $v356Seen of the $($V356_KEEP.Count) combos were read and nothing above complained - the roster lost an entry without a word (SPEC V20, B7)" }
if ($v356Seen -eq 0) { $v356Bad += "(c) not ONE of the $($V356_KEEP.Count) combos was read - the roster is empty or the two tab files are gone, and every leg above measured nothing (SPEC V209, V20, B7)" }

if ($v356Bad) { foreach ($b in $v356Bad) { Fail "V356 $b" } }
else { Pass "V356 all $v356Seen combos of VALUE stay dropdowns - $((@($V356_KEEP | ForEach-Object { $_.Field })) -join ', ') - one comboBox each on the file that draws it, every one still authoring its own items and values" }


# ---- V357: `self` on an inline handler is the FORM, so nothing writes a widget through it --
# SPEC V357, B96, R134, T818/T819, user 2026-08-30. mfOpen kept the button that opened the box
# in MF.btn and mfConfirm painted the row's label through it - but the `self` an inline <event>
# hands a function is the FORM (the generated `local self = obj;`, SPEC R134), so the write
# landed on the whole TAB: choosing the blank dimmed every widget on it to 0.60 and turned it
# italic. The other branch writes opacity 1, which is the half that UNDOES it - and that is why
# only EMPTYING a row ever showed the defect.
#
# The label was never at risk: the two dataLink of I107a2 repaint it from the field and find
# their control by NAME, so the direct write was a SECOND writer (V135) and the whole of the
# cure is that it goes away. mfLabel itself is untouched - its two states and its two numbers
# are V355(b)'s and stay whole.
$rootNC357  = NoComments $rootTxt
$labelFn357 = LuaFn $rootTxt 'mfLabel'
$v357Bad = @()

# The zero-guard runs FIRST and on purpose: with mfLabel gone, or called from nowhere, leg (a)
# reads an empty set and passes by VACANCY, which is the whole of B7 and why V20 asks for a
# mutation before a check is believed.
$calls357 = @([regex]::Matches($rootNC357, '(?<!function )mfLabel\(\s*([^,\)]+)\s*,'))
if (-not $labelFn357) { $v357Bad += "(zero) mfLabel was not found on the root form - the label painter is gone and every leg below would measure nothing (SPEC V209, V20, B7)" }
if ($calls357.Count -eq 0) { $v357Bad += "(zero) mfLabel is called nowhere on the root form - no row would ever be repainted and leg (a) would pass over an empty set (SPEC V209, V20, B7)" }

# (a) every call site hands mfLabel a control found by NAME. MF.btn, from and self are the
# three ways to hand it the FORM instead, and the first argument is the only place that
# mistake can enter (SPEC V143, R134).
foreach ($c357 in $calls357) {
    $arg357 = $c357.Groups[1].Value.Trim()
    if ($arg357 -match '^(MF\.btn|from|self)$') { $v357Bad += "(a) mfLabel is called with '$arg357' as its control - an inline handler's self is the FORM, so this paints the whole tab instead of the row (SPEC R134, B96)" }
    elseif ($arg357 -notmatch '^found\[') { $v357Bad += "(a) mfLabel is called with '$arg357' as its control - every call site has to pass a control found by NAME, and that is found[...] (SPEC V143, V357a)" }
}

# (b) the door stays SHUT, and shutting it means the VALUE is not kept either: a table field
# nothing reads is a field that lies about what it is to whoever opens the file next.
if ($rootNC357 -match 'MF\.btn') { $v357Bad += "(b) MF.btn is written or read again on the root form - the button that opened the box is not the control an inline handler receives (SPEC B96, V357b)" }
$mfTbl357 = [regex]::Match($rootNC357, '(?s)local MF = \{.*?\};')
if (-not $mfTbl357.Success) { $v357Bad += "(b) the MF table was not found on the root form (SPEC V209, I107f)" }
elseif ($mfTbl357.Value -match '(?m)\bbtn\s*=') { $v357Bad += "(b) the MF table still declares btn - the slot the removed writer filled is kept with no reader, which is a field that lies (SPEC V357b, V135)" }

# (c) the GENERAL rule B96 is one case of, across EVERY file: what the XML hands a handler is
# an ANCHOR to search from (tabRootOf), never a target to write a property on.
foreach ($f357 in $files) {
    $code357 = CodeOf $f357.FullName
    foreach ($who357 in @('from', 'self')) {
        $w357 = @([regex]::Matches($code357, "(?m)\b$who357\.[A-Za-z_][A-Za-z0-9_]*\s*=[^=]"))
        if ($w357.Count -gt 0) { $v357Bad += "(c) $($f357.Name) writes a property through '$who357' $($w357.Count) time(s) - what an inline handler receives is the FORM, so the write lands on the whole tab (SPEC R134, B96, V357c)" }
    }
}

if ($v357Bad) { foreach ($b in $v357Bad) { Fail "V357 $b" } }
else { Pass "V357 all $($calls357.Count) mfLabel call sites pass a control found by name, MF neither declares nor writes btn, and no .lfm writes a property through the self or from an inline handler receives" }

# ---- V358: the measure the ornament stamps is the RESOLVED one ----------------------------
# SPEC V358, B97, T821/T822, user 2026-08-30. The load-time measure is taken with the sheet
# body HIDDEN - applyTheme runs from the sheetTheme dataLink and the reveal is the LAST line of
# that same handler - so every align="client" box can be asked for a size the layout has not
# resolved. getWidth is _obj_getProp and answers the last value written rather than nil, so the
# nil guard in ornament() has nothing to catch, the memo stamps the wrong measure, and
# applyTheme has ONE call site: the crown stayed drawn at its smallest in the top-left corner
# of all 73 boxes for the rest of the session, with rdk -l at 0 and this gate green (B97).
#
# The cure measured here is MEASURE AGAIN, never REFUSE. A floor would leave a box plain, which
# is what V279 forbids in letter, and the number would be invented in a file that cannot read
# the XML the boxes are authored in.
$t358  = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20.6.lfm")))
$nc358 = NoComments $t358
$remFn358 = LuaFn $t358 'ornRemeasure'
$ornFn358 = LuaFn $t358 'ornament'
$v358Bad = @()

# The zero-guard runs FIRST: with the re-pass gone, or walking nothing, every leg under it
# reads an empty set and passes by VACANCY (SPEC V209, V20, B7).
$doc358   = Doc (Join-Path $dir "WoD20.6.lfm")
$ev358    = $doc358.SelectSingleNode("//dataLink[@field='sheetTheme']/event[@name='onChange']")
$evTxt358 = if ($ev358) { NoComments $ev358.InnerText } else { '' }
if (-not $remFn358) { $v358Bad += "(zero) ornRemeasure was not found in WoD20.6 - the boxes are measured once, with the sheet hidden, and never again (SPEC V209, V20, B7, B97)" }
elseif ($remFn358 -notmatch 'pairs\(ornPainted\)') { $v358Bad += "(zero) ornRemeasure does not walk ornPainted - it re-measures nothing and every leg below passes over an empty set (SPEC V358f, V20, B7)" }
if (-not $ornFn358) { $v358Bad += "(zero) ornament() was not found in WoD20.6 - there is no memo to re-measure (SPEC V209)" }
if (-not $ev358) { $v358Bad += "(zero) the sheetTheme dataLink onChange handler was not found - leg (a) would have no body to read the order from (SPEC V209, I6)" }

# (a) ONE re-pass, and it stands AFTER the reveal. Placed BEFORE it, the re-pass reads the very
# same unresolved size and is an expensive no-op - exit 0, gate green, and the screen still
# wrong, which is the whole shape of B97.
if ($ev358) {
    $calls358 = @([regex]::Matches($evTxt358, 'ornRemeasure\('))
    if ($calls358.Count -eq 0) { $v358Bad += "(a) the theme handler never reaches ornRemeasure - the load-time measure stays the last word and B97 is back (SPEC V358a)" }
    elseif ($calls358.Count -gt 1) { $v358Bad += "(a) the theme handler calls ornRemeasure $($calls358.Count) times - one re-pass, or two owners of when the boxes are measured (SPEC V358a, V135)" }
    else {
        $revealAt358 = $evTxt358.IndexOf('sheetReveal(')
        if ($revealAt358 -lt 0) { $v358Bad += "(a) the theme handler no longer reveals the sheet - the order leg (a) measures has one end missing (SPEC V166, I15)" }
        elseif ($calls358[0].Index -lt $revealAt358) { $v358Bad += "(a) ornRemeasure is called BEFORE the reveal - it re-reads the same size the layout has not resolved yet, which is an expensive no-op and leaves B97 exactly where it was (SPEC V358a, B97)" }
    }
    if ($evTxt358 -match 'setTimeout\(\s*function\(\)\s*ornRemeasure\(\);\s*end,\s*0\s*\)') { $v358Bad += "(a) the re-pass is scheduled at interval 0 - setTimeout is only asynchronous with a POSITIVE interval, so at 0 it runs in the same tick, ahead of the align pass (SPEC R44, V358a)" }
}

# (b) the re-pass does NOT repaint the sheet. A whole applyTheme after the reveal is what V167
# and B40 pay not to have: the player watches the sheet being painted.
if ($remFn358 -and $remFn358 -match 'applyTheme\s*\(') { $v358Bad += "(b) ornRemeasure calls applyTheme - a full repaint AFTER the reveal is the load paint the player was never meant to watch (SPEC V358b, V167, B40)" }

# (c) the re-pass hands `w` and `h` in as NIL and lets the control decide. This is the INVERSE
# of refreshOrnament, and the two coexist because the competitor differs: there a caller has
# just WRITTEN the height and reading it back can answer with the size the box is leaving
# (V285c, B62); here nothing was written and what is missing is the align pass.
if ($remFn358) {
    $inner358 = @([regex]::Matches($remFn358, 'ornament\(([^)]*)\)'))
    if ($inner358.Count -eq 0) { $v358Bad += "(c) ornRemeasure never calls ornament() - it walks the memo and re-measures nothing (SPEC V358c, V209)" }
    foreach ($i358 in $inner358) {
        $args358 = @($i358.Groups[1].Value -split ',')
        if ($args358.Count -ne 3) { $v358Bad += "(c) ornRemeasure calls ornament() with $($args358.Count) arguments - the re-pass passes the control, nil and the colour and NOTHING else, or the stale measure walks back in through the fourth (SPEC V358c, V285c)" }
        elseif ($args358[1].Trim() -ne 'nil') { $v358Bad += "(c) ornRemeasure hands ornament() '$($args358[1].Trim())' where the measure goes - it has to be nil so c.width decides, which is the whole cure (SPEC V358c, B97)" }
    }
    if ($remFn358 -match '\.width|\.height') { $v358Bad += "(c) ornRemeasure reads a width or a height of its own - the control is read INSIDE ornament(), and a second reader is a second answer to the one question (SPEC V358c, V67)" }
}

# (d) GLOBAL, and that is not style: B93 measured this chunk against a ceiling of 100 and a
# `local function` costs a slot exactly like a variable does. Declared BELOW ornament() because
# it calls it - the other order compiles to a GETGLOBAL and finds nil, silently (V223, B51).
if ($nc358 -match 'local\s+function\s+ornRemeasure') { $v358Bad += "(d) ornRemeasure is declared local - it costs the chunk a slot it does not have, and B93 measured that ceiling at 100 with 99 already spent (SPEC V358d, V325, V347)" }
$defAt358 = $nc358.IndexOf('function ornRemeasure')
$ornAt358 = $nc358.IndexOf('local function ornament(')
if ($defAt358 -ge 0 -and $ornAt358 -ge 0 -and $defAt358 -lt $ornAt358) { $v358Bad += "(d) ornRemeasure is declared ABOVE ornament() - reached from there the call compiles to a GETGLOBAL and answers nil, with nothing to grep (SPEC V358d, V223, B51)" }

# (e) the memo carries the CONTROL, so the re-pass measures again without a second walk of the
# tree. A second collect would be a second owner of "which rectangles are the boxes", which is
# the one thing sectionBox exists to be the only answer to (V316d, I89b).
if ($ornFn358) {
    $memo358 = [regex]::Match($ornFn358, 'e = \{[^}]*\}')
    if (-not $memo358.Success) { $v358Bad += "(e) the memo entry could not be read in ornament() - leg (e) measured nothing (SPEC V209)" }
    elseif ($memo358.Value -notmatch '\bc\s*=\s*c\b') { $v358Bad += "(e) the memo entry does not carry the box - ornRemeasure would have to walk the tree a second time to find what to measure, which is a second owner of the question sectionBox answers (SPEC V358e, V316d, I89b)" }
}
if ($remFn358 -and $remFn358 -notmatch '\be\.c\b') { $v358Bad += "(e) ornRemeasure never reads the box off the memo entry - the field is stamped and never used, which is a memo that lies about what it holds (SPEC V358e)" }

if ($v358Bad) { foreach ($b in $v358Bad) { Fail "V358 $b" } }
else { Pass "V358 ornRemeasure is global, declared below ornament, walks the memo for the box it stamped and re-measures each one AFTER the reveal on a positive timer, handing the measure in as nil so the control decides, and never repaints the sheet" }

# ---- V363: the version walks in every place that spells it --------------------------------
# SPEC V363, I120, T824/T825, user 2026-08-30. The label's own text IS the .lang key (V10), so
# a bump that touches one and not the other leaves the [pt] sheet showing the version that was,
# or the key orphaned and [pt] falling back to English - both silent, and both only visible to
# a player who reads the Settings tab in the other language.
#
# I120 said TWO places and MEASURED FOUR: the <label>, the Lua PT map, and wod.Version in the
# [pt] AND [en] halves of localization.lang. The spec undercounted; the check counts what is
# there.
$verLbl363 = @()
foreach ($f363 in $files) {
    $d363 = Doc $f363.FullName
    foreach ($n363 in $d363.SelectNodes("//label[@text]")) {
        if ($n363.GetAttribute('text') -cmatch '^Version: ') { $verLbl363 += [pscustomobject]@{ File = $f363.Name; Text = $n363.GetAttribute('text') } }
    }
}
$v363Bad = @()

# (c) the zero-guard runs FIRST: with no version label at all, (a) and (b) below read an empty
# set and pass by VACANCY, which is the whole of B7.
if ($verLbl363.Count -eq 0) { $v363Bad += "(c) no 'Version: ' label was found on any tab - the sheet stopped telling the player which build it is, and every leg below would measure nothing (SPEC I120, V209, V20, B7)" }

# (b) ONE label spells it. A second is a second owner of one number, and the day they disagree
# nothing says which is the build (V135, B70).
elseif ($verLbl363.Count -gt 1) { $v363Bad += "(b) $($verLbl363.Count) labels spell a version - $((@($verLbl363 | ForEach-Object { "$($_.File) '$($_.Text)'" })) -join ', ') - one number, one owner (SPEC V135, B70)" }

# (a) the CAMINHO DE VOLTA of a bumped version: the text is the key, so both halves of the
# .lang have to carry the NEW string. Missing in [pt] is the Portuguese sheet showing English;
# missing in [en] is a key the English half never declared.
else {
    $vt363 = $verLbl363[0].Text
    if (-not $ptK.Contains($vt363)) { $v363Bad += "(a) '$vt363' has no [pt] entry in localization.lang - the label text IS the key (V10), so a bumped version leaves the Portuguese sheet on the string that was (SPEC V363a, I120)" }
    if (-not $enK.Contains($vt363)) { $v363Bad += "(a) '$vt363' has no [en] entry in localization.lang - the key the label asks for was never declared on the English side (SPEC V363a, V10)" }
}

# NOT MEASURED, and deliberately: <version> in module.xml belongs to the rdk, which rewrites it
# at prepare (SPEC C, B2). Tying the two would hand the toolchain the power to turn this check
# red on a build nobody asked to change.
if ($v363Bad) { foreach ($b in $v363Bad) { Fail "V363 $b" } }
else { Pass "V363 one label spells the version - '$($verLbl363[0].Text)' in $($verLbl363[0].File) - and both halves of localization.lang carry it as a key" }

# ---- V360: the description pane says what it has and what it has NOT ----------------------
# SPEC V360, I116, B98, T827/T828, user 2026-08-30 (pedido 3). The right half of the search box
# was empty space with a text editor floating in it: no rule dividing it from the list, no
# heading naming it, and nothing said when a list simply has no text to show.
#
# V359 - the box in two sizes - is NOT written here. Specialties is still a comboBox, so the
# narrow variant would have no caller, and a mechanism with no caller is the dead flexibility
# this repo keeps deleting. It arrives with T826, after T836 converts that row.
$t360   = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20.2.lfm")))
$doc360 = Doc (Join-Path $dir "WoD20th.lfm")
# mfDesc since T857 - see the note on $sel352 above. One writer, three callers (V135).
$sel360 = LuaFn $rootTxt 'mfDesc'
$dsc360 = LuaFn $rootTxt 'descText'
$v360Bad = @()

$rule360  = $doc360.SelectSingleNode("//layout[@name='mfSearch']/rectangle[@name='mfDescRule']")
$title360 = $doc360.SelectSingleNode("//layout[@name='mfSearch']/label[@name='lblMfDescTitle']")
$pane360  = $doc360.SelectSingleNode("//layout[@name='mfSearch']/textEditor[@name='edtMfDesc']")

# (f) the zero-guard runs FIRST - with the heading or descText gone, the legs below read an
# empty set and pass by VACANCY, which is the whole of B7.
if (-not $title360) { $v360Bad += "(f) lblMfDescTitle was not found inside mfSearch - the pane lost the heading that names it and every leg below would measure nothing (SPEC I116a, V209, V20, B7)" }
if (-not $pane360)  { $v360Bad += "(f) edtMfDesc was not found inside mfSearch - there is no pane to name (SPEC I112g, V352a, V209)" }
if (-not $dsc360)   { $v360Bad += "(f) descText was not found on the root form - nothing resolves a description key (SPEC V344, V209)" }
if (-not $sel360)   { $v360Bad += "(f) mfSelect was not found - nothing fills the pane (SPEC I112d, V209)" }

# (a) the heading is a <label> like every other visible string, so V10 reaches it and BOTH
# halves of the .lang carry it. Spelling it in the XML with no [pt] pair is the half-translated
# sheet V10 refuses, and it would read "Description" to a player on the Portuguese sheet.
if ($title360) {
    $tx360 = $title360.GetAttribute('text')
    if ([string]::IsNullOrEmpty($tx360)) { $v360Bad += "(a) the description heading carries no text - a heading that names nothing is not a heading (SPEC I116a)" }
    else {
        if (-not $ptK.Contains($tx360)) { $v360Bad += "(a) the heading '$tx360' has no [pt] entry in localization.lang - the Portuguese sheet would read it in English (SPEC V9, V10, I116a)" }
        if (-not $enK.Contains($tx360)) { $v360Bad += "(a) the heading '$tx360' has no [en] entry in localization.lang (SPEC V10)" }
    }
}

# (b) the DIVIDER is a sibling of the pane, never a border on it: a border decorates ONE
# control, a rule SEPARATES two, and the user asked for a division down the middle.
if (-not $rule360) { $v360Bad += "(b) mfDescRule was not found inside mfSearch - the two halves of the box run together with nothing between them (SPEC I116a, V360b)" }
else {
    $rw360 = [int]$rule360.GetAttribute('width')
    if ($rw360 -gt 4) { $v360Bad += "(b) mfDescRule is $rw360 wide - a rule divides, a panel fills; past four this is a second box inside the box (SPEC I116a)" }
    if ($title360 -and $rule360.GetAttribute('left') -ge $title360.GetAttribute('left')) { $v360Bad += "(b) mfDescRule stands at $($rule360.GetAttribute('left')) and the heading at $($title360.GetAttribute('left')) - the rule divides the list from the pane, so it comes BEFORE the pane's own column (SPEC I116a)" }
    # V53 owns which colours a palette can repaint. A rule the theme cannot reach keeps its
    # Modern colour in all four eras, which is the defect V53 exists for - named here because
    # this is the control that made it happen (T827, first build red).
    $rs360 = $rule360.GetAttribute('strokeColor')
    if ($rs360 -and $rs360 -notin @('white', '#FFFFFF', 'black', '#00000000')) { $v360Bad += "(b) mfDescRule strokes '$rs360', which no palette's stroke map carries - the rule would keep one colour through all four eras (SPEC V53, V67)" }
}

# (c) a list with no module in disk SAYS so. A blank pane cannot tell "the book does not carry
# this" from "the sheet broke" - the lie I107m exists to stop, on a new target. MEASURED
# 2026-08-30: six of sixteen lists have a module, and `speciality` never will, by the user's
# call (R135, C 146th), so this is a permanent state and not a gap waiting to close.
if ($sel360) {
    $sb360 = NoComments $sel360
    if ($sb360 -notmatch 'No description available') { $v360Bad += "(c) mfDesc has no fallback for a list with no text - the pane goes blank and the player cannot tell an empty book from a broken sheet (SPEC V360c, I116c, R135)" }
    else {
        if ($sb360 -notmatch 'translateSheetText\(\s*"No description available"') { $v360Bad += "(c) the fallback is written without translateSheetText - it would read English on the Portuguese sheet, which is the half-translation V9 refuses (SPEC V360c, V9)" }
        if (-not $ptK.Contains('No description available')) { $v360Bad += "(c) 'No description available' has no [pt] entry in localization.lang (SPEC V10)" }
        if (-not $enK.Contains('No description available')) { $v360Bad += "(c) 'No description available' has no [en] entry in localization.lang (SPEC V10)" }
    }
}

# (d) the list -> module map is DERIVED from MF.mod, which is already a parameter of mfOpen
# (I113a). A roster here would need a row per wave of I113e, and the wave that forgets its row
# passes in silence - the ageing roster I114f forbids.
if ($sel360) {
    if ((NoComments $sel360) -match '(?m)\{[^}]*\bmerit\b[^}]*=\s*"[Dd]esc') { $v360Bad += "(d) mfSelect carries a list-to-module roster - MF.mod already answers this and a roster ages one row per wave (SPEC I116d, I114f, V208)" }
    # mfKind(name) IS MF.mod, resolved per NAME for the one list that reads two modules (SPEC V433c).
    if ((NoComments $sel360) -notmatch 'descFor\(\s*MF\.field,\s*(MF\.mod|mfKind\(name\))') { $v360Bad += "(d) mfDesc does not resolve the module from MF.mod - the parameter I113a added is being ignored and something else decides (SPEC I116d)" }
}

# (e) ONE function resolves the text and every reader goes through it. A second place building
# the key is B89 walking in through the door the pane opened.
if ($dsc360) {
    $callers360 = @([regex]::Matches((NoComments $rootTxt), 'descText\('))
    if ($callers360.Count -lt 2) { $v360Bad += "(e) descText has fewer than two call sites - the ? box and the pane BOTH read through it, so one of the two readers is building its own key (SPEC V344d, V135, I102g)" }
    if ((NoComments $rootTxt) -match '(?m)require\("desc"\s*\.\.\s*[A-Za-z_.]+\s*\.\.\s*"_"[^)]*\)\s*;[^\n]*\n[^\n]*\[\s*sub') { $v360Bad += "(e) a second place assembles a composite description key - descText owns that shape (SPEC V344d, B89)" }
}

if ($v360Bad) { foreach ($b in $v360Bad) { Fail "V360 $b" } }
else { Pass "V360 the pane is divided by a themable rule and named by a translated heading, mfSelect resolves its module from MF.mod through the one descText both readers share, and a list with no text in disk says so in the language in force" }

# ---- V364: a description module that is NOT there degrades to the SENTENCE ----------------
# SPEC V364, I116c, I107f, B99, T839, T840. `require` on a file that is not on disk RAISES - it
# does not answer nil - and the load runs inside mfOpen's setTimeout, so an unguarded raise
# takes the rest of the handler with it: the list is never built, mfFilter never runs, and the
# player watches the box open EMPTY. speciality is the PERMANENT case (Q33) and seven more
# lists are waiting on extraction (R135).
#
# Legs (f) and (g) were added in the 146th and they are not theory: T842 MEASURED them by
# reverting each fix and finding the gate GREEN. They are the two ways this invariant can be
# undone silently, and neither had anything watching it.
$v364Bad = @()
$open364 = LuaFn $rootTxt 'mfOpen'
$desc364 = LuaFn $rootTxt 'descText'

# (e) zero-guard FIRST: with the opener gone every leg below reads an empty string and passes.
if (-not $open364) { $v364Bad += "(e) mfOpen is gone - every leg below would read an empty function and pass by vacancy (SPEC V209, V20)" }
else {
    $body364 = NoComments $open364
    $pc364 = @([regex]::Matches($body364, 'pcall\('))

    # (e) exactly ONE pcall in the opener: zero is the guard gone, two is a second owner.
    if ($pc364.Count -eq 0) { $v364Bad += "(e) mfOpen carries no pcall - the description module is loaded unguarded and the first list without one opens the box EMPTY (SPEC V364a, B99)" }
    if ($pc364.Count -gt 1) { $v364Bad += "(e) mfOpen carries $($pc364.Count) pcall - a second guard is a second owner of the same rule (SPEC V364e, V135)" }

    # (a) the guard is on the DESCRIPTION module, and the failure branch goes on building.
    if ($body364 -notmatch 'pcall\(require,\s*"desc"') { $v364Bad += "(a) the pcall in mfOpen does not guard the description require - it is guarding something else, and the raise V364 exists to catch is still live (SPEC V364a)" }

    # (d) the guard covers ONLY the description module. meritData ships always, and a pcall
    # there would hide a broken build behind an empty picker - B19 through the door of silence.
    if ($body364 -match 'pcall\(\s*require\s*,\s*"meritData|pcall\(function[^)]*require\("meritData') { $v364Bad += "(d) the meritData require is guarded too - that file always ships, and a pcall there hides a broken build behind an empty picker (SPEC V364d, B19)" }

    # (f) NO fallback for mod. `mod or "Merit"` was left from when the box was merit-only, and
    # with the GENERIC box it makes a module-LESS wave show MERIT text for an archetype. A wrong
    # answer is worse than none: the sentence of V360c says "the book does not carry it", merit
    # text LIES, and the player has no way to tell.
    if ($body364 -match 'MF\.mod\s*=\s*mod\s+or\b') { $v364Bad += "(f) mfOpen falls back to a module when the caller names none - a wave with no module would show another list's description as if it were its own, which is a wrong answer where V360c promises no answer (SPEC V364f, B100)" }
    if ($body364 -notmatch 'MF\.mod\s*=\s*mod;') { $v364Bad += "(f) mfOpen does not hand mod straight to MF.mod - nil is the whole signal a module-less wave has, and anything in its way is the fallback under another name (SPEC V364f)" }

    # (g) the concatenation is an ARGUMENT, so it runs BEFORE pcall is called: with mod nil,
    # "desc" .. nil raises OUTSIDE the guard and kills the timer - B99 wearing a new coat, under
    # the very guard T839 installed.
    $req364 = [regex]::Match($body364, '(?m)^(.*)pcall\(require,\s*"desc"')
    if ($req364.Success -and $req364.Groups[1].Value -notmatch 'if\s+MF\.mod\s*~=\s*nil\s+then') { $v364Bad += "(g) the description require is not behind a nil test on MF.mod - the concatenation is an argument and runs BEFORE pcall, so a module-less wave raises OUTSIDE the guard and takes the rest of the timer with it (SPEC V364g, B99)" }
}

# (b) failure is not silence, and it is not a SECOND path either: descText answers "" for a nil
# kind and for a module that would not load, and V360c keys on exactly that.
if (-not $desc364) { $v364Bad += "(b) descText is gone - the pane has no reader left and the sentence of V360c is keyed to nothing (SPEC V209, V360e)" }
else {
    $db364 = NoComments $desc364
    if ($db364 -notmatch 'if kind == nil') { $v364Bad += "(b) descText does not answer for a nil kind - a module-less wave hands it nil and it would raise on the concatenation instead of falling to the sentence (SPEC V364b, V360c)" }
    if ($db364 -notmatch 'pcall\(require,\s*"desc"') { $v364Bad += "(b) descText loads its module unguarded - it is the READER, so the raise would land on the pane instead of on the opener and the box would still die (SPEC V364a)" }
}

# (c) there is NO roster of "which lists have a module" - the question is answered by the
# ATTEMPT. A roster here ages one line per wave of I113e and per round of T838 (I114f).
foreach ($ros364 in @('HAS_DESC', 'DESC_LISTS', 'MODULE_LIST', 'WITH_DESC')) {
    if ($rootTxt -match "\b$ros364\b") { $v364Bad += "(c) the root form declares '$ros364' - a roster of which lists carry a module ages one line per wave and per extraction round, and the attempt already answers it (SPEC V364c, I114f)" }
}

if ($v364Bad) { foreach ($b in $v364Bad) { Fail "V364 $b" } }
else { Pass "V364 mfOpen guards the description require with exactly one pcall, behind a nil test so the concatenation cannot raise outside it, hands mod on with no fallback, leaves meritData unguarded, and descText answers empty for a module that is not there so the pane falls to V360c's sentence - with no roster of which lists have one" }

# ---- V365: every picker has a ?, and the ? is the ONE place a description is read and written
# SPEC V365, I102h, I102i, T845, T846; amended three times in one day by B104, B105 and B106.
#
# The roster is CODE on BOTH sides, the shape V354i settled: asking the XML which templates open
# the box is what stops a converted family from passing by VACANCY. It reuses $v354Opens on
# purpose - two derivations of "which template opens a picker" is exactly the drift V135 refuses -
# and if that block ever moves, leg (g) below fires loudly instead of this one passing on an
# empty set.
$v365Bad = @()
$V365_EXEMPT = @('SpecialityRow')             # named exception, and it cuts BOTH ways (V365a, B105)
$V365_BRIGHT = @('edtPopDesc', 'edtMfDesc')   # the ? box and the search pane - the two that stay

# Which templates draw a ?, read off the same XML the opener list comes from.
$q365 = @()
foreach ($f365 in $files) {
    foreach ($t365 in @((Doc $f365.FullName).SelectNodes("//template[@name]"))) {
        if (@($t365.SelectNodes(".//button[@text='?']")).Count -gt 0) { $q365 += $t365.GetAttribute("name") }
    }
}
$q365 = @($q365 | Sort-Object -Unique)
$pop365 = LuaFn $rootTxt 'popOpen'
$sav365 = LuaFn $rootTxt 'savePopDesc'

# (g) zero-guard FIRST: every leg under this compares two SETS, and a comparison over an empty
# set is the check that measures nothing and says PASS (SPEC V365g, V20, B7, B92).
if (@($v354Opens).Count -eq 0) { $v365Bad += "(g) not one template that opens a picker was found - legs (a) and (d) would compare against an empty set (SPEC V365g, V209, V20)" }
if ($q365.Count -eq 0) { $v365Bad += "(g) not one template draws a ? - the sheet has no description door at all and leg (a) would pass by vacancy (SPEC V365g, V209, V20)" }
if (-not $pop365) { $v365Bad += "(g) popOpen is gone - legs (c) and (f) have nothing to read (SPEC V365g, V209)" }

# (a) the family in scope is the one ALREADY CONVERTED, and converting means calling mfOpen(
# (SPEC B106). A comboBox still waiting on its wave of I113e is NOT a violation - it is a family
# whose turn has not come - and charging it would make this leg red at birth against correct
# code, which is B92 from the other side: nineteen comboBox tags on the sheet and none with a ?.
#
# The scope AUTO-ENFORCES I102h rather than restating it: the moment a wave swaps comboBox for
# mfOpen(, that template enters here, and the round that does not bring the ? with it goes red.
foreach ($o365 in @($v354Opens)) {
    if ($V365_EXEMPT -contains $o365) {
        if ($q365 -contains $o365) { $v365Bad += "(a) '$o365' is the NAMED exception and draws a ? anyway - the exception cuts both ways, and speciality will never have a module for it to open (SPEC V365a, Q33, B105)" }
        continue
    }
    if ($q365 -notcontains $o365) { $v365Bad += "(a) template '$o365' opens a picker with mfOpen( and draws no ? - the family converted without its description door, which is what I102h forbids in the SAME round (SPEC V365a, I102h)" }
}
foreach ($k365 in $q365) {
    if (@($v354Opens) -notcontains $k365) { $v365Bad += "(a) template '$k365' draws a ? and opens no picker - an orphan ? opens the description of a row that chooses nothing (SPEC V365a)" }
}
foreach ($e365 in $V365_EXEMPT) {
    if (@($v354Opens) -notcontains $e365) { $v365Bad += "(a) '$e365' is on the named exception list and opens no picker - a stale exemption excuses a family that no longer exists, and that is how a real one slips through later (SPEC V365a, V20)" }
}

# (b) a TYPED row carries no ? (SPEC I102f). The exception is named by FORM - *Free / *FreeRow -
# and never by a list of names, because a list of names ages one line per row the sheet grows.
foreach ($k365 in $q365) {
    if ($k365 -match '(Free|FreeRow)$') { $v365Bad += "(b) template '$k365' is a typed row by its FORM and draws a ? - a typed row has no book entry to open (SPEC V365b, I102f)" }
}

# (c) a list with no module on disk falls to the SENTENCE, never to an empty box: suppressing it
# would teach the player the row has no description when what it has no is a FILE (SPEC V365c).
if ($pop365) {
    $pb365 = NoComments $pop365
    if ($pb365 -notmatch 'translateSheetText\("No description available"') { $v365Bad += "(c) popOpen does not fall to the No description available sentence - the six lists still waiting on T838 would open a BLANK box (SPEC V365c, V360c, V364b)" }
    if ($pb365 -notmatch 'descFor\(') { $v365Bad += "(c) popOpen does not read through descFor - the ? box would be a second reader carrying its own copy of the key rule (SPEC I112d, V135)" }
}

# (d) REWRITTEN by B105: no description textEditor outside the ? PER FAMILY ALREADY CONVERTED,
# not for the sheet at once. Each pane leaves in the round its own family gains the ?, which is
# what the user decided on 2026-08-30; charging all seven before that is a ruler ahead of the
# code. Measured per FILE: a file that holds a converted template and still holds its old pane
# is the second owner of one text that V135 refuses, and B98 cost a round finding one.
foreach ($f365 in $files) {
    $d365 = Doc $f365.FullName
    if (@($d365.SelectNodes("//template[@name]") | Where-Object { @($_.SelectNodes(".//button[contains(@onClick,'mfOpen(')]")).Count -gt 0 }).Count -eq 0) { continue }
    foreach ($te365 in @($d365.SelectNodes("//textEditor[@name]"))) {
        $nm365 = $te365.GetAttribute("name")
        if ($nm365 -notmatch '^edt.*Desc$') { continue }
        if ($V365_BRIGHT -contains $nm365) { continue }
        $v365Bad += "(d) $($f365.Name) holds a converted picker and still carries the description pane '$nm365' - two surfaces for one text is the second owner V135 refuses (SPEC V365d, B98)"
    }
}

# (e) the WINDOW goes, the DATA stays (SPEC I45, V362a). Statically that is one question: is the
# field still DERIVED? A literal <base>Desc_<n> written anywhere is a field somebody pinned by
# hand, and a pinned name goes on pointing at the old row after the row moves - which is how the
# text stops being "moved" and starts being lost.
if ($rootTxt -notmatch 'function descFieldOf\(') { $v365Bad += "(e) descFieldOf is gone - it is the ONE derivation of where a row keeps its text, and without it the fields are unreachable rather than moved (SPEC V365e, I45, V209)" }
$lit365 = @()
foreach ($f365 in $files) {
    foreach ($m365 in [regex]::Matches((NoComments ([System.IO.File]::ReadAllText($f365.FullName))), '"[a-z][A-Za-z]*Desc_\d+"')) { $lit365 += "$($f365.Name) $($m365.Value)" }
}
if ($lit365.Count -gt 0) { $v365Bad += "(e) $($lit365.Count) description field(s) written as a LITERAL ($($lit365 -join ', ')) - descFieldOf derives the name from the row, and a hand-pinned one survives the row moving while the data does not (SPEC V365e, I45)" }

# (f) the storyteller's WRITE survives the move, and the pane stays BRIGHT while it does.
# V162 pairs readOnly with opacity for a widget of VALUE (edtSpentXP, I8). edtPopDesc is a pane
# of TEXT on the DESC_BRIGHT roster, where opacity fades the text with the frame (V241) - so the
# pair is REFUSED here, and the comboBox left V162's scope on this same argument in 2026-08-22
# (I41). One write, so there are no two halves to disagree (SPEC V365f, V111(2), B104).
$edt365 = $null
foreach ($f365 in $files) {
    $n365 = (Doc $f365.FullName).SelectSingleNode("//textEditor[@name='edtPopDesc']")
    if ($null -ne $n365) { $edt365 = $n365 }
}
if ($null -eq $edt365) { $v365Bad += "(f) edtPopDesc is gone - the ? box has nothing to write into (SPEC V365f, V209)" }
else {
    if ($edt365.GetAttribute("readOnly") -ne 'true') { $v365Bad += "(f) edtPopDesc is not born readOnly - a player would type over the book text in the moment before popOpen runs (SPEC V365f, V333)" }
    if ($edt365.HasAttribute("opacity")) { $v365Bad += "(f) edtPopDesc carries opacity '$($edt365.GetAttribute('opacity'))' - it is on the DESC_BRIGHT roster and opacity fades the TEXT with the frame, so dimming it charges the player legibility of the thing he opened the box to read (SPEC V365f, V111(2), V241, B104)" }
    if ($edt365.GetAttribute("onChange") -notmatch 'savePopDesc') { $v365Bad += "(f) edtPopDesc has no savePopDesc on change - what the storyteller types would have no reader, the feature erased sideways I102i exists to stop (SPEC V365f)" }
}
if ($pop365) {
    $pc365 = NoComments $pop365
    if ($pc365 -notmatch 'readOnly = not isStoryteller\(\)') { $v365Bad += "(f) popOpen does not write readOnly = not isStoryteller() - a readOnly pinned true IS the storyteller's write erased sideways (SPEC V365f, I102i)" }
    if ($pc365 -match 'edtPopDesc"\]\.opacity') { $v365Bad += "(f) popOpen writes opacity on edtPopDesc - the V162 pair is for a widget of VALUE and this is a pane of TEXT (SPEC V365f, V111(2), V241, B104)" }
}
if (-not $sav365) { $v365Bad += "(f) savePopDesc is gone - the ? box would open editable for the storyteller and save nowhere (SPEC V365f, I102i)" }
else {
    $sb365 = NoComments $sav365
    if ($sb365 -notmatch 'descFieldOf\(') { $v365Bad += "(f) savePopDesc does not write through descFieldOf - a second path to the text is a second owner (SPEC V365f, V135, I45)" }
    if ($sb365 -notmatch 'isStoryteller\(\)') { $v365Bad += "(f) savePopDesc does not ask isStoryteller() again - the flag on the control is a look, and the rule is a rule (SPEC V266b, V80)" }
    if ($sb365 -notmatch 'descQuiet') { $v365Bad += "(f) savePopDesc does not check descQuiet - the renderer's own write comes back through the door the typing uses, so opening the ? would SAVE the book text onto that row in silence (SPEC V107, V249d, B39)" }
}

if ($v365Bad) { foreach ($b in $v365Bad) { Fail "V365 $b" } }
else { Pass "V365 $(@($v354Opens).Count) converted picker template(s) read from the code, $($q365.Count) carrying a ? and SpecialityRow named out of it, no ? on a typed row, popOpen falling to V360c's sentence through the one descText, no description pane left in a file whose family converted, every description field still derived by descFieldOf, and edtPopDesc born readOnly with no opacity on either side while savePopDesc writes through descFieldOf behind descQuiet" }

# ---- V366: descNature covers 46, and the 22 with no text are NAMED ------------------
# SPEC V366, T838, R135, I102e, V210, V24. Every other module rule charges the WHOLE list -
# V32 wants 50 in descNumina, V336e a key per background - because there the books cover the
# list. Here they do not: the picker offers 68 archetypes and the 25 PDFs on disk yield 46,
# with six of the missing appearing in no book at all. A completeness rule written with the
# PICKER total would be red from birth against a state the owner CHOSE, which is B105 for the
# fourth time and the one mistake this project has already paid for three times. So the
# exception is born INSIDE the rule, by name.
#
# What is NOT charged here is a COUNT. V366c blesses the number moving - a name that gains
# text leaves the list in the same round and the rule goes green again at 46+1 / 22-1 - so a
# literal 46 would fail on the very move the spec asks for. The partition is the law; the
# numbers below are measured and reported, never asserted.
$v366Bad = @()
$natEn = DescEntries (Join-Path $plugin "descNature_en.lua") 'NATURE_DESC' 'en'
$natPt = DescEntries (Join-Path $plugin "descNature_pt.lua") 'NATURE_DESC' 'pt'
$natPick = @()
if ($PICKER.ContainsKey('nature')) { $natPick = @($PICKER['nature'] | Where-Object { $_ -ne '' }) }

# (c) the archetypes the books on disk do not describe, written out by NAME. A count ('at
# least 46') would accept one archetype swapped for another in silence, and a generic skip
# kills the rule for every name at once, which is B7 again (SPEC V366c, V328d, V354i, V20).
#
# This is NOT the roster V364c forbids. That one ages a line per wave of I113e and per round
# of extraction, so it lies BETWEEN rounds; this one ages only when a BOOK arrives, and a book
# arriving is exactly the moment somebody must touch the rule - the same reason V365a names
# SpecialityRow and V354i names MeritPicked by hand.
#
# Six of these - Bureaucrat, Comedian, Grifter, Optimist, Pragmatist, Trendsetter - appear
# ZERO times across all 25 PDFs in the library, so what they wait on is a PURCHASE and not an
# extraction. The other sixteen wait on a round of /ck:research (SPEC R135).
$NATURE_NO_TEXT = @(
    'Bureaucrat', 'Comedian', 'Conformist', 'Critic', 'Crusader', 'Explorer', 'Follower',
    'Gambler', 'Grifter', 'Idealist', 'Leader', 'Mad Scientist', 'Mediator', 'Monster',
    'Optimist', 'Pragmatist', 'Prophet', 'Samaritan', 'Sensualist', 'Trendsetter',
    'Trickster', 'Tyrant'
)

# (d) zero-guard. Three artifacts stand behind this rule, and one it cannot find while passing
# anyway is B94 - the target slides out from under the check and the gate stays green.
if ($null -eq $natEn) { $v366Bad += "(d) descNature_en.lua is missing from the plugin root or its NATURE_DESC markers are gone - the ? on Nature and Demeanour would fall to V360c's sentence with the text sitting right there on disk (SPEC V366d, V209, I102e)" }
if ($null -eq $natPt) { $v366Bad += "(d) descNature_pt.lua is missing from the plugin root or its NATURE_DESC markers are gone - the box would answer in one language and stay mute in the other (SPEC V366d, V210, V209)" }
if ($natPick.Count -eq 0) { $v366Bad += "(d) PICKER_LIST[nature] read as zero items - the partition would compare against an empty set and pass having measured nothing (SPEC V366d, V209, V20)" }
if ($null -ne $natEn -and @($natEn).Count -eq 0) { $v366Bad += "(d) descNature_en.lua parsed to zero entries - the generated shape or the parser drifted (SPEC V366d, V209)" }
if ($null -ne $natPt -and @($natPt).Count -eq 0) { $v366Bad += "(d) descNature_pt.lua parsed to zero entries - the generated shape or the parser drifted (SPEC V366d, V209)" }

if ($null -ne $natEn -and $null -ne $natPt -and @($natEn).Count -gt 0 -and @($natPt).Count -gt 0 -and $natPick.Count -gt 0) {
    # ORDINAL throughout, the way DescMap is: PowerShell's -contains folds case, and an
    # archetype list that later grows a pair differing only in case would silently become one.
    $enSet = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
    $ptSet = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
    $pickSet = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
    $noTextSet = New-Object 'System.Collections.Generic.HashSet[string]' ([StringComparer]::Ordinal)
    foreach ($e in $natEn) { [void]$enSet.Add($e.Key) }
    foreach ($e in $natPt) { [void]$ptSet.Add($e.Key) }
    foreach ($p in $natPick) { [void]$pickSet.Add($p) }
    foreach ($n in $NATURE_NO_TEXT) { [void]$noTextSet.Add($n) }

    # (a) two halves of ONE module: the KEY set is the same on both sides, keyed in EN in both
    # files (V24). A key on one side only is the ? that answers in English and stays silent in
    # Portuguese - harder to notice than a box that is empty in both (SPEC V366a, V210).
    foreach ($k in $enSet) { if (-not $ptSet.Contains($k)) { $v366Bad += "(a) '$k' is in the [en] half and nowhere in the [pt] one - the ? would answer in one language and stay mute in the other (SPEC V366a, V210, V24)" } }
    foreach ($k in $ptSet) { if (-not $enSet.Contains($k)) { $v366Bad += "(a) '$k' is in the [pt] half and nowhere in the [en] one - the key is EN on both sides, so this is a translation with no original (SPEC V366a, V210, V24)" } }

    # (b) the PARTITION, and this is what the rule actually measures: the keys of descNature
    # plus the NAMED no-text list equal PICKER_LIST["nature"], disjoint and complete. All three
    # doors fail, because each one is a different way for the decision to rot.
    foreach ($k in $enSet) {
        if (-not $pickSet.Contains($k)) { $v366Bad += "(b) '$k' has book text and no picker offers it - dead text nobody can reach, or a spelling that drifted away from the list (SPEC V366b)" }
    }
    foreach ($n in $natPick) {
        $hasText = $enSet.Contains($n)
        $named = $noTextSet.Contains($n)
        if ($hasText -and $named) { $v366Bad += "(b) '$n' has book text AND stands on the no-text list - the exception lies about a text that exists, and it would go on excusing a real gap long after this one closed (SPEC V366b, V366c)" }
        if (-not $hasText -and -not $named) { $v366Bad += "(b) '$n' has no book text and is not on the named no-text list - neither text nor a declared gap, which is the silent hole the partition exists to close (SPEC V366b, T838, R135)" }
    }
    foreach ($n in $NATURE_NO_TEXT) {
        if (-not $pickSet.Contains($n)) { $v366Bad += "(b) '$n' is named as having no text and no picker offers it - a stale exemption excuses an archetype that no longer exists, and that is how a real one slips through later (SPEC V366b, V366c, V20)" }
    }
}

if ($v366Bad) { foreach ($b in $v366Bad) { Fail "V366 $b" } }
else { Pass "V366 descNature partitions PICKER_LIST[nature] exactly - $(@($natEn).Count) archetype(s) carry book text with the same keys in both halves, $($NATURE_NO_TEXT.Count) are NAMED as having none, together they are the $($natPick.Count) the picker offers, and no name sits on both sides or outside the list" }

# ---- V367: a floating overlay lives OUTSIDE every tab -------------------------------
# SPEC V367, I102a, I107c, B107. The ? box, the search box and the scrim were authored inside
# the Traits tab's scrollBox, and both halves of that were wrong in a way no static check was
# asking about. A tab layout is visible="false" whenever the player is somewhere else, and a
# child of an invisible parent is invisible whatever its own flag says - so the ? on Main set
# popDesc.visible = true and NOTHING appeared, the box waiting on a tab the player was not
# looking at. Being inside that tab's scrollBox was the second half: an overlay that scrolls
# with the grid it floats over.
#
# Neither symptom is a Lua bug, and that is why this check reads PARENTAGE and nothing else.
# popOpen and mfOpen already walk from tabRootOf(from) - the top of the whole form - so they
# found the controls the whole time (SPEC B9). What was broken is WHERE the controls hang.
$v367Bad = @()
$OVERLAYS_367 = @('popScrim', 'popDesc', 'mfSearch')
$rootDoc367 = Doc (Join-Path $dir "WoD20th.lfm")
$body367 = $rootDoc367.SelectSingleNode("//layout[@name='sheetBody']")

# (d) zero-guard first: a check that cannot find the body it measures against and passes anyway
# is B94 - the target slides out from under it and the gate stays green (SPEC V367d, V209, V20).
if ($null -eq $body367) { $v367Bad += "(d) the root form has no sheetBody layout - there is no shared parent to measure the overlays against (SPEC V367d, I32, V209)" }
else {
    # (a) DIRECT children of the body every tab shares. Not a grandchild through some wrapper:
    # a wrapper is a second thing that can be hidden, and hiding it hides the box again.
    foreach ($nm367 in $OVERLAYS_367) {
        $direct367 = @($body367.SelectNodes("*[@name='$nm367']"))
        if ($direct367.Count -ne 1) {
            $v367Bad += "(a) '$nm367' is a direct child of sheetBody $($direct367.Count) time(s), expected 1 - the overlay has to hang off the one parent every tab shares, or it is invisible from the tabs that are not its own (SPEC V367a, I102a, B107)"
        }
    }

    # (b) file order IS paint order (the sentence V320 and V333b already measure). Authored AFTER
    # every tab layout means painted OVER whichever tab is showing; authored before, the tab
    # covers the box and the player clicks the grid through it.
    $kids367 = @($body367.SelectNodes("*"))
    $lastTab367 = -1
    $firstOv367 = -1
    for ($i367 = 0; $i367 -lt $kids367.Count; $i367++) {
        $kn367 = $kids367[$i367].GetAttribute("name")
        # .LocalName and NOT .Name: PowerShell's XML adapter exposes the `name` ATTRIBUTE as
        # .Name and shadows the element's own, so `.Name -eq 'layout'` was false for every
        # child and leg (b) reported an empty tab list against a body full of tabs.
        if ($kn367 -like 'tab*' -and $kids367[$i367].LocalName -eq 'layout') { $lastTab367 = $i367 }
        if ($OVERLAYS_367 -contains $kn367 -and $firstOv367 -lt 0) { $firstOv367 = $i367 }
    }
    if ($lastTab367 -lt 0) { $v367Bad += "(b) sheetBody carries no tab layout - leg (b) has nothing to be after and would pass by vacancy (SPEC V367b, V209, V20)" }
    elseif ($firstOv367 -lt 0) { $v367Bad += "(b) not one overlay was found among sheetBody's children - leg (b) measured nothing (SPEC V367b, V209, V20)" }
    elseif ($firstOv367 -lt $lastTab367) { $v367Bad += "(b) an overlay is authored BEFORE the last tab layout - file order is paint order, so the tab would be painted over the box and every click inside it would land on the grid (SPEC V367b, V320, V333b)" }
}

# (c) and in NO file is any of the three a descendant of a tab layout or of a scrollBox. This is
# the leg that fails if somebody moves them back: (a) alone would go quiet if a second copy were
# authored inside a tab, and a scrollBox parent scrolls the overlay with the grid it floats over.
foreach ($f367 in $files) {
    $d367 = Doc $f367.FullName
    foreach ($nm367 in $OVERLAYS_367) {
        foreach ($node367 in $d367.SelectNodes("//*[@name='$nm367']")) {
            if ($node367.SelectSingleNode("ancestor::scrollBox")) {
                $v367Bad += "(c) $($f367.Name): '$nm367' sits inside a scrollBox - the overlay would scroll along with the grid it is drawn over (SPEC V367c, I102a)"
            }
            if ($node367.SelectSingleNode("ancestor::layout[starts-with(@name,'tab')]")) {
                $v367Bad += "(c) $($f367.Name): '$nm367' sits inside a tab layout - a tab is visible=false from every other tab, so the box would open where nobody is looking (SPEC V367c, B107)"
            }
        }
    }
}

if ($v367Bad) { foreach ($b in $v367Bad) { Fail "V367 $b" } }
else { Pass "V367 all $($OVERLAYS_367.Count) floating overlays hang directly off sheetBody, authored after every tab layout, and none of them sits inside a tab or a scrollBox in any file" }

# ---- V368: fetching a desc entry OBLIGES unwrapping its language ---------------------
# SPEC V368, B109, I21, I102e, V360c. Every desc module has been {name = {<lang> = text}}
# since I21 - the file is per language AND the entry carries the language key again - so
# DESC[key] is a TABLE and never the text. The four panes in WoD20.7/.12/.13/.14 have always
# known that and read through pick(tbl, lang) = `tbl[lang] or tbl.en`. descText did not: it
# returned DESC[key] straight, and a table is neither nil nor "" - so it walked past popOpen's
# own "no text" fallback untouched and landed in edtPopDesc.text. The ? box opened EMPTY for
# EVERY list, not just for nature, and could not even say `No description available`.
#
# That is the shape this check is really about: a WRONG answer wearing the clothes of a right
# one. V360c/V364 already prove the sentence appears when a module is ABSENT; nothing proved
# the text appears when the module is THERE, because no static check ever compared what the
# modules store against what the readers take out.
$v368Bad = @()
$descMods368 = @(Get-ChildItem -LiteralPath $plugin -Filter 'desc*_??.lua' | Sort-Object Name)

# (a) module side, and it is the leg that makes unwrapping compulsory rather than tidy: EVERY
# entry of EVERY module is a table carrying its own file's language key. One bare string entry
# and a reader that skips the unwrap would look correct on that one name (SPEC V368a, I21).
if ($descMods368.Count -eq 0) { $v368Bad += "(d) no desc*_<lang>.lua module was found in the plugin root - leg (a) would pass over an empty set and the ? box has nothing to read (SPEC V368d, V209, V20)" }
$entries368 = 0
foreach ($m368 in $descMods368) {
    $lang368 = if ($m368.Name -match '_(\w\w)\.lua$') { $matches[1] } else { '' }
    if ($lang368 -eq '') { $v368Bad += "(a) $($m368.Name) does not end in _<lang>.lua - the loader builds the name by concatenation and would never reach it (SPEC V210b)"; continue }
    $txt368 = ([System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($m368.FullName))) -replace "`r`n", "`n"
    $open368 = @([regex]::Matches($txt368, '(?m)^\s*\["[^"]+"\]\s*=\s*'))
    $wrapped368 = @([regex]::Matches($txt368, '(?ms)^\s*\["[^"]+"\]\s*=\s*\{\s*' + $lang368 + '\s*=\s*\[==\['))
    $entries368 += $open368.Count
    if ($open368.Count -eq 0) { $v368Bad += "(a) $($m368.Name) parsed to zero entries - the generated shape or this reader drifted (SPEC V368d, V209)" }
    elseif ($open368.Count -ne $wrapped368.Count) {
        $v368Bad += "(a) $($m368.Name) has $($open368.Count) entries but $($wrapped368.Count) wrapped as { $lang368 = [==[...]==] } - a bare entry makes the unwrap look optional, and a reader that skips it would be right on that one name and wrong on every other (SPEC V368a, I21)"
    }
}

# (b) reader side. Any .lfm that INDEXES a desc table has to unwrap by language in the same
# file. Phrased on the fetch and not on a roster of reader names: a roster ages one line per
# pane and this ages never - whoever reaches into DESC[...] owes the unwrap (SPEC V368b, I114f).
$readers368 = 0
foreach ($f368 in $files) {
    $raw368 = NoComments ([System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f368.FullName)))
    if ($raw368 -notmatch 'DESC\s*\[') { continue }
    $readers368++
    if ($raw368 -notmatch '\[lang\]|\[half\]|\.en\b|pick\(') {
        $v368Bad += "(b) $($f368.Name) reads DESC[...] and never unwraps the language - the entry is a table, so what comes back is neither the text nor nil, and it would be handed to a text control as-is: an EMPTY pane that cannot even fall to V360c's sentence (SPEC V368b, B109, V360c)"
    }
}

# (c) the one reader that serves BOTH the ? box and the search pane, named because it is the
# one that was wrong and the one whose silence costs the most: descText hands its caller a
# STRING. Leg (b) is file-wide and WoD20th.lfm is 5600 lines - an unwrap anywhere in it would
# satisfy (b) while descText went on returning the table (SPEC V368c, V20).
$dt368 = [regex]::Match($rootTxt, '(?s)function descText\(kind, key, sub, lang\)(.*?)\n\t{3}end;')
if (-not $dt368.Success) { $v368Bad += "(d) descText was not found on the root form - the ? box and the search pane both read through it and legs (b)/(c) would measure nothing (SPEC V368d, V209, V20)" }
else {
    $body368 = NoComments $dt368.Groups[1].Value
    if ($body368 -notmatch '\[lang\]') { $v368Bad += "(c) descText never indexes the entry by [lang] - it would return the language TABLE instead of the text, which is neither nil nor empty and so defeats every 'no text' fallback downstream (SPEC V368c, B109)" }
    if ($body368 -match 'txt\s*=\s*DESC\[') { $v368Bad += "(c) descText assigns DESC[...] straight into its return value - that is the table, not the text, and it is the exact line B109 cost a round of screen testing (SPEC V368c, B109)" }
}

if ($v368Bad) { foreach ($b in $v368Bad) { Fail "V368 $b" } }
else { Pass "V368 all $($entries368) entries across $($descMods368.Count) desc module(s) are wrapped in their own file's language key, each of the $readers368 file(s) that reaches into DESC[...] unwraps it, and descText returns the text and not the table" }

# ---- V369: the way out of the row does not depend on the page ------------------------
# SPEC V369, I121, I107k, T856. The blank first entry of V15 used to ride inside MF.pool: it
# sorted with the names, paged with them, and simply did not exist past page 1 - on the very
# row whose only way out it is. It also ate one of the twenty slots I107k promises. It is a
# control of its own now, painted outside the row loop and gated on MF.canRemove.
$v369Bad = @()
$rmDoc369 = Doc (Join-Path $dir "WoD20th.lfm")
$rm369 = $rmDoc369.SelectSingleNode("//layout[@name='mfSearch']/button[@name='btnMfRemove']")
$rows369 = @($rmDoc369.SelectNodes("//layout[@name='mfSearch']/button[starts-with(@name,'dynMfRow_')]"))

# (f) zero-guard first: a leg with nothing to measure that passes anyway is B94.
if ($null -eq $rm369) { $v369Bad += "(f) btnMfRemove is not authored inside mfSearch - the only way to empty a converted row is gone, or it went back to being a paged result (SPEC V369f, I121a, V209)" }
if ($rows369.Count -eq 0) { $v369Bad += "(f) not one dynMfRow_ was found - legs (a) and (c) would compare against an empty list (SPEC V369f, V209, V20)" }

if ($null -ne $rm369 -and $rows369.Count -gt 0) {
    # (a)+(b) it is NOT one of the paged results, and the page still offers the 20 of I107k.
    # Nineteen would be the blank quietly eating a slot again.
    if ($rows369.Count -ne 20) { $v369Bad += "(b) mfSearch carries $($rows369.Count) result row(s), expected the 20 I107k declares - the remove line is a control of its own and must not cost a slot (SPEC V369b, I107k, I121b)" }
    if ($rm369.GetAttribute("name") -like 'dynMfRow_*') { $v369Bad += "(a) the remove line is authored as a dynMfRow_ - then it is a paged result again and pages away from the row it empties (SPEC V369a, I121a)" }

    # (c) ABOVE the first result, and separated: the gap is measured against the row PITCH, so
    # it stays honest if the rows are ever re-spaced. Flush against the list it reads as one
    # more option, and emptying a row by accident is the mistake with no undo.
    $top369 = 0; $r1 = 0; $r2 = 0
    $okNums = [int]::TryParse($rm369.GetAttribute("top"), [ref]$top369)
    $byName369 = @{}
    foreach ($r in $rows369) { $byName369[$r.GetAttribute("name")] = $r }
    if ($byName369.ContainsKey('dynMfRow_1') -and $byName369.ContainsKey('dynMfRow_2') -and $okNums) {
        [void][int]::TryParse($byName369['dynMfRow_1'].GetAttribute("top"), [ref]$r1)
        [void][int]::TryParse($byName369['dynMfRow_2'].GetAttribute("top"), [ref]$r2)
        $pitch369 = $r2 - $r1
        if ($top369 -ge $r1) { $v369Bad += "(c) btnMfRemove sits at top=$top369 and the first result at top=$r1 - the way out has to be ABOVE the list, not inside it (SPEC V369c, I121c)" }
        elseif ($pitch369 -le 0) { $v369Bad += "(c) the result rows have no positive pitch ($r1 then $r2) - the separation in (c) is measured against it and would mean nothing (SPEC V369c, V20)" }
        elseif (($r1 - $top369) -le $pitch369) { $v369Bad += "(c) btnMfRemove is $($r1 - $top369) above the first result against a row pitch of $pitch369 - flush against the list it reads as one more option, and emptying a row by accident is the mistake with no undo (SPEC V369c, I121c)" }
    }
    else { $v369Bad += "(c) dynMfRow_1 and dynMfRow_2 were not both found - the pitch (c) measures against cannot be derived (SPEC V369f, V209)" }
}

# (d)+(e) the offer is gated on what the row HOLDS, and the label is the one mfShown already
# builds - no second string and no second owner of the words (V135, V10).
# mfPaint and NOT mfFilter: mfFilter works out the hits, mfPaint puts them on screen. The
# remove line is PAINTED, so this is the body that has to carry it - reading mfFilter here
# would report three failures against correct code (measured while building T856).
$filt369 = [regex]::Match($rootTxt, '(?s)function mfPaint\(from\)(.*?)\n\t{3}end;')
if (-not $filt369.Success) { $v369Bad += "(f) mfPaint was not found - nothing paints the remove line (SPEC V369f, V209)" }
else {
    $fb369 = NoComments $filt369.Groups[1].Value
    if ($fb369 -notmatch 'btnMfRemove') { $v369Bad += "(d) mfPaint never paints btnMfRemove - it would keep whatever state the last row left it in (SPEC V369d, I121a)" }
    if ($fb369 -notmatch 'MF\.canRemove') { $v369Bad += "(d) mfPaint does not gate btnMfRemove on MF.canRemove - an empty row would be offered a Remove that removes nothing (SPEC V369d, V355d)" }
    if ($fb369 -notmatch 'mfShown\(""') { $v369Bad += "(e) mfPaint does not take the label from mfShown(`"`") - a literal here is a second owner of the words in two languages (SPEC V369e, V135, V10)" }
}

# (a) again, from the Lua side: the remove line has its own marker and does NOT go through
# mfSelect, which reads MF.hits by page index - an index this line has never had.
$rmFn369 = [regex]::Match($rootTxt, '(?s)function mfRemove\(from\)(.*?)\n\t{3}end;')
if (-not $rmFn369.Success) { $v369Bad += "(a) mfRemove was not found - the remove line would have to borrow mfSelect, which indexes MF.hits by page and has no entry for it (SPEC V369a, I121d, V209)" }
else {
    $rb369 = NoComments $rmFn369.Groups[1].Value
        # (a) mfRemove MARKS what removal means, and for every field but one that mark is the blank.
    # `road` is the named exception since T876: it has no empty state at all, so removing it
    # RETURNS it to the default rather than clearing it (SPEC V328c, I135d). The exception is
    # matched BY NAME and the general case is still required beside it - a mfRemove that stopped
    # writing "" for everything else would slip through a test that only looked for the branch.
    if ($rb369 -notmatch 'MF\.picked = ') { $v369Bad += "(a) mfRemove does not mark anything - mfConfirm writes MF.picked, so without the mark OK would write whatever was marked before (SPEC V369a, I121d)" }
    elseif ($rb369 -notmatch '""') { $v369Bad += "(a) mfRemove never marks the blank - `"`" is what mfConfirm reads as 'clear the row', and it is what removal means for every field but the road (SPEC V369a, I121d)" }
    else {
        # REQUIRED, not merely validated when present. Written the other way it passed a mfRemove
        # that had dropped the branch entirely, which is the road being CLEARED to a value its own
        # list does not offer - the exact state V328(c) and the user's rule both refuse, with
        # nothing on screen to show for it (MEASURED by mutation 2026-09-02).
        $rmRoad369 = [regex]::Match($rb369, 'MF\.picked = \(MF\.field == "([^"]+)"\) and ([^\s]+|"[^"]*") or ""')
        if (-not $rmRoad369.Success) {
            $v369Bad += "(a) mfRemove has no road branch - `road` is the one field with no empty state, so removing it has to RETURN it to the default and not clear it to a value the list never offers (SPEC V328c, I135d, V15)"
        }
        else {
            if ($rmRoad369.Groups[1].Value -ne 'road') { $v369Bad += "(a) mfRemove gives '$($rmRoad369.Groups[1].Value)' its own meaning of removal - only the road has one, because only the road has no empty state (SPEC V328c, V15)" }
            if ($rmRoad369.Groups[2].Value -ne 'ROAD_DEFAULT') { $v369Bad += "(a) mfRemove returns the road to $($rmRoad369.Groups[2].Value) and not ROAD_DEFAULT - the migration of T906 and this line are ONE rule and read the same constant (SPEC I135d, B70)" }
        }
    }
    if ($rb369 -match 'MF\.hits') { $v369Bad += "(a) mfRemove reads MF.hits - the remove line was never in the pool, so any index into the hits is a name that belongs to some other row (SPEC V369a, I121b)" }
}

if ($v369Bad) { foreach ($b in $v369Bad) { Fail "V369 $b" } }
else { Pass "V369 the remove line is a control of its own above the $($rows369.Count) paged results, separated by more than their pitch, gated on MF.canRemove, labelled from mfShown and marked without touching MF.hits" }

# ---- V372: one writer for the search pane, and the box opens showing the row ---------
# SPEC V372, I122a, I112d, V135, T857. The painting used to live inside mfSelect, so a row that
# already HELD something opened with an empty pane - the player could not re-read his own pick
# without picking something else first. mfDesc pulled it out; three callers share it now.
$v372Bad = @()
$descFn372 = LuaFn $rootTxt 'mfDesc'
$openFn372 = LuaFn $rootTxt 'mfOpen'

# (c) zero-guard first - a leg with nothing to read that passes anyway is B94.
if (-not $descFn372) { $v372Bad += "(c) mfDesc was not found - the one writer of the pane is gone and legs (a) and (b) would measure nothing (SPEC V372c, V209, V20)" }
if (-not $openFn372) { $v372Bad += "(c) mfOpen was not found - nothing opens the box for leg (b) to check (SPEC V372c, V209)" }

# (a) ONE writer. Measured across every .lfm, because a second writer in a tab file is exactly
# as much a second owner as one on the root form (V135).
if ($descFn372) {
    $w372 = @()
    foreach ($f372 in $files) {
        $c372 = NoComments ([System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f372.FullName)))
        # A write of "" is a CLEAR, not a description: mfOpen and mfPaint both blank the pane
        # before anything is marked, and counting those as writers would turn this leg red
        # against correct code (measured while building T857). What has one owner is the TEXT.
        $all372 = ([regex]::Matches($c372, '\["edtMfDesc"\]\.text\s*=')).Count
        $clr372 = ([regex]::Matches($c372, '\["edtMfDesc"\]\.text\s*=\s*""')).Count
        for ($z372 = 0; $z372 -lt ($all372 - $clr372); $z372++) { $w372 += $f372.Name }
    }
    if ($w372.Count -eq 0) { $v372Bad += "(a) nothing writes a description into edtMfDesc.text anywhere - the pane only ever gets cleared, and leg (a) would pass on the silence (SPEC V372c, V209, V20)" }
    elseif ($w372.Count -ne 1) { $v372Bad += "(a) a description is written into edtMfDesc.text $($w372.Count) time(s) ($($w372 -join ', ')) - the pane has one writer and mfDesc is it, or the two disagree about what the box is showing (SPEC V372a, V135)" }
    elseif ((([regex]::Matches((NoComments $descFn372), '\["edtMfDesc"\]\.text\s*=')).Count - ([regex]::Matches((NoComments $descFn372), '\["edtMfDesc"\]\.text\s*=\s*""')).Count) -lt 1) { $v372Bad += "(a) the one write of edtMfDesc.text is NOT inside mfDesc - then mfDesc is not the writer this rule names and the three callers do not share it (SPEC V372a, V135)" }
}

# (b) the OPENER paints, under the same condition that decides there is anything to show.
if ($openFn372) {
    $ob372 = NoComments $openFn372
    if ($ob372 -notmatch 'mfDesc\(') { $v372Bad += "(b) mfOpen never calls mfDesc - a row that already holds a value would open with an EMPTY pane, which is the symptom B109 wore for three rounds (SPEC V372b, I122a, B109)" }
    if ($ob372 -notmatch 'MF\.picked = cur') { $v372Bad += "(b) mfOpen does not pre-mark what the row holds - the pane could be painted and OK would still write nothing, or write the wrong thing (SPEC V372b, I122a)" }
}

if ($v372Bad) { foreach ($b in $v372Bad) { Fail "V372 $b" } }
else { Pass "V372 the pane text is written in exactly one place and that place is mfDesc, and mfOpen pre-marks what the row holds and paints it" }

# ---- V373: the ? and the picker of ONE row name the SAME module ---------------------
# SPEC V373, B110, I102e, I113a, V364f, T860. Two doors open a description for the same row -
# the ? (popOpen) and the picker's pane (mfOpen -> mfDesc). They are two calls, so they can
# disagree, and they did: HeaderPicker's popOpen named 'Nature' while its mfOpen named NOTHING.
# The ? showed the book text and the picker for the very same row showed "No description
# available" - a gap that reads like missing DATA when what is missing is an argument.
#
# Measured per TEMPLATE and not per name: the two calls of a row live in one template, and a
# roster of row-to-module would age a line per wave of I113e, which I114f refuses.
$v373Bad = @()
$pairs373 = @{}
foreach ($f373 in $files) {
    $raw373 = NoComments ([System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f373.FullName)))
    foreach ($tpl373 in [regex]::Matches($raw373, '(?s)<template\s+name="([^"]+)"(.*?)</template>')) {
        $nm373 = $tpl373.Groups[1].Value
        $body373 = $tpl373.Groups[2].Value
        $pop373 = [regex]::Match($body373, "popOpen\(self,\s*'([^']*)'")
        $mf373 = [regex]::Match($body373, "mfOpen\(self,\s*'[^']*',\s*'[^']*'\s*(?:,\s*'([^']*)')?\s*\)")
        if (-not $pop373.Success -and -not $mf373.Success) { continue }
        $pairs373[$nm373] = @{
            File = $f373.Name
            HasPop = $pop373.Success
            HasMf = $mf373.Success
            Pop = $(if ($pop373.Success) { $pop373.Groups[1].Value } else { $null })
            Mf = $(if ($mf373.Success -and $mf373.Groups[1].Success) { $mf373.Groups[1].Value } else { $null })
        }
    }
}

# (c) zero-guard: a rule that finds no pair and passes anyway is B94.
if ($pairs373.Count -eq 0) { $v373Bad += "(c) not one template opens a description - neither popOpen nor mfOpen was found in any .lfm, and legs (a)/(b) would pass over an empty set (SPEC V373c, V209, V20)" }

foreach ($k373 in ($pairs373.Keys | Sort-Object)) {
    $p373 = $pairs373[$k373]
    # (a) a template carrying BOTH doors names ONE module through both of them. This is the leg
    # B110 cost a round of screen testing on.
    if ($p373.HasPop -and $p373.HasMf) {
        if ($p373.Pop -ne $p373.Mf) {
            $popTxt = $(if ($null -eq $p373.Pop) { "nothing" } else { "'" + $p373.Pop + "'" })
            $mfTxt = $(if ($null -eq $p373.Mf) { "nothing" } else { "'" + $p373.Mf + "'" })
            $v373Bad += "(a) $($p373.File): template '$k373' opens the ? with $popTxt and the picker with $mfTxt - two doors onto the SAME row's description, and the one naming nothing falls to V360c's sentence while the other shows the book (SPEC V373a, B110, I102e)"
        }
    }
    # (b) a template with only the picker names a module or names none, and either is legal
    # (V354b) - but the module it names has to EXIST on disk, or the pane says "no description"
    # for a list whose text is right there.
    if ($p373.HasMf -and $null -ne $p373.Mf -and $p373.Mf -ne '') {
        foreach ($half373 in @('en', 'pt')) {
            $mod373 = Join-Path $plugin ("desc" + $p373.Mf + "_" + $half373 + ".lua")
            if (-not (Test-Path -LiteralPath $mod373)) {
                $v373Bad += "(b) $($p373.File): template '$k373' names module '$($p373.Mf)' and desc$($p373.Mf)_$half373.lua is not in the plugin root - the require is guarded so nothing raises, and the pane simply says the book carries nothing (SPEC V373b, V364a, I102e)"
            }
        }
    }
}

if ($v373Bad) { foreach ($b in $v373Bad) { Fail "V373 $b" } }
else { Pass "V373 all $($pairs373.Count) description-opening template(s) agree with themselves - where a row has both a ? and a picker they name one module, and every module named is on disk in both languages" }

# ---- V382: a slot's own Custom name is not refused by that slot's filter ----------------
# SPEC V382, I132, T877, user 2026-08-31. isTypedRow was the filter's escape valve: a typed
# row answers to no table, so there was never anything there to refuse. Waves 3 and 4 (T873,
# T875) take the free rows away and isTypedRow goes false for EVERY family - at which point a
# `-- Custom --` name in a disciplines row reaches guardPick, which reverts the field and says
# so. The player types a name, confirms, and watches it vanish (SPEC I132a).
#
# The exemption is by VALUE and never by ROW. Testing `cf ~= nil` alone would exempt every row
# that HAS a custom field, for good, and the filter would die silently wherever a custom was
# used once - V199 falling over from the inside (SPEC I132b).
$v382Bad = @()
$prFn382 = [regex]::Match($rootLua379, '(?s)local function pickRefusal\(field, value, levels\)(.*?)\n\t{3}end;')

# (e) zero-guard FIRST: a rule that measures an exemption inside a function that is not there
# passes on the empty set, which is B7 born ready (SPEC V20).
if (-not $prFn382.Success) {
    $v382Bad += "(e) pickRefusal is gone - it is the ONE place that knows what is legal, and every leg below would pass vacuously without it (SPEC V199, V20, B7)"
} else {
    $body382 = $prFn382.Groups[1].Value

    # (a) the exemption asks for the slot's own custom field AND compares the VALUE to it.
    if ($body382 -notmatch 'customFieldOf\(') {
        $v382Bad += "(a) pickRefusal never asks customFieldOf - a Custom name in a filtered row is refused and guardPick reverts it in front of the player (SPEC I132a, V382a)"
    }
    if ($body382 -notmatch '\[\s*cf\s*\]\s*==\s*value') {
        $v382Bad += "(a) the exemption does not compare the slot's custom field to the VALUE being picked - exempting on the mere existence of a custom field frees the whole row for good, and the filter dies wherever a custom was used once (SPEC I132b, V382a)"
    }

    # (b) an early return BESIDE the rules, not the door that replaces them. If the family
    # rules left pickRefusal, the exemption is letting the sheet through and V199 is gone.
    foreach ($rule382 in @('mainPath_', 'secPath_', 'ritual_')) {
        if ($body382 -notmatch [regex]::Escape($rule382)) {
            $v382Bad += "(b) pickRefusal no longer carries the '$rule382' rule - the custom exemption is an early return beside the others, never the one that replaces them (SPEC V199, V382b)"
        }
    }

    # (d) isTypedRow COEXISTS until T875. Pulling it early reopens I132a in every family whose
    # wave has not run: those still have free rows, and a free row answers to no table.
    if ($body382 -notmatch 'isTypedRow\(') {
        $v382Bad += "(d) isTypedRow left pickRefusal while free rows still exist - the families whose waves have not run lose the escape valve that the Custom line does not yet replace for them (SPEC V382d, I130b)"
    }
}

# (c) ONE owner of the refusal. guardPick reverts what pickRefusal refuses; it does not get to
# hold a second copy of the exemption, because two places deciding one thing is two rulers.
$gpFn382 = [regex]::Match($rootLua379, '(?s)function guardPick\([^)]*\)(.*?)\n\t{3}end;')
if (-not $gpFn382.Success) { $v382Bad += "(c) guardPick is gone - nothing reverts what pickRefusal refuses (SPEC V199, V209)" }
elseif ($gpFn382.Groups[1].Value -match 'customFieldOf\(') {
    $v382Bad += "(c) guardPick asks customFieldOf itself - the exemption belongs to pickRefusal alone, and the second copy is the one that ages quietly (SPEC V135, V382c)"
}

if ($v382Bad) { foreach ($b in $v382Bad) { Fail "V382 $b" } }
else { Pass "V382 pickRefusal exempts a slot's own Custom name by VALUE, keeps the family rules and isTypedRow beside it, and guardPick carries no second copy" }

# ---- V383: every authored picker button is PAINTED, and the bound is DERIVED -------------
# SPEC V383, B112, I107a2, T878, user 2026-08-31. A button's text is not data: nothing brings
# it back when the sheet opens, so a button no painter reaches shows its AUTHORED label
# forever - and a row that HOLDS a value reads "Select Merit" as if it were empty. The field
# is intact, so V2 stays green while the player watches the merit vanish off the screen.
#
# T870 converted eight rows to pickers and the painter's literal bound did not follow. Nothing
# here measured PAINT, so nothing went red: V2 saw the field, V339 counted 22 instances, V378
# saw the hidden twin. The user found it on the screen (SPEC B112).
$v383Bad = @()
$lua383 = CodeOf (Join-Path $dir "WoD20th.lfm")

# The constants a painter derives its bound from, read out of the SOURCE and never copied
# here: a number pasted into the gate goes green the moment the sheet grows a row.
#
# WIDENED in T883 (SPEC V383 amended, B117): (b) used to resolve bounds out of the ROOT form
# alone while its own text says "EVERY loop that paints". Measured 2026-09-01, three painters
# lived outside it and had no ruler at all - renderDiscButtons (WoD20.12), renderPathButtons
# (WoD20.13) and renderRitualButtons (WoD20.14), five painted prefixes between them. No damage:
# all five already derived their bound. It was by a thread - the recipe T880 was handed said to
# bound renderRitualButtons at 22, which would have left ritual_26 and ritual_27 unpainted for
# good with the gate green. That is B112 in the file (b) did not open, and B112 is why (b) exists.
#
# The row constants all live on the ROOT form, so they are still read from there; what moved is
# WHERE the loops are looked for, and that is now every .lfm rather than a roster typed in here -
# a named list of painter files is the literal this rule exists to refuse, one level up.
$K383 = @{}
foreach ($kn383 in @('MERIT_ROWS','BACKGROUND_ROWS','BACKGROUND_FREE_ROWS','SPECIALITY_ROWS','SPECIALITY_FREE_ROWS',
                     'CLAN_DISC_ROWS','DISC_ROWS','DISC_FREE_ROWS','MAIN_PATH_ROWS',
                     'SEC_PATH_ROWS','SEC_PATH_FREE_ROWS','RITUAL_ROWS','RITUAL_FREE_ROWS',
                     'NUMINA_ROWS','NUMINA_FREE_ROWS','PSYCHIC_ROWS','PSYCHIC_FREE_ROWS',
                     'HEDGE_RITUAL_ROWS','HEDGE_RITUAL_FREE_ROWS')) {
    $km383 = [regex]::Match($lua383, '(?m)^\s*' + $kn383 + '\s*=\s*(\d+);')
    if ($km383.Success) { $K383[$kn383] = [int]$km383.Groups[1].Value }
}

# $txt is the source ABOVE the loop, because the one-hop alias below is a `local` inside the
# painter: renderRitualButtons writes `local last = RITUAL_ROWS - RITUAL_FREE_ROWS`, and hunting
# for it in the root form would find nothing and report a derived bound as unresolvable.
#
# ABOVE the loop and not the whole file, and the alias search below takes the LAST hit rather
# than the first: since T909 one file holds THREE painters and all three name their bound `last`
# (SPEC I136j). Reading the file top-down resolved every one of them to the first - the ritual
# painter answered DISC_ROWS - DISC_FREE_ROWS = 15 and 7 authored rows read as unpainted, which
# is the merge hazard B117 named wearing a new hat: a per-FILE regex stops being unique the day
# two things that were in two files land in one.
function Resolve383($expr, $txt) {
    $e = $expr.Trim()
    if ($e -match '^\d+$') { return @{ Ok = $true; Lit = $true; Val = [int]$e } }
    if ($K383.ContainsKey($e)) { return @{ Ok = $true; Lit = $false; Val = $K383[$e] } }
    $s = [regex]::Match($e, '^([A-Z_]+)\s*-\s*([A-Z_]+)$')
    if ($s.Success -and $K383.ContainsKey($s.Groups[1].Value) -and $K383.ContainsKey($s.Groups[2].Value)) {
        return @{ Ok = $true; Lit = $false; Val = $K383[$s.Groups[1].Value] - $K383[$s.Groups[2].Value] }
    }
    # One hop through a local alias: renderBgButtons writes `local last = BACKGROUND_ROWS -
    # BACKGROUND_FREE_ROWS` and bounds on `last`. That IS derived - naming the difference
    # before using it is not a literal, and a rule that could not see through the alias would
    # report correct code as broken, which is B106 (SPEC V383b).
    $als = [regex]::Matches($txt, '(?m)^\s*local\s+' + [regex]::Escape($e) + '\s*=\s*([^;]+);')
    if ($als.Count -gt 0) {
        $al = $als[$als.Count - 1]
        $inner = $al.Groups[1].Value.Trim()
        if ($inner -ne $e) {
            $r = Resolve383 $inner $txt
            if ($r.Ok) { return $r }
        }
    }
    return @{ Ok = $false; Lit = $false; Val = 0 }
}

# What the sheet AUTHORS, expanded template by instance - the sweep V377a runs on slots.
$authored383 = @{}
function Add383($pre, $n) {
    if (-not $authored383.ContainsKey($pre)) { $authored383[$pre] = @() }
    $authored383[$pre] += [int]$n
}
foreach ($f383 in $files) {
    $d383 = Doc $f383.FullName
    foreach ($i383 in $d383.SelectNodes("//MeritPicked")) {
        $m383 = [regex]::Match($i383.GetAttribute("num"), '^([mf])(\d+)$')
        if ($m383.Success) { Add383 ('dynMerit_' + $m383.Groups[1].Value) $m383.Groups[2].Value }
    }
    foreach ($i383 in $d383.SelectNodes("//OpenAbility")) {
        $m383 = [regex]::Match($i383.GetAttribute("field"), '^background_(\d+)$')
        if ($m383.Success) { Add383 'dynbackground_' $m383.Groups[1].Value }
    }
    foreach ($i383 in $d383.SelectNodes("//SpecialityRow")) {
        if ($i383.GetAttribute("num") -match '^\d+$') { Add383 'dynspeciality_' $i383.GetAttribute("num") }
    }
    # The four Vampire templates joined in T883. They are read as ONE shape because they are one
    # shape - <Tpl field="<root>_<n>"/> building dyn<root>_<n> inside - and DiscRow serves TWO
    # roots from one template (disc and clanDisc), which the field attribute says and a per-
    # template mapping would have to repeat (SPEC V178, V354).
    # The three Numina templates joined in T874, and they are the same shape again - the wave
    # that brought them is the one that gave this rule three more painting loops to bound.
    foreach ($tn383 in @('DiscRow', 'MainPathRow', 'SecPathRow', 'RitualRow',
                         'HedgePicker', 'PsychicPicker', 'HedgeRitualPicker')) {
        foreach ($i383 in $d383.SelectNodes("//$tn383")) {
            $m383 = [regex]::Match($i383.GetAttribute("field"), '^([A-Za-z]+)_(\d+)$')
            if ($m383.Success) { Add383 ('dyn' + $m383.Groups[1].Value + '_') $m383.Groups[2].Value }
        }
    }
}

# Every loop that PAINTS, in every file that holds one. The bound is the whole point, so the
# bound is what gets measured - and WHERE the loop lives stopped being part of the question in
# T883 (SPEC B117): a painter is told by calling mfLabel, never by the file it sits in.
$paint383 = @()
foreach ($f383 in $files) {
    $txt383 = CodeOf $f383.FullName
    foreach ($lp383 in [regex]::Matches($txt383, '(?s)for\s+i\s*=\s*([^,]+?)\s*,\s*(.+?)\s*,\s*1\s+do(.*?)\bend;')) {
        if ($lp383.Groups[3].Value -notmatch 'mfLabel\(') { continue }
        foreach ($pm383 in [regex]::Matches($lp383.Groups[3].Value, 'mfLabel\(\s*found\[\s*"([A-Za-z_]+)"\s*\.\.')) {
            $paint383 += [pscustomobject]@{ Pre = $pm383.Groups[1].Value; Lo = $lp383.Groups[1].Value; Hi = $lp383.Groups[2].Value; File = $f383.Name; Txt = $txt383.Substring(0, $lp383.Index) }
        }
    }
}

# (e) zero-guard FIRST: a rule counting painters over an empty set passes vacuously (SPEC V20, B7).
if ($authored383.Count -eq 0) { $v383Bad += "(e) not one picker button was expanded out of the templates - the sweep broke and every family below would pass on the empty set (SPEC V20, B7)" }
if ($paint383.Count -eq 0)    { $v383Bad += "(e) no mfLabel painting loop was read out of the sheet - the check carries no ruler and would pass anything (SPEC V20, B7)" }

if ($v383Bad.Count -eq 0) {
    foreach ($pl383 in $paint383) {
        $hi383 = Resolve383 $pl383.Hi $pl383.Txt
        if (-not $hi383.Ok) { $v383Bad += "(b) $($pl383.File): the loop painting '$($pl383.Pre)' bounds on '$($pl383.Hi)', which is neither a row constant nor a difference of two - a bound the gate cannot resolve is a bound nothing is checking (SPEC V383b)"; continue }
        # The expression AND what it resolves to, because they are often not the same text: an
        # alias hop turns `last` into a number, and a message naming only `last` sends the reader
        # hunting for a literal by that name instead of the `local last = 22` one line up.
        if ($hi383.Lit)     { $v383Bad += "(b) $($pl383.File): the loop painting '$($pl383.Pre)' bounds on '$($pl383.Hi)', which is the LITERAL $($hi383.Val) - a literal does not follow the family when it grows, which is how T870 left eight rows unpainted (SPEC B112, V383b)"; continue }

        $lo383 = Resolve383 $pl383.Lo $pl383.Txt
        if (-not $authored383.ContainsKey($pl383.Pre)) { $v383Bad += "(a) $($pl383.File): the loop paints '$($pl383.Pre)' but no template authors a row by that name - painter and sheet disagree about what exists (SPEC V209)"; continue }
        # SUBSET and only subset: every authored row falls INSIDE the painted range. The reverse -
        # every index in the range being authored - is the test that would redden correct code now
        # that a family can carry a hole: ritual authors {1..20, 26, 27} inside a range of 1..27,
        # and 21..25 are burned names no row may wear (SPEC V251, V204 amended, B106). What keeps
        # the loop out of the hole is V204's own leg, not this one - two owners for that would be
        # B70, and this rule's question is whether a row gets PAINTED at all.
        $miss383 = @($authored383[$pl383.Pre] | Where-Object { $_ -lt $lo383.Val -or $_ -gt $hi383.Val } | Sort-Object)
        if ($miss383.Count -gt 0) {
            $v383Bad += "(a) $($pl383.File): $($miss383.Count) authored '$($pl383.Pre)' row(s) fall outside the painted range $($lo383.Val)..$($hi383.Val) [$($miss383 -join ', ')] - a button nothing paints keeps its authored label, so a FILLED row reads as empty on every open (SPEC I107a2, B112, V383a)"
        }
    }
}

# ---- (a)(b) for the picker button authored OUTSIDE any template ------------------------
# SPEC T893, B123, V354i, V333g. The collector above expands TEMPLATES, because that is where
# all but one of the picker buttons on this sheet come from. A button authored INLINE escapes
# the whole rule - it is not a template instance, so nothing above ever adds it to a family and
# nothing ever asks whether a painter reaches it. Same vacancy V333 closed one door over for
# the ?, with $BARE_Q333, and the one V354(i) names from the code side.
#
# MEASURED 2026-09-01 (T874 half B): dropping 'hedgeAffiliation' out of the fields= of the
# dataLink that drives renderNuminaButtons left the whole gate GREEN. The damage is real and
# silent - the button would stop being repainted and sit there reading "Select Affiliation"
# with a value inside it, because picking only does setField(MF.field, MF.picked) and there is
# no direct repaint: the dataLink IS the path back to the screen (SPEC B123).
#
# NAMED and closed, never a skip: a second bare picker button has to redden here rather than be
# absorbed by a wildcard, which is the whole difference between a roster and a hole (SPEC V328d).
$BARE_DYN = @('dynhedgeAffiliation', 'dynroad', 'dynclanFamily')
$bareSeen383 = @()
foreach ($f383b in $files) {
    foreach ($b383b in (Doc $f383b.FullName).SelectNodes("//button")) {
        if ($b383b.GetAttribute("onClick") -notmatch 'mfOpen\(') { continue }
        if ($null -ne (TplOf $b383b)) { continue }
        $bn383b = $b383b.GetAttribute("name")
        $bareSeen383 += $bn383b
        if ($BARE_DYN -notcontains $bn383b) {
            $v383Bad += "(a) $($f383b.Name) draws a picker button named '$bn383b' outside any template and it is on no roster - the template walk above cannot see it, so neither leg ever asks who paints it or what watches it (SPEC B123, V354i, V328d)"
        }
    }
}
# Ghost guard, and it is the zero-guard for this half: a roster name authored nowhere is a name
# that excuses the next bare button to appear under it (SPEC V20, B7).
foreach ($bd383 in $BARE_DYN) {
    if ($bareSeen383 -notcontains $bd383) {
        $v383Bad += "(e) '$bd383' is on the bare-picker roster and is authored nowhere - a stale name here is a hole the next bare button walks through (SPEC V20, B7)"
    }
}

foreach ($bd383 in @($BARE_DYN | Where-Object { $bareSeen383 -contains $_ })) {
    # (a) SOME mfLabel reaches it. Which FILE the painter sits in stopped being part of the
    # question in T883 and it is not part of this one either - a painter is told by the call.
    $pf383 = $null
    foreach ($f383c in $files) {
        $t383c = CodeOf $f383c.FullName
        $mm383 = [regex]::Match($t383c, 'mfLabel\(\s*found\[\s*"' + [regex]::Escape($bd383) + '"\s*\]\s*,\s*([^,]+?)\s*,')
        if (-not $mm383.Success) { continue }
        # The FIELD it paints is the second argument, and the FUNCTION it paints from is the last
        # one opened before the call - which is the name a dataLink has to carry to fire it again.
        $fn383 = [regex]::Matches($t383c.Substring(0, $mm383.Index), '(?m)^\s*function\s+([A-Za-z_][A-Za-z0-9_]*)\s*\(')
        $pf383 = [pscustomobject]@{
            File = $f383c.Name
            Arg  = $mm383.Groups[1].Value.Trim()
            Fn   = $(if ($fn383.Count -gt 0) { $fn383[$fn383.Count - 1].Groups[1].Value } else { '' })
        }
        break
    }
    if ($null -eq $pf383) {
        $v383Bad += "(a) '$bd383' is authored outside a template and no mfLabel call reaches it - a button nothing paints keeps its AUTHORED label forever, so a filled row reads as empty on every open (SPEC I107a2, V383a, B123)"
        continue
    }

    $fld383 = ''
    if     ($pf383.Arg -match '^sheet\.([A-Za-z_][A-Za-z0-9_]*)$')          { $fld383 = $matches[1] }
    elseif ($pf383.Arg -match '^sheet\[\s*"([A-Za-z_][A-Za-z0-9_]*)"\s*\]$') { $fld383 = $matches[1] }
    # The third shape is T954's: two buttons over ONE field, and the one that does not own the
    # value is painted from nil so it goes back to its invitation. `<test> and sheet.X or nil`
    # still names exactly one field, which is all leg (b) needs to know (SPEC V426c, I145d).
    elseif ($pf383.Arg -match '^\(?[A-Za-z_][A-Za-z0-9_ ]*\)?\s+and\s+sheet\.([A-Za-z_][A-Za-z0-9_]*)\s+or\s+nil$') { $fld383 = $matches[1] }
    if ($fld383 -eq '') {
        $v383Bad += "(b) the mfLabel painting '$bd383' reads '$($pf383.Arg)', which names no single sheet field - leg (b) cannot ask whether the link watches a field it cannot name (SPEC V383b, B123)"
        continue
    }
    if ($pf383.Fn -eq '') {
        $v383Bad += "(b) the mfLabel painting '$bd383' sits inside no named function - there is nothing for a dataLink to call, so the button is painted once on load and never again (SPEC B123, V123)"
        continue
    }

    # (b) the dataLink driving that painter OBSERVES the field the painter writes. This is the
    # leg the mutation proved missing: everything else about the button can be perfect and the
    # value still never reaches the screen after the pick (SPEC B123, V123).
    $seen383 = $false
    $watch383 = $false
    foreach ($f383d in $files) {
        foreach ($dl383 in (Doc $f383d.FullName).SelectNodes("//dataLink")) {
            # A dataLink writes its handler in TWO shapes and this leg only knew one of them: the
            # onChange ATTRIBUTE, and the <event name="onChange"> CHILD. The road link uses the
            # child form, so T876 arrived with a correct painter, a correct watcher and a red
            # check - the same scope-short-of-the-promise shape as B122 and B123, which is what
            # this leg was written for in the first place. It can only over-report, never let a
            # defect through, which is why it stayed unnoticed until a picker used the other form.
            $h383 = $dl383.GetAttribute("onChange")
            foreach ($ev383 in $dl383.SelectNodes("event[@name='onChange']")) { $h383 += "`n" + $ev383.InnerText }
            if ($h383 -notmatch ('\b' + [regex]::Escape($pf383.Fn) + '\s*\(')) { continue }
            $seen383 = $true
            if ($dl383.GetAttribute("fields") -match ("'" + [regex]::Escape($fld383) + "'")) { $watch383 = $true }
            # A dataLink names what it watches in TWO attributes and this leg knew only the plural:
            # `fields="{'a','b'}"` and `field="a"`. The clanFamily link uses the singular, so the same
            # blind spot that hid the <event> form one line up hid this one too (SPEC B122, B123).
            if ($dl383.GetAttribute("field") -eq $fld383) { $watch383 = $true }
        }
    }
    if (-not $seen383) {
        $v383Bad += "(b) '$bd383' is painted by $($pf383.Fn) in $($pf383.File) and no dataLink anywhere calls that function - the paint runs on load and nothing ever runs it again (SPEC B123, V123)"
    }
    elseif (-not $watch383) {
        $v383Bad += "(b) '$bd383' is painted by $($pf383.Fn) out of sheet.$fld383, and no dataLink driving $($pf383.Fn) watches '$fld383' - picking only does setField(MF.field, MF.picked), so the button would keep showing its empty label with a value sitting inside the field (SPEC B123, V123, V383b)"
    }
}
# (c) ONE mfLabel and the shape lives inside it. (d) the empty label goes through the
# translator, so no PT string is ever authored into the painter (SPEC V9, V24).
$ml383 = [regex]::Matches($lua383, '(?s)function mfLabel\([^)]*\)(.*?)\n\t{3}end;')
if ($ml383.Count -ne 1) { $v383Bad += "(c) mfLabel is declared $($ml383.Count) time(s), expected 1 - the empty/filled shape is one decision and a second copy is the one that ages quietly (SPEC V135, V383c)" }
else {
    $mb383 = $ml383[0].Groups[1].Value
    foreach ($mk383 in @('"-- "', 'italic', '0.80')) {
        if ($mb383 -notmatch [regex]::Escape($mk383)) { $v383Bad += "(c) mfLabel no longer writes $mk383 - the empty row stops saying it is empty, which is the whole of the shape this rule holds (SPEC V383c)" }
    }
    if ($mb383 -notmatch 'fontStyle\s*=\s*""') { $v383Bad += "(c) mfLabel never clears fontStyle - a row that gets filled keeps the italic of the empty state and reads as still empty (SPEC V383c)" }
    if ($mb383 -notmatch 'translateSheetText') { $v383Bad += "(d) mfLabel does not send the fallback through translateSheetText - the placeholder would ship in one language only, or as a PT string authored into the source (SPEC V9, V24, V383d)" }
}

if ($v383Bad) { foreach ($b in $v383Bad) { Fail "V383 $b" } }
else { Pass "V383 all $((($authored383.Values | ForEach-Object { $_.Count }) | Measure-Object -Sum).Sum) authored picker button(s) fall inside a painted range, each of the $($paint383.Count) painting loop(s) across $(@($paint383 | ForEach-Object { $_.File } | Sort-Object -Unique).Count) file(s) bounds on a constant read from the source, the $($bareSeen383.Count) button(s) authored outside a template are each painted by a named function whose dataLink watches the field it paints, and mfLabel alone owns the empty/filled shape" }

# ---- V353: the opening focus, now that the screen has answered ---------------------------
# SPEC V353, I111, Q28, T804/T805. This id was RESERVED, not written: I111a measured fifteen
# .lfm and found no focus call anywhere, and a rule aimed at an API name nobody had exercised
# would have been a check born no-op (SPEC B92). T804 ran it on the screen and the name is
# settled - gui.Control:setFocus(), declared at rrpgGUI.lua:58 in the SDK in this repo - so
# the three candidates of I111c collapsed to one and the rule can finally measure something.
$v353Bad = @()
$lua353 = CodeOf (Join-Path $dir "WoD20th.lfm")

# Comments are already stripped by CodeOf, and they matter here: the block above the call
# spells `setFocus` four times over, so counting the raw file would find focus calls that are
# prose and pass leg (b) on them.
$calls353 = [regex]::Matches($lua353, 'setFocus\s*\(')

# (c) zero-guard FIRST: no focus call at all means the box went back to needing a click and
# every leg below would pass on the empty set (SPEC V20, B7).
if ($calls353.Count -eq 0) {
    $v353Bad += "(c) no setFocus call survives in the sheet - the box is back to opening with the cursor nowhere, which is the whole of Q28, and legs (a) and (b) would pass vacuously (SPEC I111, Q28, V20, B7)"
} else {
    # (b) ONE place asks. One per opening path is how two of them drift, and the second is the
    # one that goes on asking focus for a box that is already gone (SPEC V135).
    if ($calls353.Count -ne 1) {
        $v353Bad += "$($calls353.Count) setFocus call(s) in the sheet, expected 1 - focus is asked in one place or it is asked once per opening path, and the paths do not agree for long (SPEC V353b, V135)"
    }

    # (a) AMENDED in T923 (SPEC V353 amended, I140u, B138). The prose of leg (a) has always read
    # "o pedido de foco esta DEPOIS de mfSearch.visible = true - foco em widget invisivel ⊥ pega":
    # the requirement is ORDER, and "inside mfOpen" was a PROXY for it that held only while mfOpen
    # was the one opening path. The owner asked for Esc to close the ? as well, popOpen became a
    # second opening path, and a click inside either overlay has to hand the cursor back - so the
    # proxy started refusing the very shape it was written to protect (SPEC B138, B125).
    #
    # What replaces it is what the prose says, and it is STRONGER than the proxy was: the one
    # asker gates on .visible itself, and EVERY opening path calls it after showing its box. The
    # proxy checked one path; this checks both, and it checks the guarantee rather than a place.
    $FOCUS_PATHS = @(
        [pscustomobject]@{ Fn = 'mfOpen';  Box = 'mfSearch' }
        [pscustomobject]@{ Fn = 'popOpen'; Box = 'popDesc' }
    )
    $refoc353 = [regex]::Match($lua353, '(?s)function kbRefocus\([^)]*\)(.*?)\n\t{3}end;')
    if (-not $refoc353.Success) {
        $v353Bad += "(a) kbRefocus is gone - the one asker is where the .visible guard lives, and without it focus is asked positionally again (SPEC V353a, I140u)"
    } elseif ($refoc353.Groups[1].Value -notmatch '\.visible') {
        $v353Bad += "(a) kbRefocus asks for focus without reading .visible - asking a widget that is not on screen is the one way this cannot work, and it fails SILENTLY (SPEC V353a, I111c)"
    } elseif ($refoc353.Groups[1].Value -notmatch 'setFocus\s*\(') {
        $v353Bad += "(a) the setFocus call is not inside kbRefocus - the ask and the .visible guard are one function, or the guard is optional (SPEC V353a, V135)"
    }
    foreach ($path353 in $FOCUS_PATHS) {
        $fn353 = [regex]::Match($lua353, '(?s)function ' + $path353.Fn + '\([^)]*\)(.*?)\n\t{3}end;')
        if (-not $fn353.Success) { $v353Bad += "(a) $($path353.Fn) is gone - an opening path that cannot be read is one this leg stops measuring (SPEC V209)"; continue }
        $body353 = $fn353.Groups[1].Value
        $show353 = [regex]::Match($body353, 'found\[\s*"' + $path353.Box + '"\s*\]\s*\.visible\s*=\s*true')
        $ask353  = [regex]::Match($body353, 'kbRefocus\s*\(')
        if (-not $ask353.Success) { $v353Bad += "(a) $($path353.Fn) opens $($path353.Box) and never hands the cursor over - that is Q28 again, on the overlay that got it second (SPEC I111, V353a)" }
        elseif (-not $show353.Success) { $v353Bad += "(a) $($path353.Fn) never sets $($path353.Box).visible = true - the ordering leg has no landmark, so it would pass whatever the order actually is (SPEC V209, V20)" }
        elseif ($ask353.Index -lt $show353.Index) { $v353Bad += "(a) $($path353.Fn) asks for focus BEFORE $($path353.Box) is shown - a widget that is not on screen yet takes no focus, and it fails silently: no error, no cursor (SPEC I111c, V353a)" }
    }
}

if ($v353Bad) { foreach ($b in $v353Bad) { Fail "V353 $b" } }
else { Pass "V353 the cursor is handed over from exactly one place - kbRefocus, which gates on .visible - and both opening paths call it after showing their box" }

# ---- V405: Esc closes both overlays -----------------------------------------------------
# SPEC V405, I140c, I140u, R138, T922/T923, user 2026-09-02 item 2.
#
# T922 measured the thing the SDK source does not say: a key event reaches ONLY the control that
# has FOCUS, and only a TEXT control takes focus - edtPopDesc is readOnly and still takes it. The
# probe hung onKeyDown on the popDesc layout and on popScrim as well and neither EVER fired, so
# those two came straight back out: a listener on a control that cannot hold focus is a line that
# never runs, which is B111 wearing different clothes.
#
# That leaves exactly one keyboard-capable control per overlay, and this rule is those two plus
# the two rules that fall out of the measurement.
# (a) DERIVED, and no longer a roster of two (SPEC B139, I140x). The first shape of this rule
# NAMED edtMfQuery and edtPopDesc and said "exactly one keyboard-capable control per overlay" -
# and the picker has THREE: the filter, the `-- Custom --` name field and the read-only
# description pane. The user clicked the description, pressed Esc, and nothing happened; the
# gate was green over it, because a roster only ever measures what somebody remembered.
#
# So the subject is SWEPT: every <edit> and <textEditor> inside either overlay takes focus when
# it is clicked, therefore every one of them has to answer Esc. A control added to either box
# tomorrow enters this set by construction (SPEC V209, B7).
$V405_OVERLAYS = @('mfSearch', 'popDesc')
$v405Bad = @()
$doc405 = Doc $rootPath
$lua405 = CodeOf $rootPath
$seen405 = 0
foreach ($ov405 in $V405_OVERLAYS) {
    $box405 = $doc405.SelectSingleNode("//layout[@name='$ov405']")
    if ($null -eq $box405) { $v405Bad += "the overlay '$ov405' is gone from the root form - every leg below would sweep an empty set (SPEC V20, V209)"; continue }
    $keys405 = @($box405.SelectNodes(".//edit|.//textEditor"))
    if ($keys405.Count -eq 0) { $v405Bad += "'$ov405' holds no text control at all - it would open with nothing that can hear a key, which is Q28 by the other door (SPEC V209)"; continue }
    foreach ($k405 in $keys405) {
        $seen405++
        $nm405 = $k405.GetAttribute("name")
        $kd405 = $k405.GetAttribute("onKeyDown")
        if (-not $kd405) { $v405Bad += "'$nm405' is a text control inside $ov405 and carries no onKeyDown - click it and Esc falls on the floor, which is exactly what the owner reported about the description pane (SPEC B139, I140c)"; continue }
        if ($kd405 -notmatch 'escClose\(') { $v405Bad += "'$nm405' answers keys with '$kd405' and not escClose - one closer for both overlays, or the second one drifts (SPEC V333e, V135)"; continue }
        # the diagnostic argument is DERIVED from readOnly and never chosen: a box nobody types
        # in is a free channel for the one thing still unmeasured, and a box people type in is
        # not (SPEC V405, I140c). Reading it off readOnly is what keeps that from being taste.
        $want405 = if ($k405.GetAttribute("readOnly") -eq 'true') { 'true' } else { 'false' }
        if ($kd405 -notmatch (',\s*' + $want405 + '\s*\)')) {
            $v405Bad += "'$nm405' is readOnly=$($k405.GetAttribute('readOnly')) and asks escClose with the wrong diagnostic flag - the box that reports an unrecognised key is the one nobody types in, or the report lands on the filter mid-word (SPEC V405)"
        }
    }
}
if ($seen405 -lt 4) { $v405Bad += "only $seen405 text control(s) were swept across the two overlays, expected at least the 4 they draw - this leg is covering less than the sheet has (SPEC V209)" }
# (b) the closing goes through the SAME doors the X and the scrim use. A second closer is what
# V333(e) refuses, and it is how one of the two overlays ends up half-shut - box hidden, scrim up.
$esc405 = [regex]::Match($lua405, '(?s)function escClose\([^)]*\)(.*?)\n\t{3}end;')
if (-not $esc405.Success) { $v405Bad += "escClose is gone - the two listeners above would be calling nothing (SPEC V209)" }
else {
    foreach ($closer405 in @('mfClose', 'popClose')) {
        if ($esc405.Groups[1].Value -notmatch ($closer405 + '\s*\(')) { $v405Bad += "escClose never calls $closer405 - Esc would shut one overlay and leave the other, and the scrim belongs to both (SPEC V333e)" }
    }
    if ($esc405.Groups[1].Value -notmatch 'isEscKey\s*\(') { $v405Bad += "escClose does not ask isEscKey - closing on ANY key is not what Esc means, and typing in the filter would shut the box (SPEC I140c)" }
}
# (c) and the measurement itself, kept as a rule: onKeyDown belongs on a control that can hold
# focus. MEASURED 2026-09-02 - the layout and the rectangle the probe wore never fired once.
foreach ($f in $files) {
    foreach ($kn405 in (Doc $f.FullName).SelectNodes("//*[@onKeyDown]")) {
        if ($kn405.LocalName -in @('edit', 'textEditor')) { continue }
        $v405Bad += "$($f.Name) <$($kn405.LocalName) name='$($kn405.GetAttribute('name'))'> listens for onKeyDown and cannot hold focus - T922 hung exactly that on a layout and on a rectangle and neither ever fired (SPEC R138, B111)"
    }
}
if ($v405Bad) { foreach ($b405 in $v405Bad) { Fail "V405 $b405" } }
else { Pass "V405 Esc reaches the one focusable control of each overlay and closes it through the door the X already uses" }

# ---- V359: the box has TWO sizes and the CALL picks -------------------------------------
# SPEC V359, I115, T826, ask 2. Specialties asked for a shorter box: its pane holds no book
# prose (it is the one mfOpen caller that passes no module, and no descSpeciality_*.lua is on
# disk), so stretching the right half to 1000 was stretching it over nothing.
#
# What this rule is really holding is the shape of the decision. A third size would be a
# ruler per list, and "which lists are narrow" is a roster that grows a line every wave -
# the line nobody updates being the one that is wrong (SPEC I114f, I115a).
$v359Bad = @()
$lua359 = CodeOf (Join-Path $dir "WoD20th.lfm")
$mfSize359 = [regex]::Match($lua359, '(?s)function mfSize\(found, size\)(.*?)\n\t{3}end;')
$mfOpen359 = [regex]::Match($lua359, 'function mfOpen\(([^)]*)\)')

# (e) zero-guard FIRST: with no sizer, every leg below measures an empty string and passes
# on it, which is B7 born ready (SPEC V20).
if (-not $mfSize359.Success) {
    $v359Bad += "(e) mfSize is gone - nothing sizes the box, and every leg of this rule would pass on the empty set (SPEC I115, V20, B7)"
} else {
    $body359 = $mfSize359.Groups[1].Value

    # (a) exactly TWO widths reach the sizer, and both are named constants read from source.
    $consts359 = @([regex]::Matches($body359, 'MF_[A-Z_]+') | ForEach-Object { $_.Value } | Sort-Object -Unique)
    if ($consts359.Count -ne 2) {
        $v359Bad += "(a) mfSize names $($consts359.Count) width constant(s) [$($consts359 -join ', ')], expected exactly 2 - a third size is a ruler per list, which is the roster I114f refuses (SPEC V359a, I115a)"
    }
    foreach ($c359 in $consts359) {
        if ($lua359 -notmatch ('(?m)^\s*' + $c359 + '\s*=\s*\d+;')) {
            $v359Bad += "(a) $c359 is used by mfSize but never declared as a number in the sheet - the width would come back nil and the box would keep whatever the XML authored (SPEC V359a)"
        }
    }

    # (b) the size arrives as a PARAMETER. A test on the list name here is the roster again,
    # only hidden inside an if where no one greps for it (SPEC V359b, I115b).
    if ($mfOpen359.Success) {
        $args359 = ($mfOpen359.Groups[1].Value -split ',') | ForEach-Object { $_.Trim() }
        if ($args359 -notcontains 'size') {
            $v359Bad += "(b) mfOpen does not take a size parameter (takes: $($args359 -join ', ')) - the caller is what chooses the size, the way it already chooses the module (SPEC I113a, I115b, V359b)"
        }
    } else {
        $v359Bad += "(b) mfOpen was not found - nothing can hand the size in (SPEC V209)"
    }
    foreach ($roster359 in @('MF\.list', 'list\s*==')) {
        if ($body359 -match $roster359) {
            $v359Bad += "(b) mfSize tests the LIST to pick a width - that is the roster of I115b hidden inside an if, and it grows a line every wave (SPEC V359b, I114f)"
        }
    }

    # (d) the RIGHT half is what shrinks. The result column and the footer under it live in
    # the left half and must not be touched, or this is a shorter LIST and not a smaller box.
    foreach ($left359 in @('edtMfQuery', 'lblMfLoading', 'btnMfPrev', 'btnMfNext', 'lblMfPage', 'btnMfOk', 'btnMfRemove', 'btnMfCustom')) {
        if ($body359 -match [regex]::Escape($left359)) {
            $v359Bad += "(d) mfSize writes '$left359', which sits in the LEFT half - the result column stays at 20/480 in both sizes, and that is what separates a smaller box from a shorter list (SPEC V359d, V352c)"
        }
    }
}

# (c) the width write does NOT live in applyTheme. V57 guards that body against geometry and
# this rule must not be the reason anyone loosens it (SPEC I115c, V359c, B79).
foreach ($f359 in $files) {
    $at359 = [regex]::Match((CodeOf $f359.FullName), '(?s)function applyTheme\([^)]*\)(.*?)\n\t{3}end;')
    if (-not $at359.Success) { continue }
    foreach ($w359 in @('MF_WIDE', 'MF_NARROW', 'mfSize')) {
        if ($at359.Groups[1].Value -match [regex]::Escape($w359)) {
            $v359Bad += "(c) applyTheme in $($f359.Name) reaches '$w359' - the size is a per-opening decision, not a theme one, and V57 guards that body against exactly this (SPEC I115c, V359c, B79)"
        }
    }
}

if ($v359Bad) { foreach ($b in $v359Bad) { Fail "V359 $b" } }
else { Pass "V359 the box has two widths, both declared constants, chosen by the caller through mfOpen; only the right half moves and applyTheme is not the one moving it" }
Write-Host ""
# ---- V384/V385/V386/V387: the context tooling of SPEC I133 -------------------------------
# The 162nd round. None of these four measure the SHEET - they measure what a reader has to
# load in order to work on it, which is the cost SPEC I133 measured at 323k tokens of context
# per request. Each guards a failure that is real and SILENT: a dangling cite hands a build
# half a task, a -Quiet that filtered CHECKS would be SPEC B7 over again, an id that stayed in
# both spec files gets edited in the copy nobody reads, and four reading rules are habit, so
# they die without leaving a symptom anywhere.
$sect384     = [string][char]0xA7
$specPath384 = Join-Path $PSScriptRoot 'SPEC.md'
$arcPath384  = Join-Path $PSScriptRoot 'SPEC-ARCHIVE.md'

function SpecIdSet($ls) {
    $h = @{}
    foreach ($l in $ls) {
        if     ($l -match '^([TB][0-9]+)\|')        { $h[$Matches[1]] = $true }
        elseif ($l -match '^(V[0-9]+):')            { $h[$Matches[1]] = $true }
        elseif ($l -match '^\s*- (I[0-9]+[a-z]?) ') { $h[$Matches[1]] = $true }
    }
    return $h
}

# A cite may name a LEG: V314e is leg (e) of row V314, which is a single line. The row is the
# id, so an id that does not resolve as written gets one more try without its trailing letter.
function CiteResolves($id, $have) {
    if ($have.ContainsKey($id)) { return $true }
    return $have.ContainsKey(($id -replace '[a-z]$', ''))
}

$specLines384 = @()
if (Test-Path $specPath384) { $specLines384 = [IO.File]::ReadAllLines($specPath384, [Text.Encoding]::UTF8) }
$arcLines384 = @()
if (Test-Path $arcPath384)  { $arcLines384  = [IO.File]::ReadAllLines($arcPath384,  [Text.Encoding]::UTF8) }
$allLines384 = @($specLines384) + @($arcLines384)

$have384  = SpecIdSet $allLines384
$v384Bad  = @()
$v384Seen = 0
foreach ($l384 in $allLines384) {
    if ($l384 -notmatch '^(T[0-9]+)\|') { continue }
    $row384   = $Matches[1]
    $f384     = $l384.Split('|')
    $cites384 = @($f384[$f384.Length - 1] -split ',')
    foreach ($m384 in [regex]::Matches($l384, [regex]::Escape($sect384) + '([IVTB][0-9]+[a-z]?)')) {
        $cites384 += $m384.Groups[1].Value
    }
    foreach ($c384 in $cites384) {
        $c384 = $c384 -replace '[^A-Za-z0-9]', ''
        if ($c384 -notmatch '^[IVTB][0-9]+[a-z]?$') { continue }
        $v384Seen++
        if (-not (CiteResolves $c384 $have384)) {
            $v384Bad += "$row384 cites $c384, which is no row in SPEC.md or SPEC-ARCHIVE.md - a slice of $row384 hands back half a task"
        }
    }
}
if ($v384Seen -lt 6000) { Fail "V384 only $v384Seen cites were measured and the spec carries over 6700 - this check is reading less than the file (SPEC V20)" }
elseif ($v384Bad) { foreach ($b in ($v384Bad | Select-Object -First 8)) { Fail "V384 $b" } }
else { Pass "V384 all $v384Seen cites across the task rows resolve to a real row, the archive included" }

# V385: the quiet switch is allowed to hide a PASS line and nothing else. Both needles are
# built by concatenation on purpose - written whole they would match this check's own source
# and the count below would be reading itself.
$self385       = [IO.File]::ReadAllLines((Join-Path $PSScriptRoot 'verify-hunters-hunted.ps1'))
$okNeedle385   = 'Write-Host "' + 'ok    '
$failNeedle385 = 'Write-Host "' + 'FAIL  '
$okLines385    = @($self385 | Where-Object { $_.Contains($okNeedle385) })
$failLines385  = @($self385 | Where-Object { $_.Contains($failNeedle385) })
$v385Bad = @()
if ($okLines385.Count -ne 1) {
    $v385Bad += "(a) $($okLines385.Count) line(s) print a pass - the switch needs exactly one place to gate or the two modes drift apart"
} elseif ($okLines385[0] -notmatch '\$Quiet') {
    $v385Bad += "(a) the one line that prints a pass does not test the switch, so -Quiet still prints everything"
}
foreach ($fl385 in $failLines385) {
    if ($fl385 -match '\$Quiet') { $v385Bad += "(b) a line that prints a failure tests the switch - quiet is an OUTPUT filter, never a check filter (SPEC B7)" }
}
if (@($self385 | Where-Object { $_ -match '\$fail -eq 0' }).Count -lt 1) {
    $v385Bad += "(c) the verdict no longer comes from the failure count, so the two modes can disagree on the exit code"
}
if ($v385Bad) { foreach ($b in $v385Bad) { Fail "V385 $b" } }
else { Pass "V385 -Quiet gates the one pass line and nothing else; failures and the exit code are untouched" }

# V386: archiving is a MOVE, not a copy.
$v386Bad = @()
if (Test-Path $arcPath384) {
    $sIds386 = SpecIdSet $specLines384
    $aIds386 = SpecIdSet $arcLines384
    foreach ($k386 in $aIds386.Keys) {
        if ($sIds386.ContainsKey($k386)) { $v386Bad += "(a) $k386 is a row in BOTH spec files - one of the two copies is the one nobody reads" }
    }
    foreach ($l386 in $arcLines384) {
        if ($l386 -match '^(T[0-9]+)\|([^|]*)\|') {
            if ($Matches[2].Trim() -ne 'x') { $v386Bad += "(b) $($Matches[1]) was archived with status '$($Matches[2].Trim())' - only a CLOSED task leaves SPEC.md" }
        }
    }
    if ($v386Bad) { foreach ($b in ($v386Bad | Select-Object -First 8)) { Fail "V386 $b" } }
    else { Pass "V386 no id lives in two files and every archived task is closed ($($aIds386.Count) archived against $($sIds386.Count) live)" }
} else {
    Pass "V386 there is no SPEC-ARCHIVE.md yet, so no id can be living in two files"
}

# V387: the four reading rules of SPEC I133g. They are habit, and habit leaves no trace when
# it goes - a session that starts without them is not slower in any way the gate can see.
$cmPath387 = Join-Path $PSScriptRoot 'CLAUDE.md'
$cm387     = ''
if (Test-Path $cmPath387) { $cm387 = [IO.File]::ReadAllText($cmPath387, [Text.Encoding]::UTF8) }
$v387Bad = @()
foreach ($needle387 in @('grep -C', '-Quiet', 'Edit` em vez de `Write', 'desc*.lua')) {
    if (-not $cm387.Contains($needle387)) { $v387Bad += "the reading rule naming '$needle387' is gone from CLAUDE.md - the cost comes back with no symptom" }
}
if ($v387Bad) { foreach ($b in $v387Bad) { Fail "V387 $b" } }
else { Pass "V387 all four reading rules are in CLAUDE.md" }

# ---- V388: clearing a picker row clears what HUNG on it ---------------------------------
# SPEC V388, I121, I122c, user 2026-09-02. mfConfirm wrote setField(MF.field, "") and stopped,
# so the dots stayed lit on a row with no name and the ledger went on charging for them. It is
# V383 turned inside out - there the data exists and vanishes off the screen, here the screen
# shows points for a trait that is gone - and V2 stays green through both, because the name
# field is right there, empty and legal.
$v388Bad = @()

# (a) WHICH roots carry dots, read off the TEMPLATES and never typed here: a roster of roots
# written into the gate goes stale the day a family gains or drops its dot column.
$dotRoots388 = @{}
foreach ($f388 in $files) {
    $d388 = Doc $f388.FullName
    foreach ($tpl388 in $d388.SelectNodes("//template")) {
        $tn388 = $tpl388.GetAttribute("name")
        $opens388 = @($tpl388.SelectNodes(".//button[@onClick]") | Where-Object { $_.GetAttribute("onClick") -match 'mfOpen\(' })
        if ($opens388.Count -eq 0) { continue }
        $dots388 = @($tpl388.SelectNodes(".//imageCheckBox[@field]") | ForEach-Object { $_.GetAttribute("field") })
        if ($dots388.Count -eq 0) { continue }
        # Two shapes of dot field, and the root is in a different place in each: SpecialityRow
        # builds speciality_$(num)_1 INSIDE the template, while the rest wear $(field)_N and take
        # their root from the call site - the same split V354(e) reads as INDIRECT vs NUMBERED.
        $m388 = [regex]::Match($dots388[0], '^([A-Za-z]+)_\$\(num\)')
        if ($m388.Success) { $dotRoots388[$m388.Groups[1].Value] = $tn388; continue }
        foreach ($cs388 in $d388.SelectNodes("//$tn388")) {
            $cm388 = [regex]::Match($cs388.GetAttribute("field"), '^([A-Za-z]+)_\d+$')
            if ($cm388.Success) { $dotRoots388[$cm388.Groups[1].Value] = $tn388 }
        }
    }
}

# hedgeRitual carries no dot WIDGET since T892 and still keeps its level in the same five field
# names, which hedgeRitualLevel reads whenever the name has no prefix (SPEC V269b). A sweep over
# widgets finds six roots and loses this one entirely, so it is NAMED - never a catch-all, or the
# next unrated family is absorbed instead of reddening (SPEC V388c, V328d).
$V388_FIELD_ONLY = @('hedgeRitual')
foreach ($fo388 in $V388_FIELD_ONLY) { $dotRoots388[$fo388] = '(field-only, no widget)' }

# (g) zero-guard FIRST: a rule clearing dots over an empty set of roots passes vacuously.
if ($dotRoots388.Count -eq 0) { $v388Bad += "(g) not one picker root with dots was read off the templates - the sweep broke and every leg below would pass on the empty set (SPEC V20, B7)" }

$clear388 = LuaFn $rootTxt 'clearRowDots'
if (-not $clear388) { $v388Bad += "(a) clearRowDots is gone - nothing gives up the dots when a row is cleared, so a removed trait keeps its points on screen and in the log (SPEC V388a)" }
else {
    # (f) the RANGE is derived from XP_TRAIT, the registry xpClick already prices with. A literal
    # range here would not follow a family that changes its dot count, which is how B112 was born.
    if ($clear388 -notmatch 'XP_TRAIT\[') { $v388Bad += "(f) clearRowDots does not read XP_TRAIT - the dot count would be written a second time, free to disagree with the one xpClick prices from (SPEC V388f, B70, B112)" }
    if ($clear388 -notmatch 'tr\.first, tr\.last') { $v388Bad += "(f) clearRowDots does not walk first..last out of the trait - starting at 1 writes an attribute's fixed opening dot, which owns no field at all (SPEC V388f, I3)" }
    if ($clear388 -notmatch 'setField\(') { $v388Bad += "(e) clearRowDots does not write through setField - a second accounting beside the baseline diff is exactly what V136 and V269c keep off this sheet (SPEC V388e)" }
    # (c) the one root the registry does not answer for
    foreach ($fo388 in $V388_FIELD_ONLY) {
        if ($clear388 -notmatch [regex]::Escape($fo388)) { $v388Bad += "(c) clearRowDots never names '$fo388' - XP_TRAIT does not declare it, so the level would survive the remove and the painter would light it on an empty row (SPEC V388c, V269b, T892)" }
    }
}

# (d) ONE writer, and it is mfConfirm. mfRemove only MARKS - a clear there would be the second
# door, and the empty item of V15 would go on leaving a dirty row behind it.
$conf388 = LuaFn $rootTxt 'mfConfirm'
$rem388  = LuaFn $rootTxt 'mfRemove'
if (-not $conf388) { $v388Bad += "(d) mfConfirm is gone - this rule has no subject (SPEC V209)" }
elseif ($conf388 -notmatch 'if MF\.picked == "" then clearRowDots\(MF\.field\); end;') {
    $v388Bad += "(d) mfConfirm does not clear the dots when the pick is the empty one - '' arrives by TWO doors, the Remove line and the empty first item of V15, and this is the single write both land on (SPEC V388d, V15, I121, B94)"
}
if ($rem388 -and $rem388 -match 'setField\(') { $v388Bad += "(d) mfRemove writes with setField - it MARKS and confirms later, and a write here is the second owner of one gesture (SPEC I122b, V333e, V135)" }

# (a)/(b) every root the templates carry is REACHABLE by clearRowDots: declared as a trait, or
# on the named field-only roster. speciality rides in through XP_TRAIT with first=last=1 - its
# single dot is the storyteller's FREE stamp, and the user asked for it to go with the row
# (2026-09-02): a stamp outliving its line makes the next specialty typed there born free.
foreach ($root388 in ($dotRoots388.Keys | Sort-Object)) {
    if ($V388_FIELD_ONLY -contains $root388) { continue }
    # Declared EITHER by name or through XP_NUMINA, whose two groups hand declareTrait their
    # base at runtime - matching only the literal call reported numina and psychic, both
    # declared, as uncovered (SPEC V209).
    $decl388 = ($rootTxt -match ('declareTrait\("' + [regex]::Escape($root388) + '_"')) -or ($rootTxt -match ('base = "' + [regex]::Escape($root388) + '"'))
    if (-not $decl388) {
        $v388Bad += "(a) root '$root388' (drawn by $($dotRoots388[$root388])) carries dots and is declared to no trait - clearRowDots reaches a row through XP_TRAIT, so this family's dots would survive every remove (SPEC V388a, V388g)"
    }
}

if ($v388Bad) { foreach ($b in $v388Bad) { Fail "V388 $b" } }
else { Pass "V388 all $($dotRoots388.Count) picker root(s) carrying dots are reachable by clearRowDots, whose range comes from XP_TRAIT, and mfConfirm alone clears a row" }

# ---- V392: the Traits tab is three closing lines, and one exception ---------------------
# SPEC V392, I134j, I134k, T901, T902. V332(a) is a SNAPSHOT of this tab - it catches any drift
# at all, which is what it is for - and this is the RULE that says which of those rects would be
# right. The two are not the same job: a snapshot re-measured after a careless edit certifies the
# careless edit, and this leg is what would refuse it.
#
# The user asked for three things on 2026-09-02 and they are (a), (b) and (c): MERITS and FLAWS
# the same height with FLAWS closing where BACKGROUNDS closes; every box right of them one size,
# with the bottom row closing on that same line; and DERANGEMENTS out of that count, because it
# is the one box on the tab holding a long list rather than a note.
$v392Bad = @()
$doc392   = Doc (Join-Path $dir "WoD20.2.lfm")
$byTitle392 = @{}
foreach ($b392 in $doc392.SelectNodes("//scrollBox/layout")) {
    if ($b392.HasAttribute("name")) { continue }
    $ttl392 = $b392.SelectSingleNode("label")
    if ($null -eq $ttl392) { continue }
    $t392 = $ttl392.GetAttribute("text")
    if ($t392 -eq '') { continue }
    $byTitle392[$t392] = [pscustomobject]@{
        L = [int]$b392.GetAttribute("left");  T = [int]$b392.GetAttribute("top")
        W = [int]$b392.GetAttribute("width"); H = [int]$b392.GetAttribute("height")
    }
}
# MERITS and FLAWS are titled by their COLUMN header, `MERITS` and `FLAWS` (upper case since T958), not by a box title -
# they are tables, and the header is the first label in the box. Named here rather than derived
# because getting the wrong two boxes would make every leg below measure the wrong thing.
$merit392 = $byTitle392['MERITS']
$flaw392  = $byTitle392['FLAWS']
$bg392    = $byTitle392['BACKGROUNDS']
$der392   = $byTitle392['DERANGEMENTS']
if ($null -eq $merit392 -or $null -eq $flaw392 -or $null -eq $bg392 -or $null -eq $der392) {
    $v392Bad += "one of MERITS / FLAWS / BACKGROUNDS / DERANGEMENTS was not found on the Traits tab by its title - every leg here is measured against those four and this run found none of them (SPEC V209, B7)"
} else {
    $floor392 = $bg392.T + $bg392.H

    # (a) MERITS and FLAWS are one height.
    if ($merit392.H -ne $flaw392.H) {
        $v392Bad += "(a) MERITS is $($merit392.H)px tall and FLAWS $($flaw392.H) - the user asked for the two tables to be the same size vertically (SPEC I134j)"
    }
    # (c) FLAWS closes where BACKGROUNDS closes.
    if (($flaw392.T + $flaw392.H) -ne $floor392) {
        $v392Bad += "(c) FLAWS closes at $($flaw392.T + $flaw392.H) and BACKGROUNDS at $floor392 - the two ends were asked to coincide, and they are what makes the left of this tab read as one block (SPEC I134j)"
    }

    # (b) every 225-wide box but DERANGEMENTS is ONE height. Derived: the ten are whichever
    # height the column agrees on, and the odd one out has to be DERANGEMENTS BY NAME - an
    # exception found by arithmetic would absorb the next box that drifted (SPEC V328d).
    $col392 = @($doc392.SelectNodes("//scrollBox/layout") | Where-Object {
        -not $_.HasAttribute("name") -and [int]$_.GetAttribute("width") -eq 225
    } | ForEach-Object {
        $tt = $_.SelectSingleNode("label")
        [pscustomobject]@{
            N = if ($null -ne $tt) { $tt.GetAttribute("text") } else { '' }
            T = [int]$_.GetAttribute("top"); H = [int]$_.GetAttribute("height")
        }
    })
    if ($col392.Count -lt 4) {
        $v392Bad += "(b) only $($col392.Count) box(es) stand right of MERITS and FLAWS - this leg is reading a column that is not there (SPEC V209, V20)"
    } else {
        $ten392 = @($col392 | Where-Object { $_.N -ne 'DERANGEMENTS' })
        $h392   = @($ten392 | ForEach-Object { $_.H } | Sort-Object -Unique)
        if ($h392.Count -ne 1) {
            $bad392 = @($ten392 | Group-Object H | Sort-Object Count | ForEach-Object { "$($_.Name)px: $(($_.Group | ForEach-Object { $_.N }) -join ', ')" })
            $v392Bad += "(b) the boxes right of MERITS and FLAWS come in $($h392.Count) heights - $($bad392 -join ' | ') - and the user asked every one of them (DERANGEMENTS excepted) to be the same size (SPEC I134k)"
        }
        # (d) DERANGEMENTS is the exception and has to BE one - a DERANGEMENTS that quietly took
        # the common height would leave this leg true and the exception meaningless (SPEC V209).
        if ($h392.Count -eq 1 -and $der392.H -eq $h392[0]) {
            $v392Bad += "(d) DERANGEMENTS is $($der392.H)px, the same as the ten it is excepted from - either it stopped needing the exception, in which case the exception goes, or the ten drifted onto it (SPEC I134k, V209)"
        }
        # (c) again, for the column: the LAST box of each of the three stacks closes on the floor.
        foreach ($cl392 in @(910, 1140, 1370)) {
            $stack392 = @($doc392.SelectNodes("//scrollBox/layout") | Where-Object {
                -not $_.HasAttribute("name") -and [int]$_.GetAttribute("left") -eq $cl392
            } | ForEach-Object { [int]$_.GetAttribute("top") + [int]$_.GetAttribute("height") })
            if ($stack392.Count -eq 0) { $v392Bad += "(c) no box opens at x=$cl392 - the three right-hand columns of I134k are not all there (SPEC V209)"; continue }
            $low392 = ($stack392 | Measure-Object -Maximum).Maximum
            if ($low392 -ne $floor392) {
                $v392Bad += "(c) the column at x=$cl392 ends at $low392 and BACKGROUNDS closes at $floor392 - the user asked the bottom boxes to line up with the end of BACKGROUNDS (SPEC I134k)"
            }
        }
    }
}
if ($v392Bad) { foreach ($b392 in $v392Bad) { Fail "V392 $b392" } }
else { Pass "V392 the Traits tab keeps MERITS and FLAWS one height closing with BACKGROUNDS, its ten right-hand boxes one height with DERANGEMENTS the named exception, and all three right columns ending on the same line" }

# ---- V395: a renamed road value has a MIGRATION, and it is a WRITE ---------------------
# SPEC V395, I135b, I135d, T906. T906 renamed the default road, and a rename of a canonical VALUE
# is the one edit on this sheet that can strand DATA: V2 protects field NAMES, and nothing before
# this protected the strings saved inside them. A sheet where the player picked the old name opens
# holding a road the list no longer offers, BEARING has no row for it and ROAD_ERA filters it out
# of every era - the line goes blank and the field quietly holds a value nothing can reach.
#
# MEASURED 2026-09-02: deleting the migration branch left the gate GREEN. Every other leg of T906
# reddened under mutation and this one had nothing behind it, which is exactly the vacancy V20
# exists to refuse.
#
# (a) the migration is a WRITE. A read-side fallback would paint the line right and leave the
# stale value in the NDB for the next reader to trip on, and there is more than one reader
# (BEARING, ROAD_ERA, the era filter, the label) - which is the many-owners shape V135 refuses.
# (b) the old name is offered NOWHERE as a road any more, so the migration is the only thing that
# can resolve it. If it were still in the list, a stranded sheet would look fine and the rename
# would be half done in a way nobody sees.
$v395Bad = @()
# The write goes through ROAD_DEFAULT and not a literal since T876: Lua had the value spelled
# in three places too (the migration, the fallback, and what Remove means), so the constant is
# the same B70 fix this file made on its own side one task earlier.
$mig395 = [regex]::Match($rootLua, 'if\s+ROAD_RETIRED\[road\]\s+then\s*\r?\n\s*setField\("road",\s*(\S+)\)')
# V328(c) moved here in T876 (SPEC I135a). It used to read cboRoad's own items, and cboRoad is a
# picker button now - a widget with no items to read. The list it offers is PICKER_LIST["road"],
# which is what $roadItems329 already holds, so the leg reads the SOURCE instead of one widget's
# view of it and is stronger for the move: it would catch the same drift with no combo on the
# sheet at all. The rule itself is untouched and the user re-confirmed it on 2026-09-02 while
# the conversion was being planned: a character with no road IS on the default, so the road list
# carries no empty item and Remove RETURNS it rather than clearing it (SPEC I135a, I135d, V15).
if ($roadItems329.Count -eq 0) { $v329Bad += "PICKER_LIST['road'] parsed to zero items - V328(c) would certify any list at all (SPEC V209, V20)" }
else {
    if ($roadItems329[0] -ne $ROAD_DEFAULT) { $v329Bad += "the road list leads on '$($roadItems329[0])', not the $ROAD_DEFAULT I99b declares - it is the V15 exception, and the first item is what a sheet with no pick shows (SPEC V328c)" }
    if ($roadItems329 -contains '') { $v329Bad += "the road list carries the empty item - road is the ONE picker on the sheet that has no empty state, because a character with no road is on the default (SPEC V15, V328c, I135a)" }
}

if (-not $mig395.Success) {
    $v395Bad += "(a) renderBearing carries no migration branch - a sheet that picked one of the retired roads ($($ROAD_RETIRED -join ', ')) opens holding a road the list no longer offers, with no BEARING row and filtered out of every era, and NOTHING on screen says so (SPEC I135d, V2)"
} else {
    if ($mig395.Groups[1].Value -notin @("ROAD_DEFAULT", ("""" + $ROAD_DEFAULT + """"))) { $v395Bad += "(a) the migration writes '$($mig395.Groups[1].Value)', not the default '$ROAD_DEFAULT' (or the ROAD_DEFAULT that holds it) - the user's rule is that a road with no usable value BECOMES the default, and Remove says the same sentence (SPEC I135d, V135)" }
}
# The branch tests a TABLE and not a name since T932, because the second retirement arrived: two
# `if`s spelling the same setField would be one rule in two places, which is what B70 cured on the
# default itself. So the table has to be measured too - a name that left the list and never reached
# it migrates for NOBODY, and the branch above would go on matching while that sheet stayed broken.
$ret395 = [regex]::Match($rootLua, '(?s)\n\t{4}ROAD_RETIRED = \{(.*?)\n\t{4}\};')
$retKeys395 = @()
if ($ret395.Success) { foreach ($m395 in [regex]::Matches($ret395.Groups[1].Value, '\["([^"]+)"\]\s*=\s*true')) { $retKeys395 += $m395.Groups[1].Value } }
if (-not $ret395.Success -or $retKeys395.Count -eq 0) {
    $v395Bad += "(a) ROAD_RETIRED is not declared in Lua, or parsed to zero names - the migration branch reads it, so an empty table fires for nobody and this rule would certify a retirement that migrates nothing (SPEC I135d, V20, B70)"
} else {
    foreach ($r395 in $ROAD_RETIRED) { if ($retKeys395 -notcontains $r395) { $v395Bad += "(a) '$r395' was retired from the road list and ROAD_RETIRED does not carry it - a sheet holding it opens on a road nothing offers, with no BEARING row and filtered out of every era (SPEC I135d, V2)" } }
    foreach ($k395 in $retKeys395) { if ($ROAD_RETIRED -notcontains $k395) { $v395Bad += "(a) ROAD_RETIRED carries '$k395' and the roster here does not - either a retirement nobody wrote down, or a line left standing after the value came back to the list (SPEC V20, B70)" } }
}
$live395 = @()
foreach ($r395 in $ROAD_RETIRED) {
    if ($roadItems329 -contains $r395) { $live395 += "'$r395' in the picker list" }
    if ($bearKeys329.ContainsKey($r395)) { $live395 += "'$r395' in BEARING" }
    if ($eraKeys329.ContainsKey($r395)) { $live395 += "'$r395' in ROAD_ERA" }
}
# Lua's own single owner for the value, and it has to BE the default: three readers hang off it
# (the migration, the fallback, Remove) and a constant that drifted would move all three at once
# in a direction nobody asked for (SPEC I135d, B70).
$rd876 = [regex]::Match($rootLua, 'ROAD_DEFAULT\s*=\s*"([^"]*)"')
if (-not $rd876.Success) { $v395Bad += "(a) ROAD_DEFAULT is not declared in Lua - the migration, the fallback and what Remove means would each carry the literal again (SPEC B70, I135d)" }
elseif ($rd876.Groups[1].Value -ne $ROAD_DEFAULT) { $v395Bad += "(a) Lua declares ROAD_DEFAULT as '$($rd876.Groups[1].Value)' and the gate expects '$ROAD_DEFAULT' - the sheet and the ruler disagree about which road a character with no pick is on (SPEC I135b)" }
if ($live395.Count -gt 0) {
    $v395Bad += "(b) a retired name is still live - $($live395 -join ', ') - the retirement is half done, and a sheet stranded on that name would look correct while holding a value the list stopped meaning (SPEC I135b, T906, T932)"
}
if ($v395Bad) { foreach ($b395 in $v395Bad) { Fail "V395 $b395" } }
else { Pass "V395 the road retirements carry their migration - the $($ROAD_RETIRED.Count) retired name(s) ($($ROAD_RETIRED -join ', ')) are live nowhere, ROAD_RETIRED lists exactly those, and renderBearing WRITES '$ROAD_DEFAULT' over them once, on load" }
# ---- V434: a renamed clan VALUE carries its migration, and the old name lives nowhere -----
# SPEC V434, I149c, T973. V395 again, for the clan list: `clanFamily` stores the STRING the
# picker wrote, so renaming a value of PICKER_LIST["clan"] strands every sheet holding it
# unless ONE branch writes the new name back. (a) the branch is a WRITE (setField) at the top
# of renderClanFamilyButton, never a read-side alias - CLANS, the PT map, descClan and the
# weakness box all read the field, and an alias would have to live in each of them (V135).
# (b) the old name is offered NOWHERE: the list, CLANS, the PT map, both halves of the .lang,
# the keys of descClan_en/pt and column 1 of the research tsv - a stranded sheet would look
# right while holding a value nothing else means. (c) the new name lives in ALL of those, with
# exactly one .lang line per half. (d) the roster here and the Lua table agree key by key,
# both ways (the shape of $ROAD_RETIRED). (e) zero-guard. (f) the field `clan` of the Main
# tab (cboClan, dormant since T432, V173) is NOT migrated - it has no reader; the day it
# wakes, the branch gains a sibling on its link.
$CLAN_RENAMED = @{ 'Wu Zao' = 'Salubri (Wu Zao / Watchers)'; 'Mariners' = 'Gangrel (Marine)' }
$v434Bad = @()
$ren434 = [regex]::Match($rootLua, '(?s)\n\t{4}CLAN_RENAMED = \{(.*?)\n\t{4}\};')
$renLua434 = @{}
if ($ren434.Success) { foreach ($m434 in [regex]::Matches($ren434.Groups[1].Value, '\["([^"]+)"\]\s*=\s*"([^"]+)"')) { $renLua434[$m434.Groups[1].Value] = $m434.Groups[2].Value } }
if (-not $ren434.Success -or $renLua434.Count -eq 0) {
    $v434Bad += "(e) CLAN_RENAMED is not declared in Lua, or parsed to zero pairs - the branch reads it, so an empty table migrates nobody and this rule would certify a rename that strands every saved sheet (SPEC V434a, V20, V209)"
} else {
    foreach ($k434 in $CLAN_RENAMED.Keys) {
        if (-not $renLua434.ContainsKey($k434)) { $v434Bad += "(d) '$k434' was renamed in the clan list and CLAN_RENAMED does not carry it - a sheet holding it opens on a name nothing offers (SPEC V434a, V434d)" }
        elseif ($renLua434[$k434] -ne $CLAN_RENAMED[$k434]) { $v434Bad += "(d) CLAN_RENAMED maps '$k434' to '$($renLua434[$k434])' and the roster here says '$($CLAN_RENAMED[$k434])' - the sheet and the ruler disagree about where that sheet goes (SPEC V434d)" }
    }
    foreach ($k434 in $renLua434.Keys) { if (-not $CLAN_RENAMED.ContainsKey($k434)) { $v434Bad += "(d) CLAN_RENAMED carries '$k434' and the roster here does not - either a rename nobody wrote down, or a line left standing after the value came back (SPEC V434d)" } }
}
$fn434 = [regex]::Match($rootLua, '(?s)function renderClanFamilyButton\(from\)(.*?)\n\t{3}end;')
if (-not $fn434.Success) { $v434Bad += "(e) renderClanFamilyButton was not found in the root Lua - the migration branch has nowhere to live (SPEC V434a, V209)" }
elseif ($fn434.Groups[1].Value -notmatch 'if\s+v\s*~=\s*nil\s+and\s+CLAN_RENAMED\[v\]\s*~=\s*nil\s+then\s*\r?\n\s*setField\("clanFamily",\s*CLAN_RENAMED\[v\]\)') {
    $v434Bad += "(a) renderClanFamilyButton carries no migration branch - a sheet that saved one of the renamed clans ($($CLAN_RENAMED.Keys -join ', ')) opens holding a name the list no longer offers, with no CLANS row, no translation and no description (SPEC V434a, V395a)"
}
$w6txt434  = [System.IO.File]::ReadAllText((Join-Path $dir "WoD20.6.lfm"))
$lang434   = [System.IO.File]::ReadAllText($langFile)
$descEn434 = [System.IO.File]::ReadAllText((Join-Path $plugin "descClan_en.lua"))
$descPt434 = [System.IO.File]::ReadAllText((Join-Path $plugin "descClan_pt.lua"))
$tsv434 = @(@('clan_body_en.tsv', 'clan_body_pt.tsv', 'clan_disc.tsv', 'clan_weakness.tsv') | ForEach-Object { Join-Path $PSScriptRoot "research\$_" } | Where-Object { Test-Path $_ })
$clanOnly434 = @(); if ($PICKER.ContainsKey('clan')) { $clanOnly434 = @($PICKER['clan']) }
$clansKeys434 = @{}
$clansBody434 = [regex]::Match($rootLua, '(?s)\n\t{3}local CLANS = \{(.*?)\n\t{3}\};')
if ($clansBody434.Success) { foreach ($m434 in [regex]::Matches($clansBody434.Groups[1].Value, '(?m)^\s*\["([^"]+)"\]\s*=')) { $clansKeys434[$m434.Groups[1].Value] = $true } }
if ($clanOnly434.Count -eq 0 -or $clansKeys434.Count -eq 0) { $v434Bad += "(e) PICKER_LIST['clan'] or CLANS parsed to zero names - legs (b) and (c) would pass on nothing (SPEC V20, V209)" }
function Sites434($name) {
    $hits = @()
    $rx = [regex]::Escape($name)
    if ($clanOnly434 -contains $name) { $hits += 'PICKER_LIST[clan]' }
    if ($clansKeys434.ContainsKey($name)) { $hits += 'CLANS' }
    if ($w6txt434 -match ('\["' + $rx + '"\]\s*=')) { $hits += 'the PT map' }
    $n = ([regex]::Matches($lang434, '(?m)^wod\.' + $rx + '=')).Count
    if ($n -ge 1) { $hits += "the .lang x$n" }
    if ($descEn434 -match ('(?m)^\s*\["' + $rx + '"\]\s*=')) { $hits += 'descClan_en' }
    if ($descPt434 -match ('(?m)^\s*\["' + $rx + '"\]\s*=')) { $hits += 'descClan_pt' }
    foreach ($t434 in $tsv434) { if ([System.IO.File]::ReadAllText($t434) -match ('(?m)^' + $rx + "`t")) { $hits += (Split-Path $t434 -Leaf) } }
    return $hits
}
$want434 = 6 + $tsv434.Count
foreach ($old434 in @($CLAN_RENAMED.Keys)) {
    $live434 = @(Sites434 $old434)
    if ($live434.Count -gt 0) { $v434Bad += "(b) the old name '$old434' is still live in $($live434 -join ', ') - the rename is half done, and a sheet stranded on it would look correct while holding a value the list stopped meaning (SPEC V434b)" }
    $new434 = $CLAN_RENAMED[$old434]
    $has434 = @(Sites434 $new434)
    $langN434 = ([regex]::Matches($lang434, '(?m)^wod\.' + [regex]::Escape($new434) + '=')).Count
    if ($has434.Count -lt $want434) { $v434Bad += "(c) the new name '$new434' lives in $($has434.Count) of $want434 sites ($($has434 -join ', ')) - a reader that misses it shows the raw key, or nothing (SPEC V434c)" }
    elseif ($langN434 -ne 2) { $v434Bad += "(c) the new name '$new434' has $langN434 line(s) in the .lang and needs exactly 2, one per half (SPEC V434c, V9, V22)" }
}
if ($v434Bad) { foreach ($b434 in $v434Bad) { Fail "V434 $b434" } }
else { Pass "V434 the $($CLAN_RENAMED.Count) renamed clan value(s) carry their migration - CLAN_RENAMED and the roster agree, renderClanFamilyButton WRITES the new name, the old names are live nowhere and the new ones live in all $want434 sites" }
# ---- V409: descRoad is FOUR blocks, a FUSED table, and ONE aura name ---------------------
# SPEC V409, I141, I141a, I141b, I141e, I141g, T931. This is V192/V273 on the new module plus
# two legs neither of them has, because no other list on the sheet INHERITS: a Path shows the
# Road's hierarchy of sins with its OWN levels substituted in (I141e), and the aura it prints
# is the mother's (I141g).
#
# COVERAGE IS DELIBERATELY NOT HERE, and SPEC V409 says so in its own text: V408 already asks
# "does every picker name have an entry" for every (list, kind) with a module on disk, and Road
# stopped being exempt the moment descRoad_en.lua landed. A second coverage rule here would be
# exactly the duplicate B137 cost.
#
# Everything below is authored in ASCII on purpose: this file is patched byte for byte through
# Latin-1 to keep the fourteen UTF-8 bytes already in it, so the em dash of a level line and the
# accented "pag." come in through [char] rather than as literals.
$v409Bad = @()
$EMDASH409 = [char]0x2014
$mod409 = @{ en = (Join-Path $plugin 'descRoad_en.lua'); pt = (Join-Path $plugin 'descRoad_pt.lua') }
if (-not (Test-Path -LiteralPath $mod409['en'])) {
    Pass 'V409 descRoad_en.lua is not on disk yet - the form of I141 has nothing to measure (SPEC T931)'
} else {
    # (b) reads the =<mother> column of research/road_bearing.tsv, which is where the inheritance
    # of I141d is written down. No file, no leg - and saying so beats certifying nothing.
    $mother409 = @{}
    $bearTsv409 = Join-Path $PSScriptRoot 'research\road_bearing.tsv'
    if (-not (Test-Path -LiteralPath $bearTsv409)) {
        $v409Bad += "(b) research/road_bearing.tsv is gone - the =<mother> column is the ONLY place the aura inheritance is written down, and without it the fusion leg would certify a child with any number of levels at all (SPEC I141d, V20)"
    } else {
        foreach ($l409 in ([System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($bearTsv409)) -split "`r?`n")) {
            if ($l409.Trim() -eq '' -or $l409.StartsWith('#')) { continue }
            $c409 = $l409 -split "`t"
            if ($c409.Count -ge 2 -and $c409[1].StartsWith('=')) { $mother409[$c409[0]] = $c409[1].Substring(1) }
        }
        if ($mother409.Count -eq 0) { $v409Bad += "(b) road_bearing.tsv carries no =<mother> row - the fusion leg would measure nothing, and the fusion of I141e is the most likely way this whole list breaks (SPEC V20)" }
    }

    foreach ($lang409 in @('en', 'pt')) {
        $path409 = $mod409[$lang409]
        if (-not (Test-Path -LiteralPath $path409)) { $v409Bad += "descRoad_$lang409.lua is missing while its twin is on disk - one module PER LANGUAGE, both halves or neither (SPEC V210, I141i)"; continue }
        $ent409 = DescEntries $path409 'ROAD_DESC' $lang409
        if ($null -eq $ent409 -or @($ent409).Count -eq 0) { $v409Bad += "descRoad_$lang409.lua parsed to ZERO entries - every leg below would pass on nothing (SPEC V209, V20)"; continue }
        $pfx409 = 'p. '
        if ($lang409 -eq 'pt') { $pfx409 = 'p' + [char]0x00E1 + 'g. ' }
        $lvl409 = @{}
        $aura409 = @{}
        foreach ($e409 in $ent409) {
            $b409 = $e409.Body -split "`n`n`n"
            # (a) FORM. Four blocks, not three: I141a splits the third block of I21 into the aura
            # and the sins table, and the separator is the same two blank lines (Q56).
            if ($b409.Count -ne 4) {
                $v409Bad += "(a) [$lang409] '$($e409.Key)' holds $($b409.Count) block(s), not the FOUR I141a asks for - the third block of I21 splits into the aura and the hierarchy of sins, separated by the same two blank lines (SPEC I141a, I21, Q56)"
                continue
            }
            if ($b409[0] -notmatch (',\s*' + [regex]::Escape($pfx409) + '\d')) { $v409Bad += "(a) [$lang409] '$($e409.Key)' opens on '$($b409[0])' and not '<Book>, $pfx409<n>' - the citation is the one thing a player can carry back to the book, and only the p./pag. translates (SPEC I141a, I21, I100e)" }
            if ($b409[1] -cne $e409.Key) { $v409Bad += "(a) [$lang409] '$($e409.Key)' has '$($b409[1])' as block 2 - block 2 is the KEY verbatim, which is the value the sheet SAVES, and it does not translate (SPEC V24, I141a, I142e)" }
            if ($b409[2] -notmatch '^Aura: \S') { $v409Bad += "(a) [$lang409] '$($e409.Key)' block 3 does not open on 'Aura: ' - it is the block I141b defines, and the label is what tells the player which number the road drives (SPEC I141b)" }
            $lv409 = @([regex]::Matches($b409[3], '(?m)^(10|[1-9]) ' + $EMDASH409 + ' '))
            if ($lv409.Count -eq 0) { $v409Bad += "(a) [$lang409] '$($e409.Key)' block 4 carries no level line - the hierarchy of sins IS the block, and a heading over nothing is worse than no block (SPEC I141c)" }
            $lvl409[$e409.Key] = $lv409.Count
            $am409 = [regex]::Match($b409[2], '^Aura: (.+?)\.\s')
            if ($am409.Success) { $aura409[$e409.Key] = $am409.Groups[1].Value }
        }

        # (c) ONE identity for the aura. The name block 3 prints is the string BEARING already
        # shows on the ROAD line of the Main tab - two spellings for the same aura two fingers
        # apart is the box contradicting the sheet (SPEC I141b). On the pt half the comparison
        # goes through the sheet's OWN PT map, because that is what the ROAD line renders there:
        # translating it anywhere else would be the second identity this leg exists to refuse.
        foreach ($k409 in ($aura409.Keys | Sort-Object)) {
            if (-not $bearKeys329.ContainsKey($k409)) { continue }
            $want409 = $bearKeys329[$k409]
            if ($lang409 -eq 'pt' -and $ptVal.ContainsKey($want409)) { $want409 = $ptVal[$want409] }
            if ($aura409[$k409] -cne $want409) { $v409Bad += "(c) [$lang409] '$($k409)' prints the aura as '$($aura409[$k409])' and BEARING says '$want409' - the box would contradict the ROAD line of the Main tab two fingers away (SPEC I141b, I141g, V329c)" }
        }

        # (b) FUSION. A child carries the mother's table with its own levels substituted, BAKED
        # into the string (I141e, I141f), so it shows the SAME number of levels she does. A child
        # with fewer is the fusion that did not run, and it is the likeliest failure of this list.
        foreach ($k409 in ($mother409.Keys | Sort-Object)) {
            if (-not $lvl409.ContainsKey($k409)) { continue }
            $mk409 = $mother409[$k409]
            if (-not $lvl409.ContainsKey($mk409)) { $v409Bad += "(b) [$lang409] '$k409' inherits from '$mk409' and descRoad has no entry for the mother - the fusion had nothing to fuse onto (SPEC I141d, I141e)"; continue }
            if ($lvl409[$k409] -ne $lvl409[$mk409]) { $v409Bad += "(b) [$lang409] '$k409' shows $($lvl409[$k409]) level(s) and its mother '$mk409' shows $($lvl409[$mk409]) - a Path takes the Road's table with its OWN levels substituted, so it shows every level she does (SPEC I141e, I141f)" }
        }
    }
    if ($v409Bad) { foreach ($b409x in $v409Bad) { Fail "V409 $b409x" } }
    else { Pass "V409 descRoad carries four blocks per entry in both halves, every child shows its mother's full table, and every aura name is the one BEARING prints" }
}
# ---- V389: popScrim covers the TALLEST tab, not just the widest ------------------------
# SPEC V389, B124, I102a. The scrim is the one way a click outside the ? box closes it, and it
# only closes what it covers. I102a wrote the risk down on 2026-08-31 naming TWO axes - "a strip
# of the tab that does not close the overlay" - and the check that came out of it (V375) measured
# the WIDTH. Nothing ever measured the height, and the height was wrong the whole time: the scrim
# is authored 733 tall, which is the 40px strip plus the 693 the Traits grid covers, while the
# MAIN tab closes at 888. A click in the bottom 195px of Main did not close the box, and the gate
# was green (SPEC B124).
#
# DERIVED on both terms, because a literal here would be the THIRD owner of a tab's floor - the
# boxes own it, V376/V69 read it, and this would copy it (SPEC B70). The floor is swept off every
# content form the same way V286 sweeps them, and the strip's own height comes off the strip.
$v389Bad = @()
$rootDoc389 = Doc (Join-Path $dir 'WoD20th.lfm')
$scrim389 = @($rootDoc389.SelectNodes("//rectangle[@name='popScrim']"))[0]
$strip389 = $rootDoc389.SelectSingleNode("//layout[@name='tabStrip']")

if ($null -eq $scrim389) { $v389Bad += "popScrim is gone from WoD20th.lfm - there is no scrim to measure and no second closer either (SPEC I102a, V333e, V209)" }
elseif ($null -eq $strip389) { $v389Bad += "tabStrip is gone from WoD20th.lfm - the scrim's origin sits above it and this leg has no offset to add (SPEC V209, I102a)" }
else {
    $stripH389 = 0
    if (-not [int]::TryParse($strip389.GetAttribute("height"), [ref]$stripH389) -or $stripH389 -le 0) {
        $v389Bad += "tabStrip declares no usable height - the scrim's offset would be guessed (SPEC V209)"
    }
    # The deepest y any tab reaches, over every content form, read off the top-level boxes the
    # same way V286 reads them. ContentRoot is what makes this survive T895: three of the forms
    # stopped being scrollBoxes and not one of their boxes moved.
    $deep389 = 0; $deepFile389 = ''
    $forms389 = 0
    foreach ($f389 in $files) {
        if ($f389.Name -eq 'WoD20th.lfm') { continue }
        $cr389 = ContentRoot (Doc $f389.FullName)
        if ($null -eq $cr389) { continue }
        $forms389++
        foreach ($b389 in $cr389.SelectNodes("layout")) {
            $bt389 = 0; $bh389 = 0
            if (-not [int]::TryParse($b389.GetAttribute("top"), [ref]$bt389)) { continue }
            [void][int]::TryParse($b389.GetAttribute("height"), [ref]$bh389)
            if (($bt389 + $bh389) -gt $deep389) { $deep389 = $bt389 + $bh389; $deepFile389 = $f389.Name }
        }
    }
    if ($forms389 -eq 0 -or $deep389 -eq 0) {
        $v389Bad += "no content form was swept for its floor - this leg would certify a scrim of any height at all (SPEC V209, V20)"
    } else {
        $needH389 = $deep389 + $stripH389
        $haveH389 = 0
        [void][int]::TryParse($scrim389.GetAttribute("height"), [ref]$haveH389)
        if ($haveH389 -lt $needH389) {
            $v389Bad += "popScrim is ${haveH389}px tall against a sheet whose deepest tab closes at $deep389 ($deepFile389) under a ${stripH389}px strip - the bottom $($needH389 - $haveH389)px of that tab do not close the overlay, and a click that only fails in one band of one tab has no symptom anybody reports (SPEC I102a, B124)"
        }
    }
}
if ($v389Bad) { foreach ($b389 in $v389Bad) { Fail "V389 $b389" } }
else { Pass "V389 popScrim reaches the floor of the deepest tab plus the strip above it - every click outside the box closes it, on every tab" }

# ---- V396: popScrim covers the WIDEST tab, and not one tab named in the gate ----------
# SPEC V396, B127, B129, I136g. V375 leg (d) measured the scrim against the right edge of the
# MAIN tab's EXPERIENCE box. That is a ruler of MAXIMUM written as an equality against ONE
# member, and it never measured what it claimed: MEASURED while building T908, Traits closes at
# 1595 and Main carried 1680, so the leg certified 85px of slack as exact. It was green because
# Main happened to be WIDER than the widest, which is a different sentence. T908 takes Main to
# 1345, at which point the same leg would DEMAND a scrim 250px too narrow for Traits - B124 on
# the other axis, and with the same silence. Same sweep as V389 and for the same reason: a
# literal here would be a third owner of the sheet's own width (SPEC B70).
$v396Bad = @()
if ($null -eq $scrim389) { $v396Bad += "popScrim is gone from WoD20th.lfm - there is no scrim to measure (SPEC I102a, V209)" }
else {
    $wide396 = 0; $wideFile396 = ''
    $forms396 = 0
    foreach ($f396 in $files) {
        if ($f396.Name -eq 'WoD20th.lfm') { continue }
        $cr396 = ContentRoot (Doc $f396.FullName)
        if ($null -eq $cr396) { continue }
        $forms396++
        foreach ($b396 in $cr396.SelectNodes("layout")) {
            $bl396 = 0; $bw396 = 0
            if (-not [int]::TryParse($b396.GetAttribute("left"), [ref]$bl396)) { continue }
            [void][int]::TryParse($b396.GetAttribute("width"), [ref]$bw396)
            if (($bl396 + $bw396) -gt $wide396) { $wide396 = $bl396 + $bw396; $wideFile396 = $f396.Name }
        }
    }
    if ($forms396 -eq 0 -or $wide396 -eq 0) {
        $v396Bad += "no content form was swept for its right edge - this leg would certify a scrim of any width at all (SPEC V209, V20)"
    } else {
        $haveW396 = 0
        [void][int]::TryParse($scrim389.GetAttribute("width"), [ref]$haveW396)
        if ($haveW396 -lt $wide396) {
            $v396Bad += "popScrim is ${haveW396}px wide against a sheet whose widest tab closes at $wide396 ($wideFile396) - the right $($wide396 - $haveW396)px of that tab do not close the overlay, and nothing outside that corner would ever show it (SPEC I102a, B127)"
        }
    }
}
if ($v396Bad) { foreach ($b396 in $v396Bad) { Fail "V396 $b396" } }
else { Pass "V396 popScrim is as wide as the widest tab, swept off every content form - the width is tied to no tab by name" }
# ---- V397: an imported form paints its OWN client rect, or it is not imported ----------
# SPEC V397, R136, R137, B128, I136h. <import> compiles to _obj_newObject("form") plus
# setAlign("client") and setTheme("dark"), so an imported form is a REAL form at runtime and
# paints the theme's own ground over the rect that holds it. gui.Form carries ten properties and
# not one of them is a colour, a background or a transparency - there is no attribute that turns
# that off. The only thing that hides it is the form painting its own client rect, which on this
# sheet means a <scrollBox> with a themePaper on it (SPEC V56).
#
# So the rule is WHERE a form may be imported, and the answer is only ONE place: the root. A tab
# form fills the whole tab, so its panel edge lands exactly on the tab edge and nobody can see
# it. A form dropped into a REGION of another form - a 466x700 column inside a 1408 tab - draws
# that same edge in the middle of the page, and THAT is the box the user kept reporting.
#
# ⚠ the first draft of this rule also demanded a scrollBox and a themePaper of every imported
# form, and it was born RED against correct code: WoD20.6 carries no paper and never needed one
# (SPEC B106). Whether a form paints the Victorian texture is V56's question; this one is only
# about where a form may be dropped.
$v397Bad = @()
$v397Seen = 0
foreach ($f397 in $files) {
    $imps397 = @((Doc $f397.FullName).SelectNodes("//import"))
    if ($f397.Name -eq 'WoD20th.lfm') {
        $v397Seen = $imps397.Count
        foreach ($imp397 in $imps397) {
            $sub397 = $imp397.GetAttribute("file")
            if (-not (Test-Path (Join-Path $dir $sub397))) { $v397Bad += "the root form imports $sub397 and it is not on disk (SPEC V209)" }
        }
        continue
    }
    foreach ($imp397 in $imps397) {
        $v397Bad += "$($f397.Name) imports $($imp397.GetAttribute('file')) - only the root form may import. A form dropped inside another form draws its own panel over the rect that holds it, gui.Form has no property that turns it off, and the sheet has no symptom for it but the box itself (SPEC R136, R137, B128, I136h)"
    }
}
if ($v397Seen -ne 11) { $v397Bad += "the root form carries $v397Seen <import>, expected the 11 tabs - with none read this rule would certify a sheet that imports nothing (SPEC V209, V20)" }
if ($v397Bad) { foreach ($b397 in $v397Bad) { Fail "V397 $b397" } }
else { Pass "V397 the 11 <import> of the sheet are all on the root form and all on disk - no form is dropped inside another, where its own panel would draw a box in the middle of the page" }
# ---- V398: the two hedge boxes are one box drawn twice --------------------------------
# SPEC V398, I136m, user 2026-09-02 item 7. HEDGE MAGIC RITUALS and HEDGE MAGIC PATHS stand side
# by side on the Numina tab and held DIFFERENT numbers of slots - 17 against 20 - which reads as
# a bug in the data rather than as a choice anybody made. Both terms are READ and neither is
# typed here: the row count off each box's own XML, and the height of one box against the other.
# A literal would be the third owner of 561 and would not follow the pair the day a row is added
# (SPEC V390, B112).
#
# The count against the LUA constant is V204's pair and is not repeated here - one owner (B70).
# What this rule adds is the relation BETWEEN the two boxes, which nothing measured before.
$v398Bad = @()
$numDoc398 = Doc (Join-Path $dir "WoD20.7.lfm")
$b398 = @{}
foreach ($ttl398 in @('HEDGE MAGIC PATHS', 'HEDGE MAGIC RITUALS')) {
    $bx398 = BoxOf $numDoc398 $ttl398
    if ($null -eq $bx398) { $v398Bad += "the box '$ttl398' is gone from WoD20.7 - this rule measured nothing (SPEC V20, V209)"; continue }
    $b398[$ttl398] = [pscustomobject]@{
        H = [int]$bx398.GetAttribute("height")
        R = @($bx398.SelectNodes("layout")).Count
    }
}
if ($b398.Count -ne 2) { $v398Bad += "only $($b398.Count) of the two hedge boxes were read - an equality over one box is true of every box ever drawn (SPEC V20, B7)" }
else {
    $pr398 = $b398['HEDGE MAGIC PATHS']
    $ri398 = $b398['HEDGE MAGIC RITUALS']
    if ($pr398.R -lt 2) { $v398Bad += "HEDGE MAGIC PATHS reads $($pr398.R) row(s) - the sweep broke and the equality below would be struck over nothing (SPEC V209, V20)" }
    elseif ($pr398.R -ne $ri398.R) { $v398Bad += "HEDGE MAGIC PATHS draws $($pr398.R) row(s) and HEDGE MAGIC RITUALS $($ri398.R) - two columns side by side with different counts read as a list that lost entries, not as a choice (SPEC I136m)" }
    elseif ($pr398.H -ne $ri398.H) { $v398Bad += "HEDGE MAGIC PATHS is $($pr398.H)px tall and HEDGE MAGIC RITUALS $($ri398.H) - same rows, same pitch, same foot, so the heights are the same number or one of the three moved on its own (SPEC I136m, V280a)" }
}
if ($v398Bad) { foreach ($b398m in $v398Bad) { Fail "V398 $b398m" } }
else { Pass "V398 HEDGE MAGIC PATHS and HEDGE MAGIC RITUALS both draw $($b398['HEDGE MAGIC PATHS'].R) rows in $($b398['HEDGE MAGIC PATHS'].H)px - the same box twice, read off the boxes and never typed here" }

# ---- V394: the TRUE FAITH row is centred as a GROUP -----------------------------------
# SPEC V394, I134g, T899. The box holds ONE row - the ?, the Religion label, the field and five
# dots - and the user asked for it centred. The word that matters is GROUP: centring each control
# on its own is what leaves a row reading crooked, because the eye reads the whole line and the
# air it sits in, not each widget's own middle. So the measurement is the SPAN: leftmost edge of
# anything in the box against the box's own left, and the rightmost against its right.
#
# The title is excluded because it is already centred by horzTextAlign over the FULL width and
# would drag the span out to the margins, making any arrangement pass. The client rectangle is
# excluded because it IS the box. What is left is exactly the row (SPEC V27 owns the title).
$v394Bad = @()
$doc394 = Doc (Join-Path $dir "WoD20.7.lfm")
$box394 = $doc394.SelectSingleNode("//scrollBox/layout[label/@text='TRUE FAITH']")
if ($null -eq $box394) {
    $v394Bad += "the TRUE FAITH box was not found by title on WoD20.7 - this check measured nothing (SPEC V209, B7)"
} else {
    $bw394 = [int]$box394.GetAttribute("width")
    $lo394 = [int]::MaxValue; $hi394 = [int]::MinValue; $seen394 = 0
    foreach ($c394 in $box394.ChildNodes) {
        if ($c394.NodeType -ne 'Element') { continue }
        if ($c394.LocalName -eq 'rectangle') { continue }
        if ($c394.LocalName -eq 'label' -and $c394.GetAttribute("text") -eq 'TRUE FAITH') { continue }
        $cl394 = 0
        if (-not [int]::TryParse($c394.GetAttribute("left"), [ref]$cl394)) { continue }
        $cw394 = 0
        [void][int]::TryParse($c394.GetAttribute("width"), [ref]$cw394)
        $seen394++
        if ($cl394 -lt $lo394) { $lo394 = $cl394 }
        if (($cl394 + $cw394) -gt $hi394) { $hi394 = $cl394 + $cw394 }
    }
    # The row is eight widgets. A count that collapses is a check certifying an empty box, which
    # is the shape B7 keeps coming back as (SPEC V209, V20).
    if ($seen394 -lt 4) {
        $v394Bad += "only $seen394 widget(s) of the TRUE FAITH row were measured - the row is the ?, the label, the field and five dots, and a span read off fewer than four of them is not the group the user asked to centre (SPEC V209)"
    } else {
        $left394  = $lo394
        $right394 = $bw394 - $hi394
        if ([Math]::Abs($left394 - $right394) -gt 1) {
            $v394Bad += "the TRUE FAITH row spans $lo394..$hi394 in a box of $bw394 - $left394 px of air on the left against $right394 on the right. The user asked for the row centred as a GROUP, and centring the controls one at a time is exactly what leaves this gap (SPEC I134g, V394)"
        }
    }
}
if ($v394Bad) { foreach ($b394 in $v394Bad) { Fail "V394 $b394" } }
else { Pass "V394 the TRUE FAITH row is centred as one group in its box, to the pixel" }

# ---- V391: no scrollBox inside a scrollBox, one theme backdrop per TAB ----------------
# SPEC V391, I134c, T895, B41. BOTH legs measure the ASSEMBLED tree, and that is the whole
# point: neither defect is visible in any single file. WoD20.12/.13/.14 each opened a
# scrollBox with a themePaper in it and each was correct ALONE - it was WoD20.11 importing
# the three INTO its own scrollBox that made the tab draw four boxes and four papers, three
# of them stretched over one column each. The user named it without opening a file ("parecem
# heranca de quando eles eram sub-abas") and no file-at-a-time check could have seen it.
#
# V56 stays where it is and is NOT widened: it asks whether a form that carries content has a
# backdrop, one form at a time, and it is derived off "does this form have a scrollBox" - so
# dropping the scrollBox in the three took them off both sides of that count on its own. This
# is the question V56 does not ask: how many land on ONE TAB once the imports are resolved.
$v391Bad = @()

# (a) an <import> whose target opens a scrollBox may not itself sit under one.
foreach ($f391 in $files) {
    foreach ($imp391 in (Doc $f391.FullName).SelectNodes("//import")) {
        $tgt391 = $imp391.GetAttribute("file")
        $tp391  = Join-Path $dir $tgt391
        if (-not (Test-Path $tp391)) { continue }
        if ($null -eq (Doc $tp391).SelectSingleNode("//scrollBox")) { continue }
        $anc391 = $imp391.ParentNode
        while ($null -ne $anc391) {
            if ($anc391.LocalName -eq 'scrollBox') {
                $v391Bad += "$($f391.Name) imports $tgt391 from inside a <scrollBox> and $tgt391 opens one of its own - the assembled tab draws a box inside a box, which is what the Ghoul tab looked like until T895 (SPEC I134c)"
                break
            }
            $anc391 = $anc391.ParentNode
        }
    }
}

# (b) at most ONE themePaper per tab, counted over the tab's transitive import closure.
function Closure391($start391) {
    $seen391 = New-Object System.Collections.Generic.List[string]
    $todo391 = New-Object System.Collections.Generic.Queue[string]
    $todo391.Enqueue($start391)
    while ($todo391.Count -gt 0) {
        $cur391 = $todo391.Dequeue()
        if ($seen391.Contains($cur391)) { continue }
        $p391 = Join-Path $dir $cur391
        if (-not (Test-Path $p391)) { continue }
        [void]$seen391.Add($cur391)
        foreach ($i391 in (Doc $p391).SelectNodes("//import")) { $todo391.Enqueue($i391.GetAttribute("file")) }
    }
    return ,$seen391.ToArray()
}

$rootDoc391 = Doc (Join-Path $dir 'WoD20th.lfm')
$tabsSeen391 = 0
foreach ($tab391 in $rootDoc391.SelectNodes("//layout[starts-with(@name,'tab')]")) {
    $imps391 = @($tab391.SelectNodes(".//import"))
    if ($imps391.Count -eq 0) { continue }
    $tabsSeen391++
    $set391 = @()
    foreach ($i391 in $imps391) { $set391 += Closure391 $i391.GetAttribute("file") }
    $papers391 = @()
    foreach ($ff391 in @($set391 | Sort-Object -Unique)) {
        foreach ($img391 in (Doc (Join-Path $dir $ff391)).SelectNodes("//image[starts-with(@name,'themePaper')]")) {
            $papers391 += "$ff391/$($img391.GetAttribute('name'))"
        }
    }
    if ($papers391.Count -gt 1) {
        $v391Bad += "tab $($tab391.GetAttribute('name')) stacks $($papers391.Count) theme backdrops ($($papers391 -join ', ')) - applyTheme lights every one of them, so the extras paint a lid over the columns they cover and read as a frame drawn around those boxes (SPEC I134c, B41)"
    }
}
# A roster that resolves to nothing is a check that cannot fail (SPEC V20, V209).
if ($tabsSeen391 -eq 0) { $v391Bad += "no tab layout in WoD20th.lfm holds an <import> - this check is reading nothing (SPEC V209)" }

if ($v391Bad) { foreach ($b391 in $v391Bad) { Fail "V391 $b391" } }
else { Pass "V391 no imported form opens a scrollBox inside one, and each of the $tabsSeen391 imported tab(s) carries at most one theme backdrop across its whole import closure" }

# ---- V410: an OPEN task does not claim a file that EXISTS -----------------------------
# SPEC V410, B140, V20, B7, T690. T690 sat at `~` with its work already finished, because the
# half it still owed had become a task under ANOTHER id: T928 delivered the fifty-two hedge
# ritual descriptions, descNumina_en.lua and descNumina_pt.lua reached the disk, and nothing
# noticed that the older row had gone stale. V408 measures list -> module, never task -> task,
# so "two rows describe one delivery" has no owner here - and it cannot get one, because that
# is a statement about MEANING and no check reads meaning.
#
# What T690 DID leave behind is checkable. It said, in writing, that there was no
# descNumina_en.lua file on the sheet. That is a claim about an ARTEFACT, it is cheap to test,
# and it went false the day the file appeared. So this rule measures the CLAIM, not the
# identity, and that is the only leg of B140 an artefact can carry.
#
# A CLOSED row is skipped on purpose: an `x` line is HISTORY and is allowed to record the world
# as it stood when it was written. Only `.` and `~` - rows that still describe work to be done -
# are read as claims about TODAY.
#
# The gate carries no BOM, so PowerShell 5.1 reads this file as ANSI and a UTF-8 literal would
# arrive mangled and MATCH NOTHING, in silence. The needle is built from codepoints for that
# reason, the same way $sect384 is (SPEC I142g).
$perp410  = [string][char]0x22A5
$ex410    = [string][char]0x2203
$head410  = $perp410 + ' ' + $ex410 + ' '
$words410 = @('arquivo', 'm' + [string][char]0x00F3 + 'dulo')
# where a sheet file can live: plugin root, the sheet folder, the repo root
$roots410 = @($plugin, $dir, $PSScriptRoot)

$specPath410  = Join-Path $PSScriptRoot 'SPEC.md'
$specLines410 = @()
if (Test-Path $specPath410) { $specLines410 = [IO.File]::ReadAllLines($specPath410, [Text.Encoding]::UTF8) }

$v410Bad    = @()
$v410Rows   = 0
$v410Claims = 0
foreach ($l410 in $specLines410) {
    if ($l410 -notmatch '^(T[0-9]+)\|[.~]\|') { continue }
    $row410 = $Matches[1]
    $v410Rows++
    foreach ($w410 in $words410) {
        $needle410 = $head410 + $w410
        $at410 = 0
        while ($true) {
            $at410 = $l410.IndexOf($needle410, $at410)
            if ($at410 -lt 0) { break }
            $p410 = $at410 + $needle410.Length
            # Only the names the phrase actually NAMES: the run of backticked tokens that
            # follows it, stopping at the first thing that is not one. A task row is long and
            # mentions other files further down; reading to end of line would flag those too.
            while ($p410 -lt $l410.Length) {
                $m410 = [regex]::Match($l410.Substring($p410), '^[\s/,&]*`([^`]+)`')
                if (-not $m410.Success) { break }
                $nm410 = $m410.Groups[1].Value
                $p410 += $m410.Length
                if ($nm410 -notmatch '^\w+\.(lua|lfm|ps1)$') { continue }
                $v410Claims++
                foreach ($r410 in $roots410) {
                    if (Test-Path -LiteralPath (Join-Path $r410 $nm410)) {
                        $v410Bad += "$row410 is OPEN and says the file '$nm410' does not exist, and it is on disk at $r410 - the row describes work that is already delivered, so its status is a lie the way T690's was (SPEC B140)"
                        break
                    }
                }
            }
            $at410 = $p410
        }
    }
}
if ($v410Rows -lt 1) { Fail "V410 not one open task row (T<n> with status . or ~) was found in SPEC.md - the parser drifted and this rule would certify a spec whose every open row lies (SPEC V20, B7)" }
elseif ($v410Bad) { foreach ($b410 in ($v410Bad | Select-Object -First 8)) { Fail "V410 $b410" } }
else { Pass "V410 none of the $v410Rows open task row(s) claims a file that is on disk ($v410Claims file claim(s) tested against 3 roots)" }

# ---- V411: the [pt] half carries ACCENTS, or the translation never happened ------------
# T931 wrote the first 120 lines of road_sins_pt.tsv in plain ASCII - "egoistas", "acao",
# "proposito" with no tilde - and this gate stayed GREEN through it. Nothing here had ever
# looked at the BODY of a [pt] module: V9 measures the opposite (authored text must be
# ENGLISH), V210 measures key PARITY between the halves, V335 measures block SHAPE. Portuguese
# with no accent in it is a translation that did not happen, and it ships looking exactly like
# one that did (SPEC V411, V411b).
#
# The roster is a GLOB and not a literal list, on purpose. V210 keeps a literal one and says in
# its own comment that a literal list is how descMerit nearly shipped unchecked - and that list
# is short TWO areas today (descNature and descRoad are on disk with their markers and are not
# in it). A rule about GENERATED files has to read what is on disk (SPEC V20).
#
# N, the ceiling of leg (a), is MEASURED and not guessed (SPEC T933, B106). Accent characters
# per entry in the eight [pt] modules on disk, 2026-09-03:
#   descNature 24.0 | descMerit 27.9 | descRoad 34.4 | descRitual 38.0
#   descNumina 55.4 | descBackground 77.5 | descPath 78.7 | descDisc 100.3
# and not ONE of the 1533 entries carries a body with zero accents. So N = 1 - at least one
# accent per entry, counted over the whole module - is the tightest value the spec's wording
# ("one accent every N entries") allows, and the worst module on disk still clears it by 24x.
# A bigger N is a weaker rule: one that passes a module 96% de-accented is the vacuous pass B7
# is named for. What this rule reaches is a module de-accented WHOLESALE, which is the defect
# that shipped; a partially de-accented one needs a per-module baseline nobody has asked for.
#
# Mutation (SPEC V20, V222): run one [pt] module through a de-accenter -> RED on (a); swap one
# accented word for an accented synonym -> GREEN, because density is not the subject.
$v411Bad = @()
$v411N = 1
$v411Acc = [regex]"[\u00E0\u00E1\u00E2\u00E3\u00E9\u00EA\u00ED\u00F3\u00F4\u00F5\u00FA\u00E7]"
$v411Mods = 0
$v411Entries = 0
$v411Worst = ""
$v411WorstRatio = [double]::MaxValue
foreach ($f411 in (Get-ChildItem -LiteralPath $plugin -Filter "desc*_pt.lua" | Sort-Object Name)) {
    $area411 = $f411.BaseName -replace "_pt$", ""
    $kind411 = $area411 -replace "^desc", ""
    if (-not $DESC_MARKER.ContainsKey($kind411)) {
        $v411Bad += "(a) $($f411.Name) is on disk and DESC_MARKER names no marker for '$kind411' - this rule cannot read a module it cannot find the region of, and skipping it silently is how a half ships unmeasured (SPEC V408, V209)"
        continue
    }
    $ent411 = DescEntries $f411.FullName $DESC_MARKER[$kind411] "pt"
    if ($null -eq $ent411) {
        $v411Bad += "(a) $($f411.Name) has no $($DESC_MARKER[$kind411]) region - half the book has nowhere to live and every count below would read zero (SPEC V209, V210)"
        continue
    }
    if ($ent411.Count -eq 0) {
        $v411Bad += "(c) $($f411.Name) parsed to zero entries - a module with nothing in it makes leg (a) true by default (SPEC V20, B7)"
        continue
    }
    $v411Mods++
    $v411Entries += $ent411.Count
    $acc411 = 0
    foreach ($e411 in $ent411) { $acc411 += $v411Acc.Matches($e411.Body).Count }
    $need411 = [math]::Ceiling($ent411.Count / $v411N)
    if ($acc411 -lt $need411) {
        $v411Bad += "(a) $($f411.Name) holds $($ent411.Count) entries and only $acc411 accented character(s), and one accent every $v411N entry/entries asks for $need411 - Portuguese this bare is a translation that never happened, and V9, V210 and V335 all stay green on it (SPEC V411a)"
    }
    $r411 = $acc411 / $ent411.Count
    if ($r411 -lt $v411WorstRatio) { $v411WorstRatio = $r411; $v411Worst = $f411.Name }
}
# (c) zero-guard on the ROSTER itself: a glob that matched nothing would make the loop above run
# over an empty set and this rule would certify a plugin with no translated half at all. The
# count is a FLOOR and not an equality - a ninth area is welcome and costs nothing here.
if ($v411Mods -lt 8) { $v411Bad += "(c) only $v411Mods [pt] description module(s) were read at the plugin root and eight were measured there on 2026-09-03 - a roster that shrank is either a deleted half or a glob that stopped matching, and both make this rule pass over nothing (SPEC V411c, V20, B7)" }
if ($v411Bad) { foreach ($b411 in $v411Bad) { Fail "V411 $b411" } }
else { Pass "V411 all $v411Mods [pt] description modules carry accented Portuguese across $v411Entries entries - thinnest is $v411Worst at $([math]::Round($v411WorstRatio,1)) accent(s) per entry against a floor of $v411N" }

# ---- V413: the Revenant row belongs to the FAMILY list and to no other -----------------
# SPEC I143d, the owner's ask of 2026-09-03. The picker asks ONE list a question of its own -
# "is this custom entry a revenant, and what is its weakness" - and the row that asks it has
# the same failure modes the Ritual Level row of I129i was built around: shown for every list
# it repaints a question nobody asked, and given a field it saves an answer the player never
# confirmed. Neither leaves a mark to grep for.
#
# Mutation (SPEC V20, V222): drop the list test from mfRevenantShown -> red on (a); give the
# combo a field -> red on (b); author the combo on `Yes` -> red on (c); swap the weakness
# pair's hide for enabled="false" -> red on (d); pin edtMfDesc.top back to a constant -> red
# on (e). Probe: rename the weakness label's text -> GREEN, the wording is not the subject.
$v413Bad = @()
$revFn   = LuaFn $root 'mfRevenantShown'
$revPane = LuaFn $root 'mfRevenantPane'
$revChg  = LuaFn $root 'mfRevenantChange'
$custPn  = LuaFn $root 'mfCustomPane'
$rootDoc = Doc (Join-Path $dir "WoD20th.lfm")
$revCmb  = @($rootDoc.SelectNodes("//comboBox[@name='cmbMfRevenant']"))
$revLbls = @($rootDoc.SelectNodes("//label[@name='lblMfRevenant']")) + @($rootDoc.SelectNodes("//label[@name='lblMfWeak']"))
$revEdt  = @($rootDoc.SelectNodes("//edit[@name='edtMfWeak']"))

if (-not $revFn)  { $v413Bad += "mfRevenantShown() is gone from the root form - the ONE question the pane and the confirm both ask has no owner, and this check measured nothing (SPEC V209, V135)" }
if (-not $revPane) { $v413Bad += "mfRevenantPane() is gone - the row has no painter (SPEC V209)" }
if (-not $revChg)  { $v413Bad += "mfRevenantChange() is gone - nothing writes MF.revenant and the combo answers to no one (SPEC V209)" }
if ($revCmb.Count -ne 1) { $v413Bad += "the Revenant combo is authored $($revCmb.Count) time(s) as cmbMfRevenant and it has to be exactly once - two would be two owners of one answer (SPEC V135, V209)" }
if ($revLbls.Count -ne 2) { $v413Bad += "the Revenant row's two labels (lblMfRevenant, lblMfWeak) are $($revLbls.Count) on the root form and there have to be two (SPEC V209)" }
if ($revEdt.Count -ne 1) { $v413Bad += "edtMfWeak is authored $($revEdt.Count) time(s) and it has to be exactly once (SPEC V209)" }

if (-not $v413Bad) {
    $revBody  = NoComments $revFn
    $paneBody = NoComments $revPane
    $custBody = NoComments $custPn
    $cmb413   = $revCmb[0]

    # (a) the test is on the LIST as well as on the custom state. Gated on custom alone the row
    # shows up in all nine lists; gated on the list alone it shows over a book entry, which has
    # nothing to be asked. The pair IS the rule, and mfLevelShown next door reads the same way.
    # The list is `clanFamily` again since T972 (SPEC V433d): the line is one picker over the
    # union, and a custom entry of that picker is the one place a revenant can be declared.
    if ($revBody -notmatch 'MF\.list\s*==\s*"clanFamily"') {
        $v413Bad += "(a) mfRevenantShown does not test MF.list against clanFamily - the row would ask its question of every list on the sheet (SPEC V413a, I143d, V433d)"
    }
    if ($revBody -notmatch 'MF\.custom') {
        $v413Bad += "(a) mfRevenantShown does not test the custom state - the row would paint over a book entry, which carries its own weakness (SPEC V413a, I129i)"
    }

    # (b) no field on either control. A field here saves an answer the player never confirmed,
    # which is the door V333e keeps shut for every other control in this box.
    foreach ($n413 in (@($cmb413) + $revEdt + $revLbls)) {
        if ($n413.HasAttribute("field")) {
            $v413Bad += "(b) '$($n413.GetAttribute('name'))' owns a field - the Revenant row is a question the open box asks and its answer rides out through MF, so a field here writes without an OK (SPEC V413b, V269a, V333e)"
        }
    }

    # (c) the default is AUTHORED. Written by Lua on open it goes missing the day the open path
    # changes, and the row would come up on Yes for a player who never said so.
    $vals413 = @([regex]::Matches($cmb413.GetAttribute("values"), "'([^']*)'") | ForEach-Object { $_.Groups[1].Value })
    if ($vals413.Count -ne 2 -or $vals413[0] -cne 'No' -or $vals413[1] -cne 'Yes') {
        $v413Bad += "(c) cmbMfRevenant offers [$($vals413 -join ', ')] and the ask names exactly No then Yes, in that order - the FIRST is what an unset combo shows, so the order is the default (SPEC V413c, V15)"
    }

    # (d) the weakness pair is HIDDEN, never disabled: invisible-and-enabled still takes Tab,
    # and that is the bug no screenshot shows.
    foreach ($n413 in ($revEdt + @($rootDoc.SelectNodes("//label[@name='lblMfWeak']")))) {
        if ($n413.GetAttribute("visible") -ne "false") {
            $v413Bad += "(d) '$($n413.GetAttribute('name'))' is not authored hidden - the box opens on a book entry far more often than on a custom one, and what is authored has to be the common case (SPEC V413d, V274c)"
        }
    }
    if ($paneBody -match 'lblMfWeak"\]\.enabled' -or $paneBody -match 'edtMfWeak"\]\.enabled') {
        $v413Bad += "(d) the weakness pair is turned off with enabled= instead of visible= - a control that is invisible and still enabled takes Tab focus, which is the bug no screenshot shows (SPEC V413d, V274c, V274d)"
    }

    # (e) edtMfDesc.top is CHOSEN per lit row and the floor does not move. A single constant is
    # a pane that ignores the rows above it; a moved floor is a pane that leaves the box.
    $tops413 = @([regex]::Matches($custBody, 'top\s*=\s*(\d+)') | ForEach-Object { $_.Groups[1].Value } | Sort-Object -Unique)
    if ($tops413.Count -lt 4) {
        $v413Bad += "(e) mfCustomPane chooses edtMfDesc's top from $($tops413.Count) value(s) and the pane now has four lit states - no custom, custom, custom with a row, custom with the weakness (SPEC V413e, I143f)"
    }
    if ($custBody -notmatch '625\s*-\s*top') {
        $v413Bad += "(e) edtMfDesc's height is no longer derived from the 625 floor - the floor is the bottom of the pane and writing the height out beside the top is the same rule in two places (SPEC V413e, B70)"
    }
    if ($custBody -notmatch 'mfRevenantPane') {
        $v413Bad += "(e) mfCustomPane never calls mfRevenantPane - the row would never light, and the pane would leave its space blank (SPEC V413e, V209)"
    }

    # (f) the row's own strings carry a [pt] key. `Revenant Weakness` is NOT checked here: the
    # sheet already ships a FLAW of that name with a translation of its own, so the label reads
    # the flaw's, and forcing a second key would be two owners of one string (SPEC I102g, V135).
    if ($embedded -ne $null -and -not $embedded.ContainsKey('Revenant')) {
        $v413Bad += "(f) the label 'Revenant' has no key in the PT map - it would show in English on a sheet set to Portuguese, which is B57 exactly (SPEC V413f, V28)"
    }
}

if ($v413Bad) { foreach ($b413 in $v413Bad) { Fail "V413 $b413" } }
else { Pass "V413 the Revenant row asks the clanFamily list alone, owns no field, is authored on No, hides rather than disables, and the desc pane starts under the last lit row" }

# ---- V412: the box shows the FAMILY's weakness, and never the clan's --------------------
# SPEC I143, V412, T936, the owner's ask of 2026-09-03. `clanFamily` is ONE list with two
# species inside it (SPEC I143b): sixty-one clans and twenty-three revenant families, and the
# two share no name. The box has to ask which species a value came from, and every other way
# of asking is a SECOND owner of a fact the value already carries - a second list, a prefix on
# the name, a saved isRevenant flag (SPEC V135, V29).
#
# Mutation (SPEC V20, V222): put a CLAN name in FAMILY_WEAKNESS -> red on (b); swap the
# pertinence loop for string.find on the name -> red on (b); let the painter hide the box when
# there is nothing to show -> red on (d); drop one family from the table -> red on (f);
# reword a sentence without moving its key -> red on (f) half two. Probe: rename the box
# TITLE -> GREEN, the wording of the heading is not what this rule is about.
$v412Bad  = @()
$doc412   = Doc (Join-Path $dir "WoD20.11.lfm")
$src412   = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes((Join-Path $dir "WoD20.11.lfm")))
$box412   = @($doc412.SelectNodes("//layout[label/@name='dynRevWeakness']"))
$fam412   = @(@($PICKER['family']) | Where-Object { $_ -ne '' })
$clan412  = @(@($PICKER['clan'])   | Where-Object { $_ -ne '' })
$fw412    = [regex]::Match($rootTxt, '(?s)\n\t{3}FAMILY_WEAKNESS = \{(.*?)\n\t{3}\};')
$read412  = LuaFn $rootTxt 'familyWeakness'
$paint412 = LuaFn $src412 'renderRevWeakness'

# (e) the zero-guard runs FIRST, because every leg below reads one of these five and a set
# that comes back empty makes all of them pass over nothing (SPEC V412e, B94, B7).
if ($box412.Count -ne 1) { $v412Bad += "(e) the weakness box is authored $($box412.Count) time(s) in WoD20.11 and there has to be exactly one - two of them would be two owners of one sentence (SPEC V412e, V135, V209)" }
if ($fam412.Count -eq 0) { $v412Bad += "(e) PICKER_LIST[family] read back empty - the pertinence question, the completeness leg and the clan test below would every one of them pass over nothing (SPEC V412e, B94)" }
if ($clan412.Count -eq 0) { $v412Bad += "(e) PICKER_LIST[clan] read back empty - the leg that refuses a clan row in the weakness table would certify any table at all (SPEC V412e, B94)" }
if (-not $fw412.Success) { $v412Bad += "(e) FAMILY_WEAKNESS is not on the root form - the box has no book text to paint and every leg below reads an empty table (SPEC V412e, V209)" }
if (-not $read412) { $v412Bad += "(e) familyWeakness() is gone from the root form - the ONE reader of both the book table and the typed slot has no owner left (SPEC V412e, V135, V337c)" }
if (-not $paint412) { $v412Bad += "(e) renderRevWeakness() is gone from WoD20.11 - the box would keep the text it was authored with for ever, which is V383(a) one control over (SPEC V412e, V383a)" }

if (-not $v412Bad) {
    $keys412 = @()
    $vals412 = @{}
    foreach ($m412 in [regex]::Matches($fw412.Groups[1].Value, '\["([^"]+)"\]\s*=\s*"([^"]*)"')) {
        $keys412 += $m412.Groups[1].Value
        $vals412[$m412.Groups[1].Value] = $m412.Groups[2].Value
    }
    $body412  = NoComments $read412
    $pbody412 = NoComments $paint412

    if ($keys412.Count -eq 0) { $v412Bad += "(e) FAMILY_WEAKNESS parsed to zero rows - a table read empty passes completeness by vacuity, which is B7 arriving through the parser (SPEC V412e, B7)" }

    # (a) the box is IN the band. One that slid below it would still paint, still read right in
    # every grep, and simply not be where the ask put it. What the band CLOSES on is V414's and
    # is not restated here: one number, one owner (SPEC B70).
    $sb412  = $doc412.SelectSingleNode("//scrollBox")
    $bt412  = BandTop $sb412
    $par412 = $box412[0].ParentNode
    $top412 = -1
    [void][int]::TryParse($box412[0].GetAttribute("top"), [ref]$top412)
    if ($bt412 -lt 0) { $v412Bad += "(a) WoD20.11 has no band for the box to sit in - leg (a) measured nothing (SPEC V412e, V209)" }
    elseif ($par412.LocalName -ne 'scrollBox' -or $top412 -ne $bt412) {
        $v412Bad += "(a) the weakness box opens at top=$top412 under <$($par412.LocalName)> and this tab's band opens at $bt412 - the ask put it BETWEEN two boxes OF THE BAND, and a box that left it is not the thing that was asked for (SPEC I143a, V412a)"
    }

    # (b) pertinence, and nothing but pertinence. The list is what answers "is this a Family?",
    # and the parenthesis in `Bratovich (Dark Ages)` is SPELLING and not species - four clans
    # carry one too, which is why picking the name apart is the mutation this leg exists for.
    # Since T972 the loop lives in isFamilyName, the ONE body, and familyWeakness CALLS it (SPEC V433c).
    if ($body412 -notmatch 'isFamilyName\(') {
        $v412Bad += "(b) familyWeakness does not ask isFamilyName - whatever it asks instead is a second owner of which species a name came from (SPEC V412b, V433c, I143b, V135)"
    }
    if ($body412 -match 'string\.(find|match|sub)\s*\(') {
        $v412Bad += "(b) familyWeakness tells a family from a clan by picking the NAME apart - the value already carries which list it came from, and reading it out of the spelling is the door I143b was written to shut (SPEC V412b, I143b)"
    }
    $clanRows412 = @($keys412 | Where-Object { $clan412 -contains $_ })
    if ($clanRows412.Count) {
        $v412Bad += "(b) FAMILY_WEAKNESS carries $($clanRows412.Count) row(s) for a CLAN [$($clanRows412 -join ', ')] - a clan has no revenant weakness, and a row here is how the box would come to paint one over it (SPEC V412b, I143)"
    }

    # (c) the typed slot is READ where the picker wrote it, and there is no second copy of it
    # anywhere. The field name is DERIVED through customFieldOf, which already owns that
    # convention - a name typed out again here is its second spelling (SPEC I143g, V135).
    if ($body412 -notmatch 'clanFamilyCustomWeak') {
        $v412Bad += "(c) familyWeakness never reads clanFamilyCustomWeak - the weakness a player typed into the custom slot would be written by the picker and read by nobody (SPEC V412c, I143g)"
    }
    if ($body412 -notmatch 'customFieldOf\(') {
        $v412Bad += "(c) familyWeakness does not derive the custom field through customFieldOf - the convention has one owner and a name spelled out here is its second (SPEC V412c, V135, I143g)"
    }
    $wr412 = @([regex]::Matches((NoComments $rootTxt), 'setField\("clanFamilyCustomWeak"'))
    if ($wr412.Count -ne 1) {
        $v412Bad += "(c) clanFamilyCustomWeak is written in $($wr412.Count) place(s) and there is exactly one - the picker's OK. A second writer is the second copy the box is forbidden to read from (SPEC V412c, V135, I143g)"
    }

    # (d) the box FICA. Nothing to show is a SENTENCE, and it is the same sentence for a clan
    # and for a custom entry answered No, because the owner decided the two say the same thing
    # (SPEC Q60). A box that came and went would move the band at runtime, which is the one
    # thing V57 says this sheet's geometry never does.
    if ($pbody412 -match '\.visible\s*=') {
        $v412Bad += "(d) renderRevWeakness writes .visible - a box that comes and goes moves the band while the sheet is open, and every geometry check on this tab reads the static XML (SPEC V412d, V57, V375)"
    }
    if ($pbody412 -notmatch '-- no weakness --') {
        $v412Bad += "(d) renderRevWeakness does not fall back to the authored '-- no weakness --' sentence - a clan and a custom entry answered No would leave the box BLANK, and blank says nothing at all (SPEC V412d, Q60, V360c)"
    }
    if (-not $ptK.Contains('-- no weakness --')) { $v412Bad += "(d) '-- no weakness --' has no [pt] key in localization.lang - the Portuguese sheet would read the absence in English (SPEC V412d, V9, V10)" }
    if (-not $enK.Contains('-- no weakness --')) { $v412Bad += "(d) '-- no weakness --' has no [en] key in localization.lang (SPEC V412d, V10)" }

    # (f) COMPLETENESS, and it is asked TWICE - of the table and of the language file. The
    # owner ruled on 2026-09-03 that a family with no weakness is a QUESTION and not a state of
    # the screen: it ends with the text arriving or with the family leaving the list, which is
    # exactly how Katayama left it in T938 (SPEC Q60, V412f). The number is READ off the list
    # and never typed, so a family added tomorrow reddens here the same day.
    $missing412 = @($fam412 | Where-Object { -not $vals412.ContainsKey($_) })
    if ($missing412.Count) {
        $v412Bad += "(f) $($missing412.Count) of the $($fam412.Count) names of PICKER_LIST[family] have no line in FAMILY_WEAKNESS [$($missing412 -join ', ')] - a family on the list with no weakness behind it is the question this rule refuses to let go quiet (SPEC V412f, Q60, T938)"
    }
    $extra412 = @($keys412 | Where-Object { $fam412 -notcontains $_ })
    if ($extra412.Count) {
        $v412Bad += "(f) FAMILY_WEAKNESS carries $($extra412.Count) row(s) no family answers to [$($extra412 -join ', ')] - a weakness keyed to a name that is not on the list can never fire and is worse than absent, which is the reading V337(a) already makes of the ceiling (SPEC V412f, V337a)"
    }
    $noKey412 = @()
    foreach ($k412 in $keys412) {
        $t412 = $vals412[$k412]
        if (-not $ptK.Contains($t412) -or -not $enK.Contains($t412)) { $noKey412 += $k412 }
    }
    if ($noKey412.Count) {
        $v412Bad += "(f) $($noKey412.Count) weakness sentence(s) have no key in BOTH halves of localization.lang [$($noKey412 -join ', ')] - the table is authored in English and translateSheetText is the only thing that turns it into Portuguese, so a sentence without a key ships in one language and nothing says so (SPEC V412f, V9, V10, V24)"
    }

    # (g) the TEXT is centred, and it is the TEXT and not the CONTROL (SPEC V412g, I144j, the
    # owner's ask of 2026-09-04, Ghoul item 1). Centring the control in the body drops the foot
    # to 10 against the 15 V280(a) charges, and opening the 15 on both sides shrinks the editor
    # to 63 and eats one of the four lines the longest weakness uses (SPEC I143j). horzTextAlign
    # lives on gui.TextControl, which gui.TextEditor inherits, so the attribute is DECLARABLE -
    # whether the host honours it in a multi-line editor is a RUNTIME question, and this leg
    # guards what is in the SOURCE rather than promising a drawing, exactly as V241 does (R98).
    # T966 (SPEC B155): the text is a wrapping LABEL now - dyn* because Lua writes it (V31), no text=,
    # no opacity (it is not a read-only widget, V111) - and it centres on BOTH axes: label is the
    # one tag of this sheet the host is known to honour vertTextAlign on (15 uses, I144l).
    $txtEd412 = $doc412.SelectSingleNode("//label[@name='dynRevWeakness']")
    # (h) INVERTED 2026-09-05 (SPEC B155): the SDK wrapper exposes vertTextAlign on every TextControl
    # (rrpgGUI.lua:679) but the HOST has no VertTextAlign on a textEditor - _obj_setProp throws
    # inside constructNew_frmWoD20th and the sheet never opens, with rdk -l at 0 and this gate green.
    # No textEditor of any .lfm may declare it; the vertical centring of the weakness is T966's
    # label (dynRevWeakness). Measured over every file, not the one box: the attribute is a trap
    # wherever the tag is.
    foreach ($f412h in (Get-ChildItem (Join-Path $dir '*.lfm'))) {
        foreach ($te412h in @((Doc $f412h.FullName).SelectNodes("//textEditor[@vertTextAlign]"))) {
            $v412Bad += "(h) $($f412h.Name) textEditor '$($te412h.GetAttribute('name'))' declares vertTextAlign - the host has no such property on a textEditor and aborts the form constructor, so the sheet never opens (SPEC B155, V412h)"
        }
    }
    if ($null -ne $txtEd412 -and ($txtEd412.GetAttribute("horzTextAlign") -ne 'center' -or $txtEd412.GetAttribute("vertTextAlign") -ne 'center' -or $txtEd412.GetAttribute("wordWrap") -ne 'true')) {
        $v412Bad += "(g) dynRevWeakness declares horzTextAlign='$($txtEd412.GetAttribute("horzTextAlign"))' vertTextAlign='$($txtEd412.GetAttribute("vertTextAlign"))' wordWrap='$($txtEd412.GetAttribute("wordWrap"))' - the weakness reads as a caption under a centred title and the owner asked for it centred, and it is the TEXT that moves because centring the CONTROL costs a line of the longest one (SPEC V412g, I144j)"
    }
}

if ($v412Bad) { foreach ($b412 in $v412Bad) { Fail "V412 $b412" } }
else { Pass "V412 the weakness box sits in the band, asks pertinence in PICKER_LIST[family] alone, reads the typed slot where the picker wrote it, never hides, and every one of the $(@(@($PICKER['family']) | Where-Object { $_ -ne '' }).Count) families has a sentence with a key in both languages" }

# ---- V414: the two strips of the Ghoul tab close on the SAME number ---------------------
# SPEC V414, I143j, the owner's ask of 2026-09-03. This tab is a BAND of boxes over three
# COLUMNS, and the two are widths nobody obliges to agree: both close on the same number today
# by construction, and until this round nothing measured it. A band wider than the columns
# leaves a box hanging off the paper and a narrower one leaves a strip of nothing to the right,
# and both of them exit 0 through every other check on the sheet.
#
# It does NOT freeze the number (SPEC V414b). A literal here would be a rule born red the day
# the weakness box grows, which is the very thing I143j exists to allow - B106 by the front
# door. What is measured is the EQUALITY, read off the boxes themselves: each strip closes on
# the widest left+width it holds, so the 5px gutter needs no second owner here either - V298
# owns that number and V221 charges these columns theirs (SPEC B70).
#
# Mutation (SPEC V20, V222): widen one band box without paying it back below -> red on (a);
# take the backdrop off a Ghoul box so it drops out of the V280 census -> red on (c). Probe:
# swap the ORDER of two band boxes keeping the total -> GREEN, this is about where the strip
# closes and not about who sits where inside it.
$v414Bad = @()
$doc414  = Doc (Join-Path $dir "WoD20.11.lfm")
$sb414   = $doc414.SelectSingleNode("//scrollBox")
$bt414   = BandTop $sb414
$band414 = @()
$cols414 = @()
if ($null -eq $sb414 -or $bt414 -lt 0) {
    $v414Bad += "(d) WoD20.11 has no band - the equality would be struck between two sets this run never found (SPEC V414d, V209)"
} else {
    foreach ($l414 in $sb414.SelectNodes("layout")) {
        $t414 = 0
        if (-not [int]::TryParse($l414.GetAttribute("top"), [ref]$t414)) { continue }
        if ($t414 -eq $bt414) { $band414 += $l414 } else { $cols414 += $l414 }
    }
}
# (d) zero-guard, and it is TWO questions: an equality struck over an empty side is true of
# every drawing ever made, and 0 == 0 is the way this rule would pass having read nothing.
if ($band414.Count -eq 0) { $v414Bad += "(d) not one box was read in the band of WoD20.11 - the left side of the equality is empty and 0 would equal 0 (SPEC V414d, B7, B94)" }
if ($cols414.Count -eq 0) { $v414Bad += "(d) not one column was read below the band of WoD20.11 - the right side of the equality is empty (SPEC V414d, B7, B94)" }

if (-not $v414Bad) {
    # The gutter, DERIVED and never typed: it is what the columns actually leave between them,
    # and V221 and V298 are what hold that at the house 5 (SPEC B70). Read here so this rule
    # owns no copy of the number it depends on.
    $cs414 = @($cols414 | Sort-Object { [int]$_.GetAttribute("left") })
    $gut414 = -1
    if ($cs414.Count -ge 2) { $gut414 = [int]$cs414[1].GetAttribute("left") - ([int]$cs414[0].GetAttribute("left") + [int]$cs414[0].GetAttribute("width")) }
    if ($gut414 -lt 0) { $v414Bad += "(d) the gutter between the Ghoul columns read back $gut414 - the sum on both sides of the equality is struck with it, and a negative one would make the two sides agree by arithmetic (SPEC V414d, V298, V209)" }

    $bandSum414 = 0
    $bandLst414 = @()
    foreach ($l414 in $band414) {
        $w414 = [int]$l414.GetAttribute("width")
        $n414 = ColId361 $l414
        if (-not $n414) { $n414 = "the box at x=$($l414.GetAttribute('left'))" }
        $bandLst414 += "$n414 $w414"
        $bandSum414 += $w414
    }
    $colSum414 = 0
    $colLst414 = @()
    foreach ($l414 in $cols414) {
        $w414 = [int]$l414.GetAttribute("width")
        $n414 = ColId361 $l414
        if (-not $n414) { $n414 = "the column at x=$($l414.GetAttribute('left'))" }
        $colLst414 += "$n414 $w414"
        $colSum414 += $w414
    }
    $bandSum414 += ($band414.Count - 1) * $gut414
    $colSum414  += ($cols414.Count - 1) * $gut414

    # (a) the equality itself, and it is struck on the WIDTHS and not on where the last box
    # happens to close. A box that grows INTO its neighbour moves no closing edge at all - the
    # overlap is V40's and V298's to name - and this leg would have read 1408 on both sides and
    # passed, which is the version of it MEASURED red in T936's mutation round.
    if ($bandSum414 -ne $colSum414) {
        $v414Bad += "(a) the band wants $bandSum414 px and the columns $colSum414 - the $($band414.Count) band box(es) ($($bandLst414 -join ', ')) and the $($cols414.Count) column(s) ($($colLst414 -join ', ')) are one tab laid out with one ${gut414}px gutter, so the two sums are one number. A band that grew without being paid for below leaves a box off the paper, one that shrank leaves a strip of nothing, and neither of them raises an error (SPEC V414a, I143j)"
    }

    # (c) the squeeze goes down to the margin and NOT past it. The 20 has exactly one owner and
    # it is V280(a) - restating it here would be B70 - so what this leg guarantees is that
    # V280's census is actually LOOKING at these boxes. A Ghoul box that fell out of it would
    # take its floor with it in silence, which is B94: the case that got away is the one nobody
    # names. Matched by rect inside the file, because Doc() reparses and node identity does not
    # survive the second call.
    $seen414 = 0
    $out414  = @()
    $sect414 = @()
    foreach ($l414 in $band414) { $sect414 += $l414 }
    foreach ($c414 in $cols414) { foreach ($l414 in $c414.SelectNodes("layout")) { $sect414 += $l414 } }
    foreach ($l414 in $sect414) {
        $hit414 = @($v280Boxes | Where-Object {
            $_.F -eq 'WoD20.11.lfm' -and
            $_.L -eq [int]$l414.GetAttribute("left")  -and $_.T -eq [int]$l414.GetAttribute("top") -and
            $_.W -eq [int]$l414.GetAttribute("width") -and $_.H -eq [int]$l414.GetAttribute("height") })
        if ($hit414.Count -eq 0) {
            $n414 = ColId361 $l414
            if (-not $n414) { $n414 = "the box at $($l414.GetAttribute('left')),$($l414.GetAttribute('top'))" }
            $out414 += $n414
        } else { $seen414++ }
    }
    if ($out414.Count) {
        $v414Bad += "(c) $($out414.Count) box(es) of the two Ghoul strips [$($out414 -join ', ')] are not in the census V280 measures the 20px margin with - a box outside it keeps no floor at all, and the squeeze I143j asked for is allowed to go TO the margin and not past it (SPEC V414c, V280a, B94)"
    }
    if ($seen414 -eq 0) {
        $v414Bad += "(c) not one box of the two strips was matched against the V280 census - this leg passed over nothing (SPEC V414d, V20, B7)"
    }
}

if ($v414Bad) { foreach ($b414 in $v414Bad) { Fail "V414 $b414" } }
else { Pass "V414 the $($band414.Count) boxes of the Ghoul band and its $($cols414.Count) columns want the same $bandSum414 px over one ${gut414}px gutter, and every box of both strips is inside the census V280 holds to the 20px margin" }

# ---- V423: the five clan renames of 2026-09-04 hold across the whole pt surface ---------
# SPEC V423, T950, T951, V22, V28, V24. Two legs.
# (a) the five pairs agree BYTE FOR BYTE in the two places a translation lives - the [pt] half
# of localization.lang and the PT map of WoD20.6.lfm. The EN key does not move in any of the
# five, so PICKER_LIST["clan"], the CLANS keys (I17, V212), CLAN_DISC (V236) and the keys of
# descClan_en/pt.lua are untouched, and V24 - the canonical EN value that is what gets SAVED -
# never enters: an old sheet reopens on the right clan and only the LABEL changed.
# (b) the five OLD names are gone from every .lfm/.lua/.lang of the plugin SOURCE and from
# research\merit_flaw_body_pt.tsv. The tsv is in the sweep because it REGENERATES
# descMerit_pt.lua: fix the .lua, leave the tsv old, and the next generation quietly undoes
# all five renames with the gate green - B57 through the data door, the lesson V412 learned.
# The needles are whole PHRASES and never the stem 'Arauto': 'Arauto do Abismo' (Harbinger of
# the Abyss, localization.lang) and 'Arautos zumbidores' (descMerit_pt) are a different thing
# and must stay green - a rule on the stem would fire on a correct translation, which is B42.
# The one accented needle is spelled with [char]0xD3 on purpose: this file is BOM-less ASCII.
$V423_PAIRS = @(
    @{ En = 'Harbingers of Skulls'; New = 'Precursores do ' + [char]0xD3 + 'dio'; Old = 'Arautos das Caveiras' }
    @{ En = 'True Brujah';          New = 'Brujah Verdadeiros';            Old = 'Verdadeiros Brujah' }
    @{ En = 'Assamite Sorcerers';   New = 'Assamitas: Feiticeiros';        Old = 'Feiticeiros Assamitas' }
    @{ En = 'Assamite Viziers';     New = 'Assamitas: Vizires';            Old = 'Vizires Assamitas' }
    @{ En = 'Warrior Setites';      New = 'Seguidores de Set: Guerreiros'; Old = 'Setitas Guerreiros' }
)
$v423Bad = @()
$seen423 = 0
foreach ($p423 in $V423_PAIRS) {
    $en423 = $p423.En
    if (-not $ptVal.ContainsKey($en423))    { $v423Bad += "(a) '$en423' has no [pt] key in localization.lang"; continue }
    if (-not $embedded.ContainsKey($en423)) { $v423Bad += "(a) '$en423' is not in the PT map of WoD20.6.lfm"; continue }
    $seen423++
    if ($ptVal[$en423] -cne $p423.New)    { $v423Bad += "(a) localization.lang [pt] reads '$($ptVal[$en423])' for '$en423' - the user renamed it to '$($p423.New)' on 2026-09-04 (SPEC V423a, T950)" }
    if ($embedded[$en423] -cne $p423.New) { $v423Bad += "(a) the PT map of WoD20.6.lfm reads '$($embedded[$en423])' for '$en423' - the two sides must match byte for byte (SPEC V423a, V22)" }
}
if ($seen423 -lt 5) { $v423Bad += "(a) only $seen423 of the five renamed clans were read on BOTH sides - a rule over pairs it cannot see is true by vacancy (SPEC V423, V209, B7)" }

$v423Files = @()
foreach ($f423 in (Get-ChildItem -Path $plugin -Recurse -File -Include *.lfm, *.lua, *.lang)) {
    if ($f423.FullName -like "*\output\*") { continue }
    $v423Files += $f423.FullName
}
$tsv423 = Join-Path $PSScriptRoot "research\merit_flaw_body_pt.tsv"
if (Test-Path $tsv423) { $v423Files += $tsv423 }
foreach ($m423 in @('localization.lang', 'WoD20.6.lfm', 'descMerit_pt.lua', 'descRitual_pt.lua', 'descDisc_pt.lua', 'merit_flaw_body_pt.tsv')) {
    if (-not ($v423Files | Where-Object { [System.IO.Path]::GetFileName($_) -eq $m423 })) {
        $v423Bad += "(b) $m423 never entered the sweep - it is one of the six files that carried an old name, and a sweep blind to it passes over nothing (SPEC V423b, V209, B7)"
    }
}
foreach ($path423 in $v423Files) {
    $txt423 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($path423))
    foreach ($q423 in $V423_PAIRS) {
        if ($txt423.IndexOf($q423.Old, [System.StringComparison]::Ordinal) -ge 0) {
            $v423Bad += "(b) $([System.IO.Path]::GetFileName($path423)) still carries the old name '$($q423.Old)' - the picker would read '$($q423.New)' with the old name in the prose beside it (SPEC V423b, T951)"
        }
    }
}

if ($v423Bad) { foreach ($b423 in $v423Bad) { Fail "V423 $b423" } }
else { Pass "V423 the five clans renamed on 2026-09-04 read the same on both sides of the translation, and not one of the five old names survives the sweep of $($v423Files.Count) source files (research tsv included)" }

# ---- V419: the anchor roster is the XML's, and Lua only ever GIVES IT BACK ----------------
# SPEC V419 (amended twice on 2026-09-04), B153, T948, T955, I144h, V202.
# Leg 1 - the three Settings dropdowns author horzTextAlign="center" in the XML.
# Leg 2 - PICKER_ANCHOR, the Lua roster, EQUALS the roster the XML implies: every <comboBox>
# in the twelve .lfm that authors horzTextAlign AND carries a cbo*/cmb* name, which is exactly
# the set the two rewriting routines can reach. The language combo is excluded by having no
# name at all, and that is the control - it was the only one of the three that never lost its
# anchor (B153). The roster holds LITERALS on purpose: reading c.horzTextAlign back was tried
# twice and does not work - the getter maps to _obj_getProp(handle, "TextAlign") and not one
# sheet in this repo reads it, the SDK only ever writes it (rrpgGUI.lua:349). The literal
# cannot drift because THIS check is what compares it to the attribute, both ways.
# Leg 3 - keepAnchor is the ONE place in the twelve .lfm that writes an alignment in Lua, and
# what it writes comes out of the roster, never out of a quote at the call site.
$doc419 = Doc (Join-Path $dir "WoD20.6.lfm")
$v419Bad = @()
$seen419 = 0
foreach ($w419 in @('language', 'cboGame', 'cboSheetTheme')) {
    if ($w419 -eq 'language') { $cb419 = @($doc419.SelectNodes("//comboBox[@field='language']"))[0] }
    else                      { $cb419 = @($doc419.SelectNodes("//comboBox[@name='$w419']"))[0] }
    if ($null -eq $cb419) { $v419Bad += "(1) the Settings combo '$w419' is gone from WoD20.6.lfm (SPEC V419, I144h)"; continue }
    $seen419++
    $ha419 = $cb419.GetAttribute('horzTextAlign')
    if ($ha419 -ne 'center') { $v419Bad += "(1) the Settings combo '$w419' authors horzTextAlign='$ha419' and not 'center' (SPEC V419, I144h)" }
}
if ($seen419 -lt 3) { $v419Bad += "(1) only $seen419 of the three Settings combos were read - a rule over controls it cannot see is true by vacancy (SPEC V419, V209, B7)" }

$want419 = @{}
foreach ($f419 in $files) {
    foreach ($cb419 in (Doc $f419.FullName).SelectNodes("//comboBox[@horzTextAlign]")) {
        $nm419 = $cb419.GetAttribute('name')
        if ($nm419 -eq '' -or $nm419 -notmatch '^(cbo|cmb)') { continue }
        $want419[$nm419] = $cb419.GetAttribute('horzTextAlign')
    }
}
if ($want419.Count -eq 0) { $v419Bad += "(2) not one anchored cbo*/cmb* combo was found in the $($files.Count) .lfm - the roster would be compared against nothing (SPEC V419, V209, B7)" }

$got419 = @{}
$rost419 = [regex]::Match($hh6, '(?s)PICKER_ANCHOR = \{(.*?)\};')
if (-not $rost419.Success) { $v419Bad += "(2) PICKER_ANCHOR is gone from WoD20.6.lfm - keepAnchor would put nothing back and the anchor dies on the next language switch (SPEC V419, B153)" }
else {
    foreach ($m419 in [regex]::Matches($rost419.Groups[1].Value, '(\w+)\s*=\s*"([^"]*)"')) { $got419[$m419.Groups[1].Value] = $m419.Groups[2].Value }
    foreach ($k419 in $want419.Keys) {
        if (-not $got419.ContainsKey($k419)) { $v419Bad += "(2) '$k419' authors horzTextAlign='$($want419[$k419])' in the XML and is NOT in PICKER_ANCHOR - its list is rewritten on a language switch and nothing puts the anchor back (SPEC V419, B153)" }
        elseif ($got419[$k419] -cne $want419[$k419]) { $v419Bad += "(2) '$k419' authors horzTextAlign='$($want419[$k419])' and PICKER_ANCHOR hands back '$($got419[$k419])' - Lua would overwrite what the XML declares (SPEC V419, V208)" }
    }
    foreach ($k419 in $got419.Keys) {
        if (-not $want419.ContainsKey($k419)) { $v419Bad += "(2) PICKER_ANCHOR carries '$k419', which authors no horzTextAlign in any .lfm - writing an anchor onto a control that never asked for one is Lua deciding, not restoring (SPEC V419, V208)" }
    }
}

$anch419 = LuaFn (CodeOf (Join-Path $dir "WoD20.6.lfm")) 'keepAnchor'
$writes419 = @()
foreach ($f419 in $files) {
    foreach ($m419 in [regex]::Matches((CodeOf $f419.FullName), '\.\s*(horz|vert)TextAlign\s*=\s*([^;\r\n]+)')) {
        $writes419 += @{ File = $f419.Name; Prop = $m419.Groups[1].Value + 'TextAlign'; Rhs = $m419.Groups[2].Value.Trim(); At = $m419.Index }
    }
}
# Leg 3 - the TOGGLE, and it is exactly two writes, both inside keepAnchor: away from the anchor,
# then back to it. MEASURED 2026-09-04 (B153, 5th cut): after setItems the property still reads
# the authored value and the closed text is left-aligned anyway - the host only re-reads the
# anchor when the property CHANGES, so one write of the right value is a no-op to it. The value
# that STAYS is the roster's, which leg 2 compares to the XML; the transient is derived from it
# and never names the anchor.
if ($writes419.Count -ne 2) { $v419Bad += "(3) $($writes419.Count) Lua alignment write(s) across the $($files.Count) .lfm - it has to be exactly the two of the toggle inside keepAnchor, away and back (SPEC V419, V209, B153)" }
else {
    $ordered419 = @($writes419 | Sort-Object { $_.At })
    foreach ($x419 in $ordered419) {
        if ($x419.File -ne 'WoD20.6.lfm') { $v419Bad += "(3) $($x419.File) writes $($x419.Prop) in Lua - the only slot allowed is keepAnchor (SPEC V419, V208)"; continue }
        if ($x419.Rhs -notmatch '^[A-Za-z_][A-Za-z0-9_]*$') { $v419Bad += "(3) the write is $($x419.Prop) = $($x419.Rhs) - a quote at the write site is Lua deciding the anchor; what stays has to come out of PICKER_ANCHOR (SPEC V419, V208)"; continue }
        if ($anch419 -eq '' -or $anch419.IndexOf('.' + $x419.Prop + ' = ' + $x419.Rhs, [System.StringComparison]::Ordinal) -lt 0) { $v419Bad += "(3) the $($x419.Prop) = $($x419.Rhs) write is not inside keepAnchor - a second site is a second owner (SPEC V419, V208, B153)" }
    }
    if ($anch419 -ne '') {
        $back419 = $ordered419[1].Rhs
        $away419 = $ordered419[0].Rhs
        if ($anch419 -notmatch ("(?m)local\s+" + [regex]::Escape($back419) + "\s*=\s*PICKER_ANCHOR\[")) { $v419Bad += "(3) the value that STAYS, '$back419', is not read from PICKER_ANCHOR - the roster is what this check can vouch for (SPEC V419)" }
        if ($away419 -eq $back419) { $v419Bad += "(3) both writes are '$back419' - writing the anchor over itself is the no-op the host ignores, the first write has to move AWAY (SPEC V419, B153)" }
        elseif ($anch419 -notmatch ("(?m)local\s+" + [regex]::Escape($away419) + "\s*=\s*\(\s*" + [regex]::Escape($back419) + "\s*==")) { $v419Bad += "(3) the transient '$away419' is not derived from '$back419' - it has to be whatever the anchor is NOT, or the day the roster says leading the toggle writes leading twice (SPEC V419, B153)" }
    }
}

# Leg 4 - and it is CALLED, from both routines that rewrite a list, AFTER the write. Legs 2 and
# 3 vouch for the roster and for the writes inside keepAnchor; without this one, deleting the
# call site leaves both of them green over a sheet that loses its anchor on every language
# switch, which is B7 through the door of a rule that checks a definition and not a use.
foreach ($fn419 in @('pickerItems', 'cmbItems')) {
    $sl419 = LuaFn (CodeOf (Join-Path $dir "WoD20.6.lfm")) $fn419
    if ($sl419 -eq '') { $v419Bad += "(4) $fn419 was not found in WoD20.6.lfm - it is one of the two routines that rewrite a combo's items, and a rule that cannot read it passes over nothing (SPEC V419, V209, B7)"; continue }
    $atI419 = $sl419.IndexOf('c.items = ', [System.StringComparison]::Ordinal)
    $atK419 = $sl419.IndexOf('keepAnchor(', [System.StringComparison]::Ordinal)
    if ($atK419 -lt 0) { $v419Bad += "(4) $fn419 rewrites a combo's items and never calls keepAnchor - the host drops the anchor on that write and nothing puts it back (SPEC V419, B153)" }
    elseif ($atI419 -lt 0) { $v419Bad += "(4) $fn419 no longer writes c.items - either the routine changed shape or this rule is reading the wrong slice (SPEC V419, V209)" }
    elseif ($atK419 -lt $atI419) { $v419Bad += "(4) $fn419 calls keepAnchor BEFORE it writes the items - an anchor put back before the write that drops it restores nothing (SPEC V419, B153)" }
}

# Leg 5 - and the toggle is DEFERRED, both halves of it, with a POSITIVE interval. At +1ms the
# text the host rebuilt already exists, so the toggle lands on the control that is on screen;
# setTimeout at 0 runs in this very tick, ahead of that rebuild (SPEC R44, B97), which is the
# defect again - so the interval is read and not just the call.
if ($anch419 -eq '') { $v419Bad += "(5) keepAnchor was not found in WoD20.6.lfm (SPEC V419, V209, B7)" }
else {
    $def419 = [regex]::Match($anch419, '(?s)setTimeout\(\s*function\(\)(.*?)end\s*,\s*(\d+)\s*\)')
    if (-not $def419.Success) { $v419Bad += "(5) keepAnchor writes the anchor synchronously - the host rebuilds the closed display after this call returns and throws the write away, which is what the first four cuts all did (SPEC V419, B153, R44)" }
    else {
        $inside419 = ([regex]::Matches($def419.Groups[1].Value, '\.horzTextAlign\s*=')).Count
        if ($inside419 -ne 2) { $v419Bad += "(5) only $inside419 of the two toggle writes sit inside the deferred closure - a half outside it runs in the tick the host is about to overwrite (SPEC V419, R44, B153)" }
        if ([int]$def419.Groups[2].Value -lt 1) { $v419Bad += "(5) keepAnchor defers the toggle with interval $($def419.Groups[2].Value) - setTimeout is only asynchronous with a POSITIVE one, at 0 it runs in this very tick (SPEC V419, R44, B97)" }
    }
}

# Leg 6 - and the closure asks for a REPAINT after the toggle. The toggle alone repainted the
# enabled combos and not cboGame (enabled="false"); the user's probe showed the property was
# right all along - leave the tab and come back and it is centred - so what the disabled combo
# needs is the paint, asked for directly (gui.Control:needRepaint). Without this leg, deleting
# the call leaves legs 3 and 5 green over a Game combo that is wrong until the next tab switch.
if ($anch419 -ne '') {
    $def419b = [regex]::Match($anch419, '(?s)setTimeout\(\s*function\(\)(.*?)end\s*,\s*(\d+)\s*\)')
    if ($def419b.Success) {
        $body419 = $def419b.Groups[1].Value
        $lastW419 = $body419.LastIndexOf('.horzTextAlign =', [System.StringComparison]::Ordinal)
        $rep419 = $body419.IndexOf(':needRepaint()', [System.StringComparison]::Ordinal)
        if ($rep419 -lt 0) { $v419Bad += "(6) keepAnchor toggles the anchor and never asks for a repaint - the disabled combo keeps the stale paint until the next tab switch (SPEC V419, B153)" }
        elseif ($rep419 -lt $lastW419) { $v419Bad += "(6) keepAnchor repaints BEFORE the toggle lands - the paint it asks for is the stale one (SPEC V419, B153)" }
    }
}

if ($v419Bad) { foreach ($b419 in $v419Bad) { Fail "V419 $b419" } }
else { Pass "V419 the three Settings combos author their anchor in XML, PICKER_ANCHOR matches the $($want419.Count) anchored cbo*/cmb* combos name for name and value for value, and keepAnchor is the one Lua that writes an alignment" }

# ---- V433: the Clan/Family line is ONE button over the UNION, and the KIND follows the name --
# SPEC V433, I149a, I149b, T972, the user's decision of 2026-09-05 (5th batch) - it SUPERSEDES
# V426 (a)(c)(d)(e); V426(b) - one field, no field= on the button - is kept here as (a2).
# T954 had split the line into two buttons with a kind each so descFamily_* had an opener; the
# user put the ONE picker back: the player sees `Clan/Family` over clan + family, and the pane
# resolves the module by the NAME picked - mfKind answers `Family` for a name standing in
# PICKER_LIST["family"] and the authored kind otherwise. The pertinence test has ONE body,
# isFamilyName, and every other reader of "is it a family?" calls it (V135, V412b).
#
# Mutation (SPEC V20, V222): dynfamily back on the line -> red on (a); the union without its
# markers -> red on (b); descFor(MF.field, MF.mod, ...) back in the pane -> red on (c); a second
# loop over PICKER_LIST["family"] -> red on (c); mfRevenantShown on "family" -> red on (d);
# Basarab out of descFamily_en -> red on V408 (leg (e) lives there, by pertinence).
$v433Bad = @()
$doc433  = Doc (Join-Path $dir 'WoD20.11.lfm')
$box433  = $doc433.SelectSingleNode("//layout[label/@name='lblMaxDisc']")
$btn433  = @()
if ($null -ne $box433) { $btn433 = @($box433.SelectNodes("button[contains(@onClick, 'mfOpen(')]")) }
# (f) zero-guard FIRST
if ($null -eq $box433) { $v433Bad += "(f) the DOMINATOR box is gone from WoD20.11 - there is no line to measure (SPEC V209)" }
elseif ($btn433.Count -ne 1) { $v433Bad += "(a) the DOMINATOR box carries $($btn433.Count) picker button(s) and the line is ONE since T972 - a second button is the split of T954 the user undid (SPEC V433a, I149a)" }
else {
    $b433  = $btn433[0]
    $nm433 = $b433.GetAttribute('name')
    $m433  = [regex]::Match($b433.GetAttribute('onClick'), "mfOpen\(self,\s*'([^']*)',\s*'([^']*)'(?:,\s*'([^']*)')?")
    if ($nm433 -ne 'dynclanFamily') { $v433Bad += "(a) the picker button on the line is named '$nm433', not dynclanFamily - the name is derived from the list mfOpen is handed (SPEC V433a, I145g)" }
    if (-not $m433.Success) { $v433Bad += "(a) '$nm433' does not call mfOpen(self, field, list, kind) (SPEC V433a)" }
    else {
        if ($m433.Groups[1].Value -ne 'clanFamily') { $v433Bad += "(a) '$nm433' opens on field '$($m433.Groups[1].Value)' - the line writes clanFamily, the ONE field (SPEC V433a, V426b)" }
        if ($m433.Groups[2].Value -ne 'clanFamily') { $v433Bad += "(a) '$nm433' offers PICKER_LIST['$($m433.Groups[2].Value)'] and the line offers the UNION clanFamily - a family or a clan alone is the split coming back (SPEC V433a, V433b)" }
        if ($m433.Groups[3].Value -ne 'Clan') { $v433Bad += "(a) '$nm433' names kind '$($m433.Groups[3].Value)', expected 'Clan' - the authored kind is the clan's, and Family is what mfKind answers by pertinence (SPEC V433a, V433c)" }
    }
    # (a2) = V426(b), kept: no field on the button, the twin is the one owner
    if ($b433.HasAttribute('field')) { $v433Bad += "(a) '$nm433' carries field='$($b433.GetAttribute('field'))' - a picker button binds no field; edtClanFamily is the one owner and a second one is the duplicate V1 refuses (SPEC V426b, V1)" }
    $owners433 = @($doc433.SelectNodes("//*[@field='clanFamily']"))
    if ($owners433.Count -ne 1 -or $owners433[0].GetAttribute('name') -ne 'edtClanFamily') { $v433Bad += "(a) clanFamily is bound by $($owners433.Count) control(s) on WoD20.11 and the one is edtClanFamily (SPEC V426b, V1, V2)" }
}
# the retired names: dynclan / dynfamily coming back anywhere is the split under its old names
foreach ($f433 in $files) {
    $c433 = NoComments ([System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f433.FullName)))
    foreach ($old433 in @('dynclan', 'dynfamily')) {
        if ($c433 -cmatch "\b$old433\b") { $v433Bad += "(a) $($f433.Name) still names $old433 - the two buttons of T954 are gone and a reader waiting for one paints nothing, or swaps nothing (SPEC V433a, V274d)" }
    }
}
# (b) the union is BACK, built once inside its markers, and is what the map mirrors
if (-not $UNION_REGION.Success) { $v433Bad += "(b) the CLANFAMILY_UNION markers are gone from the root form - the one button offers a list that is built nowhere (SPEC V433b, V209)" }
elseif (-not $PICKER.ContainsKey('clanFamily')) { $v433Bad += "(b) the map never mirrored PICKER_LIST['clanFamily'] - V408 would answer 'no such list' for the one picker of the line (SPEC V433b)" }
else {
    $want433 = @($PICKER['clan']).Count + @($PICKER['family']).Count
    if (@($PICKER['clanFamily']).Count -ne $want433 -or $want433 -lt 84) { $v433Bad += "(b) the union counts $(@($PICKER['clanFamily']).Count) and clan + family count $want433 (floor 84) - the picker offers less than the two lists, or the parse broke (SPEC V433b, V209)" }
}
# (c) the kind follows the NAME, and the pertinence test has ONE body
$kind433 = LuaFn $rootTxt 'mfKind'
$isFam433 = LuaFn $rootTxt 'isFamilyName'
if (-not $isFam433) { $v433Bad += "(f) isFamilyName is gone from the root form - the one body of the species question has no owner (SPEC V433c, V209)" }
elseif ((NoComments $isFam433) -notmatch 'PICKER_LIST\["family"\]') { $v433Bad += "(c) isFamilyName does not read PICKER_LIST[family] - whatever it asks instead is a second owner of which species a name came from (SPEC V433c, V412b, V135)" }
if (-not $kind433) { $v433Bad += "(c) mfKind is gone from the root form - the pane would read the authored kind for a family name and open descClan on it (SPEC V433c)" }
else {
    $kb433 = NoComments $kind433
    if ($kb433 -notmatch 'MF\.list\s*==\s*"clanFamily"\s+and\s+isFamilyName\(name\)') { $v433Bad += "(c) mfKind does not ask isFamilyName on the clanFamily list - the family module is unreachable from the one button again (SPEC V433c)" }
    if ($kb433 -notmatch 'return\s+"Family"') { $v433Bad += "(c) mfKind never answers 'Family' - the kind is what reaches desc<KIND> (SPEC V433c)" }
}
$rootNoC433 = NoComments $rootTxt
if ($rootNoC433 -match 'descFor\(MF\.field,\s*MF\.mod\b') { $v433Bad += "(c) the pane still hands descFor the authored MF.mod - a family name opens descClan, which does not hold it (SPEC V433c)" }
if ($rootNoC433 -notmatch 'descFor\(MF\.field,\s*mfKind\(name\)') { $v433Bad += "(c) the pane does not hand descFor mfKind(name) - the ONE place the kind is decided is bypassed (SPEC V433c, V135)" }
# one body: outside isFamilyName and the union's own loop, nothing reads PICKER_LIST["family"]
$total433 = ([regex]::Matches($rootNoC433, 'PICKER_LIST\["family"\]')).Count
$inFam433 = 0; if ($isFam433) { $inFam433 = ([regex]::Matches((NoComments $isFam433), 'PICKER_LIST\["family"\]')).Count }
$inUni433 = 0; if ($UNION_REGION.Success) { $inUni433 = ([regex]::Matches((NoComments $UNION_REGION.Groups[1].Value), 'PICKER_LIST\["family"\]')).Count }
$extra433 = $total433 - $inFam433 - $inUni433
if ($extra433 -gt 0) { $v433Bad += "(c) PICKER_LIST[family] is read $extra433 time(s) outside isFamilyName and the union loop - a second loop over the family list is a second owner of the species question (SPEC V433c, V135)" }
$paint433 = LuaFn $rootTxt 'renderClanFamilyButton'
if (-not $paint433) { $v433Bad += "(f) renderClanFamilyButton is gone from the root form (SPEC V209, V429)" }
else {
    $pb433 = NoComments $paint433
    if ($pb433 -cnotmatch '\bdynclanFamily\b') { $v433Bad += "(a) renderClanFamilyButton never names dynclanFamily - a button nothing paints keeps its authored label (SPEC V433a, V429)" }
    if ($pb433 -match 'string\.(find|match|sub)\s*\(') { $v433Bad += "(c) renderClanFamilyButton tells a family from a clan by picking the NAME apart - the value already carries which list it came from (SPEC V433c, V412b)" }
}
# (d) the Revenant row belongs to the union's picker again
$rev433 = LuaFn $rootTxt 'mfRevenantShown'
if (-not $rev433) { $v433Bad += "(f) mfRevenantShown is gone from the root form (SPEC V209, V413)" }
elseif ((NoComments $rev433) -notmatch 'MF\.list\s*==\s*"clanFamily"') { $v433Bad += "(d) mfRevenantShown does not test MF.list against clanFamily - the Revenant row would show for no picker on the sheet (SPEC V433d, V413a)" }
# (g) the two phrases of the line are keyed in both halves, and the four of T954 are gone
foreach ($p433 in @('Clan/Family', 'Select Clan/Family')) {
    if (-not $ptK.Contains($p433)) { $v433Bad += "(g) '$p433' has no [pt] key - the line would read English to a player reading Portuguese (SPEC V9, V10)" }
    if (-not $enK.Contains($p433)) { $v433Bad += "(g) '$p433' has no [en] key (SPEC V28)" }
}
foreach ($p433 in @('Clan', 'Family', 'Select Clan', 'Select Family')) {
    if ($ptK.Contains($p433) -or $enK.Contains($p433)) { $v433Bad += "(g) '$p433' is still keyed in localization.lang - the phrase has no reader since T972, and a key nobody reads is dead data (SPEC I145g, B55)" }
}
if ($v433Bad) { foreach ($b433 in ($v433Bad | Select-Object -First 12)) { Fail "V433 $b433" } }
else { Pass "V433 the Clan/Family line is dynclanFamily over the union clanFamily (kind Clan), the union is built inside its markers and mirrored, mfKind answers Family by isFamilyName - the one body of the species question - and the Revenant row asks clanFamily" }
# ---- V431: the two FIXED lines of the picker wear the empty invitation - italic at 0.80 ----
# SPEC V431, I148a, T967, user 2026-09-05 (4th batch). `-- Remove --` and `-- Custom --` are
# not names and not values: they are the way OUT of a row and the way to a name of your own,
# and the owner asked that the eye read them the way it reads the empty invitation mfLabel
# paints - italic, dimmed to 0.80. Authored in the XML and not written from Lua, on purpose:
# V355(b)/(c) count the Lua writers of fontStyle and of 0.80 on the root form and want every
# one of them inside mfLabel, and routing the two buttons through mfLabel trips V383(b) (the
# second argument is a sheet.X literal) and V369(e) (the remove text comes from mfShown("")).
# The host has taken both properties on a <button> before (fontStyle in HedgePicker, opacity
# in five other sheets of the repo), so B155 asks for no probe. The NUMBER is mfLabel's and is
# read off it here - a third owner of 0.80 is B70 (SPEC V244, V429c).
#
# Mutation (SPEC V20, V222): drop fontStyle from one button -> red on (a); opacity="0.75" on
# one -> red on (b); write .opacity on btnMfRemove inside mfPaint -> red on (c) and on V355(b).
$v431Bad = @()
$doc431 = Doc (Join-Path $dir "WoD20th.lfm")
$lua431 = CodeOf (Join-Path $dir "WoD20th.lfm")
$lbl431 = LuaFn $rootTxt 'mfLabel'
$want431 = $null
$btns431 = @{}
foreach ($n431 in @('btnMfRemove', 'btnMfCustom')) { $btns431[$n431] = @($doc431.SelectNodes("//layout[@name='mfSearch']/button[@name='$n431']"))[0] }
# (d) zero-guard FIRST: a button not found, or mfLabel gone, and every leg below reads nothing
foreach ($n431 in @('btnMfRemove', 'btnMfCustom')) {
    if ($null -eq $btns431[$n431]) { $v431Bad += "(d) $n431 is not authored inside mfSearch - the fixed line is gone and legs (a)-(c) would measure nothing (SPEC V431d, V369a, V379a, V209)" }
}
if (-not $lbl431) { $v431Bad += "(d) mfLabel is gone from the root form - the number the two buttons have to match has no owner (SPEC V431d, V209)" }
if (-not $v431Bad) {
    # (b) the number is mfLabel's: the opacity it writes right after the italic of the EMPTY state
    $want431 = [regex]::Match((NoComments $lbl431), '(?s)\.fontStyle\s*=\s*"italic";\s*\w+\.opacity\s*=\s*([0-9.]+)')
    if (-not $want431.Success) { $v431Bad += "(b) mfLabel no longer writes opacity right after the italic of the empty state - the number this rule reads off it is gone (SPEC V431b, V355b, I114e)" }
    foreach ($n431 in @('btnMfRemove', 'btnMfCustom')) {
        $b431 = $btns431[$n431]
        # (a) both properties, AUTHORED
        if ($b431.GetAttribute('fontStyle') -ne 'italic') { $v431Bad += "(a) $n431 authors fontStyle='$($b431.GetAttribute('fontStyle'))' and the fixed line reads italic like the empty invitation (SPEC V431a, I148a)" }
        if (-not $b431.HasAttribute('opacity')) { $v431Bad += "(a) $n431 authors no opacity - the fixed line reads at full weight beside an invitation that is dimmed (SPEC V431a, I148a)" }
        elseif ($want431.Success -and $b431.GetAttribute('opacity') -ne $want431.Groups[1].Value) { $v431Bad += "(b) $n431 authors opacity='$($b431.GetAttribute('opacity'))' and mfLabel dims the empty state to $($want431.Groups[1].Value) - the two cues drifted apart, and the number has one owner (SPEC V431b, B70, V244)" }
        # (c) no Lua writer of either property on the two: mfLabel stays the one writer V355 counts
        if ($lua431 -match ('"' + $n431 + '"\]\s*\.(opacity|fontStyle)\s*=')) { $v431Bad += "(c) the root form writes opacity or fontStyle on $n431 from Lua - the XML is the one owner of the dress, and a Lua writer outside mfLabel is the second voice V355(b)/(c) refuse (SPEC V431c, V355)" }
    }
}
if ($v431Bad) { foreach ($b in $v431Bad) { Fail "V431 $b" } }
else { Pass "V431 btnMfRemove and btnMfCustom author italic at opacity $($want431.Groups[1].Value), the number read off mfLabel, and no Lua writes either property on them" }

# ---- V430: a painter that TRANSLATES follows the language switch, the moment it happens ----
# SPEC V430, I148b, B156, T968, user 2026-09-05 (4th batch). The language traversal of WoD20.6
# skips every dyn* control on purpose (SPEC V31): whoever writes such a text owns both
# languages for it. So a picker button changes language only when ITS painter runs, and the
# painter runs when ITS dataLink fires. Twelve painters on this sheet translate what they
# write - they call mfLabel or translateSheetText - and six of them hung off links that
# watched the row's fields and not the language: the backgrounds stayed in English after the
# switch until one background was touched, and then all sixteen followed (SPEC B156). The tabs
# had put 'language' on their links already; the root form had not.
#
# The rule is per PAINTER and not per link: a painter reached by two links needs ONE of them
# to observe the language (renderXPLedger has a language link and an xpManual link). A
# painter reached by NO link is a FAIL and not an exemption - a painter nothing calls is
# either dead or wired to an event this sweep cannot see, and both are worth a look.
# Exceptions are NAMED, and the roster is empty today: a painter that must not follow the
# language enters here by name and with a reason, never by vacuity (SPEC V430b, V366c).
#
# Mutation (SPEC V20, V222): drop 'language' from the background_* link on the root form ->
# red on (a). Probe: move 'language' to the END of a fields list -> green, position is not
# the subject.
$V430_EXEMPT = @()
$v430Bad = @()
$v430Painters = @{}
$v430Links = New-Object System.Collections.ArrayList
foreach ($f430 in $files) {
    $raw430 = [System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($f430.FullName))
    $raw430 = [regex]::Replace($raw430, '(?s)<!--.*?-->', '')
    $code430 = CodeOf $f430.FullName
    foreach ($m430 in [regex]::Matches($code430, 'function\s+(render\w+)\s*\(')) {
        $nm430 = $m430.Groups[1].Value
        $body430 = NoComments (LuaFn $code430 $nm430)
        if ($body430 -match 'mfLabel\(|translateSheetText\(') { $v430Painters[$nm430] = $f430.Name }
    }
    foreach ($m430 in [regex]::Matches($raw430, '(?s)<dataLink\b([^>]*?)(/>|>(.*?)</dataLink>)')) {
        $attrs430 = $m430.Groups[1].Value
        $handler430 = ''
        $am430 = [regex]::Match($attrs430, 'onChange="([^"]*)"')
        if ($am430.Success) { $handler430 = $am430.Groups[1].Value }
        $em430 = [regex]::Match($m430.Groups[3].Value, '(?s)<event name="onChange">(.*?)</event>')
        if ($em430.Success) { $handler430 += ' ' + $em430.Groups[1].Value }
        $obs430 = ($attrs430 -match 'field="language"') -or ($attrs430 -match "fields=`"\{[^}]*'language'")
        [void]$v430Links.Add(@{ File = $f430.Name; Observes = $obs430; Handler = $handler430 })
    }
}
# (c) zero-guard: twelve painters were measured on 2026-09-05, and a sweep that finds fewer
# is a broken regex, not a tidier sheet (SPEC V209, B7)
if ($v430Painters.Count -lt 12) { $v430Bad += "(c) only $($v430Painters.Count) translating painter(s) were found across the sheet and twelve were measured on 2026-09-05 - the sweep shrank, not the sheet (SPEC V430c, V209, B7)" }
if ($v430Links.Count -eq 0) { $v430Bad += "(c) no dataLink was read on any form - every painter below would fail for the wrong reason (SPEC V430c, V209)" }
foreach ($p430 in ($v430Painters.Keys | Sort-Object)) {
    if ($V430_EXEMPT -contains $p430) { continue }
    $calls430 = @($v430Links | Where-Object { $_.Handler -match ('\b' + $p430 + '\s*\(') })
    if ($calls430.Count -eq 0) { $v430Bad += "(a) $p430 [$($v430Painters[$p430])] translates and no dataLink calls it - a painter nothing triggers keeps the old language for ever, or is wired somewhere this sweep cannot see (SPEC V430a, V430c)" }
    elseif (@($calls430 | Where-Object { $_.Observes }).Count -eq 0) { $v430Bad += "(a) $p430 [$($v430Painters[$p430])] translates and none of its $($calls430.Count) dataLink(s) observes 'language' - its text stays in the old language until someone touches the row (SPEC V430a, B156, V31)" }
}
foreach ($x430 in $V430_EXEMPT) { if (-not $v430Painters.ContainsKey($x430)) { $v430Bad += "(b) '$x430' is on the exemption roster and is not a translating painter - a stale name excuses nothing and hides the next gap (SPEC V430b, V20)" } }
if ($v430Bad) { foreach ($b in $v430Bad) { Fail "V430 $b" } }
else { Pass "V430 all $($v430Painters.Count) translating painters ($(($v430Painters.Keys | Sort-Object) -join ', ')) are reached by a dataLink that observes 'language', with $($V430_EXEMPT.Count) named exemption(s)" }

# ---- V432: descClan and descFamily are MECHANICS derived from the sheet's own tables ------
# SPEC V432, I148d, T850 (Family) and T971 (Clan), user 2026-09-05. The
# owner asked the ? of a clan or a family to say what the book STATS - the Disciplines and the
# Weakness - and nothing else. Both facts already have an owner on the root form: CLANS holds
# every trio (SPEC I17) and FAMILY_WEAKNESS every family sentence (SPEC V412f); the clan sentence
# has no owner on the sheet - the box shows FAMILY only (V412d) - so research/clan_weakness.tsv
# (T970) owns it in both languages. The module is DERIVED from them by research/gen_traits_desc.ps1
# and this rule reads it back against each. A module that drifted from its table would show one trio in the pane and fill another
# into the slots (V135 with the player watching). An OPEN trio (choice/open in CLANS) carries the
# book's phrase from research/clan_disc_open.tsv instead of three names.
#
# Mutation (SPEC V20, V222): swap one discipline in descFamily_en.lua -> red on (b); reword one
# weakness in the module without touching FAMILY_WEAKNESS -> red on (c); add a third line of
# prose to block 3 -> red on (a). Probe: reorder two ENTRIES -> green, order is not the subject.
$V432_KINDS = @(
    [pscustomobject]@{ Kind = 'Family'; List = 'family'; Marker = 'FAMILY_DESC'; Weak = 'table' }
    [pscustomobject]@{ Kind = 'Clan';   List = 'clan';   Marker = 'CLAN_DESC';   Weak = 'tsv' }
)
$v432Bad = @()
$v432Seen = 0
$v432Open = @{}
# the clan sentences, both halves, keyed by the picker value (T970). Zero-guarded below: a tsv that
# parsed to nothing would let leg (c) pass on every clan by vacancy (B7).
$v432Tsv = @{}
$tsv432 = Join-Path $PSScriptRoot 'research\clan_weakness.tsv'
if (Test-Path -LiteralPath $tsv432) {
    foreach ($l432 in [System.IO.File]::ReadAllLines($tsv432, [System.Text.Encoding]::UTF8)) {
        if ($l432 -eq '' -or $l432.StartsWith('#')) { continue }
        $c432 = $l432 -split "`t"
        if ($c432.Count -ge 5) { $v432Tsv[$c432[0]] = @{ en = $c432[3]; pt = $c432[4] } }
    }
}
$open432 = Join-Path $PSScriptRoot 'research\clan_disc_open.tsv'
if (Test-Path -LiteralPath $open432) {
    foreach ($l432 in [System.IO.File]::ReadAllLines($open432, [System.Text.Encoding]::UTF8)) {
        if ($l432 -eq '' -or $l432.StartsWith('#')) { continue }
        $c432 = $l432 -split "`t"
        if ($c432.Count -ge 3) { $v432Open[$c432[0]] = @{ en = $c432[1]; pt = $c432[2] } }
    }
}
# (e) zero-guard FIRST: the two tables every entry is read against
if ($null -eq $clanEntries -or $clanEntries.Count -lt 84) { $v432Bad += "(e) CLANS parsed to $(@($clanEntries).Count) entries and it holds 61 clans + 23 families - the trio every module line is read against is missing (SPEC V432e, V209, B7)" }
if ($v432Tsv.Count -lt 61) { $v432Bad += "(e) research/clan_weakness.tsv parsed to $($v432Tsv.Count) row(s) and the clan list has 61 - leg (c) would pass the clans by vacancy (SPEC V432e, T970, B7)" }
if ($null -eq $vals412 -or $vals412.Count -eq 0) { $v432Bad += "(e) FAMILY_WEAKNESS read back empty - leg (c) would compare every family sentence against nothing (SPEC V432e, V412f, B7)" }
foreach ($k432 in $V432_KINDS) {
    $names432 = @(@($PICKER[$k432.List]) | Where-Object { $_ -ne '' })
    $en432 = DescEntries (Join-Path $plugin "desc$($k432.Kind)_en.lua") $k432.Marker 'en'
    $pt432 = DescEntries (Join-Path $plugin "desc$($k432.Kind)_pt.lua") $k432.Marker 'pt'
    if ($names432.Count -eq 0) { $v432Bad += "(e) PICKER_LIST[$($k432.List)] read back empty (SPEC V432e, B7)"; continue }
    if ($null -eq $en432 -or @($en432).Count -eq 0) { $v432Bad += "(e) desc$($k432.Kind)_en.lua is missing or its $($k432.Marker) markers are gone - the ? on a $($k432.List) falls to V360c's sentence (SPEC V432e, V209)"; continue }
    if ($null -eq $pt432 -or @($pt432).Count -eq 0) { $v432Bad += "(e) desc$($k432.Kind)_pt.lua is missing or its $($k432.Marker) markers are gone (SPEC V432e, V209)"; continue }
    $enMap432 = DescMap $en432
    $ptMap432 = DescMap $pt432
    # (d) keys = the list, in both halves
    foreach ($n432 in $names432) {
        if (-not $enMap432.ContainsKey($n432)) { $v432Bad += "(d) '$n432' is offered by PICKER_LIST[$($k432.List)] and desc$($k432.Kind)_en holds no entry for it (SPEC V432d, V408)" }
        if (-not $ptMap432.ContainsKey($n432)) { $v432Bad += "(d) '$n432' has no entry in desc$($k432.Kind)_pt (SPEC V432d, V210a)" }
    }
    foreach ($key432 in $enMap432.Keys) { if ($names432 -notcontains $key432) { $v432Bad += "(d) desc$($k432.Kind)_en holds '$key432' and the list does not offer it - text nobody can reach (SPEC V432d, V366b)" } }
    foreach ($n432 in $names432) {
        if (-not $enMap432.ContainsKey($n432) -or -not $ptMap432.ContainsKey($n432)) { continue }
        $v432Seen++
        foreach ($half432 in @(@('en', $enMap432[$n432], 'Disciplines: ', 'Weakness: '), @('pt', $ptMap432[$n432], 'Disciplinas: ', 'Fraqueza: '))) {
            $lang432 = $half432[0]
            $body432 = $half432[1] -replace "`r`n", "`n"
            $blocks432 = @($body432 -split "`n`n`n")
            if ($blocks432.Count -ne 3) { $v432Bad += "(a) [$lang432] '$n432' has $($blocks432.Count) block(s) and I21 gives three - the mechanics block is not where the reader looks for it (SPEC V432a, I21)"; continue }
            $lines432 = @($blocks432[2] -split "`n")
            if ($lines432.Count -ne 3 -or $lines432[1] -ne '' -or -not $lines432[0].StartsWith($half432[2]) -or -not $lines432[2].StartsWith($half432[3])) {
                $v432Bad += "(a) [$lang432] '$n432' block 3 is not exactly '$($half432[2].Trim())' + blank + '$($half432[3].Trim())' - the owner asked for the mechanics and nothing else (SPEC V432a, I148d)"
                continue
            }
            $disc432 = $lines432[0].Substring($half432[2].Length)
            $weak432 = $lines432[2].Substring($half432[3].Length)
            # (b) the trio is CLANS's, in CLANS's order; an open trio carries the book phrase
            $ce432 = $clanEntries[$n432]
            if ($null -eq $ce432) { $v432Bad += "(b) '$n432' has no CLANS entry to read its trio against (SPEC V432b, V212)" }
            elseif (@($ce432.choice).Count -eq 0 -and $ce432.open -eq 0) {
                $want432 = @()
                foreach ($d432 in $ce432.fixed) { if ($lang432 -eq 'en') { $want432 += $d432 } elseif ($ptVal.ContainsKey($d432)) { $want432 += $ptVal[$d432] } else { $want432 += $d432 } }
                if ($disc432 -ne ($want432 -join ', ')) { $v432Bad += "(b) [$lang432] '$n432' lists '$disc432' and CLANS fixes '$($want432 -join ', ')' - the pane and the slots would disagree (SPEC V432b, I17, V135)" }
            } else {
                if (-not $v432Open.ContainsKey($n432)) { $v432Bad += "(b) '$n432' has an open trio and research/clan_disc_open.tsv carries no phrase for it (SPEC V432b, I148d)" }
                elseif ($disc432 -ne $v432Open[$n432][$lang432]) { $v432Bad += "(b) [$lang432] '$n432' lists '$disc432' and clan_disc_open.tsv says '$($v432Open[$n432][$lang432])' (SPEC V432b)" }
            }
            # (c) the family sentence is FAMILY_WEAKNESS's byte for byte, and its [pt] line in the other half
            if ($k432.Weak -eq 'table' -and $null -ne $vals412) {
                if (-not $vals412.ContainsKey($n432)) { $v432Bad += "(c) '$n432' has no FAMILY_WEAKNESS row to read against (SPEC V432c, V412f)" }
                else {
                    $sent432 = $vals412[$n432]
                    $wantW432 = ''
                    if ($lang432 -eq 'en') { $wantW432 = $sent432 } elseif ($ptVal.ContainsKey($sent432)) { $wantW432 = $ptVal[$sent432] }
                    if ($weak432 -ne $wantW432) { $v432Bad += "(c) [$lang432] '$n432' says '$($weak432.Substring(0, [Math]::Min(40, $weak432.Length)))...' and the box says '$($wantW432.Substring(0, [Math]::Min(40, $wantW432.Length)))...' - one weakness, two voices (SPEC V432c, V412f, V135)" }
                }
            }
            elseif ($k432.Weak -eq 'tsv') {
                # (c) for a CLAN the sentence is the tsv's, both halves, byte for byte (SPEC V432c, T970)
                if (-not $v432Tsv.ContainsKey($n432)) { $v432Bad += "(c) '$n432' has no row in research/clan_weakness.tsv to read against (SPEC V432c, T970)" }
                elseif ($weak432 -ne $v432Tsv[$n432][$lang432]) { $v432Bad += "(c) [$lang432] '$n432' says '$($weak432.Substring(0, [Math]::Min(40, $weak432.Length)))...' and clan_weakness.tsv says '$($v432Tsv[$n432][$lang432].Substring(0, [Math]::Min(40, $v432Tsv[$n432][$lang432].Length)))...' - the pane drifted from its owner (SPEC V432c, V135)" }
            }
        }
    }
}
if ($v432Seen -eq 0) { $v432Bad += "(e) not one entry was read against its table - every leg passed by vacancy (SPEC V432e, B7)" }
if ($v432Bad) { foreach ($b in ($v432Bad | Select-Object -First 12)) { Fail "V432 $b" } }
else { Pass "V432 $v432Seen entries of $($V432_KINDS.Count) traits module(s) carry exactly Disciplines + Weakness, the trio read off CLANS and the family sentence off FAMILY_WEAKNESS, in both halves" }

Write-Host ""
if ($fail -eq 0) { Write-Host "ALL CHECKS PASSED"; exit 0 } else { Write-Host "$fail CHECK(S) FAILED"; exit 1 }
