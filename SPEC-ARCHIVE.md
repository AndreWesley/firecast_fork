# SPEC-ARCHIVE — as §T FECHADAS

As tarefas com status `x` moram aqui, ⊥ no `SPEC.md` (§I133f, §T888, §V386). Isto ⊥ é
histórico morto: `spec-slice.ps1` & o check de §V384 leem os **2** arquivos ∴ uma citação
a uma §T arquivada segue resolvendo, & quebrar isso é a §V384c.

Regras que o gate cobra (§V386):

- **(a)** 1 id vive em 1 arquivo só — id nos 2 = uma das cópias é a que ninguém lê
- **(b)** só §T com status `x` sai do `SPEC.md`; tarefa aberta ⊥ é arquivada
- **(c)** a numeração é monotônica sobre a UNIÃO dos 2 ∴ o próximo id é `max(SPEC, ARCHIVE)+1`
  & arquivar **⊥ libera id** p/ reuso

§B ficou INTEIRA no `SPEC.md`: §B ⊥ tem coluna de status ∴ ⊥ ∃ "fechada" p/ decidir o que
sai, & os §B são citados como lição viva (§B7, §B74, §B103). §I133f pede as 2 seções &
esta rodada entregou 1 — a emenda é de `/ck:spec`, ⊥ de build.

## §T TASKS (arquivadas)

id|status|task|cites
T1|x|criar `WoD20th/` + copiar `prime_on.png` `prime_off.png` `RPGmeister.jpg` ← `Mage20th/images/`|V3
T2|x|`WoD20th.lfm` — raiz sheetTemplate + tabControl 6 abas + evento autoupdater ← `Mage20th.lfm`|I1,V4
T3|x|`WoD20.1.lfm` Main — templates Header/Attribute/Ability/OpenAbility/Dots10/Willpower; header 9, ATTRIBUTES 9, ABILITIES 30, VIRTUES 3, HUMANITY, WILLPOWER, FAITH, HEALTH, BACKGROUNDS 5, EXPERIENCE, avatar|V1,V2,V5
T4|x|`WoD20.2.lfm` Traits — NUMINA & OTHER TRAITS 11 linhas abertas 5 dots + textEditor livre|V1,V5
T5|x|`WoD20.3.lfm` Combat & Items — COMBAT 4 linhas (weapon\|damage\|diff) + ITEMS/GEAR/EQUIPMENT/OTHER ← `M20.3.lfm`|V1
T6|x|`WoD20.4.lfm` Background — desc fields + Appearance/Concept/Nature/Languages/Prelude ← `M20.4.lfm`|V1
T7|x|`WoD20.5.lfm` Notes — 3 textEditors ← `M20.5.lfm`|V1
T8|x|`WoD20.6.lfm` Credits — ← `M20.6.lfm`, trocar textos p/ Hunters Hunted|-
T9|x|`module.xml` version `0.6` → `0.7`|-
T10|x|`localization.lang` — chaves pt/en faltantes: Awareness, Animal Ken, Ride, Performance, Larceny, Finance, Motivation, Occupation, Residence, Demeanor, Conscience, Self-Control, Courage, Humanity, Faith, Virtues, Numina, Difficulty|-
T11|x|`rdk -l` → compile + lint → regerar `output/World of Darkness 20th Anniversary Edition.rpk`|V6,V7
T12|x|`verify-hunters-hunted.ps1` @ raiz do repo — gate runnable p/ invariantes ⊥ checáveis a olho|V1,V3,V4,V5,V7,V8
T13|x|`module.xml` `<description>` lista 5 fichas, agora ! 6 — citar Hunters Hunted|-
T14|x|`WoD20.6.lfm` rotina de tradução — cobrir `checkBox` & `button` além de `label` ∴ 7 níveis Health passam a traduzir|V10,R2
T15|x|`WoD20.3.lfm` header COMBAT — 1 label c/ padding → 3 labels (`Weapon/Attack`, `Damage`, `Diff.`)|V11
T16|x|`WoD20.6.lfm` strings PT→EN: `Tema:`→`Theme:`, `Versão Atual:`→`Current Version:`, `Sua Versão:`→`Available Version:`, `Não pedir para atualizar.`→`Don't ask to update.`, `Conheça a Mesa:`→`Visit the Table:`, `Atualizar`→`Update`. Créditos do Mage mantidos, só traduzidos|V9
T17|x|`WoD20.6.lfm` combo tema — `items` `{'Escuro','Claro'}`→`{'Dark','Light'}` + `defaultValue="Dark"` + comparação Lua `== "Light"`, tudo no mesmo commit|V12,R3
T18|x|`localization.lang` — [pt] p/ ∀ string visível nova; [en] identidade p/ ∀ string que a ficha renderiza ∴ §R.1 vira irrelevante|V10,R1
T19|x|`verify-hunters-hunted.ps1` — check §V.10 (chave [pt] & [en] p/ todo label/checkBox/button) & §V.11 (⊥ padding em label)|V10,V11
T20|x|`?` testar em Firecast: `findClass("tab")` alcança & `tab.title` gravável → sim: traduzir 6 abas; ⊥: registrar limite em §C|R2
T21|x|`WoD20.2.lfm` MERITS AND FLAWS — template `Merit` (nome\|tipo\|custo) × 8 merits + 8 flaws ← `V20.4.lfm`|R7,V1
T22|x|`WoD20.2.lfm` DERANGEMENTS — 1 textEditor ← `V20.4.lfm`|R7
T23|x|`WoD20.2.lfm` (Traits) 9 caixas de descrição nome-fixo ← `V20.4.lfm`: ALLIES MENTOR CONTACTS RESOURCES FAME RETAINERS STATUS INFLUENCE OTHER. HERD ⊥ (vampiro-only). ≠ T28 — aquilo é picker na aba Main|R7
T24|x|`?` LISTA arquétipos Nature/Demeanor — M20 primeiro, depois VTM, depois resto, ⊥ duplicata. ! user validar antes de T27|V14,R8
T25|x|`?` LISTA antecedentes — VTM + M20 + W20, ⊥ duplicata. ! user validar antes de T28|V14,R8
T26|x|SUPERSEDED por T49 — "⊥ inventar" virou "rascunhar & mostrar p/ validação", que ⊥ afirma nada como fato|V14,R8
T27|x|`WoD20.1.lfm` (Main) Nature & Demeanor → `edit` + picker, mesma lista, valores independentes, 1ª opção vazia|V13,V15,T24
T28|x|`WoD20.1.lfm` (Main) BACKGROUNDS 5 linhas → `edit` + picker por linha, mesma lista, valores independentes. ≠ T23|V13,V15,T25
T29|x|SUPERSEDED por T50 — o bloco migra p/ `WoD20.7.lfm` & vira 8 picker + 2 livre, ⊥ 11 linhas|V13,V15,T26
T30|x|`WoD20.3.lfm` COMBAT → 8 col × 10 linhas ← `V20.3.lfm`|R6,V1
T31|x|`WoD20.3.lfm` inventário → ARMOR (class\|rating\|penalty\|desc) + GEAR + EQUIPMENT + TRANSPORTATION ← `V20.3.lfm`; HAVEN & FEEDING ⊥|R6,V1
T32|x|`localization.lang` — [pt] & [en] p/ ∀ string & ∀ item de picker novo|V10,V17
T33|x|`verify-hunters-hunted.ps1` — checks §V.13 §V.14 §V.15 §V.16 §V.17|V13,V14,V15,V16,V17
T34|x|`WoD20.6.lfm` remover botões (Change Log, Update, RPGmeister) + combo tema + pickers de cor + checkBox `noUpdate`. Manter créditos & controle de idioma|-
T35|x|`WoD20.6.lfm` `localization` checkBox → `comboBox` `language`, items `{English, Português BR}` values `{en, pt}`, default `en`|V23,B10
T36|x|`WoD20.6.lfm` travessia da RAIZ (subir `getParent()`) + snapshot do texto EN @ load ∴ 6 abas & reversível|V21,V22,R9,R10
T37|x|`WoD20.6.lfm` troca de idioma reconstrói `items` dos pickers no idioma; `values` EN intactos|V24,V17
T38|x|`verify-hunters-hunted.ps1` — checks §V.21 §V.22 §V.23 §V.24|V21,V22,V23,V24
T39|x|`WoD20th.lfm` remover autoupdater: `Internet.download` + `Dialogs.choose` + `isNewVersion` + helper `t()`. Campos `versionInstalled` `versionDownloaded` `noUpdate` saem junto (decisão user 2026-08-17)|V1
T40|x|`WoD20.1.lfm` template `CustomAbility` (edit + 5 dots, x do 1º dot = 80 ≡ `Ability`) + 2 linhas ao fim de TALENTS & SKILLS & KNOWLEDGES; caixa 300→350 alt, ABILITIES 340→390|V1,V5,V26
T41|x|`WoD20.1.lfm` realinhar Main — pitch de coluna 290 uniforme no header (era 5\|290\|580) & 1 `width` de label + 1 `left` de entrada por coluna (Concept ≡ Nature/Demeanor); títulos de seção `left=0` & width da caixa; HUMANITY & WILLPOWER viram título acima das bolinhas, dots @80 ≡ Virtues; borda direita & inferior das caixas alinhadas|V16,V25,V26,V27
T42|x|`WoD20.7.lfm` NOVO — aba Powers, recebe o bloco de núminas + template `Trait` migrado de `WoD20.2`. Título = `NUMINA`, ⊥ "& OTHER TRAITS"|I2,V9,V27
T43|x|`WoD20th.lfm` — `<tab title="Powers">` logo após Traits|I1
T44|x|`WoD20.2.lfm` — remover bloco NUMINA (11 linhas) + `textEditor` `numinaNotes` + template `Trait`|I3
T45|x|`WoD20.2.lfm` relayout — MERITS AND FLAWS & DERANGEMENTS sobem p/ o topo (buraco deixado pelas núminas), 10 caixas de antecedente em grade regular|V27
T46|x|`WoD20.2.lfm` — `RETAINERS` → `GUIDES` (campo `retainers` INTACTO + comentário XML dizendo por quê) & caixa nova `BASE OF OPERATIONS` no slot vazio (750,830), campo `baseOfOperation`|V2,V10,V16,V25
T47|x|`WoD20.2.lfm` — label `Type` → `Page` ×2 (Merit & Flaw). Campo `type_$(num)` INTACTO|V2,V10
T48|x|`WoD20.1.lfm` picker de antecedentes += `Armory` `Base of Operations` `Guide`, ordem alfabética, ⊥ duplicata|V14,V15,V17
T49|x|SUPERSEDED por T55 — rascunho de memória substituído por lista SOURCED do `M20 Sorcerer` (§R.14) ∴ `?` cai|V14,R14
T50|x|SUPERSEDED por T56 & T57 — 1 tabela de 10 linhas virou 2 tabelas de 10 c/ radio|V13,V15,V26
T51|x|`localization.lang` — pt+en p/ strings novas: `NUMINA` `Powers` `GUIDES` `BASE OF OPERATIONS` `Page` `Armory` `Base of Operations` `Guide` + ∀ item de númina|V10,V17
T52|x|`WoD20.6.lfm` regerar `local PT` a partir do `.lang` ∴ aba Powers & rótulos novos traduzem|V22,V28
T53|x|`verify-hunters-hunted.ps1` — §V5 numina 11→10 & + `customTalent`/`customSkill`/`customKnowledge` 5 dots; checks novos §V.25 §V.26 §V.27 §V.28|V5,V25,V26,V27,V28
T54|x|`rdk -l` + instalar; conferir que mtime\|size do `.rpk` mudou|V6,V7
T55|x|LISTA de núminas = §R.14 na ordem do livro (21 Paths + 22 Phenomena = 43 itens), 1 lista ÚNICA p/ os pickers das 2 tabelas, 1º item vazio, ⊥ duplicata|V14,V15,R14
T56|x|`WoD20.7.lfm` tabela 1 — título `NUMINA` → `NUMINA: HEDGE MAGIC PATHS`; template `NuminaPicker` (radio + `comboBox` + 5 dots) × 8 (`numina_1`…`_8`) + `NuminaFree` (radio + `edit` + 5 dots) × 2 (`numina_9` `_10`)|V13,V15,V26,V30,T55
T57|x|`WoD20.7.lfm` tabela 2 NOVA `NUMINA: PSYCHIC PHENOMENA` logo abaixo da 1, mesmos templates, campos `psychic_1`…`_10`|I3,V13,V15,V26,V30,T55
T58|x|`WoD20.7.lfm` bloco `DESCRIPTION` à direita das 2 tabelas — `textEditor readOnly="true"` sem `field`, `wordWrap="true"`, altura ≈ das 2 tabelas somadas|V29,R12,R15
T59|x|`WoD20.7.lfm` tabela Lua `DESC` = canonEN → `{en=…, pt=…}` + render: lê `numinaSel` → campo da linha → valor → `DESC` → `text` do bloco; sem match → texto de estado explícito|V31,V32,V33,R13
T60|x|`WoD20.7.lfm` `<dataLink fields="{'numinaSel','numina_1'…'psychic_10'}" onChange="render()"/>` ∴ trocar radio OU trocar item da linha atualiza o bloco|V33,R13
T61|x|`WoD20.6.lfm` troca de idioma ! chamar o render de T59 (bloco ⊥ passa pela travessia de tradução) ∴ descrição acompanha `en`/`pt`|V21,V22,V31
T62|x|CONTEÚDO — transcrição VERBATIM (prosa + `System`, ⊥ samples) das 43 núminas ← `M20 Sorcerer.pdf` pág. 19-52 & 55-78 + tradução [pt] minha (user autorizou 2026-08-17, ⊥ tenho termo oficial PT) ∴ ⊥ de memória, ! ler do PDF|V32,R8,R14
T68|x|`localization.lang` + `WoD20.6` mapa `PT` — 43 nomes de númina em [pt] (tradução minha, ! user validar); `values` seguem EN canônico (§V24)|V10,V17,V24
T63|x|`localization.lang` — [pt] & [en] p/ `NUMINA: HEDGE MAGIC PATHS` `NUMINA: PSYCHIC PHENOMENA` `DESCRIPTION` + os 43 itens de picker|V10,V17,V24
T64|x|`WoD20.6.lfm` regerar `local PT` a partir do `.lang` ∴ títulos novos & 43 itens traduzem; descrições ⊥ entram no mapa|V22,V28,V31
T65|x|`verify-hunters-hunted.ps1` — exceção de `radioButton` no check §V1; checks novos §V.29 §V.30 §V.31 §V.32 §V.33; §V5 numina 10→20 linhas; §V.17 sobre os 43 itens|V1,V5,V17,V29,V30,V31,V32,V33
T66|x|`module.xml` version `0.7` → `0.8`|-
T67|x|`rdk -l` @ raiz do plugin → exit 0 & mtime\|size do `.rpk` mudou|V6,V7
T69|x|`WoD20.7.lfm` `DESC` [pt] — 21 trechos: `magos menores`×8 & `mago menor`×9 & `magia menor`×3 & `bruxo menor`×1 → `magos estáticos` \| `mago estático` \| `magia estática`. Colateral (`Milagres menores` `ferimentos menores` `chama menor` `for menor que` `magias comuns`←`regular spells`) ⊥ tocado. EN intacto (§V9)|V34
T70|x|`localization.lang` [pt] & mapa `PT` de `WoD20.6.lfm` — `NÚMINAS: CAMINHOS DE MAGIA MENOR` → `NÚMINAS: CAMINHOS DE MAGIA ESTÁTICA`. Chave & label EN intactos ∴ `values` canônicos ⊥ mexidos|V10,V24,V34
T71|x|`verify-hunters-hunted.ps1` — check §V34: regex ASCII da renderização PROIBIDA sobre ∀ `.lfm` + `.lang` ∴ recorrência = gate vermelho, ⊥ precisa grafar acento no `.ps1`|V34
T72|x|`rdk -l` @ raiz do plugin → exit 0 & mtime\|size do `.rpk` mudou|V6,V7
T73|x|`WoD20th.lfm` — `<tab title="Credits">`→`Settings`, `<tab title="Combat &amp; Items">`→`Combat`, `<tab title="Inventory">` c/ `<import file="WoD20.8.lfm"/>` logo após Combat ∴ 8 abas na ordem de §C|I1,I2
T74|x|`WoD20.6.lfm` — remover `<image src="images/RPGmeister.jpg">` & apagar o arquivo `images/RPGmeister.jpg`; caixas de créditos & settings reflow p/ `left=0` (coluna da imagem vagou)|V3,V27
T75|x|`WoD20.6.lfm` — combo `Game` logo ABAIXO de `Language` na mesma caixa: `name="cboGame"` `field="game"`, `items`=`values`=`{'', 'Vampire: The Masquerade', 'Vampire: Dark Ages', 'Werewolf: The Apocalypse', 'Mage: The Ascension'}`; caixa cresce p/ 2 linhas|I3,V13,V14,V15,V17,V24
T76|x|`WoD20.8.lfm` NOVO — aba Inventory: ITEMS · ARMOR (`armorClass`\|`armorRating`\|`armorPenalty`\|`armorDescription`) · GEAR (CARRIED) · EQUIPMENT (OWNED) · TRANSPORTATION · OTHER em grade 3×2, campos IDÊNTICOS aos de `WoD20.3`|I2,I3,V1,V2,V27,V35
T77|x|`WoD20.3.lfm` — remover as 6 caixas movidas, sobra só COMBAT (8 col × 10 linhas) & largura da aba encolhe. MESMO commit que T76 ∴ ⊥ 2 donos de `items`/`gear`/…|V1,V35,T76
T78|x|`WoD20.1.lfm` — BACKGROUNDS 5→6 linhas (`background_6` + dots `_1`…`_5`), pitch 45 mantido ∴ caixa cresce & as 3 caixas de baixo terminam na mesma borda inferior|I3,V1,V5,V26,V27
T79|x|`WoD20.1.lfm` — caixa VIRTUES parte em 2: `VIRTUES` (conscience·selfControl·courage) em cima, caixa NOVA `HUMANITY` + `WILLPOWER` abaixo; linha `Faith` SAI da Main. MESMO commit que T81 ∴ `faith_1`…`_5` ⊥ ficam órfãos|V26,V27,V35,T81
T80|x|`localization.lang` — `wod.Melee` `Armas-Brancas`→`Armas Brancas` & `wod.Firearms` `Arm. Fogo`→`Armas de Fogo` (label do template `Ability` = 125px, dimensionado p/ `Empatia com Animais` ∴ cabe)|V10,V16
T81|x|`WoD20.7.lfm` — 3ª caixa `TRUE FAITH` abaixo de `NUMINA: PSYCHIC PHENOMENA`: 1 linha `NuminaPicker field="faith"` (radio `numinaSel` fieldValue `faith` + `cboFaith` + 5 dots `faith_1`…`_5`) c/ lista PRÓPRIA `{'', 'True Faith'}` ∴ template ganha `items` por-chamada \| template novo de 1 item; bloco `DESCRIPTION` cresce p/ acompanhar as 3 caixas|I3,V1,V13,V14,V15,V26,V30,V35
T82|x|`WoD20.7.lfm` — `DESC['True Faith']` = transcrição VERBATIM dos 2 prints (§R.16: Systems · Levels • a ••••• · Miracles) + [pt] minha; `dataLink fields` += `'faith'`. Print ⊥ em mão na sessão de build → PARAR & pedir reenvio|V31,V32,V33,R13,R16
T83|x|`localization.lang` [pt]+[en] & mapa `PT` de `WoD20.6` regerado — chaves novas: `Settings`=Configurações · `Inventory`=Inventário · `Combat`=Combate · `Game`=Jogo · `TRUE FAITH`=FÉ VERDADEIRA · `True Faith`=Fé Verdadeira · `Vampiro: A Máscara` · `Vampiro: Idade das Trevas` · `Lobisomem: O Apocalipse` · `Mago: A Ascensão`. `Credits` & `Combat & Items` FICAM (outras 5 fichas do plugin usam)|V10,V16,V17,V22,V24,V28
T84|x|`verify-hunters-hunted.ps1` — §V5 background 5→6 linhas & numina 20→21 (+ linha `faith`); check §V35 (dono único & arquivo certo p/ os 14 campos migrados); §V14/§V15/§V17 alcançam `cboGame` & `cboFaith`|V5,V14,V15,V17,V35
T85|x|`rdk -l` @ raiz do plugin → exit 0 & mtime\|size do `.rpk` mudou|V6,V7
T86|x|RPGmeister fora do plugin INTEIRO — `<image>`+`<button>` das 5 fichas base + 5 `RPGmeister.jpg` + 2 chaves `wod.RPGmeister` + entrada morta do mapa `PT` + tag `<contact>`; `module.xml` `0.8`→`1.0`|V3,V10,V22,V28
T87|x|mover caixa `ARMOR` `WoD20.8`→`WoD20.3` (`armorClass`\|`armorRating`\|`armorPenalty`\|`armorDescription` + desc), abaixo de COMBAT. Remover + adicionar = 1 commit (§V35) ∴ ⊥ 2 donos|V1,V2,V16,V27,V35
T88|x|`WoD20.8.lfm` — caixa do campo `items` passa a renderizar `TRANSPORTATION` (era `ITEMS`); caixas `TRANSPORTATION` (`transportation`) & `OTHER` (`other`) REMOVIDAS a pedido ∴ 2 campos órfãos, ⊥ rename (§V2 intacto)|V2,V10,V16
T89|x|`WoD20.8.lfm` — 3 caixas NOVAS `BASE OF OPERATIONS (DEFENSES)` `(ARMAMENTS)` `(OTHER)`, campos `baseDefenses` `baseArmaments` `baseOther`, forma ≡ caixas de cima (título + 1 `textEditor`); grade final 3×2 = `gear`\|`equipment`\|`items` em cima, as 3 novas embaixo|I3,V1,V16,V27
T90|x|`WoD20.6.lfm` — `cboGame` `items` & `values` += `Hunters Hunted` ∴ 5 jogos + vazio. Segue só estado, ⊥ dispara lógica|I3,V13,V14,V15,V17,V24
T91|x|`localization.lang` [pt]+[en] & mapa `PT` de `WoD20.6` — `BASE OF OPERATIONS (DEFENSES\|ARMAMENTS\|OTHER)` + `Hunters Hunted`=`Caçadores Caçados`. `TRANSPORTATION` & `ARMOR` já ∃|V10,V16,V17,V22,V24,V28
T92|x|`verify-hunters-hunted.ps1` — §V35 `$movedTo`: os 4 campos de armor `WoD20.8`→`WoD20.3`; assert `cboGame` = 5 itens; §V1 alcança `baseDefenses` `baseArmaments` `baseOther`|V1,V14,V17,V35
T93|x|`rdk -l` @ raiz do plugin → exit 0 & mtime\|size do `.rpk` mudou|V6,V7
T94|x|`?` §R — como tornar bolinha SÓ-LEITURA: `imageCheckBox` tem `enabled`\|`readOnly`? senão `<image>` pintada por Lua. Ler `sdk/rrpgGUI.lua`. BLOQUEIA T101|R4,R12
T95|x|`WoD20.1.lfm` — attr `appearance` exceção: dot1 `<image>` → `imageCheckBox field="appearance_1"` @ x=125 ∴ desligável & coluna de dot intacta. Template próprio \| `Attribute` ganha variante|I3,V1,V5,V26
T96|x|`WoD20.1.lfm` — HEALTH posicional: 7 `checkBox` de nome semântico → 10 `checkBox` `health_1`…`_10`; os 7 nomes velhos SAEM (órfãos §I3). `width` ! caber [pt] mais longo (§V38)|I3,V1,V2,V38
T97|x|`WoD20.1.lfm` — combo `healthLevels` (`1`…`10`, default `7`) ao lado do título HEALTH; nome ⊥ `cbo*` ∴ combo de VALOR, fora dos checks de picker (§C); título encurta & recentra no espaço restante (exceção §V27 declarada em §C)|I3,V12,V13,V27
T98|x|`WoD20.1.lfm` — tabela Lua `LEVELS[1..10]` verbatim de §C + `dataLink field="healthLevels"` → escreve `text` dos 10 `checkBox` & esconde as linhas > n; sem match → estado explícito|V19,V33,V39,R13
T99|x|`WoD20.1.lfm` — BACKGROUNDS 7→9 linhas (`background_7`…`_9` + dots), caixa FICA 300px, pitch 40→30|I3,V1,V5,V26,V27
T100|x|`WoD20.3.lfm` — espelho EDITÁVEL de HEALTH (`health_1`…`_10`) & VIRTUES (`conscience` `selfControl` `courage`) ∴ 2 donos por field, DECLARADO em §I3|I3,V1,V36
T101|x|`WoD20.3.lfm` — espelho SÓ-LEITURA de 7 abilities & 4 attrs pelo mecanismo que T94 achar ∴ ⊥ editável & ⊥ 2º dono|I3,V1,V36,T94
T102|x|`WoD20.2.lfm` — remover caixa externa `MERITS AND FLAWS`; as 2 tabelas sobem p/ topo (`left=0`\|`390`, `top=0`); 8→10 linhas cada (`_m9` `_m10` `_f9` `_f10`); caixa 240→290 & DERANGEMENTS + faixa de antecedentes acompanham|I3,V1,V16,V27
T103|x|`WoD20.4.lfm` — fix §B16: 3ª coluna do grid `left=585`→`635` ∴ ⊥ sobrepõe o `edit` da 2ª. Corrige as 2 linhas (Height×Eyes & Weight×Hair)|V37
T104|x|`WoD20.4.lfm` — rótulos: caixa `aparencia` → `Languages`, caixa `idiomas` → `Goals` (campos INTACTOS, §V2) + caixas NOVAS `Moral Flaws` (`moralFlaws`) & `Fears` (`fears`) abaixo|I3,V1,V2,V10,V16,V27
T105|x|`WoD20.4.lfm` — campo novo `Nationality` (`desc_nacionalidade`) abaixo de Weight, template `Descricao`|I3,V1,V10,V16,V25
T106|x|`WoD20.9.lfm` NOVO — aba Progress: `Total Experience` (⊥ `field`, = `spentXP`+`experience`, Lua) · `Spent Experience` (`spentXP`) · `Current Experience` (`experience`, ESPELHO da Main) · área livre `xpLog`|I2,I3,V1,V16,V27,V29,V36
T107|x|`WoD20th.lfm` — `<tab title="Progress">` c/ `<import file="WoD20.9.lfm"/>` entre Background & Notes ∴ 9 abas|I1,I1b,I2
T108|x|`localization.lang` [pt]+[en] & mapa `PT` de `WoD20.6` — chaves novas: `Goals` `Moral Flaws` `Fears` `Nationality` `Total Experience` `Spent Experience` `Current Experience` `Progress` + os 7 rótulos de vitalidade que o Lua injeta (`Bruised`…`Incapacitated`, já ∃ mas agora ! ∈ mapa `PT`, §V28). `1`…`10` do combo ⊥ levam chave (combo de valor, §C). `Languages` `Appearance` já ∃|V10,V16,V17,V22,V24,V28
T109|x|`verify-hunters-hunted.ps1` — §V5 background 7→9 & health 7 níveis → 10 posicionais & appearance 4→5 dots; merit 8→10; checks NOVOS §V36 (espelhos ⊆ §I3) §V37 (sobreposição) §V38 (`checkBox` vs [pt]) §V39 (`LEVELS`); órfãos += os 7 nomes de vitalidade|V1,V5,V36,V37,V38,V39
T110|x|`rdk -l` @ raiz do plugin → exit 0 & mtime\|size do `.rpk` mudou|V6,V7
T111|x|`WoD20.3.lfm` relayout — `COMBAT TRAITS` (670,430)→(670,0), 280×320; `HEALTH` (670,0)→(670,330), 280×300; `VIRTUES` (670,310,280,110)→(390,350,270,110) c/ layouts internos left 15→10 & título `width` 280→270. Só `left top width`: ⊥ campo, ⊥ nome de widget, ⊥ Lua, ⊥ `.lang`|V27,V36,V37,V40
T112|x|`verify-hunters-hunted.ps1` — check §V40: caixas irmãs do `scrollBox` c/ `left top width height` ⊥ se cruzam ∴ o lado-a-lado novo (`ARMOR`\|`VIRTUES`\|coluna direita) tem gate|V40
T113|x|`rdk -l` @ raiz do plugin → exit 0 & mtime\|size do `.rpk` mudou|V6,V7
T115|x|`WoD20th.lfm` (form raiz) — `HEALTH_MARKS = {" ", "/", "X", "*"}` + `cycleHealthMark(sheet, n)` (avança, volta ao 1º após o 4º) & `resetHealthMark(sheet, n)`; `renderHealthTrack` normaliza valor ∉ lista → `" "`|V41,V42,V19,R19
T116|x|`WoD20.1.lfm` HEALTH — cada linha vira `label` do nível (esquerda, pintado pelo Lua ≡ hoje) + caixa de marca `rectangle hitTest="true"` c/ `label field="health_N"` dentro, encostada na borda direita da caixa; `onClick`→`cycleHealthMark`, `onMenu`→`resetHealthMark`. `checkBox` `dynHealth1_N` some|V1,V16,V25,V41,V43,T115
T117|x|`WoD20.3.lfm` — mesma troca no espelho (`dynHealth3_`). Sincronia entre abas = NDB via `label field=` ∴ ⊥ mexe em `dataLink` (§V36). MESMO commit que T116 ∴ ⊥ 2 formas de vitalidade convivendo|V1,V36,V41,V43,T116
T118|x|`verify-hunters-hunted.ps1` — §V5 conta as 10 linhas de vitalidade pela forma nova (⊥ mais `checkBox field="health_N"`); checks NOVOS §V41 (lista 1× no raiz, 4 símbolos, ordem) §V42 (normalização no render) §V43 (`onClick` & `onMenu` em ∀ caixa de marca); §V38 ⊥ mede mais os níveis (viraram `label` ∴ §V16/§V25 assumem)|V5,V16,V38,V41,V42,V43
T119|x|`rdk -l` → exit 0 & `.rpk` mudou; depois `rdk -i` → `.rpk` instalado em `%APPDATA%\Firecast\Plugins\ambesek.wod.20th.rpk` c/ mesmo size (CLAUDE.md)|V6,V7
T120|x|`WoD20th.lfm` — `healHealthMark()`: acha a maior linha marcada ∈ 1…`healthLevels` & zera SÓ ela. `cycleHealthMark(n)` chama quando o próximo símbolo seria `" "` (⊥ escreve em `n`); `resetHealthMark(n)` vira chamada direta da cura ∴ 1 rotina só|V41,V44,V45
T121|x|`verify-hunters-hunted.ps1` — checks NOVOS §V44 (`cycleHealthMark` ⊥ escreve `HEALTH_MARKS[1]` em `n`; wrap → `healHealthMark`) & §V45 (busca limitada por `healthLevels`; render ⊥ chama cura)|V44,V45,V20
T122|x|`rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T123|x|`WoD20th.lfm` — `regroupHealthMarks()`: conta marcas por tipo ∈ 1…`healthLevels` & reescreve as linhas do mais grave p/ o menos, vazias no fim. Ordem vem de `HEALTH_MARKS` (⊥ lista nova). Chamada no fim de `cycleHealthMark` & dentro de `renderHealthTrack` (após normalização §V42)|V41,V42,V46,V47
T124|x|`verify-hunters-hunted.ps1` — checks NOVOS §V46 (`regroupHealthMarks` ∃, varre gravidade do topo, chamada por `cycleHealthMark` & `renderHealthTrack`) & §V47 (limitada por `healthLevels`, lê `HEALTH_MARKS`, ⊥ escreve `HEALTH_MARKS[1]` fora do bloco de vazias)|V46,V47,V20
T125|x|`rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T126|x|`WoD20.1.lfm` — caixa `ABILITIES`: `color="#FFFFFF00"` → `color="black"` ∴ para de ler cinza (§B18). `strokeColor`/`strokeSize` ficam|V48
T127|x|`WoD20.4.lfm` — remover caixas `Concept` (`personalidade`) & `Nature` (`natureza`); `Moral Flaws` & `Fears` descem p/ (0,315) & (205,315). Slot (0,675)+(205,675) vaga; `Prelude` ⊥ muda|I3,V1,V2,V27
T128|x|`verify-hunters-hunted.ps1` — check NOVO §V48 (fill de caixa de seção = black, c/ as 2 exceções); órfãos += `personalidade` `natureza`|V48,I3
T129|x|`rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T130|x|`WoD20.1.lfm` + `WoD20.3.lfm` — caixa HEALTH ganha `name="dynHealth_box"` \| `name="dynHealth3_box"`; `height` XML segue 300 = MÁXIMO (n=10) ∴ §V40 mede pior caso|V40,V49
T131|x|`WoD20th.lfm` — `HEALTH_ROW_PITCH = 27` & `HEALTH_BOX_PAD = 30` + `renderHealthTrack` faz `box.height = n * HEALTH_ROW_PITCH + HEALTH_BOX_PAD` (`box = form[prefix .. "box"]`), guardado p/ box nil|V19,V49,R20
T132|x|`verify-hunters-hunted.ps1` — check NOVO §V49: pitch do XML (linha2.top − linha1.top) = `HEALTH_ROW_PITCH` nas 2 abas; `height` XML = 10·pitch + PAD; render ajusta `height` a partir de `healthLevels`|V49,V20
T133|x|`rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T134|x|(1) `WoD20.1.lfm` — avatar `<rectangle>` + `<image>` 375→405 alto ∴ fecham em y=810 ≡ `BACKGROUNDS`. Caixa `HUMANITY & WILLPOWER` ⊥ tocada|V48
T135|x|(2) `WoD20th.lfm` `HEALTH_BOX_PAD` 30→36 + altura declarada da caixa HEALTH 300→306 em `WoD20.1` & `WoD20.3` (§V49 = caso n=10)|V49
T136|x|(3) `WoD20.2.lfm` — `Merit` box 380→410 (0..410) · `Flaw` box (390,380)→(420,410) ∴ 420..830 · `DERANGEMENTS` (810,230)→(840,200); template `Merit` 350→380 c/ coluna do nome 200→230 & 3 labels de cabeçalho acompanham|V16,V25,V26,V37,V40
T137|x|(4) `WoD20.7.lfm` — `cboFaith` items & values `{'', 'Christianity', 'Judaism', 'Islam', 'Other'}`; renderer da linha `faith` usa chave FIXA `True Faith` ∴ ∀ item abre o mesmo texto|V13,V15,V24,V32,V50
T138|x|(5) `WoD20.3.lfm` — `ARMOR` altura 300→225 & `armorDescription` 180→110; HEALTH box top 330→350; caixa `WILLPOWER` NOVA (390,470,270,105): bolinhas SÓ-LEITURA `willpower_1`…`_10` + `checkBox` espelho `willpower_c1`…`_c10`; Lua de pintura junto de `renderCombatTraits`|I3,V1,V27,V36,V40,V51
T139|x|(6) `WoD20.4.lfm` — grid de descritores 125→100; `Languages` 135→110 · `Moral Flaws`/`Fears` 315→290 · `Goals` 495→470 · `Prelude` (410,135,630,715)→(410,110,630,535) ∴ fecha 645 ≡ `Goals`|V27,V37,V40
T140|x|(7) `WoD20.9.lfm` — `EXPERIENCE` (0,0,420,150)→(0,0,1040,90) c/ os 3 pares label+edit em 3 colunas; `EXPERIENCE LOG` (0,160,1040,480)→(0,100,1040,540)|V16,V25,V27,V37,V40
T141|x|(8) `WoD20.6.lfm` — caixa de opções p/ o topo & créditos p/ o fim; `Andre (Styllern)` = Developer separado, demais nomes/papéis VERBATIM sob "Based on the RPGmeister sheet plugin, by:"; combo `Theme` (`sheetTheme`, `{'Modern','Victorian Era'}`, default `Modern`) abaixo de `Game`|I3,V9,V10,V16,V27
T142|x|(8+4) `localization.lang` [pt]+[en] & mapa `PT` de `WoD20.6` — `Theme`=Tema · `Modern`=Moderno · `Victorian Era`=Era Vitoriana · `Developer`=Desenvolvedor · `Based on the RPGmeister sheet plugin, by:` · `Christianity`=Cristianismo · `Judaism`=Judaísmo · `Islam`=Islamismo · `Other`=Outro|V10,V17,V22,V24,V28
T143|x|`verify-hunters-hunted.ps1` — checks NOVOS §V50 (itens do `cboFaith` ∉ §V32 & chave fixa no renderer) & §V51 (bolinhas de willpower ⊥ `field` em `WoD20.3` & `_c*` = espelho); §I3 espelhos += `willpower_c*`; §V49 altura 306|V49,V50,V51,I3,V20
T144|x|`rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T145|x|(1+3) `WoD20.3.lfm` — `COMBAT` (0,0,650,330)→(0,0,660,320); template `Attack` c/ grade nova (weapon 170, damage 90, resto igual); linhas de ataque 620→630 largas; 8 labels do cabeçalho c/ `horzTextAlign="center"` & `width` da coluna|V11,V16,V25,V26,V37,V40
T146|x|(2) `WoD20.3.lfm` — `VirtueMirror` só-leitura: bolinhas `roVirtue_$(field)_N` SEM `field` + `autoChange="false"`, pintadas em `renderCombatTraits` (dot1 segue `<image>` fixa); `dataLink` += `conscience_2`…`courage_5`|I3,V1,V36,V51
T147|x|(4) `WoD20.9.lfm` — `EXPERIENCE` 90→75 & `EXPERIENCE LOG` (0,100,1040,540)→(0,85,1040,555) ∴ fim 640 mantido|V27,V37,V40
T148|x|`verify-hunters-hunted.ps1` — §V51 estendido às virtudes (⊥ `field` em `VirtueMirror`, `autoChange="false"`, Lua pinta); `$mirrors` −`conscience`/`selfControl`/`courage`|V1,V36,V51,V20
T149|x|`rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T150|x|(1) `WoD20.3.lfm` — `ARMOR` `VIRTUES` `dynHealth3_box` top 350→330 & `WILLPOWER` 470→450 ∴ vão p/ `COMBAT` cai de 30 p/ 10; alturas intactas|V37,V40
T151|x|(2+3) `WoD20.9.lfm` — `EXPERIENCE` 75→95: título top 10, rótulos top 38 centrados, `edit` top 60 centrado SOB o rótulo, 3 colunas @ 15\|352\|689 (336 cada, edit +123); `EXPERIENCE LOG` → (0,105,1040,535)|V16,V25,V26,V27,V37,V40
T152|x|`rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T154|x|`WoD20.6.lfm` — `cboSheetTheme` `items`=`values`=`{'Modern','Victorian Era (Light)','Victorian Era (Dark)'}` & `dataLink defaultValue="Modern"`; corrigir o comentário XML que ainda diz "Not named cbo*" (mentira desde a 11ª rodada). MESMO commit que T155 (§V12)|I3,V12,V52
T155|x|`WoD20.6.lfm` — `THEMES` (§I5) + `applyTheme` (§I6): snapshot 1× por handle antes da 1ª pintura, `rootOf`+`collect` da raiz, `Modern` = restaurar, valor ∉ lista → `Modern`; `<dataLink field="sheetTheme" onChange>` chama ∴ roda no load & na troca|I5,I6,V52,V54,V55,V57,R21,R22,T153
T156|x|`images/` — 4 bolinhas novas 100×100 ≡ geometria de `prime_on/off.png`: `prime_on_ink.png` `prime_off_ink.png` (Claro) & `prime_on_gold.png` `prime_off_gold.png` (Escuro); paletas de §I5 apontam p/ elas|V3,V58
T157|x|paletas ganham `fontFamily` serifada & `xradius`/`yradius`=0 & `strokeSize`=2 ∴ serifa + canto reto + filete grosso nas 2 vitorianas. Tamanho de caixa ⊥ muda|V57,R24,T153
T158|x|textura — `paper_light.png` `paper_dark.png` + `<image name="themePaper" style="stretch" hitTest="false" visible="false">` como 1º filho do `scrollBox` das 9 abas; paleta liga & troca `src`, `Modern` desliga. T153(a)(b) negativo → PULAR & tema segue sem textura (§V56), ⊥ trava a rodada|V56,V58,R23,T153
T160|x|`localization.lang` [pt]+[en] & mapa `PT` de `WoD20.6` — `Victorian Era (Light)`=`Era Vitoriana (Claro)` · `Victorian Era (Dark)`=`Era Vitoriana (Escuro)`; chave `Victorian Era` velha SAI (⊥ mais ∈ `values`)|V10,V17,V22,V24,V28
T161|x|`verify-hunters-hunted.ps1` — checks NOVOS §V52 (3 valores & fallback) §V53 (∀ literal de cor dos 9 `.lfm` ∈ mapa das 2 paletas) §V54 (`Modern` ⊥ tem paleta & restaura snapshot) §V55 (⊥ `findClass` no tema) §V56 (ornamento ausente ⊥ aborta a pintura) §V57 (tema ⊥ escreve geometria) §V58 (PNG de paleta ∈ `images/`)|V52,V53,V54,V55,V56,V57,V58,V20
T162|x|`module.xml` version `1.0` → `1.1`|-
T163|x|`rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T164|x|`verify-hunters-hunted.ps1` — check §V59: ∀ `name=` de controle único entre os 10 `.lfm` (⊥ `<template>`, ⊥ `<event>`, ⊥ nome c/ `$(`). Mesmo tipo de morte muda que o check de template já cobre|V59,V20
T165|x|`WoD20.6.lfm` — `dotOn`/`dotOff`/`paper` das 2 paletas p/ `/WoD20th/images/…` (absoluto)|I5,V60,R26
T166|x|`WoD20.6.lfm` — LEDGER: `paint(c,prop,valor,autorado)` grava só c/ destino mapeado & registra o autorado; `authored(c,prop)` lê do ledger antes do controle; `Modern` = `restore(c)` do registrado. `themeSnap`/`put` SAEM|I6,V61,V62,R27
T167|x|`WoD20.6.lfm` — `fontFamily` só onde o autorado é string ⊥ vazia; backdrop fora do ledger (re-esconde só se visível) ∴ Modern segue gravando 0|V61,R27
T168|x|`verify-hunters-hunted.ps1` — checks NOVOS §V60 (forma do caminho) §V61 (⊥ gravação direta fora de `paint`, exceto o backdrop; `paint` recusa nil) §V62 (`restore` lê o ledger & `Modern` chama). §V58 resolve o prefixo antes de procurar em disco|V58,V60,V61,V62,V20
T169|x|`rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T170|x|`WoD20.6.lfm` — `cboSheetTheme` `items`=`values` 3→5: `Modern` `Present Day` `Victorian Era` `Dark Ages` `Classical Era`. MESMO commit que T171 (§V12)|I3,V12,V52
T171|x|`WoD20.6.lfm` `THEMES` — apagar paleta `Victorian Era (Light)`; `(Dark)` → `Victorian Era` (paleta intacta, só a chave); +3 paletas `Present Day` `Dark Ages` `Classical Era` c/ os valores de §C. Caminhos absolutos (§V60) & 10 chaves cada (§V63)|I5,V53,V60,V63
T172|x|`images/` — +7 PNG (`prime_on_crimson` `prime_off_slate` `prime_on_blood` `prime_off_iron` `paper_present` `paper_dark_ages` `paper_classical`), −3 (`prime_on_ink` `prime_off_ink` `paper_light`), renomear `paper_dark.png`→`paper_victorian.png`. Bolinha 100×100 ≡ geometria das ∃|I2,V3,V58,V64
T173|x|`localization.lang` [pt]+[en] & mapa `PT` de `WoD20.6` — +`Present Day`=Atual · `Dark Ages`=Idade das Trevas · `Classical Era`=Era Clássica · `Victorian Era`=Era Vitoriana (volta); −`Victorian Era (Light)` −`Victorian Era (Dark)`|V10,V17,V22,V24,V28
T174|x|`verify-hunters-hunted.ps1` — §V52 3→5 valores; §V53 conta 1 mapa POR paleta declarada (⊥ número fixo) ∴ 5ª época futura ⊥ escapa; checks NOVOS §V63 (10 chaves por paleta) §V64 (⊥ PNG órfão em `images/`)|V52,V53,V63,V64,V20
T175|x|`module.xml` version `1.1` → `1.2`|-
T176|x|`rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T177|x|`WoD20.6.lfm` — `normPath` (leaf, minúsculas) + ramo `image` do `applyTheme`: leaf `prime_on.png`→`t.dotOn` & `prime_off.png`→`t.dotOff`, via `paint` ∴ entra no ledger & `Modern` restaura|I6,V61,V62,V65,R26
T178|x|`verify-hunters-hunted.ps1` — check NOVO §V65: ∀ `//image[@src]` (⊥ `themePaper*`) tem leaf remapeável & `applyTheme` contém o remapeamento|V65,V20
T179|x|`rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T180|x|`WoD20.6.lfm` — `cboSheetTheme` `items`=`values` 5→4 (`Modern` `Victorian Era` `Dark Ages` `Classical Era`) & paleta `Present Day` renomeada p/ `Modern`. MESMO commit (§V12)|I3,V12,V52
T181|x|`WoD20.6.lfm` — `restore()` & o ramo `t == nil` SAEM (código morto); `local t = THEMES[v] or THEMES["Modern"]` ∴ nunca nil. Ledger FICA (§V62)|I6,V52,V54,V61,V62
T182|x|`WoD20.6.lfm` — `corner` sai das 4 paletas & `applyTheme` ⊥ escreve `xradius`/`yradius` ∴ detalhe de canto do XML vale em ∀ época|I5,V63,V66
T183|x|`WoD20.6.lfm` — `strokeSize` sai das paletas → constante `THEME_STROKE = 3` (o mais largo dos 4)|I5,V63,V67
T184|x|`localization.lang` [pt]+[en] & mapa `PT` — `Present Day`/`Atual` SAI. `Modern`=`Moderno` já ∃ ∴ ⊥ chave nova|V10,V17,V22,V24,V28
T185|x|`verify-hunters-hunted.ps1` — §V52 5→4 & fallback = paleta default; §V63 10→8 chaves; §V54/§V61/§V62 conforme texto novo (⊥ mais `restore`); checks NOVOS §V66 (⊥ escreve raio) §V67 (`THEME_STROKE` 1×, ⊥ `strokeSize` em paleta)|V52,V54,V61,V62,V63,V66,V67,V20
T186|x|`module.xml` version `1.2` → `1.3`|-
T187|x|`rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T188|x|9 `.lfm` — `cornerType="innerLine"`→`innerRound` (54×) & `xradius`/`yradius` `10`→`14` (53 caixas de seção). Avatar mantém raio 5; marca de vitalidade INTACTA|V66,V68
T189|x|`verify-hunters-hunted.ps1` — check NOVO §V68: caixas de seção compartilham `cornerType` & raio; as 2 exceções ficam de fora|V48,V68,V20
T190|x|`rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T191|x|`WoD20.1.lfm` caixa `VIRTUES` — título `top` 0→8; linhas 25/50/75 → 33/62/91 (pitch 29); caixa 110→126 alto ∴ 510..636|V27,V40,V68
T192|x|`WoD20.1.lfm` caixa `HUMANITY & WILLPOWER` — top 630→646 & height 180→164 ∴ fecha em 810 ≡ `BACKGROUNDS`. Internos INTACTOS|V40,V69
T193|x|`verify-hunters-hunted.ps1` — check NOVO §V69: fim da caixa `HUMANITY`/`WILLPOWER` = fim de `BACKGROUNDS`; HEALTH fora (exceção n=10). Mutação: mexer 1px no height ! ficar vermelho|V69,V20
T194|x|`WoD20.6.lfm` paleta `Dark Ages` — `stroke` 3 entradas `#8A7A5C`→`#C0C6CC` & `fill["black"]` `#1A1512`→`#0E0B09`. `DimGray` `#00000000` `font` `dot*` `paper` ⊥ mudam|I5,V53,V63
T195|x|`module.xml` version `1.3` → `1.4`|-
T196|x|`rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T197|x|`images/` — `prime_on_silver.png` (cheia) + `prime_off_silver.png` (anel) em `#C0C6CC`, 100×100 ≡ par base; `prime_on_blood.png` & `prime_off_iron.png` SAEM|I2,V3,V58,V64
T198|x|`WoD20.6.lfm` paleta `Dark Ages` — `dotOn`/`dotOff` p/ os PNG prata, caminho absoluto do plugin|I5,V58,V60,V63
T199|x|`WoD20.6.lfm` — `cboSheetTheme` items=values `Modern`→`Modern Nights` & `Classical Era`→`Classical Age`; chaves de `THEMES` & `dataLink defaultValue` & fallback no MESMO commit|I3,V12,V52
T200|x|`localization.lang` [pt]+[en] & mapa `PT` — +`Modern Nights`=`Noites Modernas` · `Classical Age`=`Idade Clássica`; −`Modern` −`Classical Era`|V10,V17,V22,V24,V28
T201|x|`localization.lang` [pt]+[en] & mapa `PT` — 9 rótulos NOVOS: `Legerdemain`=`Prestidigitação` · `Archery`=`Arquearia` · `Commerce`=`Comércio` · `Hearth Wisdom`=`Sabedoria Popular` · `Seneschal`=`Senescal` · `Theology`=`Teologia` · `Philosophy`=`Filosofia` · `Ritual`=`Ritual` · `Religion`=`Religião`. `Computer`=`Computador` & `Drive`=`Condução` já ∃|V9,V10,V17,V28,V70
T202|x|`WoD20th.lfm` (raiz) — `THEME_LABELS` (§I7) c/ as 3 listas de §C & `Victorian Era` vazio|I7,V70,V73
T203|x|`WoD20.1.lfm` — label do template `Ability` ganha `name="dynAbil$(field)"`|I7,V59,V72
T204|x|`WoD20.6.lfm` — `renderAbilityLabels(from)`: `rootOf`+`collect` da raiz, snapshot 1× do EN autorado por label, escreve `tryTranslate(THEME_LABELS[t][campo] \| autorado)`; chamado no load & pelos 2 `dataLink` (`sheetTheme` & `language`)|I6,I7,V21,V22,V31,V70,V72
T205|x|`WoD20.3.lfm` — 2 linhas de perícia viram slots (`ReadOnlyTrait field="ranged"` & `field="close"` ∴ `ro_ranged_*` `ro_close_*`) & label do `ReadOnlyTrait` ganha `name="dynRo$(field)"` ∴ as 11 linhas da aba entram no renderer|I3,V51,V59,V71
T206|x|`WoD20th.lfm` + `WoD20.3.lfm` — `THEME_COMBAT` (§I7) & `renderCombatTraits` pinta os 2 slots do campo DA ÉPOCA; rótulo dos slots sai do renderer de T204|I7,V51,V71,V72
T207|x|`verify-hunters-hunted.ps1` — checks NOVOS §V70 (tradução & largura das strings de tema) §V71 (par rótulo↔campo nas 2 abas) §V72 (renderer nos 3 gatilhos) §V73 (rótulo único por coluna por época); §V52 c/ `Modern Nights` & `Classical Age`. Mutação antes de aceitar|V52,V70,V71,V72,V73,V20
T208|x|`module.xml` version `1.4` → `1.5`|-
T209|x|`rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T210|x|`WoD20th.lfm` — `THEME_LABELS` reescrito p/ atribuição canônica (mapas de §C, 20ª rodada): `Modern Nights` 7→2 entradas · `Dark Ages` 8→6 · `Classical Age` 14→6 · `Victorian Era` segue vazio. Conjunto de nomes por época INTACTO|I7,V70,V73,V74
T211|x|`WoD20th.lfm` + `WoD20.3.lfm` — `THEME_COMBAT` SAI (flexibilidade morta); `ReadOnlyTrait field="ranged"`/`"close"` voltam a `field="firearms"`/`"melee"`, `paintAs` volta a `paint` & o `dataLink` de `WoD20.3` perde `crafts_*` `etiquette_*` `performance_*` `sheetTheme`. Rótulo segue vindo do renderer (`dynRo*`)|I3,I7,V51,V71,V74
T212|x|`verify-hunters-hunted.ps1` — check NOVO §V74 (nome→1 campo, rótulo autorado incluído); checks de slot de §V71 SAEM (invariante retirado). Mutação: pôr 1 nome em 2 campos ! ficar vermelho|V74,V71,V20
T213|x|`module.xml` version `1.5` → `1.6`|-
T214|x|`rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T215|x|`WoD20th.lfm` — `ABILITY_FIELD` (41 nomes, 11 campos novos) & `ERA_ABILITIES` (4 épocas × 3 colunas × 10 nomes, §C); `THEME_LABELS` SAI|I3,I7,V74,V75
T216|x|`WoD20.1.lfm` — template `Ability` vira SLOT: label `name="dynAbil$(col)$(num)"` & bolinhas `name="abil$(col)$(num)_<n>"`; `field=` & `nome=` autorados seguem = lista `Victorian Era`|I7,V59,V76
T217|x|`WoD20.6.lfm` — renderer por slot: nome = `ERA_ABILITIES[era][col][num]`, label = `tryTranslate(nome)`, bolinha `field = ABILITY_FIELD[nome].."_"..n` & `checked = sheet[...] == true`|I6,I7,V72,V74,V76,R28
T218|x|`WoD20.3.lfm` + renderer — linha de ataque à distância volta a SLOT (`ro_ranged_*`), campo = o nome ∈ {`Firearms`,`Archery`} da lista da época; linha `Melee` segue estática. Rótulo pelo mesmo renderer|I3,V51,V74,V76
T219|x|`verify-hunters-hunted.ps1` — §V74 vira bijeção; checks NOVOS §V75 (10 nomes distintos c/ campo por coluna/época) §V76 (re-ligação + reload de `checked`); §V70 & §V73 passam a ler `ERA_ABILITIES`. Mutação antes de aceitar|V70,V73,V74,V75,V76,V20
T220|x|`module.xml` version `1.6` → `1.7`|-
T221|x|`rdk -l` + `rdk -i` + teste no Firecast: 2 pontos em `Enigmas`, trocar p/ `Modern Nights` → `Computer` ! ficar 0 & voltar ! mostrar `Enigmas 2`|V6,V7,V76
T222|x|`WoD20th.lfm` aba `Powers`→`Numina`; `WoD20.7.lfm` títulos `NUMINA: HEDGE MAGIC PATHS`→`HEDGE MAGIC PATHS` & `NUMINA: PSYCHIC PHENOMENA`→`PSYCHIC PHENOMENA`; `localization.lang` [pt]+[en] & mapa `PT` acompanham (3 chaves novas, 3 velhas fora)|I1b,V9,V10,V17,V22,V28,V34
T223|x|`module.xml` version `1.7` → `1.8`|-
T224|x|`rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T225|x|`WoD20.1.lfm` — templates `Header` & `HeaderPicker`: `edit`/`comboBox` 235→215 (`left` 112 intacto); caixa do header 1100→1040 & as 9 linhas passam a `left` 5 \| 350 \| 695, `width` 335|V16,V26,V37
T226|x|`WoD20.1.lfm` — caixa `EXPERIENCE` (1110,0,100,100)→(1050,0,160,100), título `width` 100→160 & `edit` `left` 20→50 ∴ fecha em 1210 c/ gap 10|V27,V40
T227|x|`module.xml` version `1.8` → `1.9` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T228|x|`WoD20.1.lfm` — template NOVO `HeaderNarrow` (label 75 `trailing` + `edit` @ `left` 82, 215 largo); coluna 1 (`name` `player` `chronicle`) passa a usá-lo|V16,V25,V26
T229|x|`WoD20.1.lfm` — caixa do header 1040→1010 & colunas @ `left` 5 (w 297) \| 320 (w 327) \| 665 (w 327) ∴ conteúdo anda 30 p/ a esquerda & gap p/ `EXPERIENCE` vira 40|V37,V40
T230|x|`module.xml` version `1.9` → `2.0` (⊥ `1.10`: ordenação de string) + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size)|V6,V7
T231|x|`WoD20.1.lfm` — caixa do header 1010→1030 (cresce só pela direita, `left` 0 é a borda da aba) ∴ gap p/ `EXPERIENCE` 40→20|V37,V40
T232|x|`module.xml` version `2.0` → `2.1` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size)|V6,V7
T233|x|8 `.lfm` (⊥ `WoD20.9`) — 45 títulos de seção `top` 0\|1 → 4 & os vizinhos de faixa (`cmbHealthLevels` ×2, `Page`/`Cost` de `Merit`/`Flaw`) acompanham|V27,V37,V77
T234|x|`verify-hunters-hunted.ps1` — check NOVO §V77 (título ≥ 4, `WoD20.9` fora). Mutação: 1 título de volta p/ 0 ! ficar vermelho|V77,V20
T235|x|`module.xml` version `2.1` → `2.2` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size)|V6,V7
T236|x|`WoD20.7.lfm` — `NuminaPicker` → `HedgePicker` c/ `items`/`values` = 21 Paths de §R.14 cap.1 + template NOVO `PsychicPicker` = 22 Phenomena do cap.2; chamadas `numina_1`…`_8` → `HedgePicker` & `psychic_1`…`_8` → `PsychicPicker`. `NuminaFree`, x dos widgets, `DESC`, `.lang` & mapa `PT` ⊥ tocados|V12,V14,V15,V18,V24,V26,V78,R14
T237|x|`verify-hunters-hunted.ps1` — check NOVO §V78 (2 templates lidos por NOME, listas literais 21 + 22, ∩ = ∅, ∪ = 43). Mutação: `Telepathy` movida p/ `HedgePicker` ! ficar vermelho|V78,V20
T238|x|`module.xml` version `2.2` → `2.3` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size)|V6,V7
T240|x|`WoD20.10.lfm` NOVO — aba Storyteller: caixa `STORYTELLER SETTINGS` c/ linha `Save Initial Character` + `<button>` `Save` & os 4 `checkBox` de §I8|I2,I3,I8,V1,V38,V48,V68,V77
T241|x|`WoD20.11.lfm` & `WoD20.12.lfm` NOVOS — abas `Disciplines` & `Magika` VAZIAS (scrollBox + backdrop de tema + caixa de título só, ⊥ `field`)|I2,V48,V57,V68,V90
T242|x|`WoD20th.lfm` — 3 `<tab>` novas na ordem de §I.1b + `name=` único em ∀ tab|I1b,I2,V59
T243|x|`WoD20th.lfm` — `isStoryteller()` & `applyTabVisibility(from)` no form RAIZ; chamados no `onNodeReady` & pelo `dataLink` dos 3 flags|I8,V19,V33,V79,V80,V89,R29
T244|x|`WoD20.10.lfm` — `Save` → `Dialogs.confirmOkCancel` → `sheet.baseline = ndb.exportXML(sheet)`; 1 shot, botão desabilita & mostra estado|I8,V33,V81,V82,R30,R32
T245|x|`WoD20th.lfm` — `traitLevel(node, campo)` (§V85) & `XP_COST` (§I9) no form RAIZ ∴ ledger & futuros leitores usam a MESMA leitura|I9,V85,V86
T246|x|`WoD20.9.lfm` — bloco `EXPERIENCE LOG` vira 4 colunas só-leitura sem `field`; `xpLog` SAI do XML & entra em ÓRFÃOS de §I.3|I3,I10,V29,V84,V91
T247|x|`WoD20.9.lfm` — `renderXPLedger`: diff `baseline`×ficha por nó de memória, 1 linha por ponto, custo por §I9, 21 por tipo novo de númina, afinidade ×6; baseline vazio → texto de estado|I10,V33,V70,V83,V85,V86,V87
T248|x|`WoD20.7.lfm` — linha `numina_1` c/ `*` + negrito & nota de Affinity Path depois de `TRUE FAITH`|V9,V10,V16,V88
T249|x|`localization.lang` [pt]+[en] & mapa `PT` de `WoD20.6` — strings novas das 3 abas + 4 títulos de coluna + 7 nomes de `Type` + nota de afinidade|V9,V10,V17,V22,V28,V34,V70
T250|x|`verify-hunters-hunted.ps1` — checks NOVOS §V79…§V91. Mutação antes de aceitar ∀ check|V20,V79,V80,V81,V83,V84,V85,V86,V87,V88,V89,V90,V91
T251|x|`module.xml` version `2.3` → `2.4` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size) — CLAUDE.md|V6,V7
T253|x|`WoD20.7.lfm` — label `*` da linha `numina_1` SAI (negrito fica); `["*"]` do mapa `PT` de `WoD20.6` & `wod.*` [pt]+[en] saem junto|V10,V22,V28,V88
T254|x|`WoD20th.lfm` — `<tab>` `Disciplines` `Magika` `Storyteller` autoradas `visible="false"`; `Numina` segue visível|V89,V94
T255|x|`WoD20th.lfm` — `onNodeReady` + `dataLink` dos 3 flags MIGRAM de `WoD20.10` p/ o form RAIZ; `WoD20.10` fica só c/ `renderBaselineState`|I8,V95
T256|x|`WoD20th.lfm` — `applyTabVisibility` esconde ABA & CONTEÚDO (filhos da `<tab>`)|I8,V92
T257|x|`WoD20th.lfm` — pós-recálculo, aba ativa proibida → `Main` (`tabIndex = 0` \| `tabMain:activate()`)|I8,V93,R33
T258|x|`verify-hunters-hunted.ps1` — checks NOVOS §V92 §V93 §V94 §V95; §V88 passa a exigir negrito SEM `*`. Mutação antes de aceitar|V20,V88,V92,V93,V94,V95
T259|x|`module.xml` version `2.4` → `2.5` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size)|V6,V7
T261|x|`WoD20th.lfm` — `<event name="onShow">` no form RAIZ → `applyTabVisibility(self)`|V96,R34
T262|x|`verify-hunters-hunted.ps1` — check NOVO §V96 (form raiz tem `onShow` chamando `applyTabVisibility`). Mutação antes de aceitar|V20,V96
T263|x|`module.xml` version `2.5` → `2.6` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size)|V6,V7
T265|x|`WoD20.9.lfm` — `<event name="onShow">` → `renderTotalXP(self)` + `renderXPLedger(self)` ∴ voltar p/ a aba redesenha|V97,R34,R35
T266|x|`WoD20.9.lfm` — `#rows == 0` escreve estado "Nothing bought yet" na 1ª coluna (≡ §V33) + chave [pt] & [en]|V98,V33,V10
T267|x|`verify-hunters-hunted.ps1` — checks NOVOS §V97 §V98. Mutação antes de aceitar|V20,V97,V98
T268|x|`module.xml` version `2.6` → `2.7` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size)|V6,V7
T270|x|`WoD20th.lfm` — `XP_TRAIT[base] = {kind, first, fixed}` p/ ∀ traço de §I9, montado das MESMAS listas que `xpLedgerRows` usa (⊥ 2ª lista, §V86) + `xpSpent()` = Σ `cost` do log|I12,V86
T271|x|`WoD20th.lfm` — `xpGuard(field)` conforme §I12 (dorme sem baseline · barra compra sem saldo · barra venda de ponto do baseline · desfaz escrevendo o campo de volta)|I12,V99,V101,V103
T272|x|`WoD20.1.lfm` `WoD20.7.lfm` — `onChange="xpGuard('$(field)_N'); renderXPBoxes(self);"` em ∀ dot de traço que custa XP (templates)|V99
T273|x|`WoD20th.lfm` — `renderXPBoxes(form)` (≡ `renderHealthTrack`, §R9): escreve `Total` `Spent` `Current` + migração de `xpTotal` (§I11). `WoD20.9` & `WoD20.1` chamam|I11,V100,V102
T274|x|`WoD20.9.lfm` & `WoD20.1.lfm` — 3 caixas de XP perdem `field`; `Current` ganha `onUserChange` → `xpTotal = digitado + Spent`. `spentXP` & `experience` saem de §I3 ESPELHOS p/ ÓRFÃOS|I11,V1,V36,V102
T275|x|`verify-hunters-hunted.ps1` — checks NOVOS §V99 §V100 §V101 §V102 §V103. Mutação antes de aceitar|V20,V99,V100,V101,V102,V103
T276|x|`module.xml` version `2.7` → `2.8` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size)|V6,V7
T278|x|`WoD20th.lfm` — `onNodeReady` semeia `appearance_1 = true` quando nil (≡ semente de `stShowNumina`, 28ª rodada)|V104,R36
T279|x|`WoD20th.lfm` `xpCost` — `Willpower` devolve `from` (⊥ `from * 2`) & `verify-hunters-hunted.ps1` `$costWant` muda no MESMO commit (o gate trava a fórmula, §V86)|I9,V86,B29
T280|x|`verify-hunters-hunted.ps1` — check NOVO §V104 (semente ∃ & só dispara em nil). Mutação antes de aceitar|V20,V104
T281|x|MORTA na 95ª, MEDIDO: `module.xml` diz `<version>1.0</version>` ∴ a escada `2.8`→`2.9` ⊥ ∃ & ⊥ ∃ como buildar isto. CAUSA: `rdk -p` REESCREVE o `module.xml` (CLAUDE.md) ∴ o campo `version` ⊥ é artefato durável & bump de version ⊥ é tarefa. O build de §T278…§T280 SAIU (é o `.rpk` de então); só o número morreu. Texto velho: version `2.8` → `2.9` + `rdk -l` + `rdk -i`|V6,V7
T283|x|`WoD20.10.lfm` — 5º `checkBox` `Allow Buy Dots For Free` (`stFreeDots`) @ `top=210` & caixa `520×240` → `520×270`|I8,V38,V40,V106
T284|x|`WoD20th.lfm` — `ctx.free = sheet.stFreeDots == true` em `xpLedgerRows`; `xpCost` devolve 0 na 1ª linha quando `ctx.free` ∴ 1 lugar só (§V86 intacto)|I9,V105
T285|x|`WoD20.9.lfm` — coluna `Cost` escreve `FREE` traduzido quando `stFreeDots` ligada + `dataLink` do ledger passa a observar `stFreeDots`|V105,V70,V97
T286|x|`localization.lang` + mapa `PT` de `WoD20.6` — `Allow Buy Dots For Free` & `FREE` (pt `Grátis`)|V10,V22,V28
T287|x|`verify-hunters-hunted.ps1` — `$wantFlags` += `stFreeDots` & checks NOVOS §V105 §V106. Mutação antes de aceitar|V20,V89,V105,V106
T288|x|`module.xml` version `2.9` → `3.0` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size). Se T278…T287 forem buildados de uma vez, ESTE bump é o único ∴ T281 vira no-op declarado|V6,V7
T289|x|SUPERSEDED por T315 — a 2ª metade ("desligar → as 3 linhas voltam a mostrar preço & `Current` cai") vira o COMPORTAMENTO ERRADO na 34ª rodada (§V114). A 1ª metade (flag ON → 3 linhas `FREE` & `Current` parado) segue viva dentro de T315|I8,V105,V114
T290|x|`WoD20th.lfm` — flag global `xpQuiet`: `xpGuard` & `renderXPBoxes` saem cedo enquanto ligada|V107
T291|x|`WoD20.6.lfm` — `renderAbilityLabels` liga `xpQuiet` antes do laço de re-ligação & desliga no fim; 1 `renderXPBoxes` depois do lote|V107,V76
T292|x|`WoD20th.lfm` — semente de `appearance_1` (§V104) & migração de `xpTotal` (§I11) rodam sob `xpQuiet`|V107,V104,I11
T293|x|`WoD20th.lfm` `WoD20.1.lfm` `WoD20.7.lfm` — `xpGuard(field, form)` chama `renderXPBoxes` 1× no fim & devolve o `xpSpent()` que já calculou; `onChange` do dot vira só `xpGuard('…', self)`|V108,V99
T294|x|`verify-hunters-hunted.ps1` — checks NOVOS §V107 §V108 (& §V99 aceita a assinatura nova). Mutação antes de aceitar|V20,V99,V107,V108
T295|x|`module.xml` version `3.0` → `3.1` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size). Rodar DEPOIS de §T297…§T300 — 1 build fecha as duas rodadas|V6,V7
T297|x|`WoD20.6.lfm` — `cboGame`: `items`/`values` = `Vampire` `Hunters Hunted` `Mage` (⊥ vazio), `enabled="false"` + `<dataLink field="game" defaultValue="Hunters Hunted"/>`|V109,V15,V24
T298|x|`verify-hunters-hunted.ps1` — `V14 cboGame offers 5 games` → 3; `cboGame` entra na exceção de §V15; checks NOVOS §V109 (`enabled="false"` & default) & §V110. Mutação antes de aceitar|V20,V109,V110,V15
T299|x|`localization.lang` + mapa `PT` de `WoD20.6` — + `Vampire`=`Vampiro` & `Mage`=`Mago`; − chaves [pt]+[en] dos 4 nomes longos. `Hunters Hunted`=`Caçadores Caçados` JÁ ∃|V10,V22,V28
T300|x|`WoD20th.lfm` — `onNodeReady` normaliza `game` fora da lista p/ `Hunters Hunted` (form RAIZ, ⊥ `WoD20.6`)|V110,V95
T301|x|`WoD20.3.lfm` — `opacity="0.55"` nas bolinhas só-leitura dos 3 templates: `VirtueMirror` 4 `imageCheckBox` + a `<image>` de dot1 · `ReadOnlyTrait` 5 · `WillpowerMirror` 10. `willpower_c1`…`_c10` (espelho EDITÁVEL do mesmo bloco, §V36) ⊥ recebe|V111,V51
T302|x|`WoD20.9.lfm` — `opacity="0.55"` em `edtTotalXP` `edtSpentXP` & nos 4 `textEditor` do ledger (`dynXpType` `dynXpTrait` `dynXpLevel` `dynXpCost`). `edtCurrentXP` ⊥ recebe — é o único EDITÁVEL da caixa (§I11)|V111,I10,I11
T303|x|`WoD20.7.lfm` `edtNuminaDesc` (§V29) & `WoD20.6.lfm` `cboGame` (§V109) — `opacity="0.55"`|V111,V29,V109
T304|x|`WoD20.10.lfm` — `form.btnSaveBaseline.opacity` escrito na MESMA linha lógica do `enabled` (`saved` → `0.55`, senão `1`)|V112,V82
T305|x|`verify-hunters-hunted.ps1` — check NOVO §V111 nos 2 sentidos (só-leitura ! `0.55` · entrada editável ⊥ tem `opacity`) + §V112 (`enabled` & `opacity` do botão no mesmo lugar). Mutação antes de aceitar|V20,V111,V112
T306|x|MORTA na 95ª ≡ §T281 — `module.xml` diz `1.0` & `rdk -p` reescreve o arquivo ∴ a escada `3.1`→`3.2` ⊥ ∃. O build da 32ª saiu; o número ⊥ sobreviveu ao tooling|V6,V7,T281
T308|x|`WoD20th.lfm` — `freeDots` conforme §I13: `freeHas(dot)` `freeAdd(dot)` `freeDel(dot)` sobre a string delimitada; vazio = `" "`|I13,V113
T309|x|`WoD20th.lfm` — `pushRise` recebe o CAMPO do traço & marca `row.free = freeHas(field .. "_" .. lvl)`; `xpCost` devolve 0 p/ linha carimbada & `ctx.free` (a flag) SAI de `xpCost`|V114,I9,I10
T310|x|`WoD20th.lfm` `xpGuard` — carimba/descarimba conforme §I12, o carimbo ANTES do `xpSpent()` do mesmo clique; o DESFAZ descarimba junto|V115,V116,I12
T311|x|`WoD20th.lfm` — migração de §V117 no `onNodeReady`, sob `xpQuiet`, gravando nos 2 caminhos (carimbo do log \| `" "`)|V117,V107,I11
T312|x|`WoD20.9.lfm` — coluna `Cost` lê `r.free` POR LINHA (⊥ `sheet.stFreeDots`); `dataLink` segue observando `stFreeDots` (a flag muda o preço da PRÓXIMA compra, ⊥ das linhas de cima)|V114,I10
T313|x|`verify-hunters-hunted.ps1` — checks NOVOS §V113…§V117 + `xpCost` ⊥ pode mais ler `stFreeDots`. Mutação antes de aceitar|V20,V113,V114,V115,V116,V117
T314|x|MORTA na 95ª ≡ §T281 §T306 — `module.xml` diz `1.0` & `rdk -p` reescreve o arquivo ∴ a escada `3.2`→`3.3` ⊥ ∃. O que segue valendo da 33ª é §V6/§V7: build = `rdk -l` exit 0 **&** `.rpk` c/ mtime\|size novos — a PROVA é o `.rpk`, ⊥ o número no `module.xml`|V6,V7,T281,V7
T316|x|`WoD20th.lfm` — REMOVER o bloco de migração de `freeDots` do `onNodeReady` (§T311 desfeito): nil já é conjunto vazio ∴ herdar nada = ⊥ escrever nada|V117,V113
T317|x|`verify-hunters-hunted.ps1` — §V117 vira a PROIBIÇÃO (⊥ ∃ carimbo em lote: nenhum laço escrevendo `freeDots`, nenhuma leitura de `rows[i].dot` fora do renderer) & §V113 passa a exigir `sheet.freeDots` escrito em 2 lugares, ⊥ 3. Mutação antes de aceitar|V20,V117,V113
T318|x|`localization.lang` [pt] `wod.FREE`=`Grátis`→`GRÁTIS` + mapa `PT` de `WoD20.6` no mesmo commit; conferir se `GRÁTIS` cabe nos 85px da coluna `Cost` na mais larga das 4 fontes de época — ⊥ coube → alargar coluna & caixa juntas (§V37, §V40)|V28,V10,V22,V105
T319|x|`module.xml` version `3.3` → `3.4` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size). Firecast FECHADO: c/ o app aberto o `rdk -i` sai 0 SEM escrever no `%APPDATA%` — visto na 34ª rodada|V6,V7
T320|x|`WoD20th.lfm` — `xpGuard`: `now` calculado ANTES do carimbo; carimba `trait .. "_" .. now` (⊥ `field`); `freeTrim(trait, now)` poda acima do nível a ∀ mutação, DESFAZ incluso; `wasFree` & o restauro do desfazer SAEM|V118,V116,I12
T321|x|`verify-hunters-hunted.ps1` — check NOVO §V118 (carimbo lê nível, ⊥ campo clicado) + §V116 novo (poda a ∀ mutação) + §V117 estreitado (proíbe ADIÇÃO em lote, ⊥ poda). Mutação antes de aceitar|V20,V118,V116,V117
T322|x|`module.xml` version `3.4` → `3.5` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size, Firecast FECHADO)|V6,V7
T324|x|DIAGNÓSTICO temporário (`WoD20.9.lfm` + form raiz) — 1 label na aba Progress escreve `flag=<stFreeDots> stamps=<freeDots> guard=<nº de vezes que xpGuard passou do gate> on=<último `on` lido> now=<último nível>`; guarda alimenta os contadores. Responde em 1 clique QUAL elo quebra: guarda ⊥ roda \| `on` chega false na compra (§R39). ⊥ é feature — SAI em §T326|R39,B32
T325|x|`WoD20.9.lfm` `localization.lang` mapa `PT` de `WoD20.6` + gate — coluna `Cost` escreve `tostring(r.cost)` SEMPRE; chaves `FREE`/`GRÁTIS` REMOVIDAS dos 3 lugares; check de §V105 perde a metade do `FREE` & entra §V120. Mutação antes de aceitar|V120,V105,V28,V10,V20
T326|x|CANCELADO na 38ª rodada — era o conserto que dependeria do veredito de §T324; o user removeu a funcionalidade em vez de consertá-la (§T327…§T332)|B32
T327|x|`WoD20th.lfm` — REMOVER `freeDots` & os 5 helpers & o carimbo/poda do `xpGuard` & o `free` da linha & o param `field` de `pushRise` & os contadores de §T324|V120
T328|x|`WoD20.9.lfm` — REMOVER o label de diagnóstico & o bloco que o escreve & `stFreeDots` do `dataLink`. `Cost` segue número (§V120)|V120
T329|x|`WoD20.10.lfm` — REMOVER `chkFreeDots`; caixa `520×270` → `520×240`|V40,V38
T330|x|`localization.lang` + mapa `PT` de `WoD20.6` — REMOVER as 3 chaves de `Allow Buy Dots For Free`|V10,V22,V28
T331|x|`verify-hunters-hunted.ps1` — REMOVER os checks §V105 §V106 §V113…§V118; `$wantFlags` volta a 4; `freeDots` sai de `$luaOwned`; `stFreeDots` & `freeDots` entram nos ÓRFÃOS de §I3 (mutação: widget novo c/ o nome queimado = vermelho)|V20,V2
T332|x|`module.xml` version `3.6` → `3.7` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (instalado c/ mesmo size)|V6,V7
T333|x|`WoD20.9.lfm` — REMOVER `xpLedgerObserver` & seu `onChanged`; `renderXPLedger(form, rows)` passa a aceitar linhas prontas (nil = calcula) & `onNodeReady` grava `xpLedgerForm = self`|V121,V125
T334|x|`WoD20th.lfm` — `xpGuard` calcula as linhas 1× & alimenta `renderXPBoxes(form, spent)` & `renderXPLedger(xpLedgerForm, rows)`, os 2 sob checagem de nil ∴ log segue o clique sem depender de §R35|V125,V121
T335|x|`WoD20th.lfm` — `setField(name, value)` (grava só quando difere); `regroupHealthMarks` & `renderHealthTrack` & as sementes do `onNodeReady` escrevem por ele|V122
T336|x|`WoD20.3.lfm` — `dataLink` parte em 2: ~80 traços + `sheetTheme` → só `renderCombatTraits`; `healthLevels` + `language` → só `renderHealthTrack`|V123
T337|x|`WoD20.6.lfm` — `renderAbilityLabels` memoiza (época, idioma) & grava `c.field`/`c.checked` só quando difere|V124,V122
T338|x|`WoD20th.lfm` — desfazer do `xpGuard` escrito sob `xpQuiet` ∴ ⊥ re-entra & ⊥ paga 2ª varredura|V127
T339|x|`WoD20th/images/` — recomprimir os 4 `paper_*.png` p/ ≤120 KB cada (Σ ≤400 KB); conferir na tela nas 4 épocas ANTES de aceitar|V126,V58
T344|x|`WoD20th.lfm` — REMOVER `<tab name="tabMagika">` & seu `<import>`; `stShowMagika` sai do `dataLink` do form raiz & do mapa `shown` de `applyTabVisibility`|V128,V92,I1c
T345|x|APAGAR `WoD20.12.lfm`; `WoD20.10.lfm` — REMOVER `chkShowMagika` & o comentário morto de free-buy (§V105, 38ª rodada); caixa `520×240` → `520×210`|V128,V40,V38
T346|x|`localization.lang` — REMOVER `wod.Magika` & `wod.Show Magika` de [pt] & [en]; mapa `PT` de `WoD20.6` no MESMO commit|V10,V22,V28
T347|x|`verify-hunters-hunted.ps1` — `$wantFlags` 4→3 · `stShowMagika` entra nos ÓRFÃOS de §I3 · `WoD20.12` sai da lista de arquivos · check NOVO §V128. Mutação antes de aceitar|V20,V128,V2
T340|x|`verify-hunters-hunted.ps1` — checks NOVOS §V121…§V128. Mutação antes de aceitar|V20,V121,V122,V123,V124,V125,V126,V127,V128
T341|x|`module.xml` version `3.7` → `3.8` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (Firecast FECHADO, instalado c/ mesmo size)|V6,V7
T343|x|2ª varredura de ⊥-usado DEPOIS do build (pedido 5 do user): template · identificador de script · PNG · campo órfão · chave `.lang` exclusiva de `WoD20th/`. O que sobrar vira §T próprio — remoção calada ⊥|V64,V2
T348|x|`WoD20th.lfm` — `xpGuard`: dentro do ramo `if undo then`, DEPOIS do desfazer & do re-render, `if on then` levanta `Dialogs.showMessage(translateSheetText("Not enough experience to buy that dot", sheet.language or "en"))` sob nil-check de `translateSheetText` (≡ `xpLedgerRefresh`). Checks de §V125 (≤2 varreduras) & §V127 (bloco do desfazer) seguem VERDES|V129,I12,R41
T349|x|`WoD20th.lfm` — `xpSetCurrent`: `tonumber(typed) or 0` < 0 → ⊥ grava `xpTotal`, chama `renderXPBoxes(form)` (a caixa volta) + pop-up `Experience cannot go below zero` & retorna|V131,I11,B35
T350|x|`WoD20th.lfm` — semente da migração em `renderXPBoxes`: `math.max(0, tonumber(sheet.experience) or 0) + spent` ∴ ficha velha c/ `experience` negativo ⊥ nasce devendo. Segue 1 só escrita de `xpTotal` ali (gate de §V100 conta 2 no arquivo)|V131,I11,B35
T351|x|`localization.lang` [pt] & [en] + mapa `PT` de `WoD20.6.lfm` — 2 strings NOVAS nos 3 lugares, MESMO commit: `Not enough experience to buy that dot` = `Experiência insuficiente para comprar esse ponto` · `Experience cannot go below zero` = `A experiência não pode ficar negativa`|V130,V10,V22,V28
T352|x|`verify-hunters-hunted.ps1` — checks NOVOS §V129 §V130 §V131. Mutação antes de aceitar (§V20): apagar o pop-up · trocar por literal PT · mover o pop-up p/ o ramo de §V103 · aceitar negativo em `xpSetCurrent` · tirar o `math.max` da semente — ∀ um ! ficar VERMELHO|V20,V129,V130,V131
T353|x|`module.xml` version `3.8` → `3.9` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (Firecast FECHADO, instalado c/ mesmo size)|V6,V7
T355|x|`WoD20th.lfm` — `xpGuard`: no ramo do desfazer, achar o dot no form CLICADO (varredura recursiva por `field` igual) & escrever `.checked = not on` junto do `setField`, os 2 sob `xpQuiet`. ⊥ achou → só NDB, sem erro|V132,B36,I12
T356|x|`WoD20th.lfm` + `WoD20.1.lfm` + `WoD20.9.lfm` — `xpBoxForms` registrado no `onNodeReady` de quem tem caixa; `xpGuard` repinta TODAS c/ o `spent` que já calculou (inclui o caminho de saída c/ `baseline` vazio). `renderXPBoxes(form, spent)` segue igual p/ os `dataLink`|V133,I11,V125
T357|x|`verify-hunters-hunted.ps1` — checks NOVOS §V132 §V133 + ajustar o check de §V125 (o guarda passa a chamar o repintor de TODAS as caixas). Mutação antes de aceitar (§V20): tirar a escrita do `checked` · tirar o `xpQuiet` de volta · repintar só o form clicado · ⊥ registrar `WoD20.1`|V20,V132,V133,V125
T358|x|`module.xml` version `3.9` → `4.0` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (Firecast FECHADO, instalado c/ mesmo size)|V6,V7
T360|x|`WoD20.1.lfm` (44 dots) + `WoD20.7.lfm` (20 dots) — ∀ bolinha que custa XP: `onChange="xpGuard(…)"` SAI, entram `autoChange="false"` & `onClick="xpClick(…)"`. Linha de habilidade segue passando o `field` do controle nomeado (campo re-ligado por época, §V76)|V134,V99,V76
T361|x|`WoD20th.lfm` — `xpGuard` vira `xpClick(field, form)` (§I13): decide antes de escrever & ⊥ tem ramo de desfazer; `xpLedgerRows(field, want)` simula; `traitLevel` lê o valor simulado do campo em jogo. `findDot` muda de dono — pinta a bolinha ACEITA|V135,V136,V132,V127,I13
T362|x|`verify-hunters-hunted.ps1` — §V99 retargetado (dot chama `xpClick` no `onClick` & declara `autoChange="false"`) · §V111 retargetado (só-leitura = `autoChange="false"` SEM `onClick`) · §V129 retargetado (pop-up no ramo de recusa de `xpClick`) · checks de §V127 & §V132 REMOVIDOS · §V134 §V135 §V136 novos. Mutação antes de aceitar|V20,V134,V135,V136,V99,V111,V129
T363|x|`module.xml` version `4.0` → `4.1` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (Firecast FECHADO, instalado c/ mesmo size)|V6,V7
T365|x|`WoD20.10.lfm` — `checkBox` NOVO `chkFreeBuy` `left="15" top="180" width="490" height="25" field="stFreeBuy" text="Free dots"`; caixa `520×210` → `520×240`. ⊥ ∃ semente no `onNodeReady` (nil = OFF)|I8d,V10,V38
T366|x|`localization.lang` [pt] & [en] + mapa `PT` de `WoD20.6.lfm` — `Free dots` = `Pontos grátis` nos 3 lugares, MESMO commit|V10,V22,V28,V9
T367|x|`WoD20th.lfm` — preço: `xpLedgerRows(clickField, clickValue, clickFree)`; `ctx.free` = `sheet.xpFree` + a chave do clique, montado 1× por varredura; `pushRise` volta a receber `field` & zera `cost` quando `<field>#<lvl>` ∈ `ctx.free` — DEPOIS de `xpCost`. `xpSpent()` segue chamando sem param|V138,V139,V86,I13
T368|x|`WoD20th.lfm` — `xpClick`: `free` = `sheet.stFreeBuy == true` lido 1× no ramo de COMPRA; `key` = `<traço>#<traitLevel+1>`; grátis → salta o teste de saldo; aceite grava `xpFree` junto do `markDot`; venda aceita apaga `<traço>#<nível velho>`. ⊥ escreve nada antes da decisão|V137,V140,V141,V135,I13
T369|x|`verify-hunters-hunted.ps1` — `$wantFlags` 3→4 (`stFreeBuy`) · check de §V136 aceita o 3º param · checks NOVOS §V137…§V141. Mutação antes de aceitar (§V20): ler `stFreeBuy` num renderer · carimbar c/ o campo clicado em vez do nível · zerar dentro do `xpCost` · gravar o carimbo antes da decisão · tirar a apagada da venda — ∀ um ! ficar VERMELHO|V20,V137,V138,V139,V140,V141
T370|x|`module.xml` version `4.1` → `4.2` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (Firecast FECHADO, instalado c/ mesmo size)|V6,V7
T372|x|`WoD20th.lfm` — `xpFind(node, names, out)` (1 passada recursiva, preenche `out[nome] = controle`); `renderXPBoxes(node, spent)` acha as 4 caixas por ele em vez de `form.<nome>`; núcleo de pintura SEM teste de `xpQuiet` & o teste fica só na entrada de `dataLink`/`onShow`. `xpBoxForms` & `registerXPBoxes` SAEM|V133,V142,V143,I11
T373|x|`WoD20th.lfm` — `renderAllXPBoxes(from, spent)` = `tabRootOf(from)` + pinta; `xpClick` passa o `form` clicado nos 2 ramos aceitos (compra & venda) & `xpSetCurrent` passa o dele ∴ ⊥ sobra caminho que repinta só 1 aba|V133,V142,I13,I11
T374|x|`WoD20.9.lfm` — `renderXPLedger(node, rows)` acha `dynXpType` `dynXpTrait` `dynXpLevel` `dynXpCost` `xpLogBox` pelo `xpFind` (1 passada, ⊥ 5); `xpLedgerForm` & `xpLedgerRefresh(rows)` viram `xpLedgerRefresh(from, rows)`. `WoD20.1.lfm` & `WoD20.9.lfm` — as 3 linhas de registro SAEM|V133,V143,V125
T375|x|`verify-hunters-hunted.ps1` — §V133 retargetado (varredura da raiz, ⊥ registro) · checks de `xpBoxForms`/`registerXPBoxes`/`xpLedgerForm` REMOVIDOS (§V121 §V125 §V133) · §V142 §V143 novos. Mutação antes de aceitar (§V20): pintar só o form clicado · repintar sob `xpQuiet` · 2º achador por nome · ledger achado por registro|V20,V133,V142,V143,V125
T376|x|`module.xml` version `4.2` → `4.3` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (Firecast FECHADO, instalado c/ mesmo size)|V6,V7
T378|x|`WoD20.10.lfm` — 3 caixas `Notes` `400×580` clonadas de `WoD20.5` (`left` `0`/`410`/`820`, `top="0"`, `rectangle` preto 14/14 `innerRound`, rótulo `Notes` centrado, `textEditor` `380×540` em `10`/`25` c/ `field` `stNotes1` `stNotes2` `stNotes3`, SEM `name=`); caixa `STORYTELLER SETTINGS` desce p/ `top="590"`|V144,V40,V48,V68,V27,I8e
T379|x|`verify-hunters-hunted.ps1` — check NOVO §V144 (`stNotes1..3` = 3 donos únicos, todos em `WoD20.10` · `anotacoes*` ∉ `WoD20.10` · `stNotes*` ∉ `WoD20.5`). Mutação antes de aceitar (§V20): apontar 1 caixa p/ `anotacoes1` · pôr `stNotes3` também em `WoD20.5` · deixar só 2 caixas|V20,V144,V1
T380|x|`module.xml` version `4.3` → `4.4` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (Firecast FECHADO, instalado c/ mesmo size)|V6,V7
T382|x|`WoD20.1.lfm` — REMOVER a caixa `BACKGROUNDS` (`890`/`510`, `320×300`) & as 9 linhas; o template `OpenAbility` SAI daqui (vai p/ `WoD20.2`); mapa do grid no comentário do topo atualizado & o buraco `890..1210` × `510..810` fica DECLARADO no comentário|I3,V69,V59
T383|x|`WoD20.2.lfm` — caixa `BACKGROUNDS` NOVA em `left="0" top="0" width="320" height="640"` (rótulo centrado, `rectangle` preto 14/14 `innerRound`), 20 linhas `<OpenAbility field="background_N">` em `left=15`, `top` `25`+30·(N-1), `290×25`; template `OpenAbility` entra neste arquivo; ∀ outra caixa +330 em x; mapa do comentário atualizado (fecha em `x=1370`)|V145,V40,V48,V68,V27,V5,I3
T384|x|`WoD20th.lfm` — `BACKGROUND_ROWS = 20` declarado 1× & lido pelos 2 laços (`declareTrait("background_"…)` & o do ledger), hoje `for i = 1, 9` nos 2|V145,V86,I9
T385|x|`verify-hunters-hunted.ps1` — §V5 passa a esperar 20 linhas & 5 bolinhas em `background_1..20` · §V69 retargetado (`HUMANITY` fecha em 810, sem comparar c/ `BACKGROUNDS`) · §V145 novo (XML = `BACKGROUND_ROWS` = maior índice). Mutação antes de aceitar (§V20): 21ª linha só no XML · `BACKGROUND_ROWS` 20→9 c/ XML em 20 · `HUMANITY` fechando em 800|V20,V145,V69,V5
T386|x|`module.xml` version `4.4` → `4.5` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (Firecast FECHADO, instalado c/ mesmo size). Carrega tb o install pendente de §T380|V6,V7
T388|x|`WoD20.1.lfm` — caixa `SPECIALITIES` em `left="890" top="510" width="320" height="300"` (`rectangle` preto 14/14 `innerRound`, título `left=0 top=4 width=320` centrado) + template `SpecialityRow`: `comboBox name="cboSpeciality$(num)"` `150×25` @0 `field="speciality_$(num)"` c/ os 51 itens INLINE (§V18 na forma de então; §I27/§T493 movem esta lista p/ `PICKER_LIST["speciality"]` — a §T está `x` & descreve o que foi FEITO, ⊥ o alvo de hoje) · `edit` `115×25` @155 `field="specialityName_$(num)"` · `imageCheckBox` `25×25` @275 `field="speciality_$(num)_1"` `autoChange="false"` `onClick="xpClick('speciality_$(num)_1', self);"`. 9 linhas em `left="10"` `top=25+30·(N-1)`; o mapa do comentário do topo deixa de dizer `(empty)`|I14,V146,V147,V148,V134,V13,V15,V18,V27,V40,V48
T389|x|`WoD20th.lfm` — `SPECIALITY_ROWS = 9` & `SPEC_TRAIT` DERIVADA de `XP_ATTRS` + `ABILITY_FIELD` (`{ name, free }`; `free = 1` p/ `crafts` `expression` `performance` `academics` `technology`, `4` p/ os outros 45) & `declareTrait("speciality_" .. i, 1, 1, 0)` p/ as 9 linhas|V147,V149,I14
T390|x|`WoD20th.lfm` — `specCost()` (`stSpec4XP` → 4 · `stSpec3XP` → 3 · nenhuma → 0) & `xpCost` c/ `kind == "Speciality"`; `xpLedgerRows` ganha o laço das 9 linhas (`pushRise(rows, "Speciality", slotName(field), …)`) DEPOIS do de númina ∴ a ordem do log fecha por §I10|I9,I10,V154,V86,V120
T391|x|`WoD20th.lfm` — `xpClick`, ramo de linha de especialidade: `speciality_N` vazio → `xpWarn("Choose a trait first")` & `return` · nenhuma flag ligada → `xpWarn("Specialities cannot be bought")` & `return`. As 2 ANTES de qualquer escrita; passadas elas o clique segue o caminho de sempre (preço, saldo, §V129)|V154,V156,V135,V129,I13
T392|x|`WoD20th.lfm` — `grantSpeciality(form, trait, reached)` + chamada nos 2 caminhos ACEITOS do `xpClick` (criação c/ `baseline` vazio & compra preçada): dedup traço+carimbo · slot = menor `speciality_N` vazio · nome + `markDot` + carimbo `speciality_N#1` num passo · sem slot → `xpWarn("No free speciality slot")` & ⊥ escreve nada · concessão re-anda o ledger 1× antes de `renderAllXPBoxes` & `xpLedgerRefresh`|V150,V151,V152,V153,V125,I14
T393|x|`WoD20.10.lfm` — `chkSpec3XP` (`stSpec3XP`) @ `top="210"` & `chkSpec4XP` (`stSpec4XP`) @ `top="240"`, `490×25` ≡ as 4 irmãs; caixa & título `520×240` → `520×300`; 2 `<dataLink>` novos — flag que virou `true` faz `setField(<irmã>, false)`|I8f,V155,V40,V48,V27
T394|x|`localization.lang` [en] & [pt] + mapa `PT` de `WoD20.6.lfm` — 7 strings: `SPECIALITIES`=`ESPECIALIDADES` · `Allow buy Specialization for 3 experience points`=`Permitir comprar Especialização por 3 pontos de experiência` · `... for 4 ...`=`... por 4 ...` · `Speciality`=`Especialidade` · `Choose a trait first`=`Escolha um traço primeiro` · `Specialities cannot be bought`=`Especialidades não podem ser compradas` · `No free speciality slot`=`Não há espaço livre para especialidade`. Os 50 nomes de traço ⊥ entram — já ∃ nos 2 lados|V9,V10,V17,V28,V130
T395|x|`verify-hunters-hunted.ps1` — §V5 aprende `speciality_1..9` (9 linhas × 1 bolinha) · `$wantFlags` 4→6 (§V89) · checks NOVOS §V146…§V157, c/ §V148 comparando XML × Lua nos 2 sentidos. Mutação antes de aceitar (§V20): 10ª linha só no XML · item a mais no picker · `free = 4` p/ `crafts` · brinde escrito por um renderer · dedup só pelo nome · conceder sem slot livre · as 2 flags ligadas juntas · recusa muda — ∀ uma ! ficar VERMELHA|V20,V5,V89,V146,V147,V148,V149,V150,V151,V152,V153,V154,V155,V156,V157
T396|x|`module.xml` version `4.5` → `4.6` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (Firecast FECHADO, instalado c/ mesmo size)|V6,V7
T398|x|`WoD20th.lfm` — `isFreeRow(field)` (1 linha, `\|<field>#1\|` ∈ `xpFree`) & `freeRowOf(name)` extraídos do dedup de `grantSpeciality`, que passa a chamar o 2º; `revokeSpeciality(form, trait, reached)` = revoga ⟺ `SPEC_TRAIT[trait]` ∃ & `reached < .free` & `freeRowOf` acha, limpando traço + texto + bolinha + carimbo num passo & devolvendo se escreveu|V159,V160,V163,I14
T399|x|`WoD20th.lfm` — `xpClick`: `revokeSpeciality(form, trait, level - 1)` na VENDA aceita, espelho do `grantSpeciality(form, trait, level + 1)` da compra, nos 2 caminhos (criação c/ `baseline` vazio & preçado); qualquer um dos 2 escrevendo → re-anda o ledger 1× & chama `renderSpecialities`|V158,V125,V143,I13,I14
T400|x|`WoD20th.lfm` — trava da bolinha: no bloco de linha de especialidade, movido p/ ANTES do teste de `baseline`, `not want` **&** `isFreeRow(trait)` → `xpWarn("Free specialties are lost only by lowering the trait")` & ⊥ escreve nada. A recusa das 2 flags do mestre fica sob `base ~= nil` (§V101) & a de traço vazio segue valendo sempre|V161,V101,V129,V130,V135,I13
T401|x|`WoD20th.lfm` — `renderSpecialities(from)`: `xpFind` a partir de `tabRootOf(from)` acha `cboSpeciality1..9`; p/ ∀ linha, `enabled` = ⊥ `isFreeRow` & `opacity` = `1 \| 0.55` escritos na MESMA linha. ⊥ toca `specialityName_*` nem a bolinha|V161,V162,V163,V164,V143
T402|x|`WoD20.1.lfm` — `renderSpecialities(self)` no `onNodeReady` (junto de `renderHealthTrack`/`renderXPBoxes`) + `<dataLink field="xpFree" onChange="renderSpecialities(self);">` ∴ concessão feita por outro cliente da mesa também trava a linha aqui|V164,V8,V19,I14
T403|x|rename de TEXTO (⊥ de campo): `WoD20.1.lfm` título `SPECIALITIES`→`SPECIALTIES` · `WoD20th.lfm` `kind` `"Speciality"`→`"Specialty"` nos 2 pontos (`pushRise` & `xpCost`) & os 2 pop-ups · `localization.lang` [en]+[pt] & mapa `PT` de `WoD20.6`: 4 chaves velhas SAEM, 4 novas + `Free specialties are lost only by lowering the trait` entram (`ESPECIALIZAÇÕES` · `Especialização` · `Especializações não podem ser compradas` · `Não há espaço livre para especialização` · `Especializações gratuitas só são perdidas ao baixar o traço`)|V165,V9,V10,V17,V28,V130,I10
T404|x|`verify-hunters-hunted.ps1` — checks NOVOS §V158…§V165 + retarget: §V129 3→4 pop-ups no `xpClick` (o 4º é a trava) · §V138 3→4 escritas de `xpFree` (a 4ª é a revogação) · §V152/§V157 c/ o texto só escrito na revogação · §V154/§V156 com o bloco movido p/ antes do `baseline`. Mutação antes de aceitar (§V20): revogar no nível errado (`<=` em vez de `<`) · revogar a linha PAGA · deixar o texto na revogação · vender a linha carimbada em silêncio · trava só depois do `baseline` · `enabled` sem `opacity` · renderer achando controle por 2ª via · chave `.lang` velha sobrevivendo|V20,V158,V159,V160,V161,V162,V163,V164,V165
T405|x|`module.xml` version `4.6` → `4.7` + `rdk -l` (exit 0 & `.rpk` mudou) + `rdk -i` (Firecast FECHADO, instalado c/ mesmo size)|V6,V7
T407|x|`WoD20.1.lfm` — `<layout left="310" top="110" width="900" height="390">` (o `<rectangle>` & o `<label text="ABILITIES">`) APAGADO; as 3 colunas viram filhas diretas do `scrollBox`: `TALENTS` `310/110/290/390` · `SKILLS` `615/110/290/390` · `KNOWLEDGES` `920/110/290/390`, rótulo de título `width` 280→290 (§V27). O mapa do comentário do topo perde a linha `ABILITIES 310..1210` & ganha a fileira de 3|V168,V169,V27,V40,V48
T408|x|`WoD20.1.lfm` — a linha de habilidade recebe os 10px: `<layout left="15" … width="250">` → `width="260"` nas 36 chamadas das 3 colunas & templates `Ability` (`label` 125→135) & `CustomAbility` (`edit` 122→132) c/ os 5 dots em `135/160/185/210/235`|V170,V26,V16
T409|x|`localization.lang` [pt] & [en] + mapa `PT` de `WoD20.6.lfm` — chave `wod.ABILITIES` SAI dos 3 lugares (o rótulo deixou de existir)|V169,V10,V28
T410|x|`WoD20th.lfm` — `<tabControl name="tabsHH" … visible="false">` + `sheetReveal()` (idempotente, ÚNICA escrita de `tabsHH.visible = true`) + `setTimeout(sheetReveal, 1500)` armado no `onNodeReady` da raiz|I15,V166,V167,R44,R48
T411|x|`WoD20.6.lfm` — `sheetReveal()` no FIM do handler do `<dataLink field="sheetTheme">`, depois de `applyTheme` & `renderAbilityLabels` ∴ a 1ª entrada é a última pintura de carga que ∃|I15,V167,I6
T412|x|`verify-hunters-hunted.ps1` — checks NOVOS §V166…§V170 + retarget: §V40 passa a ver 3 caixas soltas em `WoD20.1` · §V27 c/ os títulos em 290 · §V26 c/ o 1º dot em 135. Mutação antes de aceitar (§V20): `visible="true"` de volta no `tabControl` · 2º lugar escrevendo `tabsHH.visible` · reveal só pela pintura (sem o timer) · `TALENTS.left` 310→325 · `KNOWLEDGES` fechando em 1195 · `label` 135 c/ os dots em 125 · chave `wod.ABILITIES` sobrevivendo — ∀ uma ! ficar VERMELHA|V20,V166,V167,V168,V169,V170,V26,V27,V40
T413|x|`module.xml` version `4.7` → `4.8` + `rdk -l` (exit 0 & `.rpk` mudou de mtime & size) + `rdk -i` (Firecast FECHADO, instalado c/ mesmo size)|V6,V7
T415|x|`WoD20.1.lfm` — as 3 colunas `height` 390→340 (`y 110..450`): `TALENTS` `310/110/290/340` · `SKILLS` `615/110/290/340` · `KNOWLEDGES` `920/110/290/340`. ⊥ toca linha, largura nem x. Mapa do comentário do topo atualizado|V171,V168,V40
T416|x|`WoD20.1.lfm` — fileira de baixo sobe 50 & herda o x da coluna: `VIRTUES` `310/460/290/126` & `HUMANITY & WILLPOWER` `310/596/290/164` (títulos 280→290 §V27, trilhas de 250 `left` 15→20) · `HEALTH` (`dynHealth_box`) `615/460/290/306` (título 205→215 & `cmbHealthLevels` `left` 212→222; as 10 linhas ficam) · avatar `<rectangle>` & `<image>` `0/405/300/355`|V69,V168,V27,V40,V49
T417|x|`WoD20.1.lfm` — `SPECIALTIES` `920/460/290/300` (título 320→290) & a linha re-cabe em 270: `<layout left="10" … width="300">` → `width="270"` nas 9 chamadas & `SpecialityRow` `edit` `width` 115→85 & `imageCheckBox` `left` 275→245. `comboBox` de 150 INTACTO (§V16)|V146,V168,V16,V27
T418|x|`verify-hunters-hunted.ps1` — §V69 §V146 §V168 reescritos (literal → relação) + §V171 novo. Mutação antes de aceitar (§V20): `HUMANITY` fechando 10 acima de `SPECIALTIES` · avatar deixado em 810 · `HEALTH` c/ `left` 600 · `SPECIALTIES` c/ `width` 320 · coluna de volta aos 390 · linha de especialidade somando 300 numa caixa de 290 · 1 coluna c/ altura diferente das irmãs — ∀ uma ! ficar VERMELHA|V20,V69,V146,V168,V171
T419|x|`module.xml` version `4.8` → `4.9` + `rdk -l` (exit 0 & `.rpk` mudou de mtime & size) + `rdk -i` (Firecast FECHADO, instalado c/ mesmo size)|V6,V7
T451|x|`WoD20.11.lfm` — `themePaper11` REMOVIDO; `tabsVamp` fica como ÚNICO filho `align="client"` do form ∴ espelha exatamente a raiz (`tabsHH` sozinho em `WoD20th.lfm`)|V190,B41
T452|x|`WoD20.12.lfm` `WoD20.13.lfm` `WoD20.14.lfm` — cada um ganha `<image name="themePaper12\|13\|14" align="client" style="stretch" hitTest="false" visible="false"/>` como 1º filho do PRÓPRIO `scrollBox`, ≡ `WoD20.7` ∴ papel atrás do conteúdo da sub-aba|V56,V190,V57
T453|x|`verify-hunters-hunted.ps1` — check NOVO §V190 (varre ∀ container dos `.lfm`, ≤1 filho `align="client"`) + §V56 RETARGET: backdrop contado por form de CONTEÚDO (12), ⊥ por `<tab>` da raiz, c/ `WoD20.6` & `WoD20.11` como exceções declaradas. Mutação antes de aceitar em CADA um (§V20): repor `themePaper11` ao lado de `tabsVamp` → §V190 vermelho; tirar 1 dos 3 papéis novos → §V56 vermelho|V20,V190,V56
T454|x|`rdk -l` (exit 0 & `.rpk` mudou de mtime & size) + `rdk -i` (Firecast FECHADO, instalado c/ mesmo size). ⊥ sobe version — `module.xml` `4.9`→`5.0` segue sendo do §T449, no fim da rodada|V6,V7
T431|x|rename da aba — `WoD20th.lfm` `<tab name="tabDisciplines" title="Vampire">` · `localization.lang` [en]+[pt] chave `wod.Vampire` · mapa `PT` de `WoD20.6` · `WoD20.10.lfm` `checkBox` `Show Disciplines` → `Show Vampire`. `stShowDisciplines` & `tabDisciplines` & o `kind` `Discipline` de §I10 ⊥ TOCA|V172,V10,V28,V2
T433|x|`WoD20.10.lfm` — `checkBox` `Edit Clan Disciplines` (`stEditClanDisc`, default OFF) como 8ª linha & caixa `STORYTELLER SETTINGS` cresce p/ caber|I8g,V175,V10,V28
T436|x|`WoD20.12.lfm` — área Disciplines: caixa `CLAN DISCIPLINES` (4 linhas) + `DISCIPLINES` (15) + `DESCRIPTION` à direita; radio `discSel` nas 19; 1 template de picker servindo as 2 caixas (§V178). Geometria copiada de `WoD20.7`. 51ª→52ª rodada: picker nasce VAZIO (só o item vazio de §V15) ∴ ⊥ depende da pesquisa & os itens entram em §T460|I16,V176,V178,V26,V27,V29,V40,V48,V68,V15
T438|x|`WoD20.13.lfm` — área Paths: `MAIN PATHS` (5) + `SECONDARY PATHS` (15) + `DESCRIPTION`; radio `pathSel` nas 20; as 5 bolinhas de `mainPath_*` SEM `field`, `autoChange="false"`, sem `onClick` & esmaecidas. Picker nasce VAZIO ≡ §T436 ∴ itens em §T460|I16,I18,V176,V179,V111,V26,V40,V15
T443|x|`WoD20.14.lfm` — área Rituals: 20 linhas de picker (⊥ bolinha) + `DESCRIPTION`; radio `ritualSel` nas 20. Picker nasce VAZIO ≡ §T436 ∴ itens em §T460, & é lá que §V184 (prefixo `<nível>. `) passa a ter o que medir|I16,I19,V176,V29,V40,V15
T437|x|`WoD20th.lfm` + `WoD20.12.lfm` — `renderClanDiscLock(form)`: `enabled` & `opacity` dos 4 combos na MESMA linha, lidos de `stEditClanDisc`; entradas = `onNodeReady` de `WoD20.12` + `<dataLink field="stEditClanDisc">`|V175,V162,V122
T442|x|`WoD20th.lfm` — §I9 ACORDA: custo `clanDisc_*` = `n×5` (`n=0`→10) · `disc_*` = `n×7` (`n=0`→10) · `secPath_*` = `n×4` (`n=0`→7). `declareTrait` & o laço do ledger ganham os 3 grupos; categorias `Discipline` & `Path` entram na ordem de §I10. `mainPath_*` ⊥ entra em nenhum dos 2|I9b,I10b,V86,V120,V145
T456|x|`localization.lang` [en]+[pt] + mapa `PT` de `WoD20.6.lfm` — só os RÓTULOS da fase 1 (títulos de caixa `CLAN DISCIPLINES` `DISCIPLINES` `MAIN PATHS` `SECONDARY PATHS` `RITUALS` `DESCRIPTION` + `Edit Clan Disciplines` + os textos de estado do bloco de descrição). Item de picker ⊥ entra aqui — ⊥ ∃ item ainda (§T447 fecha isso depois da pesquisa)|V10,V28,V17,V33
T457|x|`verify-hunters-hunted.ps1` — checks da fase 1, os que a ESTRUTURA já permite medir: §V176 (3 grupos de radio, `fieldValue` único por grupo) · §V179 (bolinha de `mainPath_*` sem `field`, sem `onClick`, esmaecida) · §V175 (trava dos 4 combos de clã) · §V29 (bloco de descrição `readOnly` & sem `field`) · §V15 (1º item vazio) · §V40/§V26/§V27 nas caixas novas. §V177/§V178/§V184 ficam p/ §T448 — medem ITEM, que ⊥ ∃ ainda. Mutação antes de aceitar (§V20)|V20,V176,V179,V175,V29,V15,V40,V26,V27
T458|x|`rdk -l` (exit 0 & `.rpk` mudou de mtime & size) + `rdk -i` (Firecast FECHADO, instalado c/ mesmo size). ⊥ sobe version — `module.xml` `4.9`→`5.0` segue no §T449, fim da rodada|V6,V7
T461|x|`WoD20.11.lfm` — aninhamento SAI: 1 `tabControl name="tabsVamp"` c/ 3 `<tab>` irmãs — `tabDisc` `Disciplines` · `tabPaths` `Blood Sorcery: Paths` · `tabRituals` `Blood Sorcery: Rituals`, cada uma c/ seu `<import>`. `tabsBS` & `tabBloodSorcery` APAGADOS. ⊥ toca `WoD20.12`/`WoD20.13`/`WoD20.14`|I16,V188,V59,V190
T462|x|`localization.lang` [en]+[pt] + mapa `PT` de `WoD20.6.lfm` — ENTRAM `Blood Sorcery: Paths` & `Blood Sorcery: Rituals`; SAEM como título de aba `Blood Sorcery` & `Paths` & `Rituals` (checar antes se sobrou outro uso visível: `RITUALS` MAIÚSCULO é título de caixa em `WoD20.14` & FICA)|V10,V28,V17
T463|x|`verify-hunters-hunted.ps1` — §V188 RETARGET (1 `tabControl` em `WoD20.11` & 3 abas irmãs; + check NOVO: ⊥ ∃ `tabControl` dentro de `tabControl` em `.lfm` nenhum) + §V192 NOVO (forma de 3 blocos de §I21, medida sobre ∀ entrada de `DESC` dos 3 arquivos — passa vazio enquanto ⊥ ∃ entrada, mas ⊥ pode passar entrada MALFORMADA). Mutação antes de aceitar em cada um (§V20)|V20,V188,V192,I21
T464|x|`rdk -l` (exit 0 & `.rpk` mudou de mtime & size) + `rdk -i` (Firecast FECHADO, mesmo size). ⊥ sobe version — `4.9`→`5.0` segue no §T449|V6,V7
T459|x|PORTÃO DE LAYOUT — user abre o Firecast & JULGA a estrutura inteira c/ os pickers vazios: proporção das caixas · alinhamento das colunas · nº de linhas por caixa (4 clã · 15 disc · 5 main · 15 sec · 25 ritual, era 20 até o 1º ajuste do portão §T465) · tamanho do bloco de descrição · as 3 sub-abas IRMÃS (`Disciplines` · `Blood Sorcery: Paths` · `Blood Sorcery: Rituals`, 54ª rodada). Ajuste pedido aqui vira §T ANTES da pesquisa ∴ ⊥ ∃ ~300 entrada de livro encodada sobre layout ⊥ aprovado. É o portão que a 51ª rodada ⊥ teve — as 4 tarefas que rodaram lá eram todas estrutura invisível ∴ ⊥ ∃ o que julgar|I16,V176,V179,V40
T465|x|`WoD20.14.lfm` — 20 → 25 linhas: `ritual_21`…`_25` em `top` 525·550·575·600·625 (`RitualRow`, ≡ as 20 irmãs, `left=12 width=315`) · caixa `RITUALS` `340×550`→`340×675` (25 + 25×25 + 25) · `DESCRIPTION` `850×550`→`850×675` & `edtRitualDesc` `height` 515→640 (§V193) · `dataLink fields` ganha os 5 campos ∴ a descrição repinta neles também. Comentário do topo (`20 slots`) & o mapa da conta atualizados|I16,V193,V40,V26,V27,V176
T466|x|`verify-hunters-hunted.ps1` — §V193 NOVO, medido como RELAÇÃO nos 3 arquivos (base da caixa mais baixa da esquerda ≡ `height` da `DESCRIPTION` ≡ `textEditor` + 35). Mutação antes de aceitar (§V20): crescer só a coluna da esquerda · encolher só o `textEditor`. §V176 c/ 25 ⊥ entra aqui — segue em §T448, onde sempre esteve|V20,V193,V176
T467|x|`rdk -l` (exit 0 & `.rpk` mudou de mtime & size) + `rdk -i` (Firecast FECHADO, instalado c/ mesmo size). ⊥ sobe version — `4.9`→`5.0` segue no §T449|V6,V7
T422|x|§R — core EN `vampire the masquerade - 20th anniversary edition.pdf` (528 pág; o core PT SAIU, §R53). Clãs & disciplinas & paths JÁ colhidos & gravados em §I17 §I18 §I20 (§R55 §R56) ∴ o que RESTA aqui = (a) reancorar pelo índice, os offsets do PT ⊥ valem (§R57); (b) RITUAIS de Thaumaturgy & Necromancy c/ o nível CERTO — ⊥ por varredura de linha curta (§R54), ler a seção nível a nível; (c) linhagens do cap. Linhagens p/ o `choice` de §I17. Compactar ao terminar|R53,R54,R57,I17,I18,I20,V178
T423|x|§R — `_Vampiro V20 - Dark Ages - Livro Base.pdf` (EN). Mesma extração, DEDUPADA contra T422 — item repetido ⊥ vira 2ª entrada. Compactar ao terminar|V178,R50
T424|x|§R — `Vampire V20 - Anarchs Unbound.pdf` **&** `Vampiro V20 - Dark Ages - Tome of Secrets.pdf` (EN), dedup contra T422-T423. Compactar ao terminar|V178
T425|x|§R — `Vampiro V20 - Lore of the Clans.pdf` (EN, ~1.1M chars, o maior em texto): clãs & linhagens & as disciplinas de clã de cada — fonte PRINCIPAL de `CLANS` (§I17). Dedup. Compactar ao terminar|V178,I17
T426|x|§R — `Vampire V20 - Rites of Blood.pdf` (EN): blood sorcery, paths & rituais — fonte PRINCIPAL de §I18 & §I19. Dedup. Compactar ao terminar|V178,I18,I19
T427|x|§R — `Vampiro V20 - Dark Ages - Lore of the Bloodlines.pdf` (EN), dedup. Compactar ao terminar|V178
T428|x|§R — `Vampiro V20 - Dark Ages - Companion.pdf` (EN), dedup. Compactar ao terminar|V178
T429|x|§R — `Vampiro V20 - The Black Hand A Guide to the TalMaRahe.pdf` (EN), dedup. Compactar ao terminar|V178
T430|x|§R FECHA — as 4 listas CONGELADAS & contadas: disciplinas · clãs+linhagens · paths por blood sorcery · rituais c/ nível. Contagem real decide se os slots pedidos bastam (15 disc, 5 main, 15 sec, 20 ritual); ⊥ bastar → `?` ao user ANTES do XML|V178,V14,I16
T468|x|`WoD20th.lfm` — `ERA_NAME` (§I22) declarada 1× no form RAIZ (≡ `ERA_ABILITIES`), só as entradas que DIVERGEM. Encodada do §R depois de §T430 congelar ∴ ⊥ nasce c/ as trilhas latinas do DA até a pesquisa fechar|I22,V194,V24
T469|x|`WoD20th.lfm` + `WoD20.12`/`WoD20.13`/`WoD20.14` — render de picker aplica `ERA_NAME` sobre o `items` (⊥ sobre `values`, §V194); entradas = as MESMAS de `applyTheme`/§I6 ∴ ⊥ ∃ 2º gatilho de época p/ dessincronizar|I22,V24,V194,I6
T470|x|`DESC` por época — as 3 tabelas ganham o 2º corpo (`daEn`/`daPt` \| tabela irmã, forma decidida em §T444) & o leitor escolhe por `sheetTheme` ∈ {`Dark Ages`,`Classical Age`}. Entrada SEM corpo medieval cai no do core, ⊥ mostra vazio (≡ §V33)|I21,V177,V33,V192
T471|x|`verify-hunters-hunted.ps1` — §V194 NOVO: chaves de `ERA_NAME` ⊆ `values` canônicos · ∀ display c/ chave [pt]+[en] & ∈ mapa `PT` · nenhum `value` de picker de vampiro fora da lista canônica. Mutação antes de aceitar (§V20): pôr `Creatio Ignis` como `value` · display sem chave [pt]|V20,V194,V17,V28
T432|x|`WoD20.1.lfm` — `<comboBox name="cboClan" field="clan" visible="false" enabled="false">` c/ os clãs & linhagens de §R + 1º item vazio (§V15). ⊥ escreve `clan` em Lua nenhum|V173,V15,V17,V8
T434|x|`WoD20th.lfm` — `CLANS` (§I17) encodada do §R, DORMENTE: ⊥ ∃ leitor em `.lfm` nenhum|I17,V174
T435|x|`WoD20th.lfm` — `BLOOD_SORCERY` (§I18) & `RITUAL_DISC` (§I19) encodadas do §R, declaradas 1× no form RAIZ (≡ `HEALTH_MARKS`)|I18,I19,V178
T473|x|`verify-hunters-hunted.ps1` — §V195 NOVO: §V9 & §V24 param de recusar `[^\x00-\x7F]` CRU & passam a recusar `[^\x00-\x7F]` MENOS `’` + check NOVO de `'` ASCII em `items=`\|`values=` de `cbo*`. Mutação antes de aceitar em CADA um (§V20): pôr `Poção` num item (! vermelho em §V9) · pôr `Devil's Touch` c/ apóstrofo reto (! vermelho no check novo & ⊥ no de §V9). BLOQUEIA §T460 — sem ele os 384 itens ⊥ entram|V20,V195,V9,V24
T460|x|`WoD20.12.lfm` `WoD20.13.lfm` `WoD20.14.lfm` — os pickers RECEBEM os itens congelados em §T430: disciplina (serve `clanDisc_*` & `disc_*`, 1 lista só §V178) · path (serve `mainPath_*` & `secPath_*`) · ritual (∀ item c/ prefixo `<nível>. `, §V184). Edita SÓ `items=`/`values=` — layout ⊥ muda, já foi aprovado em §T459|V178,V184,V24,V14,V15,I16
T439|x|`WoD20th.lfm` — `renderMainPaths(form)`: acende as bolinhas de ∀ `mainPath_N` conforme o nível da blood sorcery da linha; entradas = `onNodeReady` de `WoD20.13` + `<dataLink>` dos campos de disciplina que ELE lê (§V123), passando por `setField` (§V122)|V180,V123,V122,V143
T440|x|`WoD20th.lfm` — guarda de PICKER por `dataLink` (⊥ pelo `onChange` do combo, §V186): `mainPath_*` recusa disciplina ausente (§V182) & 2º main path da mesma blood sorcery (§V181) · `secPath_*` recusa disciplina ausente · `ritual_*` recusa disciplina ausente & nível acima (§V185). Recusa reverte o campo & levanta 1 pop-up (§V129 §V130)|V181,V182,V185,V186,V129,V130
T441|x|`WoD20th.lfm` — `xpClick`: teto de `secPath_*` = nível da blood sorcery − 1, testado ANTES de escrever; acima → ⊥ escreve nada & fala|V183,V135,V129
T474|x|`WoD20.12.lfm` `WoD20.13.lfm` `WoD20.14.lfm` — CASCATA de largura, regra textual do user 2026-08-20 (saída do portão §T459): "aumente os campos para caber, empurre o que precisar para a direita de forma cascateada … Sempre empurra o início e o fim" ∴ ⊥ encolhe NADA, `DESCRIPTION` segue 850 & só muda de `left`. Alvo = `ceil(char × 6,5) + 24`, arredondado p/ múltiplo de 5 (§V196). `DiscRow` combo `160`→`170`, bolinhas `190·215·240·265·290`→`200·225·250·275·300`, linha `315`→`325`, as 2 caixas `340`→`350` c/ os 2 rótulos de título junto (§V27), `DESCRIPTION` `left` `350`→`360` · `MainPathRow`+`SecPathRow` (as 2, §V26) combo `160`→`345`, bolinhas →`375·400·425·450·475`, linha →`500`, caixas `340`→`525` + títulos, `DESCRIPTION` `left`→`535` · `RitualRow` combo `290`→`460`, linha →`485`, caixa `340`→`510` + título, `DESCRIPTION` `left`→`520`. Largura total da sub-aba passa a `1210`\|`1385`\|`1370` ∴ a aba Vampire fica MAIS LARGA que a grade de `1210` da Main & rola na horizontal — consequência ACEITA da regra de empurrar (§C)|V196,V16,V25,V26,V27,V37,V40,V193,I16
T475|x|`verify-hunters-hunted.ps1` — §V196 NOVO: p/ ∀ `comboBox` c/ `items=` inline, medir `width` contra o item mais longo entre [en] & [pt] & os display de `ERA_NAME` (§I22) que casem um `value` dele. Reusa `NeededPx`/`$PX_PER_CHAR` de §V16 ∴ 1 régua só p/ label & picker. Mutação antes de aceitar (§V20): estreitar `RitualRow` p/ `290` (! vermelho) · tirar `ERA_NAME` da conta e ver `WoD20.13` passar com `160` (! prova que o eixo de época está sendo medido)|V20,V196,V16,I22,V24
T476|x|`WoD20.6.lfm` + `verify-hunters-hunted.ps1` — §V197: `pickerItems` passa a decidir o eixo de ÉPOCA pela RAIZ do campo, ⊥ pelo prefixo `cbo` (que pega o sheet inteiro): renomeia sse a raiz ∈ {`clanDisc`,`disc`,`mainPath`,`secPath`,`ritual`}. O eixo de IDIOMA ⊥ muda — esse vale p/ ∀ picker. No gate: §V196 soma display de `ERA_NAME` SÓ p/ picker de vampiro (senão dimensiona `WoD20.7` p/ um nome que ela nunca mostra) + check NOVO das 2 pernas de §V197. Mutação (§V20): tirar o escopo & ver `WoD20.7` pedir 343px|V197,V194,V196,I22,I16,V34
T477|x|`WoD20.2.lfm` `WoD20.7.lfm` — cascata de §T474 nos estouros VELHOS que §V196 revelou (⊥ são desta rodada, ∃ desde que o campo nasceu — §B43). Número MEDIDO, ⊥ estimado (a 1ª versão desta linha errou os 2, §B45). `WoD20.2`: `OpenAbility` combo `160`→`170` (`Identidade Alternativa` 22ch), bolinhas `165·190·215·240·265`→`175·200·225·250·275`, linha `290`→`300`, caixa `BACKGROUNDS` `320`→`330` + rótulo (§V27), & TUDO c/ `left` ≥ `330` anda **+10** (`330`→`340` · `540`→`550` · `750`→`760` · `960`→`970` · `1170`→`1180`) ∴ borda direita `1370`→`1380`. `WoD20.7`: pior caso = **`Summoning, Binding, and Warding` 31ch ≈202+24=226**, ⊥ `Psychic Invisibility` ∴ entrada `160`→`230` nos 4 tipos de linha da coluna — `HedgePicker` · `PsychicPicker` · `NuminaFree` (é `edit`, ⊥ combo) · a linha INLINE de `TRUE FAITH` c/ `cboFaith` —, bolinhas `190·215·240·265·290`→`260·285·310·335·360`, linha `315`→`385`, as 3 caixas da esquerda `340`→`410` + os 3 rótulos, `DESCRIPTION` `left` `350`→`420`. `cboFaith` cabe em 160 SOZINHO mas anda junto: deixar 1 das 4 linhas c/ a bolinha 70px à esquerda das irmãs é o escalonamento que §B12 registrou (§V26)|V196,V16,V25,V26,V27,V37,V40,B43,B45
T444|x|`WoD20.12.lfm` — tabela `DESC` de DISCIPLINA, [en]+[pt], 1 entrada por item do picker, na FORMA DE 3 BLOCOS de §I21 (54ª rodada): `<livro>, p. <pág>` · 2 linhas em branco · nome ≡ chave da entrada · 2 linhas em branco · prosa + bloco de sistema VERBATIM. Página é a do ITEM, ⊥ a da seção ∴ anotar ao extrair. [en] verbatim & [pt] tradução one-way (§V9 — a exceção da 51ª saiu junto c/ o core PT)|V177,V192,V189,V9,V32,V33,I21
T445|x|`WoD20.13.lfm` — tabela `DESC` de PATH, mesma forma de §T444 (3 blocos de §I21, página do item)|V177,V192,V189,V9,V33,I21
T446|x|`WoD20.14.lfm` — tabela `DESC` de RITUAL, mesma forma de §T444. Chave = nome COM prefixo de nível (§V184) ∴ o bloco 2 do cabeçalho carrega o prefixo também|V177,V192,V189,V9,V33,V184,I21
T472|x|RENAME `Caminho`→`Trilha` no sentido blood sorcery (§C 57ª) — **16** strings, ⊥ toca campo, valor canônico nem chave [en] (≡ §V165). `localization.lang` [pt] (7): `MAIN PATHS`=`TRILHAS PRINCIPAIS` · `SECONDARY PATHS`=`TRILHAS SECUNDÁRIAS` · `Blood Sorcery: Paths`=`Feitiçaria de Sangue: Trilhas` · `Choose a path first`=`Escolha uma trilha primeiro` · `That blood sorcery already has a main path`=`…já tem uma trilha principal` · `That path is above what its blood sorcery allows`=`Essa trilha passa do que a feitiçaria de sangue dela permite` · `That path needs its blood sorcery on the sheet`=`Essa trilha precisa da feitiçaria de sangue dela na ficha` · mapa `PT` de `WoD20.6.lfm` as MESMAS 7 · `WoD20.13.lfm` as 2 de `NO_SELECTION`/`EMPTY_ROW`, que são só-Lua (§V33) ∴ ⊥ ∃ no `.lang` & escapam de varredura que só olhe o `.lang`. GÊNERO muda junto (`trilha` f., `caminho` m.): `Esse caminho`→`Essa trilha`, `dele`→`dela`, `um … principal`→`uma … principal`. ⊥ TOCA: `HEDGE MAGIC PATHS` · `* The first path is the Affinity Path` · as 71 de `WoD20.7` — são o OUTRO sentido (§C). §V16/§V25 a favor: `TRILHAS PRINCIPAIS` é 1 char MENOR ∴ ⊥ estoura caixa|V34,V16,V25,V33,I10b
T447|x|`localization.lang` [en]+[pt] + mapa `PT` de `WoD20.6.lfm` — chave p/ ∀ item de picker novo (§V17), ∀ rótulo de caixa & sub-aba (§V10), ∀ pop-up das recusas (§V130). Glossário PT de §C aplicado (§V34)|V17,V10,V28,V130,V34
T448|x|`verify-hunters-hunted.ps1` — checks NOVOS §V172…§V189 + retarget §V59 (conta os `.lfm` da pasta em vez do literal `10`) & §V128 (as 3 manejadas seguem 3, `tabDisciplines` só mudou de título). Mutação antes de aceitar em CADA check (§V20)|V20,V187,V189,V59
T449|x|`module.xml` version `4.9` → `5.0` + `rdk -l` (exit 0 & `.rpk` mudou de mtime & size) + `rdk -i` (Firecast FECHADO, instalado c/ mesmo size) + REPORTAR o tamanho do `.rpk` antes e depois (§C: volume aceito, mas MEDIDO). Baseline da 65ª p/ o "antes" = **3.775.337 B** (2026-08-21 01:52, `.rpk` & instalado batendo) ∴ o "depois" mede só o custo do bump, ⊥ o da migração de §I24. §R91 ESCRITO na 66ª ∴ ⊥ é trabalho daqui — o `?` que sobra dele (subpasta→raiz, 0 precedente) cai em §T450 & ⊥ trava o bump|V6,V7,R91
T478|x|PILOTO de §I24 — `WoD20.7.lfm` (numina, **380 KB** de `DESC`): a tabela SAI p/ o módulo, `require` DENTRO de `numinaText()` (§V198); renderer & os 3 textos de estado FICAM no `.lfm`. Decide MEDINDO se `require` aceita subpasta; se ⊥, módulo vai p/ a RAIZ do plugin (caso provado por `Plugins/Sheets/DnD5e/common.lua`) & o achado vira §R91. No gate: §V198 NOVA + §V32/§V34/§V192 passam a ler o `.lua` — a região entre marcadores ⊥ muda de FORMA ∴ só o caminho lido muda & os parsers de regex seguem valendo. Mutação antes de aceitar (§V20): subir o `require` p/ o topo do CDATA. FECHA c/ gate VERDE sozinha ∴ ⊥ deixa a migração pela metade|V198,V189,V34,V32,V192,V20,I24,I21
T479|x|`WoD20.12.lfm` (disciplina, **502 KB**) — mesma forma de §T478, c/ o caminho JÁ decidido por §R91 ∴ ⊥ remede nada. §V177 (∀ item de picker → ∃ entrada nos 2 idiomas) passa a ler o `.lua`. Gate VERDE sozinha|V198,V189,V34,V177,V192,I24
T480|x|`WoD20.13.lfm` (path, **754 KB**, o maior) — ≡ §T479. `NO_SELECTION`/`EMPTY_ROW` daqui carregam o rename `Trilha` de §T472 & são só-Lua (§V33) ∴ FICAM no `.lfm` c/ o renderer, ⊥ migram junto do texto. Gate VERDE sozinha|V198,V189,V34,V177,V192,V33,I24
T481|x|`WoD20.14.lfm` (ritual, **414 KB**) — ≡ §T479. DEPENDE de §T446 FECHADA: enquanto ela está `~` (123/284) o gerador `research/rebuild_ritual_desc.sh` escreve no `.lfm` ∴ mover o alvo no meio da extração quebra o gerador & o mapa `research/ritual_pages.tsv`. Rodar ANTES = §T446 & §T481 brigando pelo mesmo arquivo. Chave c/ prefixo de nível (§V184) ⊥ muda ao migrar|V198,V189,V34,V177,V192,V184,I24
T482|x|`WoD20th.lfm` — `RITUAL_ROWS` `20`→`25` (§B46) · `discLevels()` NOVA (mapa `<disciplina> = <nível>`, 1 varredura das 19 linhas) · `pickRefusal(field, value, levels)` ganha o 3º parâmetro & PERDE a chamada a `discLevel` de dentro (§V205) · `pickAllowed(field, value, levels)` GLOBAL = `pickRefusal(...) == nil`. `guardPick` monta o mapa 1× & repassa. Regra ⊥ muda — só de onde vem o nível|V199,V204,V205,B46,I25
T483|x|`WoD20.6.lfm` — `pickerItems` ganha snapshot autorado (`AUTHORED_VALUES` por handle, ≡ `originalText`/§V22) + 3º passo de filtro por `pickAllowed`, c/ item vazio (§V15) & valor da linha (§V200) preservados & `items`/`values` escritos como par alinhado (§V202). `FILTER_ROW` = 3 raízes, tabela PRÓPRIA ao lado de `VAMP_ROW` (§V203). `renderVampPickers(from)` GLOBAL: monta o mapa 1× (§V205) & refiltra os 45 combos pelo walker de §V143|I25,V199,V200,V201,V202,V203,V205,V143,V15
T484|x|`WoD20.13.lfm` `WoD20.14.lfm` — GATILHO: o `dataLink` de 114 campos que `WoD20.13` já tem passa a chamar `renderVampPickers(self)` junto de `renderMainPaths(self)`; `WoD20.14` ganha 1 IGUAL (os mesmos 114 + os 25 `ritual_*`, ∴ trocar 1 linha reavalia §V181/§V200 nas irmãs) & os 2 `onNodeReady` chamam. `WoD20.12` ⊥ é tocada (§V203). ⊥ ∃ campo novo, ⊥ ∃ rename ∴ ⊥ ∃ órfão (§V2)|I25,V203,V200,V181,V187,V2
T485|x|`verify-hunters-hunted.ps1` — checks NOVOS §V199…§V205. Mutação antes de aceitar em CADA um (§V20): pôr um 2º teste de nível dentro de `pickerItems` (! §V199 vermelho) · `pickerItems` lendo `c.values` como fonte (! §V201) · escrever `items` sem `values` (! §V202) · reusar `VAMP_ROW` como escopo do filtro (! §V203) · `RITUAL_ROWS` de volta a `20` (! §V204) · `discLevel` de volta p/ dentro de `pickRefusal` (! §V205)|V20,V199,V200,V201,V202,V203,V204,V205
T486|x|`rdk -l` (exit 0 & `.rpk` mudou de mtime & size) + `rdk -i` (Firecast FECHADO, instalado c/ mesmo size). ⊥ sobe version — o bump `4.9`→`5.0` é de §T449|V6,V7
T488|x|`WoD20.7.lfm` `WoD20.12.lfm` `WoD20.13.lfm` `WoD20.14.lfm` — §I26, o conserto de §B47. `render<Área>Desc` SAI do `<event name="onNodeReady">` (`WoD20.7`:268 · `WoD20.12`:204 · `WoD20.13`:232 · `WoD20.14`:221) & entra em `<event name="onShow">` do mesmo form; ∀ um dos 4 abre c/ `if ⊥ form.isShowing then return; end;`; os 4 `require` DESCEM p/ depois do early-return de seleção vazia (`WoD20.7`:39 · `WoD20.12`:54 · `WoD20.13`:54 · `WoD20.14`:57). `dataLink` ⊥ é tocado. O comentário de `WoD20.7.lfm:265-267` que se assina §V19/§V33 é REESCRITO — as 2 invariantes ⊥ mudam (§I26). GANHO: abertura deixa de parsear 2.754.357 B, c/ OU SEM seleção salva|I26,V206,B47,V19,V33
T489|x|`verify-hunters-hunted.ps1` — §V206 NOVA, as 2 pernas. Mutação antes de aceitar em CADA perna (§V20): devolver `renderDiscDesc(self)` ao `onNodeReady` de `WoD20.12` ! VERMELHO (perna a) · tirar a guarda de `isShowing` de `renderRitualDesc` ! VERMELHO (perna b). §V198 ⊥ é tocada — as 2 convivem & medem coisas diferentes. JUNTO, a MENSAGEM do check de §V33 (`:1471`): ele testa `$hh7 -match 'renderNuminaDesc\(self\)'` ∴ segue VERDE depois de §T488 — o que quebra é o TEXTO dele, `"renderer is wired (dataLink + onNodeReady)"`, que passaria a afirmar uma fiação que ⊥ ∃ mais. Check verde c/ mensagem mentirosa é §B6 em miniatura (o próximo leitor confia na frase, ⊥ no regex) ∴ o texto vira `(dataLink + onShow)`. A ASSERÇÃO ⊥ muda — §V33 mede que o renderer é CHAMADO, ⊥ de onde|V206,V198,V33,V20,B6
T490|x|`WoD20.13.lfm` OU `WoD20.14.lfm` — só UM `onNodeReady` chama `renderVampPickers`. O filtro é do sheet inteiro (§V203) ∴ 1 passada basta & a 2ª varre 1900 controles à toa (§B48). + §V207 no gate, mutação: devolver a 2ª chamada|V207,V203,B48
T491|x|`WoD20.6.lfm` — `pickerItems` memoiza a lista TRANSFORMADA por (`lang`, `era`, assinatura de níveis): hoje os 25 combo de ritual refazem os MESMOS 284 itens 25× por passada (`eraName` + `PT[]` por item) & escrevem 25 pares no controle nativo. 1 tabela transformada por assinatura, 25 atribuições. Par `items`/`values` segue ALINHADO (§V202) & o snapshot autorado segue por handle (§V201) ∴ o filtro ⊥ muda de regra — muda quantas vezes ele recalcula o mesmo. ORDEM FIXADA: DEPOIS de §T493+§T494. Os 2 reescrevem `pickerItems` & §T493 muda DE ONDE vem a lista AUTORADA enquanto este memoiza a lista TRANSFORMADA ∴ memoizar primeiro é memoizar uma fonte que §T493 reescreve, & o trabalho é REFEITO|V202,V201,V205,B48,T493,T494
T492|x|instrumentação TEMPORÁRIA — `Log.i("WOD", …)` c/ contador no `onNodeReady` da raiz, em `renderVampPickers`, nos 4 `<área>Text()` & em ∀ `onShow` de aba: quantas vezes cada um roda & se o `require` foi ALCANÇADO. É o "antes" que §C exige MEDIDO (≡ §T449 c/ o tamanho do `.rpk`) & é o que §T497 lê. SAI antes de fechar a rodada — ⊥ fica no `.rpk` de entrega|R92i,R92b,B47,B48
T493|x|`WoD20th.lfm` `WoD20.1` `WoD20.2` `WoD20.7` `WoD20.12` `WoD20.13` `WoD20.14` — §I27: as 10 listas `cbo*` do escopo viram tabela Lua (⊥ "as 15" — a contagem 15 é do compilado, §I27/§R92d), ∀ combo recebe `items`/`values` por Lua, os 25 de ritual compartilham 1 tabela. GANHO: 480.053 B (25,4%) fora de `WoD20th.lfm.lua`. ⊥ separar de §T494 — migrar a lista sem mover o gate junto é §B7 de novo, & §B7 já custou os checks §V14/§V15/§V17 passando sem ler nada. FORMA (§I27, 68ª): 1 mapa `PICKER_LIST` no `<script>` da RAIZ, chaveado por raiz de campo, entre `PICKER_LIST_BEGIN`/`_END` — ⊥ 1 tabela por form. A regra velha ("no `<script>` do form que a usa") era INSATISFAZÍVEL: `WoD20.1.lfm` & `WoD20.2.lfm` ⊥ têm `<script>` (CONFERIDO, `grep -c` = 0) & 4 das 10 listas moram neles. `pickerItems` passa a ler `PICKER_LIST[fieldRoot(nm)] or c.values` — a MUDANÇA DE FONTE é o coração desta §T, ⊥ só a origem do dado: os 2 ramos velhos terminavam em `c.values` ∴ tirar o atributo sem trocar a fonte esvazia ∀ picker da ficha, calado (§I27 regra 2, §V201). `AUTHORED_VALUES` & `authoredValues()` SAEM. 10 tabelas ↔ 13 chaves (3 alias por IDENTIDADE, §V211c). Mais 3 coisas: (1) §V18 & §V20 são PRÉ-REQUISITO — as 2 diziam o CONTRÁRIO de §I27 até a 68ª, & §V20 é a regra de aceite que esta §T cita; (2) `MainPathRow` & `SecPathRow` carregam `values=` byte a byte iguais (md5) ∴ `WoD20.13` ! ser consolidado nesta MESMA rodada ou §V208 nasce vermelha; (3) RISCO DE LOAD, ⊥ invariante: o CDATA dos 15 forms é inlinado numa ÚNICA função (`constructNew_frmWoD20th`, `WoD20th.lfm.lua:10..~30492`, ~30,5 mil linhas) & Lua tem teto de 200 locals por função. +14 tabelas cabe c/ folga HOJE, mas estourar falha no LOAD c/ erro de compilação & NENHUM check do gate pegaria — o gate ⊥ roda Lua|I27,V208,V211,V212,V201,V202,V18,V20,R92c,R92d,T494
T494|x|`verify-hunters-hunted.ps1` — os 12 sítios de §V209 passam a ler a tabela Lua, ⊥ os 7 que a lista velha nomeava: §V13 §V14 §V15 §V17/§V28 (`:364` `:384` `:411`) · §V24 (`:1108`) · §V195 (`:1132`) · §V194 (`:1169`) · §V196 (`:1229`) · §V197 (`:1287`) · §V32 (`:1394`) · §V50 (`:1422`) · §V78 (`:1438`) · §V148 (`:3240`) · §V177/§V178 (`:3635`). + §V208 & §V211 & §V212 NOVAS. 5 dos 12 (**a c d f g**) ! GANHAR ZERO-GUARD nesta rodada — sem isso §B7 sobrevive DENTRO da invariante escrita p/ matá-lo (§V209). SAEM: os 3 pinos de `AUTHORED_VALUES`/`authoredValues(c)` (§I27 regra 2) & a assinatura pinada de `pickerItems`, que ganha parâmetro. VIRAM DE TEXTO: a falha de §V148 & o comentário de §V94, os 2 citam `(SPEC V18)` c/ a semântica que a emenda apagou; o comentário de §V178 ("Declared twice is allowed"), porque §V178 mede DIVERGÊNCIA & §V208 mede CÓPIA. Rótulos corrigidos em §V209: **a** alimenta §V9/§V10/§V28 (⊥ 1) · **b** ⊥ inclui §V13 · **l** carrega §V184 & mata §V177/§V184 em silêncio. ÂNCORA de ∀ cite = id de check + string de mensagem, ⊥ número de linha (§V18, §V211b): o build de §T490 já invalidou toda cite acima de `:1935`. Mutação POR SÍTIO (§V20), ⊥ uma p/ o conjunto — a cláusula velha ("esvaziar 1 área ! VERMELHO nos 12") é falsa, §V209. MESMA rodada que §T493|V209,V208,V211,V212,V201,V202,V18,V20,V78,V148,V194,V195,B7,I27,T493
T495|x|`desc{Disc,Numina,Path,Ritual}.lua` → `desc<X>_en.lua` + `desc<X>_pt.lua`, & os 4 `require` viram `require("desc<X>_" .. lang .. ".lua")`. `daEn` vai p/ o `_en`, `daPt` p/ o `_pt`. Trocar idioma em runtime puxa o outro arquivo — o `dataLink` de `language` já repinta (§I26 ⊥ é tocada). GANHO ~50% de QUALQUER módulo que chegue a carregar (§R92f) ∴ vale c/ ou sem §T497 & ⊥ fica atrás dele. `research/rebuild_ritual_desc.sh` (`MOD=`) emite 2 arquivos. §V210 NOVA. NADA É REMOVIDO: os 2 entram no `.rpk` c/ as mesmas chaves (restrição user)|V210,R92f,V177,V34,V32,V192
T498|x|criar `Plugins/Sheets/World of Darkness 20th Anniversary Edition/` & copiar c/ o mapa de §I28 JÁ aplicado: 15 `.lfm` (nome do arquivo · `<import file=>` · `name="frm*"` · dataType · título) · `images/` c/ as 12 `.png` (nomes ⊥ mudam) · os 4 `desc*.lua` · `localization.lang` · `sdk/` · `module.xml` NOVO. ⊥ copiar: as 5 fichas do Ambesek, o `README.md` dele, os 2 `.bat`, `output/`. PROVA antes de seguir p/ §T499: ∀ `.png` c/ md5 IGUAL ao velho & ∀ `.lfm` novo IGUAL ao velho depois de aplicado o mapa ao velho ∴ mede CÓPIA **&** RENAME, ⊥ "∃ arquivo c/ esse nome". A comparação nasce & MORRE nesta §T (§T499 apaga o lado velho) ∴ ⊥ vira §V — check que ⊥ pode rodar amanhã é §V20 ao contrário|I28,V213,V215
T499|x|apagar `HuntersHunted/` & os 4 `desc{Disc,Numina,Path,Ritual}.lua` de `Plugins/Sheets/World of Darkness 20th/`. MESMA rodada que §T498: 2 cópias vivas = 2 fontes p/ divergir a cada §T & o dataType velho declarado 2× no repo. `localization.lang` & `module.xml` do velho ⊥ são podados — as chaves `wod.` são COMPARTILHADAS c/ as 5 fichas dele & ⊥ ∃ medida de quais são só nossas (§C)|V214,V4,T498
T500|x|`verify-hunters-hunted.ps1` — repontar `$plugin` `$dir` `$rpk` `$langFile` (`:16-19`) p/ a pasta & o `.rpk` novos; §V4 passa a caçar `AndreOliveira.Styllern.WoD20th`; as 200 refs `HH.<n>` & as 20 `HuntersHunted` viram os nomes de §I28. + §V213 §V214 §V215 NOVAS, c/ a mutação de CADA uma rodada de verdade (§V20). MESMA rodada que §T498/§T499: gate que siga lendo a pasta velha fica VERDE sobre arquivo que ⊥ existe mais — §B7 literal (check que vira no-op)|V213,V214,V215,V20,B7,T498,T499
T501|x|repontar a prosa: `SPEC.md` (625 `HH.<n>` + 127 `HuntersHunted`, §I1 & §I2 INCLUSIVE — as 2 nomeiam hoje o dataType & os caminhos velhos) · `HANDOFF.md` (5) · `CLAUDE.md` (plugin ativo, raiz de build, nome do `.rpk`, nome da ficha) · `research/rebuild_ritual_desc.sh:26` (`MOD=`). ORDEM da substituição IMPORTA: `Ambesek.HuntersHunted.20th` PRIMEIRO — senão `HuntersHunted`→`WoD20th` produz `Ambesek.WoD20th.20th`; `HH.<n>` só c/ dígito à direita (`HH\.[0-9]+`); `frmHH_<n>` à parte. sed cego nos 3 padrões ⊥ é seguro|I28,V213,T500
T502|x|build + install do plugin NOVO (`rdk -l` & `rdk -i` da raiz DELE) & LER o nome REAL do `.rpk` emitido: as 2 regras de nome de §I28 são INFERÊNCIA ∴ se o rdk emitir outro, `$rpk` de §T500 & o `CLAUDE.md` de §T501 seguem o REAL, ⊥ o suposto. Provas §B1: `.rpk` c/ mtime **&** size novos · `%APPDATA%\Firecast\Plugins\<id minúsculo>.rpk` c/ o size do `.rpk` recém-gerado & mtime da hora do install — CONFERIR o nome do instalado na PASTA, ⊥ supor. Gate `-Build` VERDE fecha a rodada|I28,B1,T500,T501
T503|x|`WoD20th.lfm` `applyTabVisibility` — SAEM: `local tabMain, denied;` · `if name == "tabMain" then tabMain = tab; end;` · `if tab.visible and not want then denied = true; end;` · `if denied and tabMain ~= nil then tabMain:activate(); end;`. O laço segue escrevendo `tab.visible` & o `visible` dos filhos ∴ §V92 INTACTA & ⊥ ∃ campo novo, ⊥ ∃ rename (§V2 intacta). + gate: o sítio de §V93 (`verify-hunters-hunted.ps1`, checks "V93 …", ancorados por STRING de mensagem & ⊥ por número de linha — §V18) vira §V217 medindo AUSÊNCIA, c/ as 2 mutações RODADAS até vermelho. MESMA rodada: check velho de pé exige a linha que esta §T apaga ∴ gate vermelho por construir o que o spec mandou|V217,V92,V20,V18,I8
T504|x|os 5 preços novos de §I9 em 2 sítios, MESMA rodada (§V86 exige tabela do spec == tabela do código): (a) `WoD20th.lfm` `xpCost` — `Discipline` `10`→20 & `from * 5`→`* 15` & `from * 7`→`* 25`; `Path` `7`→20 & `from * 4`→`* 15`; (b) gate `$costWant` — as 4 chaves de `Discipline (…)`/`Path (…)` seguem os mesmos números. + APERTAR uma âncora frouxa que este §T desfaz por acidente: hoje `Numina` é casada por `return from * 7` SEM contexto de `kind` ∴ ela casava também a linha da disciplina fora do clã & passaria verde com a regra de Númina APAGADA (§B7). Com a disciplina indo p/ ×25 o falso par some por sorte, ⊥ por conserto ∴ ancorar `Numina` em `kind == "Numina"` & ADICIONAR chave `Discipline (fora do clã)`. Mutação (§V20) por chave: trocar 1 número no código ! VERMELHO na chave dele · apagar a regra de Númina ! VERMELHO (hoje ⊥ fica)|I9,V86,V85,V20,B7
T505|x|`WoD20.11.lfm` vira `<scrollBox align="client">` único c/ tudo absoluto dentro (§I29): 2 `<layout>` vazios lado a lado em cima (`DOMINATOR` · `BLOOD POOL`) & `tabsVamp` SEM `align`, `top` abaixo deles. SÓ layout, 0 campo — a caixa vazia é o que torna o §T511 possível cedo. RISCO conhecido: sub-aba pode CLIPAR se `tabControl` absoluto dentro de `scrollBox` ⊥ repassar altura aos filhos — ⊥ ∃ precedente (o sheet só usa `align="client"`, 102× MEDIDO) ∴ `?` que só o Firecast responde (§T511)|I29,V221,V190,B41
T506|x|BOX 1 campos: `dominator` `<edit>` · `dominatorGen` `comboBox` (11 + vazio, par `items`/`values` alinhado, §V202) · `clanFamily` `comboBox` lendo chave PRÓPRIA de `PICKER_LIST` c/ os 60 de `clan` (⊥ alias, §I29b) · label `Max Discipline Level` só-leitura. + `GEN_MAX` no form RAIZ entre marcadores, 1 cópia (§V218), & o label repintado por `dataLink` de `dominatorGen`. Os 3 campos ENTRAM em §I3 ∴ §V2 passa a protegê-los|I29,I3,V218,V202,V2,I27
T507|x|o TETO em `xpClick`: recusa de `clanDisc_<n>_<1..5>` / `disc_<n>_<1..5>` acima de `GEN_MAX[dominatorGen]`, ANTES de escrever, colada na recusa de §V183 (mesmo bloco, mesma forma) & falando por `xpWarn`. Geração vazia → sem teto. Ponto velho acima do teto FICA (§V187). Lê `GEN_MAX` de §T506, ⊥ reimplementa a tabela (§V218)|V220,V218,V183,V135,V187,B38
T508|x|BOX 2: `bloodPool_1`…`_20` `imageCheckBox` c/ arte de bolinha, 2 fileiras de 10, LIVRES (sem `autoChange="false"`, sem `onClick`) & semente nil→true nas 10 primeiras no `onNodeReady` da raiz (≡ `appearance_1`). ⊥ entram em `declareTrait`/`XP_TRAIT`/§I9. `applyTheme` JÁ repinta bolinha por arte ∴ elas seguem o tema de graça — CONFERIR que a varredura ⊥ as ignora por ⊥ estarem em `XP_TRAIT`|V219,I3,V134,V2
T509|x|gate: §V218 §V219 §V220 §V221 NOVAS, c/ a mutação de CADA uma RODADA até vermelho (§V20 — §B7 nasceu de check que virou no-op). + §V134 ganha a exceção por PREFIXO `bloodPool_` (⊥ afrouxar a regra geral) & §V5/§V1 passam a contar as 20 bolinhas novas como campo Único. MESMA rodada que §T505…§T508. + 4 FATOS DO GATE que a feature envelheceu (§B50), ⊥ são regra nova: (a) §V99 & §V134 ganham a exceção `bloodPool_` por PREFIXO; (b) §V90 tira `WoD20.11` da lista de forms VAZIOS — a invariante diz "enquanto vazias" & ela ⊥ está mais; (c) §V129 sobe de 6 p/ 7 recusas que FALAM (o teto de geração é a 7ª); (d) §V205 casa `discLevel(` SEM fronteira de palavra & o `-match` do PowerShell é CASE-INSENSITIVE ∴ `maxDiscLevel(` acende um check de OUTRA coisa — ancorar c/ fronteira, ≡ o que §T504 fez em Númina|V218,V219,V220,V221,V222,V99,V134,V90,V129,V205,V20,B7,B50
T510|x|`clanFamily` ganha as 22 famílias de revenant — §R93 FECHADA 2026-08-25 ∴ ⊥ é mais bloqueada. `PICKER_LIST["family"]` AUTORADA c/ as 22 na ordem de §R93 (sem item vazio) · `PICKER_LIST["clanFamily"]` deixa de ser `= PICKER_LIST["clan"]` & vira a UNIÃO montada no construtor (62 + 22 = **84**) · 22 chaves `wod.` NAS 2 LÍNGUAS do `.lang`, sufixo CURTO: EN `Bratovich (Dark Ages)` & `Bratovitch (Modern)` → PT `(Trevas)` & `(Moderna)`, molde de `Gargoyles (Modern)`/`Gárgulas (Modernas)` — ⊥ `(Idade das Trevas)` nem `(Noites Modernas)`, que estouram §V196 · check de §V294 no gate c/ teste de mutação (§V20). ⊥ MEXE EM LAYOUT: pior item segue `Servants of Anushin-Rawan` (187px ≤ 192 de `cboClanFamily`). ⚠ MESMA ONDA que §T640 — sozinha, deixa §V212 & §V236a VERMELHAS (família no picker sem chave em `CLANS`)|R93,I29,V294,V211,V208,V201,V196,V20
T512|x|`WoD20th.lfm` — `renderMaxDisc` DESCE p/ logo depois de `applyTabVisibility` (∴ abaixo do `local function tabRootOf`, L1291) SEM mudar 1 caractere do corpo: é RELOCAÇÃO, ⊥ reescrita. `GEN_MAX` & `maxDiscLevel` FICAM onde estão — §V218 acha o mapa por MARCADOR & ⊥ por linha, & `maxDiscLevel` ⊥ chama `tabRootOf` ∴ ⊥ tem o defeito. Os 2 sítios ganham 1 linha de comentário dizendo POR QUE o trio está partido, senão a rodada seguinte "junta" de volta & reabre §B51. ⊥ promover `tabRootOf` a global (§V223). + gate: §V223 NOVA c/ as 2 provas de §V222 (mutação & sonda) na MESMA rodada. + `rdk -i` obrigatório: o `.rpk` INSTALADO hoje carrega o bug ∴ build sem install ⊥ conserta nada p/ o user|V223,V222,V218,B51,V20,B1
T513|x|`WoD20.11.lfm` vira grade 2×2 (§I30): os 4 pares rótulo/entrada nos 2 x de rótulo & 2 x de entrada · `Dominator`→`Name` & `Dominator Generation`→`Generation` SÓ no `text=` (`field="dominator"` & `field="dominatorGen"` INTACTOS — §V2, & o user grafou os lógicos errado, ver §I30) · as 2 caixas `height` 150→120 & `tabsVamp` `top` 160→130 `height` 620→650 · `wod.Dominator` & `wod.Dominator Generation` SAEM dos 3 lugares porque perdem o último leitor, & ⊥ entra chave nova (`wod.Name`/`wod.Generation` já ∃). + gate §V224 c/ as 2 provas de §V222. §V221 ⊥ muda: ela é paramétrica & passa a guardar 130 sozinha|I30,V224,V2,V16,V221,V28,V222
T514|x|esmaecimento (§V111 REESCRITA): as 4 caixas de descrição PERDEM o atributo `opacity` (exceção NOMINAL no laço, ⊥ afrouxar o teste) & os outros **33** literais `0.55`→`0.40` no XML + os **3** sítios Lua (`WoD20.10:51` · `WoD20th:2438` · `WoD20th:2461`) na MESMA rodada. Gate: `$DIM` é 1 linha SÓ, mas as 2 regex de §V112 (`saved and 0\.55 or 1`) & §V162 (`locked and 0\.55 or 1`) carregam o número HARDCODED & ⊥ leem `$DIM` ∴ mudar só a constante deixa o gate VERMELHO — & isso é FEATURE, é o que prova que os 3 sítios Lua ⊥ foram esquecidos (§V112 nasceu p/ "prop irmã ! mudar no mesmo commit")|V111,V112,V162,V179,V222,V20
T516|x|`WoD20.11.lfm` — `DOMINATOR` `width` 600→875 c/ os 4 x em 12/112/500/675 (entrada da col A 200→320 ∴ o vão de coluna vai a 68px & `cboClanFamily` ganha o espaço que §T510 vai querer) · `BLOOD POOL` `left` 610→885 & `width` 600→500 · as 20 bolinhas recentralizadas nos 2 eixos (`x` 111..388 c/ passo 28 · `top` 42 & 77) · `tabsVamp` `width` 1210→1395. §V224 ! seguir verde (4 colunas, 2 controles cada) & §V221 ⊥ muda (as caixas seguem acabando em 120, `tabsVamp` segue em 130)|I31,V224,V221,V219,V16
T517|x|`WoD20.12.lfm` & `WoD20.14.lfm` — a caixa `DESCRIPTION` fecha em 1385: `12` `width` 850→1025 (rótulo idem, `edtDiscDesc` 830→1005) · `14` 850→865 (rótulo idem, `edtRitualDesc` 830→845), os 2 mantendo os 10px de margem. `WoD20.13` **⊥ é tocado** — ele já fecha em 1385 & é a fonte da régua ∴ mexer nele seria mover o alvo. + gate §V225 c/ as 3 mutações & a sonda de §V222|I31,V225,V111,V222,V20
T519|x|`WoD20.11.lfm` — `tabsVamp` `height` 650→**730** ∴ o fundo vai de 780 p/ 860. `WoD20.14` fecha em **675** & precisa de 675 + inset (§R94, que no eixo Y inclui a FAIXA DE ABAS, ⊥ só a borda) ∴ 55px de folga, & o teto do inset é 40 MEDIDO por dedução: `WoD20.13` fecha em 610 & ⊥ mostra barra ∴ o cliente ≥ 610 ∴ inset ≤ 650-610. CUSTO = **ZERO**: a ficha já exige 920 pela aba Storyteller (`WoD20.10`, MEDIDO nos 15 forms) ∴ 860 ⊥ move o ponto mais alto. ⊥ encolher `WoD20.14`: os 675 são 25 linhas de ritual & cortar altura corta LINHA (§R8). + gate: §V225(a) mede `top+height` ao lado de `left+width`, c/ mutação PRÓPRIA p/ o eixo novo (§V222 — a mutação do X ⊥ prova o Y, foi exatamente essa a lição)|B52,V225,R94,I31,V222,V20
T521|x|**PROBE** no Firecast [USER], ⊥ é a feature (≡ §T239 §T421 §T496): responder §R95 antes de reescrever 14 abas. 1 arquivo descartável c/ 2 painéis irmãos & 1 botão que alterna, nas 2 formas — **(a)** os 2 `align="client"`, 1 c/ `visible="false"` · **(b)** os 2 c/ geometria absoluta + `anchors="{left,top,right,bottom}"`. Pergunta única: o painel visível ocupa a área toda & sobrevive a redimensionar a janela? Se (a) passar, §V190 ganha exceção ESCOPADA (⊥ revogação) & §T522 é barato; se só (b) passar, ∀ painel leva `anchors` & o gate ganha invariante disso; se nenhum, sobra (c) `setParent` & §T496 deixa de ser probe separado. ⊥ escrever §T522 antes desta responder|R95,V190,B41,T496
T522|x|`WoD20th.lfm` — `tabsHH` SAI & entra a barra: `<layout>` de topo c/ 11 botões (`<rectangle hitTest="true" cursor="handPoint" onClick="selectTab('<nome>', self);">` + `<label hitTest="false">`, ≡ `WoD20.1:162`) & 1 corpo c/ os 11 painéis na forma que §T521 aprovar. `selectTab(name, from)` GLOBAL pinta os botões (ativa = `color` do tema · inativa = `#00000000` + `strokeColor`) & liga 1 painel. Estado em variável Lua, ⊥ campo (§V227). `applyTabVisibility` passa a esconder BOTÃO + painel (§V92 intacta em sentido, nova em alvo) & segue ⊥ ativando nada (§V217). `sheetReveal` liga o container novo & o nome `tabsHH` morre (§V213)|I32,R95,V92,V217,V166,V227,V2,I3
T523|x|`WoD20.11.lfm` — as 3 sub-abas de `tabsVamp` na MESMA forma de §T522, reusando `selectTab`. ⚠ §V225 mede `tabsVamp` por NOME & por `width`/`height` de `tabControl` ∴ ela ! ser reescrita p/ medir o container novo na MESMA rodada, senão o gate fica vermelho por construir o que o spec mandou (≡ §T503). §V221 idem (ela lê `//tabControl[@name='tabsVamp']`)|I32,V225,V221,T522
T524|x|gate — §V226 & §V227 NOVAS c/ as 2 provas de §V222 cada · §V190 recebe o que §T521 decidir (exceção escopada \| nada) · §V225 & §V221 reapontadas p/ o container novo · §V213 passa a caçar `tabsHH` como nome MORTO · §V56/§V166 conferidas contra a estrutura nova (o backdrop segue sendo o único `align="client"` do container dele)|V226,V227,V190,V225,V221,V213,V56,V166,V222,V20
T526|x|`WoD20th.lfm` — `tabStrip` ganha PELE: (a) `<rectangle align="client" color="black" xradius="14" yradius="14" cornerType="innerRound"/>` como **1º filho**, sem `strokeColor` (≡ as caixas de `WoD20.1`) · (b) `height` 34→**38** & ∀ `top` de botão/pílula 2→**4** · (c) os 11 `tabOn*` de `black` p/ `DimGray` · (d) larguras novas por §V228 (`left`/`width`, vão 4, 1º em 16 p/ limpar o arco da faixa): Main 16/99 · Traits 119/79 · Numina 202/79 · Disciplines 285/86 · Combat 375/86 · Inventory 465/105 · Background 574/105 · Progress 683/99 · Notes 786/99 · Settings 889/125 · Storyteller 1018/112 — fecha em 1130 & ∀ `label` de dentro copia a `width` do botão. `tabOn*` & `btnTab*` do mesmo par seguem c/ geometria IDÊNTICA. ⊥ mexe em Lua|I33,V228,V229,V190,V53,V16,V68
T527|x|`WoD20.11.lfm` — `vampStrip` só na LARGURA & na cor da pílula: Disc 6/112 · Paths 122/235 · Rituals 361/235 (fecha em 596 de 1395) & os 3 `tabOn*` p/ `DimGray`. ⊥ ganha faixa (já tem `themePaper11` atrás) & ⊥ TOCA `top`=130 nem `height`=34 nem os painéis: 4px de altura ali reabrem §B52, & os painéis de lá são ABSOLUTOS, ⊥ `align="client"`|I33,V228,V229,B52,V221,V225
T528|x|gate — §V228 & §V229 NOVAS, c/ as 2 provas de §V222 cada (mutação que acende + sonda que ⊥ acende, as 4 RODADAS). §V228 reusa `NeededPx`/`$PX_PER_CHAR` de §V16 ∴ 1 régua só p/ label, picker & botão de aba, & lê o `xradius` DO XML em vez de assumir 14 (senão a fórmula afrouxa junto c/ o que mede). Zero-guard obrigatório nos 2: < 14 botões lidos ! VERMELHO citando §V209 — ≡ o que §V226 já faz|V228,V229,V222,V20,V209,V16
T530|x|`WoD20th.lfm` — o chão de `tabStrip` perde `xradius` & `yradius` & `cornerType` ∴ vira `<rectangle align="client" color="black"/>`, o 1º retângulo QUADRADO da ficha (§I33). ⊥ toca em mais nada: os 11 botões & as 11 pílulas seguem `innerRound` 14/14 (§V228) & o `left`=16 do 1º botão FICA (a razão mudou, o número ⊥)|I33,V68,V229,V228
T531|x|gate — §V68 & §V229 na MESMA rodada, senão o canto fica sem dono ou o gate fica vermelho de propósito: §V68 corta o chão por CONSTRUÇÃO (`rectangle[@align='client'][@color='black']` cujo layout pai tem irmão `rectangle[@onClick]`) & ⊥ por nome nem por "⊥ autora canto"; §V229 passa a exigir que o chão ⊥ autore raio nenhum. Zero-guard nos 2: se o corte de §V68 casar ≠1 retângulo ! VERMELHO citando §V209 (hoje casa exatamente 1, MEDIDO) & §V68 ! seguir lendo ≥ 60 caixas pretas. 2 provas de §V222 em cada clause NOVA: devolver o raio ao chão ! VERMELHO em §V229 · esquadrar uma caixa de seção ! VERMELHO em §V68 (prova que o corte ⊥ virou buraco) · sonda: mudar o canto de um `btnTab*` ! VERDE nos 2|V68,V229,V222,V20,V209,I33
T532|x|`WoD20.6.lfm` & `localization.lang` na MESMA rodada (§V10 §V22 §V28 amarram os 3 sítios) — a caixa de créditos vira 3 labels: `Developer: Andre (Styllern)` (`top`=10) · `Based on the sheet plugin by Vinny (Ambesek)` (`top`=50, `fontSize="12"`, `left`=0 `width`=330 por §V27) · `System: Storyteller` (`top`=85); o `<layout>` 265→**110** (último `top` 85 + `height` 20 + os 5 de respiro que a caixa velha tinha em 240+20 sob 265). SAEM os 7 labels de §C & as 7 entradas do mapa `PT` (`:36` `:58` `:103` `:107` `:194` `:283` `:330`) & os 7 pares [pt]+[en] do `.lang` — chave órfã ⊥ acende gate NENHUM (§V22 mede mapa ⊆ `.lang`, ⊥ o inverso) ∴ quem limpa é esta §T ou ninguém. ENTRA 1 par [pt]+[en] & 1 entrada no mapa. A caixa é o ÚLTIMO filho do `scrollBox` ∴ encolher ⊥ pede reflow de irmão nenhum|I3,V9,V10,V16,V22,V27,V28,V213,V230
T533|x|gate — §V213 EMENDADA & §V230 NOVA na MESMA rodada que §T532, senão o gate fica vermelho de propósito: as 3 needles de `verify-hunters-hunted.ps1:4459-4461` cobram HOJE as strings que §T532 apaga. §V213: trocar as 3 pela linha nova + a chave [pt] gêmea. §V230: as 3 pernas, zero-guard em CADA — < 15 `.lfm` lidos ! VERMELHO citando §V209 (≡ o que `:4438` já faz) & `.lang` lido vazio ! VERMELHO. As 2 provas de §V222 por perna: mutação que acende + sonda que ⊥ acende, as 6 RODADAS. `RPGmeister` ⊥ entra na lista de padrões proibidos de §V213 (`HuntersHunted`/`frmHH_`/`HH.\d+`) — ela é do RENAME, & a marca é §V230a: misturar as 2 faz a mensagem de falha mentir sobre a causa|V213,V230,V222,V20,V209
T535|x|os **14** forms de conteúdo — somar **12** ao `top` dos **75** filhos de 1º nível do `<scrollBox>` (`WoD20.1`=13 `.2`=14 `.3`=6 `.4`=6 `.5`=3 `.6`=2 `.7`=5 `.8`=6 `.9`=2 `.10`=4 `.11`=6 `.12`=3 `.13`=3 `.14`=2, MEDIDO). Seletor ESTRUTURAL, ⊥ por recuo (§I34: `WoD20.5` usa 4 espaços & recuo pega 73/75). `<template>` & `themePaper*` ficam de fora sozinhos, ⊥ carregam `top`. + o comentário-régua de `WoD20.1` vai de `y=760` p/ **`y=772`**, senão §V69 acende — ele é o ÚNICO que acende, MEDIDO em ensaio|I34,V231,V69,V40,V225,V209
T536|x|`WoD20th.lfm` — `tabStrip`: os 22 retângulos de `top`=4 p/ **8** & `height` da faixa 38→**46** (=8+30+8, simétrica). `vampStrip` ⊥ é tocada: ⊥ tem chão ∴ ⊥ ∃ vão p/ mostrar, & mexer na `height` dela reabre §B52 (§I33). Os `<label>` de dentro dos botões seguem em `top`=0, são relativos ao botão|I34,V231,I33,B52
T537|x|gate — §V231 NOVA c/ as 3 pernas & zero-guard próprio (< 14 forms lidos ! VERMELHO citando §V209). 2 provas de §V222 POR PERNA, as 6 RODADAS — & a de (c) ! ser a mutação `top`=12 **com** faixa 54, senão ela acende (b) junto & ⊥ prova que (c) existe (≡ a lição da 78ª, onde a 1ª mutação de §V226 acendeu pelo zero-guard em vez da cláusula que ela mirava)|V231,V222,V20,V209
T539|x|renames VISÍVEIS (itens 5·6·9 do pedido 2026-08-22, §I36, §V233): `text="Vampire"`→`Ghoul` no label de `btnTabDisciplines` (`WoD20th.lfm:2827`) · `text="Show Vampire"`→`Show Ghoul` (`WoD20.10.lfm:115`) · `text="Progress"`→`Experience` (`WoD20th.lfm:2859`) · `cboGame` `items` **&** `values` `Hunters Hunted`→`Mortal` (`WoD20.6.lfm:1706`) & `dataLink defaultValue` (`:1708`) & os 2 literais da normalização da RAIZ (`WoD20th.lfm:2990-2991`). `.lang` (os 2 blocos) & mapa `PT` de `WoD20.6`: ENTRAM `Ghoul`=`Carniçal` · `Show Ghoul`=`Mostrar Carniçal` · `Mortal`=`Mortal`; SAEM `Show Vampire` · `Hunters Hunted` · `Progress` (⊥ são mais string visível). FICAM: o item `Vampire` do roster & `["Vampire"]="Vampiro"` (exceção do pedido) · `Psychic Vampirism` & os 2 rituais `Ward Versus Ghouls` (livro) · a proibição de `Hunters Hunted - Mortal` em §V213-leftover (título da ficha VELHA, alvo outro). ⚠ GATE — **5** sítios c/ o literal HARDCODED, ! virarem na MESMA rodada senão o gate reprova código certo (§B7): `$wantGames` (l.506) · needle de `defaultValue` (l.1167) · needle de `setField("game", …)` (l.1168) · §V172 `'Vampire'` (l.3884) **&** o guarda `kind == "Vampire"` (l.3886) · §V213-roster (l.4467-4472) REESCRITO p/ needles de `Mortal`, ⊥ contagem — `Mortal` já aparece 2× em `WoD20.6` dentro de nome de poder ∴ contar palavra solta passaria VERDE c/ o roster vazio|I36,V233,V109,V110,V172,V10,V22,V28,V24,V213,V209,V20,V222
T540|x|barra de abas SEM BURACO (itens 3·5·9, §I36, §V232): `TAB_W` + `STRIP_X0`=16 + `STRIP_GAP`=4 entram no `<script>` da RAIZ entre marcadores (≡ `TAB_LIST`, §V211) · `btnTabDisciplines`/`tabOnDisciplines` `width` 86→**92** & `btnTabProgress`/`tabOnProgress` 99→**112**, as 2 pela fórmula de §V228 (`Carniçal` 8 char · `Experiência` 11 char) · os 11 `left` recalculados p/ 16·119·202·285·381·471·580·689·805·908·1037 (fim **1149**) · `applyTabVisibility` escreve `left` do par no MESMO laço que escreve `visible`, cursor anda só em aba visível. ⚠ ⊥ LER `.width` em runtime — escrita de geometria é provada (`WoD20.9`), leitura ⊥ é (§V232). `vampStrip` ⊥ é tocada. DEPENDE de §T539 (os 2 rótulos novos são o que decide as 2 larguras) ∴ ⊥ paralelizar c/ ela: mesmo arquivo, mesmos 22 retângulos|I36,V232,V228,V231,V49,V217,V20,V222
T541|x|aba Storyteller reorganizada (itens 1·2·12, §I35): a 3ª caixa de `Notes` SAI (`WoD20.10.lfm:84-88`) & `stNotes3` entra em ÓRFÃOS de §I3 · `STORYTELLER SETTINGS` de `(0,602,520,330)` p/ **`(820,12,540,367)`** · ordem NOVA dos `checkBox` — `Show Numina` 40 & `Show Ghoul` 70 PRIMEIRO, depois `Can Buy Backgrounds With Experience` 100 · `Free dots` 130 · Especialização 160 & 190 · `Edit Clan Disciplines` 220, `width` 490→**510** · a linha `Save Initial Character` vai p/ o FIM (label 15/300 · `btnSaveBaseline` 225/297) & `dynBaselineState` desce p/ linha própria (15/332, `width` 170→**510**: a frase PT pede ~221px & §V16 nunca viu isso, ela mede `text=` autorado & `dyn*` ⊥ tem). Altura **367** = última linha + 15 (§V171), ⊥ 580 — 580 deixaria 213px de rabo preto, a mesma estranheza dos itens 7 & 10 do MESMO pedido. §V238 NOVA guarda as 2 pontas da ordem. ⚠ MEDIR a altura nova da aba (era 932, a mais alta da ficha) & dizer qual aba passa a decidir a barra vertical — ⊥ supor (§V209)|I35,V238,V144,V16,V171,V231,V40,V2
T542|x|`Health Levels` MUDA DE CASA (itens 11·13, §I35, §V234) — 1 rodada SÓ, as 2 pontas juntas: SAEM `cmbHealthLevels` (`WoD20.1:534`) & `cmbHealthLevels3` (`WoD20.3:261`) & o `<dataLink field="healthLevels" defaultValue="7">` (`WoD20.1:535`) vai junto p/ `WoD20.10`; ENTRA em `STORYTELLER SETTINGS` a linha `Health Levels` (label 15/258 `width`=170 · `comboBox` 190/255 `58×25`, mesmos 10 itens & mesmo default). `healthLevels` SAI de ESPELHOS (§I3) & da lista `$mirrors` do gate (l.260). Título `HEALTH`: `width` 215→**290** (`WoD20.1:533`) & 205→**280** (`WoD20.3:260`), `left`=0 ∴ centraliza na caixa (§V27). ⚠ ⊥ DÁ p/ partir em 2 §T: tirar os combos antes deixa §V8 vermelha (dataLink sem dono) & pôr o novo antes deixa 3 donos & §V1 vermelha. As 2 `<dataLink fields="{'healthLevels','language'}">` de `WoD20.1`/`WoD20.3` FICAM — observam (§B.3) & são quem redesenha a trilha de cada aba (§V123). DEPENDE de §T541 (a caixa ! estar no lugar novo)|I35,V234,V1,V8,V36,V27,V123,V49,V16
T543|x|Main troca `HEALTH` ⇄ `SPECIALTIES` de lugar (item 14): `dynHealth_box` `left` 615→**920** (`WoD20.1:531`) & a caixa `SPECIALTIES` 920→**615** (`:565`), MAIS NADA — as 2 têm `width`=290 & o mesmo `top`=472 ∴ §V69 ⊥ se mexe (`SPECIALTIES` segue fechando em 772 & `HEALTH` segue pendurando os 6px declarados). REAPONTAR na MESMA rodada, senão o gate reprova código certo: §V168 (mapa da grade — `SKILLS`↔`SPECIALTIES` & `KNOWLEDGES`↔`HEALTH`, gate l.3740) · §V146 (o texto dela diz "herda as 2 bordas x de `KNOWLEDGES`" → `SKILLS`) · os 2 mapas em COMENTÁRIO de `WoD20.1` (l.43-44 & l.460-461), que o gate lê de propósito p/ ⊥ apodrecerem (≡ a régua de §V69)|V168,V146,V69,V40,V26,V209,V222
T544|x|aba Settings — caixas mais largas & créditos c/ header (itens 7·8, §I38): as 2 `<layout>` `width` 330→**380** (`WoD20.6.lfm:1682` & `:1756`) & os 3 `comboBox` 220→**270** (`left`=95 intacto ∴ margem direita segue 15) & os `label` da caixa de créditos 330→**380** · header `Credits` NOVO (`left`=0 `top`=14 `width`=380, centralizado §V27 — `wod.Credits`/`["Credits"]` JÁ ∃ nos 3 lugares) & as 3 linhas descem p/ 54·88·124 c/ `height` 110→**158** ∴ vão de cima **14** ≡ vão de baixo **14** (eram 10 & 5). ⊥ ∃ §V nova & é de propósito: §V10/§V28 já cobrem a string, §V16 a largura, §V27 o centro & §V230 o TEXTO do crédito — o vão é número SOLTO, ⊥ contrato entre arquivos (o que faz uma relação merecer invariante é §V231c, & aqui ⊥ ∃ 2ª ponta p/ divergir)|I38,V27,V16,V10,V28,V230,V40
T545|x|log de XP vazio parece log (item 10, §I39, §V235): `XP_MSG_W` & as 3 escritas de `form.dynXpType.width` MORREM (`WoD20.9.lfm:24` `:62` `:94` `:96`) ∴ as 4 colunas ficam na largura autorada em ∀ estado; a frase de estado vai p/ `dynXpEmpty` (`<label>` NOVO 15/50 `442×20` centralizado `opacity="0.40"`, autorado `visible="false"`), ligado nos 2 caminhos vazios & desligado quando ∃ linha; `xpLogBox.height` passa a ser escrita nos **3** caminhos & nos 2 vazios volta ao piso `XP_LOG_MIN_H + XP_LOG_PAD` — hoje o caminho `rows == nil` ⊥ escreve altura & tirar o baseline de uma ficha c/ 40 linhas deixa a caixa gigante & vazia. `dynXpType` deixa de precisar ser declarado POR ÚLTIMO (o comentário sai junto). §V33 & §V98 seguem satisfeitas — a frase muda de dono, ⊥ some. ⚠ GATE: o needle de §V143 (l.3191) lê `form.dynXpType.width` DENTRO de `renderXPLedger` ∴ REAPONTAR na mesma rodada (§B7)|I39,V235,V33,V98,V143,V84,V209,V222
T546|x|**REESCRITA 83ª rodada** (decisão user): §R97 estava RESPONDIDA no próprio sheet — `CLANS` (§I17, congelada por §T430/§T434) já tinha os 60 clãs, & com forma MELHOR (`fixed`/`choice`/`open`). ∴ `CLAN_DISC` SAI do form raiz inteiro & `research/clan_disc.tsv` FICA (é a única fonte por-linha que ∃ p/ esses 60, & `CLANS` ⊥ registra livro). §V174 perde a perna "⊥ ∃ leitor" — dado que o user mandou acordar ⊥ é dormente — & a perna "`CLANS` é declarada no form raiz" FICA. §V236 passa a medir `CLANS`: 60 chaves · ∀ nome ∈ `PICKER_LIST["disc"]` · `fixed` ≤ `CLAN_DISC_ROWS` · entrada c/ `choice` \| `open` é LIBERADA por construção, ⊥ por lista vazia|R97,I37,V236,V174,V211,V20,V222
T547|x|**REESCRITA 83ª rodada**: `renderClanDisc(from)` lê `CLANS[sheet.clanFamily]`, ⊥ `CLAN_DISC`. Escreve `clanDisc_1..n` = `entry.fixed` (0..4 nomes) &, no slot cujo NOME MUDOU, zera as 5 bolinhas na MESMA escrita (§V237 ⊥ muda). LIBERA os 4 slots — `enabled` mesmo c/ `stEditClanDisc` OFF — quando a entrada tem `choice` ou `open`, que é mais fino que a lista vazia da 82ª: `Ventrue Antitribu` passa a PREENCHER `Dominate` & `Fortitude` & abrir só o 3º, em vez de largar os 4. Segue no form RAIZ (§B26 §V95) & segue ⊥ escrevendo `clan` (§V173 intacta)|I37,V236,V237,V173,V175,V95,V162,V222
T549|x|eixo X simétrico (item 1 do pedido 2026-08-22, §I40, §V239): as **16** caixas c/ `left` ≠ `right` fecham nos 2 lados — `ATTRIBUTES` **-40/10** (a coluna começa FORA da caixa: é o pior caso & o único que hoje DESENHA fora) · `HEALTH` Main 20/25 & Combat 20/15 · `EXPERIENCE` de `WoD20.9` 15/17 · `BLOOD POOL` 111/112 · os **6** de 12/13 (`CLAN DISCIPLINES` `DISCIPLINES` @ `WoD20.12` · `MAIN PATHS` `SECONDARY PATHS` @ `WoD20.13` · `RITUALS` @ `WoD20.14` · `HEDGE MAGIC PATHS` `PSYCHIC PHENOMENA` `TRUE FAITH` @ `WoD20.7` — 9 no total) · os 3 cabeçalhos sem título (`WoD20.1` 5/38 · `WoD20.4` 5/15 · `WoD20.6` 0/15). Fechar pelo lado que ⊥ move conteúdo quando der; onde os 2 lados discordarem por 1px (12/13), o par vai p/ **12/12** & a caixa ⊥ muda de largura. ⚠ `ATTRIBUTES` é o único que pede DECISÃO: -40 quer dizer que a linha de atributo é mais larga que a caixa ∴ ou a caixa cresce ou a linha encolhe — MEDIR antes, & §V16 manda na resposta|I40,V239,V27,V16,V40,V20,V222
T550|x|eixo Y a **10/10** c/ o título contando (item 1 do pedido 2026-08-22, §I40, §V240) — 1 §T só, & ⊥ dá p/ partir: o check é da FICHA inteira ∴ qualquer corte deixa o gate vermelho no meio. Mecânica: título 4→**10**, ∀ filho da caixa desce **+6** (58 das 66), `height` = borda inferior + 10. RÉGUAS re-derivadas na MESMA rodada, senão o gate reprova código certo (§B7): §V69 (o `y` da Main muda & o mapa mora num COMENTÁRIO que o gate LÊ) · §V171 (última linha + 15 → +10) · §V49 (`HEALTH_BOX_PAD`) · §V146 (`SPECIALTIES` 300→306) · §V225 (as 3 sub-abas: `RITUALS` −9 & `DESCRIPTION` +6 ∴ fundo a **693** dentro dos 700) · §V168 (a fileira de baixo da Main tem 3 deltas DIFERENTES — `HUMANITY` −4 · `HEALTH` +1 · `SPECIALTIES` +6 — ∴ re-derivar os `top`, ⊥ deslocar em bloco). CUSTO medido: 37 caixas **+1px**, 8 +6, 7 **−9**, extremos `WoD20.6` opções −20 · `WILLPOWER` −14 · `COMBAT TRAITS` +11. ⚠ MEDIR a altura final de ∀ aba & dizer qual passa a decidir a barra vertical — ⊥ supor (§V209)|I40,V240,V69,V171,V49,V146,V225,V168,V40,V231,V20,V222
T551|x|dropdown travado deixa o texto legível (item 2 do pedido 2026-08-22, §I41, §V241): `opacity` SAI dos 4 sítios — XML `cboClan` (`WoD20.1`) & `cboGame` (`WoD20.6`); Lua `renderClanDiscLock` (`c.opacity = open and 1 or 0.40`) & `renderSpecialities` (`c.opacity = locked and 0.40 or 1`), as 2 no form RAIZ. `enabled` FICA em todos, é ele que trava. GATE: §V111 ganha `comboBox` na exceção NOMINAL (≡ `$DESC_BRIGHT`, ⊥ afrouxar a perna (a)) · §V162 passa a medir só quem ainda escreve o PAR (o combo agora escreve 1 lado só) · §V175 continua exigindo que os 4 slots travem, mas pelo `enabled` sozinho — os 3 sítios do gate mudam na mesma rodada. ⚠ o literal `0.40` de §V179 & o `$DIM` de §V111 ⊥ mudam de valor: eles seguem valendo p/ `edit`/`textEditor`/`imageCheckBox`, & é só o `comboBox` que sai do alcance|I41,V241,V111,V162,V175,V179,V109,V173,V20,V222
T553|x|`V242` no gate — 0 prosa solta dentro de Lua (§B54): varre o CDATA de `<script>` & o corpo de `<event>` dos 15 `.lfm` & acende em linha que ⊥ começa c/ `--`, ⊥ tem NENHUM de `= ( ) { } [ ] ; :` & lê como frase (4+ palavras). Heurística ESTREITA de propósito — as 3 condições JUNTAS, medido **0** falso positivo nos 15 arquivos — porque check de prosa frouxo acende em código legítimo & vira ruído que se aprende a ignorar. ⊥ substitui `rdk -l` antes de marcar §T (HANDOFF §8): pega ESTE erro, ⊥ sintaxe em geral|V242,V20,V222,V209
T554|x|caixa `STORYTELLER SETTINGS` — as 3 mudanças de 1 vez (itens 1·2·4 do pedido 2026-08-22, §I42): `btnSaveBaseline` `left` 225→**190** (= `cmbHealthLevels`) · o `<label name="dynBaselineState">` (`WoD20.10.lfm:164`) SAI & c/ ele as 2 escritas de `.text` (`:53-57`) — `renderBaselineState` FICA, ela ainda escreve o par `enabled`+`opacity` · `chkEditSpentXP` NOVO (`field="stEditSpentXP"`, `text="Edit Spent XP"`, `top`=**256**) & `Health Levels` (294/291) & `Save` (336/333) descem 30 · `height` 368→**369**. 1 rodada SÓ: as 3 mexem no MESMO eixo Y da MESMA caixa & partir isso é mover o mesmo widget 2× (§V240 mede o vão de baixo)|I42,V243,V238,V240,V82,V112
T555|x|`localization.lang` — 2 chaves novas nos 3 sítios que §V10 §V22 §V28 exigem ([en] & [pt] do `.lang` + o mapa `PT` de `WoD20.6`): `wod.Edit Spent XP` = `Editar XP Gasto` & `wod.MANUAL` = `MANUAL`. A 2ª é identidade DE PROPÓSITO — a palavra é a mesma nas 2 línguas & a chave ∃ só p/ o texto ⊥ escapar dos 3 checks (≡ o que §I10 já faz c/ as categorias do log)|I42,I44,V10,V22,V28
T556|x|esmaecimento de TEXTO 0.40→**0.75** (item 3 do pedido 2026-08-22, §I43, §V244): 7 literais no XML de `WoD20.9` (`edtTotalXP` `edtSpentXP` `dynXpType` `dynXpTrait` `dynXpLevel` `dynXpCost` `dynXpEmpty`) + a escrita Lua de `WoD20.10.lfm:51`. As 26 ocorrências de ARTE (as 20 de `WoD20.3`, as 5 `ro_mainPath_*` de `WoD20.13`, a `<image>` de dot1) ⊥ são tocadas — bolinha ⊥ é texto|I43,V244,V111,V179
T557|x|gate — §V244 NOVA na MESMA rodada que §T556, senão o gate fica vermelho de propósito: `$DIM` vira o PAR `$DIM_TEXT`=`0.75` & `$DIM_ART`=`0.40`, & os 4 sítios que carregam o literal por fonte (§V112 §V162 §V175 §V179) dizem QUAL dos 2 medem. 2 provas de §V222: `0.40` num `dyn*` do log ! VERMELHO · `0.75` num `ro_*` de `WoD20.3` ! VERMELHO|V244,V111,V222,V20
T558|x|`Spent` editável & o campo `xpManual` (item 4 do pedido 2026-08-22, §I44): `edtSpentXP` ganha `onUserChange` & `renderXPBoxes` escreve `readOnly`+`opacity` na MESMA linha a partir de `stEditSpentXP` · `xpManual` entra em LUA-OWNED de §I3 · a linha `MANUAL` entra em `xpLedgerRows` por ÚLTIMO, depois de `Specialty`, ⟺ `xpManual ≠ 0` · digitar X grava `X − Σ(log sem MANUAL)` & `Spent` < 0 RECUSA c/ pop-up & re-render, copiando o caminho de `Current` (§V131)|I44,I3,I11,V245,V246,V131,V83
T559|x|geometria da aba Experience (item 4 do pedido 2026-08-22, §I44, §V247): coluna `Level` 54→**80** · `Cost` `left` 412→**438** · `xpLogBox` & a caixa `EXPERIENCE` `width` 475→**501** & `dynXpEmpty` junto · as 3 colunas de `EXPERIENCE` a 16·**180**·**344** (label) & **41**·**205**·**369** (edit), margem 16/16. DEPOIS de §T558: a palavra ! ∃ na tela antes de a coluna ser medida por ela|I44,V247,V240,V26,V16
T560|x|`WoD20.14` 25→**20** linhas (item 6 do pedido 2026-08-22, §I46): as 5 `<RitualRow>` de `ritual_21` a `ritual_25` SAEM · `RITUAL_ROWS` 25→**20** no `<script>` da raiz · as 3 listas de `dataLink` do arquivo encolhem p/ os 20 (§V123) · caixa `RITUALS` `height` 666→**541** & a `DESCRIPTION` ao lado fecha junto (§V193) · os 5 campos entram em ÓRFÃOS de §I3. ANTES de §T561: o corte é quem define em que índice a linha livre nasce|I46,V251,V204,V193,V123,I3
T561|x|linhas de TEXTO LIVRE nas 3 áreas da aba Ghoul (itens 5·7·8 do pedido 2026-08-22, §I45): templates `DiscFreeRow` `SecPathFreeRow` `RitualFreeRow` (`<edit>` dono do campo, MESMO x de entrada & MESMA coluna de bolinha do irmão de picker) nas linhas `disc_13..15` `secPath_13..15` `ritual_16..20` · constantes `DISC_FREE_ROWS`=3 `SEC_PATH_FREE_ROWS`=3 `RITUAL_FREE_ROWS`=5 · `renderVampPickers` (`WoD20.6`) & `pickRefusal` (`WoD20th`) param em total−livres · os 11 campos de descrição entram em §I3|I45,V248,V204,V211,V186,V26,I3
T562|x|`DESCRIPTION` editável p/ o mestre (itens 5·7·8 do pedido 2026-08-22, §I45): os 3 renderers ganham o ramo livre — texto do campo da linha marcada + `readOnly` escrito no MESMO passo + flag de reentrância — & o `onChange` do `textEditor` grava no campo da linha marcada LIDA na hora. Linha livre vazia: mestre vê caixa editável, jogador vê frase de estado (§V33). As 3 áreas na MESMA rodada: é 1 desenho copiado 3× & partir garante 3 dialetos (§B12 §B13)|I45,V249,V250,V29,V33,V123,I8a
T563|x|ordenação alfabética & destaque de 1s (item 9 do pedido 2026-08-22, §I47): `sortRituals` roda no `dataLink` de `WoD20.14` DEPOIS de `guardPick`, ordena as 15 de picker por (nível do prefixo, nome) sobre o valor CANÔNICO, fecha buraco & re-aponta `ritualSel` p/ o slot novo do valor marcado · `<rectangle>` de destaque no template (`visible="false"`, `hitTest="false"`, último filho) c/ a cor nova nas 4 paletas de §I5 · `setTimeout(…, 1000)` apaga. Acende só quando a escrita deixa a linha ⊥ vazia|I47,V252,V253,V53,V122,V184,R99
T564|x|gate — as 10 §V novas da 85ª rodada (§V243 §V245 §V246 §V247 §V248 §V249 §V250 §V251 §V252 §V253), ∀ uma c/ 2 provas de mutação (§V222) & zero-guard próprio: check que leu 0 arquivo \| 0 linha ! VERMELHO citando §V209, ⊥ verde calado (§B7 §V20). §V244 ⊥ entra aqui — ela nasce em §T557, junto do código que ela mede|V222,V20,V209,V243,V245,V246,V247,V248,V249,V250,V251,V252,V253
T566|x|a RÉGUA desce p/ **1235** — **4 arquivos, 1 movimento** (§I48): partir esta §T deixa §V225(b) VERMELHA no meio, ela mede os 4 fechando no mesmo x. `WoD20.11` `BLOOD POOL` `width` 500→**350** (`left` 885 intacto) + rótulo 500→**350** + as 20 bolinhas −75 em `x` (`36 64 92 120 148 176 204 232 260 289`, 2 fileiras, `top` 48/83 intacto) · `WoD20.14` coluna `RITUALS` 510→**480** (combo & `edit` 460→**430**, `hl$(field)` & as 20 `<layout>` de linha 486→**456**, rótulo junto) & a `DESCRIPTION` dela `left` 520→**490** & `width` 865→**745** (`edtRitualDesc` 845→**725**) · `WoD20.12` `DESCRIPTION` 1025→**875** (`edtDiscDesc` 1005→**855**) · `WoD20.13` 850→**700** (`edtPathDesc` 830→**680**). ∀ rótulo de título acompanha a caixa (§V27) & ∀ editor mantém 10/10 (§V239). Os **430** = `ceil(62×6,5)+24` = 427 arredondado p/ múltiplo de 5 (§V196, ≡ §T474), c/ 62 char = o PT de `5. Warding Circles versus Spirits, Ghosts, and Demons`, MEDIDO nos 284 itens de `PICKER_LIST["ritual"]`|I48,V225,V239,V27,V196,V26,V219,V193,V240
T567|x|`WoD20.11.lfm` — `vampStrip` & `tabDisc` `tabPaths` `tabRituals` `width` 1395→**1245** (régua + 10, §I48 §R94) ∴ a aba Ghoul deixa de exigir 1395px de janela p/ conteúdo que acaba em 1235. ⊥ toca `top`, `height`, `left` nem os 3 botões (fecham em 596): 4px de altura ali reabrem §B52. §V225(a) passa a medir contra **1245** & segue verde c/ 10px de folga nos 2 eixos|I48,R94,V225,B52,V221
T568|x|gate — §V196 lê a lista ONDE ELA ESTÁ (§B56): trocar `//comboBox[@items][@width]` por travessia de ∀ `comboBox` c/ `width`, resolvendo a lista por `ListOf` (JÁ escrita, `verify-hunters-hunted.ps1:97`, c/ **0** chamadas hoje) ∴ os 5 pickers de vampiro entram na conta pela 1ª vez · o eixo de época segue ESCOPADO a `WoD20.12`/`13`/`14` (§V197, §B44) · zero-guard de CONTEÚDO & NOMINAL: < **17** combo medidos **ou** nenhum dos 5 de vampiro entre eles ! VERMELHO citando §V209 (o count-guard de hoje fica verde pelas 3 listas inline de `WoD20.6` — é assim que o buraco durou 18 rodadas) · 2 provas de §V222: mutação = combo de ritual p/ **425** ! VERMELHO nomeando o item PT de 62 char · sonda = mudar a ALTURA da linha ! VERDE. ⚠ RODAR o gate ANTES de §T566: se ele já acender em `mainPath` (345 p/ 343 medidos), o vermelho é ANTERIOR a esta rodada & ⊥ é regressão dela|B56,V196,V209,V222,V20,V197,I27
T570|x|item 1 INTEIRO — XML + `.lang` + Lua + os 3 sítios de gate numa §T só (§I49): partir deixa a ficha c/ `specCost()` lendo campo morto (preço 0 = recusa em tudo) & o gate vermelho por §V155. **`WoD20.10`**: SAEM `chkSpec3XP` `chkSpec4XP` & os 2 `dataLink` de exclusão; ENTRA `<label text="Specialty Cost">` (15/200) + `cmbSpecCost` (190/**150**, `items` `{'0','1','2','3','4','5','Purchase Forbidden'}` & `values` `{'0','1','2','3','4','5','forbidden'}`) + `<dataLink field="stSpecCost" defaultValue="3"/>`, em `top` **166** · caixa `540`→**405** & `checkBox` `510`→**375** · ∀ linha abaixo −30 (`196` `226` `264` `306`, botão `303`) & `height` 369→**339** (§V240). **RAIZ**: `specCost()` lê `stSpecCost` (`nil`\|`""`→**3**, `forbidden`→recusa, número→preço c/ `0` LEGAL) & `xpClick` troca `specCost() == 0` por igualdade c/ o sentinela (§V254b). **`.lang`** nos 2 blocos: SAEM `Allow buy Specialization for 3\|4 experience points`, ENTRAM `Specialty Cost`=`Custo por Especialização` & `Purchase Forbidden`=`Compra Proibida`. ⚠ GATE junto senão ele reprova código certo (§B7): `$wantFlags` de §V89 (`:2514`) perde os 2 & ganha `stSpecCost` · o bloco de §V155 (`:3667-3678`) SAI inteiro · §V154 (`:3644-3663`) reescrito p/ o campo novo|I49,V254,V154,V155,V238,V239,V240,V16,V196,V27,V12,V10,V22,V28,V89,V20,V2,I3
T571|x|item 2 — as **3** últimas `SPECIALTIES` digitadas (§I50): template `SpecialityFreeRow` (`<edit name="edtSpeciality$(num)">` de 150 no x do combo, `specialityName_$(num)` & a bolinha INTACTOS, §V26) nas linhas `speciality_7` `_8` `_9` de `WoD20.1` · constante `SPECIALITY_FREE_ROWS`=**3** no form RAIZ · `renderSpecialities` & a busca de slot de `grantSpeciality` param em `SPECIALITY_ROWS − SPECIALITY_FREE_ROWS` (§V255d — o presente do traço ⊥ pode cair em linha sem combo p/ travar) · `declareTrait` segue cobrindo as **9** (linha digitada custa o mesmo, §I9)|I50,V255,V204,V211,V26,V148,V151,V160,V1
T572|x|item 3 — os **5** últimos `BACKGROUNDS` digitados (§I50): template `OpenAbilityFreeRow` (`<edit name="edt$(field)">` de 170 no x do combo, as 5 bolinhas & os `onClick` de `xpClick` INTACTOS) nas linhas `background_16`…`_20` de `WoD20.2` · constante `BACKGROUND_FREE_ROWS`=**5** no form RAIZ · `BACKGROUND_ROWS` segue **20** & os 2 laços de XP seguem lendo ELA (§V145) ∴ ⊥ ∃ ponto grátis nem invisível no log. ⊥ ∃ Lua p/ ajustar: `pickerItems` só toca `cbo*` & nada mais lê essas linhas por nome (MEDIDO)|I50,V255,V145,V204,V211,V26,V1
T573|x|item 4 — fundo ESCURO atrás dos 2 valores travados de XP (§V256, §R103): `WoD20.9` ganha 1 `<rectangle>` ANTES de `edtTotalXP` & outro antes de `edtSpentXP` (mesmo `left`/`top`/`width`/`height` do `edit`, `hitTest="false"`, cantos ≡ os da caixa) & os 2 `edit` ganham `transparent="true"` · 1 chave de cor NOVA nas **4** paletas de §I5 (§V53), cada uma mais escura que o `black` daquela paleta · `edtCurrentXP` ⊥ é tocado (§V256 escopo). ⚠ o mecanismo é `?` até a tela (§R103): se o `edit` seguir opaco, o fix ⊥ é número, é outro caminho — & aí a §T volta p/ o spec, ⊥ p/ tentativa|V256,V53,R103,I5,I44,V111
T574|x|item 5 — `Caitiff` (§C, §V236): entra em `PICKER_LIST["clan"]` (ao lado de `Panders`) & em `CLANS` como `["Caitiff"] = { open = 3 }`, byte a byte a entrada de `Panders` · `.lang` nos 2 blocos (`Caitiff`=`Caitiff`, ≡ `Panders`) · ⚠ os **3** literais `60` do check de §V236 (`:5365` `:5384` `:5386`) viram **61** na MESMA rodada, o texto do Pass incluído. ⊥ toca `Panders`: os 2 nomes coexistem no roster (Sabbat & não-Sabbat), ≡ o par `Assamite Sorcerers`/`Assamite Viziers`|V236,I17,I37,V10,V22,V28,V209
T575|x|gate — §V254 §V255 §V256 NOVAS, ∀ uma c/ as 2 provas de §V222 (mutação que acende + sonda que ⊥ acende) & zero-guard próprio citando §V209: §V254 c/ as 4 mutações da linha dela · §V255 c/ zero-guard de TEMPLATE (0 livre lido em qualquer das 2 áreas ! VERMELHO) & mutação = mover 1 linha livre p/ o meio da caixa · §V256 c/ mutação = tirar `transparent` de 1 dos 2 `edit` (! VERMELHO, é o modo de falha CALADO que ela existe p/ pegar) & sonda = mudar a COR autorada p/ outra que tenha chave nas 4 (! VERDE, quem manda no valor é `applyTheme`)|V222,V20,V209,V254,V255,V256
T577|x|a caixa `STORYTELLER SETTINGS` re-ordenada **&** o antecedente virando PREÇO, numa §T só (§I51): partir deixa o gate vermelho no meio — o censo de §V89 nomeia o campo & a caixa perde 1 `checkBox` no mesmo passo. **`WoD20.10`**: `chkBackgroundsXP` SAI & entram `<label text="Background Cost">` (15/170) + `cmbBgCost` (190/**150**, `items`=`values`=`{'0','1','2','3','4','5'}`) + `<dataLink field="stBackgroundCost" defaultValue="0"/>` · `cmbHealthLevels` 58→**150** · `cmbSpecCost` vai p/ **234** & `Health Levels` sobe p/ **204** · os 5 `checkBox` re-derivados (46 · 76 · **106** · **136** · **166**) · `Save` INTACTO em 306/303 & `height` **339** ⊥ muda (§V240). **RAIZ**: `ctx.backgroundsXP` → `ctx.backgroundCost` = `tonumber(sheet.stBackgroundCost) or 0` & o ramo `Background` de `xpCost` vira `from × v` c/ `from=0` → `v` (o `if not ctx.backgroundsXP then return 0; end;` SOME). **`.lang`** nos 2 blocos & mapa `PT`: ENTRA `Background Cost`=`Custo por Antecedente`, SAI `Can Buy Backgrounds With Experience`. ⚠ GATE junto (§B7): `$wantFlags` de §V89 troca o campo · §V238 mede `< 6` `checkBox` & passam a ser **5** · §V243 segue medindo x DISTINTO (agora 3 entradas) — as 3 são literais que reprovariam código certo|I51,I9,V89,V238,V243,V240,V239,V16,V196,V26,V27,V10,V22,V28,V2,I3,V83
T578|x|`cmb*` traduz os próprios itens (§I52, conserto de §B57): `values` do `cmbSpecCost` `forbidden`→**`Purchase Forbidden`** & `SPEC_FORBIDDEN` da RAIZ junto (o mesmo literal nos 2 lados ou a porta nunca fecha, §V12) · `["Purchase Forbidden"] = "Compra Proibida"` entra no mapa `PT` de `WoD20.6` · `applyLanguage` ganha o ramo `cmb*` — `items[i] = PT[values[i]] or values[i]`, `values` ⊥ tocado (§V22) — & ele é POR PREFIXO, ⊥ por nome (§V257a). As 2 chaves `.lang` FICAM: eixo do host. DEPOIS de §T577: o 3º combo de valor ! existir p/ o ramo ser medido em 3|I52,V257,V254,V12,V22,V24,V28,V208,B57
T579|x|gate — §V257 NOVA c/ as 4 pernas & as 2 provas de §V222 (as 5 RODADAS: 4 mutações + 1 sonda) & zero-guard próprio (0 `cmb*` lido ! VERMELHO citando §V209) · §V254 reapontada p/ o value novo (`Purchase Forbidden`) & p/ a perna (d) de 2 eixos — `.lang` **&** mapa `PT` · §V89 & §V238 conferidas contra a caixa nova (5 flags, 5 `checkBox`)|V257,V254,V222,V20,V209,V89,V238,B57
T581|x|itens 1 & 2 em `WoD20.4` (§I53, §I54) — a grade de descritores `width` 960→**1040** & colunas `5\|320\|645`→**`15\|365\|715`** (largura 310 INTACTA ∴ fecha em 1025 & o vão bate 15/15, §V239) & as 3 colunas ROTACIONADAS: col 1 `altura`/`peso`/`nacionalidade` · col 2 `olhos`/`cabelo`/`etinicidade` · col 3 `sexo`/`idade`/`idadeAparente` — só o par `field=`/`nome=` das 9 chamadas de `<Descricao>` muda, template & campos INTACTOS (§V2) · `Languages` (`aparencia`) `Goals` (`idiomas`) `Prelude` (`background`) recuam `left` 10→**15** & perdem 10 de `width` (380→**370** · 380→**370** · 610→**600**) · `Moral Flaws` & `Fears` já em 15/15 ∴ ⊥ são tocadas|I53,I54,V258,V239,V240,V37,V25,V2
T582|x|item 2 em `WoD20.1` `WoD20.5` `WoD20.10` (§I54) — grade do cabeçalho de `WoD20.1` colunas `5\|320\|698`→**`15\|336\|688`**, larguras 297/327/327 INTACTAS ∴ fecha em 1015 c/ vão 15/15 (os gutters ficam 24 & 25: 49 é ímpar & 1px de diferença ⊥ tem leitor) · `SPECIALTIES` linhas `left` 10→**15** & `width` 270→**260**, & os 2 templates REAJUSTADOS p/ span 260 — `specialityName_$(num)` 85→**75** & bolinha `left` 245→**235**, `cboSpeciality$(num)`/`edtSpeciality$(num)` em **150** INTACTO (§V196 pede 148 ∴ ⊥ ∃ 10px nele) · `anotacoes1`…`_3` & `stNotes1` `_2` `left` 10→**15** & `width` 380→**370** (5 `textEditor`, 2 arquivos). ⚠ §V146 mede `rowSpan ≡ largura da linha`: mexer na caixa sem reajustar o template é o modo de falha que ela nomeia|I54,V258,V146,V196,V26,V239,V240,V27
T583|x|item 3 em `WoD20.11` (§I55) — `dominator` & `cboClanFamily` `width` 320→**192** · `cmbDominatorGen` & `lblMaxDisc` 188→**56** · os 4 x da grade `12\|112\|500\|675`→**`15\|115\|374\|549`** (§V224: 4 colunas × 2 controles, larguras de rótulo 95 & 165 INTACTAS) · `DOMINATOR` `width` 875→**620** · `BLOOD POOL` `left` 885→**630**, `width` 350 & as 20 bolinhas INTACTAS · `vampStrip` & `tabDisc`/`tabPaths`/`tabRituals` `width` 1245→**990**. ALTURA de NADA muda (§V221, & 4px ali reabrem §B52). ⚠ `cboClanFamily` fica c/ **12px** sobre o piso de §V196 (180): é a entrada mais justa da ficha & §T510 pode fechá-la|I55,V258,V259,V224,V239,V196,V225,V221,V16
T584|x|itens 3 & 2 nas 3 sub-abas (§I55, §I54) — as 3 `DESCRIPTION` fecham em **980** c/ `left` INTACTO: `WoD20.12` 875→**620** (`edtDiscDesc` 855→**600**) · `WoD20.13` 700→**445** (`edtPathDesc` 680→**425**) · `WoD20.14` 745→**490** (`edtRitualDesc` 725→**470**), rótulo acompanhando a largura nas 3 (§V27) & os 10px dos 2 lados mantidos · `WoD20.14` item 2: x de ENTRADA 25→**24** em `RitualRow` **&** `RitualFreeRow` (os 2 juntos ou §V26 acende) ∴ o `edit` da linha livre fecha em 466 & sobram 14 do canto, & a `width` **430** ⊥ é gasta (§V196 piso 427)|I55,I54,V258,V225,V27,V26,V196
T585|x|gate — §V258 & §V259 NOVAS, ∀ uma c/ as 2 provas de §V222 (mutação que acende + sonda que ⊥ acende): §V258 c/ zero-guard de CENSO (< **44** campos opacos medidos ! VERMELHO citando §V209), `r` LIDO do `xradius` (⊥ literal 14) & a válvula `transparent` declarada em (a) · §V259 c/ a mutação `left="885"`. ⚠ §V225 §V239 §V240 §V146 §V196 §V37 CONFERIDAS contra os números novos ANTES de marcar: as 6 são paramétricas & ⊥ devem pedir 1 caractere — se pedirem, quem está errado é o número novo, ⊥ o check (§B7)|V258,V259,V222,V20,V209,V225,V239,V240,V146,V196,V37
T587|x|`WoD20th.lfm` — `xpOrder` (§I56): carimbo appendado em `xpClick` COLADO na linha do `xpFree` & `gsub` na venda · `xpSetSpent` re-appenda `xpManual#0` ∴ ajuste digitado sobe · 4º param `clickOrder` em `xpLedgerRows` (≡ `clickFree`, §V136) · ordenação DESC ESTÁVEL (índice decorado) no FIM de `xpLedgerRows`, antes do `return` · §I3 ganha `xpOrder` em LUA-OWNED. ⚠ `xpSum` & `renderXPLedger` ⊥ são tocados: ordem ⊥ é soma (§V100)|I56,I3,V138,V136,V125,V100,V260
T588|x|`WoD20.9.lfm` — §I57: `xpLogHeight` & `XP_LOG_PAD` MORREM & c/ eles as 5 escritas de altura · `<scrollBox>` NOVO dentro de `xpLogBox` c/ os 4 `dynXp*` como FILHOS (altura das colunas segue por Lua, piso `XP_LOG_MIN_H`) · título, os 4 rótulos & `dynXpEmpty` ficam FORA, & `dynXpEmpty` segue por ÚLTIMO no XML (§V253) · `xpLogBox` fica na altura autorada **540**. ⚠ a largura da barra sai da margem de 16 (§R104): se ⊥ couber, `xpLogBox` **&** `EXPERIENCE` crescem JUNTAS (§V247 é relação, ⊥ literal)|I57,I39,V235,V247,V253,V261,R104
T589|x|gate — §V260 & §V261 NOVAS, ∀ uma c/ as 2 provas de §V222 (mutação que acende + sonda que ⊥ acende) & zero-guard próprio (§V209) · §V235 REESCRITA: a perna (c) de ALTURA sai & (a) largura & (b) frase própria ficam — a perna velha exige o OPOSTO de §V261(a) ∴ as 2 mudam no MESMO commit ou o gate reprova código certo (§B7) · §V247 conferida contra a barra. ⚠ §V138 é PARAMÉTRICA na gramática de chave & ⊥ deve pedir 1 caractere p/ `xpOrder` — se pedir, quem está errado é a chave nova|V260,V261,V235,V247,V138,V222,V20,V209
T591|x|itens 1 & 2 numa §T só (§I58, §I59) — a casca de 3 sub-abas & o painel `Psychic Phenomena` INTEIRO. Partir deixa o gate vermelho no meio: §V40 acende nos 3 painéis empilhados & §V30 acende no 2º grupo de radio, & os 2 são o MESMO commit da casca. **`WoD20.7`**: `numStrip` (`0/12/1280/34`) c/ os 3 pares `tabOn*`/`btnTab*` (`Hedge` `6`/**132** · `Psychic` `142`/**165** · `Faith` `311`/**126**, `top=2 height=30`) & os 3 painéis `tabHedge`/`tabPsychic`/`tabFaith` em `0/58/1280/660`, `tabHedge` VISÍVEL & os 2 ⊥. `PSYCHIC PHENOMENA` `0/0/410/`**641** c/ **24** linhas (`psychic_1`…`_19` `<PsychicPicker>` + `psychic_20`…`_24` `<NuminaFree>`), radio novo `field="psychicSel"` `groupName="psychicSel"` no `PsychicPicker`, & 3 `DESCRIPTION` — `edtPsychicDesc` (`420/0/850/641`) · `edtHedgeDesc` (`420/140/850/466`) · `edtFaithDesc` (`0/86/1270/574`). Hedge & fé recebem o conteúdo de HOJE movido VERBATIM p/ dentro do painel (`HEDGE MAGIC PATHS` `0/140/410/291` c/ as 10 linhas de hoje & a nota em `top=616`; `TRUE FAITH` `0/0/1270/66` c/ a fileira CENTRADA em `left=442` p/ §V239 fechar) — §T592 & §T593 remodelam as 2. `dataLink` ganha `psychicSel` & as 14 linhas novas, & PERDE `faith` (o bloco de fé ⊥ lê mais o campo). **RAIZ**: `SUB_TABS` + `renderSubTabs(from)` (o `renderVampTabs` MORRE & `WoD20.11.onNodeReady` re-aponta) · `NUMINA_ROWS`=**10** `NUMINA_FREE_ROWS`=**2** `PSYCHIC_ROWS`=**24** `PSYCHIC_FREE_ROWS`=**5** (as 2 de hedge nascem c/ o número de HOJE & viram em §T592) · `XP_NUMINA` LÊ as 2 de contagem & os 2 literais `10` de `numinaTypeInUse` SOMEM. **`.lang`** nos 2 blocos & mapa `PT`: `Hedge Magic`=`Magia Estática` & `Psychic Phenomena`=`Fenômenos Psíquicos`. ⚠ GATE junto senão ele reprova código certo (§B7): §V40 ganha a perna de painel POR RETÂNGULO (`:735-737`) · §V5 lê `PSYCHIC_ROWS`/`NUMINA_ROWS` em vez do `1..10` literal (`:341` `:359`) · §V30 mede **2** grupos de radio, cada um cobrindo as SUAS linhas, & a linha de fé sai da conta (`:1486-1507`) · §V228 censo 14→**17** (`:4987`) & os pares de barra ganham `WoD20.7.lfm/numStrip` (`:4894` `:4957` `:5030`) · §V239/§V240 censo 66→**68** (`:5644` `:5710`)|I58,I59,V262,V263,V40,V30,V5,V228,V239,V240,V225,V221,V226,V206,V33,V29,V26,V27,V12,V10,V22,V28,V204,V2,I3
T592|x|itens 3, 4 & 5 (§I60) — o painel `Hedge Magic` c/ as 2 caixas do topo & a lista estendida. **`WoD20.7`**: `HEDGE MAGIC` (`0/0/410/130`) c/ 3 linhas em `top` 40/68/96, rótulo `15/150` & entrada `175/220` — `hedgeEssence` `<edit>` · `hedgeAffiliation` `<edit>` · `hedgeAttr` `<comboBox name="cboHedgeAttr">` · `QUINTESSENCE` (`420/0/850/83`) c/ `quint_1`…`_10` em `top=48`, `left` 282 + 29·(i−1), LIVRES (`autoChange` do host, ⊥ `onClick`, ⊥ `xpClick`) · `HEDGE MAGIC PATHS` 291→**466** c/ **17** linhas (`numina_1`…`_12` `<HedgePicker>`, linha 1 `style="bold"`, + `numina_13`…`_17` `<NuminaFree>`) · nota de afinidade em `12/616/315/20` · `dataLink` ganha as 7 linhas novas. **`WoD20.6`**: `PICKER_LIST["hedgeAttr"]` = vazio + os 9 nomes de `XP_ATTRS`. **RAIZ**: `NUMINA_ROWS` 10→**17** & `NUMINA_FREE_ROWS` 2→**5** — & mais nada, porque §T591 já pôs ∀ laço lendo elas. **`.lang`** nos 2 blocos & mapa `PT`: `HEDGE MAGIC`=`MAGIA ESTÁTICA` · `QUINTESSENCE`=`QUINTESSÊNCIA` · `Essence`=`Essência` · `Affiliation`=`Afiliação` · `Casting Attribute`=`Atributo de Conjuração`. ⚠ GATE junto (§B7): §V99 & §V134 ganham `quint_*` na exceção NOMINAL por prefixo, ao lado de `bloodPool_*` — afrouxar as 2 em vez de nomear é §B38 de volta · §V88 re-ancora a nota (agora sob `HEDGE MAGIC PATHS`, ⊥ sob `TRUE FAITH`) · §V239/§V240 censo 68→**70**. ⚠ §V16 §V196 §V26 §V37 §V239 §V240 CONFERIDAS contra os números novos ANTES de marcar: as 6 são paramétricas & ⊥ devem pedir 1 caractere — se pedirem, quem está errado é o número, ⊥ o check|I60,V264,V263,V219,V99,V134,V88,V148,V208,V17,V16,V196,V239,V240,V26,V27,V37,V10,V22,V28,V2,I3,I9
T593|x|item 6 (§I61) — o painel `True Faith`. **`WoD20.7`**: `cboFaith` SAI & entra `<edit field="faith">`; a caixa `TRUE FAITH` vira `0/0/1270/76` c/ a fileira CENTRADA — `Religion` `352/95` · `edit` `452/300` · `faith_1`…`_5` em `left` 777 + 29·(i−1), os 3 grupos em `top=41` — & a `DESCRIPTION` do painel fica ONDE §T591 a pôs (`0/86/1270/574`), EMBAIXO & ⊥ ao lado. O ramo `if selected == "faith" then value = "True Faith"` SAI & `edtFaithDesc` passa a receber `DESC['True Faith']` sem condição. **`WoD20.6`**: `PICKER_LIST["faith"]` SAI. As 4 chaves de religião FICAM no `.lang` (⊥ ∃ check de chave sem leitor). ⚠ GATE junto (§B7): o bloco de §V50 (`:1570-1581`) SAI inteiro & vira §V265 em §T594 · o must-list de §V14/§V15/§V17 (`:533`) troca `WoD20.7.lfm/cboFaith` por `WoD20.7.lfm/cboHedgeAttr` · §V32 mantém `True Faith` fora dos órfãos · §V35 ⊥ é tocada (`faith_1`…`_5` seguem em `WoD20.7`)|I61,V265,V50,V32,V35,V1,V2,V29,V13,V239,V240,V16,V209
T594|x|gate — §V262 §V263 §V264 §V265 NOVAS, ∀ uma c/ as 2 provas de §V222 (mutação que ACENDE + sonda VIZINHA que ⊥ acende) & zero-guard próprio citando §V209: §V262 c/ as 4 mutações da linha dela & o zero-guard de painel isento POR RETÂNGULO (< 3 ! VERMELHO — 0 significa que a perna (d) virou no-op, que é §B7 pelo outro lado) · §V263 c/ zero-guard das 4 constantes & a mutação da linha livre no MEIO da caixa · §V264 c/ o censo de 10 `quint_*` & a comparação de `hedgeAttr` nos 2 SENTIDOS · §V265 substituindo §V50, que passa a medir a AUSÊNCIA de picker no painel de fé. ⚠ §V225 §V221 §V228 §V239 §V240 §V204 §V255 CONFERIDAS contra a aba nova ANTES de marcar: as 7 são paramétricas & ⊥ devem pedir 1 caractere|V262,V263,V264,V265,V222,V20,V209,V50,V225,V221,V228,V239,V240,V204,V255
T596|x|`DESCRIPTION` do mestre nas linhas DIGITADAS de `Hedge Magic` & `Psychic Phenomena` (pedido user 2026-08-23, §I62) — é §T561/§T562 na 6ª & 7ª área, sem XML de linha p/ mexer: as 10 linhas JÁ são `<edit>`. **RAIZ**: `TYPED_ROW_FROM` ganha `numina = NUMINA_ROWS - NUMINA_FREE_ROWS + 1` & `psychic = PSYCHIC_ROWS - PSYCHIC_FREE_ROWS + 1` — & mais nada, porque `isTypedRow`/`descFieldOf` já derivam o resto. **`WoD20.7`**: `NO_TEXT_YET` `{en, pt}` NOVO · `numinaText` ganha o ramo `isTypedRow(selected)` ACIMA do `require` (texto próprio → ELE · vazio & mestre → `""` · vazio & jogador → a frase) · helper local `paintNuminaPane(box, selected, lang)` c/ `text` & `readOnly` grudados, declarado ACIMA de `renderNuminaDesc` (§V223, §B51) · `renderNuminaDesc` chama o helper p/ `edtHedgeDesc` & `edtPsychicDesc` dentro de 1 janela só de `descQuiet` & segue pintando fé por `faithText` · `saveNuminaDesc(form, selField)` global c/ `local sel = sheet[selField];` & `if selField == "psychicSel" then box = form.edtPsychicDesc; end;` · `onChange="saveNuminaDesc(self, 'numinaSel');"` no `edtHedgeDesc` & `('psychicSel')` no `edtPsychicDesc`. Os 10 campos de descrição ⊥ entram em `dataLink` nenhum & `edtFaithDesc` ⊥ é tocado. ATÔMICA: `isTypedRow` respondendo `true` sem o ramo de leitura deixa a linha digitada mostrando `No description on file for:` numa caixa travada. ⊥ ∃ sítio de gate que acenda com esta §T (MEDIDO: o laço de §V249 lê só os 3 `.lfm` de vampiro · §V29 mede `readOnly`/`field`, que ⊥ mudam · §V111 já tem `edtHedgeDesc`/`edtPsychicDesc` em `$DESC_BRIGHT`) ∴ o gate novo é §T597|I62,V266,V249,V250,V206,V223,V33,V29,V123,V80,I3,I8a
T597|x|gate — §V266 NOVA, c/ as 6 pernas, zero-guard próprio citando §V209 & as **5** mutações + a sonda da linha dela RODADAS (§V222, ⊥ aceitar check que ⊥ saiba falhar — §V20, §B7). O laço de §V249 (`$descAreas`, os 3 de vampiro) ⊥ é tocado & `$typedSeen -lt 3` segue **3**: o painel de `Numina` tem 2 blocos em 1 arquivo, 1 renderer p/ os 2 & `save` c/ ARGUMENTO ∴ enfiá-lo naquele laço afrouxaria os literais que ele mede hoje (`local sel = sheet.<Sel>;`, `readOnly = not write;`, `$Save(self);`). ⚠ §V29 §V111/§V244 §V193 §V206 §V123 CONFERIDAS contra os 2 blocos ANTES de marcar: as 5 já os cobrem & ⊥ devem pedir 1 caractere|V266,V222,V20,V209,V249,V250,V29,V111,V244,V193,V206,V123
T599|x|banda de topo de `tabHedge` (itens 1 & 2 do pedido 2026-08-23, §I63) — **`WoD20.7`**: `QUINTESSENCE` `420/0/850/83` → `420/0/`**420**`/130` c/ as 10 bolinhas em `top=`**73** & `left` = 67 + 29·(i−1) · caixa `WILLPOWER` NOVA em `850/0/420/130` c/ o bloco de `250×45` em `85/63` · template NOVO `WillpowerNumina` (10 `imageCheckBox` SEM `field`, `autoChange="false"`, `opacity="0.40"`, nomes `wpNum_1`…`_10`, passo 25 + 10 `checkBox field="willpower_c1..c10"` em `top=20`, passo 25) · `paintNuminaWillpower(form)` local, declarado ACIMA de quem o chama (§V223) & lendo `form["wpNum_" .. d]` · `dataLink` ganha `willpower_1`…`_10`. **`.lang`** nos 2 blocos & mapa `PT`: `WILLPOWER` já ∃ (aba Combat) ∴ conferir antes de duplicar chave. ⚠ GATE junto senão ele reprova código certo (§B7): §V240 ganha a exceção NOMINAL das 2 caixas esticadas + a cobrança de CENTRAGEM no lugar · §V239/§V240 censo **70 → 71** · §V111 lê as 10 `wpNum_*` como só-leitura & elas JÁ nascem a 0.40 ∴ ⊥ pedem exceção · §V36/§I3 passam a declarar espelho de 3 pontas|I63,V267,V240,V239,V111,V36,V26,V264,V209,V223,I3,B19
T600|x|sub-abas `Paths` & `Rituals` em `tabHedge` (itens 3 & 4, §I64) — **RAIZ**: 3º grupo em `SUB_TABS` (`{ list = { "HedgePaths", "HedgeRituals" }, active = "tabHedgePaths" }`) & MAIS NADA: `renderSubTabs`/`selectTab` já são genéricos desde §T591. **`WoD20.7`**: `hedgeStrip` `0/142/1280/34` SEM `align`, c/ os 2 pares `tabOnHedgePaths`/`btnTabHedgePaths` (`6/2/95/30`) & `tabOnHedgeRituals`/`btnTabHedgeRituals` (`105/2/90/30`) · a NOTA de afinidade sai do rodapé & vira label da barra (`210/2/400/30`, `vertTextAlign="center"`) · painéis `tabHedgePaths` & `tabHedgeRituals` em `0/188/1280/472`, o 1º visível · TODO o conteúdo de hoje abaixo da banda entra em `tabHedgePaths` VERBATIM c/ `top` re-origem em 0 (`HEDGE MAGIC PATHS` `0/0/410/466` & `DESCRIPTION` `420/0/850/466`). **`.lang`** nos 2 blocos & mapa `PT`: `Paths`=`Caminhos` · `Rituals`=`Rituais` (as chaves de vampiro são `Blood Sorcery: …` ∴ estas são NOVAS). ⚠ nomes `tabHedge*` & ⊥ `tabPaths`/`tabRituals`: os de vampiro já ocupam esses nomes & `found`/`selectTab` casam por NOME (§V268a). ⚠ GATE junto (§B7): §V228 censo **17 → 19** & os pares de barra ganham `WoD20.7.lfm/hedgeStrip` · §V227 conta 3 grupos de `SUB_TABS` · §V88 re-ancora a nota (agora na barra, ⊥ sob a lista) · §V262b/c re-derivadas p/ o painel-mãe (banda + barra + painéis fecham em 660 & em 1270)|I64,V268,V228,V227,V88,V262,V221,V226,V225,V40,V209,B41,B52
T601|x|sub-aba `Rituals` INTEIRA — caixa, níveis & preço (item 5, §I65) — ATÔMICA: bolinha c/ `field` sem `declareTrait` acende §V99/§V134, & `declareTrait` sem o ramo de ledger cobra 0 calado ∴ XML, `XP_TRAIT` & ledger no MESMO passo. **RAIZ**: `HEDGE_RITUAL_ROWS`=**17** & `HEDGE_RITUAL_FREE_ROWS`=**5** · `TYPED_ROW_FROM.hedgeRitual` derivada (§I62) · `declareTrait("hedgeRitual_" .. i, 1, 5, 0)` SÓ p/ `i` ≥ 13 · `xpCost` ganha `kind == "Ritual"` → **1** · `xpLedgerRows` ganha o bloco de ritual LOGO ABAIXO do de númina: 1 linha por ritual, `level` = nível lido, `cost` = nível − nível do baseline c/ carimbo de grátis descontado por nível, `from`=0 quando o nome mudou · leitor de nível `^(%d+)%. ` no picker & bolinhas na digitada. **`WoD20.7`**: templates `HedgeRitualPicker` (radio `hedgeRitualSel` + combo `25/0/230/25` + 5 bolinhas MOSTRUÁRIO `roHR$(field)_1..5` em 260…360, SEM `field`, `autoChange="false"`, `opacity="0.40"`) & `HedgeRitualFree` (radio + `<edit>` + as 5 bolinhas de VERDADE c/ `xpClick`) · caixa `HEDGE MAGIC RITUALS` `0/0/410/466` c/ 17 linhas & `DESCRIPTION` `420/0/850/466` (`edtHedgeRitualDesc`) dentro de `tabHedgeRituals` · `paintHedgeRitualDots(form)` · `renderNuminaDesc` pinta o 3º bloco pelo helper de §I62 & `saveNuminaDesc(self, 'hedgeRitualSel')` no `onChange` · `dataLink` ganha `hedgeRitualSel` & as 17 linhas (os campos `hedgeRitualDesc_*` ⊥, §V266f). **`WoD20.6`**: `PICKER_LIST["hedgeRitual"]` = só o vazio de §V15. **`.lang`**: `HEDGE MAGIC RITUALS`=`RITUAIS DE MAGIA ESTÁTICA`. ⚠ DEPENDE de §T596 (o helper & `saveNuminaDesc` nascem lá) & de §T600 (o painel). ⚠ GATE junto (§B7): §V30 mede **3** grupos de radio em `WoD20.7` · §V239/§V240 censo **71 → 73** · §V204/§V263 leem as 2 constantes novas · §V26 conta 6 templates na mesma coluna|I65,V269,V266,V263,V204,V26,V30,V15,V99,V134,V111,V139,V83,V100,I9,I10,I3,I62
T602|x|gate — §V267 §V268 §V269 NOVAS, ∀ uma c/ as pernas, zero-guard próprio citando §V209 & as mutações + sonda da linha dela RODADAS (§V222; check que ⊥ sabe falhar é §B7 esperando). ⚠ CONFERIDAS contra a aba nova ANTES de marcar — as 9 são paramétricas & ⊥ devem pedir 1 caractere: §V225 §V221 §V226 §V262 §V196 §V16 §V37 §V204 §V255. ⚠ o censo de §V228 (19) & o de §V239/§V240 (73) já vêm de §T599/§T600/§T601 ∴ aqui eles só são CONFERIDOS, ⊥ reescritos|V267,V268,V269,V222,V20,V209,V225,V221,V226,V262,V196,V16,V37,V204,V255
T604|x|item 1 (§I66) — `Affiliation` vira picker. **`WoD20.7`**: o `<edit …field="hedgeAffiliation">` da caixa `HEDGE MAGIC` SAI & entra `<comboBox name="cboHedgeAffiliation" left="175" top="68" width="220" height="24" fontSize="12" field="hedgeAffiliation">` no MESMO retângulo (a grade de 3 linhas ⊥ se mexe). **RAIZ**: `PICKER_LIST["hedgeAffiliation"]` NOVA dentro da região `PICKER_LIST_BEGIN`/`_END`, c/ o vazio de §V15 + as **26** de §I66 na ordem que §I66 fixa — chave `hedgeAffiliation` & ⊥ `affiliation`, senão o combo cai no `or c.values` vazio c/ `rdk -l` exit 0 (§V211a §B6). `.lang` & mapa `PT` ⊥ ganham nada: os 26 são nome próprio (§I17). ⊥ mexer em `cboHedgeAttr` nesta linha — o filtro dele é §T605|I66,V270,V15,V16,V208,V211,V24
T605|x|itens 2 & 3 (§I67 §I68) — os 2 filtros de afiliação, ATÔMICA: tabela sem ramo em `pickAllowed` é dado morto (§B55) & ramo sem entrada no carimbo do memo serve lista velha calado (§B48) ∴ os 5 sítios no MESMO passo. **RAIZ**: `AFFINITY` (25 chaves) & `CASTING_ATTR` (20 chaves) NOVAS ao lado de `PICKER_LIST`, c/ os mapas que §I67 & §I68 tabelam · `pickAllowed` (`WoD20th.lfm:2943`) ganha 2 ramos, `field == "numina_1"` & `field == "hedgeAttr"`, os 2 devolvendo `true` quando ⊥ ∃ chave. **`WoD20.6`**: `FILTER_NAME` NOVA (`cbonumina_1` & `cboHedgeAttr`) & `isFilterRow` passa a consultar as 2 tabelas · o `field` de `pickerItems` (`:1072`) vira "acaba em `_<n>` → `string.sub(nm,4)`; senão → `fieldRoot(nm)`" · o carimbo do memo (`:1082`) vira `lang\|era\|<afiliação>` · `renderHedgePickers(from)` NOVA, `renderVampPickers` c/ 2 nomes. **`WoD20.7`**: `<dataLink field="hedgeAffiliation" onChange="renderHedgePickers(self);"/>`. ⚠ §V223 — declarar abaixo de quem a função chama, ou o corpo compila `GETGLOBAL` & acha nil (§B51)|I67,I68,V271,V272,V200,V205,V203,V143
T606|x|item 7 (§I71) — as 2 flags de mestre & os 2 espelhos, ATÔMICA (XML + Lua + `.lang` + 2 literais de gate). **`WoD20.10`**: caixa `STORYTELLER SETTINGS` `height` 339 → **399** · `chkManualAffiliation` (`15/196/375/25`, `stManualAffiliation`) & `chkManualClanFamily` (`15/226/375/25`, `stManualClanFamily`) NOVOS · os 3 pares rótulo/combo & o par `Save` descem 60 (264/261 · 294/291 · 324/321 · 366/363). **`WoD20.7`**: `edtHedgeAffiliation` NOVO no mesmo retângulo do combo, `visible="false" enabled="false"`. **`WoD20.11`**: `edtClanFamily` idem, em `115/82/192/24`. **`WoD20.6`**: `renderManualEntry(from)` NOVA (4 pares `visible`+`enabled`) + os 2 `<dataLink>` de gatilho + chamada no `onShow` das 2 abas. **`.lang`** (os 2 blocos) & mapa `PT`: `Manual Affiliation` = `Afiliação Manual` · `Manual Clan/Family` = `Clã/Família Manual`. **§I3**: os 2 pares entram em ESPELHOS. ⚠ **gate na MESMA rodada** (§B7): `$stChecks.Count -lt 5` → `-lt 7` em `:5310` **&** `:5324` — a caixa passa a ter 7 `checkBox`|I71,V274,V36,V238,V240,V80,V56
T607|x|itens 4 & 6 (§I69 §R106 §R107 §R108) — as **6** núminas novas, ATÔMICA c/ o texto delas: §V32 cobra `DESC` p/ ∀ item do picker ∴ item sem entrada reprova no mesmo passo. **RAIZ**: `PICKER_LIST["numina"]` 21 → **27**, c/ `Chronomancy` `Curses` `Dowsing` `Spirit-Chasing` `Via Geniorum` `Via Necromantiae` no lugar alfabético de §I69 (`Ephemera` segue antes de `Enchantment` — ⊥ reordenar o resto). `PICKER_LIST["psychic"]` ⊥ muda. **`descNumina.lua`**: as 6 entradas NOVAS, `en`+`pt`, JÁ na forma de 3 blocos de §I21 — prosa + níveis VERBATIM do livro, sem ritual de exemplo (§C). ⚠ **gate na MESMA rodada** (§B7): §V78 conta o total das 2 caixas c/ literal `-eq 43` (`verify-hunters-hunted.ps1:1670`) → **49**. ⚠ ler §R110 antes de transcrever `Via Necromantiae`: se os níveis forem os de `Necromancy` c/ verniz, ela é rótulo & ⊥ entrada, & a linha vira 5|I69,I21,V32,V78,V263,V24,V9,R106,R107,R108,R110
T608|x|item 5 (§I70) — o cabeçalho de 3 blocos nas **43** entradas que já ∃ em `descNumina.lua`, `en`+`pt`. Trabalho MECÂNICO & ⊥ criativo: o corpo (bloco 3) ⊥ é tocado, só ganha `"<livro>, p. <pág>\n\n\n<nome>\n\n\n"` na frente. Livro & página: as 42 do `M20 Sorcerer` saem da tabela MEDIDA de §I70 (hedge 18-50, psíquicas 54-76) & `True Faith` sai de `The Hunters Hunted II` p. 83 (§R107). [pt] repete o MESMO título & troca só `p.` → `pág.` (§I21). ⚠ o bloco 2 ! ser IDÊNTICO à chave da entrada, `Shadow` & `Shadows` inclusive — são 2 entradas distintas (psíquica p. 74 & hedge p. 45) & trocá-las passa em §V177 & morre em §V273b|I70,I21,V273,V24,V9,V177,R109
T609|x|gate — §V270 §V271 §V272 §V273 §V274 NOVAS, ∀ uma c/ as pernas que §V lista, zero-guard próprio citando §V209 & as mutações + a sonda VIZINHA que ⊥ acende RODADAS (§V222; check que ⊥ sabe falhar é §B7 esperando — §V20). RODA DEPOIS de §T607 & §T608: §V273 mede as 49 entradas & antes disso as 43 velhas ainda ⊥ têm cabeçalho. §V272b & §V272c são as 2 que ⊥ dão sintoma na tela ∴ a mutação delas ! ser a que o coletor mede (carimbo sem `hedgeAffiliation` & `string.sub(nm,4)` cru), ⊥ uma reescrita que só some c/ a linha. §V238 & §V78 ⊥ são tocadas aqui — os literais delas foram c/ §T606 & §T607|V270,V271,V272,V273,V274,V222,V209,V20
T611|x|gate — §V275 NOVA (backprop de §B58): ler a EXPRESSÃO da chave do memo em `pickerItems` (`WoD20.6.lfm`, hoje `:1172`) & exigir que ∀ nome ∈ `FILTER_NAME` entre na chave POR NOME, ⊥ por raiz. Âncora = a construção, ⊥ o nome do local (§V222). Zero-guard próprio citando §V209 (`FILTER_NAME` vazia \| expressão ⊥ achada ! VERMELHO) + as 2 provas de §V222 RODADAS — mutação: chave só-raiz (`per .. "\|" .. odd`) ! VERMELHO · sonda: renomear o local `key` ! VERDE|V275,V222,V209,V20,I67,B58
T612|x|96ª — rename de RÓTULO dos 2 `checkBox` de `STORYTELLER SETTINGS`, ATÔMICA em **3** arquivos / **6** linhas. **`WoD20.10.lfm`** (`:151` `:152`): `text="Manual Affiliation"` → `text="Edit Affiliation"` & `text="Manual Clan/Family"` → `text="Edit Clan/Family"` — `name=` & `field=` & a geometria (`15/196/375/25` & `15/226/375/25`) ⊥ são tocados. **`WoD20.6.lfm`** (mapa `PT`, `:169` `:170`): as 2 CHAVES viram `["Edit Affiliation"] = "Editar Afiliação"` & `["Edit Clan/Family"] = "Editar Clã/Família"`. **`localization.lang`**: [pt] (`:460` `:461`) & [en] (`:1403` `:1404`) — `wod.Edit Affiliation` = `Editar Afiliação`\|`Edit Affiliation` & `wod.Edit Clan/Family` = `Editar Clã/Família`\|`Edit Clan/Family`, & as 2 chaves VELHAS SAEM dos 2 blocos (⊥ ∃ leitor que as busque depois de `WoD20.10` mudar). Gate ⊥ é tocado (MEDIDO: 0 literal do TEXTO em `verify-hunters-hunted.ps1`) & §V274 ⊥ é tocada (mede `name`/`field`). Meia rodada acende check que já ∃: rótulo sem chave = §V10, chave sem entrada no mapa `PT` = §V28|I71,V2,V9,V10,V16,V22,V28
T613|x|item 2 — braço ADAPTATIVO: `ornCorner`/`ornPath` recebem o braço já resolvido (`min(ORN_ARM, floor(min(w,h)/2) - 2)`) & o guard de tamanho SAI do `ornament()` ∴ `TRUE FAITH` (`WoD20.7.lfm:853`, `1270×76`) ganha filigrana & ⊥ ∃ caixa pulada. Abaixo do braço mínimo o que cai é o MOTIVO (contas, palmeta), nunca o filete|V279,I72,B59
T614|x|item 3 — ornamento das 19 pílulas de aba: 1 `<path>` por pílula pelo mesmo caminho de §I72c (filho da `<rectangle>`, `align="client"`, escondido quando a época ⊥ declara `ornament`), motivo de §R111 escolhido NA TELA. ⊥ reusa `ornPath` — 80 ⊥ cabe em 30|V282,R111,I72,V226
T615|x|item 4 — margem **20** nas 73 caixas de seção **&** vão **20** entre elas, nos 2 eixos (§I73; as 2 decisões tomadas pelo user na 103ª). **REGRA = "a margem vira EXATAMENTE 20"** & ⊥ "+40 fixo" — a 99ª/100ª escreviam +40 uniforme, decidido ANTES de alguém MEDIR que as 73 ⊥ são 1 grupo só: cresce `max(0,20-L)+max(0,20-R)` de largura & `max(0,20-T)+max(0,20-B)` de altura ∴ **68** caixas +40 de largura & **5** +10 (as de `L=R=15`), **71** +20 de altura & **2** +10 — §I73 nomeia as 7. O piso em 0 garante que ⊥ ∃ caixa que ENCOLHE. Nenhum conteúdo é re-disposto & nenhuma linha re-desenhada: a CAIXA cresce & o filho desloca (o caminho (B), "conteúdo encolhe", segue REJEITADO — os 4 boxes dos painéis de hedge têm folga interna de 10px ∴ encolher come conteúdo). **REFLOW, 3 NÍVEIS** (MEDIDO: **62** caixas em depth 0, **7** em depth 1, **4** em depth 2): caixa cresce → vizinha da MESMA fileira anda o delta ACUMULADO & o vão entre elas vira 20 → fileira de baixo desce o delta acumulado da coluna & o vão vira 20 → container aninhado cresce p/ caber o conteúdo. Vão de botão (**4**) & de barra↔painel (**12** & **4**) ⊥ mudam — §T616 os congelou de propósito. **AVATAR da Main ACOMPANHA** (decisão 2): `rectangle` DimGray + `image field="avatar"` (`WoD20.1.lfm:330-331`) vão de `300` p/ **340** de largura. ⊥ é caixa de seção — ⊥ é preta ∴ §V278 ⊥ o pega & ⊥ entra nas 73 — mas mora sob `ATTRIBUTES` & ficar em 300 abre degrau de 40 na coluna esquerda; `style="autoFit"` ∴ o retrato ⊥ distorce. A ALTURA dele é DERIVADA de §V69 (fecha na linha de `HUMANITY` & `SPECIALTIES`) & ⊥ é número novo. **LINHA DE BAIXO (§V69, §B63)**: colunas c/ número DIFERENTE de caixas crescem DIFERENTE ∴ a linha única de `WoD20.1` ⊥ sobrevive ao crescimento uniforme — MEDIDO: x=310 (3 caixas) fecha **894**, x=615 (2) fecha **872**, x=920 (2, altura SOLDADA) fecha **873**. A régua é a coluna MAIS FUNDA & a última caixa das rasas CRESCE até ela (`SPECIALTIES` `326`→**348**); o mapa `Everything closes on x=… / y=…` (`WoD20.1.lfm:36`) acompanha, que é de onde §V69 LÊ. **VITALIDADE**: `HEALTH_BOX_PAD` `37` → **79** (`WoD20th.lfm:1168`) & o estático das 2 caixas `307` → **349** — os 22px que faltavam na coluna do `HEALTH` entram DENTRO da caixa (decidido pelo user na 103ª) & ⊥ acima dela: topo da fileira segue ALINHADO & ∀ vão segue 20 ∴ §V280(b) fica IGUALDADE & ⊥ precisa virar piso. `PAD` vale p/ `dynHealth3_box` (`WoD20.3`) junto. A altura é escrita em RUNTIME ∴ §V49 pede mutação nova & isto encosta em §V284/§V285. **MEDIDO 2026-08-24** (largura nova, já c/ o vão 20): `WoD20.2` **1670**×743 · `WoD20.7` panes **1410**×734 · `WoD20.1` **1390**×895 · `WoD20.10` 1365 · `WoD20.5` 1360 · `WoD20.8` 1300 · `WoD20.4` 1180 · `WoD20.3` 1090 · `WoD20.11` 1080×**934** · `WoD20.12`/`13`/`14` 1070 · `WoD20.9` 541 · `WoD20.6` 420. Barra horizontal ACEITA pelo user (99ª, re-afirmada na 103ª) ∴ `WoD20.2` a 1670 ⊥ é defeito. ⚠ ATÔMICA c/ §T618 §T621 §T623|V280,I73,V239,V240,V262,V267,V69,V49,T618,T621
T616|x|item 5 — margem **15** vertical & **30** horizontal nos 19 botões: as 4 barras vão a `height=60` (`tabStrip` 46→60, as 3 sub-barras 34→60) & o 1º botão de cada uma a `left=30`; gap 4 INALTERADO. `TAB_W` & a cadeia de `left` do Lua acompanham|V281,I73,V232,V228
T617|x|gate — checks NOVOS §V276 (path runtime sem geometria) §V277 (esconde, ⊥ destrói) §V278 (filtro por construção) §V279 (⊥ ∃ caixa pulada, guard LIDO do Lua) §V280 (20 nos 4 lados) §V281 (15/30 nos 19 botões) §V282 (pílula ornamentada). ∀ check c/ mutação RODADA antes de aceitar (§V20, §B7). ⚠ **§V280 ! CORTAR filho c/ `rotationAngle`**, ≡ §V27 (`:1019`) §V239 (`:5860`) §V240 (`:5935` `:5973`) já cortam: `left`/`top` de rotacionado é a caixa PRÉ-rotação & medi-la como margem acende nos 3 rótulos de `ATTRIBUTES` que desenham DENTRO (§B61). ⚠ §V281c ("o último fechando 30 antes do fim") é `?` DE LETRA & ⊥ dá p/ implementar como está: os 19 botões são dimensionados pelo TEXTO (§V228) ∴ o último fecha em 1163 numa barra de 1280 & em 437 numa de 1280 — ou a perna vira "≥30 de folga" ou ela sai; decidir ANTES de escrever o check, ⊥ depois|V276,V277,V278,V279,V280,V281,V282,V20,B61
T618|x|reconciliar as invariantes que leem os números que §T615 & §T616 mudam, na MESMA rodada. **MEDIDO na 99ª — a lista da 98ª errava dos 2 lados.** ⊥ precisam de NADA (são RELAÇÕES & se reconciliam sozinhas se o deslocamento for certo): §V221 · §V225 · §V232 — & `1149`/`1153` só ∃ em PROSA (§C §I36 §T618), o gate ⊥ guarda nenhum dos 2. PRECISAM: **(1)** `$CONTENT_GAP = 12` → **20** — ✅ **FEITO na 101ª** (`verify-hunters-hunted.ps1:5360`), os 14 forms deslocaram os filhos de 1º nível **+8**; **(2)** §V240 respiram 10 → **20**; **(3)** **§V262 & §V267, que a 98ª ⊥ nomeava & são o que TRAVOU as 2 tarefas por 4 rodadas** — ✅ a RÉGUA foi ESCRITA na 103ª: §V262b larga `1280`/`660`, §V262c & §V267a largam `1270` (& §V267a larga `130`), & os 3 viram RELAÇÃO — fecho comum = a fileira MAIS LARGA das 3 sub-abas, **1410** medido. O que sobra p/ o build é LIGAR isso no gate: `:6776` (`$paneW - 10`) & `:7037` (`-ne 1270`) são os 2 literais a matar, & `tabHedge` tem folga vertical ZERO (688/688) ∴ as 3 panes de `WoD20.7` vão a **734** (§V262a exige as 3 no MESMO rect); **(4)** §V247 (`501` wide, última coluna 16 curta) → **541**. Separar de §T615/§T616 acende vermelho DE PROPÓSITO|V280,V281,V240,V231,V221,V225,V232,V247,V262,V267,B52
T619|x|~~o filho que VAZA~~ — **REVOGADA na 100ª, virou §B61**: a premissa estava errada nas 3 partes (`:278` é outro nó, o vazamento ⊥ ∃, & o conserto proposto criava o bug que ele dizia curar). ⚠ **FECHADA na 146ª (§T843, item 4 do user)**: `x` aqui quer dizer RESOLVIDA, ⊥ construída — o desfecho mora em §B61 & ⊥ ∃ código a escrever. Ficar `.` p/ sempre é inflar a conta de "a fazer" c/ trabalho que ninguém quer|B61
T621|x|item 1 — a filigrana por CIMA (§I72c REVOGADA, §V283). **RAIZ**: em `ornament()` (`WoD20.6.lfm:2086`) o `p:setParent(c)` passa a receber o `<layout>` (`c.parent`) em vez da `<rectangle>` `c`, & o comentário de `:2122-2123` — que hoje AFIRMA "under the labels and fields the box carries" — é reescrito, senão vira 2 verdades no mesmo arquivo. `align="client"` FICA: a `<rectangle align="client">` já ocupa o layout inteiro ∴ a caixa medida é a mesma. `hitTest=false` FICA & vira dependência declarada (§V283c). ⚠ **⊥ usar `p:setParent(p:getParent())` p/ re-elevar** — é NO-OP por `rrpgObjs.lua:637` (§R112) & sai exit 0 sem fazer nada. ⚠ **MESMA RODADA que §T615** — decidido pelo user na 103ª, CONTRA a recomendação de separar (a recomendação era: margem primeiro, tela, z-order depois, porque §T615 sozinha JÁ revela a filigrana & §B62 acabou de custar 1 rodada c/ gate verde sobre código quebrado). A dependência de §V283d ⊥ sumiu, ela vira **ORDEM DENTRO da rodada**: o filete só sobe DEPOIS que a margem 20 aterrissar nas 73. Se a rodada abortar no meio, §T621 ⊥ fecha sozinha — c/ margem < 20 o filete de `ORN_OUT=5`/`ORN_IN=9` risca os filhos de largura cheia de **68** das 73|I72,V283,R112,V276,T615
T623|x|gate — §V283 & §V284 NOVAS, ∀ uma c/ as pernas que §V lista, zero-guard próprio citando §V209 & as mutações + a sonda VIZINHA que ⊥ acende RODADAS (§V222; check que ⊥ sabe falhar é §B7 esperando — §V20). §V283b é a que ⊥ dá sintoma de tela nenhum: a mutação dela ! ser `setParent(getParent())` (o no-op de §R112), ⊥ uma reescrita que só some c/ a linha. §V284b ! ter a mutação de ORDEM (chamar a filigrana ANTES da escrita de `box.height`), que tb sai exit 0|V283,V284,V222,V209,V20,B7
T626|x|§B62 — o redesenho da filigrana para de depender de leitura de runtime, ATÔMICA (as 2 metades falham caladas & consertar 1 deixa a outra de pé). **`WoD20.6`**: `refreshOrnament(box)` → **`refreshOrnament(box, w, h)`**, & o laço deixa de casar `k.align == "client"` & passa a casar **`ornPainted[k.handle] ~= nil`** (o memo ∃ porque `applyTheme` criou; ⊥ ∃ → ⊥ ∃ o que redesenhar & sair é certo). `ornament(c, fill, colour, w, h)` ganha os 2 opcionais: quando vêm, a 3ª porta compara & redesenha COM ELES; quando ⊥ vêm (o caminho de `applyTheme`), cai em `c.width`/`c.height` ≡ hoje (§V276 intacta). **`WoD20th`**: `renderHealthTrack` guarda a altura num local, escreve `box.height` c/ ele & passa `box.width` & ele p/ `refreshOrnament` — a largura é AUTORADA (290 & 280) & ⊥ muda. ⚠ ⊥ trocar `mode` p/ `stretch` p/ "resolver" (§R113b, §V284d)|B62,V285,V284,I72,R113,V276
T627|x|gate — §V285 NOVA, 4 pernas, c/ zero-guard citando §V209 & as mutações RODADAS antes de aceitar (§V20 §B7). ⚠ (a) & (c) são as que §V284 já deixou passar VERDE sobre código quebrado ∴ a mutação delas ! ser EXATAMENTE o que a 101ª tinha: achador por `k.align == "client"` ! VERMELHO · medida lida de `c.height` no caminho de refresh ! VERMELHO. Sonda vizinha: a leitura de `c.width`/`c.height` na CRIAÇÃO ! VERDE, senão a perna (c) proíbe §V276 junto|V285,V222,V209,V20,B62,V276
T620|x|âncora frouxa nos 2 sítios conhecidos: o coletor de §V10/§V28 casa `t\("([^"]+)"\)` SEM fronteira de palavra (o `t` de `format` + `("…")` de 1 argumento entra como string visível) & §V205 casa o IDENTIFICADOR `key` (achado 96ª, aberto). Os 2 são a letra de §V222|V222,B60
T629|x|item 2 — `CONTENT_GAP` **0** nos 11 forms top-level & **20** MANTIDO nos 3 de sub-aba (§I74a). ⊥ é 1 sed: 2 dos 11 carregam sub-barra & as 2 têm formas DIFERENTES (§B64) — `WoD20.7` `numStrip` 20→0 · as 3 panes 92→72, & `hedgeStrip` & as 2 sub-panes ⊥ se tocam (filhas de `tabHedge`, descem c/ o pai); `WoD20.11` 1ª caixa 20→0 · `vampStrip` 170→150 · as 3 panes 234→214, que ali são IRMÃS do `scrollBox`. Nos outros 9, ∀ filho direto do `scrollBox` sobe 20. O vão barra↔painel (12 & 4) que §I73 congelou ⊥ é tocado|V286,V293,I74,V231,I34
T630|x|item 3 — o chão de `tabStrip` (`WoD20th.lfm:3523`) ganha `strokeColor="#00000000"` & as **4** tabelas `stroke` de `THEMES` (`WoD20.6.lfm`) ganham `["#00000000"] = "#00000000"`. A chave é p/ §V53, ⊥ p/ o look — o look já sai do early-return de §V61. Fundo & canto quadrado & cor das pílulas & corte do ornamento INTOCADOS|V287,I74,V53,V229
T631|x|item 4 — `Physical` `Social` `Mental` (`WoD20.1.lfm:284` `:299` `:316`) `top` 76·161·246 → **68**·**153**·**238** = ⌊centro das 3 linhas do grupo⌋ − 10. ⊥ mexer no `left` (é §B61, os 3 renderizam DENTRO) nem no título `ATTRIBUTES`|V288,V280,B61
T632|x|item 5 — `BACKGROUNDS` 20→**22** linhas & `height` 656→**723** (fecha em 743 = fundo de `RESOURCES`; margem de baixo 27, piso de §V280a). `BACKGROUND_ROWS` 20→22 na raiz & `BACKGROUND_FREE_ROWS` fica **5** ∴ as linhas **16** & **17** trocam `OpenAbilityFreeRow`→`OpenAbility` & nascem `background_21` `_22` como `OpenAbilityFreeRow`. Campo ⊥ renomeia (§V2) & valor digitado que virou picker sobrevive por §V14/§V17|V289,V145,V2,V14,V17,V280
T633|x|item 6 — coluna `Book`: template `Merit` (`WoD20.2.lfm:23`) 380→460 c/ `book_$(num)` em 230…310 & `type_`/`costy_` p/ 310/410; rótulo `Book` `left=250 w=80` & `Page`/`Cost` p/ 330/430; `Merit` & `Flaw` `width` 420→**500**, `Flaw` `left` 830→**910**, `DERANGEMENTS` `left` 1270→**1430** (largura 240 intacta). 20 campos `book_*` no §I3 & 1 chave `.lang` `Book`/`Livro` nas 3 casas|V290,I3,V1,V16,V10,V22,V28,V280
T634|x|itens 7·8 — `WoD20.4`: `Prelude` `height` 556→**628** & o `richEdit` 495→**567**; cabeçalho `width` 1050→**1180** & as 3 colunas `Descricao` 310→**350** (o `edit` 200→240, o `label` fica 100) c/ `left` 20·**415**·**810**, vão **45** & margem 20 dos 2 lados|V291,V170,V280
T635|x|item 9 — `Settings`: 1ª caixa `width` 390→**420** & os 3 `comboBox` 270→**300**|V292,V170,V280
T639|x|gate — §V293 NOVA (backprop de §B64): a `hedgeStrip` respira IGUAL dos 2 lados dentro de `tabHedge`, medido como RELAÇÃO (vão de cima = vão de baixo, os 2 > 0) & ⊥ como o literal 12 de §I73. Zero-guard citando §V209 & as 2 mutações + a sonda RODADAS (§V20 §V222). ⚠ o lado de BAIXO já é de §V268 ∴ a mutação `top=210` ! acender as DUAS — o que ⊥ pode acontecer é a de CIMA (`top=170`) sair verde, que é exatamente o buraco que §B64 atravessou|V293,V268,V209,V20,V222
T640|x|`CLANS` (§I17) ganha as 22 famílias & o campo `choiceN` — 22 linhas novas entre `CLANS_BEGIN`/`CLANS_END` c/ os trios de §R93 (61 → **83** chaves) · `Krevcheski` = `fixed` de **2** nomes, forma já aceita · `Keskinen` = `fixed={Thaumaturgy}` + `choice={Auspex,Dominate,Obfuscate}` + `choiceN=2`, ÚNICO usuário do campo · `renderClanDisc` (§T547) lê `choiceN` c/ default **1** ∴ as 3 linhas c/ `choice` de hoje ⊥ mudam de comportamento · §V212 & §V236a passam a medir `clan` ∪ `family` no gate. ⚠ MESMA ONDA que §T510|R93,I17,I29,V212,V236,V237,V20
T641|x|gate — §V295 NOVA: o TOTAL de slots de ∀ entrada de `CLANS` (`#fixed` + `choiceN`\|1 + `open`) c/ as 3 pernas, zero-guard citando §V209 & mutação obrigatória (`choiceN` do `Keskinen` p/ 4 ! acender a & b). ⊥ toca `.lfm` NENHUM: o conserto de comportamento JÁ entrou em §T640 & o que falta é o check que teria pego (§B65)|V295,I17,V236,V20
T642|x|`WoD20.7.lfm` (§I75) — FILEIRA DE CIMA: `HEDGE MAGIC` `width` 450→**618** (título 410→**578**, rótulos x 35→**37**, `Affiliation` `top` 50→**78**, `cboHedgeAffiliation` **&** `edtHedgeAffiliation` `left` 32→**195** c/ `width` **386** INTACTA) · `QUINTESSENCE` `left`/`width` → **638**/**376** (título 336, 1ª bolinha `left` 87→**45**, pitch 29 INTACTO) · `WILLPOWER` → **1034**/**376** (título 336, `<layout>` de 250 `left` 105→**63**). FILEIRA DE BAIXO, nas 2 sub-abas: lista `width` 450→**618** (título 578) · `DESCRIPTION` `left`/`width` → **638**/**772** (título 732, `edtHedgeDesc` `width` 880→**712**) · as **34** linhas `width` 386→**554**. Os **4** templates de Hedge: entrada 230→**398** & as 5 bolinhas → **428·453·478·503·528**. ⊥ TOCA `tabPsychic` nem os 2 templates dele (§I75 `?`). ALTURA de NADA muda ∴ §V267 §V293 §V221 ficam de pé|I75,V296,V239,V280,V196,V36,V274,V224
T643|x|gate — §V296 NOVA (as 2 pernas + zero-guard de 6 caixas, casando por TÍTULO) & CONFERIR os literais de largura da aba Numina que §T642 move: §V262 (a barra & os 3 painéis) & ∀ check que cite 450·460·470·940·386·230 em `WoD20.7`. Mutação obrigatória antes de aceitar (§V20): `DESCRIPTION` de `tabHedgeRituals` 4px mais estreita ! acender (b) & ⊥ (a)|V296,V262,V209,V20
T645|x|`WoD20.7.lfm` (§I75 EMENDADA) — 2º aperto, & ⊥ toca o dropdown: `HEDGE MAGIC` `width` 618→**586** (título →**546**, rótulos x 37→**20**, coluna de entrada 195→**180**, `cboHedgeAffiliation` & `edtHedgeAffiliation` `width` **386** INTACTA, `Essence` `<edit>` & `cboHedgeAttr` `width` 220→**386**) · `QUINTESSENCE` →**606**/**392** (título 352, 1ª bolinha `left` 45→**53**) · `WILLPOWER` →**1018**/**392** (título 352, `<layout>` de 250 `left` 63→**71**) · as 2 listas `width` 618→**586** (títulos 546) · as 2 `DESCRIPTION` →**606**/**804** (títulos 764, `edtHedgeDesc` **&** `edtHedgeRitualDesc` `width` →**744**) · as **34** linhas `width` 554→**522** · os 4 templates de Hedge: entrada 398→**366** & bolinhas →**396·421·446·471·496**. ⊥ TOCA `tabPsychic` (editar por REGIÃO, §I75). ALTURA de NADA muda|I75,V297,V296,V239,V280,V196,V36,V274,V224
T646|x|gate — §V297 NOVA (as 2 pernas + zero-guard de 6 controles) & CONFERIR que §V296 segue verde c/ os números de §T645 (ela é RELAÇÃO ∴ ⊥ deve ter literal p/ mexer). Mutação obrigatória (§V20): `cboHedgeAttr` de volta p/ `width="220"` ! acender (b) & ⊥ (a)|V297,V296,V209,V20
T636|x|gate — §V286 & §V287 NOVAS (o topo: vão por ESCOPO & chão sem contorno), c/ zero-guard citando §V209 & as mutações **&** as sondas RODADAS antes de aceitar (§V20 §V222 §B7). ⚠ §V286 ! REVOGAR §V231a & §V231c no MESMO commit — as 2 de pé = vermelho de propósito, & tirá-las sem pôr §V286 = o buraco de §B7. A lista dos 11 & dos 3 sai da RAIZ, ⊥ digitada. ⚠ a sonda de §V287 é a que prova que §V53 é o dono da chave de paleta & esta ⊥ é: ela ! sair VERDE aqui & VERMELHA lá|V286,V287,V231,V53,V209,V20,V222
T637|x|gate — §V288…§V292 NOVAS (as 5 relações de alinhamento), c/ zero-guard citando §V209 & mutação dos **2** lados de cada relação RODADA (§V222): mexer na caixa medida **&** na régua ! acender as 2 vezes — relação que só acende de 1 lado é literal disfarçado, & é o que §T615 provou ao desalinhar `BACKGROUNDS` de `RESOURCES` sem 1 check reclamar|V288,V289,V290,V291,V292,V209,V20,V222
T647|x|item 1a — vão 20→**15** nas **4** abas que têm régua p/ reconciliar (§I76a §V298). `WoD20.1`: cabeçalho `width` 1030 & 2º `left` 1045 · fileira 2 `top` **140** & `left` **355**·**700**·**1045** · avatar `top` **461** · `top` **516**·**679** · col B fica 5 curta ∴ última caixa `height` 180→**185** (§V69). `WoD20.2`: fileira de baixo `left` **385**·**640**·**895**·**1150**·**1405** & `top` **326**·**527** · `Merit` **385**/**495** · `Flaw` **895**/**495** · `DERANGEMENTS` **1405**/240 (§V290b) · template `Merit` 460→**455** c/ `merit_` 230→**225** & `book_`/`type_`/`costy_` em **225**·**305**·**405** & os 4 rótulos junto (§V290a) · `BACKGROUNDS` `height` **713** & `BACKGROUND_ROWS` 22→**21** na raiz, `background_22` → ÓRFÃOS de §I3, `BACKGROUND_FREE_ROWS` fica 5 (§V145 §V204 §V128). `WoD20.3`: `top` **351**·**366**·**502** · `left` **435**·**765** · as 2 caixas de 310 → **315** (fecham em 750 c/ a de cima). `WoD20.4`: `top` **140**·**351**·**562** · `Prelude` `left` **505** `width` **675** `height` **618** & `richEdit` **557** · cabeçalho & as 3 colunas INTOCADOS (§V291)|V298,I76,V69,V289,V290,V291,V145,V204,V128,I3
T648|x|item 1b — vão 20→**15** nos **10** forms restantes, reflow sem régua nova: `WoD20.5` & `WoD20.10` `left` **455**·**910** · `WoD20.8` `left` **435**·**870** & `top` **336** · `WoD20.9` `top` **130** · `WoD20.11` `left` **675** & 2ª caixa `width` **395** · `WoD20.12` `top` **196** · `left` **405** `width` **665** & caixa de baixo da esquerda `height` **441** · `WoD20.13` `top` **221** · `left` **580** `width` **490** & caixa de baixo da esquerda `height` **441** · `WoD20.14` `left` **535** `width` **535** · `WoD20.7` banda `left` **601**·**1013** c/ as 2 esticadas `width` **397** as duas (§V267b), `tabPsychic` `left` **465** `width` **945**, `tabFaith` `top` **111**. ⚠ o `top`=20 do 1º filho de `WoD20.12` `.13` `.14` ⊥ é tocado (§V286b) & o vão de 12 acima da `vampStrip` também ⊥ (é barra↔caixa)|V298,I76,V267,V286,V225,V262
T649|x|item 2 — as 4 barras `height` 60→**40** & os 19 botões `top` 15→**5** (§I76b §V299). `tabStrip` (`WoD20th.lfm`) só isso, ela é `align="top"`. As 3 absolutas puxam as panes: `numStrip` + `tabHedge`/`tabPsychic`/`tabFaith` `top` **52** · `hedgeStrip` + `tabHedgePaths`/`tabHedgeRituals` `top` **242** · `vampStrip` + `tabDisc`/`tabPaths`/`tabRituals` `top` **194**. `height` de pane NENHUMA cresce · pílula 30 · gap 4 · `left` de botão INTOCADOS|V299,I76,V293,V262,V268,V281
T650|x|item 3 — a nota vira `name="noteHedgePaths"` (`WoD20.7.lfm:603`) & `renderSubTabs` (`WoD20th.lfm:1654`) ganha **2** linhas: `names["note" .. list[i]] = true` no coletor & `local note = found["note" .. grp.list[i]]; if note ~= nil then note.visible = live; end` no pintor, ao lado da pílula. `visible` autorado da nota ≡ o de `tabHedgePaths` (visível) ∴ ⊥ pisca (§V94). ⊥ tocar `SUB_TABS`, ⊥ criar função, ⊥ mexer em `.lang` nem no mapa `PT`|V300,I76,V94,V268
T651|x|gate — §V298 §V299 §V300 NOVAS, ∀ uma c/ as pernas & zero-guard citando §V209 & as mutações **&** as sondas RODADAS antes de aceitar (§V20 §V222 §B7). ⚠ §V298 ! REVOGAR §V280b & §V299 ! REVOGAR as 2 pernas verticais de §V281 no MESMO commit — deixar as velhas de pé é vermelho de propósito & tirá-las sem pôr as novas é §B7. CONFERIR (⊥ reescrever) as relações que §T647/§T648 movem os 2 lados: §V69 §V289 §V290 §V291 §V292 §V267 §V293 §V262 §V225 — nenhuma tem literal p/ editar, & se alguma pedir 1 caractere ela ⊥ era relação|V298,V299,V300,V280,V281,V209,V20,V222
T653|x|XML — os **19** `btnTab<X>` ganham `strokeColor="#00000000"` & os **19** `tabOn<X>` viram filete: `color` `DimGray`→`#00000000` + `strokeColor="#FFFFFF"` + `sides="bottom"` (§I77a §I77b §V301). `WoD20th.lfm` **11** (`tabStrip`) · `WoD20.7.lfm` **5** (`numStrip` 3 + `hedgeStrip` 2) · `WoD20.11.lfm` **3** (`vampStrip`). ⊥ tocar `left` `top` `width` `height` `xradius` `yradius` `cornerType` `hitTest` `cursor` `onClick` de retângulo NENHUM & ⊥ tocar as 4 paletas — as 2 chaves já ∃ (§V53) & `DimGray` segue viva na moldura do avatar (`WoD20.1.lfm:333`)|V301,I77,V229,V287,V53,V67
T654|x|XML+Lua — os **8** labels das 3 sub-barras ganham `name="lbl<X>"` & `fontStyle` autorado ≡ o `active` do grupo (§V94): romano em `lblDisc` `lblHedge` `lblHedgePaths`, `italic` nos 5 restantes. `renderSubTabs` (`WoD20th.lfm:1663`) ganha **2** linhas — `names["lbl" .. list[i]] = true` no coletor & `local lbl = found["lbl" .. grp.list[i]]; if lbl ~= nil then lbl.fontStyle = (live and "" or "italic"); end` no pintor, ao lado da nota de §T650. ⊥ tocar `SUB_TABS`, ⊥ criar função, ⊥ tocar os 11 labels da `tabStrip`, ⊥ tocar `.lang` nem o mapa `PT` — muda ESTILO, ⊥ texto ∴ §V10 §V22 §V28 intactos|V302,I77,V94,V300
T655|x|Lua — `pillRule` (`WoD20.6.lfm:2265`) muda ALVO & DESENHO (§I77d §V303): 3ª perna no filtro (`strokeColor` autorado ≠ `#00000000`) ∴ pega os 19 marcadores & ⊥ os 19 botões, & `pillPath` (`:2123`) larga as 2 regras das bordas longas (`ORN_PILL = {4,7}`, `:1881`) & passa a dar 2º filete + losango centrado sobre o de baixo. O path segue FILHO do alvo, `align="client"`, `mode="original"`, criado 1× & escondido por época plana (§V277) — ⊥ criar tabela nova, ⊥ tocar `ornament()` nem `ornPath`|V303,I77,V282,V277,R111
T656|x|gate — §V301 §V302 §V303 NOVAS, ∀ uma c/ as pernas & zero-guard citando §V209 & as mutações **&** as sondas RODADAS antes de aceitar (§V20 §V222 §B7). ⚠ 3 velhas cedem no MESMO commit: §V229 perde a perna `DimGray` (o chão temado & o canto quadrado FICAM), §V282 perde "as 19 pílulas" (o resto fica) & §V287 passa a admitir os 19 `btnTab<X>` no 1º sentido — o 2º (caixa de seção c/ contorno transparente ! VERMELHO) ⊥ é tocado, é ele que impede o idioma de vazar & matar o filete de §I5. CONFERIR (⊥ reescrever) §V226 §V228 §V231b §V232 §V299: coordenada nenhuma muda ∴ nenhuma delas pede 1 caractere, & se alguma pedir ela ⊥ media o que dizia medir|V301,V302,V303,V229,V282,V287,V209,V20,V222
T658|x|paleta + XML dos portadores (§I78a §V304a). **(1)** chave de fonte NOVA `["#C2A14D"]` nas **4** tabelas `font` de `THEMES` (`WoD20.6.lfm:1600`): Modern `#A83232` · Vitoriano `#D8BC72` · Idade das Trevas `#C0C6CC` · Clássico `#BFA05A`; **(2)** os **19** `tabOn<X>` perdem `sides="bottom"` & trocam `strokeColor` `#FFFFFF`→`#00000000` — `color` segue `#00000000` & `left` `top` `width` `height` `xradius` `yradius` `cornerType` `visible` INTOCADOS. `WoD20th.lfm` 11 · `WoD20.7.lfm` 5 · `WoD20.11.lfm` 3|V304,I78,V53,V67
T659|x|XML — os **8** `lbl<X>` ficam FECHADOS de vez (`fontStyle="italic"` em TODOS, `fontColor="white"`) & ganham 1 irmão `lblOn<X>` — mesmo `left/top/width/height/text/horzTextAlign/vertTextAlign/hitTest`, SEM `fontStyle`, `fontColor="#C2A14D"`. `visible` autorado OPOSTO dentro do par & ≡ o `active` do grupo (§V94): `lblOnDisc` `lblOnHedge` `lblOnHedgePaths` visíveis & os 5 `lblOn` restantes `visible="false"`; nos `lbl<X>` o inverso. ⊥ ∃ chave `.lang` nova — o `text` é o MESMO dos 2 lados ∴ §V10 §V22 §V28 intactos|V305,I78,V94,V10
T660|x|XML — os **5** `sep<X>` (§I78d): `<rectangle name="sep<X>" left="L" top="5" width="16" height="30" color="#00000000" strokeColor="#00000000" hitTest="false"/>` c/ L = fim do botão à esquerda − 6 — `WoD20.11`: `sepPaths` **136** · `sepRituals` **375** · `WoD20.7`: `sepPsychic` **156** · `sepFaith` **325** · `sepHedgeRituals` **119**. ⊥ tocar `left`/`width` de botão nenhum, ⊥ tocar o gap de 4|V306,I78,V228,V281
T661|x|Lua — o pintor da barra (§I78b §I78e §V306): `pillRule`→**`markRule`** & `pillPath`→**`markPath`** (`WoD20.6.lfm:2265` & `:2123`), a chamada em `applyTheme` passa a mandar `t.stroke[normColor("#FFFFFF")]` no lugar de `t.ornament`, & o filtro passa a ser nome — `tabOn*` **\|** `sep*` — c/ o motivo escolhido pelo nome do PAI (`tabStrip` → 2 filetes + losango · outra barra → 1 filete curto) & pelo prefixo (`sep` → losango). `renderSubTabs` (`WoD20th.lfm`) troca a escrita de `fontStyle` pela alternância de `visible` dos 2 labels do par. ⊥ tocar `ornament()`/`ornPath` (a filigrana das caixas segue em `t.ornament`), ⊥ tocar `SUB_TABS`, ⊥ criar tabela nova|V304,V305,V306,I78,V277,V57
T662|x|gate — §V304 §V305 §V306 NOVAS c/ pernas, zero-guard citando §V209 & as mutações **&** as sondas RODADAS antes de aceitar (§V20 §V222 §B7). ⚠ 3 velhas caem no MESMO commit: §V282 INTEIRA (o marcador ⊥ é mais ornamento), §V301 perde (a) & (b) — o botão sem contorno FICA, o `sides` do marcador SAI — & §V302 INTEIRA (o `fontStyle` escrito por Lua). §V303 vira §V306 & sai. CONFERIR (⊥ reescrever) §V226 §V228 §V229 §V231b §V232 §V287 §V299: coordenada nenhuma muda nesta rodada também|V304,V305,V306,V282,V301,V302,V303,V209,V20,V222
T664|x|gate — EMENDA de §V226 & §V287, as 2 que a 109ª atravessa sem revogar (§B67). **§V226**: um botão de SUB-aba carrega **2** labels (o par `lbl<X>`/`lblOn<X>` de §I78c) & os **11** da `tabStrip` seguem carregando **1** — a invariante mede que ∀ botão tem label & que ⊥ ∃ `<button>`/`<tab>` na barra, ⊥ que o número é 1. **§V287**: a lista de quem pode autorar contorno transparente passa a ser o chão + os 19 `btnTab` (108ª) + os 19 `tabOn` (§I78a) + os 5 `sep` (§I78d) + o **1** portador de enfeite do avatar (§I84d, 2026-08-26 — transparente nos 2 sentidos porque a foto ! aparecer através dele; ele ⊥ é caixa de seção, é `<rectangle>` solto ∴ o 2º sentido de §V287, que barra caixa de seção c/ contorno transparente, ⊥ é afrouxado) — o 2º sentido (caixa de seção c/ contorno transparente ! VERMELHO) ⊥ é tocado, é ele que impede o idioma de matar o filete de §I5. Mutação (§V222) das 2 RODADA antes de aceitar: 3º label num botão de sub-aba ! VERMELHO · `strokeColor="#00000000"` numa caixa de seção ! VERMELHO|V226,V287,I78,B67,V20,V222
T665|x|XML+Lua — o desenho desce (§I79 §V307). **(1)** os **27** labels de botão vão p/ `height="20"`: **11** na `tabStrip` (`WoD20th.lfm`) & os **16** dos 8 pares (`WoD20.7.lfm` 10 · `WoD20.11.lfm` 6) — `left` `top` `width` `text` & o resto INTOCADOS; **(2)** `WoD20.6.lfm`: `ORN_MARK` 7→**3** · `ORN_MARK2` 11→**7** · `ORN_MARK_RY` 3.5→**3**. ⊥ tocar `ORN_SUB` `ORN_MARK_GAP` `ORN_PILLR`, ⊥ tocar retângulo nenhum, ⊥ tocar as 4 paletas|V307,I79,V305,V301
T666|x|gate — §V307 NOVA c/ as 2 pernas, zero-guard citando §V209 & as mutações **&** a sonda RODADAS antes de aceitar (§V20 §V222), ⚠ a de (a) nos **2** lados: label de volta a 30 **&** `ORN_MARK2` de volta a 11, cada uma sozinha ! acender. As 3 constantes ! ser LIDAS de `WoD20.6` — soletrá-las no gate faz a invariante concordar consigo mesma p/ sempre (§B7). CONFERIR (⊥ reescrever) §V226 §V228 §V301 §V304 §V305 §V306: nenhuma mede altura de label|V307,I79,V209,V20,V222
T668|x|item a — vão 15→**10** nas **4** abas que têm régua p/ reconciliar (§I76a §V298 EMENDADAS, ⊥ renumeradas). `WoD20.1`: cabeçalho `width` 1030→**1020** c/ as 2 colunas de dentro `left` 336→**331** & 683→**673** (a 3ª fecha em 1000 ∴ margem direita segue 20, §V280a) · 2ª caixa `left` **1030** · fileira 2 `top` **135** & `left` **350**·**690**·**1030** · avatar `top` **451** (`height` 403 INTACTA) · `top` **506**·**664** · col B fica 5 curta de novo (3 caixas = 2 vãos, contra 1 das outras) ∴ `HUMANITY` `height` 185→**190** (§V69) · o mapa do comentário (`:38`) passa a fechar em **x=1360 / y=854** & `HEALTH` segue 1 abaixo, em 855. `WoD20.2`: fileira de baixo `left` **380**·**630**·**880**·**1130**·**1380** & `top` **321**·**517** · `Merit` **380**/**490** · `Flaw` **880**/**490** · `DERANGEMENTS` **1380**/240 (§V290b) · template `Merit` 455→**450** c/ `merit_` 225→**220** & `book_`/`type_`/`costy_` em **220**·**300**·**400**, os 4 rótulos das 2 caixas em **20**·**240**·**320**·**420** (§V290a) & as **20** linhas `width` 455→**450** · `BACKGROUNDS` `height` 713→**703** (§V289, fecha c/ `RESOURCES`) & `BACKGROUND_ROWS` fica **21** — a 21ª fecha em 666 & sobram 37 de margem ∴ ⊥ cai linha nenhuma nem nasce órfão, ao contrário de §T647 (§V145 §V204 §V128 §V280a). `WoD20.3`: `top` **346**·**361**·**492** · `left` **430**·**760** · as 2 caixas de 315 → **320** (seguem fechando em 750 c/ `COMBAT`) & os 2 títulos 275→**280**. `WoD20.4`: `top` **135**·**341**·**547** · `Fears` `left` **245** `width` **245** c/ rótulo **205** & `textEditor` **175** (a caixa da direita absorve, ≡ §T647) · `Prelude` `left` **500** `width` **680** `height` **608**, rótulo **640** & `richEdit` `width` **610** `height` **547** (fecha em 588 ∴ margem de baixo = 20 CRAVADO) · cabeçalho & as 3 colunas INTOCADOS — ele já fecha em 1180 c/ `Prelude` (§V291)|V298,I76,V69,V289,V290,V291,V280,V145,V204,V128
T669|x|item b — vão 15→**10** nos **10** forms restantes, reflow sem régua nova. `WoD20.5` & `WoD20.10` `left` **450**·**900** · `WoD20.6` `Credits` `top` **155** (§V292 ⊥ é tocada — as 2 caixas já fecham no mesmo x) · `WoD20.8` `left` **430**·**860** & `top` **331** · `WoD20.9` `top` **125** · `WoD20.11` `BLOOD POOL` `left` **670** `width` **400** & rótulo 355→**360**, as **20** bolinhas ⊥ são tocadas (≡ §T648) · `WoD20.12` `DISCIPLINES` `top` **191** `height` 441→**446** & `DESCRIPTION` `left` **400** `width` **670** c/ rótulo 625→**630** & `edtDiscDesc` 605→**610** · `WoD20.13` `SECONDARY PATHS` `top` **216** `height` 441→**446** & `DESCRIPTION` `left` **575** `width` **495** c/ rótulo 450→**455** & `edtPathDesc` 430→**435** · `WoD20.14` `DESCRIPTION` `left` **530** `width` **540** c/ rótulo 495→**500** & `edtRitualDesc` 475→**480** · `WoD20.7` banda `left` **596**·**1008** c/ as 2 esticadas `width` **402** as duas & títulos **362** (§V267), as 10 bolinhas de `QUINTESSENCE` `left` **58**·**87**·**116**·**145**·**174**·**203**·**232**·**261**·**290**·**319** ((402−286)/2) & o `<layout>` de 250 da `WILLPOWER` `left` **76** ((402−250)/2), as 2 `DESCRIPTION` de Hedge `left` **596** `width` **814** c/ títulos **774** & `edtHedgeDesc` **&** `edtHedgeRitualDesc` **754** (os 2 — trocar 1 só deixa §V239 vermelha), `tabPsychic` `DESCRIPTION` `left` **460** `width` **950** c/ título **910** & `edtPsychicDesc` **890**, `tabFaith` `DESCRIPTION` `top` **106** (`height` 594 INTACTA). ⚠ nos `WoD20.12` `.13` `.14` o `top`=20 do 1º filho ⊥ é tocado (§V286b) & em `WoD20.7` a `hedgeStrip` `top`=190, o vão de 12 acima dela & o de 4 em `WoD20.11` ⊥ são tocados — §I76a os nomeia FORA do escopo (§V293 §V299)|V298,I76,V286,V293,V267,V262,V225,V239,V292
T670|x|gate — §V298 EMENDADA (15→**10**): os **3** literais do check (as 2 mensagens de FAIL & a de Pass) & o bloco de comentário que soletra "20 -> 15". ⊥ nasce §V, ⊥ nasce id — a perna, o escopo & o zero-guard ficam de pé como estão, & é por isso que ⊥ ∃ velha p/ revogar nesta rodada (§T664 é o precedente, §T651 é o contraste). Mutação obrigatória ANTES de aceitar (§V20 §V222): 1 vão de volta a **15** ! acender — a de 20 ⊥ serve, check que ainda aceita o número da rodada passada é o no-op de §B7 · sonda: mudar a margem INTERNA de 1 caixa ! VERDE aqui & VERMELHO em §V280a. CONFERIR (⊥ reescrever) as relações que §T668/§T669 movem dos 2 lados: §V69 §V289 §V290 §V291 §V292 §V267 §V293 §V262 §V225 §V145 §V280a — nenhuma tem literal p/ editar, & se alguma pedir 1 caractere ela ⊥ era relação. ⚠ o gate ⊥ guarda coordenada nenhuma de §T668/§T669 (conferido: `1375` `864` `1645` `713` `1030` `1045` = 0 ocorrências; `1410` só ∃ dentro de comentário)|V298,I76,V209,V20,V222,V280
T672|x|XML+Lua — as sub-barras voltam ao estado da 109ª (§I80b §I80c §V307 §V308). **(1)** os **16** labels dos 8 pares voltam a `height="30"` (`WoD20.7.lfm` 10 · `WoD20.11.lfm` 6) — os **11** da `tabStrip` FICAM em 20; **(2)** `WoD20.6.lfm`: 3 constantes NOVAS — `ORN_SUB_MARK` = **7** (ao lado de `ORN_SUB`) & `ORN_SEP_RX` = **4** & `ORN_SEP_RY` = **3.5**; **(3)** o ramo `sub` de `markPath` troca `ORN_MARK` por `ORN_SUB_MARK` & o ramo `sep` troca `ORN_MARK_RX`/`ORN_MARK_RY` por `ORN_SEP_RX`/`ORN_SEP_RY`. O ramo `tab` ⊥ é tocado, `ORN_SUB` ⊥ é tocado, retângulo nenhum é tocado|V307,V308,I80,I79
T673|x|gate — §V307 EMENDADA p/ medir por NÍVEL (⊥ renumerada, ≡ o precedente de §T664/§T670) & §V308 NOVA. §V307 passa a ter **3** pernas: (a) os **11** da `tabStrip` c/ a régua da 110ª (fim do label ≤ `h` − max(`ORN_MARK2`, `ORN_MARK`+`ORN_MARK_RY`) − **3**) · (b) os **8** de sub-barra c/ label `height="30"` & o filete em `ORN_SUB_MARK`, o estado que o user aprovou — a perna existe p/ que "consertar" isto exija PEDIDO (§B61) · (c) os 2 labels de um par medem igual. §V308 c/ zero-guard citando §V209. Mutações & sondas das 2 RODADAS antes de aceitar (§V20 §V222), ⚠ as de §V307 nos 2 lados & nos 2 NÍVEIS: label de sub-aba a 20 ! VERMELHO por (b) · label de aba a 30 ! VERMELHO por (a)|V307,V308,I80,V209,V20,V222
T675|x|item a — vão 10→**5** nas **4** abas que têm régua p/ reconciliar (§I76a §V298 EMENDADAS no lugar, 4ª vez). `WoD20.1`: cabeçalho `width` 1020→**1010** c/ as 2 colunas de dentro `left` 331→**326** & 673→**663** (a 3ª fecha em 990 ∴ margem direita segue 20) · 2ª caixa `left` **1015** · fileira 2 `top` **130** & `left` **345**·**680**·**1015** · avatar `top` **441** (`height` 403 INTACTA pela 2ª rodada seguida — o avatar só desliza) · `top` **496**·**649** · col B fica 5 curta de novo ∴ `HUMANITY` `height` 190→**195** (§V69) · o mapa do comentário (`:38`) passa a fechar em **x=1345 / y=844**, `HEALTH` em 845. `WoD20.2`: fileira de baixo `left` **375**·**620**·**865**·**1110**·**1355** & `top` **316**·**507** · `Merit` **375**/**485** · `Flaw` **865**/**485** · `DERANGEMENTS` **1355**/240 (§V290b) · template `Merit` 450→**445** c/ `merit_` 220→**215** & `book_`/`type_`/`costy_` em **215**·**295**·**395**, os 4 rótulos das 2 caixas em **20**·**235**·**315**·**415** (§V290a) & as **20** linhas `width` 450→**445** · `BACKGROUNDS` `height` 703→**693** (§V289) & `BACKGROUND_ROWS` fica **21** — a 21ª fecha em 666 & sobram **27** de margem ∴ ⊥ cai linha (§V145 §V280a). `WoD20.3`: `top` **341**·**356**·**482** · `left` **425**·**755** · as 2 caixas de 320 → **325** (seguem fechando em 750 c/ `COMBAT`) & os 2 títulos 280→**285**. `WoD20.4`: `top` **130**·**331**·**532** · `Fears` `left` **240** `width` **250** c/ rótulo **210** & `textEditor` **180** · `Prelude` `left` **495** `width` **685** `height` **598**, rótulo **645** & `richEdit` `width` **615** `height` **537** — fecha em 578 ∴ margem de baixo **20 CRAVADO** pela 2ª rodada seguida, ⊥ ∃ folga p/ um 5º aperto sem encolher o texto · cabeçalho & as 3 colunas INTOCADOS (§V291)|V298,I76,V69,V289,V290,V291,V280,V145
T676|x|item b — vão 10→**5** nos **10** forms restantes, reflow sem régua nova. `WoD20.5` & `WoD20.10` `left` **445**·**890** · `WoD20.6` `Credits` `top` **150** (§V292 intacta) · `WoD20.8` `left` **425**·**850** & `top` **326** · `WoD20.9` `top` **120** · `WoD20.11` `BLOOD POOL` `left` **665** `width` **405** & rótulo 360→**365**, as 20 bolinhas ⊥ tocadas · `WoD20.12` `DISCIPLINES` `top` **186** `height` 446→**451** & `DESCRIPTION` `left` **395** `width` **675** c/ rótulo 630→**635** & `edtDiscDesc` 610→**615** · `WoD20.13` `SECONDARY PATHS` `top` **211** `height` 446→**451** & `DESCRIPTION` `left` **570** `width` **500** c/ rótulo 455→**460** & `edtPathDesc` 435→**440** · `WoD20.14` `DESCRIPTION` `left` **525** `width` **545** c/ rótulo 500→**505** & `edtRitualDesc` 480→**485** · `WoD20.7` banda `left` **591**·**1003** c/ as 2 esticadas `width` **407** as duas & títulos **367** (§V267), as 10 bolinhas de `QUINTESSENCE` `left` **60**·**89**·**118**·**147**·**176**·**205**·**234**·**263**·**292**·**321** ((407−286)/2) & o `<layout>` de 250 da `WILLPOWER` `left` **78**, as 2 `DESCRIPTION` de Hedge `left` **591** `width` **819** c/ títulos **779** & `edtHedgeDesc` **&** `edtHedgeRitualDesc` **759** (os 2), `tabPsychic` `DESCRIPTION` `left` **455** `width` **955** c/ título **915** & `edtPsychicDesc` **895**, `tabFaith` `DESCRIPTION` `top` **101**. ⚠ o `top`=20 dos 3 forms de sub-aba ⊥ é tocado (§V286b) & a `hedgeStrip` `top`=190, o vão de 12 acima dela & o de 4 em `WoD20.11` ⊥ são tocados — §I76a os nomeia FORA (§V293 §V299). ⚠ a 111ª mexeu em `WoD20.7` `WoD20.11` `WoD20.6` `WoD20th` (rótulo de barra & ornamento) ∴ CONFERIR que o reflow ⊥ pisa nos `lbl<X>`/`lblOn<X>`/`sep<X>` — eixos diferentes, & se pisar é §B|V298,I76,V286,V293,V267,V262,V225,V292
T677|x|gate — §V298 EMENDADA (10→**5**): **1** check só, as 2 comparações + a mensagem de Pass + os comentários que soletram o número. É o JURO de §B70 pago de volta — na 110ª esta MESMA emenda acendeu **6** FAIL em 2 donos extras (§V259 & a perna ⊥ declarada de §V262), & agora ⊥ ∃ 2º dono p/ discordar. ⚠ ANTES de aceitar, GREP do gate pelo literal VELHO (`10`) em contexto de vão **&** pelo NOVO — ⊥ ter feito exatamente isso é o que abriu §B70, & a lição só vale se virar passo. Mutação obrigatória (§V20 §V222): 1 vão de volta a **10** ! acender · sonda: mudar a margem INTERNA de 1 caixa ! VERDE aqui & VERMELHO em §V280a — & esta sonda passa a valer 4×, ⊥ 2×. CONFERIR (⊥ reescrever) as relações que §T675/§T676 movem dos 2 lados: §V69 §V289 §V290 §V291 §V292 §V267 §V293 §V262 §V225 §V145 §V280a|V298,I76,V209,V20,V222,V280
T679|x|Lua — `WoD20.6.lfm`: `ORN_MARK2` 7→**9** & `ORN_MARK` 3→**5** (§I81 §V307a). ⊥ tocar `ORN_MARK_RX` `ORN_MARK_RY` `ORN_MARK_GAP` `ORN_SUB` `ORN_SUB_MARK` `ORN_SEP_RX` `ORN_SEP_RY` `ORN_PILLR`, ⊥ tocar `.lfm` de XML nenhum, ⊥ tocar as 4 paletas. É a rodada mais curta desde a 16ª: **2** números|V307,V308,I81
T680|x|gate — a folga de §V307a passa de **3** p/ **1**: **1** literal (`$clear307`) & a mensagem de Pass que o soletra. ⊥ nasce §V & ⊥ nasce id — a relação, o zero-guard & as pernas (b) & (c) ficam de pé (§T664 §T670 são o precedente). Mutação obrigatória ANTES de aceitar (§V20 §V222): `ORN_MARK2` de volta a **7** ! acender — a de 11 ⊥ serve, check que ainda aceita o número da rodada passada é o no-op de §B7 · sonda: `ORN_SUB_MARK` ! VERDE em §V307a & §V308, que é o que prova que a folga nova ⊥ desceu p/ o nível de baixo|V307,V308,I81,V209,V20,V222
T682|x|item 1 — SPECIALTIES ganha o 7º picker (§I82a). `WoD20th.lfm`: `SPECIALITY_ROWS` 9→**10** (`:2059`), `SPECIALITY_FREE_ROWS` fica **3** ∴ os 3 loops de §V147 & `grantSpeciality` acompanham sozinhos. `WoD20.1.lfm`: a linha **7** troca `SpecialityFreeRow`→**`SpecialityRow`** & nasce a **10ª** (`<layout left="35" top="311" width="260" height="25"><SpecialityFreeRow num="10"/></layout>`) ∴ picker 1…7 & livre 8·9·10, & `cboSpeciality7` passa a ∃ (§V147 conta os combos por `SPECIALITY_ROWS − SPECIALITY_FREE_ROWS`). Caixa `SPECIALTIES` `height` 348→**356**: o conteúdo fecha em 336 ∴ margem de baixo **20 CRAVADA** (§V280a), ⊥ sobra folga p/ uma 11ª sem mover a régua de novo. §V69 SOBE: a linha 844→**852** ∴ `HUMANITY` `height` 195→**203** & a `<image>` do avatar `height` 403→**411** (`top` 441 fica) · o literal `$HEALTH_TEN_ROW_OVERHANG` do gate vai 1→**-7** NESTA passada, senão §V69 fica vermelha entre tarefas · mapa do comentário (`:36`) fecha em **y=852** c/ `avatar 441..852` · `HUMANITY+WILLPOWER 649..852` · `SPECIALTIES 496..852` · `HEALTH 496..845`. ⚠ a MOLDURA do avatar (`:333`) ⊥ é tocada aqui — ela é de §T685, & é de propósito: mexer nos 2 na mesma passada esconderia qual dos 2 §T consertou §B71. ⚠ `speciality_7` MUDA de natureza (§I82a §V14 §V17): ficha salva c/ texto livre ali abre num combo|I82,V69,V146,V147,V280,V14,V17,I3
T683|x|item 2 — o rabo da caixa de vitalidade (§I82b). ⚠ CORRIGIDA na 112ª ao buildar (§B72) — a redação ANTERIOR mandava `HEALTH_BOX_PAD` 79→**49** & alturas →**319**, & isso o gate REPROVA na hora: o rabo vale `pad − 37` em ∀ tamanho de trilha (⊥ os 55 que §I82b escreveu — são **42**) ∴ 49 deixaria **12** sob a última linha & §V280a pede **20** nos 4 lados. O delta do pedido (−30) ⊥ cabe; o corte máximo é **−22**. `WoD20th.lfm`: `HEALTH_BOX_PAD` 79→**57** (`:1234`) ∴ o rabo cai de **42** p/ **20**, o piso de §V280a, & `HEALTH_ROW_PITCH` **27** ⊥ é tocado — a caixa segue respondendo ao tamanho da trilha exatamente como responde hoje (§V49, §I82b). As **2** alturas declaradas caem 349→**327** (`dynHealth_box` em `WoD20.1.lfm:563` & `dynHealth3_box` em `WoD20.3.lfm:261`) — §V49 já cobra `10 × pitch + pad` nas duas & acende sozinha se uma ficar p/ trás. `$HEALTH_TEN_ROW_OVERHANG` vai p/ **-29** (`HEALTH` fecha em 823 c/ a linha de §T682 em 852) ∴ a EXCEÇÃO de §V69 troca de lado: era ABAIXO desde a 11ª, passa a ser ACIMA. ⚠ se esta rodar ANTES de §T682 o literal passa por **-21** & só depois chega a -29 — cada passada ! deixá-lo certo|I82,V49,V69,V280,V284,V40,B72
T684|x|item 3 — a 11ª linha de COMBAT (§I82c). `WoD20.3.lfm`: nasce `<layout left="35" top="316" width="680" height="25"><Attack num="11"/></layout>`, IDÊNTICA às 10 (mesmo `left` `width` `height`, pitch 25, mesmo template) · caixa `COMBAT` `height` 336→**361** (a 11ª fecha em 341 ∴ margem 20, ≡ a que a 10ª já tinha) · a coluna esquerda desce junto: `ARMOR` `top` **366** · `VIRTUES` `top` **366** · `WILLPOWER` `top` **507** ∴ os 3 vãos seguem **5** (§V298). Nascem **8** campos (`attack_11` `roll_11` `difficulty_11` `damage_11` `range_11` `rate_11` `clip_11` `conceal_11`) & §I3 passa a N=1…11. ⊥ ∃ Lua p/ tocar: as linhas de COMBAT são XML puro — sem `declareTrait`, sem XP, sem constante de contagem (⊥ ∃ `ATTACK_ROWS`) ∴ ⊥ nasce §V de contagem, & isso é DECLARADO: inventar uma aqui seria a régua sem 2º lado de §B64|I82,I3,V298,V280,V1
T685|x|item 4 — a moldura do avatar volta a ser moldura (§I82d §B71). ⚠ DEPOIS de §T682, que é quem move a imagem p/ `height="411"`. `WoD20.1.lfm:333`: `top` 471→**441** & `height` 403→**411** (passa a copiar a `<image>` de `:334` nos 4 números) · `xradius`/`yradius` 5→**14**, ≡ o contorno de ∀ caixa da ficha · `left` 0 & `width` 340 já batem · `cornerType="innerRound"` já ∃ · `color="DimGray"` **FICA** — é a única portadora da chave nas 4 paletas (§V53, declarado em §T653) & trocar p/ `black` a mataria; se o cinza do letterbox incomodar na tela, é §T novo & ⊥ §B (§T688). Ela SEGUE `<rectangle>` solto ∴ ⊥ entra nos 73 de §I73 & ⊥ ganha a filigrana — DECIDIDO pelo user 2026-08-26 contra virar caixa (a foto encolheria p/ 300×371). ⚠ o comentário de `:329` ainda diz "355 tall, not 405" & "the preview ends on y=760": as 2 frases são de 3 réguas atrás & saem junto|I82,B71,V309,V69,V53
T686|x|gate — §V309 NOVA (a moldura copia a imagem nos 4 números) c/ zero-guard citando §V209 & as **2** mutações RODADAS antes de aceitar (§V20 §V222): moldura de volta a `top="471"` ! VERMELHO **&** mover a IMAGEM deixando a moldura ! VERMELHO — relação que só acende de 1 lado é literal disfarçado (§T637). Sonda: mudar `xradius` da moldura ! VERDE, §V309 é de POSIÇÃO. §V69 EMENDADA (⊥ renumerada, ≡ §T664 §T670 §T677): o literal `$HEALTH_TEN_ROW_OVERHANG` fica, o SINAL vira, & o Pass ! parar de dizer "hangs below" p/ um número negativo — verde que diz mais do que mediu é §V209. CONFERIR (⊥ reescrever) §V49 §V146 §V147 §V40 §V298 §V280 §V284 §V285: §V49 cobra as 2 alturas de `HEALTH` sozinha, §V146/§V147 cobram as 10 linhas de speciality sozinhas, & se alguma pedir 1 caractere ela ⊥ era relação. ⚠ GREP do gate pelos literais que estas 4 tarefas movem ANTES de aceitar (`348` `403` `471` `336` `349` `79`) — ⊥ ter feito isso é o que abriu §B70|V309,V69,I82,V209,V20,V222
T688|x|MECANISMO do filtro de ritual por nível de caminho (§I83), ATÔMICA pelo motivo de §T605: tabela sem ramo é dado morto (§B55) & ramo sem entrada na geração do memo serve lista velha CALADO (§B48) ∴ os 5 sítios no MESMO passo. **RAIZ** (`WoD20th.lfm`): `HEDGE_RITUAL_PATH` NOVA & VAZIA ao lado de `AFFINITY`/`CASTING_ATTR` (`:321`) · `hedgePathLevels()` NOVA & GLOBAL, varredura das **17** `numina_*` c/ `traitLevel` & maior-ganha, ≡ `discLevels` (`:2956`) · `pickAllowed` (`:3191`) ganha o **4º** parâmetro `hedge` & o ramo `^hedgeRitual_%d+$` → caminho ausente na tabela devolve `true`, caminho na ficha compara `nível ≥ prefixo`. **`WoD20.6`**: `FILTER_ROW` ganha `hedgeRitual = true` (`:1059`) · `memo` ganha o campo `hedge` & o bloco de reset (`:1170`) ganha `memo.hedge = hedgePathLevels();` · as **2** chamadas de `pickAllowed` dentro de `pickerItems` (a do `odd` & a do laço) passam `memo.hedge` · `renderHedgePickers` (`:1284`) passa de 2 p/ **14** nomes. **`WoD20.7`**: `<dataLink>` NOVO c/ os 17 `numina_<i>` & as 85 `numina_<i>_<d>` → `renderHedgePickers(self)`, PRÓPRIO (§V206). ⚠ §V223 — `hedgePathLevels` é GLOBAL ∴ ordem ⊥ pesa, mas ela ! ser global de verdade (`local` aqui compila `GETGLOBAL` nil em `WoD20.6`, §B51 §B9). ⚠ nesta rodada ⊥ ∃ mudança de TELA: a lista está vazia (§I83b) ∴ a prova é `rdk -l` + o gate de §T689, ⊥ screenshot|I83,V310,V205,V200,V203,V206,V223,V269,B48,B55,B51
T689|x|gate — §V310 NOVA (6 pernas + zero-guard). Mutação RODADA antes de aceitar em CADA perna (§V20 §V222), as 7 que §V310 lista: `FILTER_ROW` sem `hedgeRitual` · `cbohedgeRitual_1` em `FILTER_NAME` · `memo.hedge` fora do bloco de reset · 1 chamada de `pickAllowed` sem o 4º argumento · `discLevels()` no lugar de `hedgePathLevels()` · `<dataLink>` sem as 85 bolinhas · sonda VERDE = ritual novo na lista COM entrada na tabela. ⚠ a perna (a) tem 2 REGIMES (tabela vazia = legal enquanto a lista estiver vazia; lista cheia = #chaves = #itens − 1 & prefixo obrigatório) & os 2 ! nascer no mesmo check, senão §T690 enche a lista & o gate segue verde sem medir nada (§B7)|V20,V222,V209,V310,I83
T692|x|itens 1 & 2 — o topo de `WoD20.3` fecha numa linha só (§I84a §I84b). `COMBAT TRAITS` `height` 351→**361** (fecha em 361, ≡ `COMBAT`) & `dynHealth3_box` `top` 356→**366** (abre ≡ `VIRTUES`) ∴ o vão `COMBAT TRAITS`→`HEALTH` segue **5** (§V298) & `ARMOR` `VIRTUES` `HEALTH` abrem as 3 em 366. `height` de `HEALTH` (**327**) & `left` de caixa NENHUMA são tocados. As 2 letras vão JUNTAS: `HEALTH` em 366 c/ `COMBAT TRAITS` em 351 daria vão **15** & §V298 acende — pedir uma sem a outra é pedir vermelho. `COMBAT TRAITS` ganha 10 de rabo interno (§V280a cobra piso, ⊥ igualdade ∴ VERDE)|I84,V298,V280,V40
T693|x|item 4 — o enfeite do avatar passa por CIMA da foto (§I84d). Nasce **1** `<rectangle>` em `WoD20.1.lfm`, declarado DEPOIS da `<image field="avatar">`, c/ os MESMOS `left="0" top="441" width="340" height="411"` & `color="#00000000" strokeColor="#00000000" cornerType="innerRound"` c/ o raio da moldura (**14**) ∴ o path que `ornament()` pendura nele cai ACIMA da foto. ⊥ nasce regra de pintura: `THEME_SHAPES = { rectangle = true }` (`WoD20.6.lfm:1797`) já alcança qualquer `rectangle` & `applyTheme` já chama `ornament(c, fill, t.ornament)` — é por isso que o enfeite JÁ ∃ hoje na moldura, só que atrás. ⊥ tocar a moldura de baixo (`:341`), ⊥ tocar a `<image>`, ⊥ tocar `ornament()` nem `ornPath`, ⊥ tocar as 4 paletas. ⚠ o enfeite só aparece no VITORIANO (`t.ornament` é a única paleta que o declara, §V277) ∴ nas outras 3 épocas o avatar ⊥ muda — isso é o esperado, ⊥ defeito. ⚠ a MÁSCARA do pedido ⊥ entra: SDK3 ⊥ tem recorte & nenhum `style` corta (§R115), & hoje ⊥ vaza nada — DECIDIDO manter `autoFit` c/ a barra cinza|I84,R115,V309,V287,V277,V53
T694|x|gate — §V311 NOVA (o rabo da vitalidade ≡ a margem do rótulo, §I84c) & **2** EMENDAS (⊥ renumeradas, ≡ §T664 §T670 §T677 §T686): §V309 passa de 2 p/ **3** controles (moldura · foto · portador) & §V287 admite o portador na lista de quem autora contorno transparente, SEM afrouxar o 2º sentido dela. ⚠ item 4 do pedido ⊥ TEM código nesta tarefa & item 3 ⊥ tem código em NENHUMA: o `HEALTH_BOX_PAD`=**57** que a 112ª acertou (§B72) já satisfaz a relação — o que falta é ela ser MEDIDA, senão o 57 volta a ser literal & a próxima rodada o move sem ninguém reclamar. Mutações RODADAS antes de aceitar (§V20 §V222): §V311 nos 2 lados (`HEALTH_BOX_PAD` p/ 79 ! VERMELHO **&** rótulo `HEALTH` p/ `top="30"` ! VERMELHO) · §V309 c/ o portador movido sozinho ! VERMELHO · sondas: `HEALTH_ROW_PITCH` ! VERDE em §V311 & VERMELHO em §V49 · `xradius` do portador ! VERDE em §V309. CONFERIR (⊥ reescrever) §V49 §V280 §V298 §V69 §V284 §V285|V311,V309,V287,I84,V209,V20,V222
T696|x|`WoD20.7.lfm` — o buraco sai & a banda encolhe (§I85). **(a)** `HEDGE MAGIC`: `Affiliation` (o `<label>` **&** o `cboHedgeAffiliation` **&** o `edtHedgeAffiliation` — são **3** controles no mesmo `top`, & mover 2 deixa o espelho de §I71/§V274 torto) `top` 78→**41** · `Essence` (label + edit) 106→**69** · `Casting Attribute` (label + `cboHedgeAttr`) 134→**97**. `left` `width` `height` de controle NENHUM muda ∴ §V16 §V196 §V211 §V274 intactas; **(b)** as **3** caixas da banda `height` 178→**146** (§V267a); **(c)** as 10 bolinhas de `QUINTESSENCE` `top` 107→**91** ((146+36)/2) & o `<layout>` de 250 da `WILLPOWER` `top` 97→**81** ((146+16)/2) — os 2 CAEM de §V267b, ⊥ são escolhidos; **(d)** `hedgeStrip` `top` 190→**158** & `tabHedgePaths`/`tabHedgeRituals` `top` 242→**210** (§V293 §I73, 12 dos 2 lados). ⊥ tocar `tabHedge` `height`=748 (§V262a — `tabFaith` precisa de 705 & os 3 painéis dividem 1 retângulo exato), ⊥ tocar `left`/`width` de nada, ⊥ tocar `numStrip` nem `tabPsychic`|I85,V267,V280,V293,V262,V274,V196
T697|x|gate — ⊥ ∃ check NOVO & ⊥ ∃ literal p/ editar, & isso é o RESULTADO, ⊥ preguiça: §V267a (as 3 iguais), §V267b (a centragem, c/ `bodyTop` derivado do título), §V280a (piso de 20) & §V293 (os 2 lados da barra) já forçam ∀ número de §T696 — o `178` só ∃ em COMENTÁRIO no gate (conferido: 1 ocorrência, linha 5871) & o 146 ⊥ é escrito em lugar nenhum. O trabalho é PROVAR isso antes de aceitar (§V20 §V222): rodar as mutações de §V267b nos **2** lados (bolinha em `top=107` c/ banda 146 ! VERMELHO · banda de volta a 178 c/ bolinha em 91 ! VERMELHO) · a de §V293 (`hedgeStrip` em 190 c/ banda 146 ! VERMELHO por CIMA) · a de §V280a (`WILLPOWER` c/ banda 144 ! VERMELHO, que é o que prova que 146 é PISO & ⊥ gosto). ⚠ se alguma dessas 4 sair VERDE, a invariante que eu disse que cobria ⊥ cobria, & aí NASCE §V — é o passo que §B70 comprou|I85,V267,V280,V293,V20,V222
T699|x|item 1 — o conteúdo das 2 caixas ESTICADAS sobe 21px (§I86a §I86b §I86c). `WoD20.7.lfm`: as **10** `imageCheckBox` `quint_1`…`_10` `top` 91→**70** (`left` 60·89·118·147·176·205·234·263·292·321 & `width`/`height` 25 ⊥ mudam) · o `<layout left="78" top="81" width="250" height="45">` de `WillpowerNumina` `top` 81→**60**. ⚠ ATÔMICA c/ o gate: §V267b é a régua que hoje ACEITA 91/81 & vai RECUSAR 70/60 — mover o XML sem mover o datum deixa o gate vermelho, & mover o datum sem o XML também (§B7 nos 2 sentidos). O datum de `V240`/`$bodyTop` deixa de ser `título.bottom + 1 + 20` & passa a ser a margem da caixa (**20**), c/ o pixel ímpar EMBAIXO. ⊥ tocar `height` nem `width` de caixa nenhuma (pedido user em letra)|I86,V267,V240,V280,V239,B7
T700|x|item 2 — a régua ganha eixo de `fontSize` & os 4 campos estreitam (§I86d §I86e §I86f, §V312, destravado por §R116). **gate**: `$PX_PER_CHAR` 6,5 vira par — 6,5 padrão & **6,0** p/ controle c/ `fontSize="12"` — lido por `NeededPx`/§V16 & por §V196 no MESMO caminho, ⊥ num 3º sítio. **`WoD20.7.lfm`**: `cboHedgeAffiliation` · `edtHedgeAffiliation` · `<edit field="hedgeEssence">` · `cboHedgeAttr` `width` 386→**360** ∴ os 3 fecham em **540** (§V297b segue verde por RELAÇÃO). ⚠ a régua ! descer ANTES do XML: c/ 6,5 o mínimo é 382 & 360 acende §V196 na hora. ⊥ encolher combo nenhum dos outros 13 de 12pt (§V312c)|I86,V312,V196,V297,V16,R116,V274
T701|x|gate — §V312 NOVA (4 pernas + zero-guard) & §V267b EMENDADA (datum 61→20, pixel ímpar embaixo). Mutação RODADA antes de aceitar em CADA uma (§V20 §V222): as 5 de §V312 · p/ §V267b, devolver `quint_*` p/ `top=91` ! VERMELHO · subir `WillpowerNumina` p/ `top=59` (19 do título) ! VERMELHO por §V280a · pôr o pixel ímpar EM CIMA (71/61) ! VERMELHO · sonda: mudar o `left` das bolinhas ! VERDE, esta régua é do eixo Y. ⚠ o contador `$stretchSeen -ne 2` ⊥ muda — as caixas esticadas seguem 2|V20,V222,V209,V312,V267,V240,I86
T703|x|`WoD20.6.lfm` `THEMES` — a paleta `["Dark Ages"]` (`:1753`) ganha **2** chaves: `ornament = "#C0C6CC"` (a MESMA prata do contorno, escolha user 2026-08-26 — ⊥ o molde vitoriano de "mais claro que a moldura") & `ornStyle = "filete"` (§I87a). ⊥ tocar as outras 3 paletas & ⊥ dar `ornStyle` à Vitoriana — ela cai no default, que é o desenho de §I72. §V63 cobra as **8** obrigatórias & ⊥ proíbe a 9ª nem a 10ª ∴ ⊥ é emendada. ⚠ **SOZINHA esta linha deixa as 73 caixas c/ a FILIGRANA vitoriana pintada de prata** — quem dá o desenho novo é §T704 ∴ as 2 na MESMA rodada, & se a rodada abortar no meio o estado intermediário é VISÍVEL (⊥ é gate vermelho, é tela errada)|I87,V313,V63,V53
T704|x|`WoD20.6.lfm` — `ornPath(w, h, style)` despacha & nasce o ramo `filete` (§I87b §I87c). **3** constantes NOVAS ao lado das de §I72: `ORN_CROSS`=**3** · `ORN_CROSS2`=**5** · `ORN_CROSS_AT`=**26**. Desenho: 1 corrida FECHADA a `ORN_IN`=9 da borda c/ os 4 cantos em arco de raio `ORN_BOXR + ORN_IN`=**23** (côncavo — reusa `ornArc`, & offset ao contrário CRUZA o contorno, §I72a) · **8** cruzes de meio-braço 3 a **26** do canto, **2 por canto**, 1 em cada corrida · **2** cruzes de meio-braço 5 no meio do TOPO & da BASE, laterais SEM motivo. Cruz = 2 pares `M`/`L`, line work sem fill ≡ o resto, & ∀ decimal por **`ornNum`** — `string.format` c/ verbo float lê `LC_NUMERIC` & host pt-BR escreve VÍRGULA, que parte cada coordenada em 2 & mata o path INTEIRO, só na máquina do jogador brasileiro (§I72b). ⊥ tocar `ornCorner` `ornEdge` `ornVolute` `ornLeaf` `ornDot` nem `ORN_OUT`/`ORN_ARM`/`ORN_FLEUR`. ⚠ **⊥ pôr `return ""` por tamanho neste ramo**: o que cai por falta de espaço é a CRUZ, nunca o filete (§V314e, ≡ §T613 & §B59)|I87,V314,V313,I72,B59
T705|x|`WoD20.6.lfm` — o memo carimba o ESTILO (§I87d §I87e). `ornPainted[handle]` passa de `{p, w, h}` p/ `{p, w, h, s}` · a CRIAÇÃO grava `s` · a 3ª porta de `ornament()` (`:2325`) passa a abrir por medida **OU** por estilo & regrava os 3 · o estilo em vigor vira global de módulo escrito no MESMO ponto que `ornColour = t.ornament` (`:2519`), & `ornament()` o lê de lá em vez de receber 6º argumento — o refresh de §I72e roda FORA da travessia & ⊥ tem paleta à mão. ⚠ **sem esta tarefa §T703+§T704 saem c/ `rdk -l` 0 & gate VERDE & a tela ERRADA**: trocar de época na ficha aberta ⊥ move 1px de caixa nenhuma ∴ o memo devolve a filigrana vitoriana repintada de prata & §V284a ⊥ alcança isso (ela mede MEDIDA). É §B58/§B62 num 3º eixo|I87,V313,V284,V285,B62,B58
T706|x|gate — §V313 & §V314 NOVAS & **1** EMENDA (⊥ renumerada, ≡ §T664 §T670 §T677 §T686 §T694): **§V279** passa a ler o piso **por ESTILO** em vez de 1 só — o ramo `filigrana` segue medido contra `ornArm`/`a < 12` & o ramo `filete` contra `2*(ORN_CROSS_AT + ORN_CROSS + ORN_CROSS2)`, os 2 LIDOS do Lua, & a doutrina dela ⊥ afrouxa (⊥ ∃ caixa SILENCIOSAMENTE lisa; o que encolhe é o MOTIVO). Mutação RODADA antes de aceitar em CADA perna (§V20 §V222): as 4 de §V313 & as 5 de §V314, mais a sonda de cada uma — check que ⊥ pode vermelhar é §B7 esperando & §V20 ∃ por isso. CONFERIR (⊥ reescrever) §V63 §V276 §V277 §V283 §V284 §V285 §V287|V313,V314,V279,V209,V20,V222
T707|x|teste no Firecast [USER] — DEPOIS de §T703…§T706 & ⊥ antes (com 1 das 3 faltando o que aparece é o estado intermediário, ⊥ o desenho): **(1)** época `Idade das Trevas`: as caixas ganham **1** filete prateado só, a 9px da borda, & ⊥ ∃ voluta, conta nem palmeta em canto nenhum; **(2)** as cruzes: **8** minúsculas perto dos cantos (2 por canto) & **1** um pouco maior no meio do TOPO & da BASE, & as 2 bordas LATERAIS limpas — se aparecer cruz nas laterais, o ramo copiou o `ornEdge` das 4 bordas; **(3)** ⚠ trocar `Victorian Era` → `Idade das Trevas` **SEM fechar a ficha**: a filigrana some & o filete aparece na hora. Se aparecer FILIGRANA PRATEADA, §T705 ⊥ aterrissou — é o defeito que gate nenhum pega (§V313c); **(4)** `Modern Nights` & `Classical Age` ⊥ mudam: seguem SEM moldura, & isso é o correto (§V277), ⊥ relatar como defeito; **(5)** aba Main & aba Combate, caixa de VITALIDADE: trocar `healthLevels` entre os tamanhos & confirmar que o filete acompanha a altura (§V284) & que as cruzes do canto de BAIXO ⊥ ficam por cima da última linha de vitalidade; **(6)** ⊥ ∃ filete nem cruz cruzando rótulo ou campo em caixa nenhuma — o traço corre a 9 & a cruz alcança 14, contra os 20 onde o conteúdo abre (§V314c); **(7)** a caixa mais BAIXA da ficha (`TRUE FAITH`, aba Numina, 1410×96) & a mais ESTREITA (`WoD20.3`, 325×111): as 2 ! ter as 10 cruzes, ⊥ só o filete|T703,T704,T705,T706,I87,V314,V277
T708|x|`WoD20.6.lfm` — `markPath(w, h, kind, style)` despacha & nascem os **3** ramos `filete` (§I88a §I88c §I88e §I88f §I88h). **3** constantes NOVAS ao lado das de §I78, cada uma do SEU estilo (§V308 §V315b): `ORN_FIL_MARK`=**6** · `ORN_FIL_CROSS`=**3** · `ORN_FIL_SEP`=**2,5**. Desenho: `tab` = 1 filete em `h−6`=**24** de `s`=**19,08** a `w−s` + cruz grega de meio-braço 3 em CADA pé (de **21** a **27**) · `sub` = 1 filete em `h−ORN_SUB_MARK`=**23** de **19,80** a `w−19,80`, SEM cruz & SEM `ORN_SUB` · `sep` = cruz de meio-braço 2,5 no centro do portador 16×30. O estilo entra por PARÂMETRO & quem passa é `markRule` (`:2579`) c/ `ornStyleNow` — `markPath` (`:2363`) mora ACIMA de `local ornStyleNow` (`:2447`) ∴ upvalue vira `GETGLOBAL`/nil calado (§V223, §B51). Guard ordenado: sem espaço cai a CRUZ, nunca o filete (§I88h). ⊥ toca XML, ⊥ toca paleta, ⊥ toca `ornPath`. SUPÕE §T703…§T705 no código (estão)|I88,V315,V308,V223
T709|x|`WoD20.6.lfm` — o memo do marcador carimba o ESTILO (§I88g §V315f). `markPainted[handle]` passa do `<path>` PELADO p/ `{p, k, s}` (`k` = `kind` já resolvido, `s` = estilo desenhado) · a CRIAÇÃO grava os 3 · & `markRule` reescreve `.data` quando `s ≠ ornStyleNow`, ≡ a 3ª porta que §T705 abriu em `ornament()`. Sem esta linha, trocar de época na ficha ABERTA serve o desenho da época anterior repintado na cor nova, c/ `rdk -l` 0 & gate verde (§B58 §B62). ! sair no MESMO commit que §T708 — o despacho sozinho é o defeito|I88,V315,B58,B62
T710|x|gate — §V315 NOVA (6 pernas + zero-guard) & §V307 EMENDADA (⊥ renumerada, ≡ §T664 §T670 §T677 §T686 §T694): **(1)** o TEXTO de §V307a vai de "− **3**" p/ "− **1**", que é o que `$clear307` diz desde a 112ª — §B73, & é EMENDA de spec, ⊥ de check; **(2)** §V307a passa a valer nos **2** estilos SEM 2º número, porque §V315c solda `ORN_FIL_MARK`+`ORN_FIL_CROSS` a `ORN_MARK2`. Mutação RODADA até vermelho em CADA perna antes de aceitar (§V20 §V222), incluindo a SONDA de `ORN_SUB` que ! ficar VERDE. ⚠ grepar o gate pelo NOME (`markPath` `markPainted` `ORN_`) & pelos LITERAIS que a rodada mexe ANTES de fechar a linha — §V307 §V308 §V313 §V314 falam de constante de desenho & a lista de quem ATRAVESSA ⊥ é a de quem a rodada revoga (§B67)|V315,V307,I88,B73
T711|x|teste no Firecast [USER] — DEPOIS de §T708…§T710 & ⊥ antes (c/ 1 das 3 faltando o que aparece é o estado intermediário, ⊥ o desenho): **(1)** época `Idade das Trevas`, barra de CIMA: sob a aba aberta ∃ **1** filete só, prateado, c/ **1** cruz em cada ponta — ⊥ ∃ mais 2º filete nem losango; **(2)** a cruz **⊥ encosta no texto** em aba nenhuma, & as de olhar são as 2 mais estreitas, `Traits` & `Numina` (é onde a medida diz que ela passa sob a 1ª & a última letra); **(3)** sub-barra (`Disciplines` / `Blood Sorcery: Paths` / `Rituals`): o filete corre quase de ponta a ponta & ⊥ ∃ cruz nele — & nas 2 largas ele é bem MAIOR que antes; **(4)** entre os nomes de sub-aba o separador é uma cruz minúscula, ⊥ um losango, & ela é MENOR que a das pontas da aba; **(5)** trocar p/ `Vitoriano` `Moderno` `Clássico` c/ a ficha ABERTA devolve 2 filetes + losango na hora, & voltar p/ `Idade das Trevas` devolve o filete c/ cruzes — é §T709 que responde isto & é o único item que o gate ⊥ alcança; **(6)** as 4 fontes: o item (2) vale nas 4 épocas? (só a Idade das Trevas desenha cruz, mas o filete de cima de todas corre a **21** & o glifo ⊥ se lê do XML, §R102)|I88,T708,T709,R102
T712|x|`WoD20.6.lfm` — o CANTO sai da PALETA (§I89a §I89b §I89c). **(1)** `sectionBox(c, fill)` EXTRAÍDA do corpo de `ornament()`, c/ as 2 pernas de §V278 INTACTAS dentro dela (fill AUTORADO `#FF000000` · `c.xradius` lido & RECUSANDO, ⊥ só lido) & chamada pelos **2** pontos; **(2)** `["Modern Nights"]` ganha `boxCorner = { type = "bevel", radius = 18 }` — 11ª chave, OPCIONAL, TABELA (§I89a), & as outras 3 paletas ⊥ mudam; **(3)** no ramo `THEME_SHAPES` de `applyTheme` (`WoD20.6.lfm:2823`), dentro de `sectionBox` & DEPOIS dos paints de cor, **3** `paint` c/ fallback AUTORADO: `cornerType` `xradius` `yradius`. O `or` autorado ⊥ é detalhe — sem ele a ficha ⊥ SAI do chanfro & o gate ⊥ vê (§V316c). ⊥ toca os 15 `.lfm` · ⊥ toca `ornPath`/`markPath`/`ORN_BOXR` · ⊥ toca cor nem `THEME_STROKE`. Os 3 itens ! sair no MESMO commit — a chave sozinha ⊥ faz nada & o write sem o fallback é o defeito|I89,V316,V278,V54
T713|x|gate — §V316 NOVA (6 pernas + zero-guard) & **4** EMENDAS, nenhuma renumerada (≡ §T664 §T670 §T677 §T686 §T694 §T710): **(1)** §V66 — o check que hoje reprova `xradius`/`yradius` em `applyTheme` INVERTE p/ exigir que a escrita passe pelo seletor, & a proibição da chave `corner` em paleta FICA; **(2)** §V68 — texto & mensagem passam a dizer canto **AUTORADO**; **(3)** §V258 — a régua vira `MAX(xradius do XML, ∀ boxCorner.radius)`, os 2 lados LIDOS & ⊥ soletrados (§V316f); **(4)** §V278 RE-APONTADA p/ `sectionBox` — deixá-la grepando `ornament()` depois da extração é o no-op de §V20/§B7. Mutação RODADA até vermelho em CADA perna antes de aceitar (§V20 §V222), c/ a SONDA `radius` 18→16 que ! ficar VERDE. ⚠ grepar o gate por `xradius` `cornerType` `boxCorner` `ornament(` `ORN_BOXR` ANTES de fechar a linha — §V66 §V68 §V229 §V258 §V278 §V279 §V283 §V313 §V314 falam de canto ou de raio, & a lista de quem ATRAVESSA ⊥ é a de quem a rodada revoga (§B67)|V316,V66,V68,V258,V278,I89
T715|x|`WoD20.6.lfm` — a 3ª família de desenho de caixa (§I90a §I90b §I90c). **(1)** `["Modern Nights"]` ganha `ornament = "#A83232"` **&** `ornStyle = "corrente"`, ao lado do `boxCorner` da 117ª — o carmesim é o valor que a paleta JÁ tem em `font["#C2A14D"]` ∴ ⊥ nasce cor (§V53); **(2)** `ornPath(w, h, style)` ganha 4º arg `radius` & DESPACHA `"corrente"` → `ornCorrente(w, h, r)`, ≡ o despacho de §I87b, & as **2** chamadas dentro de `ornament()` passam `c.xradius`; **(3)** `ornCorrente` NOVA: **1** constante própria `ORN_LINK` = **3,2** & TUDO o resto por relação de `r` & `ORN_IN` — `R = r + ORN_IN·(√2−1)` · `L = √2·r − 2·ORN_IN·(√2−1)` · 4 retas ABERTAS de `R` a `w−R` / `h−R` · 12 elos, 3 por corte, centros a `L/6` `L/2` `5L/6`, passo & comprimento `L/3`, meia-largura `ORN_LINK` perpendicular · cada elo = 2 cúbicas fechando lente ∴ **16** subpaths. ∀ decimal por `ornNum` (§I72b). ⊥ toca `ornFiligree`/`ornFilete`/`markPath`/`ORN_BOXR`/`boxCorner`/`sectionBox`, ⊥ toca XML nem `.lang`. Os 3 itens ! sair no MESMO commit — a chave sem o ramo pinta filigrana carmesim nas 73|I90,V317,V313,I72
T716|x|gate — §V317 NOVA (6 pernas + zero-guard) · §V318 NOVA (fim de linha) · **2** EMENDAS, nenhuma renumerada (≡ §T710 §T713): **(1)** §V316b REVOGADA & substituída pelo ACORDO de §V317a — a perna que hoje reprova `boxCorner` + `ornament` na mesma paleta SAI, & o que entra é o par estilo↔canto LIDO dos 2 lados; o resto de §V316 (a c d e f) ⊥ é tocado; **(2)** §V315a — a contagem "exatamente 1 paleta declara `ornStyle`" vira `nº de paletas c/ estilo == nº de ramos de ornPath`, & o teste de digitação passa do `markPath` p/ o `ornPath` (§I90h); **(3)** §V318 — os **15** `.lfm` & `verify-hunters-hunted.ps1` & `SPEC.md` ! ter `CR == LF` & `LF > 0`, é ~5 linhas & é o backprop de §B74. Mutação RODADA até vermelho em CADA perna antes de aceitar (§V20 §V222), incluindo a SONDA do carmesim que ! ficar VERDE. ⚠ grepar o gate por `ornStyle` `ornPath` `ORN_BOXR` `ORN_IN` `boxCorner` `THEME_STROKE` ANTES de fechar — §V63 §V277 §V279 §V283 §V284 §V308 §V313 §V314 §V315 §V316 falam de estilo, de raio ou de recuo, & a lista de quem ATRAVESSA ⊥ é a de quem a rodada revoga (§B67). ⚠ editar `.lfm` & `.ps1` SÓ por PowerShell, medindo `[regex]::Matches($t,[char]13)` antes & depois (§B74)|V317,V318,V316,V315,I90,B74
T718|x|`WoD20.6.lfm` — `markPath(w, h, kind, style)` (`:2576`) passa a despachar `"corrente"` & nascem os **3** ramos (§I91a §I91b §I91c §I91d). **7** constantes NOVAS ao lado das de §I78 & §I88, cada uma do SEU estilo & do SEU motivo (§V308 §V315b): `ORN_COR_MARK`=**7** · `ORN_COR_IN`=**14** · `ORN_COR_MARK2`=**3** · `ORN_COR_SPAN`=**0,30** · `ORN_COR_SUB2`=**3** · `ORN_COR_SUB_SPAN`=**0,35** · `ORN_COR_DOT`=**3,2**. Desenho: `tab` = reta em `h−7`=**23** de **14** a `w−14` + 2ª reta em `h−3`=**27** de `0,30·w` a `0,70·w` · `sub` = reta em `h−ORN_SUB_MARK`=**23** de **19,80** a `w−19,80` (exceção DECLARADA, ≡ §I88e) + 2ª reta em **27** de `0,35·w` a `0,65·w` · `sep` = disco CHEIO de raio **3,2** no centro do portador 16×30, **4** cúbicas c/ `ORN_KAPPA` (`:2004`). ∀ decimal por `ornNum` — verbo float segue `LC_NUMERIC` & host pt-BR escreve VÍRGULA, que parte ∀ coordenada em 2 números & mata o path inteiro, calado (§I72b). Piso: 1 por motivo & ⊥ ∃ ordem de queda, a 2ª reta é proporcional (§I91g). ⊥ toca XML, ⊥ toca paleta, ⊥ toca `markFilete` nem `ornPath`|I91,V319,V308,V223
T719|x|`WoD20.6.lfm` — o FILL sai do DESENHO & o memo passa a carregá-lo (§I91e §I91f §V319e §V319f). `markPath` devolve **2** valores (`d`, `f`) & SÓ o ramo `sep` do `corrente` devolve `f` verdadeiro — os 3 default & os 3 `filete` seguem c/ 1 `return` só ∴ `f` chega nil neles sem 1 linha a mais · `markPainted[handle]` passa de `{p, k, s}` p/ `{p, k, s, f}` & as **2** portas de `markRule` (`:2815`) gravam o 4º campo · `p.color` deixa de ser `#00000000` cravado na criação (`:2844`) & passa a ser escrito a CADA repintura ao lado de `e.p.strokeColor` (`:2873`): cheio ⟺ `e.f`. `markRule` ⊥ soletra `"corrente"` nem `"sep"` — quem sabe é o desenho. ⚠ ATÔMICA c/ §T718: sozinha ela ⊥ tem o que pintar, & §T718 sozinha nasce c/ a bolinha VAZADA & deixa a cruz da Idade das Trevas CHEIA ao trocar de época na ficha aberta (§B58 §B62)|I91,V319,B58,B62
T720|x|gate — §V319 NOVA (6 pernas + zero-guard) & **1** EMENDA, ⊥ renumerada (≡ §T710 §T713 §T716): **§V315b** deixa de ENUMERAR 2 estilos & passa a valer p/ N — ramo de estilo X ⊥ menciona constante de estilo Y, c/ os 3 prefixos LIDOS (`ORN_MARK*`/`ORN_SUB`/`ORN_SEP_*` = default · `ORN_FIL_*` = `filete` · `ORN_COR_*` = `corrente`) — & a lista de exceções DECLARADAS vai de **2** p/ **3**: `ORN_PILLR` · `ORN_SUB_MARK` · `ORN_KAPPA`, esta última a única que ⊥ é escolha de desenho (é a constante do arco, ≡ π). Mutação RODADA até vermelho em CADA perna antes de aceitar (§V20 §V222), incluindo as **2** SONDAS que ! ficar VERDES. ⚠ grepar o gate pelo NOME (`markPath` `markPainted` `markRule` `ORN_`) & pelos LITERAIS que a rodada mexe ANTES de fechar a linha — §V307 §V308 §V313 §V314 §V315 falam de constante de desenho & a lista de quem ATRAVESSA ⊥ é a de quem a rodada revoga (§B67)|V319,V315,I91,B67
T722|x|`WoD20.1.lfm` — a moldura do avatar (`:341`) ganha `name="avatarFrame"` & MAIS NADA: `left` `top` `width` `height` `color="DimGray"` `xradius` `yradius` `cornerType` INTOCADOS (§V309 mede os 4 números dos 3 controles & esta rodada ⊥ move 1px). No MESMO commit, o comentário de `:343`…`:352` PERDE as 2 frases que mentem desde §T693 — "o enfeite JÁ ∃ hoje na moldura, só que atrás" & a que diz que `applyTheme` já o alcança — porque `sectionBox` recusa os 2 controles & §I87f (115ª) LISTOU isso 1 rodada depois de §T693 o afirmar, sem ninguém acender (§B75)|I92,V309,V320,B75
T723|x|`WoD20.6.lfm` — `sectionBox` (`:2787`) ganha o 2º braço & o 2º valor de retorno (§I92a §I92b): devolve `veste, desenha` — `veste` = (fill `#FF000000` + raio) **OU** nome ∈ {`avatarFrame`, `ornAvatar`} · `desenha` = (fill `#FF000000` + raio) **OU** nome == `ornAvatar`. O write do canto (`:3090`) lê o **1º**; `ornament()` (`:2845`) lê o **2º** & é a ÚNICA linha que muda lá dentro. ⊥ tocar: a assinatura de `ornament()` (§V285c conta os 5) · `ornPath` · `markPath` · as 4 paletas · `ORN_BOXR` · `THEME_SHAPES` · a ordem de §I89c (canto ANTES do desenho na mesma iteração — é ela que dá o raio 18 ao elo do avatar)|I92,V320,V316,V317,V67
T724|x|`verify-hunters-hunted.ps1` — §V320 NOVA: as **6** pernas + zero-guard. Mutação RODADA antes de aceitar (§V20 §V222): trocar o braço de nome por construção ! VERMELHO citando as 19 pílulas · mover `ornAvatar` p/ antes da `<image>` ! VERMELHO · apagar 1 dos 2 nomes ! VERMELHO · sonda: trocar o carmesim do Moderno ! VERDE. §V309 é RE-APONTADA p/ o `name` novo & ⊥ renumerada (§V320e)|V320,V309,V20,V222,V209
T726|x|`WoD20.6.lfm` — o braço do avatar responde `true, false` nos **2** nomes (§I93a): o `ornAvatar` deixa de responder `true, true`. 1 linha. ⊥ tocar: o par de valores de `sectionBox` · a leitura do 2º em `ornament()` · `p:setParent(c.parent)` · o write do canto, que lê o 1º & é o que entrega o chanfro no avatar|I93,V321,B76
T727|x|`verify-hunters-hunted.ps1` — §V321 c/ as 3 pernas + zero-guard. §V320 (d) é RE-APONTADA & ⊥ renumerada: a ordem no XML segue medida, mas agora pelo outro sentido — a moldura ANTES da `<image>` porque ela é o FUNDO do letterbox, ⊥ porque o portador desenha. Mutação RODADA antes de aceitar (§V20 §V222): devolver `true, true` ao `ornAvatar` ! VERMELHO|V321,V320,V20,V222,V209
T729|x|`WoD20.6.lfm` — paleta `Classical Age` (`:1827`) ganha as **2** chaves: `ornament = "#BFA05A"` & `ornStyle = "corona-muralis"`. ⊥ tocar `boxCorner` (§V317a o proíbe na clássica) nem `fill`/`stroke`/`font`/`dotOn`/`dotOff`/`paper`|I94a,V322a,V53
T730|x|`WoD20.6.lfm` — `ornCoronaMuralis(w, h)` + as **4** `ORN_MUR_*` + o **4º** ramo em `ornPath` (`:2526`). Assinatura de **2** parâmetros & chamada SEM raio (§I94b). Guard solta o MERLÃO & devolve a RÉGUA, nunca `""`. ∀ decimal por `ornNum` — verbo float segue LC_NUMERIC & host pt-BR escreve VÍRGULA (§I72b)|I94a,I94b,V322b,V322c,V314e
T731|x|`verify-hunters-hunted.ps1` — §V320f EMENDADA de **3** p/ **4** paletas c/ `ornament` (`:10559`). ⊥ apagar a perna: o que ela mede segue valendo & é ela que impede a 5ª época de entrar calada|V322e,V320
T732|x|`verify-hunters-hunted.ps1` — §V322 NOVA c/ as **5** pernas + zero-guard. Mutação RODADA antes de aceitar (§V20 §V222): tirar `ornament` da clássica ! VERMELHO · pôr o raio no ramo novo ! VERMELHO · `ornCoronaMuralis` lendo `ORN_IN` ! VERMELHO · sonda `ORN_MUR_STEP` ! VERDE|V322,V222,V20
T734|x|`WoD20.6.lfm` — `markCoronaMuralis(w, h, kind)` + as **8** `ORN_COL_*`/`ORN_GUT_*` + o **3º** ramo em `markPath`. Devolve **2** valores (§I95e) & lê `ORN_SUB_MARK` p/ a régua. ⊥ tocar `ornCoronaMuralis`, `markFilete`, `markCorrente`, `markRule` nem a paleta|I95,I95b,I95c,V323
T735|x|`verify-hunters-hunted.ps1` — §V323 NOVA c/ as **6** pernas + zero-guard. Mutação RODADA antes de aceitar (§V20 §V222) — a de (c) é a que prova que a perna morde, ∴ ⊥ aceitar sem ela|V323
T737|x|`WoD20.6.lfm` — EMENDADA na 123ª (era `bevel` **18**): paleta `Classical Age` ganha `boxCorner = { type = "innerLine", radius = 14 }` + `ornMuralFoot` vira `R + d` + `ornMuralCorner` troca `ornArc` por **2** retas em L + `ornCoronaMuralis(w, h, radius)` + o ramo passa `radius`. DEPOIS de §T741 & ⊥ antes — o `?` de `innerLine` é o risco da rodada. ⊥ tocar as **4** `ORN_MUR_*` (§I96e), `sectionBox`, o write do canto nem as outras 3 paletas|I96,I97,I97b,V324,V326
T738|x|`verify-hunters-hunted.ps1` — §V324 NOVA c/ as **5** pernas + zero-guard **&** §V326 NOVA c/ as **3**, **&** as **4** emendas: §V316e **1**→**2** paletas · §V317a nomeia `corrente` **&** `corona-muralis` **&** deixa de cravar `bevel` no 3º ramo (`:9078`, que hoje reprova QUALQUER tipo ≠ `bevel` & barraria `innerLine` sozinho) · §V322b invertida p/ COBRAR o raio. Mutação RODADA antes de aceitar (§V20 §V222) — a de §V324a é a que prova que a perna morde|V324,V326,V316,V317,V322
T740|x|`verify-hunters-hunted.ps1` — §V325 NOVA c/ as **3** pernas + zero-guard. Conta `local` no escopo do chunk de `WoD20.6.lfm` contra o teto **100** (medido **105**, §B77). Mutação RODADA antes de aceitar (§V20 §V222)|V325,B77
T741|x|`?` RESOLVIDO na tela 2026-08-27 pelo ramo **(1)**: o user LEU o degrau em L ∴ o SDK3 desenha `innerLine` como o par RETO do `innerRound` & §I97 estava certo. §R117 fecha. Texto original: `?` RESOLVE `innerLine` na tela [USER] — **ANTES** de §T737 & §T738, & é a única fora de ordem: §R117 lê o valor no enum do SDK3 & ∃ `setCornerType`, mas NINGUÉM o rodou nesta ficha ∴ o degrau de §I97 é LEITURA, ⊥ fato. 1 caixa basta: pôr `cornerType="innerLine"` `xradius="14"` `yradius="14"` num `<rectangle>` de teste & OLHAR. **(1)** é degrau em L (o par RETO do `innerRound`, ≡ `bevel` é o par reto do `round`) ! §I97 está certo & §T737 segue; **(2)** é outra coisa ! `/ck:spec bug:` c/ o que a tela mostrou, & §I97 §V324c §V324d §V326a são refeitas ANTES de 1 linha de código; **(3)** o SDK ignora o valor calado (canto fica reto) ! `innerLine` sai do leque & o user escolhe entre as **3** que sobram. Fecha §R117 na parte que ela deixou aberta|R117,I97,V324
T742|x|`verify-hunters-hunted.ps1` — mutação de §V326 RODADA antes de aceitar (§V20 §V222): `radius` **14**→**20** ! VERMELHO · `boxCorner` fundo em outra paleta ! VERMELHO · `ORN_MUR_OUT` > `ORN_MUR_IN` ! VERMELHO · sonda `ORN_MUR_STEP` ! VERDE. Perna que ⊥ morde é no-op & §V20 ∃ porque 1 check já virou (§B7)|V326,V222,V20
T743|x|`WoD20.6.lfm` — paleta `Classical Age` ganha `tabLabelHeight = 30` + o write da altura no ramo `THEME_TEXTS`, c/ o `or` AUTORADO (§I98a §I98b) & fechado pelo seletor do avô `tabStrip` (§I98b). ⊥ tocar os **8** labels das sub-barras, o XML dos 11, nem as outras 3 paletas|I98,V327
T744|x|`WoD20.6.lfm` — `ORN_COL_EDGE` = **2** dentro de `markCoronaMuralis` & o capitel nasce a 2 da borda (§I98d). ⊥ tocar `ORN_COL_W` nem o motivo `sub`/`sep`. MESMO arquivo de §T743 & independente dela|I98,I98b,V327
T745|x|`verify-hunters-hunted.ps1` — §V327 NOVA c/ as **4** pernas + zero-guard. Mutação RODADA antes de aceitar (§V20 §V222) — a de (b) é a que prova que a perna morde|V327
T747|x|`research/README.md` — caminhos NOVOS dos livros (§R126): a tabela `código → título` ganha coluna de SUBPASTA (`Vampiro/` · `Mago/` · `Sorcerer/` · raiz), `anarch` & os **7** livros NOVOS entram, & a armadilha do `á` no Git Bash vira a **4ª** da lista. ⊥ reextrair nada — só o mapa de onde as coisas estão. PRIMEIRA da rodada: ∀ §T de extração abaixo lê este arquivo p/ achar o PDF|R126
T748|x|extração Road/Path/Bearing → `research/road_bearing.tsv` (`<nome EN>\t<bearing EN>\t<livro>\t<pág impressa>\t<época>`). Fontes: `core` p.314-316 (**12** paths, §R122) · `da` p.115-140 & 432-449 (**13** roads + **18** paths, §R123) · `bh` & `lotc` & `lob` (paths de linhagem, ~**7** LIDOS c/ coluna FUNDIDA ∴ RELER por coluna). `Humanity`/`Normalcy` é a 1ª linha (§R121). Ler c/ `-layout` & POR COLUNA — a linha `Bearing:` cai ao lado da `Nickname:` & o casamento por proximidade ERRA (armadilha 1 do README). Fecha o `?` das 3 auras que faltam em §R123|R121,R122,R123,I99c
T749|x|extração qualidade/defeito → `research/mf_<livro>.tsv` (`<nome EN>\t<Merit\|Flaw>\t<custo verbatim>\t<livro>\t<pág impressa>`). **2** parsers, ⊥ 1: o padrão `Nome (Npt. Merit)` cobre `core` `lotc` `lob` `gr` `hh` `bh` `anarch` `dac` `rob`; o `da` escreve `Nome (N points):` & `tos` põe CATEGORIA no parêntese (`(2 Pt. Supernatural Merit)`) — §R125. Dedupar ENTRE livros na precedência do README (`core` > … ). **Q5 RESPONDIDA: TODOS os livros**, & isso inclui os **5** de `Books/Mago/` ∴ ⊥ ∃ recorte no fim & o **3º** parser (o de Mago) ! ser medido antes de rodar — a notação dos livros de Mago ⊥ foi conferida em §R125. ⊥ ∃ item sem chave `.lang` (§V17) ∴ o custo do "todos" é a tradução, & ela ⊥ é opcional|R125,I100c,I100d,V17
T750|x|`meritData.lua` NOVO c/ as **2** tabelas de §I100d (`MERIT_DATA` **311** & `FLAW_DATA` **336**, `return { merit = …, flaw = … }`) + `PICKER_LIST["merit"]` & `["flaw"]` no `WoD20th.lfm` + as **646** chaves `.lang` dos itens em [pt] & [en] & no PT map de `WoD20.6` (§V17 §V28). Módulo PRÓPRIO & ⊥ `<script>` do `.lfm` (§I100g, ≡ §I24/§T479). `cost` = STRING verbatim do livro & VAZIA nas 2 de §V340a. GERADO por `research/gen_merit_data.ps1` a partir de `merit_flaw.tsv` + `merit_flaw_pt.tsv` + as 3 regras de `merit_flaw_dedupe.md` — ⊥ editado à mão (647 itens × 4 casas)|I100c,I100d,I100e,I100g,V331,V340,V17
T751|x|`descMerit_en.lua` & `descMerit_pt.lua` NOVOS — descrição de ∀ item das 2 listas, forma de §I21 (livro+pág · 2 linhas · nome · 2 linhas · texto). **1** módulo por idioma (§V210, ⊥ os 2 lados num arquivo só). DEPOIS de §T750 — a chave é o nome canônico que ela grava|I102e,V210,V336
T752|x|`WoD20.1.lfm` — rótulo `HUMANITY`→`ROAD` · `cboRoad` em `top="45"` · bolinhas descem p/ `74` · `lblBearingName` + `lblBearingMod` em `top="99"` · `WILLPOWER` desce p/ `139` & track p/ `164` · a caixa vai a `height="229"`. **&** as outras 2 da linha de baixo crescem no MESMO commit (§I99i §I99j): `SPECIALTIES` `356`→**382** & os **3** controles do avatar `411`→**437** — mover 1 sozinha deixa §V69 VERMELHA, ∴ as 3 são 1 tarefa atômica. O mapa do topo do arquivo (`y 441..852` ×3) reescrito p/ **878** no mesmo commit — §V69 LÊ o mapa (§B73). ⊥ tocar `humanity_1`…`_10` (§V2 §V5)|I99a,I99b,I99e,I99i,I99j,V2,V5,V69,B73
T753|x|`PICKER_LIST["road"]` + `BEARING` + as chaves `.lang` dos 2 (nome do caminho & nome do bearing), do `research/road_bearing.tsv`. `Humanity` = item **1** (⊥ vazio) & `BEARING["Humanity"]` = `Normalcy`. DEPOIS de §T748|I99c,R121,V328,V329,V17
T754|x|`renderBearing()` + `bearingMod()` + os `<dataLink>` de `road` & `humanity_1`…`_10`. Portão = `clanFamily` PREENCHIDO (§I99d emendada por **Q1**) ∴ ⊥ nasce SET `REVENANT` aqui — quem lê `PICKER_LIST["family"]` é §T774. **1** função escreve os 3 estados; o link de `clanFamily` já ∃ (`WoD20th.lfm:3972`) & só ganha a chamada. ⚠ conferir o TETO DE LOCAIS do chunk antes (§B77, hoje **99** de **105**): `BEARING` & `bearingMod` são **2** novas ∴ cabem, mas contar ANTES & pôr dentro da função que as lê se apertar|I99d,I99f,I99g,I99h,V329,V330,B77
T755|x|`verify-hunters-hunted.ps1` — §V328 §V329 §V330 NOVAS c/ as pernas & o zero-guard de cada, **&** a emenda do check de §V15 p/ nomear `cboRoad` como exceção (whitelist NOMEADA, ⊥ genérica — §V328d). Mutação RODADA antes de aceitar (§V20 §V222): `PICKER_LIST["road"][1]` → `""` ! VERMELHO · tirar 1 corte de `bearingMod` ! VERMELHO · `BEARING` sem 1 road ! VERMELHO · 2ª leitura de `PICKER_LIST["family"]` ! VERMELHO|V328,V329,V330,V15,V20,V222
T756|x|`WoD20.2.lfm` — templates `MeritPicked` & `MeritFree` no lugar do `Merit` único, & as **20** chamadas re-apontadas (`m1`…`m6`/`f1`…`f6` → picked, `m7`…`m10`/`f7`…`f10` → free). ⊥ ∃ comentário DENTRO de template (§B19, o `rdk` sai 1 sem mensagem & apaga o `.rpk`)|I100a,I100b,V331,B19
T757|x|auto-preenchimento: `onChange` do combo escreve `book_*` `type_*` `costy_*` de `MERIT_DATA` 1× & item vazio LIMPA os 3. DEPOIS de §T750 & §T756|I100f,V331
T758|x|`WoD20.2.lfm` layout — `FLAWS` desce p/ (375, 316) · `ALLIES` `MENTOR` `RESOURCES` `GUIDES` sobem p/ o 2×2 em (865, 0) · as outras **6** no 3×2 de y **316** · as **10** viram **235×155** c/ `textEditor` **165×94** · `STATUS`→`ARTIFACTS` (`field="status"` INTACTO) · o comentário-mapa do topo do arquivo REESCRITO no MESMO commit (§B73). ⊥ tocar `BACKGROUNDS` nem `DERANGEMENTS`|I101,V332,V2,B73
T759|x|~~`verify-hunters-hunted.ps1` — §V331 §V332 NOVAS c/ as pernas & o zero-guard~~ — **SUPERSEDIDA por §T803, que já está `x`**: §T803 escreveu as 2 §V c/ as pernas que sobraram depois de §V331 ser reescrita na 141ª, & rodou as mutações. ⚠ **FECHADA na 146ª (§T843, item 4 do user)**: `x` = RESOLVIDA por supersessão, ⊥ construída duas vezes. Ficar `.` p/ sempre é inflar a conta de "a fazer" c/ check que já mede|T803,V331,V332
T760|x|`popDesc` + `popScrim` na aba Traits + `popOpen(key)` / `popClose()`. Scrim ANTES do popDesc no arquivo. `edtPopDesc` c/ `wordWrap="true"`. **1** caixa p/ a ficha ∴ abrir outra sobrescreve, & ⊥ ∃ lista de abertas|I102a,I102c,I102d,V333
T761|x|os **28** `btnQ*`: **12** nas linhas picker de qualidade/defeito & **16** nas de antecedente. Todos chamam `popOpen` c/ a chave da linha. Linha DIGITADA ⊥ ganha botão (§I102f) & a coluna de entrada segue alinhada (§V26). DEPOIS de §T756 & §T758|I102b,I102f,V333,V26
T762|x|`fontStep(pane, delta)` + os **16** botões nas **8** panes + a PERSISTÊNCIA de **Q7**: `fontStep` grava `descFontSize` & `onNodeReady` o aplica nas 8 (ficha sem o campo → **12**). `descFontSize` entra na lista LUA-OWNED de §I3 no MESMO commit, senão §V8 acende. Literais `10` `32` `2` SÓ dentro de `fontStep`. ⚠ teto de locais (§B77) — `fontStep` é 1 local nova|I103,I3,V334,V8,B77
T763|x|`verify-hunters-hunted.ps1` — §V333 §V334 NOVAS c/ as pernas & o zero-guard. Mutação RODADA (§V20 §V222): scrim DEPOIS do popDesc ! VERMELHO · 2º `popDesc` ! VERMELHO · `btnQ` = 27 ou 29 ! VERMELHO · teto `32`→`40` fora de `fontStep` ! VERMELHO · pane sem par de botão ! VERMELHO|V333,V334,V20,V222
T764|x|extração de antecedente → `research/bg_essence.tsv` (`<nome EN>\t<jogo>\t<livro>\t<pág>\t<resumo de 1 linha da mecânica>`) & a decisão de dedupe de §I105b tomada A PARTIR dela, ⊥ de memória (§R8). Saída = a lista FINAL de `PICKER_LIST["background"]`, c/ `(Origem)` só onde ∃ colisão de sentido|I105b,R8,V336
T765|x|`descBackground_en.lua` & `descBackground_pt.lua` NOVOS, forma de §I21. DEPOIS de §T764 — a chave é o nome final que ela decide|I102e,V336,V210
T766|x|`PICKER_LIST["background"]` — `Artifacts` entre `Armory` & `Avatar` · a lista final de §T764 aplicada · `.lang` [pt] `wod.Node`=`Nodo` & `wod.Pure Breed`=`Raça Pura` · chaves novas dos itens que ganharam `(Origem)`|I105a,I105c,V336,V17
T767|x|`verify-hunters-hunted.ps1` — §V336 NOVA c/ as **6** pernas & o zero-guard. Mutação RODADA (§V20 §V222): `Artifacts` fora de ordem ! VERMELHO · `Nódulo` de volta ! VERMELHO · `Status (Vampire)` SEM irmão ! VERMELHO · item sem entrada em `descBackground` ! VERMELHO|V336,V20,V222
T768|x|regenerar os **8** `.lua` de descrição p/ SÓ o bloco `System` (§I104). Gerador re-rodado, ⊥ edição à mão — `research/gen_desc_system.ps1` já ∃ & já mede; falta ensinar a ele a forma do númina de §I104e, & ele RECUSA `-Apply` até lá (§B88 pôs a trava ANTES da escrita). ⚠ escrever c/ `[IO.File]::WriteAllText` & `\r\n` — `sed`/`awk` do Git Bash comem o CR do arquivo inteiro, calados (§B74, §B87). BACKUP antes (§B78: `git show HEAD:` ⊥ é o estado de trabalho deste repo)|I104,I104e,V335,V210,V345,B74,B78,B87,B88
T769|x|`verify-hunters-hunted.ps1` — §V335 NOVA c/ as **5** pernas & o zero-guard, & a lista NOMEADA de exceção de §I104c preenchida c/ o que §T768 MEDIU (⊥ com o que se supôs) — as **3** classes já estão medidas em §R133 & escritas em §I104e. Mutação RODADA (§V20 §V222): 1 bloco de nível sem `System` ! VERMELHO · 1 entrada c/ bloco 3 vazio ! VERMELHO · contagem menor que §V210a ! VERMELHO|V335,V210,V20,V222,I104c,I104e,R133
T774|x|varredura dos tetos de família que FALTA (§R127): as **6** medievais do `da` p.453-454 (`Basarab` `Bratovich (Dark Ages)` `Danislav` `Khavi` `Szantovich` `Vlaszy`) & as **4** de §R93 sem trio no livro (`Katayama` `Keskinen` `Krevcheski` `Marijava`) atrás dos MESMOS padrões que acharam a `Enrathi` (`higher than` · `maximum` · `may not` · `cannot exceed` · `limited to`). Saída = as linhas que faltam de `FAMILY_CAP`, ou a confirmação de que ⊥ ∃ nenhuma. Fraqueza de COMPORTAMENTO ⊥ vira linha — o critério é valor NUMÉRICO de traço|R127,I106a
T775|x|~~`FAMILY_CAP` + a recusa no `xpWarn`~~ — **SUBSTITUÍDA por §T781, que já está `x`**: §T781 refez a tarefa inteira & c/ a régua certa. ⚠ **FECHADA na 146ª (§T843, item 4 do user)**: `x` aqui quer dizer RESOLVIDA por supersessão, ⊥ construída duas vezes — o trabalho ∃ em disco, entregue por §T781. Ficar `.` p/ sempre é inflar a conta de "a fazer" c/ trabalho já feito|T781
T776|x|`verify-hunters-hunted.ps1` — ⚠ **a §V337 ⊥ saiu daqui.** Quem a escreveu foi **§T782** (inteira, 7 pernas + zero-guard, `verify-hunters-hunted.ps1:11826`), quando §T781/§T782 refizeram o par c/ alvo em `BLOOD POOL` (§I108c) & ninguém voltou p/ virar o status desta — ≡ o que §T775 conta do seu lado. As **3 emendas** tb já estavam no gate quando a 140ª abriu (§V330 perna (b) `:10940` · §V334 pernas (d)-(f) `:11800`-`:11821` · §V328/§V69 no fundo **878**) ∴ o que sobrava era o que a própria linha exige ANTES de aceitar. O que a 140ª ENTREGOU: **(1)** a MUTAÇÃO (§V20 §V222) — **13** rodadas, as **5** nomeadas abaixo + o zero-guard §V337g + **4** das emendas VERMELHAS + **2** do que foi reescrito + **1** sonda VERDE (nome em COMENTÁRIO ⊥ acende), c/ os arquivos restaurados byte a byte (SHA256); **(2)** **§V330c REESCRITA** — era LOOP MORTO, contava & ⊥ comparava (§B92); **(3)** **§V328b PARTIDA EM 2** — 1 teste só respondia "casa errada" p/ VALOR errado & mandava o leitor atrás de um build morto que ⊥ tinha acontecido. Texto original: §V337 NOVA c/ as **7** pernas & o zero-guard, **&** as **3** emendas que as respostas obrigam. Mutação RODADA antes de aceitar (§V20 §V222): teto p/ família fora de `PICKER_LIST["family"]` ! VERMELHO · 2º `xpWarn` de teto ! VERMELHO · código que zera `humanity_*` na troca ! VERMELHO · `descFontSize` fora da lista LUA-OWNED ! VERMELHO · avatar em **852** c/ as outras 2 em **878** ! VERMELHO (§V69)|V337,V330,V334,V328,V69,V20,V222,B7,B92
T778|x|`verify-hunters-hunted.ps1` — §V338 NOVA c/ as 3 pernas & o zero-guard (§B80). PRIMEIRA da 3ª volta & ⊥ depois: ela é o check que teria poupado a bisseção desta rodada, & escrevê-la agora é o único momento em que o custo dela é zero. Mutação RODADA (§V20 §V222): `defaultValue` mudado p/ o `cboRoad` ! VERMELHO · `dataLink field="road"` removido ! VERMELHO · sonda (mudar o VALOR de um `defaultValue`) ! VERDE|V338,B80,V20,V222
T779|x|filtro por época dos `items` do `cboRoad` (§I99c2) — o §T que a 2ª volta ESQUECEU de abrir: `ROAD_ERA` ∃ & o gate já a mede, mas nada filtra ainda ∴ o dropdown mostra as **55** em qualquer época. Aplicar no MESMO ponto onde `eraName()` já roda (§I22), c/ `currentTheme()` como fonte da época & `Humanity` sempre presente (§I99c3). Valor salvo fora da época corrente ! virar item (§I99c4)|I99c2,I99c3,I99c4,V24
T780|x|`PICKER_LIST["family"]` **22**→**23** & `CLANS` ganha a 2ª `Grimaldi` (§I108b). §V212/§V236a/§V294 mudam de número no MESMO commit — número de check que fica p/ trás é §B70/§B73, & aqui são 3 de uma vez. `.lang` das 2 entradas novas em [pt] & [en] & no PT map|I108b,V341,V212,V294
T781|x|`FAMILY_CAP` + a recusa no `xpWarn` que já ∃ + a chave `.lang` do aviso, agora c/ alvo em `BLOOD POOL` (§I108c). §V219 EMENDADA no mesmo commit p/ dizer que teto ⊥ faz da bolinha um traço. SUBSTITUI §T775, que foi escrita quando o teto era de Humanity/Conscience|I106,I108c,I108d,V337,V219
T782|x|`verify-hunters-hunted.ps1` — §V341 NOVA + §V337 NOVA c/ as pernas & zero-guard. Mutação RODADA (§V20 §V222): `Grimaldi` pelado de volta na lista ! VERMELHO · teto na moderna ! VERMELHO · união em 84 ! VERMELHO|V341,V337,V20,V222
T783|x|`MeritPicked` EMENDADA (§I107a): o `comboBox` saiu & entrou **`<button name="dynMerit_$(num)">`** — ⊥ o `<edit readOnly>` que esta linha pedia, porque a **2ª volta** de §I107a trocou o portador DEPOIS que ela foi escrita, & botão é terreno PROVADO nesta ficha. SUBSTITUI a parte de picker de §T756. As **8** linhas digitadas (`m7`…`m10`, `f7`…`f10`) ⊥ mudam. FEITO na 140ª junto c/ §T756: os 2 templates ∃, as **20** chamadas re-apontadas (12 picked · 8 free), o `<edit>` do campo ficou ATRÁS do botão c/ `visible="false"` (§I107a1) & `renderMeritButtons` repinta os 12 pelo campo — 1 `dataLink` sobre os 12 `merit_*` + 1 chamada no `onNodeReady`, ≡ o par que `applyDescFont` já faz (§I107a2). §V290 EMENDADA no mesmo passo (2 templates, & eles ! tilar igual), c/ mutação RODADA: costura entre as metades ! VERMELHO · coluna do nome apagada ! VERMELHO · `<edit>` escondido revelado ! VERMELHO|I107a,I107a1,I107a2,I107b,V339,V290
T789|x|**SONDA na tela [USER] — os 2 comportamentos de SDK que §I107 pressupõe & esta ficha NUNCA exerceu** (§I107n, medido por varredura dos 15 `.lfm`). PRIMEIRA da cadeia & ⊥ depois: §T783/§T784 são ~200 linhas em cima destes 2, & ⊥ ∃ erro de compilação quando um ⊥ ∃ — dá tela que ⊥ responde. 1 fatia MÍNIMA da caixa de verdade (§T784 com 1 linha de resultado em vez de 20 & sem paginação), ⊥ 1 aba descartável: aba nova mexe em ~20 checks amarrados ao literal **19** do gate & seria apagada logo depois. Nada da fatia é jogado fora: **(1)** `onChange` de `<edit>` dispara A CADA TECLA, ou só ao sair do campo?; **(2)** ∃ duplo clique — `onDoubleClick` ou equivalente? (eram **4**: a 2ª volta do pedido do user pôs `<button>` no lugar do `<edit readOnly>` & do `<label>`, & clique em botão é provado ∴ os outros 2 sumiram do risco) O que reprovar volta p/ `/ck:spec` ANTES de código: (1) negativo ! o filtro ganha um botão `Buscar` & §I107h é emendada; (2) negativo ! o aceite fica só no `btnMfOk` & §I107l perde 1 dos 2 caminhos. As 2 quedas são BARATAS — é isso que a 2ª volta comprou. ⚠ **PARCIALMENTE RESPONDIDA na 140ª SEM tela** (§I107n1/§I107n2): o `rdk` é oráculo de NOME (§B80 — atributo desconhecido = exit 1 & `.rpk` apagada) ∴ 3 builds mediram que `onDoubleClick` é REJEITADO, `onDblClick` COMPILA & `<edit onChange>` COMPILA. A pergunta (2) deixa de ser "∃?" & vira "DISPARA?"; o NOME está resolvido. Sobra p/ a tela SÓ o disparo das 2. ⚠ **A FATIA ESTÁ CONSTRUÍDA & INSTALADA** (140ª): `mfSearch` c/ `edtMfQuery` `lblMfLoading` `dynMfRow_1` `btnMfOk` `dynMfProbe` em `WoD20.2.lfm` & as **7** `mf*` no root, tudo forma de §I107 ∴ §T784 CRESCE isto & ⊥ substitui. 1 linha descartável: `btnMfProbe`, o abridor — §I107b abre pelo botão do NOME, que é de §T783 & ⊥ pode vir antes desta resposta. `dynMfProbe` é o INSTRUMENTO: `mfFilter` conta as próprias chamadas, porque "disparou por tecla?" ⊥ é pergunta que o olho responde numa lista de 1 linha. Roteiro de 6 passos no `HANDOFF.md`. ⚠ a fatia gastou **1** local de chunk & ⊥ 6 (§B93, §V347): teto do root = **53**, ele declara **49**. ⚠ **RESPONDIDA NA TELA 2026-08-29** (§I107n3): **(a) REPROVADA** — o `onChange` ⊥ dispara por tecla **NEM** ao sair do campo (clicar fora ⊥ acende nada); dispara no **ENTER** ∴ as 2 hipóteses da pergunta estavam as 2 erradas, & §I107h ganha `Buscar` + Enter · **(b) APROVADA** — `onDblClick` dispara, `mfConfirm` escreveu & fechou ∴ §I107l fica c/ os 2 caminhos. ⊥ medido: o `loading` piscando (§I107f) — a fatia fecha rápido demais p/ o olho & o teste é o item (1) de §T787. **A CADEIA DESTRAVOU:** §T756 §T757 §T759 §T761 §T763 §T783 §T784 §T785 §T787|I107n,I107n1,I107n2,I107n3,I107n4,R128,V339,B80,B93,V347
T784|x|`mfSearch` + `mfOpen`/`mfFilter`/`mfSelect`/`mfConfirm`/`mfClose` + `mfPage(delta)` + os **20** `<button>` `dynMfRow_*` + `btnMfPrev`/`btnMfNext`/`btnMfOk`/`lblMfPage`, reusando o `popScrim` de §I102a (§I107c-n). DEPOIS de §T789 & ⊥ antes. A caixa abre PAGINADA c/ a lista inteira (§I107k), 1 clique MARCA & ⊥ escreve (§I107l), & `mfConfirm` é o único ponto de escrita — botão ou duplo clique chegam nele (§I107i). `btnMfOk` nasce desligado & desliga de novo ao filtrar, ao paginar & ao fechar (§I107m). ⚠ o `require` DENTRO do `setTimeout` é o ponto da tarefa, ⊥ detalhe: fora dele o `loading` nunca pinta. ⚠ teto de locais (§B77)|I107,V339,R128,R44,R47
T785|x|`verify-hunters-hunted.ps1` — §V339 NOVA c/ as pernas & zero-guard (§V340 JÁ ∃ desde §T788). Mutação RODADA (§V20 §V222): `require` fora do timer ! VERMELHO · 19 ou 21 `mfRow` ! VERMELHO · `string.find` sem o 4º argumento ! VERMELHO · timer sem parada em `mfClose` ! VERMELHO · `btnMfOk` nascendo `enabled="true"` ! VERMELHO · 2º ponto de escrita fora de `mfConfirm` ! VERMELHO · sonda (renomear uma `mfRow`) ! VERDE|V339,V20,V222
T786|x|`/ck:spec` — reconferir a `Obertus` pelo 2º eixo (§R129, **Q17**): FEITA 2026-08-28 & o veredito é que §V342 já estava certa. As 2 fraquezas foram lidas lado a lado — `da` p.454 `Obsession Derangement` sobre uma área de conhecimento escolhida · `gr` p.102 `Obsessive-Compulsive` p/ a maioria & `Megalomania` p/ a linha Narov — & as 2 param em PERTURBAÇÃO, que esta ficha ⊥ modela. ⊥ é o caso `Grimaldi`, onde o lado medieval trazia NÚMERO (`only eight blood points`). ∴ o eixo que parte a `Obertus` segue sendo o NAROV & ⊥ nasce entrada por época. Registrado em §R130|R129,R130,V342
T787|x|teste no Firecast [USER] — busca de qualidade/defeito. DEPOIS de §T783…§T785. **(1)** clicar no NOME da linha 1 de Merit abre a caixa & mostra `loading` c/ os 3 pontos ANIMANDO — se a caixa aparecer já cheia, sem piscar o loading, o `require` está fora do timer & §I107f caiu; **(2)** digitar `ambi` filtra; **(3)** escolher preenche nome, Livro, Página e Custo de uma vez & ⊥ dá p/ digitar neles; **(4)** clicar FORA fecha; **(5)** abrir a busca c/ a caixa do `?` ABERTA fecha a do `?` (1 scrim só); **(6)** a 2ª linha abre INSTANTÂNEA — o parse já foi pago (§I107e); **(7)** as 4 últimas linhas seguem digitáveis & ⊥ abrem busca; **(8)** a caixa abre JÁ CHEIA, na página 1, c/ `1 de N` & `<p> de <P>` (§I107k) — ⊥ vazia esperando digitar; **(9)** `>` avança a página & `<` volta, & o fim da lista ⊥ dá página em branco; **(10)** 1 clique numa linha MARCA & ⊥ preenche nada na ficha (§I107l); **(11)** o botão de confirmar está CINZA antes de marcar, liga ao marcar, & volta a CINZA ao digitar no filtro | ao paginar (§I107m); **(12)** DUPLO clique numa linha preenche & fecha de uma vez, ≡ marcar + confirmar (§I107l). ⚠ **PARCIAL 2026-08-29** — o user confirmou "tudo certo" p/ **6** pontos, & eles cobrem os itens **(2) (3) (4) (8) (9) (10) (12)**: filtra ao digitar · escolher preenche nome+Livro+Página+Custo de uma vez · clicar fora fecha · abre JÁ CHEIA na página 1 c/ contador · `<`/`>` paginam · 1 clique MARCA & liga o confirmar · duplo clique preenche & fecha. **⊥ FORAM PEDIDOS & seguem ABERTOS:** **(1)** o `loading` c/ os 3 pontos ANIMANDO — o user já avisou na sonda que ele fecha rápido demais p/ o olho · **(6)** a 2ª linha abrir INSTANTÂNEA (é o que prova que o parse foi pago 1× só, §I107e) · **(7)** as 4 últimas linhas seguirem DIGITÁVEIS & ⊥ abrirem busca · **(11)** o confirmar VOLTAR a cinza ao filtrar & ao paginar. **BLOQUEADO:** **(5)** abrir a busca c/ a caixa do `?` ABERTA — ⊥ ∃ botão `?` nas linhas de qualidade/defeito até `§T761` entrar ∴ ⊥ é testável hoje|I107,V339,T761
T788|x|`verify-hunters-hunted.ps1` — §V343 NOVA c/ as **5** pernas & o zero-guard, **&** §V340 EMENDADA (as **2** tabelas de §I100d, o `cost` vazio NOMEADO em 2 entradas, a contagem **310**/**336**). DEPOIS de §T750. Mutação RODADA antes de aceitar (§V20 §V222): apagar o `tipo` de 1 linha do `merit_flaw.tsv` ! VERMELHO · apagar 1 tradução de `merit_flaw_pt.tsv` ! VERMELHO · `cost = ""` numa 3ª entrada ! VERMELHO · `MERIT_DATA` c/ 309 chaves ! VERMELHO · sonda: trocar a ORDEM de 2 linhas do `.tsv` ! VERDE, porque o que se mede é o conteúdo, ⊥ a ordem|V343,V340,V20,V222
T790|x|`WoD20.2.lfm` — a aba Traits redistribui LARGURA p/ o nome da qualidade/defeito (pedido user 2026-08-28, **3 partes de 1 decisão só & 1 commit só**). **(1)** `BACKGROUNDS` **370→364**, & os 6 saem do AR em volta do conteúdo, ⊥ do conteúdo: as **21** linhas SEGUEM `width="300"` & andam `left="35"`→**32**, & o label vai p/ `20/324` (senão a margem direita cai p/ 14 & §V280a reprova). ⚠ a 1ª escrita desta tarefa mandava encolher as linhas p/ **294**, o que arrasta o picker de antecedente p/ **164** — & §V312c REPROVA: o piso dele é **167** (`Identidade Alternativa`, 22 char a 6,5 px/char + 24 da seta), ⊥ os 156 que §V196 mede a 6,0. Foi assim que o gate ficou vermelho no build & é §B86. ∴ `OpenAbility` & `OpenAbilityFreeRow` ⊥ são TOCADOS: picker **170** & dots em 175/200/225/250/275 nos 2 (§V255 compara os 2 templates & exige os mesmos x); **(2)** as **11** caixas à DIREITA das tabelas **240→225** cada, & as 3 colunas ANDAM p/ a esquerda: `865→910` · `1110→1140` · `1355→1370` (label interno `20/200`→**185**, `textEditor` `35/170`→**155**); **(3)** todo o espaço liberado (6 + 3×15 = **51**) vai p/ as tabelas: `MERITS` & `FLAWS` de `left="375" width="485"` p/ `left="369" width="536"`, & os 51 vão INTEIROS p/ o NOME. A linha interna `left="20" width="445"`→**496** & a geometria de §I100a/§I107a vira `?` **0/20** · nome **20/246** · livro **266/80** · página **346/100** · custo **446/50**; os 4 labels de cabeçalho acompanham em **40/246** · **286/80** · **366/100** · **466/50**. CONTA FECHADA: 364+5+536+5+225+5+225+5+225 = **1595**, o mesmo x de hoje, & os vãos seguem **5** (§V298 intacta). O mapa no topo do arquivo REESCRITO no mesmo commit — §B73 como PRÁTICA & ⊥ como check: nada no gate lê o mapa do `.2` (§V69 lê o do `.1`). §V290 ⊥ precisou de emenda nenhuma, ao contrário do que a tarefa previa: ela ⊥ carrega literal de x — lê o template, cobra encaixe SEM COSTURA (`L+W ≡ próximo L`) & 1 cabeçalho em `rowL + c.L` c/ a MESMA largura ∴ a geometria nova passou nela inteira. MEDIDO: os **11** títulos em [pt] cabem nos 185 (o pior, `BASE DE OPERAÇÕES`, pede 117) · **o nome de 246 mata o corte de §I107a4**: o pior nome em [pt] pede **240** ∴ **0 de 646** cortam, contra 2 hoje|I100a,I101,I107a,V290,V298,V16,V196,V312,V280,V255,B73,B86
T791|x|chave COMPOSTA no módulo de descrição (§I102g): `gen_merit_data.ps1` grava `merit\|<nome>` / `flaw\|<nome>` p/ os nomes que ∃ nas 2 listas (hoje **1**, `Cannibal`) & chave NUA p/ o resto; `popOpen` monta a chave concatenando o `kind` que já recebe. Os 2 idiomas no mesmo commit (§V210a). DEPOIS ⊥ ∃ — ⊥ depende de tela nem de user|I102g,V344,B89,V210
T792|x|`verify-hunters-hunted.ps1` — §V344 & §V345 NOVAS c/ as pernas & os zero-guards. Mutação RODADA (§V20 §V222): 2ª `["Cannibal"]` de volta ! VERMELHO · `merit\|Cannibal` sem o irmão `flaw\|Cannibal` ! VERMELHO · prefixo num nome de 1 lista só ! VERMELHO · 1 `desc*.lua` reescrito com LF nu ! VERMELHO. DEPOIS de §T791 p/ a §V344, & a §V345 já pode entrar hoje|V344,V345,V20,V222,B87,B89
T793|x|`pickerItems` (`WoD20.6.lfm`) — `field` & `current` calculados p/ ∀ combo (as 2 SAEM de dentro do `if filter then`) & append de `current` no par `kept`/`shown` quando ele ⊥ ∈ `kept`. É §C virando código & fecha §B91 nos 14 roots de uma vez, inclusive o `road`: §I99c4 deixa de ser CÓDIGO MORTO (a linha do `odd` exige `current ~= nil` & `cboRoad` ⊥ ∈ `FILTER_NAME` ∴ ela nunca rodou). ⚠ **⊥ usar `string.sub(nm, 4)` p/ todos**: `cboSpeciality1` ! `speciality_1` ∴ o campo se monta c/ `fieldRoot(nm)` + `_` + o número, padrão `_?%d+$` — a grafia sem `_` é a que o template `Speciality` autora (§V346d). ⚠ o `odd` da chave do memo passa a significar "`current` ⊥ ∈ `kept`", ⊥ só o caso do `road` (§V346c). ⊥ criar 2º caminho de append: 1 lugar só, depois do laço & dentro do `if hit == nil`|I27,I99c4,V200,V24,V346,B91
T794|x|`verify-hunters-hunted.ps1` — §V346 NOVA c/ as **5** pernas & o zero-guard. Mutação RODADA antes de aceitar (§V20 §V222): `current` de volta p/ dentro do `if filter` ! VERMELHO · append do órfão apagado ! VERMELHO · chave do memo sem o valor órfão ! VERMELHO · `string.sub(nm, 4)` servindo o `speciality` ! VERMELHO. DEPOIS de §T793|V346,V20,V222,B91
T796|x|`research/gen_merit_data.ps1` + `meritData.lua` + `PICKER_LIST["merit"]`/`["flaw"]` + as chaves `.lang` + o PT map de `WoD20.6` + `descMerit_en.lua`/`_pt.lua` — a partição de custo de **Q24**: as **87** entradas de custo ⊥ fixo viram **313**, & as tabelas fecham em **380**/**493** = **873**. Sufixo ` (N)`, & `book`/`page`/texto IGUAIS entre as irmãs. O GERADOR parte, ⊥ a mão (§I109e). ⚠ arrasta **5** artefatos num commit só (§I109c) & §V340f muda de número junto (311/336/647 → 380/493/873)|I109,V348,V340,Q24
T797|x|`WoD20.2.lfm` — as 4 colunas de **Q25** (§I107a5): `Page` 100→**45** · `Book` 80→**117** · nome 246→**264**, & os 4 cabeçalhos junto. Fecha em **496** ≡ hoje ∴ a caixa ⊥ muda. DEPOIS de §T796 — a régua do nome só é medível c/ os nomes NOVOS|I107a5,V349,V290,Q25
T798|x|`verify-hunters-hunted.ps1` — §V348 §V349 NOVAS c/ as pernas & o zero-guard, & §V340f re-mirada nos números de §T796. Mutação RODADA (§V20 §V222): 1 `cost` voltando a `1-3` ! VERMELHO · sufixo sem irmã ! VERMELHO · `Page` em 30 ! VERMELHO · nome em 246 c/ a lista nova ! VERMELHO · 1 chave só em `descMerit_en` ! VERMELHO|V348,V349,V340,V20,V222
T799|x|`WoD20.6.lfm` — a ordenação de **Q26** em `pickerItems` + `fold` (§I110): ordena o PAR depois do órfão, vazio pregado no índice 1, dentro do ramo do memo, & os `cmb*` + os 2 `cbo*` de estado FORA|I110,V350,V202,V15,Q26
T800|x|`verify-hunters-hunted.ps1` — §V350 NOVA c/ as pernas & o zero-guard. Mutação RODADA (§V20 §V222): ordenar só `shown` ! VERMELHO · ordenar ANTES do órfão ! VERMELHO · vazio entrando na ordem ! VERMELHO · `fold` sem a tabela de acento ! VERMELHO · `table.sort` em `cmbItems` ! VERMELHO|V350,V20,V222
T801|x|`WoD20.1.lfm` — a linha do `ROAD` de **Q27** (§I99k): rótulo **20/20 w46** & `cboRoad` **71/20 w239**, tudo abaixo sobe **25** & a caixa vai a **204**; `SPECIALTIES` 382→**357** & o avatar (os 3 controles) 437→**412** NO MESMO COMMIT (§I99l), & o mapa no topo do arquivo reescrito junto. ⚠ `cboRoad` entra como exceção NOMEADA de §V196/§V312c (§I99m) & §V69 muda de **878** p/ **853**|I99k,I99l,I99m,V351,V69,Q27
T802|x|`verify-hunters-hunted.ps1` — §V351 NOVA c/ as pernas & o zero-guard, & §V69 re-mirada em **853**. Mutação RODADA (§V20 §V222): rótulo voltando p/ fileira própria ! VERMELHO · 1 das 3 caixas de baixo ficando em 878 ! VERMELHO · 2ª entrada na exceção de §V196 ! VERMELHO · rótulo em 30 ! VERMELHO|V351,V69,V20,V222
T803|x|`verify-hunters-hunted.ps1` — §V331 & §V332 REESCRITAS c/ as pernas que sobraram & o zero-guard (§V331 = a/b/c · §V332 = a…f contra §I101h), & o `readOnly` de §V331b ENTRANDO nos 3 `<edit>` de `MeritPicked` (é mudança de código, ⊥ só check). Mutação RODADA (§V20 §V222): `m7` apontando p/ `MeritPicked` ! VERMELHO · tirar `readOnly` de 1 `<edit>` picked ! VERMELHO · 1 das 14 caixas fora do x de §I101h ! VERMELHO · 2 caixas sobrepostas ! VERMELHO · `ARTIFACTS` c/ `field` novo ! VERMELHO. ⚠ **SUBSTITUI `§T759`**, que fica `.` DE PROPÓSITO — id ⊥ se reusa, & a linha dela mede contra spec morta|V331,V332,I101h,V20,V222
T804|x|**SONDA na tela [USER] — o SDK dá FOCO?** (§I111, **Q28**). PEGA CARONA na sessão de §T787, ⊥ nasce sessão nova. 1 linha dentro do `setTimeout` de `mfOpen`, DEPOIS de `mfSearch.visible = true`, & os 3 candidatos de §I111c em ordem, parando no 1º que funcionar. ⚠ **§B80 ⊥ responde esta**: ele é oráculo de ATRIBUTO & chamada de método Lua compila sempre ∴ só a tela diz. Queda BARATA: reprovada, a caixa fica como hoje & Q28 vira linha de §C|I111,Q28
T805|x|`WoD20th.lfm` + `verify-hunters-hunted.ps1` — o foco de §I111c no `mfOpen` & §V353 c/ as pernas & o zero-guard. DEPOIS de §T804 & ⊥ antes: escrever contra nome que ninguém mediu é §B92|I111,V353,T804
T806|x|`WoD20.2.lfm` — a caixa cresce & ganha a pane (§I112a-c): `mfSearch` `left="440" width="520"` → **`left="300" width="1000"`**, `edtMfDesc` em **520/460** c/ o par **`btnFontUpMf`/`btnFontDownMf`** (⚠ esta linha dizia `btnFontUpMfDesc`/`btnFontDownMfDesc` & estava ERRADA: §V334b DERIVA o sufixo do nome da pane — tira `edt` & `Desc` — & as 8 panes velhas já obedecem. Corrigido 2026-08-29, user confirmou que o lado errado era o texto), & a pane entra em `DESC_PANES` ∴ §V334 vai de **8**/**16** p/ **9**/**18**. A coluna de resultado ⊥ se mexe|I112,V334,V333,Q29
T807|x|`WoD20th.lfm` + `verify-hunters-hunted.ps1` — `mfSelect` pinta a pane · `mfFilter`/`mfPage` a LIMPAM · o `require` de `descMerit_` entra no `setTimeout` de `mfOpen` · **1** função de busca de texto servindo `popOpen` **&** a pane. §V352 NOVA c/ as pernas & o zero-guard. Mutação RODADA (§V20 §V222): pane como IRMÃ de `mfSearch` ! VERMELHO · `require` movido p/ `mfSelect` ! VERMELHO · `mfFilter` sem limpar a pane ! VERMELHO · 2ª função montando a chave ! VERMELHO · pane c/ `field` ! VERMELHO|I112,V352,V334,V135,Q29
T808|x|**PILOTO de §I113 — antecedente** (`OpenAbility`, **16** linhas, **47** itens, SEM filtro): a caixa genérica parametrizada por LISTA + CAMPO + MÓDULO (§I113a-b), as 16 linhas trocando `comboBox` por `<button>` + `<edit>` gêmeo `readOnly` (§I107a1). ⚠ **1** tabela local & todo helper GLOBAL (§I113g, §B93). DEPOIS de §T806/§T807 — a caixa que ela generaliza ! estar na forma final|I113,V354,V13,B93,Q30
T809|x|`verify-hunters-hunted.ps1` — §V354 pernas (a) (b) (d) (e) (g) contra o PILOTO, & o zero-guard. Mutação RODADA (§V20 §V222): 1 das 16 linhas ficando `comboBox` ! VERMELHO · `<edit>` gêmeo sem `readOnly` ! VERMELHO · caixa lendo lista própria em vez de `PICKER_LIST` ! VERMELHO · órfão sumindo ! VERMELHO · ordem alfabética perdida ! VERMELHO|V354,V350,V200,V20,V222
T810|x|teste no Firecast [USER] — o PILOTO na tela. AGRUPAR c/ §T787 & §T804. **(1)** clicar no nome de uma linha de antecedente abre a caixa c/ os 47 + o vazio; **(2)** filtra ao digitar; **(3)** escolher preenche & ⊥ dá p/ digitar; **(4)** a descrição aparece ao lado ao MARCAR; **(5)** as 5 linhas digitadas (`background_17`…`_21`) seguem digitáveis & ⊥ abrem caixa; **(6)** o `?` da linha segue abrindo a caixa do `?`, ⊥ a de busca|I113,I112,T808
T811|x|ONDAS 2 a 5 de §I113e — **⊥ buildar antes de §T810 passar.** Cada onda é 1 §T própria, criada por `/ck:spec` QUANDO a anterior fechar, & a ordem é a de §I113e: sem filtro → filtradas por nível → filtradas por afiliação → avulsos c/ regra própria. ⚠ a onda 3 é a que precisa da porta de §I113c (o filtro fora do `pickerItems`) & ela ! ser MEDIDA antes de escrita — é a mesma armadilha que §I107n pagou por declarar queda barata cedo demais|I113,V354,Q30
T812|x|`WoD20.1` `WoD20.2` `WoD20.3` `WoD20.7` `.lfm` — a GEOMETRIA & os RÓTULOS de §C (143ª 2ª metade): `merit_m0`/`merit_f0` no topo das 2 tabelas c/ as caixas **311→336** & `FLAWS` **316→341** · `Page` & `Cost` centralizados nos 2 templates · rótulo `ROAD` **20/80** & `cboRoad` **105/205** · bearing em **1** label `20/74 w290` centralizado @**0,80** & `dynBearingMod` REMOVIDO · os 3 títulos `WILLPOWER` de volta em CAPS. ⚠ **FEITA ANTES desta linha ∃** — o user pediu direto numa sessão de build & a spec veio depois|C,I99e,I99k,I101h,I114,V331,V332,V333,V351
T813|x|`WoD20th.lfm` + `localization.lang` + `WoD20.6.lfm` — o CONTRATO DE EXIBIÇÃO de §I114: `mfShown` & `fitSize` NOVAS & GLOBAIS (§I113g — o root está em **50** de 53) · `mfLabel` pintando os 2 estados · `mfPaint` traduzindo · `mfFilter` ordenando por `foldKey(mfShown)` · o vazio entrando na pool só c/ valor & `mfConfirm` deixando `""` passar & limpando `book_`/`type_`/`costy_` junto · `renderMeritButtons` de **0** a 6 & o `dataLink` c/ as 14 chaves · `renderBearing` dimensionando `cboRoad` · chaves `Remove` & `Select Background` nas **3** casas. ⊥ ∃ §V no gate p/ nada disto — §T815 é quem paga|I114,V355,V24,V15,V350,C
T814|x|`verify-hunters-hunted.ps1` — as réguas re-miradas & as **2** AFROUXADAS c/ motivo MEDIDO: §V244 vira lista nomeada (`0.75` `0.60`; `0.40` segue fora do Lua) · §V305c tira `fontStyle` da proibição cega c/ **3** pernas, & a premissa (`WoD20.6` ⊥ nomear `fontStyle`) passa a ser MEDIDA · §V330a **2** controles · §V331a `m0`/`f0` · §I101h o mapa · §V333g **30** · §V334b **9**/**18** + o roster de `DESC_PANES` · §V339a **14** · §V351 perna (f) · §V354d o `table.sort` c/ comparador. Mutação RODADA (§V20 §V222) — **9** VERMELHAS: `0.40` em Lua · 2º escritor de `fontStyle` · `WoD20.6` nomeando `fontStyle` · `m0` virando digitada · `FLAWS` ⊥ descendo os 25 · ordenar pelo EN · rótulo `ROAD` espremido · `renderBearing` sem dimensionar · `fitSize` sumida|V244,V305,V330,V331,V332,V333,V334,V339,V351,V354,V20,V222
T815|x|`verify-hunters-hunted.ps1` — **§V355** c/ as **9** pernas & o zero-guard. Todas as pernas são de FORMA de código ∴ mensuráveis JÁ, ⊥ esperam tela (§I114i é outra coisa). Mutação RODADA (§V20 §V222): 2º lugar decidindo exibição ! VERMELHO · `mfLabel` sem um dos 2 estados ! VERMELHO · 2º escritor de `fontStyle` ! VERMELHO (& ! bater c/ a contagem de §V305c) · vazio entrando na pool sem valor no campo ! VERMELHO · escolher vazio ⊥ limpando as 3 colunas ! VERMELHO · roster de rótulo em vez da derivação ! VERMELHO · `fitSize` sem piso ou contando byte ! VERMELHO · ordem sem `foldKey` ! VERMELHO|V355,I114,V20,V222
T816|x|teste no Firecast [USER] — §I114i, o que ⊥ dá p/ medir fora da tela. **AGRUPAR c/ §T787 §T804 §T810**, ⊥ nasce sessão nova. **(1)** a linha vazia lê `-- Select Merit --` em ITÁLICO & apagada (se o itálico ⊥ pintar, os `--` & a opacidade já dizem "vazio" ∴ queda BARATA); **(2)** escolher um valor tira o itálico (o `""` limpa a propriedade de volta?); **(3)** um nome longo ENCOLHE em vez de cortar — no botão & no `cboRoad` (é isto que diz se `ctrl.width` é legível); **(4)** `-- Remove --` ⊥ aparece em linha vazia, aparece em linha cheia, & escolher ele limpa nome + livro + página + custo; **(5)** as 2 linhas novas `m0`/`f0` abrem a caixa & escrevem na própria linha; **(6)** o bearing lê como **1** frase centralizada|I114,V355,T813
T817|x|`verify-hunters-hunted.ps1` — **§V356** c/ as 3 pernas & o zero-guard. ⊥ toca `.lfm` nenhum: os 6 combos JÁ estão na forma certa (medido 2026-08-30), o que falta é a RÉGUA. ⚠ 2 dos 6 (`language` & `cmbBgCost`) ⊥ têm guarda de TIPO nenhuma hoje; os outros 4 têm por ACIDENTE, de checks que ∃ p/ outra coisa (§V109 §V52 §V234/§V243 & o de `cmbSpecCost`) ∴ §V356 ⊥ é redundante com eles — é a 1ª que mede a REGRA. Mutação RODADA (§V20 §V222): 1 dos 6 virando `<button>` ! VERMELHO · `items` fora de 1 dos 6 ! VERMELHO · 2º widget no mesmo `field` ! VERMELHO|V356,C,I113e,B95,V20,V222
T818|x|`WoD20th.lfm` — tirar `MF.btn` (§B96): apagar `btn = nil` da tabela `MF`, `MF.btn = from;` de `mfOpen` & o bloco `if MF.btn ~= nil then … mfLabel(MF.btn, MF.picked, MF.title, lang); end;` de `mfConfirm`, junto com o comentário que cita §I107a2 ali — a citação está ERRADA: §I107a2 nomeia `renderMeritButtons` + `dataLink`, ⊥ escrita direta no botão. ⊥ tocar `mfLabel` nem `mfShown` nem `fitSize`: os 2 estados & os 2 números são de §V355(b) & seguem inteiros. Vale p/ qualidade, defeito & antecedente de uma vez — a caixa é a mesma (§I113)|B96,V357,I107a2
T819|x|`verify-hunters-hunted.ps1` — **§V357** c/ as 3 pernas & o zero-guard. Mutação RODADA (§V20, §V222): `mfLabel(MF.btn, …)` de volta no `mfConfirm` ! VERMELHO · `MF.btn = from;` de volta no `mfOpen` ! VERMELHO · `from.opacity = 1;` numa função qualquer do root ! VERMELHO · sonda: 1 `mfLabel(found[…], …)` a mais num renderer ! VERDE|V357,V20,V222
T820|x|teste no Firecast [USER] — DEPOIS de §T818. **AGRUPAR c/ §T787 §T804 §T810 §T816** (todas de tela): escolher `-- Remover --` (1) numa linha de QUALIDADE, (2) numa de DEFEITO & (3) numa de ANTECEDENTE. Em ∀ 3 a aba ⊥ esmaece · ⊥ vira itálica · ⊥ muda de tamanho de fonte; só a LINHA limpa fica `-- Selecionar X --` itálica @ 0,60, & Livro/Página/Custo esvaziam junto (§I114e, §V355e). Depois escolher um valor NORMAL & conferir que a aba segue em opacidade 1 — foi o ramo `opacity = 1` que ESCONDIA §B96|T818,B96,I114e
T821|x|`WoD20.6.lfm` — o repasse do enfeite (§B97, §V358). **(1)** o memo ganha o CONTROLE: `ornPainted[c.handle] = {p, w, h, s}` → `{p, w, h, s, c}` na criação · **(2)** nasce `function ornRemeasure()` **GLOBAL** (⊥ `local` — o chunk está em **99** de **100**, §V325 §B93) & DECLARADA abaixo de `ornament()` (§V223 §B51) ≡ `refreshOrnament`: percorre `ornPainted` & chama `ornament(e.c, nil, ornColour)` c/ `w`/`h` **nil** ∴ a 3ª porta relê `c.width`/`c.height` & refaz só o que MUDOU — o carimbo de §V284a/§V313c já paga essa comparação ∴ ⊥ nasce régua nova · **(3)** o `onChange` do `dataLink sheetTheme` ganha `setTimeout(function() ornRemeasure(); end, <N>)` DEPOIS de `sheetReveal(self)`. ⚠ `<N>` > **0**: `setTimeout` só é assíncrono c/ intervalo positivo (§R44) & `0` rodaria no MESMO tick, antes do alinhamento — que é o bug de volta. RECOMENDAÇÃO **1**, & só sobe se §T823 mostrar que ⊥ bastou. ⊥ toca `applyTheme` & ⊥ toca desenho NENHUM: os 4 estilos estão certos, o que estava errado era o `w`/`h` que eles receberam|V358,B97,I72e,V325,R44
T822|x|`verify-hunters-hunted.ps1` — **§V358** c/ as 6 pernas & o zero-guard. Mutação RODADA antes de aceitar (§V20 §V222): repasse ANTES de `sheetReveal` ! VERMELHO · corpo trocado por `applyTheme` ! VERMELHO · `c.width` passado como `w` ! VERMELHO · `local function ornRemeasure` ! VERMELHO · sonda: intervalo do `setTimeout` 1→50 ! VERDE|V358,V20,V222
T823|x|teste no Firecast [USER] — §B97 é de TELA & o gate ⊥ alcança. **AGRUPAR c/ §T787 §T804 §T810 §T816 §T820**. ANTES do conserto, & são as **3** previsões que separam esta causa de qualquer outra: **(1)** c/ a tela errada, trocar a época no combo & voltar CONSERTA as 73 — se ⊥ consertar, a causa ⊥ é a medida & §T821 é o conserto errado · **(2)** c/ a tela errada, as **2** caixas de vitalidade estão CERTAS enquanto as outras 71 estão erradas, porque só elas passam por `refreshOrnament` (`WoD20th.lfm:1848`) · **(3)** o defeito atinge as **73** de uma vez, ⊥ 1 aba. SONDA do número que §B97 ⊥ mediu: imprimir `c.width`/`c.height` na CRIAÇÃO p/ 1 caixa de **225×153**. DEPOIS de §T821: abrir & fechar a ficha **5×**, 1 delas logo após `rdk -i`, & ⊥ ∃ enfeite espremido em nenhuma|B97,V358
T824|x|`WoD20.6.lfm` + `localization.lang` — a VERSÃO p/ `0.1.1` (§I120, §V363, pedido 7). Os **2** lugares no MESMO commit: `text="Version: 0.1.0"` do `<label>` & a chave `["Version: 0.1.0"] = "Versão: 0.1.0"` do `.lang`, que vira `["Version: 0.1.1"] = "Versão: 0.1.1"`. ⊥ tocar `<version>` do `module.xml` — é do rdk (§B2). É a linha mais barata das 7 & fecha sozinha|I120,V363,V10
T825|x|`verify-hunters-hunted.ps1` — **§V363** c/ as 3 pernas & o zero-guard. Mutação RODADA (§V20 §V222): mexer no `text=` sem mexer na chave ! VERMELHO · 2º rótulo de versão ! VERMELHO · sonda: mudar `<version>` do `module.xml` ! VERDE, porque a perna ⊥ olha p/ lá de propósito|V363,V20
T826|x|`WoD20.2.lfm` + `WoD20th.lfm` — a caixa em 2 tamanhos (§I115, §V359, pedido 2). `mfOpen` ganha 1 parâmetro de tamanho ≡ `mod` já é; o `Specialties` abre `narrow` & o resto segue `wide`. O que encolhe é a metade DIREITA — a coluna de resultado fica em `20/480` nos 2 (§V352c). ⚠ DEPOIS de §T827: a pane é quem define o que "ajustado aos campos" quer dizer, & medir a largura antes de a pane ter moldura é medir o vazio|I115,V359,T827
T827|x|`WoD20.2.lfm` + `WoD20th.lfm` — a pane de descrição ganha DIVISÃO & título (§I116, §V360, §B98, pedido 3). Título `Description` como `<label>` c/ chave `.lang` nos 2 idiomas · uma divisória vertical IRMÃ da pane · `No description available` (chave `.lang`) quando a lista ⊥ tem módulo · o mapa lista→módulo sai de `MF.mod`, ⊥ de roster novo. ⚠ **§B98 fecha AQUI ou ⊥ fecha**: a pane já é escrita hoje & ⊥ pinta, & a causa ⊥ é de FORMA (medido — o wiring está inteiro no fonte) ∴ esta rodada ! ver a pane pintando na tela ANTES de marcar `x`, senão só ganhou moldura em cima de um defeito vivo|I116,V360,B98,V344
T829|x|**[USER] DECIDE antes de construir** — §Q32, a contagem dos slots de Numina (§I117c). `psychic` hoje = **24** & o pedido diz 20 ∴ 4 somem; `numina` hoje = **17** & o pedido diz 20 ∴ 3 nascem. A frase "nada removido ou criado" ⊥ cobre isso, & ⊥ dá p/ construir os 2 sentidos. §T830 & §T831 ficam BLOQUEADAS até esta responder|I117,Q32,V362
T832|x|`WoD20.11.lfm` — aba Ghoul SEM sub-abas, 3 colunas (§I118, §V361, §V362, pedido 5). **BLOQUEADA por §T872** (onda 3), que é quem dá `?` a `disc`/`mainPath`/`secPath`/`ritual` (§I131g) ∴ só ENTÃO as 3 panes de `WoD20.12`/`WoD20.13`/`WoD20.14` podem sair (§V365(d), §B105 — tirar antes deixa a linha SEM superfície de descrição nenhuma). ⚠ **§T846 ⊥ tira pane NENHUMA** (REESCRITA na 148ª, §B105) ∴ o bloqueio citado nunca valeu. RAIZ: **§T810**, via §T811. ⚠ o CÓDIGO desta §T já saiu inteiro & CERTO na 157ª & foi REVERTIDO — o custo dela é de **GATE** (~20 réguas, §I118e), ⊥ de layout (as 3 panes saem antes: listas somam **1475** contra 1070, §I118d). Mesma forma de §T830 & é de propósito (§V361d): col.1 `Clan Disciplines`/`Disciplines` · col.2 `Main Paths`/`Secondary Paths` · col.3 `Blood Sorcery Rituals`. ⚠ MEDIDO 2026-08-30: os 3 arquivos importados JÁ SÃO essas 3 colunas — `WoD20.12` traz as 2 caixas da col.1, `WoD20.13` as 2 da col.2, `WoD20.14` a da col.3 ∴ o trabalho é tirar o `vampStrip` & pôr os 3 `<import>` lado a lado a ~350 cada, ⊥ redesenhar caixa nenhuma. ⚠ **A DÍVIDA de §V225(b) FOI LIQUIDADA AQUI, & ⊥ do jeito que esta linha prometia.** A promessa era "as 3 colunas & a `BLOOD POOL` fecham no MESMO x"; MEDIDO ao construir, elas fecham em **390** / **960** / **1485** contra **1070** & ⊥ ∃ desenho que as concilie, porque as 3 larguras são DIFERENTES de propósito (§I118d). O que (b) comprava migrou p/ **§V221(3)+(4)** — disjunção em x + largura da coluna ≡ largura declarada pelo form dela — que pega a MESMA deriva mais cedo, por sobreposição em vez de por borda. §V225(b) sai de vez & §V225(a) fica, medindo ∀ import contra o `<layout>` DELE|I118,V361,V362,T872,T810,V365,V225,V221
T833|x|`WoD20.11.lfm` — a bloodpool centralizada verticalmente no box (pedido 5.4) & `Clan/Family` + `Max Discipline Level` subindo, descolando do fundo (pedido 5.5). 2 ajustes de posição, 0 `field` tocado ∴ §V362 ⊥ pode acender por eles — se acender, o "reajuste" comeu campo|I118,V362
T834|x|`verify-hunters-hunted.ps1` — **§V362** c/ as pernas que têm sujeito. ⚠ **§V361 SAIU desta linha & mora em §T830** (medido 2026-09-01, ↓). ⚠ **PARTIDA EM 2 2026-09-01 (160ª): só o que TEM SUJEITO entra hoje.** MEDIDO: a Numina ⊥ perdeu as sub-abas (`numStrip` & `hedgeStrip` ∃), a força de vontade ⊥ saiu dela (17 leituras) & `psychic_16`…`_19` ⊥ foram apagados ∴ §V361(a)(d)(e) cobradas das 2 abas & §V361(c) & §V362(c)(d) nascem VERMELHAS contra código CERTO (§B106) ou medem conjunto VAZIO (§B7). **ENTROU**: **§V362** c/ (a) (b) (e), & `$FIELD_CONTRACT` congelado em `b5111c35` — **325** nomes lidos do XML PARSEADO, ≡ o gate lê (a 1ª extração pegou `willpower_c*` de dentro de um COMENTÁRIO & mediu 328). ⚠ ela PAGOU no 1º disparo: `discSel` `pathSel` `ritualSel` sumiram em §T872 & ⊥ ∃iam em lista nenhuma de §I3 — entraram como ÓRFÃO. **⊥ ENTROU: §V361 INTEIRA**, & ⊥ por metade nenhuma — as 4 pernas caem cada uma por um motivo diferente (2 donos p/ 1 régua · absorvida por §V362(a) · sem sujeito · de 2 abas por natureza), tudo MEDIDO & escrito na emenda de §V361. Ela vai p/ **§T830** de uma vez, c/ as 2 abas na mesa, & §V362(c)(d) junto Mutação RODADA (§V20 §V222): apagar 1 `field` de qualquer das 2 abas ! VERMELHO por §V362a · devolver um `tabControl` à Numina ! VERMELHO · tirar a força de vontade da lista de exceção ! VERMELHO · reusar um id vago num slot novo ! VERMELHO · sonda: mover uma coluna 10px ! VERDE|V361,V362,V20
T835|x|`WoD20.9.lfm` — aba Experience **+20px** nos boxes (§I119, pedido 6). Em cima o ganho se redistribui; embaixo ele vai INTEIRO p/ a coluna `Trait`. ⚠ medir contra §V37/§V40 (sobreposição) & §V258 (folga do canto) ANTES de fechar — 20px é exatamente a ordem de grandeza que a mordida do `cornerType` come (§B53, §R96)|I119,V37,V40,V258
T837|x|**[USER] DECIDE** — §Q31: `dominatorGen` (`4`…`14`) & `hedgeAttr` são combos de VALOR, ⊥ de prosa. RECOMENDAÇÃO: ficam dropdown, & a régua de §V356 passa de "combo de aba de ajuste" p/ "combo de VALOR" — o que a separa de prosa é `items`/`values` AUTORADOS & ⊥ ler `PICKER_LIST`, que é a razão que §V356b já escreve. Sem isto, o jogador abre busca c/ 11 números dentro|Q31,V356,I113
T838|x|`/ck:research` — o TEXTO da lista `nature`/`demeanor`, & SÓ dela (§R135, §I102e, §I116c, §Q33). ⚠ **ENCOLHIDA na 150ª: era a §T das 6 listas & virou a de 1**, porque "1 rodada por lista" já estava escrito no corpo dela & §T com 6 entregas ⊥ tem status que diga a verdade — ela ficou `~` c/ 1/6 pronta, que lê igual a 5/6 pronta. As outras **5** saíram p/ §T848…§T852, 1 por lista. ENTREGUE: `descNature_en.lua` & `descNature_pt.lua` na raiz do plugin, **46** entradas cada, chaves IDÊNTICAS nos 2 & ⊂ as **68** do picker (0 órfã), forma de §I21 verbatim. PT é TRADUÇÃO & ⊥ extração — ⊥ ∃ livro em português destes arquétipos, & é o que `descBackground_pt.lua` já faz; terminologia dos módulos PT que já ∃ (**Força de Vontade** · **Narrador** · **Cainita** · **Príncipe** · **Clã** · **Sabá** · **Anarquista** · **Abraço**, & `Masquerade` fica em INGLÊS). Fonte MULTI-LIVRO, medida em §R135 (V20 33 · DA20 6 · M20 5 · W20 2). **PARCIAL POR DECISÃO DO DONO, ⊥ por atraso**: os **22** sem texto caem na frase de §V360c pelo caminho de §V364b, que é a resposta CERTA (§V365c), & **6** deles ⊥ ∃ em nenhum dos 25 PDFs. `popOpen` passou a nomear `'Nature'` no `HeaderPicker` (`WoD20.1.lfm:105`) — sem isso o `?` ⊥ acende, & é metade da entrega (§I102e). Pipeline versionado em `research/` (`arch_extract.pl` `gen_nature.pl` `arch_qa_words.pl` `arch_body_*.tsv` `arch_names.txt`) c/ receita & armadilhas no `research/README.md` ∴ refazer ⊥ é do zero. ⚠ **`x` cobre o ARQUIVO & o gate VERDE; a confirmação de TELA é [USER]** & anda junto c/ §T846 — abrir o `?` de Nature/Demeanor & ver o texto do livro, DEPOIS de instalar (§B103)|R135,I102e,I116,Q33,V360,V364,V365,V366
T839|x|`WoD20th.lfm` — o `require` do módulo de descrição vira GUARDADO (§V364, §I116c emendada, §B99). Dentro do `setTimeout` de `mfOpen`, `require("desc" .. MF.mod .. "_" .. lang .. ".lua")` passa a rodar em `pcall` & o ramo de falha SEGUE montando a lista — a descrição é acessório, a lista é o motivo da caixa ∃. ⊥ tocar o `require("meritData.lua")` logo abaixo: ele ∃ sempre & guardá-lo esconderia build quebrado (§V364d, §B19). ⚠ **DESBLOQUEIA §T836**: sem isto, converter `speciality` p/ a caixa estoura no `require` de um módulo que o user decidiu que ⊥ vai existir (§Q33)|V364,I116,I107,B99,T836
T841|x|`verify-hunters-hunted.ps1` — **§V354 REESCRITA** (§B100). ⚠ **ANTES da onda 2 de §I113e** (§T842): sem isto a onda entra sem guarda, que é §B100 acontecendo 2ª vez. (b) vira COERÊNCIA — a entrada do roster `$V354_WAVE` passa a levar `Mod` OPCIONAL & as **2** saídas são VERDES (c/ módulo → a abertura nomeia & os 2 `desc<Mod>_*.lua` ∃; sem módulo → a abertura ⊥ nomeia módulo & ⊥ ∃ arquivo a cobrar) · (e) passa a ler as **3** formas (NUMERADA `field="<root>_N"` · AVULSA `field="<root>"` c/ >1 raiz · INDIRETA `num="N"` c/ o `field` nascendo DENTRO do template), & a entrada do roster sai de **1** `Root` p/ LISTA de raízes c/ FORMA & contagem próprias cada · (h) sai de dentro de (d), vira perna NOMEADA & roda p/ ∀ raiz nas 3 formas · (i) NOVA — completude do roster pelo lado do código: ∀ template c/ `<button … onClick="mfOpen(">` ∈ roster ou ∈ exceção NOMEADA (hoje **1**: `MeritPicked`) · (g) zero-guard por RAIZ. O roster segue NOMEADO, ⊥ curinga (§V328d) — & o comentário do bloco cita `V328c` onde quer dizer `V328d`, conserta junto. Mutação RODADA antes de aceitar (§V20 §V222): onda sem `Mod` ! VERDE · onda c/ `Mod` declarado & `desc<Mod>_pt.lua` fora do disco ! VERMELHO · 1 dos **68** arquétipos sem chave [pt] ! VERMELHO · 1 raiz da onda avulsa sumindo do roster ! VERMELHO · raiz INDIRETA contada por `field=` no call site ! VERMELHO (acha 0) · raiz declarada c/ **0** linha no `.lfm` ! VERMELHO por (g) · template novo c/ `mfOpen(` fora do roster & fora da exceção ! VERMELHO por (i) · `MeritPicked` saindo da exceção ! VERMELHO por (i) · sonda: trocar a ORDEM das raízes no roster ! VERDE|V354,B100,V20,V222,V328
T842|x|**ONDA 2 de §I113e** — as SEM filtro que sobram viram caixa de busca (§V354, §T836, §T811). BLOQUEADA por §T841 (a régua ! saber contar as 3 formas ANTES de a onda entrar, senão é §B100 2ª vez), por §T839 (sem o `pcall` a caixa ESTOURA no `require` de `speciality`, que ⊥ terá módulo NUNCA — §Q33, §V364) & por §T827. ESCOPO MEDIDO 2026-08-30, & são **2** templates, ⊥ os 4 que §I113e lista: `HeaderPicker` (`WoD20.1.lfm`, **2** linhas AVULSAS — `nature` & `demeanor`, & `PICKER_LIST["demeanor"]` é ALIAS de `["nature"]` ∴ **68** arquétipos servem as 2 raízes; SEM módulo de descrição hoje ∴ a pane cai na frase de §V360c até §T838 trazer o texto) · `SpecialityRow` (`WoD20.1.lfm`, **7** linhas INDIRETAS `num="N"` c/ `field="speciality_$(num)"`; SEM módulo p/ SEMPRE, §Q33). FORA da onda, & os 2 são erro de §I113e: `HeaderNarrow` ⊥ tem `comboBox` NENHUM — é `<edit field="$(field)">` de texto livre (`name` `player` `chronicle`) ∴ ⊥ ∃ dropdown a converter; `cboHedgeAttr` FICA dropdown por §Q31 RESPONDIDA (§T837 — combo de VALOR, §V356). Corrigir §I113e é `/ck:spec` sobre §I113, ⊥ desta §T. ⚠ **a dívida ESCONDIDA da onda é §V161**: a trava do brinde de especialidade é `enabled = false` no `comboBox` & a razão ESCRITA lá é "⊥ ∃ hook de clique em `comboBox` ∴ é a única trava possível ali" — o `<button>` TEM `onClick` ∴ a trava passa a ser RECUSA dentro do abridor, ≡ a bolinha já faz no `xpClick`, & a razão de §V161 deixa de valer p/ esse widget (§V161, §V255d, `renderSpecialities`, §I113d)|V354,I113,Q33,Q31,V161,V255,T841,T839,T838
T845|x|`WoD20th.lfm` — **o `?` abre EDITÁVEL p/ o narrador** (§I102i, §V365f, user 2026-08-30). É o MECANISMO, & vem 1º porque §T846 apaga as 7 panes & sem isto o narrador fica sem onde escrever. `popOpen` passa a escrever `edtPopDesc.readOnly = not isStoryteller()` & `edtPopDesc.opacity` na MESMA linha (§V162, ≡ `edtSpentXP` em §I8), & ganha o `onChange` que grava por `descFieldOf(<linha>)` — o MESMO caminho de `saveNuminaDesc` ∴ ⊥ nasce 2º dono do texto (§V135). ⚠ `popOpen` recebe `kind`+`key`+`sub`, ⊥ a LINHA: p/ gravar ela precisa saber QUAL linha abriu, & isso é 1 parâmetro a mais ≡ `mod` já é em `mfOpen` (§I113a) — ⊥ inventar roster de "qual `?` é de qual linha" (§I114f). ⚠ teto de locais do root: **50/50** medido 2026-08-30 (real 53, §V347) ∴ helper novo aqui ! ser GLOBAL, & `local function` é build morto sem mensagem (§I113g, §B19, §B93)|I102,V365,V162,V135,I113a,V347
T846|x|**`?` no `HeaderPicker` (nature/demeanor), & SÓ nele** (§I102h, §V365, §T845). ⚠ **REESCRITA na 148ª (2026-08-30, 2 decisões do user — §B105)**: era "`?` em TODA família de picker & as 7 panes SAEM" & encolheu p/ **1** família & **0** pane. **(1) `SpecialityRow` SAIU do roster** — o user decidiu que `speciality` ⊥ leva `?`, & §Q33 já dizia que ele ⊥ terá arquivo NUNCA ∴ o botão diria `No description available` p/ SEMPRE. Ele é a EXCEÇÃO NOMEADA de §V365a & pôr `?` nele ! FAIL igual. **(2) as 7 panes ⊥ saem aqui** — CADA pane sai na rodada em que a família DELA ganha `?` (ondas 3-5 de §I113e), porque tirar antes deixa a linha de Numina/Ghoul SEM superfície de descrição nenhuma, nem pane nem `?`, que é o recurso apagado de lado de §I102i. ∴ §T830 & §T832 seguem BLOQUEADAS & o custo foi ACEITO pelo user (§B102: Numina 1622 contra 1410, Ghoul 1475 contra 1070). ⚠ **§I117e §I118d §I103 §V334 ficam ADIANTADAS em relação ao código**: as 4 prometem as 7 panes fora & o zoom caindo p/ 2 alvos, & §V334 mede por CONTAGEM ∴ ⊥ emendar §V334 p/ 2 panes/4 botões antes de as panes saírem, senão ela nasce VERMELHA contra código certo (§B92). GEOMETRIA, & é o CONSTRUÍDO, ⊥ proposta (medido 2026-08-30, ⊥ REMEDIR): `WoD20.1.lfm`, 2 sítios (`nature` l.254, `demeanor` l.264) na coluna `left=326`. ANTES: label 0..105 `horzTextAlign="trailing"` · botão `dyn$(field)` 112..327 · `<edit>` gêmeo no mesmo left/width, tudo dentro de `<layout width="327">`. DEPOIS: label 0..105 **INTACTO** · `?` 108..128 · botão & gêmeo `left="132" width="195"` = 132..327 ∴ **o layout ⊥ mudou & a calha de 10px ⊥ foi usada** — os 20 do `?` saíram INTEIROS do botão, que tinha 215. ⚠ **§V26 OBRIGOU a 2ª metade, & ela ⊥ estava prevista**: o `?` ⊥ pode deslocar a entrada, & a coluna `left=326` mistura `HeaderPicker` c/ `Header` (concept, l.274) ∴ o `<edit>` de `Header` foi de 112/215 p/ **132/195** junto, & as 3 linhas da coluna voltaram a alinhar. É o modelo que §I102f já enuncia — o vão do `?` é da FAMÍLIA & a linha SEM picker o deixa VAZIO, ≡ `MeritFree` deixa o slot que `MeritPicked` preenche. ⚠ **o label ⊥ encolhe, & isto está FECHADO**: `WoD20.1.lfm:92` registra, desde a rodada que o escreveu, que "The 105px of `Header` is what `Demeanor` needs in pt". ⚠ **§V16 ⊥ é ressalva aqui: a 1ª escrita desta §T abriu dúvida que a MEDIDA fecha, & a ressalva foi RETIRADA** — pela régua do próprio gate (`$PX_PER_CHAR = 6.5`, `verify-hunters-hunted.ps1:685`) o placeholder `Selecionar Comportamento` = 24 chars = **156px** & o MAIOR arquétipo em PT = 16 chars (`Olho do Furacão`, `Show de Horrores`, de `PICKER_LIST["nature"]` em `WoD20th.lfm:106-116`, que É a tabela de `demeanor` por `WoD20th.lfm:549`) = **104px**, contra **195** de botão ∴ o PLACEHOLDER sozinho cabe c/ **39px** de folga & a leitura "o VALOR que o widget mostra, ⊥ o placeholder" NUNCA precisa ser feita. O 215 de antes tinha **59** de folga ∴ o corte de 20 ⊥ chegou perto de §V16 em momento nenhum, & ⊥ nasce §Q daqui|I102,V365,T845,V16,V26,B104,B105,I113e,I117,I118,I103,V334
T847|x|`verify-hunters-hunted.ps1` — **§V365** c/ as 7 pernas & o zero-guard. ⚠ **DESBLOQUEADA — §T846 fechou em `x` na 148ª.** A perna (a) lê o CÓDIGO dos 2 lados ≡ §V354i & mede a família **JÁ CONVERTIDA**: ∀ template que chama `mfOpen(` ! ter `?`, & ∀ `?` ! pertencer a template que chama `mfOpen(` (§B106) — `comboBox` esperando onda de §I113e ⊥ entra na conta. ⚠ **a lista de mutação abaixo foi REESCRITA na 149ª p/ bater c/ §V365 como ela está HOJE**: a de antes media a régua anterior a §B104/§B105/§B106, & quem construísse por ela mediria a régua velha. Mutação RODADA antes de aceitar (§V20 §V222): tirar o `?` de 1 família JÁ CONVERTIDA ! VERMELHO por (a) · pôr `?` em `SpecialityRow` ! VERMELHO por (a), porque a exceção nomeada corta nos 2 sentidos · `?` em linha DIGITADA ! VERMELHO por (b) · devolver a pane de família JÁ CONVERTIDA ! VERMELHO por (d), ⊥ as 7 de uma vez que é o que §B105 tirou · `readOnly="true"` fixo no `edtPopDesc` ! VERMELHO por (f) · **escrever `opacity` no `edtPopDesc`, XML ou Lua, ! VERMELHO por (f)** — metade NOVA, §B104 · 2ª escrita de descrição fora de `descFieldOf` ! VERMELHO por (f) · campo `descFieldOf` sumindo junto c/ a pane ! VERMELHO por (e) · sonda: mover o `?` de x dentro da linha ! VERDE · & **§V334 REESCRITA** junto: a contagem passa a sair do CÓDIGO (nº de `<textEditor name="edt*Desc">` = entradas em `DESC_PANES` = pares `btnFont*` ÷ 2) em vez de número escrito, que é o que a fez envelhecer 3× (8 → 9 → 2). Mutação: pane fora de `DESC_PANES` ! VERMELHO · par de botão sem pane ! VERMELHO|V365,V334,I103,T846,V20,V222,B104,B105,B106,I113e
T853|x|`verify-hunters-hunted.ps1` — **§V366** c/ as 4 pernas & o zero-guard. Lê os **3** arquivos (`descNature_en.lua`, `_pt.lua`, `PICKER_LIST["nature"]` em `WoD20th.lfm`) & mede a PARTIÇÃO, ⊥ um total: **46** ⊎ **22** NOMEADOS = **68**, disjunta & completa. ⚠ a lista dos 22 é ESCRITA no gate por NOME (§V366c) — `≥ 46` ou `skip` genérico ⊥ passa no teste de mutação & §V20 ∃ por causa disso (§B7). ⚠ **⊥ FECHAR sem a mutação**: trocar o alvo p/ **68** ! acender VERMELHO, & é a única prova de que a régua nasceu c/ a decisão do dono dentro (§B105, §V222)|V366,V20,V222,T838,B105,B7
T854|x|`WoD20th.lfm` + `WoD20.2.lfm` + `verify-hunters-hunted.ps1` — **os 3 overlays saem da aba p/ `sheetBody`** (§B107, §V367, §I102a, §I107c, §I112a). MEDIDO na tela pelo user 2026-08-30: o `?` & o picker da Main abriam "na aba errada". ENTREGUE: `popScrim` `popDesc` `mfSearch` cortados do `<scrollBox>` da Traits & autorados como filhos DIRETOS de `sheetBody`, DEPOIS de ∀ aba; `top` +40 nos 2 layouts (110→150, 40→80) & o scrim 693→**733** p/ cobrir a faixa de abas junto. **0 linha de Lua** — `popOpen`/`mfOpen` já andam de `tabRootOf(from)` (§B9). Gate: **§V367** nova c/ 4 pernas & mutação 3/3 VERMELHA; **6** checks repontados p/ a raiz (§V333 §V339 §V352 §V360 liam `WoD20.2.lfm` por NOME) & **2** consertados por defeito PRÓPRIO — §V40 perdeu a exceção de overlay, que morreu junto c/ o pai (as caixas ⊥ são mais filhas de `scrollBox` ∴ "esperava 2" virava régua de nada), & §V166 parou de contar comentário (§B108). §V339 passou a ler **2** arquivos de propósito: `MeritPicked` segue template da Traits, a caixa ⊥. Gate **620 ok / 0 FAIL**. ⚠ TELA [USER]: abrir o `?` & o picker em CADA aba que os tem (Main & Traits) & ver a caixa aparecer NA aba em que se está|B107,V367,I102a,I107c,I112a,V333,V339,V352,V360,V40,V166,B108
T855|x|`WoD20th.lfm` + `verify-hunters-hunted.ps1` — **`descText` desembrulha o idioma da entrada** (§B109, §V368, §I21, §V360c). MEDIDO na tela pelo user 2026-08-30, logo depois de §T854: o `?` de nature/demeanor abre na aba certa & mostra **NADA** — nem texto, nem a frase. Causa: `txt = DESC[key] or ""` devolve a TABELA `{<idioma> = texto}`, que ⊥ é nil & ⊥ é `""` ∴ atravessa o fallback do `popOpen` & vai crua p/ `edtPopDesc.text`. ENTREGUE: `descText` passa a buscar a ENTRADA (composta por `sub` 1º, nua depois) & devolver `ent[lang] or ent.en or ""` — o MESMO `pick(tbl, lang)` que as 4 panes usam desde sempre, ⊥ um 2º jeito (§V135). **1** conserto cobre as 2 superfícies: a caixa do `?` (`popOpen`) & a pane da caixa de busca (`mfSelect`). Gate: **§V368** nova c/ 4 pernas, mutação **5/5 VERMELHA** (o bug literal · só o `[lang]` fora · entrada de módulo sem chave de idioma · pane sem desembrulho · `descText` sumida) & sonda VERDE (renomear a local). MEDIDO pela régua: **2800** entradas em **14** módulos, **5** arquivos indexam `DESC[`. Gate **621 ok / 0 FAIL**. ⚠ TELA [USER]: o `?` de Nature/Demeanor mostra o texto do livro p/ os 46 & a frase `No description available` p/ os 22 — & é a 1ª vez que a 2ª metade disso é testável|B109,V368,I21,I102e,V360,V364,T854,T838,T845,T846
T856|x|`WoD20th.lfm` — **`-- Remove --` sai da paginação** (§I121, §V369, pedido 1). Hoje o `""` de §V15 é empurrado p/ `MF.pool` quando o slot `has` valor ∴ ordena, pagina & SOME da página 2 — & é a única saída da linha. Vira controle PRÓPRIO, irmão dos `dynMfRow_*`: fora de `MF.pool`/`MF.hits`, fora de `total`/`pages` (a página segue com **20** nomes, §I107k), visível ⟺ o slot tem valor, ACIMA da 1ª linha c/ vão > 20px. Rótulo segue de `mfShown("")` ∴ **0** string nova. ⚠ `mfSelect` indexa `MF.hits[(MF.page-1)*MF.rows + idx]` ∴ o controle novo ⊥ pode chamar `mfSelect` c/ um `idx` de linha — ele precisa do caminho dele p/ marcar `MF.picked = ""` sem passar pelo índice de página|I121,V369,I107,V15,T857
T857|x|`WoD20th.lfm` — **o picker abre PRÉ-MARCADO & `-- Remove --` empresta a descrição do slot** (§I122, §V369, §V368, pedido 2). `mfOpen` passa a marcar o valor corrente do campo: a pane de descrição nasce com o texto DELE em vez de vazia, & a linha aparece marcada se cair na página aberta. `mfSelect` na linha de remoção mostra a descrição do valor que está NO SLOT — `-- Remove --` ⊥ tem texto próprio & ⊥ deve cair na frase de §V360c, que diria "o livro ⊥ traz" sobre uma linha que ⊥ é um item. Leitura por `descText` (§V368), ⊥ um 2º leitor. ⚠ marcado ⊥ é escrito: abrir & fechar sem confirmar ⊥ toca o campo (§V333e). DEPOIS de §T856 — a linha precisa existir como controle antes de ganhar comportamento|I122,V369,V368,V360,T856
T858|x|`WoD20th.lfm` + `WoD20.1.lfm` + `WoD20.6.lfm` + `localization.lang` — **7 nomes novos de habilidade** (§I123, §V371, §V70, §V74, pedidos 4-6). `Arts` em `BASE_TALENTS` (serve 3 épocas) & na lista própria de Dark Ages · `Martial Arts` `Meditation` `Research` nas 4 listas de skills · `Cosmology` `Enigmas` `Esoterica` nas de knowledges, **pulando `Enigmas` em Victorian Era & Dark Ages, que já o têm**. Ordem alfabética EN. **6** entradas novas em `ABILITY_FIELD` (`arts` `martialArts` `meditation` `research` `cosmology` `esoterica`) ∴ §V74 vai de 41 p/ **47** & `PICKER_LIST["speciality"]` cresce junto. ∀ nome ! ter `wod.<nome>` [pt]+[en] & entrada no mapa `PT` de `WoD20.6` (§V70). ⚠ **DESBLOQUEADA — Q34 RESPONDIDA 2026-08-30: época ⊥ empresta habilidade de época nenhuma**, ∴ Victorian Era & Dark Ages FICAM c/ 12 conhecimentos & a diferença é paga em linha DIGITÁVEL. **3** campos digitáveis novos c/ 5 pontos cada: `customTalent_3` `customTalent_4` `customKnowledge_3`. O par gêmeo de §I123b (slot 13 nomeado XOR `customKnowledge_3` no mesmo `top`) & o `else` do render de §I123c saem NESTA §T — sem eles o slot 13 fica c/ o rótulo & o campo da época anterior|I123,V371,V70,V74,I7,T859
T859|x|`WoD20.1.lfm` — **a régua de 25 & as caixas que encolhem** (§I124, §V370, pedidos de layout 1-4 + 3). Grid vai a **15** linhas por coluna (13 nomeadas + 2 digitáveis, §I123a) ∴ as caixas de habilidade crescem & tudo abaixo desce; as economias pagam parte: cabeçalho `20 50 80`→`20 45 70` (**−10**, & sobe tudo) · VIRTUES `45 74 103`→`45 70 95` (**−8**) · SPECIALTIES `41 71 …311`→`41 66 …266` (**−45**) · `WILLPOWER` de `114`→**99** & a trilha `139`→`124` (**−15**) · rótulo `ROAD` ganha `horzTextAlign="trailing"` (pedido 3). ⚠ **§V69 ⊥ afrouxa & as 2 colunas ⊥ poupam igual** — esquerda 23, direita 45: o y comum de VIRTUES+ROAD, SPECIALTIES & avatar sai RECALCULADO das alturas novas, & quem sobrar curto ganha FOLGA na caixa, nunca passo maior (§V370b). DEPOIS de §T858 — a altura do grid depende de quantas linhas ele tem|I124,V370,V69,I123,T858
T860|x|`WoD20.1.lfm` + `verify-hunters-hunted.ps1` — **o `mfOpen` do `HeaderPicker` nomeia `'Nature'`** (§B110, §V373, §I102e). MEDIDO na tela pelo user 2026-08-30: com antecedente, mérito & defeito o picker mostrava a descrição; com nature/demeanor, **nada que se clicasse mostrava texto**. Causa: das 2 portas da linha só o `?` nomeava o módulo. **1 argumento** — `mfOpen(self, '$(field)', '$(field)', 'Nature')` — & a pane do picker passa a ler o mesmo `descNature` que o `?` já lia. **§V373** NOVA, mutação **4/4 VERMELHA** (o bug literal · as 2 portas discordando · módulo inexistente em disco · a mesma discordância no antecedente) + sonda VERDE. ⚠ o roster de ondas do gate DECLARAVA `HeaderPicker` sem módulo — verdade enquanto §T838 extraía, falsa desde que `descNature_en/_pt` nasceram — & passa a declarar `Nature` (§V354b, as 2 saídas seguem legais; `SpecialityRow` segue sem módulo p/ sempre, §Q33). Gate **624 ok / 0 FAIL**|B110,V373,I102e,I113a,V354,V364,T838,T857
T861|x|`WoD20.1.lfm` + `verify-hunters-hunted.ps1` — **`VIRTUES` abre na hairline & a aba fecha 4 acima** (§I125c, §V374, §V69, §V370, §V171, pedido user 2026-08-31, item 3). Linhas `45 70 95` → **`41 66 91`** & caixa `330×140` → **`×136`** ∴ fundo `701`→**697** · `ROAD` SOBE de `top=706` p/ **702**, altura `189` INTACTA (ela já está no piso: 20/20 c/ ar interno de 5+5+5 que §I124d apertou a pedido do user) ∴ fundo `895`→**891** · `SPECIALTIES` `330×334`→**`×330`** (o `top=561` ⊥ muda) & o avatar — as **3** peças de §V309, `avatarFrame` + `<image field="avatar">` + `ornAvatar` — `340×464`→**`×460`**, as 2 fechando em **891** c/ §V69 · `HEALTH` ⊥ SE MEXE (§I125e) ∴ `$HEALTH_TEN_ROW_OVERHANG` de `-7` p/ **-3** & o mapa da aba de `y=895` p/ **`y=891`**. ⚠ **4px é TUDO o que os 3 pedidos deixam na mesa**: o cabeçalho & as 3 colunas de habilidade estão no PISO medido (§I125a, §I125b — margem 20 nos 4 lados, linhas que se tocam, rodapé igual ao de `ATTRIBUTES`) & o vão entre caixas é **5** EXATO (§V298) ∴ os itens 1 & 2 do pedido ⊥ viram código, viram §I125 & **Q38**. ⚠ `SPECIALTIES` segue c/ **39** de rodapé & isso ⊥ é bug (§I125d): ela ! fechar na linha, a linha é da coluna do meio, & **11ª linha ⊥ cabe** — pede 336, tem 330. §V374 NOVA no gate + mutação (devolver `VIRTUES` p/ 45 ! VERMELHO; mexer em rodapé ! VERDE)|I125,V374,V69,V370,V171,V298,V280,T859
T862|x|`WoD20.1.lfm` — **a 5ª faixa & as 4 caixas que trocam de lugar** (§I126, §V375, §V376, pedidos 2, 3, 5). `SPECIALTIES` sai da fileira de baixo p/ `1350..1680` y`120..556` (`330×436`) & vira a 4ª caixa do grid · `ROAD` sai de baixo de `VIRTUES` p/ `680..1010` y`561..888` · `VIRTUES` cresce p/ `330×327` & fica ao lado dele · `EXPERIENCE` vai a `1015..1680` (`665×115`) · avatar `340×460`→`340×457` · borda direita da aba `1345`→`1680` & fundo `891`→`888` · `popScrim.width` `1595`→`1680` no MESMO commit (§C). ⚠ o mapa no comentário do topo do arquivo é PARTE da tarefa: ele é o que a rodada seguinte lê antes de medir, & mapa velho custou 2 rodadas em §I125f/g|I126,V375,V376
T863|x|`WoD20.1.lfm` + `WoD20th.lfm` — **`EXPERIENCE` da Main recebe a forma da caixa da Progress** (§I127, pedido 1). Geometria de `WoD20.9.lfm` VERBATIM deslocada `+32` em x, título a `625` · **4** nomes de controle novos (`edtTotalXPMain` `edtSpentXPMain` `bgTotalXPMain` `bgSpentXPMain`) & `edtCurrentXPMain` só muda de geometria · `XP_BOXES` de **4** p/ **6** & `xpPaint` c/ 2 escritas a mais, `edtSpentXPMain` c/ a dança de trava de §I44/§V245 na MESMA linha (§V162) · **0** campo NDB & **0** chave `.lang` novos. BLOQUEADA por §T862 — a forma só cabe nos 665|I127,V245,V256
T864|x|`WoD20.1.lfm` + `WoD20th.lfm` — **`WILLPOWER` volta a respirar & o par `ROAD` centra por idioma** (§I128, §V380, pedidos 4 & 6). `WILLPOWER` `99`→`104` & a trilha `124`→`129` (vão 5→10) · `ROAD_LABEL_W = {en = 26, pt = 46}` c/ os 2 `left` DERIVADOS dele, escritos no passo de `language` que já roda · o rótulo GUARDA `text=` & a chave `.lang` — só geometria é Lua (§I128f). ⚠ os 2 números são ESTIMATIVA de §V312 & **só a TELA fecha** (§I128e): entra na sessão de teste de tela junto do resto da rodada|I128,V380,V312
T865|x|`verify-hunters-hunted.ps1` — **§V375 & §V376** c/ as 5+1 pernas & os 2 zero-guards. §V375 SUPERSEDE §V168 nos números & §V376 SUPERSEDE §V69 ∴ as 2 velhas SAEM do gate no mesmo commit — régua velha & nova juntas dão vermelho garantido & ensinam a próxima rodada a ignorar as 2. ⚠ `$HEALTH_TEN_ROW_OVERHANG` vai a **0** & FICA no arquivo (§V376). Mutação obrigatória antes de aceitar: `SPECIALTIES` de volta p/ `680..1010` ! VERMELHO · `VIRTUES` em `330×136` ! VERMELHO · `popScrim` em 1595 ! VERMELHO · caixa fora de borda de faixa ! VERMELHO|V375,V376,V20
T866|x|`verify-hunters-hunted.ps1` — **§V380** c/ as 5 pernas. A perna (e) lê o TIPO do controle do fonte em vez de assumir `comboBox`, que é o defeito de §B94 escrito de propósito p/ sobreviver à onda 5 (§T876). Mutação: rótulo de volta p/ `left=20 width=80` ! VERMELHO · `ROAD_LABEL_W` c/ 1 idioma só ! VERMELHO · `pt` c/ o valor de `en` ! VERMELHO. BLOQUEADA por §T864|V380,I128
T867|x|`WoD20th.lfm` + `verify-hunters-hunted.ps1` — **`descFieldOf` alcança os 28 slots que ela perde, & §V377 no gate** (§B111, §I129g). Padrão `^(%a+)_(%d+)$` → `^(%a+)_(%w+)$` ∴ os **22** de qualidade/defeito (`merit_m7` `flaw_f10`) passam a resolver; os **6** avulsos sem `_` (`nature` `demeanor` `road` `hedgeAffiliation` `hedgeAttr` `clanFamily`) ganham o sufixo colado (`<campo>Desc`). ⚠ é conserto de buraco MUDO — `savePopDesc` volta em `nil` sem erro nenhum ∴ a prova é o gate, ⊥ a tela. PRIMEIRA da fila do bloco de `-- Custom --`: §T868 deriva 2 campos pelo mesmo caminho & herdaria o buraco|V377,I129,B111
T868|x|`WoD20th.lfm` + `localization.lang` — **`-- Custom --`** (§I129, §V379, §V381, pedidos 12 & 13). `btnMfCustom` fixo & fora da paginação ≡ §I121 · `lblMfCustomName` + `edtMfCustomName` na pane & `edtMfDesc` `524`→`470` quando marcada · `mfSearch` `620`→`645` & a variante CURTA de §I115 junto · `customFieldOf`/`customDescFieldOf` derivados & LUA-OWNED em §I3 · `descFor` c/ a ordem de 4 níveis & os 2 leitores existentes migrados p/ ela · **3** chaves `.lang` novas nos 2 idiomas. BLOQUEADA por §T867|I129,V379,V381
T869|x|`verify-hunters-hunted.ps1` — **§V379 & §V381** c/ as 4 pernas cada. ⚠ a perna §V381(b) é a que dá trabalho & a que importa: ela procura leitura de `descText`/`descFieldOf`/`customDescFieldOf` FORA de `descFor` & tem de achar **0** — hoje ∃ 2 leitores c/ a ordem copiada (`popOpen` & `mfSelect`) & é essa cópia que §T868 apaga. Mutação: 1 dos 2 leitores de volta à ordem própria ! VERMELHO · `-- Custom --` dentro de `MF.pool` ! VERMELHO (a página cai p/ 19) · chave `.lang` faltando em [pt] ! VERMELHO|V379,V381,V20
T870|x|`WoD20.1.lfm` + `WoD20.2.lfm` — **as 3 famílias que JÁ são picker perdem a linha LIVRE** (§I130a, §V378, pedidos 7, 8, 9, 10). **16** instâncias trocam de template & **0** campo renomeia: `MeritFree`×8 → `MeritPicked` · `OpenAbilityFreeRow`×5 → `OpenAbility` · `SpecialityFreeRow`×3 → `SpecialityRow`; os **3** `<template>` SAEM do XML no mesmo commit (§I130e). ⚠ **§I102f MORRE** — os 2 sujeitos dela deixam de ∃ (§I130f) · ⚠ `book_` `type_` `costy_` das 8 linhas de qualidade passam a `readOnly` (§I130c, **§Q39**) · ⚠ o nome de controle das 3 especialidades muda (§I130d). BLOQUEADA por §T868 — sem `-- Custom --` isto TIRA capacidade|I130,V378,V2
T871|x|`verify-hunters-hunted.ps1` — **§V378** c/ as 4 pernas, & a perna (a) nasce c/ **3** nomes na lista (`MeritFree` `OpenAbilityFreeRow` `SpecialityFreeRow`), ⊥ com os 9: perna que cobra hoje o que só §T873/§T875 entregam nasce VERMELHA contra código certo, que é §B106 ao pé da letra. Os outros 6 entram POR NOME em §T873 & §T875. A perna (c) escreve a exceção por NOME (`CustomAbility` `HeaderNarrow` `specialityName_*`) — "salvo texto livre" ⊥ passa no teste de mutação. BLOQUEADA por §T870|V378,I130
T872|x|`WoD20.12.lfm` + `WoD20.13.lfm` + `WoD20.14.lfm` + `WoD20th.lfm` — **onda 3 de §I113e: as FILTRADAS por nível viram picker** (§I131a, §I113c, §I113e, pedido 11). `DiscRow` · `MainPathRow` · `SecPathRow` · `RitualRow` — **48** linhas picker. ⚠ **CORRIGIDO 2026-08-31 (§I113c)**: o filtro ⊥ é "o programa" — são **8** linhas no laço que monta `MF.pool` em `mfOpen` (`discLevels()` & `hedgePathLevels()` lidos **1×** por ABERTURA, ⊥ por nome, + a chamada a `pickAllowed`), & a MESMA função dos 2 lados mantém §V199. O que faz esta onda ser CARA é §V365(d) — a pane de descrição por família, cujo custo de GATE está em §I118e. ∀ família entrega **3** coisas juntas: caixa de busca + `?` + `-- Custom --` (§I131g). BLOQUEADA por **§T811** & §T868 (`x`) & §T877 (`x`) — §T811 proíbe onda antes de §T810 passar, & §T877 é quem impede o `-- Custom --` desta onda de ser revertido por `guardPick` (§I132) — ⚠ **& por §T811**, que proíbe onda antes de §T810 passar: §T810 é `[USER]`, de TELA, & está `.` ∴ os 2 bloqueios citados estarem `x` ⊥ torna esta linha construível. ⚠ MEDIDO 2026-08-31: §V365(d) reprova enquanto `WoD20.12`/`WoD20.13`/`WoD20.14` tiverem pane & elas têm **1** cada ∴ esta onda ! sair na MESMA rodada em que §T832 tira as panes, ou depois dela|I131,I113,V354,T877,T811,T810,V365
T873|x|`WoD20.12.lfm` + `WoD20.13.lfm` + `WoD20.14.lfm` + `verify-hunters-hunted.ps1` — **as 11 linhas livres da onda 3 saem** (§I130b, §V378): `DiscFreeRow`×3 → `DiscRow` · `SecPathFreeRow`×3 → `SecPathRow` · `RitualFreeRow`×5 → `RitualRow`, os 3 `<template>` apagados & os **3** nomes entram na perna (a) de §V378 no mesmo commit. BLOQUEADA por §T872|I130,V378
T877|x|`WoD20th.lfm` + `verify-hunters-hunted.ps1` — **o `-- Custom --` herda a isenção de filtro da linha DIGITADA** (§I132, §V382, user 2026-08-31). `pickRefusal` ganha `local cf = customFieldOf(field);` + `if sheet ~= nil and cf ~= nil and sheet[cf] == value then return nil; end;` — **por VALOR, ⊥ por linha** (§I132b) — & §V382 entra c/ as **5** pernas no MESMO commit. ⚠ **BLOQUEIA §T872**: a onda 3 é a 1ª que dá `-- Custom --` a família FILTRADA ∴ sem isto o jogador digita o nome, confirma & `guardPick` REVERTE (§I132a, §I132d). ⚠ `isTypedRow` FICA até §T875 — as 2 isenções convivem (§V382d). BLOQUEADA por §T868 (`x`)|I132,V382,V199,I129
T878|x|`WoD20th.lfm` + `verify-hunters-hunted.ps1` — **as 8 linhas que §T870 converteu ganham o pintor que nunca tiveram** (§B112, §V383, §I107a2, user 2026-08-31). `renderMeritButtons` roda `for i = 0, 6` (**14** linhas) & as instâncias de `MeritPicked` são **22** ∴ `dynMerit_m7`…`_m10` & `dynMerit_f7`…`_f10` mostram o `lbl` AUTORADO em toda abertura: sem os `--`, sem itálico, sem `opacity 0.60`, & a linha CHEIA lendo `Select Merit`. ⚠ o limite passa a ser DERIVADO ≡ `renderBgButtons` & `renderSpecialities` já fazem (§V383b) — **⊥ trocar `6` por `10`**, que é o MESMO literal 1 rodada adiante & a próxima conversão o quebra igual. **§V383** entra c/ as 5 pernas & MUTAÇÃO rodada no mesmo commit (§V20, §V222)|V383,I107,B112,T870
T879|x|`WoD20.13.lfm` + `WoD20th.lfm` + `verify-hunters-hunted.ps1` — **a aba Ghoul perde 1 MAIN PATH** (pedido user 2026-09-01). `MAIN_PATH_ROWS` **5 → 4** · sai `<MainPathRow field="mainPath_5"/>` · caixa `MAIN PATHS` `height` **186 → 161** · `SECONDARY PATHS` `top` **211 → 186** (o vão de 5 de §V298 anda junto) ∴ a col.2 fecha em **637** & passa a bater EXATO c/ a col.1, que é o que o pedido comprou (§I118f). `mainPath_5` vira **ÓRFÃO de §I3** — o dado FICA no NDB & o nome é QUEIMADO (§V2) — & **§V362(b) REPROVA até que ele entre na lista**, que é a régua nova de §T834 cobrando no 2º disparo dela. §V354(e) `mainPath` **5 → 4** · §V333 roster `MainPathRow` **5 → 4** & total **104 → 103** · §V204/§V248 seguem DERIVADOS & ⊥ mudam. ⚠ **CUSTO DECLARADO, & o user decidiu de olho nele**: §V181 dá ≤1 main path por feitiçaria de sangue & `BLOOD_SORCERY` tem **6** (Thaumaturgy · Necromancy · Koldunic · Assamite · Setite · Dark Thaumaturgy) ∴ a ficha JÁ era 1 slot curta & passa a ser **2** — personagem c/ 5 ou 6 feitiçarias ⊥ consegue registrar a trilha principal de todas. ⊥ ∃ recusa CALADA (§V129 fala), mas ⊥ ∃ slot|I118,V181,V362,V354,V333,I3,V2
T880|x|`WoD20.14.lfm` + `WoD20th.lfm` + `verify-hunters-hunted.ps1` — **os rituais crescem até a col.3 ENCAIXAR: 20 → 22 linhas, pé 26** (user 2026-09-01, DECIDIDA — §I118f). A caixa `RITUALS` `height` **561 → 617** (`41 + 25·22 + 26`) & fecha em **637**, batendo exato c/ as col.1 & col.2 (esta c/ §T879 aplicada). ⚠ **os 2 slots novos são `ritual_26` & `ritual_27`, ⊥ `_21`/`_22`**: `ritual_21`…`_25` são QUEIMADOS (§V251, §I46, §T560) & ficha salva antes da 85ª ainda carrega valor neles ∴ reusar o id dá ao campo velho o dado do novo, CALADO — é a regra de §V362(c) ao pé da letra. ∴ **`RITUAL_ROWS` 20 → 27**, o MAIOR ÍNDICE & ⊥ a contagem, ≡ §V263(a) já faz p/ `psychic` (§Q32): desenhadas = constante − **5** vagos, & os 5 vagos JÁ estão nomeados em §I3. ⚠ **§V204 ! aprender o buraco ANTES**, nas 2 pernas — a contagem (`declarado ≠ desenhado` acende em 27 vs 22) & o índice `1..N` de §B116 (o conjunto vira `{1..20, 26, 27}`) — senão a régua nova reprova código CERTO, que é §B106. Andam junto: §V251 (20 → **22** desenhados, & os 5 nomes seguem PROIBIDOS) · §V333 roster `RitualRow` 20 → 22 & total 103 → **105** · §V354(e) raiz `ritual` 20 → 22 · §V252(a) segue DERIVADA. BLOQUEADA por §T879 (é ela que põe a col.2 em 637)|I118,V204,V251,V252,V263,V333,V354,V362,I3
T881|x|`verify-hunters-hunted.ps1` — **a régua do PÉ de caixa de seção**, que §I118f mostrou ⊥ ter dono: hoje ∃ **20** em 3 caixas & **35** em 2, & §V280(a) só cobra ≥20 ∴ os 2 passam & ninguém sabe qual é o certo. Enquanto ⊥ ∃ régua, ∀ rodada de layout escolhe um pé por conta & a deriva de §I73 volta pela porta de baixo. BLOQUEADA por §T880, & o que ela decidiu já está na mesa: o pé dos rituais é **26** ∴ a ficha passa a ter **3** valores (20 · 26 · 35) & esta §T ⊥ pode mais dizer "normalize" sem escolher qual dos 3 — o user ADIOU essa escolha de propósito em 2026-09-01, p/ ⊥ refazer a conta das colunas no meio da rodada de layout. Quando entrar: a régua ! ser RELAÇÃO ou lista NOMEADA dos pés legais, ⊥ um literal solto — pé é o que faz coluna fechar, & literal aqui é a deriva de §I73 voltando pela porta de baixo|I118,V280,I73,T880
T882|x|`verify-hunters-hunted.ps1` — **`$I3_ORPHANS` está 1 nome CURTO & a lista vira 27** (§I3 emendada 2026-09-01). Entra **`xpLog`**, que saiu na 27ª rodada quando o bloco de texto livre virou ledger derivado (§I10, §V91): MEDIDO, `WoD20.9` só o nomeia em 1 comentário & nos CONTROLES `xpLogBox`/`xpLogScroll` ∴ `field=` ⊥ ∃ & o laço de §I3 passa VERDE — ele ⊥ possui widget & ninguém o observa. ⚠ **o que esta §T ⊥ é**: ela ⊥ mexe em §V362, & a leitura de que `xpLog` "nunca foi campo ∴ ⊥ pode entrar" é FALSA — a perna (b) só consulta a lista como teste de PERTINÊNCIA p/ nome DO `$FIELD_CONTRACT` ∴ nome de fora nunca é visitado & ⊥ ∃ risco de acender por nada. O ganho é o laço de §I3 passar a cobrar dele o que já cobra dos outros 26. ⚠ **a medida que ! ir JUNTO no comentário do gate, senão a rodada seguinte a redescobre**: dos 27, só **4** (`discSel` `pathSel` `ritualSel` `mainPath_5`) ∈ `$FIELD_CONTRACT` ∴ §V362(b) cobra 4 por nome & os outros **23** ⊥ têm guarda além do laço de §I3 — o contrato foi congelado em `b5111c35` & ⊥ enxerga nada que saiu antes|I3,V362,V2,V209
T883|x|`verify-hunters-hunted.ps1` — **§V383(b) promete "∀ laço que pinta" & lê só o form RAIZ** (§V383 emendada 2026-09-01). MEDIDO: (a) varre os templates de picker em TODOS os `.lfm` mas (b) resolve limite só em `WoD20th.lfm` ∴ **3** pintores ficam sem régua — `renderRitualButtons` (`WoD20.14`) · `renderPathButtons` (`WoD20.13`) · o de disciplina (`WoD20.12`). ⊥ ∃ dano hoje (os 3 derivam de constante) & foi por 1 fio: a receita de §T880 mandava o limite de `renderRitualButtons` virar 22, o que deixaria `ritual_26`/`_27` sem pintor c/ o gate VERDE (§B117, & §B112 é a razão de (b) ∃ir). Ao entrar: `$K383` ! aprender `RITUAL_ROWS` `MAIN_PATH_ROWS` `SEC_PATH_ROWS` `DISC_ROWS` `CLAN_DISC_ROWS` & o varredor de laços ! abrir o arquivo de CADA família, ⊥ só a raiz. ⚠ **& (a) ! aprender o BURACO junto**: `ritual` autora `{1..20, 26, 27}` ∴ conferir a faixa pintada contra `1..maior índice` reprovaria código CERTO (§B106) — o alvo é `{índices autorados} ⊆ {faixa pintada}`, que já é a forma de (a), c/ os vagos de §V204(ii) FORA dos 2 lados. Mutação: limite de `renderRitualButtons` p/ 22 ! VERMELHO|V383,V204,B112,B117,I107
