<?xml version="1.0" encoding="UTF-8"?>
<form name="frmWoD20_6" align="client" theme="dark">
	<script>
		<![CDATA[
			-- English -> pt-BR. Generated from localization.lang, kept in sync by the build gate.
			-- The sheet cannot use Locale.tryLang here: that native call resolves against the
			-- Firecast APP language, not this dropdown, so a pt-locale user picking "English"
			-- would still get Portuguese. SPEC R.1 / R.5.
			local PT = {
				["Remove"] = "Remover",
				["Description"] = "Descrição",
				["Ritual Level"] = "Nível do Ritual",
				["No description available"] = "Nenhuma descrição disponível",
				["Select Background"] = "Selecionar Antecedente",
				["Select Nature"] = "Selecionar Natureza",
				["Select Demeanor"] = "Selecionar Comportamento",
				["Select Speciality"] = "Selecionar Especialidade",
				["Select Discipline"] = "Selecionar Disciplina",
				["Select Main Path"] = "Selecionar Trilha Principal",
				["Select Secondary Path"] = "Selecionar Trilha Secundária",
				["Select Ritual"] = "Selecionar Ritual",
				["Select Affiliation"] = "Selecionar Afiliação",
				["Select Hedge Path"] = "Selecionar Caminho de Magia Estática",
				["Select Road"] = "Selecionar Estrada",
				["Select Clan"] = "Selecionar Clã",
				["Select Family"] = "Selecionar Família",
				["Select Phenomenon"] = "Selecionar Fenômeno",
				["Select Hedge Ritual"] = "Selecionar Ritual de Magia Estática",
				["Select Flaw"] = "Selecionar Defeito",
				["Select Merit"] = "Selecionar Qualidade",
				["OK"] = "OK",
				["* The first path is the Affinity Path"] = "* O primeiro caminho é o de Afinidade",
				["Ability"] = "Habilidade",
				["Academics"] = "Acadêmicos",
				["Activist"] = "Ativista",
				["Age"] = "Idade",
				["Alchemy"] = "Alquimia",
				["Alertness"] = "Prontidão",
				["ALLIES"] = "ALIADOS",
				["Allies"] = "Aliados",
				["Alternate Identity"] = "Identidade Alternativa",
				["Ancestors"] = "Ancestrais",
				["Animal Ken"] = "Empatia com Animais",
				["Animal Psychics"] = "Psiquismo Animal",
				["Animal-Ken"] = "Emp.Animais",
				["Anti-Psychic"] = "Antipsíquico",
				["Apparent Age"] = "Idade Aparente",
				["Appearance"] = "Aparência",
				["Appearence"] = "Aparência",
				["Arcane"] = "Arcano",
				["Arcane (1)"] = "Arcano (1)",
				["Arcane (2)"] = "Arcano (2)",
				["Arcane (3)"] = "Arcano (3)",
				["Arcane (4)"] = "Arcano (4)",
				["Arcane (5)"] = "Arcano (5)",
				["Archery"] = "Arquearia",
				["Architect"] = "Arquiteto",
				["ARMOR"] = "ARMADURA",
				["Armor"] = "Armadura",
				["Armory"] = "Arsenal",
				["Arts"] = "Artes",
				["Astral Projection"] = "Projeção Astral",
				["Athetics"] = "Esportes",
				["Athletics"] = "Esportes",
				["Attribute"] = "Atributo",
				["ATTRIBUTES"] = "ATRIBUTOS",
				["Auspice"] = "Augúrio",
				["Autocrat"] = "Autocrata",
				["Avaiable Version"] = "Versão Disponivel",
				["Available Version:"] = "Versão Disponível:",
				["AVAILABLE VERSION:"] = "VERSÃO DISPONÍVEL:",
				["Avatar"] = "Avatar",
				["Awareness"] = "Sensitividade",
				["Background"] = "História",
				["Background Cost"] = "Custo por Antecedente",
				["BACKGROUNDS"] = "ANTECEDENTES",
				["Backgrounds"] = "Antecedentes",
				["Backup"] = "Reforços",
				["BASE OF OPERATIONS"] = "BASE DE OPERAÇÕES",
				["Base of Operations"] = "Base de Operações",
				["BASE OF OPERATIONS (ARMAMENTS)"] = "BASE DE OPERAÇÕES (ARMAMENTOS)",
				["BASE OF OPERATIONS (DEFENSES)"] = "BASE DE OPERAÇÕES (DEFESAS)",
				["BASE OF OPERATIONS (OTHER)"] = "BASE DE OPERAÇÕES (OUTROS)",
				["Based on the sheet plugin by Vinny (Ambesek)"] = "Baseado no plugin de fichas de Vinny (Ambesek)",
				["Battle Scars"] = "Cicatrizes de Batalhas",
				["Benefactor"] = "Benfeitor",
				["Biocontrol"] = "Biocontrole",
				["Blood Sorcery: Paths"] = "Feitiçaria de Sangue: Trilhas",
				["Blood Sorcery: Rituals"] = "Feitiçaria de Sangue: Rituais",
				["Bon Vivant"] = "Bon Vivant",
				["Book"] = "Livro",
				["BOOK"] = "LIVRO",
				["Bravo"] = "Valentão",
				["Brawl"] = "Briga",
				["BRAWLING CHART"] = "TABELA DE COMBATE",
				["Breed"] = "Raça",
				["Bruised"] = "Escoriado",
				["Bureaucrat"] = "Burocrata",
				["Caregiver"] = "Protetor",
				["Celebrant"] = "Celebrante",
				["Certification"] = "Certificação",
				["Chameleon"] = "Camaleão",
				["Change Log"] = "Notas da Versão",
				["Channeling"] = "Canalização",
				["Chantry"] = "Capela",
				["Charisma"] = "Carisma",
				["Child"] = "Criança",
				["Choose a path first"] = "Escolha uma trilha primeiro",
				["Choose a trait first"] = "Escolha um traço primeiro",
				["Christianity"] = "Cristianismo",
				["Chronicle"] = "Crônica",
				["Chronomancy"] = "Cronomancia",
				["Clairvoyance"] = "Clarividência",
				["Class"] = "Classe",
				["Clip"] = "Pente",
				["Colors"] = "Cores",
				["Combat"] = "Combate",
				["COMBAT"] = "COMBATE",
				["Combat & Items"] = "Combate e Itens",
				["COMBAT TRAITS"] = "TRAÇOS DE COMBATE",
				["Comedian"] = "Comediante",
				["Commerce"] = "Comércio",
				["Competitor"] = "Competidor",
				["Computer"] = "Computador",
				["Conceal"] = "Ocultação",
				["Concept"] = "Conceito",
				["Conformist"] = "Conformista",
				["Conjuration"] = "Conjuração",
				["Cosmology"] = "Cosmologia",
				["Conniver"] = "Conspirador",
				["Conscience"] = "Consciência",
				["CONTACTS"] = "CONTATOS",
				["Contacts"] = "Contatos",
				["Contrary"] = "Contrário",
				["Conveyance"] = "Transporte",
				["Cost"] = "Custo",
				["COST"] = "CUSTO",
				["DOMINATOR"] = "DOMINADOR",
				["Max Discipline Level"] = "Nível Máx. de Disciplina",
				["-"] = "-",
				["+"] = "+",
				["<"] = "<",
				[">"] = ">",
				["X"] = "X",
				["Clan"] = "Clã",
				["Family"] = "Família",
				["BLOOD POOL"] = "RESERVA DE SANGUE",
				["REVENANT WEAKNESS"] = "FRAQUEZA DE REVENANTE",
				["Courage"] = "Coragem",
				["Crafts"] = "Ofícios",
				["CREDITS"] = "CRÉDITOS",
				["Creep Show"] = "Show de Horrores",
				["Crippled (-5)"] = "Aleijado (-5)",
				["Critic"] = "Crítico",
				["Crusader"] = "Cruzado",
				["Cult"] = "Culto",
				["Curmudgeon"] = "Rabugento",
				["Current Experience"] = "Experiência Atual",
				["Current Version:"] = "Versão Atual:",
				["Curses"] = "Maldições",
				["Cyberkinesis"] = "Cibercinese",
				["Cyberpathy"] = "Ciberpatia",
				["Dabbler"] = "Diletante",
				["Dowsing"] = "Radiestesia",
				["Damage"] = "Dano",
				["damage"] = "Dano",
				["Defender"] = "Defensor",
				["Demeanor"] = "Comportamento",
				["Demesne"] = "Domínio Onírico",
				["DERANGEMENTS"] = "PERTURBAÇÕES",
				["DESCRIPTION"] = "DESCRIÇÃO",
				["Destiny"] = "Destino",
				["Developer: Andre (Styllern)"] = "Desenvolvedor: Andre (Styllern)",
				["Deviant"] = "Divergente",
				["dexterity"] = "Destreza",
				["Dexterity"] = "Destreza",
				["Diff."] = "Dif.",
				["Director"] = "Diretor",
				["DISCIPLINES"] = "DISCIPLINAS",
				["CLAN DISCIPLINES"] = "DISCIPLINAS DE CLÃ",
				["MAIN PATHS"] = "TRILHAS PRINCIPAIS",
				["SECONDARY PATHS"] = "TRILHAS SECUNDÁRIAS",
				["RITUALS"] = "RITUAIS",
				["Disciplines"] = "Disciplinas",
				["Divination"] = "Divinação",
				["Dodge"] = "Esquiva",
				["Domain"] = "Domínio",
				["Don't ask again."] = "Não perguntar novamente.",
				["Don't ask to update."] = "Não pedir para atualizar.",
				["Dream"] = "Sonho",
				["Drive"] = "Condução",
				["Ectoplasmic Generation"] = "Geração Ectoplásmica",
				["Empathy"] = "Empatia",
				["Enchantment"] = "Encantamento",
				["English"] = "English",
				["Enhancement"] = "Aprimoramento",
				["Enigma"] = "Enigma",
				["Enigmas"] = "Enigmas",
				["Ephemera"] = "Efêmera",
				["EQUIPMENT (OWNED)"] = "EQUIPAMENTO (POSSUÍDO)",
				["Esoterica"] = "Esotérica",
				["Ethnicity"] = "Etnia",
				["Etiquette"] = "Etiqueta",
				["EXPERIENCE"] = "EXPERIÊNCIA",
				["Experience"] = "Experiência",
				["Edit Clan Disciplines"] = "Editar Disciplinas de Clã",
				["Edit Spent XP"] = "Editar XP Gasto",
				["Edit Affiliation"] = "Editar Afiliação",
				["Edit Clan/Family"] = "Editar Clã/Família",

				-- Same word in both languages, and the key exists for exactly that reason
				-- (SPEC I44): the ledger writes this one through translateSheetText like every
				-- other column text, so without an entry it would be a visible string with no
				-- key - which is what V10/V22/V28 are there to refuse.
				["MANUAL"] = "MANUAL",
				["Experience cannot go below zero"] = "A experiência não pode ficar negativa",
				["EXPERIENCE LOG"] = "REGISTRO DE EXPERIÊNCIA",
				["Explorer"] = "Explorador",
				["Expression"] = "Expressão",
				["Eye of the Storm"] = "Olho do Furacão",
				["Eyes"] = "Cor dos Olhos",
				["Faith"] = "Fé",
				["Fame"] = "Fama",
				["FAME"] = "FAMA",
				["Familiar"] = "Familiar",
				["Fanatic"] = "Fanático",
				["Fascination"] = "Fascinação",
				["Fears"] = "Medos",
				["FEARS"] = "MEDOS",
				["Fetish"] = "Fetiche",
				["FETISHES"] = "FETICHES",
				["Finance"] = "Finanças",
				["Firearms"] = "Armas de Fogo",
				["Flaw"] = "Defeito",
				["FLAWS"] = "DEFEITOS",
				["Follower"] = "Seguidor",
				["Font"] = "Fonte",
				["Fortune"] = "Fortuna",
				["Free dots"] = "Pontos grátis",
				["Free specialties are lost only by lowering the trait"] = "Especializações gratuitas só são perdidas ao baixar o traço",
				["Gallant"] = "Galante",
				["Gambler"] = "Jogador",
				["Game"] = "Jogo",
				["GEAR (CARRIED)"] = "EQUIPAMENTO (CARREGADO)",
				["Generation"] = "Geração",
				["Gifts"] = "Presentes",
				["Glory"] = "Glória",
				["Gnosis"] = "Gnose",
				["Goals"] = "Metas",
				["GOALS"] = "METAS",
				["Grifter"] = "Esperto",
				["Guide"] = "Guia",
				["GUIDES"] = "GUIAS",
				["Guru"] = "Guru",
				["Hacker"] = "Hacker",
				["Hair"] = "Cabelos",
				["Healing"] = "Cura",
				["HEALTH"] = "VITALIDADE",
				["Health Levels"] = "Níveis de Vitalidade",
				["Hearth Wisdom"] = "Sabedoria Popular",
				["Height"] = "Altura",
				["Hellfire"] = "Fogo do Inferno",
				["Herd"] = "Rebanho",
				["Honor"] = "Honra",
				["HUMANITY"] = "HUMANIDADE",
				["Humanity"] = "Humanidade",
				["Path of Humanity"] = "Trilha da Humanidade",
				["Mortal"] = "Mortal",
				["Hurt (-1)"] = "Machucado (-1)",
				["Idealist"] = "Idealista",
				["Illusion"] = "Ilusão",
				["Incapacitated"] = "Incapacitado",
				["INFLUENCE"] = "INFLUÊNCIA",
				["Influence"] = "Influência",
				["Initial character not saved yet"] = "Personagem inicial ainda não salvo",
				["Injured (-1)"] = "Ferido (-1)",
				["Innovator"] = "Inovador",
				["INSTALLED VERSION:"] = "VERSÃO INSTALADA:",
				["Inteligence"] = "Inteligência",
				["Intelligence"] = "Inteligência",
				["Intimidation"] = "Intimidação",
				["Inventory"] = "Inventário",
				["Investigation"] = "Investigação",
				["Islam"] = "Islamismo",
				["ITEMS"] = "ITENS",
				["Jester"] = "Bobo da Corte",
				["Judaism"] = "Judaísmo",
				["Judge"] = "Juiz",
				["Kinfolk"] = "Parentes",
				["KNOWLEDGES"] = "CONHECIMENTOS",
				["Language"] = "Idioma",
				["Languages"] = "Idiomas",
				["LANGUAGES"] = "IDIOMAS",
				["Larceny"] = "Furto",
				["Law"] = "Direito",
				["Leader"] = "Líder",
				["Leadership"] = "Liderança",
				["Legend"] = "Lenda",
				["Legerdemain"] = "Prestidigitação",
				["Level"] = "Nível",
				["Library"] = "Biblioteca",
				["Lines"] = "Linhas",
				["Loner"] = "Solitário",
				["Machine"] = "Máquina",
				["Mad Scientist"] = "Cientista Louco",
				["Mage"] = "Mago",
				["Maelstroms"] = "Maelstroms",
				["Main"] = "Principal",
				["Manipulation"] = "Manipulação",
				["Martial Arts"] = "Artes Marciais",
				["Martyr"] = "Mártir",
				["Masochist"] = "Masoquista",
				["Mauled (-2)"] = " Espancado (-2)",
				["Mediator"] = "Mediador",
				["Medicine"] = "Medicina",
				["medicine"] = "Medicina",
				["Meditation"] = "Meditação",
				["Melee"] = "Armas Brancas",
				["Mental"] = "Mentais",
				["Mentor"] = "Mentor",
				["MENTOR"] = "MENTOR",
				["Mercenary"] = "Mercenário",
				["Merit"] = "Qualidade",
				["MERITS"] = "QUALIDADES",
				["MERITS AND FLAWS"] = "QUALIDADES E DEFEITOS",
				["Metis Deformity"] = "Deformidade de Impuro",
				["Mind Shields"] = "Escudos Mentais",
				["Modern Nights"] = "Noites Modernas",
				["Monster"] = "Monstro",
				["Moral Flaws"] = "Falhas Morais",
				["MORAL FLAWS"] = "FALHAS MORAIS",
				["Motivation"] = "Motivação",
				["Name"] = "Nome",
				["name"] = "Nome",
				["Nationality"] = "Nacionalidade",
				["Nature"] = "Natureza",
				["Necromancy"] = "Necromancia",
				["Necronics"] = "Necrônica",
				["No"] = "Não",
				["No free specialty slot"] = "Não há espaço livre para especialização",
				["Node"] = "Nodo",
				["Nothing bought yet"] = "Nada comprado ainda",
				["Not enough experience to buy that dot"] = "Experiência insuficiente para comprar esse ponto",
				["Notes"] = "Anotações",
				["NOTES"] = "ANOTAÇÕES",
				["NUMINA"] = "NÚMINAS",
				["NUMINA & OTHER TRAITS"] = "NUMINA E OUTROS TRAÇOS",
				["HEDGE MAGIC PATHS"] = "CAMINHOS DE MAGIA ESTÁTICA",
				["PSYCHIC PHENOMENA"] = "FENÔMENOS PSÍQUICOS",
				["Hedge Magic"] = "Magia Estática",
				["Psychic Phenomena"] = "Fenômenos Psíquicos",
				["Paths"] = "Caminhos",
				["HEDGE MAGIC RITUALS"] = "RITUAIS DE MAGIA ESTÁTICA",
				["HEDGE MAGIC"] = "MAGIA ESTÁTICA",
				["QUINTESSENCE"] = "QUINTESSÊNCIA",
				["Essence"] = "Essência",
				["Affiliation"] = "Afiliação",
				["Casting Attribute"] = "Atributo de Conjuração",
				["Balamo’ob"] = "Balamo’ob",
				["Forn Jafnaðr"] = "Forn Jafnaðr",
				["Lone Practitioner"] = "Praticante Solitário",
				["Maison Liban"] = "Maison Liban",
				["Mogen HaLev"] = "Mogen HaLev",
				["Nebuu-Afef, The Order of The Golden Fly"] = "Nebuu-Afef, a Ordem da Mosca Dourada",
				["Path-Dancers"] = "Dançarinos dos Caminhos",
				["Thal’hun"] = "Thal’hun",
				["The Ancient Order of The Aeon Rites"] = "A Antiga Ordem dos Ritos de Aeon",
				["The Arcanum"] = "O Arcanum",
				["The Children of Osiris"] = "Os Filhos de Osíris",
				["The Crystal Circle"] = "O Círculo de Cristal",
				["The Cult of Isis"] = "O Culto de Ísis",
				["The Cult of Mercury"] = "O Culto de Mercúrio",
				["The Dozen Priests of the Pythian Order"] = "Os Doze Sacerdotes da Ordem Pítia",
				["The Fenian"] = "Os Fenianos",
				["The Nephite Priesthood"] = "O Sacerdócio Nefita",
				["The Seven Thunders"] = "Os Sete Trovões",
				["The Silver Portal"] = "O Portal de Prata",
				["The Society of Enlightened Altruistic Ideologies (SEAI)"] = "Sociedade de Ideologias Altruístas Esclarecidas (SEAI)",
				["The Society of Leopold"] = "A Sociedade de Leopold",
				["The Star Council"] = "O Conselho Estelar",
				["U.S. Government (Project Twilight)"] = "Governo dos EUA (Projeto Twilight)",
				["Uzoma"] = "Uzoma",
				["Occult"] = "Ocultismo",
				["Occupation"] = "Ocupação",
				["Oneiromancy"] = "Oniromancia",
				["Optimist"] = "Otimista",
				["Other"] = "Outro",
				["OTHER"] = "OUTROS",
				["Other Traits"] = "Outros Traços",
				["Pack Name"] = "Matilha",
				["Pack Totem"] = "Totem",
				["Page"] = "Página",
				["PAGE"] = "PÁGINA",
				["Past Lives"] = "Vidas Passadas",
				["Patron"] = "Patrono",
				["Patron (1)"] = "Patrono (1)",
				["Patron (2)"] = "Patrono (2)",
				["Patron (3)"] = "Patrono (3)",
				["Patron (4)"] = "Patrono (4)",
				["Patron (5)"] = "Patrono (5)",
				["Pedagogue"] = "Pedagogo",
				["Penalty"] = "Penalidade",
				["Penitent"] = "Penitente",
				["Perception"] = "Percepção",
				["Perfectionist"] = "Perfeccionista",
				["Performance"] = "Performance",
				["Philosopher"] = "Filósofo",
				["Philosophy"] = "Filosofia",
				["Physical"] = "Físicos",
				["Player"] = "Jogador",
				["Politics"] = "Política",
				["Português BR"] = "Português BR",
				["power"] = "Poder",
				["Numina"] = "Númina",
				["Discipline"] = "Disciplina",
				["Path"] = "Trilha",
				["Pragmatist"] = "Pragmático",
				["Precognition"] = "Precognição",
				["Prelude"] = "Prelúdio",
				["PRELUDE"] = "PRELÚDIO",
				["Primal-Urge"] = "Inst. Primitivo",
				["Programmer: Andre (Styllern)"] = "Programador: André (Styllern)",
				["Prophet"] = "Profeta",
				["Purchase Forbidden"] = "Compra Proibida",
				["Psychic Healing"] = "Cura Psíquica",
				["Psychic Hypnosis"] = "Hipnose Psíquica",
				["Psychic Invisibility"] = "Invisibilidade Psíquica",
				["Psychic Vampirism"] = "Vampirismo Psíquico",
				["Psychokinesis"] = "Psicocinese",
				["Psychometry"] = "Psicometria",
				["Psychoportation"] = "Psicoportação",
				["Pure Breed"] = "Raça Pura",
				["Pyrokinesis"] = "Pirocinese",
				["Quintessence Manipulation"] = "Manipulação de Quintessência",
				["Rage"] = "Fúria",
				["Range"] = "Alcance",
				["range"] = "Alcance",
				["Rank"] = "Posto",
				["Rate"] = "Taxa",
				["rate"] = "Taxa",
				["Rating"] = "Índice",
				["Rebel"] = "Rebelde",
				["Rebel (1)"] = "Rebelde (1)",
				["Rebel (2)"] = "Rebelde (2)",
				["Rebel (3)"] = "Rebelde (3)",
				["Religion"] = "Religião",
				["Requisitions"] = "Requisições",
				["Research"] = "Pesquisa",
				["Residence"] = "Residência",
				["Resources"] = "Recursos",
				["RESOURCES"] = "RECURSOS",
				["RETAINERS"] = "LACAIOS",
				["Retainers"] = "Lacaios",
				["Ride"] = "Cavalgar",
				["Rites"] = "Ritos",
				["Rituais"] = "Rituais",
				["Ritual"] = "Ritual",
				["Rituals"] = "Rituais",
				["Rogue"] = "Canalha",
				["roll"] = "Rolagem",
				["Roll"] = "Rolagem",
				["Sadist"] = "Sádico",
				["Samaritan"] = "Samaritano",
				["Sanctum"] = "Santuário",
				["Save"] = "Salvar",
				["Save Initial Character"] = "Salvar Personagem Inicial",
				["Save the initial character? This cannot be undone."] = "Salvar o personagem inicial? Esta ação não pode ser desfeita.",
				["Science"] = "Ciência",
				["Scientist"] = "Cientista",
				["Secret Weapons"] = "Armas Secretas",
				["Self-Control"] = "Autocontrole",
				["Seneschal"] = "Senescal",
				["Sensualist"] = "Sensualista",
				["Settings"] = "Configurações",
				["Sex"] = "Sexo",
				["Shadow"] = "Sombra",
				["Shadows"] = "Sombras",
				["Shapeshifting"] = "Metamorfose",
				["Sheet made for the table"] = "Ficha feita para a mesa",
				["Show Ghoul"] = "Mostrar Carniçal",
				["Show Numina"] = "Mostrar Númina",
				["SKILLS"] = "PERÍCIAS",
				["Social"] = "Sociais",
				["Soldier"] = "Soldado",
				["SPECIALTIES"] = "ESPECIALIZAÇÕES",
				["Special thanks to Kazuo (YonHoshiahra)"] = "Agradecimento especial à Kazuo (YonHoshiahra)",
				["Specialties cannot be bought"] = "Especializações não podem ser compradas",
				["Specialty"] = "Especialização",
				["Specialty Cost"] = "Custo por Especialização",
				["Spent Experience"] = "Experiência Gasta",
				["Spies"] = "Espiões",
				["Spirit-Chasing"] = "Perseguição de Espíritos",
				["Stamina"] = "Vigor",
				["Starlight"] = "Luz das Estrelas",
				["STATUS"] = "STATUS",
				["Status (Mage)"] = "Status (Mago)",
				["Status (Vampire)"] = "Status (Vampiro)",
				["Stealth"] = "Furtividade",
				["Storyteller"] = "Narrador",
				["STORYTELLER SETTINGS"] = "CONFIGURAÇÕES DO NARRADOR",
				["Streetwise"] = "Manha",
				["Strength"] = "Força",
				["strength"] = "Força",
				["Subterfuge"] = "Lábia",
				["Summoning, Binding, and Warding"] = "Invocação, Vínculo e Proteção",
				["Survival"] = "Sobrevivência",
				["Survivor"] = "Sobrevivente",
				["Synergy"] = "Sinergia",
				["System: Storyteller 20th Anniversary Edition"] = "Sistema: Storyteller edição de aniversário de 20 anos",
				["TALENTS"] = "TALENTOS",
				["Technology"] = "Tecnologia",
				["Telepathy"] = "Telepatia",
				["That blood sorcery already has a main path"] = "Essa feitiçaria de sangue já tem uma trilha principal",
				["That Discipline is above what the dominator's generation allows"] = "Essa Disciplina passa do que a geração do dominador permite",
				["That path is above what its blood sorcery allows"] = "Essa trilha passa do que a feitiçaria de sangue dela permite",
				["That family caps this trait"] = "Essa família limita esse traço",
				["That path needs its blood sorcery on the sheet"] = "Essa trilha precisa da feitiçaria de sangue dela na ficha",
				["That ritual is above what its blood sorcery allows"] = "Esse ritual passa do que a feitiçaria de sangue dele permite",
				["That ritual needs its blood sorcery on the sheet"] = "Esse ritual precisa da feitiçaria de sangue dele na ficha",
				["Theme:"] = "Tema:",
				["Theology"] = "Teologia",
				["There is a new version of this plugin. Install it?"] = "Há uma nova versão desse plugin. Deseja instalar?",
				["Thrill-Seeker"] = "Aventureiro",
				["Total Experience"] = "Experiência Total",
				["Totem (Mage)"] = "Totem (Mago)",
				["Totem (Werewolf)"] = "Totem (Lobisomem)",
				["Traditionalist"] = "Tradicionalista",
				["Trait"] = "Traço",
				["Traits"] = "Traços",
				["Translate"] = "Traduzir",
				["TRANSPORTATION"] = "TRANSPORTE",
				["Trendsetter"] = "Vanguardista",
				["Tribe"] = "Tribo",
				["Trickster"] = "Trapaceiro",
				["True Faith"] = "Fé Verdadeira",
				["TRUE FAITH"] = "FÉ VERDADEIRA",
				["Type"] = "Tipo",
				["Tyrant"] = "Tirano",
				["Update"] = "Atualizar",
				["Version"] = "Versão Atual",
				["Version: 0.1.1"] = "Versão: 0.1.1",
				["Victorian Age"] = "Era Vitoriana",
				-- Latin, and the book leaves it Latin inside English prose - so the entry is the SAME
				-- string on both sides (SPEC I17). The key still has to exist: V10 collects every picker
				-- item into the visible set, and a name with no key fails there whether it translates or
				-- not (SPEC V28).
				["Via Geniorum"] = "Via Geniorum",
				["Via Necromantiae"] = "Via Necromantiae",
				["Dark Ages"] = "Idade das Trevas",
				["Classical Age"] = "Idade Clássica",
				["Virtue"] = "Virtude",
				["VIRTUES"] = "VIRTUDES",
				["Visionary"] = "Visionário",
				["Visit the Table:"] = "Conheça a Mesa:",
				["Weapon/Attack"] = "Arma/Ataque",
				["Weather Control"] = "Controle do Clima",
				["Weight"] = "Peso",
				["Willpower"] = "Força-de-Vontade",
				["WILLPOWER"] = "FORÇA DE VONTADE",
				["Wisdom"] = "Sabedoria",
				["Ghoul"] = "Carniçal",
				["Vampire"] = "Vampiro",
				["Wits"] = "Raciocínio",
				["Wonder"] = "Maravilha",
				["Wounded (-2)"] = "Ferido Gravemente (-2)",
				["Yes"] = "Sim",

				-- >>> VAMPIRE_ITEMS_BEGIN (generated - SPEC T447)
				-- Every picker item the Vampire tab and the dormant clan combo offer (SPEC V17,
				-- V28), plus the Dark Ages display names of ERA_NAME, which pass through here
				-- like any other label once the era has renamed the value (SPEC I22, V194).
				["Abombwe"] = "Abombwe",
				["Abyss Mysticism"] = "Misticismo do Abismo",
				["Animalism"] = "Animalismo",
				["Assamite Sorcery"] = "Feitiçaria Assamita",
				["Auspex"] = "Auspícios",
				["Bardo"] = "Bardo",
				["Celerity"] = "Rapidez",
				["Chimerstry"] = "Quimerismo",
				["Daimonion"] = "Daimonion",
				["Dark Thaumaturgy"] = "Taumaturgia Negra",
				["Dementation"] = "Demência",
				["Dominate"] = "Dominação",
				["Flight"] = "Voo",
				["Fortitude"] = "Fortitude",
				["Koldunic Sorcery"] = "Feitiçaria Koldúnica",
				["Melpominee"] = "Melpominee",
				["Mytherceria"] = "Mytherceria",
				["Obeah"] = "Obeah",
				["Obfuscate"] = "Ofuscação",
				["Obtenebration"] = "Tenebrosidade",
				["Ogham"] = "Ogham",
				["Potence"] = "Potência",
				["Presence"] = "Presença",
				["Protean"] = "Metamorfose",
				["Quietus"] = "Quietus",
				["Sanguinus"] = "Sanguinus",
				["Serpentis"] = "Serpentis",
				["Setite Sorcery"] = "Feitiçaria Setita",
				["Spiritus"] = "Spiritus",
				["Temporis"] = "Temporis",
				["Thanatosis"] = "Tanatose",
				["Thaumaturgy"] = "Taumaturgia",
				["Valeren"] = "Valeren",
				["Vicissitude"] = "Vicissitude",
				["Visceratika"] = "Visceratika",
				["Ash Path"] = "Trilha das Cinzas",
				["Awakening of the Steel"] = "O Despertar do Aço",
				["Bone Path"] = "Trilha dos Ossos",
				["Cenotaph Path"] = "Trilha do Cenotáfio",
				["Corpse in the Monster"] = "O Cadáver no Monstro",
				["Die Herrschaft des Wyrm"] = "Die Herrschaft des Wyrm",
				["Elemental Mastery"] = "Domínio dos Elementos",
				["Grave’s Decay"] = "A Decomposição da Sepultura",
				["Hands of Destruction"] = "Mãos da Destruição",
				["Iter Pernix"] = "Iter Pernix",
				["Lure of Flames"] = "O Fascínio das Chamas",
				["Mastery of the Mortal Shell"] = "Domínio do Invólucro Mortal",
				["Movement of the Mind"] = "Movimento da Mente",
				["Neptune’s Might"] = "O Poder de Netuno",
				["Path of Conjuring"] = "Trilha da Conjuração",
				["Path of Corruption"] = "Trilha da Corrupção",
				["Path of Haunting"] = "Trilha da Assombração",
				["Path of Mars"] = "Trilha de Marte",
				["Path of Spirit Manipulation"] = "Trilha da Manipulação de Espíritos",
				["Path of Technomancy"] = "Trilha da Tecnomancia",
				["Path of the Father’s Vengeance"] = "Trilha da Vingança do Pai",
				["Path of the Focused Mind"] = "Trilha da Mente Focada",
				["Path of the Levinbolt"] = "Trilha do Raio",
				["Potestas Exsecrabilis"] = "Potestas Exsecrabilis",
				["Rego Calatio"] = "Rego Calatio",
				["Sepulchre Path"] = "Trilha do Sepulcro",
				["The Bialowieza Kraina"] = "A Kraina de Bialowieza",
				["The Black Sea Kraina"] = "A Kraina do Mar Negro",
				["The Blessings of the Great Dark Mother"] = "As Bênçãos da Grande Mãe Sombria",
				["The Covenant of Enki"] = "O Pacto de Enki",
				["The Fires of the Inferno"] = "Os Fogos do Inferno",
				["The Flow of Ashe"] = "O Fluxo do Ashe",
				["The Green Path"] = "A Trilha Verde",
				["The Hand of the Magi"] = "A Mão dos Magos",
				["The Hunter’s Winds"] = "Os Ventos do Caçador",
				["The Keeper’s Way"] = "A Via do Guardião",
				["The Kraina of Enoch"] = "A Kraina de Enoque",
				["The Kraina of the Well"] = "A Kraina do Poço",
				["The Laws of Suleiman"] = "As Leis de Suleiman",
				["The Nightshade Path"] = "A Trilha da Beladona",
				["The Path of Blood"] = "A Trilha do Sangue",
				["The Path of Jinn’s Gift"] = "A Trilha do Dom do Djinn",
				["The Path of Life’s Waters"] = "A Trilha das Águas da Vida",
				["The Path of Phobos"] = "A Trilha de Phobos",
				["The Path of Praapti"] = "A Trilha de Praapti",
				["The Path of Skulls"] = "A Trilha das Caveiras",
				["The Path of Woe"] = "A Trilha da Aflição",
				["The Path of the Ailing Jackal"] = "A Trilha do Chacal Enfermo",
				["The Path of the Evil Eye"] = "A Trilha do Mau-Olhado",
				["The Path of the Twilight Garden"] = "A Trilha do Jardim Crepuscular",
				["The Revelations of Duat"] = "As Revelações de Duat",
				["The Revelations of Eden"] = "As Revelações do Éden",
				["The Revelations of Midgard"] = "As Revelações de Midgard",
				["The Taking of the Spirit"] = "A Tomada do Espírito",
				["The Transylvanian Kraina"] = "A Kraina da Transilvânia",
				["The Way of Earth"] = "A Via da Terra",
				["The Way of Fire"] = "A Via do Fogo",
				["The Way of Sorrows"] = "A Via das Dores",
				["The Way of Water"] = "A Via da Água",
				["The Way of Wind"] = "A Via do Vento",
				["The Way of the Spirit"] = "A Via do Espírito",
				["Ushabti"] = "Ushabti",
				["Vitreous Path"] = "Trilha Vítrea",
				["1. Ambrus Kelemen’s Aegis"] = "1. A Égide de Ambrus Kelemen",
				["1. Armor of Diamond Serenity"] = "1. Armadura da Serenidade de Diamante",
				["1. Bind the Accusing Tongue"] = "1. Atar a Língua Acusadora",
				["1. Blood Mastery"] = "1. Domínio do Sangue",
				["1. Death Curse"] = "1. Maldição da Morte",
				["1. Deathsight"] = "1. Visão da Morte",
				["1. Eldritch Mark"] = "1. Marca Arcana",
				["1. Face Theft"] = "1. Roubo de Rosto",
				["1. Healing Slumber"] = "1. Sono Curativo",
				["1. Lifting Shadows"] = "1. Erguer Sombras",
				["1. Sprint"] = "1. Disparada",
				["1. Symbol Interpretation"] = "1. Interpretação de Símbolos",
				["2. Bedtime Story"] = "2. História de Ninar",
				["2. Cruel Whispers"] = "2. Sussurros Cruéis",
				["2. Enhance Craftsmanship"] = "2. Aprimorar o Artesanato",
				["2. Fire's Weal"] = "2. Bênção do Fogo",
				["2. Hellblade"] = "2. Lâmina Infernal",
				["2. Jolt"] = "2. Choque",
				["2. Object Permanence"] = "2. Permanência do Objeto",
				["2. Quintessence Infusion"] = "2. Infusão de Quintessência",
				["2. Rest in Peace"] = "2. Descanse em Paz",
				["2. Step on a Crack"] = "2. Pisar na Rachadura",
				["2. Teleport Ward"] = "2. Barreira de Teleporte",
				["3. Always Armed"] = "3. Sempre Armado",
				["3. Bashert"] = "3. Bashert",
				["3. Belle/Beau/Bright of the Ball"] = "3. Beldade/Galã/Estrela do Baile",
				["3. Calm Above, Hell Below"] = "3. Calmaria Acima, Inferno Abaixo",
				["3. Get Me the Heck Outta Here!"] = "3. Me Tira Daqui!",
				["3. Grip of Shades"] = "3. Garra das Sombras",
				["3. Hard-Light Constructs"] = "3. Construtos de Luz Sólida",
				["3. Invade Demesne"] = "3. Invadir o Domínio",
				["3. Mike's Cure-All"] = "3. Cura-Tudo do Mike",
				["3. Purification of the Inferno"] = "3. Purificação do Inferno",
				["3. Shape Quintessence"] = "3. Moldar Quintessência",
				["3. Shitstorm"] = "3. Tempestade de Merda",
				["3. Shroud Bubble"] = "3. Bolha da Mortalha",
				["3. Shut It Down"] = "3. Desligar Tudo",
				["3. Wrapped in a Shroud"] = "3. Envolto em Mortalha",
				["4. Doxxing"] = "4. Doxxing",
				["4. Dream Scream"] = "4. Grito Onírico",
				["4. Extraction"] = "4. Extração",
				["4. Fix the True Form"] = "4. Fixar a Forma Verdadeira",
				["4. Forced Medium"] = "4. Médium Forçado",
				["4. Freudian Slip"] = "4. Ato Falho",
				["4. Humor Alignment"] = "4. Alinhamento dos Humores",
				["4. Information Superhighway"] = "4. Superestrada da Informação",
				["4. Instant Feast"] = "4. Banquete Instantâneo",
				["4. Love Potion Number 9"] = "4. Poção do Amor Número 9",
				["5. Generational Wealth"] = "5. Riqueza Geracional",
				["5. Megafauna Transformation"] = "5. Transformação em Megafauna",
				["5. Oubliette"] = "5. Oubliette",
				["5. Overwrite"] = "5. Sobrescrever",
				["5. Shelter for the Dead"] = "5. Abrigo para os Mortos",
				["5. Smoldering Ruin"] = "5. Ruína Fumegante",
				["5. Steal Life"] = "5. Roubar a Vida",
				["5. Teleportal"] = "5. Teleportal",
				["1. Blood Rush"] = "1. Ímpeto de Sangue",
				["1. Blood into Water"] = "1. Sangue em Água",
				["1. Brand"] = "1. Marca a Ferro",
				["1. CCTV"] = "1. CCTV",
				["1. Call of the Hungry Dead"] = "1. O Chamado dos Mortos Famintos",
				["1. Calling Card"] = "1. Cartão de Visita",
				["1. Chime of Unseen Spirits"] = "1. Sino dos Espíritos Invisíveis",
				["1. Communicate with Kindred Sire"] = "1. Comunicar-se com o Senhor Membro",
				["1. Death’s Communion"] = "1. A Comunhão da Morte",
				["1. Defense of the Sacred Haven"] = "1. Defesa do Refúgio Sagrado",
				["1. Deflection of Wooden Doom"] = "1. Desviar a Perdição de Madeira",
				["1. Devil’s Touch"] = "1. O Toque do Diabo",
				["1. Domino of Life"] = "1. Dominó da Vida",
				["1. Drinking Death"] = "1. Beber a Morte",
				["1. Eldritch Beacon"] = "1. Farol Sobrenatural",
				["1. Encrypt Missive"] = "1. Cifrar Missiva",
				["1. Engaging the Vessel of Transference"] = "1. Engajar o Receptáculo da Transferência",
				["1. Eyes of the Abyss"] = "1. Olhos do Abismo",
				["1. Final Sight"] = "1. Última Visão",
				["1. Flatline"] = "1. Linha Reta",
				["1. Foxfire"] = "1. Fogo-Fátuo",
				["1. Horoscope"] = "1. Horóscopo",
				["1. Illuminate the Trail of Prey"] = "1. Iluminar o Rastro da Presa",
				["1. Impressive Visage"] = "1. Semblante Imponente",
				["1. Incantation of the Shepherd"] = "1. Encantamento do Pastor",
				["1. Insight"] = "1. Perspicácia",
				["1. Knowing Stone"] = "1. Pedra do Saber",
				["1. Learning the Mind Enslumbered"] = "1. Sondar a Mente Adormecida",
				["1. Light within Shadow"] = "1. Luz dentro da Sombra",
				["1. Locating the Oubliette"] = "1. Localizar o Oubliette",
				["1. Master of the Domain"] = "1. Senhor do Domínio",
				["1. Memento Mori"] = "1. Memento Mori",
				["1. Minestra di Morte"] = "1. Minestra di Morte",
				["1. Pierce the Veil"] = "1. Perfurar o Véu",
				["1. Preserve Corpse"] = "1. Preservar o Cadáver",
				["1. Purify Blood"] = "1. Purificar o Sangue",
				["1. Purity of Flesh"] = "1. Pureza da Carne",
				["1. Reawakening the Dead Water"] = "1. Reavivar a Água Morta",
				["1. Rite of Introduction"] = "1. Rito de Apresentação",
				["1. Rite of Reclamation"] = "1. Rito de Reivindicação",
				["1. Ritual of Return"] = "1. Ritual do Retorno",
				["1. Ritual of the Smoking Mirror"] = "1. Ritual do Espelho Fumegante",
				["1. Sanctify the Temple"] = "1. Santificar o Templo",
				["1. Sanguineous Phial"] = "1. Frasco Sanguíneo",
				["1. Sense the Mystical"] = "1. Sentir o Místico",
				["1. Servitor’s Perception"] = "1. A Percepção do Servo",
				["1. Shew-Stone"] = "1. Pedra-de-Ver",
				["1. Sigil of Authority"] = "1. Sigilo da Autoridade",
				["1. Strength of Haqim"] = "1. A Força de Haqim",
				["1. Tame the Maddening Flame"] = "1. Domar a Chama Enlouquecedora",
				["1. The Abyss Knows"] = "1. O Abismo Sabe",
				["1. The Word of the Dark God"] = "1. A Palavra do Deus Sombrio",
				["1. Treasured Ornaments"] = "1. Ornamentos Preciosos",
				["1. Truth in Water"] = "1. Verdade na Água",
				["1. Typhon’s Brew"] = "1. A Poção de Tífon",
				["1. Unnatural Decay"] = "1. Decomposição Antinatural",
				["1. Wake with Evening’s Freshness"] = "1. Despertar com o Frescor da Noite",
				["1. Water Walking"] = "1. Caminhar sobre as Águas",
				["1. Weight of Shadows"] = "1. O Peso das Sombras",
				["1. Widow’s Spite"] = "1. O Rancor da Viúva",
				["1. Word of Insight"] = "1. Palavra de Perspicácia",
				["2. Abyssal Blade"] = "2. Lâmina Abissal",
				["2. Antonius’s Denial"] = "2. A Negação de Antonius",
				["2. Ascension of the Blood"] = "2. Ascensão do Sangue",
				["2. Autumn’s Armor"] = "2. A Armadura do Outono",
				["2. Banning Rituals"] = "2. Rituais de Banimento",
				["2. Beat Your Way to Glory"] = "2. Espancar-se até a Glória",
				["2. Blade of Smoke and Shadow"] = "2. Lâmina de Fumaça e Sombra",
				["2. Blood Calls to Blood"] = "2. Sangue Chama Sangue",
				["2. Blood Crystal"] = "2. Cristal de Sangue",
				["2. Blood Walk"] = "2. Caminhada de Sangue",
				["2. Borne by Wind"] = "2. Levado pelo Vento",
				["2. Burning Blade"] = "2. Lâmina Ardente",
				["2. Comforting Darkness"] = "2. Escuridão Reconfortante",
				["2. Commune with Cainite"] = "2. Comungar com o Cainita",
				["2. Concentrated Vitae"] = "2. Vitae Concentrada",
				["2. Craft Bloodstone"] = "2. Forjar a Pedra de Sangue",
				["2. Craft Gris-Gris"] = "2. Confeccionar o Gris-Gris",
				["2. Donning the Mask of Shadows"] = "2. Vestir a Máscara de Sombras",
				["2. Draining the Well of Life"] = "2. Drenar o Poço da Vida",
				["2. Extinguish"] = "2. Extinguir",
				["2. Eyes of Babel"] = "2. Olhos de Babel",
				["2. Eyes of the Dead"] = "2. Olhos dos Mortos",
				["2. Eyes of the Grave"] = "2. Olhos da Sepultura",
				["2. Eyes of the Night Hawk"] = "2. Olhos do Falcão Noturno",
				["2. Feed the Darkness"] = "2. Alimentar a Escuridão",
				["2. Flaming Weapon"] = "2. Arma Flamejante",
				["2. Generation of the Acheron Vortex"] = "2. Geração do Vórtice de Aqueronte",
				["2. Haruspicy"] = "2. Haruspicina",
				["2. Haunting Breeze"] = "2. Brisa Assombrada",
				["2. Hidden Haven"] = "2. Refúgio Oculto",
				["2. Illusion of Peaceful Death"] = "2. Ilusão de Morte Serena",
				["2. Implant the Bezoar"] = "2. Implantar o Bezoar",
				["2. Infusion of Kalif"] = "2. Infusão de Kalif",
				["2. Invoke the Lesser Sign of Power"] = "2. Invocar o Sinal Menor do Poder",
				["2. Iron Body"] = "2. Corpo de Ferro",
				["2. Machine Blitz"] = "2. Pane na Máquina",
				["2. Occhio d’Uomo Morto"] = "2. Occhio d’Uomo Morto",
				["2. Oinos of Dionysus"] = "2. O Oinos de Dionísio",
				["2. Parting the Veil"] = "2. Afastar o Véu",
				["2. Prepare the Vessel"] = "2. Preparar o Receptáculo",
				["2. Preserve"] = "2. Preservar",
				["2. Principal Focus of Vitae Infusion"] = "2. Foco Principal da Infusão de Vitae",
				["2. Puppet"] = "2. Marionete",
				["2. Recure of the Homeland"] = "2. Restauro da Terra Natal",
				["2. Seal Egress"] = "2. Selar a Saída",
				["2. Self-Executing File"] = "2. Arquivo Autoexecutável",
				["2. Subsume the Darkness"] = "2. Absorver a Escuridão",
				["2. Summon Guardian Spirit"] = "2. Convocar o Espírito Guardião",
				["2. Thanatos’ Caress"] = "2. A Carícia de Thanatos",
				["2. The Hand of Glory"] = "2. A Mão da Glória",
				["2. The Jinx"] = "2. O Azar",
				["2. The Ritual of Pochtli"] = "2. O Ritual de Pochtli",
				["2. To Lace with Hidden Nectar"] = "2. Adulterar com o Néctar Oculto",
				["2. Totenpass"] = "2. Totenpass",
				["2. Two Centimes"] = "2. Dois Cêntimos",
				["2. Ward"] = "2. Proteção",
				["2. Ward Versus Ghouls"] = "2. Proteção contra Carniçais",
				["2. Warding Circle versus Ghouls"] = "2. Círculo de Proteção contra Carniçais",
				["2. Whispers of the Ghost"] = "2. Sussurros do Fantasma",
				["2. Witch Eye"] = "2. Olho de Bruxa",
				["2. Witness of Whispers"] = "2. Testemunha dos Sussurros",
				["3. A Shield of Mirror"] = "3. Um Escudo de Espelho",
				["3. A Touch of Nightshade"] = "3. Um Toque de Beladona",
				["3. Abyssal Threads"] = "3. Fios Abissais",
				["3. Amulet of Mnemosyne"] = "3. Amuleto de Mnemosine",
				["3. Angra Mainyu Consumes Spenta Mainyu"] = "3. Angra Mainyu Consome Spenta Mainyu",
				["3. Animate Curios"] = "3. Animar Bugigangas",
				["3. Animated Assistants"] = "3. Assistentes Animados",
				["3. BFU"] = "3. BFU",
				["3. Balthazar’s Revelation"] = "3. A Revelação de Balthazar",
				["3. Bind the Familiar"] = "3. Atar o Familiar",
				["3. Blood Dance"] = "3. Dança de Sangue",
				["3. Blood and Bane"] = "3. Sangue e Ruína",
				["3. Bound by Oath"] = "3. Atado por Juramento",
				["3. Brotherhood of the Cup"] = "3. Irmandade do Cálice",
				["3. Bull of Heaven"] = "3. O Touro do Céu",
				["3. Calling the Hungry Shade"] = "3. Chamar a Sombra Faminta",
				["3. Claiming the Dark"] = "3. Reivindicar a Escuridão",
				["3. Clinging of the Insect"] = "3. Aderência do Inseto",
				["3. Cowing the Servant"] = "3. Subjugar o Servo",
				["3. Craft Dream Catcher"] = "3. Confeccionar o Filtro dos Sonhos",
				["3. Dante’s Urban Legend"] = "3. A Lenda Urbana de Dante",
				["3. Death’s Head"] = "3. A Caveira da Morte",
				["3. Din of the Damned"] = "3. Algazarra dos Condenados",
				["3. Displacement of the Pneuma"] = "3. Deslocamento do Pneuma",
				["3. Divine Sign"] = "3. Sinal Divino",
				["3. Enfolding the Believers"] = "3. Envolver os Fiéis",
				["3. Father’s Freedom"] = "3. A Liberdade do Pai",
				["3. Flesh of Fiery Touch"] = "3. Carne de Toque Ardente",
				["3. Hell’s Calling"] = "3. O Chamado do Inferno",
				["3. Incorporeal Passage"] = "3. Passagem Incorpórea",
				["3. Inscription"] = "3. Inscrição",
				["3. Mirror of Second Sight"] = "3. Espelho da Segunda Visão",
				["3. Nightmare Drums"] = "3. Tambores do Pesadelo",
				["3. One with the Dark"] = "3. Um com a Escuridão",
				["3. One with the Forest"] = "3. Um com a Floresta",
				["3. Pavis of Foul Presence"] = "3. Pavês da Presença Repulsiva",
				["3. Rings Like Chains"] = "3. Anéis como Correntes",
				["3. Ritual of The Unearthed Fetter"] = "3. Ritual do Grilhão Desenterrado",
				["3. Rutor’s Hands"] = "3. As Mãos de Rutor",
				["3. Samira’s Kihanah"] = "3. A Kihanah de Samira",
				["3. Sanguine Assistant"] = "3. Assistente Sanguíneo",
				["3. Sanguine Trail"] = "3. Rastro Sanguíneo",
				["3. Scorpion Sending"] = "3. Envio do Escorpião",
				["3. Scry the Hearthstone"] = "3. Vidência da Pedra do Lar",
				["3. Shaft of Belated Quiescence"] = "3. Estaca da Quietude Tardia",
				["3. Soul of the Homunculus"] = "3. Alma do Homúnculo",
				["3. Strength in Wisdom"] = "3. Força na Sabedoria",
				["3. Summoning the Blatherskites"] = "3. Invocar os Tagarelas",
				["3. Telecommunication"] = "3. Telecomunicação",
				["3. Tempest Prison"] = "3. Prisão de Tempestade",
				["3. Tempesta Scudo"] = "3. Tempesta Scudo",
				["3. The Bottled Voice"] = "3. A Voz Engarrafada",
				["3. The Floating Dead"] = "3. Os Mortos Flutuantes",
				["3. The Third Eye of Rickard Argentis"] = "3. O Terceiro Olho de Rickard Argentis",
				["3. Translocation of the Vessel"] = "3. Translocação do Receptáculo",
				["3. Transubstantiation of Seven"] = "3. Transubstanciação dos Sete",
				["3. Ward of the Winged Sepulcher"] = "3. Proteção do Sepulcro Alado",
				["3. Ward versus Lupines"] = "3. Proteção contra Lupinos",
				["3. Warding Circle versus Lupines"] = "3. Círculo de Proteção contra Lupinos",
				["3. Wield the Spear of Damnation"] = "3. Empunhar a Lança da Danação",
				["4. Animated Weapon"] = "4. Arma Animada",
				["4. Baleful Doll"] = "4. Boneca Maligna",
				["4. Baron Zaraguin’s Sting"] = "4. O Ferrão do Barão Zaraguin",
				["4. Bastone Diabolico"] = "4. Bastone Diabolico",
				["4. Bone of Lies"] = "4. Osso das Mentiras",
				["4. Boukephos’ Gateway"] = "4. O Portal de Boukephos",
				["4. Cadaver’s Touch"] = "4. O Toque do Cadáver",
				["4. Candle of Haunting"] = "4. Vela da Assombração",
				["4. Directing Ahriman’s Lance"] = "4. Dirigir a Lança de Ahriman",
				["4. Dismemberment of Osiris"] = "4. O Desmembramento de Osíris",
				["4. Evoke the Storm"] = "4. Evocar a Tempestade",
				["4. Firewalker"] = "4. Andarilho do Fogo",
				["4. Heart of Stone"] = "4. Coração de Pedra",
				["4. Houngan’s Doll"] = "4. A Boneca do Houngan",
				["4. Impregnable Soul"] = "4. Alma Inexpugnável",
				["4. Infirm Inert"] = "4. Enfermo e Inerte",
				["4. Innocence of the Child’s Heart"] = "4. A Inocência do Coração Infantil",
				["4. Invisible Chains of Binding"] = "4. Correntes Invisíveis do Vínculo",
				["4. Lord Aludian’s Orifices"] = "4. Os Orifícios do Lorde Aludian",
				["4. Maw of Ahriman"] = "4. As Fauces de Ahriman",
				["4. Mirror Walk"] = "4. Caminhada pelo Espelho",
				["4. Non-Disclosure Clause"] = "4. Cláusula de Confidencialidade",
				["4. Peek Past the Shroud"] = "4. Espiar Além da Mortalha",
				["4. Point of the Needle"] = "4. A Ponta da Agulha",
				["4. Preservation of the Sanguine Heart"] = "4. Preservação do Coração Sanguíneo",
				["4. Rebirth in the Light of Haqim"] = "4. Renascimento na Luz de Haqim",
				["4. Reflections of Hollow Revelation"] = "4. Reflexos da Revelação Oca",
				["4. Return of the Heart"] = "4. O Retorno do Coração",
				["4. Ritual of Xipe Totec"] = "4. Ritual de Xipe Totec",
				["4. Scry"] = "4. Vidência",
				["4. Scrying Pool"] = "4. Lago de Vidência",
				["4. Splinter Servant"] = "4. Servo de Lasca",
				["4. Stolen Kisses"] = "4. Beijos Roubados",
				["4. Summon Ethereal Horde"] = "4. Convocar a Horda Etérea",
				["4. Summon the Blood Imp"] = "4. Convocar o Diabrete de Sangue",
				["4. The Curse Belated"] = "4. A Maldição Tardia",
				["4. Ties That Bind"] = "4. Laços que Prendem",
				["4. Track Transgressor"] = "4. Rastrear o Transgressor",
				["4. Transcending Barriers Through Love"] = "4. Transcender Barreiras pelo Amor",
				["4. Unweave Ritual"] = "4. Desfiar o Ritual",
				["4. Vigil of the Stone Guardian"] = "4. Vigília do Guardião de Pedra",
				["4. Vision of Shadow"] = "4. Visão de Sombra",
				["4. Vision of St. Anthony"] = "4. A Visão de Santo Antônio",
				["4. Ward versus Kindred"] = "4. Proteção contra Membros",
				["4. Ward versus Vitae"] = "4. Proteção contra Vitae",
				["4. Warding Circle versus Kindred"] = "4. Círculo de Proteção contra Membros",
				["5. Abandon the Fetters"] = "5. Abandonar os Grilhões",
				["5. Ahriman’s Wight"] = "5. A Criatura de Ahriman",
				["5. Asymmetrical Reproduction"] = "5. Reprodução Assimétrica",
				["5. At Our Command It Breathes"] = "5. Ao Nosso Comando Ele Respira",
				["5. Beckon the Ingurgitant"] = "5. Acenar ao Devorador",
				["5. Blood Contract"] = "5. Contrato de Sangue",
				["5. Bloody Mary"] = "5. Maria Sangrenta",
				["5. Cheat the Scale of Hades"] = "5. Burlar a Balança de Hades",
				["5. Chill of Oblivion"] = "5. O Frio do Esquecimento",
				["5. Cobra’s Favor"] = "5. O Favor da Naja",
				["5. Commune with the Abyss"] = "5. Comungar com o Abismo",
				["5. Court of Hallowed Truth"] = "5. Corte da Verdade Sagrada",
				["5. Creation of the Shabti"] = "5. Criação do Shabti",
				["5. Dead Man’s Hand"] = "5. A Mão do Homem Morto",
				["5. Dedicate the Haven"] = "5. Consagrar o Refúgio",
				["5. Enchant Talisman"] = "5. Encantar o Talismã",
				["5. Enochian Passage"] = "5. Passagem Enoquiana",
				["5. Escape to a True Friend"] = "5. Fuga para um Verdadeiro Amigo",
				["5. Esilio"] = "5. Esilio",
				["5. From Marduk’s Throat"] = "5. Da Garganta de Marduk",
				["5. Ghost in the System"] = "5. Fantasma no Sistema",
				["5. Grasp the Ghostly"] = "5. Agarrar o Espectral",
				["5. Mirror Prison"] = "5. Prisão de Espelho",
				["5. Mirror of Blood"] = "5. Espelho de Sangue",
				["5. Nectar of the Bitter Rose"] = "5. Néctar da Rosa Amarga",
				["5. Occlude the Western Road"] = "5. Obstruir a Estrada do Oeste",
				["5. Orphic Sojourn"] = "5. Estada Órfica",
				["5. Paper Flesh"] = "5. Carne de Papel",
				["5. Ravening Walls of Ceoris"] = "5. As Muralhas Vorazes de Ceoris",
				["5. Rickard Argentis’s Homunculus"] = "5. O Homúnculo de Rickard Argentis",
				["5. Rite of Marduk Slain and Risen"] = "5. Rito de Marduk Morto e Ressurgido",
				["5. Sanctuary"] = "5. Santuário",
				["5. Seeing With the Sky’s Eyes"] = "5. Ver com os Olhos do Céu",
				["5. Severed Hand"] = "5. Mão Decepada",
				["5. Shackles of Blood"] = "5. Algemas de Sangue",
				["5. Skinwalker’s Belt"] = "5. O Cinto do Andarilho de Pele",
				["5. Stone Slumber"] = "5. Sono de Pedra",
				["5. Stone of the True Form"] = "5. Pedra da Forma Verdadeira",
				["5. The Ferryman’s Recall"] = "5. O Chamado do Barqueiro",
				["5. The Inmost Tug"] = "5. O Puxão Mais Íntimo",
				["5. The Pursuit of Apotheosis"] = "5. A Busca da Apoteose",
				["5. Treasures of Hades"] = "5. Tesouros de Hades",
				["5. Ward versus Spirits"] = "5. Proteção contra Espíritos",
				["5. Warding Circle versus Technology"] = "5. Círculo de Proteção contra a Tecnologia",
				["5. Warding Circles versus Spirits, Ghosts, and Demons"] = "5. Círculos de Proteção contra Espíritos, Fantasmas e Demônios",
				["5. Way of the Wendigo"] = "5. A Via do Wendigo",
				["5. Whispers in the Dark"] = "5. Sussurros na Escuridão",
				["6. Into the Chasm"] = "6. Rumo ao Abismo",
				["7. Evocation of the Oubliette"] = "7. Evocação do Oubliette",
				["8. Cry That Slays the Light"] = "8. O Grito que Mata a Luz",
				["8. Lord Aludian’s Claim"] = "8. A Reivindicação do Lorde Aludian",
				["9. Boukephos’ Chosen Oubliette"] = "9. O Oubliette Escolhido de Boukephos",
				["Ahrimanes"] = "Ahrimanes",
				["Anda"] = "Anda",
				["Angellis Ater"] = "Angellis Ater",
				["Assamite"] = "Assamita",
				["Assamite Antitribu"] = "Assamitas Antitribu",
				["Assamite Sorcerers"] = "Assamitas: Feiticeiros",
				["Assamite Viziers"] = "Assamitas: Vizires",
				["Baali"] = "Baali",
				["Blood Brothers"] = "Irmãos de Sangue",
				["Brahman"] = "Brahman",
				["Brujah"] = "Brujah",
				["Brujah Antitribu"] = "Brujah Antitribu",
				["Cappadocians"] = "Capadócios",
				["Children of Osiris"] = "Filhos de Osíris",
				["City Gangrel"] = "Gangrel Urbanos",
				["Country Gangrel"] = "Gangrel Rurais",
				["Daitya"] = "Daitya",
				["Daughters of Cacophony"] = "Filhas da Cacofonia",
				["Dominate Malkavians"] = "Malkavianos de Dominação",
				["Followers of Set"] = "Seguidores de Set",
				["Gangrel"] = "Gangrel",
				["Gargoyles (Modern)"] = "Gárgulas (Era Moderna)",
				["Gargoyles (Scout)"] = "Gárgulas (Batedoras)",
				["Gargoyles (Sentinel)"] = "Gárgulas (Sentinelas)",
				["Gargoyles (Warrior)"] = "Gárgulas (Guerreiras)",
				["Giovanni"] = "Giovanni",
				["Harbingers of Skulls"] = "Precursores do Ódio",
				["Kiasyd"] = "Kiasyd",
				["Lamia"] = "Lamia",
				["Lasombra"] = "Lasombra",
				["Lasombra Antitribu"] = "Lasombra Antitribu",
				["Lhiannan"] = "Lhiannan",
				["Maeghar"] = "Maeghar",
				["Malkavian"] = "Malkaviano",
				["Malkavian Antitribu"] = "Malkavianos Antitribu",
				["Mariners"] = "Marinheiros",
				["Nagaraja"] = "Nagaraja",
				["Noiad"] = "Noiad",
				["Nosferatu"] = "Nosferatu",
				["Nosferatu Antitribu"] = "Nosferatu Antitribu",
				["Old Clan Tzimisce"] = "Tzimisce do Clã Antigo",
				["Caitiff"] = "Caitiff",
				["Panders"] = "Panders",
				["Ravnos"] = "Ravnos",
				["Ravnos Antitribu"] = "Ravnos Antitribu",
				["Salubri"] = "Salubri",
				["Salubri Antitribu"] = "Salubri Antitribu",
				["Samedi"] = "Samedi",
				["Serpents of the Light"] = "Serpentes da Luz",
				["Telyavelic Tremere"] = "Tremere Telyavélicos",
				["Tlacique"] = "Tlacique",
				["Toreador"] = "Toreador",
				["Toreador Antitribu"] = "Toreador Antitribu",
				["Tremere"] = "Tremere",
				["Tremere Antitribu"] = "Tremere Antitribu",
				["True Brujah"] = "Brujah Verdadeiros",
				["Tzimisce"] = "Tzimisce",
				["Ventrue"] = "Ventrue",
				["Ventrue Antitribu"] = "Ventrue Antitribu",
				["Warrior Setites"] = "Seguidores de Set: Guerreiros",
				["Wu Zao"] = "Wu Zao",
				["Basarab"] = "Basarab",
				["Bratovich (Dark Ages)"] = "Bratovich (Idade das Trevas)",
				["Bratovitch (Modern)"] = "Bratovitch (Era Moderna)",
				["D'Habi"] = "D'Habi",
				["Danislav"] = "Danislav",
				["Ducheski"] = "Ducheski",
				["Enrathi"] = "Enrathi",
				["Grimaldi (Dark Ages)"] = "Grimaldi (Idade das Trevas)",
				["Grimaldi (Modern)"] = "Grimaldi (Era Moderna)",
				["Kairouan Brotherhood"] = "Irmandade de Kairouan",
				["Keskinen"] = "Keskinen",
				["Khavi"] = "Khavi",
				["Krevcheski"] = "Krevcheski",
				["Marijava"] = "Marijava",
				["Obertus"] = "Obertus",
				["Obertus (Narov)"] = "Obertus (Narov)",
				["Oprichniki"] = "Oprichniki",
				["Rafastio"] = "Rafastio",
				["Rossellini"] = "Rossellini",
				["Servants of Anushin-Rawan"] = "Servos de Anushin-Rawan",
				["Szantovich"] = "Szantovich",
				["Vlaszy"] = "Vlaszy",
				["Zantosa"] = "Zantosa",
				["1. Encrypt/Decrypt Missive"] = "1. Cifrar/Decifrar Missiva",
				["2. Cure of the Homeland"] = "2. Cura da Terra Natal",
				["2. Hand of Glory"] = "2. Mão da Glória",
				["3. Tempest Shield"] = "3. Escudo de Tempestade",
				["4. Bastone del Diavolo"] = "4. Bastone del Diavolo",
				["5. Escape to a True Haven"] = "5. Fuga para um Refúgio Verdadeiro",
				["Creatio Ignis (The Creation of Fire)"] = "Creatio Ignis (A Criação do Fogo)",
				["Genius Loci"] = "Genius Loci",
				["Potestas Elementorum (The Power of the Elements)"] = "Potestas Elementorum (O Poder dos Elementos)",
				["Potestas Motus (Power of Motion)"] = "Potestas Motus (Poder do Movimento)",
				["Potestas Tempestatum (Power Over Storms)"] = "Potestas Tempestatum (Poder sobre as Tempestades)",
				["Potestas Vitae (The Power of Lifeblood)"] = "Potestas Vitae (O Poder do Sangue Vital)",
				["The Grave’s Decay"] = "A Decomposição da Sepultura",
				["The Path of Ash"] = "A Trilha das Cinzas",
				["The Path of Bone"] = "A Trilha dos Ossos",
				["The Path of Haunting"] = "A Trilha da Assombração",
				["The Path of the Cenotaph"] = "A Trilha do Cenotáfio",
				["The Path of the Corpse in the Monster"] = "A Trilha do Cadáver no Monstro",
				["The Path of the Sepulchre"] = "A Trilha do Sepulcro",
				["The Vitreous Path"] = "A Trilha Vítrea",
				-- <<< VAMPIRE_ITEMS_END
				["ROAD"] = "CAMINHO",
				["The Path of the Bones"] = "A Trilha dos Ossos",
				["The Path of Caine"] = "A Trilha de Caim",
				["The Path of Cathari"] = "A Trilha dos Cátaros",
				["The Path of the Feral Heart"] = "A Trilha do Coração Selvagem",
				["The Path of Honorable Accord"] = "A Trilha do Acordo Honrado",
				["The Path of Lilith"] = "A Trilha de Lilith",
				["The Path of Metamorphosis"] = "A Trilha da Metamorfose",
				["The Path of Night"] = "A Trilha da Noite",
				["The Path of Paradox"] = "A Trilha do Paradoxo",
				["The Path of Power and the Inner Voice"] = "A Trilha do Poder e da Voz Interior",
				["The Path of Typhon"] = "A Trilha de Tífon",
				["The Path of the Hive"] = "A Trilha da Colmeia",
				["The Path of the Scorched Heart"] = "A Trilha do Coração Chamuscado",
				["The Path of Self-Focus"] = "A Trilha do Foco Interior",
				["The Path of Asakku"] = "A Trilha de Asakku",
				["The Path of Entelechy"] = "A Trilha da Entelequia",
				["The Path of Ecstasy"] = "A Trilha do Êxtase",
				["The Path of the Warrior"] = "A Trilha do Guerreiro",
				["Road of the Beast"] = "Caminho da Besta",
				["Road of Humanity"] = "Caminho da Humanidade",
				["Road of Kings"] = "Caminho dos Reis",
				["Road of Lilith"] = "Caminho de Lilith",
				["Road of Metamorphosis"] = "Caminho da Metamorfose",
				["Road of Sin"] = "Caminho do Pecado",
				["Road of Bones"] = "Caminho dos Ossos",
				["Road of Yasa"] = "Caminho de Yasa",
				["Road of Blood"] = "Caminho do Sangue",
				["Road of Paradox"] = "Caminho do Paradoxo",
				["The Code of Samiel"] = "O Código de Samiel",
				["Road of the Abyss"] = "Caminho do Abismo",
				["Road of Set"] = "Caminho de Set",
				["Path of the Archangel"] = "Trilha do Arcanjo",
				["Road of Angra Mainyu"] = "Caminho de Angra Mainyu",
				["Road of Ahura Mazda"] = "Caminho de Ahura Mazda",
				["Path of the Hunter"] = "Trilha do Caçador",
				["Path of Journeys"] = "Trilha das Jornadas",
				["Path of Liberation"] = "Trilha da Libertação",
				["Path of Christianity"] = "Trilha do Cristianismo",
				["Path of Life/Judaism"] = "Trilha da Vida/Judaísmo",
				["Path of the Prophet/Islam"] = "Trilha do Profeta/Islamismo",
				["Path of Breath"] = "Trilha do Sopro",
				["Path of Community"] = "Trilha da Comunidade",
				["Path of Illumination"] = "Trilha da Iluminação",
				["Path of Chivalry"] = "Trilha da Cavalaria",
				["Path of Devaraja"] = "Trilha de Devaraja",
				["Path of Daena"] = "Trilha de Daena",
				["Path of Thorns"] = "Trilha dos Espinhos",
				["Path of Veils"] = "Trilha dos Véus",
				["Path of Making"] = "Trilha da Criação",
				["Path of Pleasure"] = "Trilha do Prazer",
				["Path of the Devil"] = "Trilha do Diabo",
				["Path of Screams"] = "Trilha dos Gritos",
				["Path of Watchful Gods"] = "Trilha dos Deuses Vigilantes",
				["Normalcy"] = "Normalidade",
				["Resolve"] = "Determinação",
				["Silence"] = "Silêncio",
				["Seduction"] = "Sedução",
				["Menace"] = "Ameaça",
				["Devotion"] = "Devoção",
				["Tribulation"] = "Tribulação",
				["Inhumanity"] = "Desumanidade",
				["Darkness"] = "Escuridão",
				["Confidence"] = "Confiança",
				["Command"] = "Comando",
				["Justice"] = "Justiça",
				["Intellect"] = "Intelecto",
				["Balance"] = "Equilíbrio",
				["Otherworldly"] = "Sobrenatural",
				["Stoicism"] = "Estoicismo",
				["Rapture"] = "Arrebatamento",
				["Fury"] = "Fúria",
				["Holiness"] = "Santidade",
				["Carved and bleeding"] = "Talhado e sangrando",
				["Temptation"] = "Tentação",
				["Trust"] = "Confiabilidade",
				["Sacrifice"] = "Sacrifício",
				["Melancholia"] = "Melancolia",
				["Corruption"] = "Corrupção",
				["ARTIFACTS"] = "ARTEFATOS",
				["Artifacts"] = "Artefatos",
				["Ability Deficit"] = "Déficit de Habilidade",
				["Absent-Minded"] = "Distraído",
				["Acute Sense"] = "Sentido Aguçado",
				["Acute Senses (1)"] = "Sentidos Aguçados (1)",
				["Acute Senses (3)"] = "Sentidos Aguçados (3)",
				["Addicted to Vampire Blood"] = "Viciado em Sangue de Vampiro",
				["Addiction"] = "Vício",
				["Addictive Blood"] = "Sangue Viciante",
				["Additional Discipline"] = "Disciplina Adicional",
				["Advanced Tech (1)"] = "Tecnologia Avançada (1)",
				["Advanced Tech (2)"] = "Tecnologia Avançada (2)",
				["Advanced Tech (3)"] = "Tecnologia Avançada (3)",
				["Advanced Tech (4)"] = "Tecnologia Avançada (4)",
				["Advanced Tech (5)"] = "Tecnologia Avançada (5)",
				["Aging"] = "Envelhecimento",
				["Albino"] = "Albino",
				["Alcohol /Drug Tolerance (1)"] = "Tolerância a Álcool/Drogas (1)",
				["Alcohol /Drug Tolerance (2)"] = "Tolerância a Álcool/Drogas (2)",
				["Alien Perfection"] = "Perfeição Alienígena",
				["Ambidextrous"] = "Ambidestria",
				["Amnesia"] = "Amnésia",
				["Anachronism (1)"] = "Anacronismo (1)",
				["Anachronism (2)"] = "Anacronismo (2)",
				["Anachronism (3)"] = "Anacronismo (3)",
				["Ancestral Soil Dependence"] = "Dependência do Solo Ancestral",
				["Animal Magnetism"] = "Magnetismo Animal",
				["Animate Shadow"] = "Sombra Animada",
				["Anosmia"] = "Anosmia",
				["Antitoxin Blood"] = "Sangue Antitoxina",
				["Apostate (Dark Ages)"] = "Apóstata (Dark Ages)",
				["Apostate (Modern)"] = "Apóstata (Modern)",
				["Apprentice (1)"] = "Aprendiz (1)",
				["Apprentice (2)"] = "Aprendiz (2)",
				["Apprentice (3)"] = "Aprendiz (3)",
				["Apprentice (4)"] = "Aprendiz (4)",
				["Apprentice (5)"] = "Aprendiz (5)",
				["Arcane Curse (1)"] = "Maldição Arcana (1)",
				["Arcane Curse (2)"] = "Maldição Arcana (2)",
				["Arcane Curse (3)"] = "Maldição Arcana (3)",
				["Arcane Curse (4)"] = "Maldição Arcana (4)",
				["Arcane Curse (5)"] = "Maldição Arcana (5)",
				["Artificially Aged"] = "Envelhecido Artificialmente",
				["Artistically Gifted"] = "Talento Artístico",
				["Aura of Command"] = "Aura de Comando",
				["Aura of the Typhon"] = "Aura de Tifão",
				["Avatar Companion"] = "Companheiro do Avatar",
				["Backup Identity"] = "Identidade Reserva",
				["Bad Sight (1)"] = "Visão Ruim (1)",
				["Bad Sight (3)"] = "Visão Ruim (3)",
				["Banshee-in-Waiting"] = "Banshee em Espera",
				["Bardic Gift"] = "Dom Bárdico",
				["Beacon of the Unholy"] = "Farol do Profano",
				["Beast Within"] = "Besta Interior",
				["Bedeviled"] = "Atormentado por Demônios",
				["Benevolent Domitor"] = "Domitor Benevolente",
				["Berserker (Mage)"] = "Furioso (Mage)",
				["Berserker (Vampire)"] = "Furioso (Vampire)",
				["Bestial"] = "Bestial",
				["Betrayer's Mark"] = "Marca do Traidor",
				["Bigger Boys Came"] = "Vieram os Maiores",
				["Bigot"] = "Preconceituoso",
				["Bioluminescence"] = "Bioluminescência",
				["Bizarre Hunger (2)"] = "Fome Bizarra (2)",
				["Bizarre Hunger (3)"] = "Fome Bizarra (3)",
				["Bizarre Hunger (4)"] = "Fome Bizarra (4)",
				["Bizarre Hunger (5)"] = "Fome Bizarra (5)",
				["Black and White"] = "Preto e Branco",
				["Black Sheep"] = "Ovelha Negra",
				["Blacklisted (1)"] = "Na Lista Negra (1)",
				["Blacklisted (2)"] = "Na Lista Negra (2)",
				["Blacklisted (3)"] = "Na Lista Negra (3)",
				["Blacklisted (4)"] = "Na Lista Negra (4)",
				["Blacklisted (5)"] = "Na Lista Negra (5)",
				["Blasphemous Pact"] = "Pacto Blasfemo",
				["Blessed by St. Gustav"] = "Abençoado por São Gustavo",
				["Blind"] = "Cego",
				["Blood Feud"] = "Rixa de Sangue",
				["Blood Flaw Immunity"] = "Imunidade a Defeito de Sangue",
				["Blood Hunted (4)"] = "Caçado em Caçada de Sangue (4)",
				["Blood Hunted (6)"] = "Caçado em Caçada de Sangue (6)",
				["Blood Magick"] = "Mágika de Sangue",
				["Blood Weakness (4)"] = "Fraqueza do Sangue (4)",
				["Blood Weakness (7)"] = "Fraqueza do Sangue (7)",
				["Blooded by the Code"] = "Sangrado pelo Código",
				["Blood-Hungry Soul (2)"] = "Alma Faminta de Sangue (2)",
				["Blood-Hungry Soul (3)"] = "Alma Faminta de Sangue (3)",
				["Blood-Hungry Soul (5)"] = "Alma Faminta de Sangue (5)",
				["Blush of Health"] = "Rubor da Saúde",
				["Body Trail"] = "Rastro de Corpos",
				["Boon (1)"] = "Favor (1)",
				["Boon (2)"] = "Favor (2)",
				["Boon (3)"] = "Favor (3)",
				["Boon (4)"] = "Favor (4)",
				["Boon (5)"] = "Favor (5)",
				["Boon (6)"] = "Favor (6)",
				["Botched Presentation"] = "Apresentação Malfeita",
				["Bound (Mage)"] = "Vinculado (Mage)",
				["Bound (Vampire)"] = "Vinculado (Vampire)",
				["Bound to the Clan"] = "Vinculado ao Clã",
				["Brahmin"] = "Brâmane",
				["Branded (3)"] = "Marcado a Ferro (3)",
				["Branded (4)"] = "Marcado a Ferro (4)",
				["Branded (5)"] = "Marcado a Ferro (5)",
				["Brittle Body (2)"] = "Corpo Frágil (2)",
				["Brittle Body (4)"] = "Corpo Frágil (4)",
				["Broken Antitribu"] = "Antitribu Quebrado",
				["Broken Bond"] = "Vínculo Rompido",
				["Bruiser"] = "Brutamontes",
				["Bulimia"] = "Bulimia",
				["Bullyboy"] = "Capanga",
				["Burning Aura"] = "Aura Ardente",
				["Call of the Sea"] = "Chamado do Mar",
				["Calm Heart"] = "Coração Calmo",
				["Cannibal"] = "Canibal",
				["Cannot Embrace"] = "Incapaz de Abraçar",
				["Can't Cross Running Water"] = "Não Cruza Água Corrente",
				["Carrion Presence"] = "Presença de Carniça",
				["Cast No Reflection"] = "Não Reflete",
				["Cast No Shadow or Reflection (1)"] = "Sem Sombra nem Reflexo (1)",
				["Cast No Shadow or Reflection (2)"] = "Sem Sombra nem Reflexo (2)",
				["Cast-Iron Stomach"] = "Estômago de Ferro",
				["Catlike Balance"] = "Equilíbrio Felino",
				["Catspaw"] = "Testa de Ferro",
				["Celestial Affinity"] = "Afinidade Celestial",
				["Celestial Attunement"] = "Sintonia Celestial",
				["Chandala"] = "Chandala",
				["Chorus Trained (3)"] = "Treinado pelo Coro (3)",
				["Chorus Trained (5)"] = "Treinado pelo Coro (5)",
				["Chronic Depression"] = "Depressão Crônica",
				["Circumspect Avatar"] = "Avatar Circunspecto",
				["Clan Apostate"] = "Apóstata do Clã",
				["Clan Enmity"] = "Inimizade de Clã",
				["Clan Friendship"] = "Amizade de Clã",
				["Clan Weakness"] = "Fraqueza de Clã",
				["Clear-Sighted"] = "Vista Clara",
				["Cloak of the Seasons"] = "Manto das Estações",
				["Cloaked in Shadow"] = "Envolto em Sombra",
				["Cloistered"] = "Enclausurado",
				["Code of Honor"] = "Código de Honra",
				["Cold Breeze"] = "Brisa Fria",
				["Cold Read"] = "Leitura Fria",
				["Coldly Logical"] = "Friamente Lógico",
				["Combat Novice"] = "Novato em Combate",
				["Common Sense"] = "Bom Senso",
				["Compulsion"] = "Compulsão",
				["Compulsive Speech (1)"] = "Fala Compulsiva (1)",
				["Compulsive Speech (2)"] = "Fala Compulsiva (2)",
				["Computer Aptitude"] = "Aptidão com Computadores",
				["Concentration"] = "Concentração",
				["Conflicting Loyalties (1)"] = "Lealdades Conflitantes (1)",
				["Conflicting Loyalties (2)"] = "Lealdades Conflitantes (2)",
				["Conflicting Loyalties (3)"] = "Lealdades Conflitantes (3)",
				["Connoisseur"] = "Conhecedor",
				["Consanguineous Resistance"] = "Resistência Consanguínea",
				["Conspicuous Consumption"] = "Consumo Ostensivo",
				["Construct"] = "Construto",
				["Consumption"] = "Tísica",
				["Contagious"] = "Contagioso",
				["Controllable Night Sight"] = "Visão Noturna Controlável",
				["Court Favorite (1)"] = "Favorito da Corte (1)",
				["Court Favorite (2)"] = "Favorito da Corte (2)",
				["Court Favorite (3)"] = "Favorito da Corte (3)",
				["Court Favorite (4)"] = "Favorito da Corte (4)",
				["Court Favorite (5)"] = "Favorito da Corte (5)",
				["Credulous (3)"] = "Crédulo (3)",
				["Credulous (5)"] = "Crédulo (5)",
				["Critters"] = "Bichinhos",
				["Crucial Component (2)"] = "Componente Crucial (2)",
				["Crucial Component (3)"] = "Componente Crucial (3)",
				["Crucial Component (4)"] = "Componente Crucial (4)",
				["Crucial Component (5)"] = "Componente Crucial (5)",
				["Cultural Chameleon"] = "Camaleão Cultural",
				["Cultural Other (1)"] = "Estrangeiro Cultural (1)",
				["Cultural Other (2)"] = "Estrangeiro Cultural (2)",
				["Cultural Other (3)"] = "Estrangeiro Cultural (3)",
				["Cultural Other (4)"] = "Estrangeiro Cultural (4)",
				["Cultural Other (5)"] = "Estrangeiro Cultural (5)",
				["Curiosity"] = "Curiosidade",
				["Cursed (1)"] = "Amaldiçoado (1)",
				["Cursed (2)"] = "Amaldiçoado (2)",
				["Cursed (3)"] = "Amaldiçoado (3)",
				["Cursed (4)"] = "Amaldiçoado (4)",
				["Cursed (5)"] = "Amaldiçoado (5)",
				["Danger Sense"] = "Senso de Perigo",
				["Dangerous Secret (1)"] = "Segredo Perigoso (1)",
				["Dangerous Secret (2)"] = "Segredo Perigoso (2)",
				["Dangerous Secret (3)"] = "Segredo Perigoso (3)",
				["Dangerous Secret (4)"] = "Segredo Perigoso (4)",
				["Dangerous Secret (5)"] = "Segredo Perigoso (5)",
				["Daredevil"] = "Destemido",
				["Dark Aura"] = "Aura Sombria",
				["Dark Fate"] = "Destino Sombrio",
				["Dark Secret"] = "Segredo Sombrio",
				["Dark Triad"] = "Tríade Sombria",
				["Darksight"] = "Visão nas Trevas",
				["Dead Inside"] = "Morto por Dentro",
				["Dead Zone"] = "Zona Morta",
				["Deaf"] = "Surdo",
				["Death Grip"] = "Aperto Mortal",
				["Deathsight"] = "Visão da Morte",
				["Deathwalker"] = "Andarilho da Morte",
				["Debt of Gratitude (1)"] = "Dívida de Gratidão (1)",
				["Debt of Gratitude (3)"] = "Dívida de Gratidão (3)",
				["Debts (1)"] = "Dívidas (1)",
				["Debts (2)"] = "Dívidas (2)",
				["Debts (3)"] = "Dívidas (3)",
				["Debts (4)"] = "Dívidas (4)",
				["Debts (5)"] = "Dívidas (5)",
				["Deceptive Aura"] = "Aura Enganosa",
				["Deceptive Eating"] = "Comer Enganoso",
				["Deep Sleeper"] = "Sono Pesado",
				["Deformity"] = "Deformidade",
				["Degeneration (3)"] = "Degeneração (3)",
				["Degeneration (6)"] = "Degeneração (6)",
				["Degeneration (9)"] = "Degeneração (9)",
				["Demented Eidolon"] = "Eidolon Demente",
				["Demon-Hounded (1)"] = "Perseguido por Demônios (1)",
				["Demon-Hounded (2)"] = "Perseguido por Demônios (2)",
				["Demon-Hounded (3)"] = "Perseguido por Demônios (3)",
				["Demon-Hounded (4)"] = "Perseguido por Demônios (4)",
				["Demon-Hounded (5)"] = "Perseguido por Demônios (5)",
				["Demonic Patron"] = "Patrono Demoníaco",
				["Deranged (3)"] = "Perturbado (3)",
				["Deranged (5)"] = "Perturbado (5)",
				["Detached"] = "Desapegado",
				["Devil's Mark"] = "Marca do Diabo",
				["Diabolical Mentor"] = "Mentor Diabólico",
				["Discerning Palate"] = "Paladar Refinado",
				["Disciple of Lazarus / Japheth"] = "Discípulo de Lázaro/Jafé",
				["Discredited"] = "Desacreditado",
				["Disease Carrier"] = "Portador de Doença",
				["Disfigured"] = "Desfigurado",
				["Disgrace to the Blood"] = "Desgraça para o Sangue",
				["Distracting Aura"] = "Aura Perturbadora",
				["Dogmatic"] = "Dogmático",
				["Domitor Pariah"] = "Domitor Pária",
				["Double Agent"] = "Agente Duplo",
				["Dracon's Temperament"] = "Temperamento do Dracon",
				["Dreadful Mara"] = "Mara Terrível",
				["Driving Goal"] = "Meta Obsessiva",
				["Drug Resistance"] = "Resistência a Drogas",
				["Dual Affiliation"] = "Afiliação Dupla",
				["Dulled Bite"] = "Mordida Embotada",
				["Duplicitous"] = "Dissimulado",
				["Dynamic Personality"] = "Personalidade Dinâmica",
				["Early Riser"] = "Madrugador",
				["Easily Intoxicated"] = "Embriaga-se Fácil",
				["Eat Food"] = "Comer Alimentos",
				["Ecclesial Rank (1)"] = "Posto Eclesiástico (1)",
				["Ecclesial Rank (2)"] = "Posto Eclesiástico (2)",
				["Ecclesial Rank (3)"] = "Posto Eclesiástico (3)",
				["Echo Chamber"] = "Câmara de Eco",
				["Echoes (1)"] = "Ecos (1)",
				["Echoes (2)"] = "Ecos (2)",
				["Echoes (3)"] = "Ecos (3)",
				["Echoes (4)"] = "Ecos (4)",
				["Echoes (5)"] = "Ecos (5)",
				["Ecstatic"] = "Extático",
				["Ecumenist"] = "Ecumenista",
				["Eerie Presence"] = "Presença Sinistra",
				["Efficient Digestion"] = "Digestão Eficiente",
				["Eidetic Memory"] = "Memória Eidética",
				["Elysium Regular"] = "Frequentador do Elísio",
				["Embedded Agent"] = "Agente Infiltrado",
				["Embraced without the Cup"] = "Abraçado sem o Cálice",
				["Enchanting Feature"] = "Traço Encantador",
				["Enchanting Voice"] = "Voz Encantadora",
				["Enemy (1)"] = "Inimigo (1)",
				["Enemy (2)"] = "Inimigo (2)",
				["Enemy (3)"] = "Inimigo (3)",
				["Enemy (4)"] = "Inimigo (4)",
				["Enemy (5)"] = "Inimigo (5)",
				["Enemy Brood"] = "Ninhada Inimiga",
				["Entrepreneur"] = "Empreendedor",
				["Escaped Target"] = "Alvo Fugitivo",
				["Esoteric Discourse / Technobabbler"] = "Discurso Esotérico/Tecnotagarela",
				["Ethical Prohibition (2)"] = "Proibição Ética (2)",
				["Ethical Prohibition (4)"] = "Proibição Ética (4)",
				["Expendable"] = "Descartável",
				["Expert Driver"] = "Motorista Experiente",
				["Expiration Date"] = "Data de Validade",
				["Extra Sharp"] = "Extra Afiado",
				["Extreme Kink (3)"] = "Fetiche Extremo (3)",
				["Extreme Kink (4)"] = "Fetiche Extremo (4)",
				["Extreme Kink (5)"] = "Fetiche Extremo (5)",
				["Eyes of Shadow (1)"] = "Olhos de Sombra (1)",
				["Eyes of Shadow (2)"] = "Olhos de Sombra (2)",
				["Eyes of Shadow (3)"] = "Olhos de Sombra (3)",
				["Eyes of Shadow (4)"] = "Olhos de Sombra (4)",
				["Faceless"] = "Sem Rosto",
				["Fae Blood"] = "Sangue Feérico",
				["Faerie Affinity"] = "Afinidade Feérica",
				["Failure"] = "Fracasso",
				["Faithless"] = "Descrente",
				["False Reflection"] = "Reflexo Falso",
				["Family Issues (1)"] = "Problemas de Família (1)",
				["Family Issues (2)"] = "Problemas de Família (2)",
				["Family Issues (3)"] = "Problemas de Família (3)",
				["Family Support (1)"] = "Apoio da Família (1)",
				["Family Support (2)"] = "Apoio da Família (2)",
				["Family Support (3)"] = "Apoio da Família (3)",
				["Fangless"] = "Sem Presas",
				["Fatalist"] = "Fatalista",
				["Faulty Enhancements (2)"] = "Melhorias Defeituosas (2)",
				["Faulty Enhancements (3)"] = "Melhorias Defeituosas (3)",
				["Faulty Enhancements (4)"] = "Melhorias Defeituosas (4)",
				["Faulty Enhancements (5)"] = "Melhorias Defeituosas (5)",
				["Favor (1)"] = "Favor (1)",
				["Favor (2)"] = "Favor (2)",
				["Favor (3)"] = "Favor (3)",
				["Feral Mind"] = "Mente Feral",
				["Fifteenth Generation"] = "Décima Quinta Geração",
				["Fifth Degree"] = "Quinto Grau",
				["Finicky Palate"] = "Paladar Exigente",
				["Fist of God"] = "Punho de Deus",
				["Flashbacks"] = "Flashbacks",
				["Flawed Reality"] = "Realidade Falha",
				["Flesh Eater"] = "Comedor de Carne",
				["Flesh of the Corpse"] = "Carne do Cadáver",
				["Force of Spirit"] = "Força de Espírito",
				["Forked Tongue"] = "Língua Bífida",
				["Former Ghoul"] = "Ex-Carniçal",
				["Former Prince"] = "Ex-Príncipe",
				["Foul Blood"] = "Sangue Impuro",
				["Fourteenth Generation"] = "Décima Quarta Geração",
				["Friend of the Underground"] = "Amigo do Submundo",
				["Friendly Face"] = "Rosto Amigável",
				["Fugitive"] = "Fugitivo",
				["Fugue Instinct"] = "Instinto de Fuga",
				["Glowing Eyes"] = "Olhos Brilhantes",
				["Green Thumb"] = "Dedo Verde",
				["Gremlin (1)"] = "Gremlin (1)",
				["Gremlin (2)"] = "Gremlin (2)",
				["Gremlin (3)"] = "Gremlin (3)",
				["Gremlin (4)"] = "Gremlin (4)",
				["Gremlin (5)"] = "Gremlin (5)",
				["Grip of the Damned"] = "Aperto dos Condenados",
				["Groomed for Embrace"] = "Preparado para o Abraço",
				["Guardian Angel"] = "Anjo da Guarda",
				["Guilt-Wracked"] = "Consumido pela Culpa",
				["Gullible"] = "Ingênuo",
				["Half-Life"] = "Meia-Vida",
				["Hands of Daedalus"] = "Mãos de Dédalo",
				["Harbinger of the Abyss"] = "Arauto do Abismo",
				["Hard of Hearing"] = "Audição Ruim",
				["Harmless"] = "Inofensivo",
				["Hatred"] = "Ódio",
				["Haunted"] = "Assombrado",
				["Haven Affinity"] = "Afinidade com o Refúgio",
				["Healing Touch"] = "Toque Curativo",
				["Heart of Needles"] = "Coração de Agulhas",
				["Heartless"] = "Sem Coração",
				["Heavy Hands"] = "Mãos Pesadas",
				["Hemophiliac"] = "Hemofílico",
				["Heretical Sympathizer"] = "Simpatizante Herético",
				["Hero Worship"] = "Idolatria",
				["Hidden Diablerie"] = "Diablerie Oculta",
				["Hideaway /Safehouse (2)"] = "Esconderijo/Casa Segura (2)",
				["Hideaway /Safehouse (4)"] = "Esconderijo/Casa Segura (4)",
				["Hideaway /Safehouse (6)"] = "Esconderijo/Casa Segura (6)",
				["Hit List"] = "Lista de Alvos",
				["Hive-Minded (1)"] = "Mente Coletiva (1)",
				["Hive-Minded (2)"] = "Mente Coletiva (2)",
				["Holy Aura"] = "Aura Sagrada",
				["Horrific"] = "Horrendo",
				["Huge Size"] = "Tamanho Descomunal",
				["Hunted"] = "Caçado",
				["Hunted by the Hierophants"] = "Caçado pelos Hierofantes",
				["Hunted Like a Dog"] = "Caçado como um Cão",
				["Hyperflexible"] = "Hiperflexível",
				["Hyperfocus"] = "Hiperfoco",
				["Hypersensitivity"] = "Hipersensibilidade",
				["Icy"] = "Gélido",
				["Ignorance"] = "Ignorância",
				["Illiterate (1)"] = "Analfabeto (1)",
				["Illiterate (2)"] = "Analfabeto (2)",
				["Immortal Enemy (5)"] = "Inimigo Imortal (5)",
				["Immortal Enemy (6)"] = "Inimigo Imortal (6)",
				["Immortal Enemy (7)"] = "Inimigo Imortal (7)",
				["Immortal Enemy (8)"] = "Inimigo Imortal (8)",
				["Impaired Sense"] = "Sentido Prejudicado",
				["Impatient"] = "Impaciente",
				["Impediment (1)"] = "Impedimento (1)",
				["Impediment (2)"] = "Impedimento (2)",
				["Impediment (3)"] = "Impedimento (3)",
				["Impediment (4)"] = "Impedimento (4)",
				["Impediment (5)"] = "Impedimento (5)",
				["Impediment (6)"] = "Impedimento (6)",
				["Impossible Ritual"] = "Ritual Impossível",
				["Impressive Restraint"] = "Autocontrole Impressionante",
				["Inappropriate (1)"] = "Inadequado (1)",
				["Inappropriate (2)"] = "Inadequado (2)",
				["Inappropriate (3)"] = "Inadequado (3)",
				["Inappropriate (4)"] = "Inadequado (4)",
				["Inbred (1)"] = "Consanguíneo (1)",
				["Inbred (2)"] = "Consanguíneo (2)",
				["Inbred (3)"] = "Consanguíneo (3)",
				["Inbred (4)"] = "Consanguíneo (4)",
				["Inbred (5)"] = "Consanguíneo (5)",
				["Incantation"] = "Encantamento",
				["Incoherent"] = "Incoerente",
				["Incomplete Understanding"] = "Entendimento Incompleto",
				["Indelible (1)"] = "Indelével (1)",
				["Indelible (2)"] = "Indelével (2)",
				["Infamous Sire"] = "Senhor Infame",
				["Infamy (1)"] = "Infâmia (1)",
				["Infamy (2)"] = "Infâmia (2)",
				["Infamy (3)"] = "Infâmia (3)",
				["Infamy (4)"] = "Infâmia (4)",
				["Infamy (5)"] = "Infâmia (5)",
				["Infectious"] = "Infeccioso",
				["Infectious Bite"] = "Mordida Infecciosa",
				["Infertile Vitae"] = "Vitae Infértil",
				["Initiate of the Road"] = "Iniciado do Caminho",
				["Inner Knight"] = "Cavaleiro Interior",
				["Inner Strength"] = "Força Interior",
				["Innocent"] = "Inocente",
				["Inoffensive to Animals"] = "Inofensivo aos Animais",
				["Insane /Infamous Mentor"] = "Mentor Louco/Infame",
				["Insensate to Pain"] = "Insensível à Dor",
				["Instrument of God"] = "Instrumento de Deus",
				["Insubordinate"] = "Insubordinado",
				["Intemperate"] = "Intemperante",
				["Inter-Agency Liaison"] = "Ligação Interagências",
				["Introspection"] = "Introspecção",
				["Iron Will"] = "Vontade de Ferro",
				["Isolated Upbringing"] = "Criação Isolada",
				["Jack-of-All-Trades"] = "Faz-Tudo",
				["Jinx /Infernal Contraption (10)"] = "Azar/Engenhoca Infernal (10)",
				["Jinx /Infernal Contraption (2)"] = "Azar/Engenhoca Infernal (2)",
				["Jinx /Infernal Contraption (3)"] = "Azar/Engenhoca Infernal (3)",
				["Jinx /Infernal Contraption (4)"] = "Azar/Engenhoca Infernal (4)",
				["Jinx /Infernal Contraption (5)"] = "Azar/Engenhoca Infernal (5)",
				["Jinx /Infernal Contraption (6)"] = "Azar/Engenhoca Infernal (6)",
				["Jinx /Infernal Contraption (7)"] = "Azar/Engenhoca Infernal (7)",
				["Jinx /Infernal Contraption (8)"] = "Azar/Engenhoca Infernal (8)",
				["Jinx /Infernal Contraption (9)"] = "Azar/Engenhoca Infernal (9)",
				["Judge's Wisdom"] = "Sabedoria do Juiz",
				["Kashaph"] = "Kashaph",
				["Keys to the Library (1)"] = "Chaves da Biblioteca (1)",
				["Keys to the Library (2)"] = "Chaves da Biblioteca (2)",
				["Keys to the Library (3)"] = "Chaves da Biblioteca (3)",
				["Keys to the Library (4)"] = "Chaves da Biblioteca (4)",
				["Keys to the Library (5)"] = "Chaves da Biblioteca (5)",
				["King or Queen of Shadow"] = "Rei ou Rainha das Sombras",
				["Kiss of Death"] = "Beijo da Morte",
				["Kshatriya"] = "Kshatriya",
				["Lame"] = "Manco",
				["Laughingstock"] = "Motivo de Chacota",
				["Lawman's Friend"] = "Amigo da Lei",
				["Lazy"] = "Preguiçoso",
				["Legacy"] = "Legado",
				["Legendary Attributes"] = "Atributos Lendários",
				["Lesser Mark of the Beast"] = "Marca Menor da Besta",
				["Lifesaver"] = "Salva-Vidas",
				["Light Sleeper"] = "Sono Leve",
				["Lightning Calculator"] = "Calculadora Humana",
				["Light-Sensitive"] = "Sensível à Luz",
				["Lightweight"] = "Fraco para Bebida",
				["Lingua Franca"] = "Língua Franca",
				["Literacy"] = "Alfabetização",
				["Lizard Limbs"] = "Membros de Lagarto",
				["Loathsome Regnant"] = "Regente Repugnante",
				["Local Hero"] = "Herói Local",
				["Locked Vidare"] = "Vidare Trancado",
				["Long Fingers"] = "Dedos Longos",
				["Long-Term Planning"] = "Planejamento de Longo Prazo",
				["Loose Lips"] = "Boca Grande",
				["Lord of the Flies"] = "Senhor das Moscas",
				["Lord of the Night"] = "Senhor da Noite",
				["Lost Svadharma"] = "Svadharma Perdido",
				["Loyalty (Mage)"] = "Lealdade (Mage)",
				["Loyalty (Vampire)"] = "Lealdade (Vampire)",
				["Lucky"] = "Sortudo",
				["Lunacy"] = "Lunacia",
				["Mage Blood"] = "Sangue de Mago",
				["Magic Resistance"] = "Resistência à Magia",
				["Magical Signature"] = "Assinatura Mágica",
				["Manchurian Candidate"] = "Candidato Manchuriano",
				["Manifest Avatar"] = "Avatar Manifesto",
				["Mark of Favor"] = "Marca de Favor",
				["Marked for Death"] = "Marcado para Morrer",
				["Masquerade Breaker"] = "Quebrador da Máscara",
				["Master of Red Tape"] = "Mestre da Burocracia",
				["Master of the Masquerade"] = "Mestre da Máscara",
				["Mayfly Curse (10)"] = "Maldição da Efêmera (10)",
				["Mayfly Curse (5)"] = "Maldição da Efêmera (5)",
				["Mechanical Aptitude"] = "Aptidão Mecânica",
				["Medium"] = "Médium",
				["Member of the Pack"] = "Membro da Matilha",
				["Mental Lock"] = "Bloqueio Mental",
				["Methuselah's Thirst"] = "Sede do Matusalém",
				["Mistaken Identity"] = "Identidade Trocada",
				["Mole"] = "Infiltrado",
				["Monstrous"] = "Monstruoso",
				["Monstrous Maw"] = "Fauces Monstruosas",
				["Mortal Flashbacks"] = "Flashbacks Mortais",
				["Mortuario (2)"] = "Mortuario (2)",
				["Mortuario (4)"] = "Mortuario (4)",
				["Mr. Red Tape"] = "Sr. Burocracia",
				["Multi-Lingual"] = "Multilíngue",
				["Multiple Curses"] = "Maldições Múltiplas",
				["Mute"] = "Mudo",
				["Mute Devotion"] = "Devoção Muda",
				["Nameless"] = "Sem Nome",
				["Narc"] = "Dedo-Duro",
				["Natural Channel"] = "Canal Natural",
				["Natural Leader"] = "Líder Nato",
				["Natural Linguist"] = "Linguista Nato",
				["Natural Shapeshifter"] = "Metamorfo Natural",
				["Nephilim /Laham"] = "Nefilim/Laham",
				["New Arrival"] = "Recém-Chegado",
				["New Kid"] = "Novato",
				["Nightmares"] = "Pesadelos",
				["Nightsight"] = "Visão Noturna",
				["Nine Lives"] = "Sete Vidas",
				["Noble Blood"] = "Sangue Nobre",
				["Non-Standard Access"] = "Acesso Não Padrão",
				["Noted Messenger"] = "Mensageiro Notório",
				["Notoriety"] = "Notoriedade",
				["Oathbreaker"] = "Quebrador de Juramento",
				["Oathbreaker (Ravnos)"] = "Quebrador de Juramento (Ravnos)",
				["Obsession"] = "Obsessão",
				["Obvious Predator"] = "Predador Óbvio",
				["Occult Library (2)"] = "Biblioteca Oculta (2)",
				["Occult Library (4)"] = "Biblioteca Oculta (4)",
				["OCPD"] = "TPOC",
				["Odd Gait"] = "Andar Estranho",
				["Offensive to Animals"] = "Ofensivo aos Animais",
				["Officially Dead"] = "Oficialmente Morto",
				["Offline (1)"] = "Fora da Rede (1)",
				["Offline (3)"] = "Fora da Rede (3)",
				["Old Flame"] = "Antiga Paixão",
				["Old Pal"] = "Velho Amigo",
				["One Eye"] = "Um Olho",
				["Open Road"] = "Estrada Aberta",
				["Open Wound (2)"] = "Ferida Aberta (2)",
				["Open Wound (4)"] = "Ferida Aberta (4)",
				["Oracular Ability"] = "Habilidade Oracular",
				["Out of Phase"] = "Fora de Fase",
				["Outcast"] = "Proscrito",
				["Outside Haven"] = "Refúgio Externo",
				["Outsider"] = "Forasteiro",
				["Outspoken Heretic"] = "Herege Declarado",
				["Overconfident"] = "Autoconfiante Demais",
				["Overextended"] = "Sobrecarregado",
				["Overstimulated"] = "Superestimulado",
				["Pacifist"] = "Pacifista",
				["Pain Tolerance"] = "Tolerância à Dor",
				["Pale Aura"] = "Aura Pálida",
				["Paper Trail"] = "Rastro de Papel",
				["Paradise Lost"] = "Paraíso Perdido",
				["Paragon"] = "Paradigma",
				["Paranoid"] = "Paranoico",
				["Paranormal Link"] = "Elo Paranormal",
				["Paranormal Prohibition or Imperative (2)"] = "Proibição ou Imperativo Paranormal (2)",
				["Paranormal Prohibition or Imperative (3)"] = "Proibição ou Imperativo Paranormal (3)",
				["Paranormal Prohibition or Imperative (4)"] = "Proibição ou Imperativo Paranormal (4)",
				["Paranormal Prohibition or Imperative (5)"] = "Proibição ou Imperativo Paranormal (5)",
				["Paranormal Prohibition or Imperative (6)"] = "Proibição ou Imperativo Paranormal (6)",
				["Paranormal Prohibition or Imperative (7)"] = "Proibição ou Imperativo Paranormal (7)",
				["Paranormal Prohibition or Imperative (8)"] = "Proibição ou Imperativo Paranormal (8)",
				["Parasitic Infestation"] = "Infestação Parasitária",
				["Parlor Trick"] = "Truque de Salão",
				["Patagia"] = "Patágio",
				["Path Dabbler"] = "Diletante de Trilha",
				["Path Inept"] = "Inepto em Trilha",
				["Path Natural"] = "Talento Natural para Trilha",
				["Peacemaker"] = "Pacificador",
				["Perfect Liar"] = "Mentiroso Perfeito",
				["Permanent Fangs"] = "Presas Permanentes",
				["Permanent Paradox Flaw (2)"] = "Defeito de Paradoxo Permanente (2)",
				["Permanent Paradox Flaw (4)"] = "Defeito de Paradoxo Permanente (4)",
				["Permanent Paradox Flaw (6)"] = "Defeito de Paradoxo Permanente (6)",
				["Permanent Wound"] = "Ferida Permanente",
				["Personal Masquerade"] = "Máscara Pessoal",
				["Phobia"] = "Fobia",
				["Phylactery"] = "Filactério",
				["Physically Impressive"] = "Fisicamente Impressionante",
				["Piscine"] = "Piscino",
				["Pitiable"] = "Digno de Pena",
				["Plague of Demons"] = "Praga de Demônios",
				["Poison Resistance"] = "Resistência a Venenos",
				["Poisonous Blood"] = "Sangue Venenoso",
				["Poker Face"] = "Cara de Paisagem",
				["Postulant of Echidna"] = "Postulante de Equidna",
				["Powerful Ally (5)"] = "Aliado Poderoso (5)",
				["Powerful Ally (6)"] = "Aliado Poderoso (6)",
				["Powerful Ally (7)"] = "Aliado Poderoso (7)",
				["Powerful Ally (8)"] = "Aliado Poderoso (8)",
				["Precocious"] = "Precoce",
				["Prestige"] = "Prestígio",
				["Prestigious Mentor"] = "Mentor Prestigioso",
				["Prestigious Sire"] = "Senhor Prestigioso",
				["Prey Exclusion"] = "Exclusão de Presa",
				["Primal Marks"] = "Marcas Primais",
				["Primogen/Bishop Friendship"] = "Amizade com Primogênito/Bispo",
				["Privacy Obsession"] = "Obsessão por Privacidade",
				["Private Life"] = "Vida Reservada",
				["Prized Collection (1)"] = "Coleção Preciosa (1)",
				["Prized Collection (2)"] = "Coleção Preciosa (2)",
				["Prized Patch"] = "Emblema Cobiçado",
				["Probationary Member"] = "Membro em Estágio",
				["Probationary Sect Member"] = "Membro Probatório da Seita",
				["Problem with Authority"] = "Problema com Autoridade",
				["Profane Trappings"] = "Adereços Profanos",
				["Profiled Appearance"] = "Aparência Fichada",
				["Promethean Clay"] = "Barro Prometeico",
				["Prone to Quiet (4)"] = "Propenso ao Silêncio (4)",
				["Prone to Quiet (5)"] = "Propenso ao Silêncio (5)",
				["Property (2)"] = "Propriedade (2)",
				["Property (3)"] = "Propriedade (3)",
				["Property (4)"] = "Propriedade (4)",
				["Property (5)"] = "Propriedade (5)",
				["Prophetic Dreams"] = "Sonhos Proféticos",
				["Psi Focus (3)"] = "Foco Psíquico (3)",
				["Psi Focus (4)"] = "Foco Psíquico (4)",
				["Psi Focus (5)"] = "Foco Psíquico (5)",
				["Psychic Awareness"] = "Percepção Psíquica",
				["Psychic Leech"] = "Sanguessuga Psíquica",
				["Psychic Vampire"] = "Vampiro Psíquico",
				["PTSD (2)"] = "TEPT (2)",
				["PTSD (3)"] = "TEPT (3)",
				["PTSD (4)"] = "TEPT (4)",
				["PTSD (5)"] = "TEPT (5)",
				["Putrescent"] = "Putrefato",
				["Quartermaster"] = "Intendente",
				["Ragged Bite"] = "Mordida Irregular",
				["Rat in a Cage"] = "Rato na Gaiola",
				["Ravnos Jati"] = "Jati Ravnos",
				["Recruitment Target"] = "Alvo de Recrutamento",
				["Red List"] = "Lista Vermelha",
				["Refined Palate (1)"] = "Paladar Refinado (1)",
				["Refined Palate (2)"] = "Paladar Refinado (2)",
				["Refined Palate (3)"] = "Paladar Refinado (3)",
				["Regal Bearing"] = "Porte Régio",
				["Religious Hysteria"] = "Histeria Religiosa",
				["Rep"] = "Reputação",
				["Repelled by Crosses"] = "Repelido por Cruzes",
				["Repulsed by Garlic"] = "Repelido por Alho",
				["Repulsive Feature"] = "Traço Repulsivo",
				["Repulsive to Animals"] = "Repulsivo aos Animais",
				["Research Grant"] = "Bolsa de Pesquisa",
				["Revenant"] = "Revenante",
				["Revenant Disciplines"] = "Disciplinas de Revenante",
				["Revenant Weakness"] = "Fraqueza de Revenante",
				["Rising Star"] = "Estrela em Ascensão",
				["Ritual Dabbler"] = "Diletante em Rituais",
				["Ritual Ineptness"] = "Inépcia Ritual",
				["Ritual Sleeper"] = "Ritual Adormecido",
				["Rival House (1)"] = "Casa Rival (1)",
				["Rival House (2)"] = "Casa Rival (2)",
				["Rival House (3)"] = "Casa Rival (3)",
				["Rival House (4)"] = "Casa Rival (4)",
				["Rival House (5)"] = "Casa Rival (5)",
				["Rival Sires"] = "Senhores Rivais",
				["Rivalry (1)"] = "Rivalidade (1)",
				["Rivalry (2)"] = "Rivalidade (2)",
				["Rivalry (3)"] = "Rivalidade (3)",
				["Rivalry (4)"] = "Rivalidade (4)",
				["Rivalry (5)"] = "Rivalidade (5)",
				["Romantic Notions"] = "Ideias Românticas",
				["Rose-Colored Glasses"] = "Óculos Cor-de-Rosa",
				["Rotten Liar"] = "Péssimo Mentiroso",
				["Rugged Bad Looks"] = "Feiúra Marcante",
				["Sabbat Survivor"] = "Sobrevivente do Sabá",
				["Sanction"] = "Sanção",
				["Sanctioned Witch"] = "Bruxa Sancionada",
				["Sanctity"] = "Santidade",
				["Sanctuary"] = "Santuário",
				["Sanguine Incongruity"] = "Incongruência Sanguínea",
				["Scales (1)"] = "Escamas (1)",
				["Scales (2)"] = "Escamas (2)",
				["Scales (3)"] = "Escamas (3)",
				["Scent of the Other"] = "Cheiro do Outro",
				["Scholar of Enemies"] = "Estudioso dos Inimigos",
				["Scholar of Others"] = "Estudioso dos Outros",
				["Science Laboratory"] = "Laboratório Científico",
				["Scientific Mystic /Techgnosi"] = "Místico Científico/Tecgnose",
				["Secret Code Language"] = "Linguagem Cifrada",
				["Secret Society Member"] = "Membro de Sociedade Secreta",
				["Secret Stash (2)"] = "Reserva Secreta (2)",
				["Secret Stash (3)"] = "Reserva Secreta (3)",
				["Secret Stash (4)"] = "Reserva Secreta (4)",
				["Secret Stash (5)"] = "Reserva Secreta (5)",
				["Sect Enmity"] = "Inimizade de Seita",
				["Sectarian Ally"] = "Aliado Sectário",
				["Seizures"] = "Convulsões",
				["Self-Confident"] = "Autoconfiante",
				["Self-Preservation"] = "Autopreservação",
				["Sense Influence"] = "Sentir Influência",
				["Setite Initiate"] = "Iniciado Setita",
				["Shadow Scarred"] = "Marcado pelas Sombras",
				["Shadow Walker"] = "Andarilho das Sombras",
				["Shapechanger Kin"] = "Parente de Metamorfo",
				["Shattered Avatar"] = "Avatar Despedaçado",
				["Short"] = "Baixinho",
				["Short Fuse"] = "Pavio Curto",
				["Shy"] = "Tímido",
				["Siege Mentality"] = "Mentalidade de Cerco",
				["Sight Beyond Sight"] = "Visão Além da Visão",
				["Simply Waiting"] = "Apenas Esperando",
				["Sire's Resentment"] = "Ressentimento do Senhor",
				["Skald"] = "Escaldo",
				["Skin of Porcelain"] = "Pele de Porcelana",
				["Sleep Unseen"] = "Dormir Invisível",
				["Sleeping With the Enemy"] = "Dormindo com o Inimigo",
				["Slimy"] = "Viscoso",
				["Slow Healing"] = "Cura Lenta",
				["Slowed Degeneration"] = "Degeneração Retardada",
				["Smell of the Grave"] = "Cheiro de Túmulo",
				["Soapbox"] = "Palanque",
				["Socially Networked"] = "Bem Relacionado",
				["Soft-Hearted"] = "Coração Mole",
				["Spark of Life"] = "Faísca de Vida",
				["Spawning Pool (1)"] = "Poço de Desova (1)",
				["Spawning Pool (2)"] = "Poço de Desova (2)",
				["Spawning Pool (3)"] = "Poço de Desova (3)",
				["Special Responsibility"] = "Responsabilidade Especial",
				["Speech Impediment"] = "Problema de Fala",
				["Sphere Inept"] = "Inepto em Esfera",
				["Sphere Natural"] = "Talento Natural para Esfera",
				["Spirit Mentor"] = "Mentor Espiritual",
				["Squeamish (1)"] = "Impressionável (1)",
				["Squeamish (3)"] = "Impressionável (3)",
				["Stench"] = "Fedor",
				["Stereotype"] = "Estereótipo",
				["Stigmata (2)"] = "Estigmas (2)",
				["Stigmata (4)"] = "Estigmas (4)",
				["Stillness of Death"] = "Imobilidade da Morte",
				["Stitcher"] = "Costureiro",
				["Stone Tongue"] = "Língua de Pedra",
				["Stormwarden"] = "Guardião da Tempestade",
				["Stormwarden /Quantum Voyager (3)"] = "Guardião da Tempestade/Viajante Quântico (3)",
				["Stormwarden /Quantum Voyager (5)"] = "Guardião da Tempestade/Viajante Quântico (5)",
				["Strangeness"] = "Estranheza",
				["Strength of Psyche"] = "Força da Psique",
				["Stress Atavism"] = "Atavismo sob Estresse",
				["Stubborn Magic (1)"] = "Magia Teimosa (1)",
				["Stubborn Magic (4)"] = "Magia Teimosa (4)",
				["Sturdy"] = "Robusto",
				["Style Sleeper"] = "Estilo Adormecido",
				["Styx Baptism"] = "Batismo do Estige",
				["Subculture Insider"] = "Iniciado na Subcultura",
				["Sugar Daddy"] = "Patrocinador",
				["Supernatural Companion"] = "Companheiro Sobrenatural",
				["Sympathetic Mastery"] = "Maestria Simpática",
				["Sympathizer"] = "Simpatizante",
				["Taint of Corruption"] = "Mácula da Corrupção",
				["Territorial"] = "Territorial",
				["Thaumaturgically Inept"] = "Inepto em Taumaturgia",
				["The Bard's Tongue"] = "A Língua do Bardo",
				["The Company of Saints and Heroes"] = "A Companhia dos Santos e Heróis",
				["The Eighth"] = "O Oitavo",
				["The Flow of Ki"] = "O Fluxo do Ki",
				["The High Price"] = "O Preço Alto",
				["The Largest Maw"] = "As Maiores Fauces",
				["Thin Blood"] = "Sangue Fraco",
				["Thirst for Innocence"] = "Sede de Inocência",
				["Thirteenth Generation"] = "Décima Terceira Geração",
				["Thousand Meter Killer"] = "Matador de Mil Metros",
				["Throwback (1)"] = "Atavismo (1)",
				["Throwback (2)"] = "Atavismo (2)",
				["Throwback (3)"] = "Atavismo (3)",
				["Throwback (4)"] = "Atavismo (4)",
				["Throwback (5)"] = "Atavismo (5)",
				["Thugs"] = "Capangas",
				["Tic/Twitch"] = "Tique/Espasmo",
				["Ties"] = "Vínculos",
				["Time Sense"] = "Senso de Tempo",
				["Too Tough to Die"] = "Duro de Matar",
				["Tortured Artist"] = "Artista Atormentado",
				["Totemic Change"] = "Mudança Totêmica",
				["Touch of Frost"] = "Toque de Gelo",
				["Tough Hide"] = "Couro Grosso",
				["Tracker's Mark"] = "Marca do Rastreador",
				["Troublemaker"] = "Encrenqueiro",
				["True Celerity"] = "Celeridade Verdadeira",
				["True Love"] = "Amor Verdadeiro",
				["Tunnel Rat (1)"] = "Rato de Túnel (1)",
				["Tunnel Rat (2)"] = "Rato de Túnel (2)",
				["Tunnel Rat (3)"] = "Rato de Túnel (3)",
				["Tunnel Rat (4)"] = "Rato de Túnel (4)",
				["Tunnel Rat (5)"] = "Rato de Túnel (5)",
				["Turncoat"] = "Vira-Casaca",
				["Twin Link (4)"] = "Elo Gêmeo (4)",
				["Twin Link (6)"] = "Elo Gêmeo (6)",
				["Twin Souls"] = "Almas Gêmeas",
				["Twisted Apprenticeship"] = "Aprendizado Deturpado",
				["Umbral Affinity"] = "Afinidade Umbral",
				["Unaging"] = "Sem Idade",
				["Unblinking"] = "Sem Piscar",
				["Unbondable"] = "Invinculável",
				["Uncanny (1)"] = "Estranho (1)",
				["Uncanny (2)"] = "Estranho (2)",
				["Uncanny (3)"] = "Estranho (3)",
				["Uncanny (4)"] = "Estranho (4)",
				["Uncanny (5)"] = "Estranho (5)",
				["Unclean (2)"] = "Impuro (2)",
				["Unclean (3)"] = "Impuro (3)",
				["Uncommon Vitae Preference"] = "Preferência Incomum de Vitae",
				["Uncontrollable Night Sight"] = "Visão Noturna Incontrolável",
				["Unconvinced"] = "Não Convencido",
				["Unholy Stain"] = "Mácula Profana",
				["Unlucky (1)"] = "Azarado (1)",
				["Unlucky (2)"] = "Azarado (2)",
				["Unlucky (3)"] = "Azarado (3)",
				["Unlucky (4)"] = "Azarado (4)",
				["Unlucky (5)"] = "Azarado (5)",
				["Unmarked Antitribu (2)"] = "Antitribu Sem Marca (2)",
				["Unmarked Antitribu (5)"] = "Antitribu Sem Marca (5)",
				["Unobtrusive"] = "Discreto",
				["Unproven"] = "Não Provado",
				["Unsanctioned Embrace"] = "Abraço Não Sancionado",
				["Uppity"] = "Atrevido",
				["Useful Knowledge"] = "Conhecimento Útil",
				["Vaishya"] = "Vaishya",
				["Vanilla"] = "Comum",
				["Vassal of the Clan"] = "Vassalo do Clã",
				["Vengeful"] = "Vingativo",
				["Venomous Bite"] = "Mordida Venenosa",
				["Vicissitude Modifications (1)"] = "Modificações de Vicissitude (1)",
				["Vicissitude Modifications (2)"] = "Modificações de Vicissitude (2)",
				["Vicissitude Modifications (3)"] = "Modificações de Vicissitude (3)",
				["Vicissitude Modifications (4)"] = "Modificações de Vicissitude (4)",
				["Vicissitude Modifications (5)"] = "Modificações de Vicissitude (5)",
				["Vicissitude Modifications (6)"] = "Modificações de Vicissitude (6)",
				["Vicissitude Modifications (7)"] = "Modificações de Vicissitude (7)",
				["Victim of the Masquerade"] = "Vítima da Máscara",
				["Virulent Strike"] = "Golpe Virulento",
				["Vitae Mutation"] = "Mutação da Vitae",
				["Vitae Sink"] = "Sorvedouro de Vitae",
				["Vulgar"] = "Vulgar",
				["Vulnerability (1)"] = "Vulnerabilidade (1)",
				["Vulnerability (2)"] = "Vulnerabilidade (2)",
				["Vulnerability (3)"] = "Vulnerabilidade (3)",
				["Vulnerability (4)"] = "Vulnerabilidade (4)",
				["Vulnerability (5)"] = "Vulnerabilidade (5)",
				["Vulnerability (6)"] = "Vulnerabilidade (6)",
				["Vulnerability (7)"] = "Vulnerabilidade (7)",
				["Vulnerability to Silver"] = "Vulnerabilidade à Prata",
				["Ward"] = "Protegido",
				["Warrior's Heart"] = "Coração de Guerreiro",
				["Weak Blood"] = "Sangue Fraco",
				["Weak-Willed"] = "Vontade Fraca",
				["Well-Rounded"] = "Versátil",
				["Whimsy"] = "Capricho",
				["Witch-Hunted"] = "Caçado por Bruxas",
				["Without a Trace"] = "Sem Deixar Rastro",
				["Wolverine's Palate"] = "Paladar de Carcaju",
				["Clear Sighted (Hunter)"] = "Vista Clara (Caçador)",
				["Clear Sighted (Mage)"] = "Vista Clara (Mago)",
			};

			-- PT is local to this file, but text written by Lua lives on other tabs too - the
			-- health track here, the experience ledger on WoD20.9 (SPEC I10). One exported lookup
			-- keeps ONE map: a second copy in another file is exactly what V28 is written to
			-- catch, and it would drift the first time a term was retranslated.
			function translateSheetText(txt, lang)
				if lang == "pt" then return PT[txt] or txt; end;

				return txt;
			end;

			-- SPEC V21 / B.9: a form's getChildren() returns only its own declared children,
			-- so recursing from this Credits form reached this tab and nothing else. Walk UP
			-- to the sheet root first, then down over everything.
			local function rootOf(ctrl)
				local p = ctrl;

				while true do
					local up = p:getParent();
					if up == nil then return p; end;
					p = up;
				end;
			end;

			local function collect(node, out)
				local children = node:getChildren();

				for i = 1, #children, 1 do
					out[#out + 1] = children[i];
					collect(children[i], out);
				end;
			end;

			-- SPEC V24 / I22: a picker item is its canonical value read through TWO lenses, era
			-- first and language second - `Lure of Flames` shows as `Creatio Ignis (The Creation
			-- of Fire)` in the Dark Ages and then as its [pt] key from there. `values` are never
			-- touched: they are what the sheet SAVES, and that is the whole point (SPEC V194, B23).
			--
			-- ONE function because BOTH switches change what a row reads - applyLanguage calls it
			-- and applyTheme calls it, on the traversal each already runs. A dataLink of its own on
			-- `sheetTheme` would be a second era trigger, which is exactly the thing that falls out
			-- of step (SPEC T469).
			--
			-- Only the `cbo*` pickers: the language combo must not be rewritten with its own
			-- values ("en" / "pt").
			--
			-- The ERA axis is narrower than the language one, and SPEC B44 is why. ERA_NAME is
			-- keyed by the canonical value, and a value does not carry its sense: "Weather
			-- Control" is a Thaumaturgy path on the Vampire tab AND a hedge magic path on WoD20.7.
			-- Renaming every `cbo*` put "Potestas Tempestatum (Power Over Storms)" on the hedge
			-- wizard's row in the Dark Ages. Same lesson as B15 and T472, third time asked: the
			-- rule holds by SENSE, so the era is scoped to the rows that carry that sense
			-- (SPEC V197). The language axis stays sheet-wide - a translation is a translation.
			local VAMP_ROW = {
				clanDisc = true, disc = true, mainPath = true, secPath = true, ritual = true,
			};

			-- The FILTER reaches three of those five, and it is a table of its own rather than
			-- VAMP_ROW reused (SPEC V203). A Discipline is what the player BUYS: filtering its
			-- list by what he already owns would leave a new sheet nineteen empty combos. Same
			-- lesson as the era axis just above, asked a fourth time - the scope holds by SENSE,
			-- not by a prefix that happens to match.
			-- `hedgeRitual` joined them in the 112th round (SPEC I83g, V310f). It goes by ROOT and
			-- not into FILTER_NAME below, and the two tables are not interchangeable: all twelve
			-- picked ritual rows answer to ONE question - is the path on the sheet, at the level -
			-- while only the FIRST numina row is the Affinity Path. Scope by SENSE, the fifth time
			-- this file has had to say it. The five typed rows carry an `edt` and are never asked
			-- (SPEC V248c).
			local FILTER_ROW = {
				mainPath = true, secPath = true, ritual = true, hedgeRitual = true,
			};

			-- A SECOND filter table, keyed by CONTROL NAME instead of by root (SPEC I67, V272a).
			-- Two of them and not one, for the reason V203 has already charged four times: the
			-- scope holds by SENSE. `FILTER_ROW["numina"] = true` would filter all seventeen hedge
			-- rows, and only the FIRST is the Affinity Path (M20 Sorcerer p. 8). `cboHedgeAttr` is
			-- a singleton and rowRoot returns nil for it - it demands a trailing _<n> - so a table
			-- keyed by root could not reach it at all (SPEC I68).
			--
			-- `cbonumina_1` left in T874, with wave 4 of I113e: the Affinity Path row is a picker
			-- now and there is no combo of that name to filter. The RULE did not move an inch - it
			-- is still pickAllowed reading AFFINITY - only the door did: mfOpen asks it while it
			-- builds the pool, once per opening, for every row of every converted family (SPEC
			-- I113c, V199, V205, V354c). What is left here is the one combo wave 2 has not reached.
			local FILTER_NAME = {
				cboHedgeAttr = true,
			};

			local function rowRoot(nm)
				return string.match(string.sub(nm, 4), "^(%a+)_%d+$");
			end;

			local function isVampireRow(nm)
				local root = rowRoot(nm);

				return root ~= nil and VAMP_ROW[root] == true;
			end;

			local function isFilterRow(nm)
				local root = rowRoot(nm);

				return FILTER_NAME[nm] == true or (root ~= nil and FILTER_ROW[root] == true);
			end;

			-- Which PICKER_LIST key a combo reads (SPEC I27). The control name is "cbo" plus the
			-- field, so the key is the field with its row number taken off. Two spellings have to
			-- survive this: the templates instantiate name="cbo$(field)" and come out lowercase
			-- (cboritual_7 -> ritual), while the hand-named ones are capitalised (cboClan -> clan,
			-- cboSpeciality1 -> speciality) - hence the lower() on the first character, and the
			-- optional underscore in the pattern.
			--
			-- rowRoot above cannot serve here: it demands a trailing _<n> and returns nil for every
			-- SINGLETON picker (cboClan, cboFaith, cboNature, cboDemeanor).
			local function fieldRoot(nm)
				local f = string.gsub(string.sub(nm, 4), "_?%d+$", "");

				return string.lower(string.sub(f, 1, 1)) .. string.sub(f, 2);
			end;

			-- One transformed pair per LIST, not one per control (SPEC T491, B48). Twenty-five
			-- ritual combos read the same 284 authored items and each rebuilt them - eraName and
			-- the PT lookup, item by item - on every one of the three passes an opening makes.
			-- The pair is written to all twenty-five: setValues marshals per handle (SPEC R92c),
			-- so sharing the table costs the widgets nothing.
			--
			-- There is nothing to invalidate here, in the same sense as the level map itself
			-- (SPEC V205): the stamp names everything the answer depends on, so a stale entry is
			-- never SERVED - it is simply never found. discLevels() hands back a FRESH table each
			-- traversal, so the identity test is exactly "a new render started", and the map it
			-- was built under is held right here, which is why no later map can share its
			-- identity. `levels` starts at false rather than nil so a first caller passing nil
			-- still opens a generation of its own.
			-- `hedge` rides the same generation (SPEC I83f, V310e). It is the hedge PATH level map,
			-- and it is built HERE, inside the reset below, rather than handed down like `levels`:
			-- the identity test above is exactly "a new render started", so one build per reset is
			-- one build per render, which is what V205 asks. Built outside the reset it would be
			-- rebuilt once per CONTROL, and read from anywhere the reset cannot reach it would
			-- serve last render's levels with rdk exiting 0 and the gate green - B48's own lesson,
			-- through a third door.
			--
			-- Threading it as a fifth argument to pickerItems was the alternative and it is worse:
			-- the four callers that build `levels` would each have to build this one too, and two
			-- of them - applyLanguage and applyTheme - have no business knowing what a hedge path
			-- is.
			local memo = { stamp = nil, levels = false, hedge = nil, lists = {} };

			-- `levels` is the map discLevels() builds, handed in by the caller so it is built once
			-- per traversal and not once per item (SPEC V205). Callers that filter nothing may
			-- pass nil.
			-- ---- Q26: alphabetical order, in whichever language is on screen (SPEC I110) ------
			--
			-- GLOBAL and not local, and that is not style: WoD20.6 declares 99 locals at chunk
			-- scope against the 100 the gate allows and the 105 the compiler really has, so a
			-- local function plus a local table would be two of the one slot left (SPEC B77,
			-- B93, V325). popOpen and fontStep are global for the same reason.
			--
			-- `table.sort` compares BYTES. In [pt] that puts `Ácido` AFTER `Zebra`, because the
			-- first byte of a UTF-8 `Á` is 0xC3 and every ASCII letter is under 0x80 - the list
			-- would come out looking sorted until the first accent, and nothing would say so.
			-- So the comparison runs on a FOLDED key: ASCII-lowercased, accents stripped.
			--
			-- string.lower only touches ASCII, so the map carries BOTH cases: `Á` is not made
			-- into `á` on the way in.
			FOLD_MAP = {
				["á"] = "a", ["à"] = "a", ["â"] = "a", ["ã"] = "a", ["ä"] = "a",
				["é"] = "e", ["è"] = "e", ["ê"] = "e", ["ë"] = "e",
				["í"] = "i", ["ì"] = "i", ["î"] = "i", ["ï"] = "i",
				["ó"] = "o", ["ò"] = "o", ["ô"] = "o", ["õ"] = "o", ["ö"] = "o",
				["ú"] = "u", ["ù"] = "u", ["û"] = "u", ["ü"] = "u",
				["ç"] = "c", ["ñ"] = "n", ["ð"] = "d",
				["Á"] = "a", ["À"] = "a", ["Â"] = "a", ["Ã"] = "a", ["Ä"] = "a",
				["É"] = "e", ["È"] = "e", ["Ê"] = "e", ["Ë"] = "e",
				["Í"] = "i", ["Ì"] = "i", ["Î"] = "i", ["Ï"] = "i",
				["Ó"] = "o", ["Ò"] = "o", ["Ô"] = "o", ["Õ"] = "o", ["Ö"] = "o",
				["Ú"] = "u", ["Ù"] = "u", ["Û"] = "u", ["Ü"] = "u",
				["Ç"] = "c", ["Ñ"] = "n", ["Ð"] = "d",
			};

			-- ONE gsub over the two-byte run, not one gsub per map entry: the map has 50 keys and
			-- the list has 872 names, so per-entry would be 43600 passes for one dropdown.
			-- The class is the UTF-8 lead bytes of Latin-1 Supplement and Latin Extended-A; the
			-- curly apostrophe of `Boukephos’ Gateway` is a THREE-byte sequence and is left alone,
			-- which is fine - it sorts by its own byte, the same way every time (SPEC V195).
			function foldKey(s)
				if s == nil then return ""; end;

				return (string.gsub(string.lower(s), "[\194-\197][\128-\191]", function(c) return FOLD_MAP[c] or c; end));
			end;

			-- The two combos whose order IS the answer, named one by one (SPEC I110d). A wildcard
			-- here would sort cboGame into `Mage, Mortal, Vampire` and quietly break the roster
			-- V109 pins. The `cmb*` combos never reach this function at all - pickerItems opens
			-- with a `cbo` test - so they need no entry.
			PICKER_NOSORT = { cboGame = true, cboSheetTheme = true };

			-- Two lists are the SAME when they are the same length with the same bytes in the same
			-- order. It exists so a write that changes nothing never reaches the host (SPEC V122,
			-- B153): every such write rebuilds the closed combo's display and drops the text anchor
			-- the XML authored, and the language combo is centred for exactly one reason - nobody
			-- ever writes to it, it has no name and pickerItems returns on the first line (I144h).
			function sameList(a, b)
				if a == nil or b == nil then return false; end;
				if #a ~= #b then return false; end;

				for i = 1, #a, 1 do
					if a[i] ~= b[i] then return false; end;
				end;

				return true;
			end;

			-- The combos that author a text anchor in the XML **and** get their items rewritten by
			-- one of the two routines below. On a LANGUAGE switch the list really does change, so
			-- the write cannot be skipped the way sameList skips it on a theme walk (SPEC V202,
			-- B153) - and the host rebuilds the closed combo's display on that write and drops the
			-- anchor. So it is put back, right after.
			--
			-- The value is a LITERAL and not a read-back of c.horzTextAlign. That round-trip was
			-- tried twice and does not work: the getter maps to _obj_getProp(handle, "TextAlign")
			-- and there is not ONE read of it in any sheet in this repo - the SDK itself only ever
			-- WRITES the property, as a literal (SDK3/API/rrpgGUI.lua:349). The literal is safe
			-- because it cannot drift: V419 builds this same roster out of the XML and compares it
			-- name for name and value for value, so the day one of them authors `leading` the gate
			-- goes red instead of the screen going wrong. The language combo is NOT here and that
			-- is the control - it has no name, neither routine reaches it, and it was the only one
			-- of the three that never lost its anchor (SPEC I144h).
			PICKER_ANCHOR = { cboGame = "center", cboSheetTheme = "center",
							  cmbHealthLevels = "center", cmbSpecCost = "center", cmbBgCost = "center" };

			function keepAnchor(c, nm)
				local a = PICKER_ANCHOR[nm];

				if a == nil then return; end;

				-- MEASURED in Firecast 2026-09-04 (SPEC B153, 5th cut, the one that was instrumented
				-- instead of guessed): after setItems the property still READS the authored value -
				-- in the same tick, at +1ms and at +400ms - and the closed display is left-aligned
				-- anyway. The host rebuilt the text without looking at the stored anchor, and it only
				-- looks again when the property CHANGES, so writing `center` over `center` is a
				-- no-op to it. That is why four cuts that wrote the right value in four different
				-- places all did nothing. What repaints is a real change: away, then back.
				--
				-- DEFERRED with a positive interval (SPEC R44, B97): at +1ms the rebuilt text already
				-- exists, so the toggle lands on the control that is on screen, not on the one about
				-- to be replaced. `away` is derived from the roster value and never names the anchor
				-- itself: the value that STAYS is the authored one, and V419 compares it to the XML.
				setTimeout(function()
					local away = (a == "leading") and "center" or "leading";

					c.horzTextAlign = away;
					c.horzTextAlign = a;

					-- And a REPAINT, explicitly (SDK gui.Control:needRepaint, native "Repaint"). The
					-- toggle alone repainted cboSheetTheme and not cboGame, which is enabled="false" -
					-- and the user's own probe closed the gap: leave the tab and come back, and cboGame
					-- is centred, so the property is right and only the paint is stale. This is what
					-- the tab switch does, asked for directly; enabled (V109) and opacity (V241) are
					-- both fenced and neither is touched (SPEC B153, 6th cut).
					c:needRepaint();
				end, 1);
			end;

			local function pickerItems(c, lang, era, levels)
				local nm = c:getName();

				if nm == nil or string.sub(nm, 1, 3) ~= "cbo" then return; end;

				local filter = isFilterRow(nm) and levels ~= nil;

				-- The AUTHORED list, and it is a CONSTANT nobody writes (SPEC V201, I27). It used to be
				-- a per-handle snapshot of c.values, taken off the XML attribute; both branches of that
				-- ended at c.values, so the moment the attribute left the XML the snapshot would have
				-- caught an EMPTY list, once and forever, and every dropdown on the sheet would have
				-- come up blank with rdk exiting 0 and the gate green.
				--
				-- The `or c.values` tail is for the pickers that did NOT migrate - cboGame and
				-- cboSheetTheme on this form, and WoD20.3 - which still author their list inline.
				local vals = PICKER_LIST[fieldRoot(nm)] or c.values;

				if vals == nil then return; end;

				local vamp  = isVampireRow(nm);

				-- Which FIELD the control writes, and the two spellings do not share one rule
				-- (SPEC I68, V272c). A ROW carries its number - cbonumina_1 -> numina_1 - and
				-- dropping the "cbo" is the whole of it. A SINGLETON does not: string.sub on
				-- cboHedgeAttr hands back `HedgeAttr` with the capital still on it, sheet[field]
				-- comes out nil, and V200 lets go of the stored value without a symptom anywhere -
				-- no error, rdk exiting 0, gate green. fieldRoot lowercases that first letter,
				-- which is what it was written to do, and it cannot serve the rows in turn: it
				-- strips `_?%d+$` and would answer `numina` for all seventeen.
				-- Computed for EVERY combo, not only the filtered ones. It used to live inside
				-- `if filter then`, and that is the whole of B91: `current` below reads from
				-- `field`, so for the fourteen unfiltered roots it came out nil and V200's rule -
				-- the saved value stays in the list however illegal it has become - had no
				-- executor at all. There was no symptom while no list changed its TEXT, because
				-- the saved value had come from that very list and was in it anyway. Two rounds of
				-- renaming (Status, Totem, Grimaldi) woke it up and the sheet opened blank
				-- (SPEC V346a, B91).
				--
				-- `cboSpeciality<n>` is the NAMED spelling exception (SPEC V346d): the control has
				-- no underscore before the number and the field has one, so string.sub(nm, 4)
				-- answers `Speciality1`, sheet[field] comes out nil, and nothing anywhere says so.
				-- The field is built from fieldRoot plus the number, and the number is matched
				-- with `_?%d+$` so both spellings land on it.
				local num = string.match(nm, "_?(%d+)$");
				local field = (num ~= nil) and (fieldRoot(nm) .. "_" .. num) or fieldRoot(nm);

				-- What the ROW is holding right now. It stays in the list however illegal it has
				-- become (SPEC V200): lowering a Discipline does not clear a row (SPEC V187), and
				-- a comboBox whose value is not among its values renders BLANK, so dropping it
				-- would make the sheet look like it had thrown the data away. It leaves the list
				-- the moment the player picks something else - then it is no longer the value.
				local current = (field ~= nil and sheet ~= nil) and sheet[field] or nil;

				-- The stamp names EVERYTHING the answer depends on (SPEC V205, V272b). The
				-- affiliation joined it in the 94th round with the two filters of I67/I68: without
				-- it, switching affiliation leaves lang and era untouched, the stamp holds, and
				-- every combo is served the list built under the OLD affiliation - the filters
				-- born broken with rdk exiting 0 and the gate green. This is the same memo B48
				-- caught serving a stale list one lesson ago.
				local stamp = tostring(lang) .. "|" .. tostring(era) .. "|" .. tostring(sheet ~= nil and sheet.hedgeAffiliation or "");

				if memo.stamp ~= stamp or memo.levels ~= levels then
					memo.stamp  = stamp;
					memo.levels = levels;
					memo.hedge  = hedgePathLevels();
					memo.lists  = {};
				end;

				-- The key names what the answer depends on and nothing else. The row's own value
				-- only changes the list when the filter would otherwise DROP it (SPEC V200), and
				-- that is the rare case, so it is asked once per control instead of keyed blindly:
				-- keying on the value itself would give twenty-five filled ritual rows twenty-five
				-- keys and share nothing. mainPath is keyed by the CONTROL because its answer
				-- really is per row - one main path per blood sorcery (SPEC V181) means row 1
				-- offers what row 2 must not, so those five never share and must not appear to.
				--
				-- FILTER_NAME is on the same footing and for the same reason (SPEC I67, T605):
				-- cbonumina_1 is the Affinity Path and answers to the affiliation, while
				-- cbonumina_2..17 answer to nothing - yet fieldRoot hands all seventeen the root
				-- `numina`. Sharing one key would let whichever rendered FIRST decide: row 1 first
				-- and the other sixteen inherit its pruned list, rows 2..17 first and row 1 is
				-- served the whole one with its filter silently doing nothing. Neither shows an
				-- error and both survive rdk (SPEC B48, V272).
				local root = fieldRoot(nm);

				-- The era axis of the road roster (SPEC I99c2, user Q10). Two themes read `medieval` and
				-- two read `modern` - the SAME pairing eraName() uses, and it is asked by NAME so that a
				-- fifth theme has to come here and decide instead of defaulting into one of them.
				local roadEra = (era == "Dark Ages" or era == "Classical Age") and "medieval" or "modern";
				-- `odd` means exactly one thing: the value this row is holding will NOT be among
				-- the items the loop below builds. That happens THREE ways, not one (SPEC V346c) -
				-- the filter would drop it, the road era would drop it, or the authored list
				-- simply does not carry it any more, which is what a renamed item looks like from
				-- here (SPEC B91). It belongs in the memo key in all three, and for the same
				-- reason: two controls of the same root holding DIFFERENT orphans cannot share one
				-- list (SPEC B48, V272). Keying only the road case was the old shape, and cboRoad
				-- is a singleton, so that key never distinguished anything.
				--
				-- pickAllowed is asked only under `filter`: the unfiltered callers pass levels=nil
				-- and it is that argument the filter reads.
				local odd = "";

				if current ~= nil and current ~= "" then
					if filter and not pickAllowed(field, current, levels, memo.hedge) then
						odd = current;
					elseif root == "road" and current ~= "Path of Humanity"
					       and ROAD_ERA ~= nil and ROAD_ERA[current] ~= roadEra then
						odd = current;
					else
						local inList = false;

						for k = 1, #vals, 1 do
							if vals[k] == current then inList = true; break; end;
						end;

						if not inList then odd = current; end;
					end;
				end;

				local per  = (root == "mainPath") and nm or root;
				local key  = (FILTER_NAME[nm] == true and nm or per) .. "|" .. odd;
				local hit  = memo.lists[key];
				local shown = hit ~= nil and hit.shown or nil;
				local kept  = hit ~= nil and hit.kept or nil;

				if hit == nil then
					shown = {};
					kept  = {};

					for k = 1, #vals, 1 do
						local raw = vals[k];
						local ok  = true;

						-- The empty item always passes (SPEC V15), and so does the row's own value.
						if filter and raw ~= "" and raw ~= current then
							ok = pickAllowed(field, raw, levels, memo.hedge);
						elseif root == "road" and raw ~= current then
							-- The DEFAULT road shows in all four eras: an era that dropped it
							-- would leave the field holding a value its own list does not offer (SPEC I99c3).
							ok = (raw == "Path of Humanity") or (ROAD_ERA ~= nil and ROAD_ERA[raw] == roadEra);
						end;

						if ok then
							local v = raw;

							if vamp then v = eraName(v, era); end;
							if lang == "pt" then v = PT[v] or v; end;

							kept[#kept + 1]   = raw;
							shown[#shown + 1] = v;
						end;
					end;

					-- SPEC C turned into code: a value outside the list BECOMES an item of the
					-- list (SPEC V200, V346b). ONE place, after the loop and inside this branch,
					-- so there is no second path that appends and no way for the two to disagree
					-- (SPEC V135, B38). The loop already keeps `current` whenever it is in `vals`
					-- - both the filter and the road era test let `raw == current` through - so
					-- what reaches here is the orphan case and nothing else: the day a list
					-- changes an item's TEXT, the saved value stops being in `vals` and a comboBox
					-- whose value is not among its values renders BLANK, which is the sheet
					-- looking like it threw the player's data away (SPEC B91).
					if current ~= nil and current ~= "" then
						local has = false;

						for k = 1, #kept, 1 do
							if kept[k] == current then has = true; break; end;
						end;

						if (not has) then
							local v = current;

							if vamp then v = eraName(v, era); end;
							if lang == "pt" then v = PT[v] or v; end;

							kept[#kept + 1]   = current;
							shown[#shown + 1] = v;
						end;
					end;

					-- Q26: out in the alphabetical order of what is READ, in either language, and
					-- never in the order the constant happens to be authored in. Both halves of the
					-- pair move by ONE permutation, so item N still shows value N (SPEC V202) and
					-- V24 is untouched - what the sheet SAVES is still the canonical EN.
					--
					-- LAST inside this branch on purpose: the orphan of V200/V346b is appended just
					-- above, and sorting before it would leave the one item the player can actually
					-- see stuck at the bottom, out of order (SPEC I110a).
					--
					-- The empty item does not take part - it is not a name, it is the blank row, and
					-- V15 pins it at index 1 (SPEC I110b).
					if PICKER_NOSORT[nm] ~= true then
						local first = 1;

						if kept[1] == "" then first = 2; end;

						local ord = {};

						-- the fold is paid ONCE per item and not once per comparison: sorting 872
						-- names is ~8600 comparisons, and folding inside the comparator would run
						-- the gsub every one of them.
						for i = first, #shown, 1 do ord[#ord + 1] = { k = foldKey(shown[i]), i = i }; end;

						table.sort(ord, function(a, b) return a.k < b.k; end);

						local k2 = {};
						local s2 = {};

						if first == 2 then k2[1] = kept[1]; s2[1] = shown[1]; end;

						for i = 1, #ord, 1 do
							k2[#k2 + 1] = kept[ord[i].i];
							s2[#s2 + 1] = shown[ord[i].i];
						end;

						kept  = k2;
						shown = s2;
					end;

					memo.lists[key] = { kept = kept, shown = shown };
				end;

				-- Written as one aligned pair (SPEC V202): same length, same order, so item N
				-- still shows value N. The filter only ever REMOVES a row of the pair - it never
				-- rewrites a value, which is what leaves SPEC V24 untouched.
				--
				-- BOTH are written on every pass, unconditionally. It used to be `if filter then`,
				-- which was right only while the XML authored values=: the eight unfiltered pickers
				-- got their values from the attribute and needed items alone. With the list coming
				-- from PICKER_LIST (SPEC I27) that attribute is gone, so a guarded write would leave
				-- them showing every entry and STORING none.
				--
				-- The host rebuilds the CLOSED combo's display when it receives a new items list, and
				-- that rebuild drops the text anchor the XML authored. Measured in Firecast 2026-09-04
				-- (SPEC B153, T948, I144h): cboGame and cboSheetTheme lose it, the OPEN list stays
				-- centred, and the language combo never does - it has no name, this function returns
				-- on its first line, and nothing here ever writes to it. Two rules come out of that.
				-- GUARDED, and this is the whole of the theme case (SPEC B153, V122). cboGame and
				-- cboSheetTheme did NOT migrate to PICKER_LIST, so `vals` came out of c.values and
				-- `kept` goes straight back in byte for byte - on a theme walk BOTH writes below were
				-- silent, and each one still made the host rebuild the closed display and drop the
				-- anchor the XML authored. On a language walk only `items` really moves. Guarding
				-- them separately is what makes these two behave like the language combo, which stays
				-- centred because nothing ever writes to it at all (SPEC I144h).
				if not sameList(c.values, kept) then c.values = kept; end;
				if not sameList(c.items, shown) then c.items = shown; end;

				-- The SELECTION comes back once the LIST exists, and that is a WINDOW and not lost data
				-- (SPEC V424, B150). The host binds field -> control when the control is CREATED, and a
				-- combo that authors no items= has ZERO of them at that instant - the list moved to
				-- PICKER_LIST (SPEC I27) and arrives later, on applyLanguage's walk at LOAD (SPEC B151).
				-- A selection has nowhere to land in an empty list, so it is simply dropped, and nothing
				-- put it back: `current` above is READ from sheet[field] for the orphan rule of V200 and
				-- was never written back. The screen is indistinguishable from data that was thrown away,
				-- which is how the same report sent two rounds hunting a paint bug (SPEC B146).
				--
				-- GUARDED, like cmbMfRevenant (WoD20th.lfm:4968): an unconditional write to .value would
				-- fire onChange on every theme walk and every language walk. It is written HERE and not in
				-- renderHedgePickers because this is the one place that already knows `current` and already
				-- writes the pair - a second site would leave the language walk without it (SPEC V135).
				if current ~= nil and current ~= "" and c.value ~= current then c.value = current; end;

				-- LAST, after the value write above: the host rebuilds the closed display on that write
				-- too, so an anchor put back any earlier is undone again (SPEC B153, V424).
				keepAnchor(c, nm);
			end;

			-- Re-filter the pickers of one Vampire area after the Disciplines moved (SPEC I25).
			-- Reached from the dataLinks on WoD20.13 and WoD20.14, which watch the nineteen Discipline
			-- rows and their dots - what the answer actually READS.
			--
			-- The map is built ONCE here and handed to every item (SPEC V205). Controls come from
			-- xpFind walking the root, the one finder this sheet has (SPEC V143): `form.<name>`
			-- does not cross an <import>, and this function lives in WoD20.6 while the combos live
			-- two files away.
			-- Re-filter the hedge pickers of WoD20.7 (SPEC I67, I68, V272d, I83i). Reached from the
			-- dataLink that watches `hedgeAffiliation` and from the one that watches the seventeen
			-- numina rows AND their eighty-five dots - what the two answers actually READ - and it
			-- is renderVampPickers with fourteen names instead of forty-nine.
			--
			-- FOURTEEN since the 112th round: the twelve picked ritual rows joined the Affinity
			-- Path and the casting attribute here rather than in a renderer of their own. One
			-- function because it is one tab and one xpFind walk; the cost of a numina dot also
			-- refreshing two combos that do not depend on it is two setValues, and the cost of a
			-- second renderer is a second name list to keep in step with FILTER_ROW.
			--
			-- It lives here beside its sister and for the same reason: the combos are drawn in
			-- WoD20.7, `form.<name>` does not cross an <import>, and xpFind is the one finder this
			-- sheet has (SPEC V143, B9).
			--
			-- `levels` is still built and handed in, though none of these read it: filter is
			-- `isFilterRow(nm) and levels ~= nil`, so passing nil would turn every filter OFF and
			-- leave the round doing nothing at all with rdk exiting 0. The hedge path map is NOT
			-- built here - it rides the picker memo's own generation (SPEC I83f).
			function renderHedgePickers(from)
				wodTrace("renderHedgePickers");
				if sheet == nil or from == nil then return; end;

				-- ONE name since T874, and the list is the same list FILTER_NAME holds - that is
				-- what V272d welds, and it is why the two shrank together. Wave 4 turned the
				-- Affinity Path row and the twelve picked ritual rows into pickers, so neither
				-- `cbonumina_1` nor `cbohedgeRitual_*` exists to be re-filtered: their filter is
				-- asked where the POOL is built now, inside mfOpen, once per opening (SPEC I113c,
				-- V199, V205). What is left is the casting attribute, whose wave has not run.
				local names  = { cboHedgeAttr = true };

				local found  = {};
				local lang   = sheet.language or "en";
				local era    = currentTheme();
				local levels = discLevels();

				xpFind(rootOf(from), names, found);

				for nm, c in pairs(found) do
					pickerItems(c, lang, era, levels);
				end;
			end;

			-- Show the picker or the typed twin, one of each pair at a time (SPEC I71, V274, user
			-- 2026-08-23). The storyteller who runs homebrew needs an affiliation or a clan the
			-- books never printed, and a dropdown cannot offer what nobody wrote down.
			--
			-- ONE function for BOTH pairs, and the two flags are read together: it is reached from
			-- two dataLinks and from two tab events, and either entry has to leave all four
			-- controls agreeing with the two flags - a renderer that only fixed "its" pair would
			-- leave the other one wherever the last event left it.
			--
			-- The flags are fail-closed: nil is not true, so a sheet saved before this round opens
			-- with the pickers showing (SPEC V80, V89, V175).
			--
			-- `visible` AND `enabled`, on the same two lines, for the five of them (SPEC V274d).
			-- Writing only `visible` leaves an invisible control taking Tab focus, which is the bug
			-- that shows up in no screenshot - the same pairing cboClan is authored with.
			--
			-- FIVE since T954, not four: the clanFamily side is a TRIO - two picker buttons over
			-- one field and the typed twin - and the storyteller's flag swaps BOTH buttons for the
			-- edit, never one of them (SPEC I145e, V274d amended, V426).
			--
			-- xpFind from the ROOT and not the tab: the two groups live in WoD20.7 and WoD20.11,
			-- two tabs apart, and `form.<name>` does not cross an <import> (SPEC V143, B9).
			function renderManualEntry(from)
				wodTrace("renderManualEntry");
				if sheet == nil or from == nil then return; end;

				local names = {
					dynhedgeAffiliation = true, edtHedgeAffiliation = true,
					dynclan = true, dynfamily = true, edtClanFamily = true,
				};

				local found = {};

				xpFind(rootOf(from), names, found);

				-- Which of the five is on show. The picker is the DEFAULT side of each group, so
				-- it reads the flag negated and the typed twin reads it straight.
				local on = {
					dynhedgeAffiliation = sheet.stManualAffiliation ~= true,
					edtHedgeAffiliation = sheet.stManualAffiliation == true,
					dynclan             = sheet.stManualClanFamily ~= true,
					dynfamily           = sheet.stManualClanFamily ~= true,
					edtClanFamily       = sheet.stManualClanFamily == true,
				};

				for nm, show in pairs(on) do
					local c = found[nm];

					if c ~= nil then
						c.visible = show;
						c.enabled = show;
					end;
				end;
			end;

			-- A combo of VALUE - cmbHealthLevels, cmbSpecCost, cmbBgCost - translates its own
			-- items, and pickerItems will not do it: that one opens with a cbo* test and returns
			-- (SPEC I52, V257, B57). The .lang keys these strings carry answer to the HOST's
			-- language; this answers to the sheet's own `language` field, which is the one the
			-- player switches, and the two are different axes rather than two copies.
			--
			-- BY PREFIX, never by name (SPEC V257a). Naming cmbSpecCost would have fixed today's
			-- combo and left the next one born mute with nothing measuring it - which is exactly
			-- the shape B57 arrived in.
			--
			-- `values` is READ and never written (SPEC V257b, V22): the item is what the player
			-- sees and the value is what the NDB keeps, so a language switch cannot touch stored
			-- data and running this twice lands in the same place. A numeral has no key and falls
			-- through `or v`, so the two price combos cross this without a map entry each.
			--
			-- Declared here rather than at the top of the script on purpose: PT and the traversal
			-- both live above it, and a local called from above its own declaration compiles to a
			-- GETGLOBAL that finds nil (SPEC V223, B51).
			-- The one cmb* whose item is NOT its value, declared rather than guessed at: the
			-- dominator's generation shows `4th` and stores `4`, and maxDiscLevel reads that
			-- number, so the pair cannot be collapsed without repricing every saved sheet
			-- (SPEC I30, V2). Its items are authored once and translated by nobody - `4th` is
			-- `4th` in both languages - so skipping it costs nothing.
			--
			-- This is a declared EXCEPTION, not a nominal selector: the branch below still
			-- reaches every cmb* by prefix, and the gate holds the list to BOTH directions -
			-- a cmb* whose items differ and is not named here turns red, and a name here whose
			-- items match turns red as a dead exception (SPEC V257c). A combo added later can
			-- neither be born mute nor sit in this list unnoticed.
			local CMB_DISPLAY_MAP = { cmbDominatorGen = true };

			local function cmbItems(c, lang)
				local nm = c:getName();

				if nm == nil or string.sub(nm, 1, 3) ~= "cmb" then return; end;
				if CMB_DISPLAY_MAP[nm] then return; end;

				local vals = c.values;

				if vals == nil then return; end;

				local shown = {};

				for i = 1, #vals, 1 do
					local v = vals[i];

					shown[i] = (lang == "pt") and (PT[v] or v) or v;
				end;

				-- Same two rules pickerItems carries, for the same reason and the same three
				-- symptoms (SPEC B153, V202, V419): never write a list that did not move, and put
				-- the anchor back when it did. cmbHealthLevels, cmbSpecCost and cmbBgCost author
				-- horzTextAlign="center" and their items ARE translated, so a language switch
				-- rewrites them and the host drops the anchor - the same defect as cboGame, on a
				-- tab nobody had looked at yet.
				if not sameList(c.items, shown) then c.items = shown; end;

				keepAnchor(c, nm);
			end;

			-- SPEC V22 / B.8: the authored English is snapshotted once and never overwritten.
			-- The old routine translated label.text in place with no original kept, which is
			-- why it could not switch back and why a second run did nothing.
			local originalText = {};

			local function applyLanguage(lang, from)
				local root = rootOf(from);
				local all = {root};
				collect(root, all);

				-- Built once for the whole traversal, not once per picker (SPEC V205). This walk
				-- is also what filters the vampire lists on LOAD and on a language switch, so the
				-- filter needs no third trigger of its own.
				local levels = discLevels();

				for i = 1, #all, 1 do
					local c = all[i];
					local cls = c:getClassName();

					-- Controls whose text belongs to Lua are named `dyn*` and are skipped here.
					-- SPEC V31 made the same call for the numina description block: this routine
					-- snapshots the authored English ONCE, so a control whose text is rewritten
					-- later (the health track changing length) would be restored to a stale
					-- label. Whoever writes the text owns both languages for it.
					local dynName = c:getName();
					local isDyn = dynName ~= nil and string.sub(dynName, 1, 3) == "dyn";

					if isDyn then
						-- nothing: renderHealthTrack keeps this one in the right language
					elseif cls == "label" or cls == "checkBox" or cls == "button" then
						local h = c.handle;

						if originalText[h] == nil then
							originalText[h] = c.text or "";
						end;

						local orig = originalText[h];

						if orig ~= "" then
							if lang == "pt" then
								c.text = PT[orig] or orig;
							else
								c.text = orig;
							end;
						end;
					elseif cls == "comboBox" then
						pickerItems(c, lang, currentTheme(), levels);
						cmbItems(c, lang);
					end;
				end;
			end;
			-- ---- Era abilities (SPEC I7 / V70 / V72 / V74 / V76, rewritten in the 21st) -
			--
			-- A row on the Main tab is a SLOT: the era says which ability sits there, and the
			-- five dots are rebound to THAT ability's field before they are repainted. A name
			-- owns one field and shares it with nothing (SPEC V74), so switching era can no
			-- longer show one trait's dots under another trait's name (SPEC B24 / B23).
			--
			-- The labels are named dyn* so the language traversal above leaves them alone
			-- (SPEC V31): whoever writes a text owns both languages for it, and here even the
			-- English depends on the era, so it is resolved before translating.
			--
			-- Runs on load and on BOTH switches, era and language (SPEC V72).
			--
			-- What it renders depends on exactly two things, so it remembers the pair and
			-- returns when neither moved (SPEC V124). Both dataLinks below carry a
			-- defaultValue, so BOTH fire when the sheet opens - this ran twice over the whole
			-- control tree, rebinding a hundred and fifty dots each time, before the sheet was
			-- even on screen (SPEC B34).
			local abilityMemo = nil;

			local function renderAbilityLabels(lang, from)
				local era = currentTheme();
				local memo = era .. "|" .. tostring(lang);

				if abilityMemo == memo then return; end;

				abilityMemo = memo;

				local lists = ERA_ABILITIES[era];

				-- field -> name, rebuilt here rather than at load: this file is a tab, and the
				-- root form's tables are not guaranteed to be in place while it parses.
				local nameOf = {};

				for n, f in pairs(ABILITY_FIELD) do nameOf[f] = n; end;

				local function show(c, text)
					local want = text;

					if lang == "pt" then want = PT[text] or text; end;

					if c.text ~= want then c.text = want; end;
				end;

				local root = rootOf(from);
				local all = {root};

				collect(root, all);

				-- Rebinding 150 dots writes 150 fields, and every one of those writes lands on
				-- the experience guard. Left alone that is 300 walks of the ledger before the
				-- sheet is even on screen - twice over, since the language and the theme link
				-- both fire on load - which is what froze it (SPEC B30 / V107). None of these
				-- writes changes a rating, so there is nothing here for the guard to weigh and
				-- nothing to re-render once the batch is done.
				xpQuiet = true;

				for i = 1, #all, 1 do
					local c = all[i];
					local cls = c:getClassName();
					local nm = c:getName();

					if nm == nil then
						-- unnamed control: nothing here addresses it
					elseif cls == "label" and string.sub(nm, 1, 7) == "dynAbil" then
						local col, num = string.match(string.sub(nm, 8), "^(%a+)(%d+)$");

						if col ~= nil and lists[col] ~= nil then
							local name = lists[col][tonumber(num)];

							if name ~= nil then show(c, name); end;
						end;
					elseif cls == "imageCheckBox" and string.sub(nm, 1, 4) == "abil" then
						local col, num, dot = string.match(string.sub(nm, 5), "^(%a+)(%d+)_(%d+)$");

						if col ~= nil and lists[col] ~= nil then
							local name = lists[col][tonumber(num)];
							local field = nil;

							if name ~= nil then field = ABILITY_FIELD[name]; end;

							if field ~= nil then
								-- Rebinding does not repaint by itself, so the state is reloaded
								-- in the same step - otherwise the dot would keep showing what
								-- the PREVIOUS field had (SPEC V76).
								--
								-- Both writes are compared first (SPEC V122). A bound dot writes
								-- its field through the NDB, so putting back the value that is
								-- already there wakes every link watching that trait for nothing.
								-- Most eras differ from the authored list by a couple of rows;
								-- this makes the other twenty-eight cost nothing.
								local full = field .. "_" .. dot;

								if c.field ~= full then c.field = full; end;

								if sheet ~= nil then
									local want = sheet[full] == true;

									if c.checked ~= want then c.checked = want; end;
								end;
							end;
						end;
					elseif cls == "label" and string.sub(nm, 1, 5) == "dynRo" then
						-- The Combat mirror. Its rows are fixed fields except the ranged one,
						-- which is Firearms or Archery depending on the era (SPEC V74).
						local f = string.sub(nm, 6);
						local text = nil;

						if f == "ranged" then
							text = eraRangedName();
						else
							text = nameOf[f];
						end;

						if text == nil then
							-- An attribute row: no era ever renames it, so the authored English
							-- stands, snapshotted once like the translation does (SPEC V22).
							local h = c.handle;

							if originalText[h] == nil then originalText[h] = c.text or ""; end;

							text = originalText[h];
						end;

						if text ~= "" then show(c, text); end;
					elseif string.sub(nm, 1, 11) == "dynAbilRow_" then
						-- The ELSE the two branches above do not have, and it is not cosmetic (SPEC
						-- I123c, V371). Both are `if name ~= nil then`, so a named slot the era does
						-- NOT fill keeps the previous era's label AND keeps its dots bound to the
						-- previous era's field: the player sees a trait this era has not got, and the
						-- points he puts on it land on the old one. It slept while all four eras had
						-- ten a column; I123 gives knowledges thirteen in two eras and twelve in two.
						--
						-- The row layout is what moves, not the label: hiding the label alone would
						-- leave five live dots on an unnamed line.
						local col, num = string.match(string.sub(nm, 12), "^(%a+)_(%d+)$");

						if col ~= nil and lists[col] ~= nil then
							local want = lists[col][tonumber(num)] ~= nil;

							if c.visible ~= want then c.visible = want; end;
						end;
					elseif string.sub(nm, 1, 12) == "dynAbilFill_" then
						-- The typed twin of that slot, authored on the SAME top: exactly one of the
						-- pair shows, so the column closes on the same fifteen rows in every era and
						-- the era that is a name short pays for it with a row the player fills in,
						-- never with a name borrowed from another book (SPEC I123b, V371b, Q34). Two
						-- tops instead of one would leave a 25px hole in the era that hides the upper
						-- row - the same hidden pair MeritPicked already runs (SPEC V354a).
						local col, num = string.match(string.sub(nm, 13), "^(%a+)_(%d+)$");

						if col ~= nil and lists[col] ~= nil then
							local want = lists[col][tonumber(num)] == nil;

							if c.visible ~= want then c.visible = want; end;
						end;
					end;
				end;

				xpQuiet = false;
			end;

			-- ---- Theme (SPEC I5/I6, 14th round) --------------------------------------
			--
			-- The sheet ships three looks. `Modern` is the sheet exactly as authored; the two
			-- Victorian variants repaint it at runtime. Every property involved has a setter
			-- (SPEC R22), so there is no second .lfm per theme and no field but `sheetTheme`.
			--
			-- Modern deliberately has NO palette below (SPEC V54): going back restores the
			-- snapshot taken before the first repaint, so the "modern" look cannot drift from
			-- what the XML declares. Same reason the translation keeps the authored English
			-- instead of a second table (SPEC V22).
			--
			-- Colour keys are the literals AUTHORED in the nine .lfm files, spelled as they
			-- appear there. The gate greps color=/strokeColor=/fontColor= out of those files
			-- and asserts every literal is a key here (SPEC V53) - a new box painted with a
			-- new colour turns the gate red instead of sitting black in the parchment.
			local THEMES = {
				-- The night the table actually plays in: cool charcoal, graphite rules, one
				-- crimson accent. Named `Modern` since the 16th round, when the palette-less
				-- baseline that used to hold that name was dropped (SPEC C, 16th round).
				["Modern Nights"] = {
					form       = "dark",
					fontFamily = "Segoe UI",
					fill = {
						["black"]     = "#12141A",    -- charcoal, blue cast
						["#0A0A0A"]  = "#0A0B10",    -- charcoal, one step under the box
						["DimGray"]   = "#2A2F3A",
						["#00000000"] = "#00000000",  -- health mark box: the cut-out IS the effect (SPEC V48)
						-- The one-second flash on a newly sorted ritual row (SPEC I47, V253). Alpha,
						-- so the name reads through it, and the period's own accent rather than one
						-- loose colour outside the theme (SPEC V53).
						["#55FFFFFF"] = "#55A83232",  -- crimson, the era's dot art
					},
					stroke = {
						["white"]   = "#3A4150",      -- cold graphite
						["#FFFFFF"] = "#3A4150",
						["black"]   = "#3A4150",
						["#00000000"] = "#00000000",  -- tab-strip floor: the band keeps its fill and loses its 3px outline -- SPEC I74b, V287
					},
					font = {
						["white"] = "#E6E8EC",
						["#C2A14D"] = "#A83232",   -- era accent: the open sub-tab wears it (SPEC I78c, V305d)
					},
					-- Plugin-absolute, NOT the relative form the XML uses. The rdk resolves
					-- src=/checkedImage= at COMPILE time - the generated Lua reads
					-- setImageChecked("/WoD20th/images/prime_on.png") - so a relative
					-- path handed to a setter at runtime resolves to nothing and the art
					-- silently fails to load (SPEC B20 / V60 / R26).
					dotOn  = "/WoD20th/images/prime_on_crimson.png",
					dotOff = "/WoD20th/images/prime_off_slate.png",
					paper  = "/WoD20th/images/paper_present.png",

					-- The tenth and eleventh keys, and they arrive together in the 118th round: the
					-- era gets a frame, and it is drawn FOR the cut corner below rather than in
					-- spite of it (SPEC I90a).
					--
					-- The crimson is the one the palette already carries as the era's accent, the
					-- one the open sub-tab wears (font["#C2A14D"] above). Not a new value: a frame
					-- that introduced a colour of its own would be the loose colour V53 exists to
					-- refuse, and the plate's gold is the VICTORIAN key - the plate says shape,
					-- not colour (user 2026-08-26).
					ornament = "#A83232",
					ornStyle = "corrente",

					-- The twelfth key, OPTIONAL like the two above, and the only one that is
					-- geometry rather than colour (SPEC I89a). ONE table and not two scalars: "a
					-- type with no radius" and "a radius with no type" are then not representable
					-- at all, so there is no invariant left to write - the opposite of what
					-- ornStyle forced V313a to do.
					--
					-- A straight 45 degree cut takes LESS corner than the concave arc of the same
					-- radius, so 18 gives back the weight the bevel loses (user 2026-08-26). The
					-- other three periods declare nothing and fall to the corner the XML authored,
					-- which is the ONLY way back out (SPEC V316c).
					--
					-- The 117th round forbade an `ornament` beside this key outright (V316b). The
					-- 118th REVOKES that and puts AGREEMENT in its place (SPEC V317a): what was
					-- wrong was never the pairing, it was a CONCAVE drawing over a cut corner -
					-- the filigree and the filete arc at ORN_BOXR + ORN_IN = 23 and would cross
					-- the outline they decorate. `corrente` is drawn for the bevel, so the rule is
					-- that the style and the corner AGREE, which also catches the direction the
					-- old ban let through: a bevelled era asking for `filete`.
					boxCorner = { type = "bevel", radius = 18 },
				},

				-- Was "Victorian Era (Dark)" until the 15th round and "Victorian Era" until T957 (SPEC I146b): the suffix went with the
				-- light variant it existed to distinguish; the palette itself is untouched.
				["Victorian Age"] = {
					form       = "dark",
					fontFamily = "Georgia",
					fill = {
						["black"]     = "#1C1310",    -- mahogany
						["#0A0A0A"]  = "#120C0A",    -- mahogany, one step under the box
						["DimGray"]   = "#6B4F2A",
						["#00000000"] = "#00000000",
						["#55FFFFFF"] = "#55C2A14D",  -- aged gold
					},
					stroke = {
						["white"]   = "#C2A14D",      -- aged gold
						["#FFFFFF"] = "#C2A14D",
						["black"]   = "#C2A14D",
						["#00000000"] = "#00000000",
					},
					font = {
						["white"] = "#E8DCC0",        -- cream
						["#C2A14D"] = "#D8BC72",   -- era accent: the open sub-tab wears it (SPEC I78c, V305d)
					},
					dotOn  = "/WoD20th/images/prime_on_gold.png",
					dotOff = "/WoD20th/images/prime_off_gold.png",
					paper  = "/WoD20th/images/paper_victorian.png",
					ornament = "#D8BC72",   -- brighter than the 3px outline, so the filigree reads as applied metal
				},

				-- Europe around 1300: stone and oak by candlelight, now with a silvered rule. The
				-- outline is polished silver over an interior darker than the other three periods
				-- (18th round), and the text is the colour of a tallow candle rather than white.
				["Dark Ages"] = {
					form       = "dark",
					fontFamily = "Book Antiqua",
					fill = {
						["black"]     = "#0E0B09",    -- soot, darker than the candle-lit oak it was
						["#0A0A0A"]  = "#070605",    -- soot, one step under the box
						["DimGray"]   = "#5A4A32",
						["#00000000"] = "#00000000",
						["#55FFFFFF"] = "#55C0C6CC",  -- polished silver
					},
					stroke = {
						["white"]   = "#C0C6CC",      -- polished silver
						["#FFFFFF"] = "#C0C6CC",
						["black"]   = "#C0C6CC",
						["#00000000"] = "#00000000",
					},
					font = {
						["white"] = "#E3D8BE",        -- candle on vellum
						["#C2A14D"] = "#C0C6CC",   -- era accent: the open sub-tab wears it (SPEC I78c, V305d)
					},
					dotOn  = "/WoD20th/images/prime_on_silver.png",
					dotOff = "/WoD20th/images/prime_off_silver.png",
					paper  = "/WoD20th/images/paper_dark_ages.png",

					-- The era's OWN silver, and not a brighter one (user 2026-08-26). The Victorian
					-- key above is lighter than its outline so the filigree reads as applied metal;
					-- here rule and frame are meant to be one family, which is what austerity means
					-- in a drawing whose whole argument is that there is only one line (SPEC I87a).
					ornament = "#C0C6CC",

					-- The tenth key, and it is OPTIONAL like the ninth: an era that declares no
					-- style falls to the default, which is the filigree of I72 - so the Victorian
					-- palette gains nothing and changes nothing. A style with no `ornament` beside
					-- it cannot exist: the colour is what hides the path (V277), so the style would
					-- be a dead key nobody could see was dead (SPEC I87a, V313a).
					ornStyle = "filete",
				},

				-- Rome around 200 BC - the Republic, not the Empire, so laurel gold and a
				-- single note of Tyrian purple rather than gilt everywhere. Times New Roman is
				-- literally a Roman face. The gold dots are the Victorian ones: gold is gold,
				-- and a second pair of near-identical art would earn nothing.
				["Classical Age"] = {
					form       = "dark",
					fontFamily = "Times New Roman",
					fill = {
						["black"]     = "#171218",    -- basalt, faint purple cast
						["#0A0A0A"]  = "#0E0B0F",    -- basalt, one step under the box
						["DimGray"]   = "#6B2A4A",    -- Tyrian purple on the avatar frame
						["#00000000"] = "#00000000",
						["#55FFFFFF"] = "#55BFA05A",  -- laurel gold
					},
					stroke = {
						["white"]   = "#BFA05A",      -- laurel gold
						["#FFFFFF"] = "#BFA05A",
						["black"]   = "#BFA05A",
						["#00000000"] = "#00000000",
					},
					font = {
						["white"] = "#F0EAD9",        -- ivory
						["#C2A14D"] = "#BFA05A",   -- era accent: the open sub-tab wears it (SPEC I78c, V305d)
					},
					dotOn  = "/WoD20th/images/prime_on_gold.png",
					dotOff = "/WoD20th/images/prime_off_gold.png",
					paper  = "/WoD20th/images/paper_classical.png",

					-- The 122nd round: of the four palettes this was the ONLY one declaring no ornament,
					-- so V277 hid its path and the box has been bare outline since the system was written.
					-- The colour is the laurel gold the palette ALREADY carries in stroke["white"] - the
					-- Dark Ages choice, where rule and frame are one family (SPEC I87a), not the Victorian
					-- one a step lighter than the outline. The plate draws in a lighter gold and that does
					-- NOT come in: the plate says FORM, never colour, so no loose colour is born (SPEC V53).
					--
					-- The 122nd round declared NO boxCorner here, on the grounds that V317a gives the cut
					-- corner only to a style whose branch receives ornPath radius - and the merlon did not.
					-- The 123rd round REVOKES that by taking the radius (below), which is what turns the
					-- same derivation around: the gate now DEMANDS the key instead of forbidding it.
					ornament = "#BFA05A",
					ornStyle = "corona-muralis",

					-- The thirteenth key, and the second that is GEOMETRY rather than colour
					-- (SPEC I98a). The eleven labels on the top bar are authored top="0"
					-- height="20" inside a 30px button, so vertTextAlign="center" centres them in
					-- the TOP TWENTY and the word sits 5px above the button's middle. I88d sized
					-- that box to leave the foot clear for the marker, and in the other three eras
					-- it still is: they draw ON the foot and they do NOT declare this key.
					--
					-- Here the foot is EMPTY - the column is lateral (I95b) - so the word can have
					-- the middle back, and nothing is under it to collide with. Written the way
					-- boxCorner is, through paint() with the AUTHORED value as the way back out
					-- (V316c, V327b): drop the `or` and the eleven stay at 30 forever once this
					-- era has been on, with rdk -l exiting 0 and the gate green.
					tabLabelHeight = 30,

					-- The 123rd round gave the era a STEP; the 124th shrinks it to 1 (user 2026-08-27, after
					-- seeing 14 on screen). At radius 1 the step is a single pixel, so the outline reads as a
					-- square 90 degree corner while the SELECTOR stays alive: sectionBox refuses radius <= 0
					-- and a true zero would drop all 73 boxes out of the ornament walk (SPEC V316a, V278).
					-- It also pulls the rule back to radius + ORN_MUR_IN = 16, inside the 20px margin of I73
					-- and the 23 the filigree respects - the overshoot I97b measured at 29 is gone with it.
					--
					-- innerLine is no longer a guess: the user read the step on screen in the 124th round,
					-- which closes what R117 left open and answers T741 with its first branch. It is the
					-- straight partner of innerRound the way bevel is the straight partner of round, and
					-- V317a is satisfied by AGREEMENT and not by a list: the branch above now takes the
					-- radius, which is exactly what the gate derives "wants a cut corner" from.
					--
					-- The avatar comes along for free and needs no task of its own: sectionBox answers true
					-- on its FIRST value for both roster names, and the corner write sits under that first
					-- value (SPEC I96b, V324e).
					boxCorner = { type = "innerLine", radius = 1 },
				},
			};

			local NAMED_COLORS = {
				["black"]   = "#FF000000",
				["white"]   = "#FFFFFFFF",
				["dimgray"] = "#FF696969",
			};

			-- The colour getter is native, so it may hand back the literal the XML authored
			-- ("black") or a normalised value ("#FF000000"), and that cannot be settled without
			-- running Firecast (SPEC R25). Canonicalise both sides instead - the palette keys
			-- once at load, every snapshot on the way in - and the question stops mattering.
			local function normColor(v)
				if v == nil then return nil; end;
				if type(v) == "number" then return string.format("#%08X", v); end;

				local s = tostring(v);
				local named = NAMED_COLORS[string.lower(s)];

				if named ~= nil then return named; end;
				if string.sub(s, 1, 1) ~= "#" then return string.upper(s); end;

				local hex = string.upper(string.sub(s, 2));

				if string.len(hex) == 6 then hex = "FF" .. hex; end;

				return "#" .. hex;
			end;

			for _, t in pairs(THEMES) do
				local fill, stroke, font = {}, {}, {};

				for k, v in pairs(t.fill)   do fill[normColor(k)]   = v; end;
				for k, v in pairs(t.stroke) do stroke[normColor(k)] = v; end;
				for k, v in pairs(t.font)   do font[normColor(k)]   = v; end;

				t.fill, t.stroke, t.font = fill, stroke, font;
			end;

			-- Same trick as normColor, same reason: the XML authors "images/prime_on.png" and the
			-- rdk rewrites it to "/WoD20th/images/prime_on.png" at compile time (SPEC R26),
			-- so the getter and the source never agree. Compare the file name and both forms
			-- answer the same question.
			local function normPath(v)
				if v == nil then return nil; end;

				local s = string.lower(tostring(v));
				local cut = string.find(s, "/[^/]*$");

				if cut ~= nil then s = string.sub(s, cut + 1); end;

				return s;
			end;

			-- The dot the first column always shows. It is a plain <image>, not an imageCheckBox
			-- (templates `Attribute` in WoD20.1 and `VirtueMirror` in WoD20.3): an attribute's first dot
			-- cannot be switched off, so nothing owns it as a field.
			--
			-- SPEC B22: the 14th round left it out on the grounds that fixed art was not the
			-- theme's business. Wrong - it is the SAME art a ticked box shows, so it stayed white
			-- next to gold, crimson and blood dots in every period.
			local DOT_ON  = "prime_on.png";
			local DOT_OFF = "prime_off.png";

			-- One outline width for every period (SPEC V67), and it lives here rather than four
			-- times inside THEMES: the requirement is that they MATCH, and four copies that
			-- happen to agree today are four copies that can drift tomorrow. Same reasoning as
			-- HEALTH_MARKS on the root form. 3 was the widest of the four when they were merged.
			--
			-- There is deliberately no corner constant to match it (SPEC V66): the corner detail
			-- - cornerType="innerRound" over a radius - is authored in the XML on 53 boxes, so it
			-- already holds in every period. Three rectangles author a radius of their own (the
			-- health mark box and the avatar frame); a single theme-wide radius would flatten
			-- them. The theme simply stops writing radii.
			local THEME_STROKE = 3;

			local THEME_SHAPES = { rectangle = true };
			local THEME_TEXTS  = { label = true, checkBox = true, button = true, comboBox = true,
			                       edit = true, textEditor = true, radioButton = true };

			-- The ledger: handle -> { prop = the value the XML authored }. A property appears
			-- here only once it has actually been REPAINTED, and it holds the original, not
			-- the current value.
			--
			-- SPEC B21: the first version snapshotted every control at load and wrote the
			-- snapshot straight back under Modern, on the assumption that writing a value
			-- equal to the one just read costs nothing. It does not. Every font setter runs
			-- _gui_prepareForFontColorChange / ...FontFamilyChange BEFORE it writes, and that
			-- step detaches the control from the font it inherits through the form theme. The
			-- whole sheet came back wrong on all three options, Modern included - the one that
			-- had not changed in the XML at all.
			--
			-- Hence the two rules below: never write a property without a mapped target
			-- (SPEC V61), and put back only what was actually painted (SPEC V62). A sheet
			-- opened on Modern that was never repainted writes nothing whatsoever.
			local themePainted = {};

			-- What the control had before any theme touched it. Switching straight from one
			-- Victorian variant to the other must map from the ORIGINAL colour, not from the
			-- one the previous variant left behind.
			local function authored(c, prop)
				local rec = themePainted[c.handle];

				if rec ~= nil and rec[prop] ~= nil then return rec[prop]; end;

				return c[prop];
			end;

			local function paint(c, prop, value, original)
				-- No mapped target: leave it alone. A colour the palettes do not know keeps
				-- its Modern value, and the gate turns red for it statically (SPEC V53).
				if value == nil then return; end;

				-- No original to record: the XML never authored this property, so painting it
				-- would be a change nothing could undo. This is the guard B21 was missing.
				if original == nil then return; end;

				local h = c.handle;
				local rec = themePainted[h];

				if rec == nil then rec = {}; themePainted[h] = rec; end;
				if rec[prop] == nil then rec[prop] = original; end;

				c[prop] = value;
			end;

			-- SPEC V55 / R21 / B9: `findClass`, which the rdk generates into every .lfm, starts
			-- from `self` - it reaches the tab it runs on and nothing else, which is why the
			-- theme and colour pickers on the five base sheets only ever painted their own
			-- Credits tab. Walk up to the sheet root first, exactly like applyLanguage.
			--
			-- Nothing here writes left/top/width/height (SPEC V57): the gate keeps measuring
			-- the static XML for overlap, and no theme can invent a collision at runtime.
			-- ---- Victorian filigree (user 2026-08-24) ---------------------------------------
			--
			-- The era's section boxes get applied gold ornament: a double rule following the box's
			-- own corner, a scroll closing each corner arm, bead courses, and a fleuron at the
			-- middle of the long edges. ONE <path> per box, built from that box's width and height.
			--
			-- Vector and not PNG. The boxes run from 160x101 to 1270x574 and one piece of art
			-- stretched across that range shears its own corners; data built per box keeps every
			-- scroll the size it was drawn at and lets only the straight runs grow. It also adds
			-- nothing to `images/`, so V64 has no new art to police and V58/V60 no new path.
			--
			-- MEASURED before drawing: across the 73 boxes the free margin is 10px top and bottom
			-- and ZERO left and right - children span the full box width. So the ornament stays in
			-- the border band and the density goes where there is actually room, the corners, which
			-- centred content never reaches. Deepest point is 23px, on the diagonal.
			local ORN_BOXR  = 14;      -- the radius V68 authors on every section box
			local ORN_OUT   = 5;       -- outer rule, in from the box edge
			local ORN_IN    = 9;       -- inner rule

			-- The six frames under the XP numbers (SPEC V417, I144e) wear the palette's CORNER but
			-- never its radius as declared: 18 on a 25px-high frame would eat the frame. Named, the
			-- way sectionBox names the avatar pair, and never by fill - the frames are #0A0A0A and
			-- not the box black, which is exactly why sectionBox passes them over (SPEC V427a, V427d).
			local XP_FRAME = {
				bgTotalXP = true, bgSpentXP = true, bgCurrentXP = true,
				bgCurrentXPMain = true, bgCurrentXPNumina = true, bgCurrentXPGhoul = true,
			};
			local ORN_ARM   = 40;      -- corner reach along each edge
			local ORN_FLEUR = 150;     -- an edge run shorter than this carries no centre motif
			local ORN_KAPPA = 0.5523;  -- cubic approximation of a quarter arc

			-- ---- Dark Ages filete (user 2026-08-26, plate `Filete e Cruz`) ------------------
			--
			-- A SECOND family of box frame, chosen by the palette (SPEC I87). The plate was the
			-- floor of a series - "if this is still too much, the answer is no frame at all" - and
			-- the user picked the floor: ONE hairline at ORN_IN, unbroken the whole way round, a
			-- minute Greek cross on each run leaving a corner and a slightly larger one at the
			-- middle of the top and of the bottom. No scroll, no bead, no palmette. It is not the
			-- filigree shrunk; it is the other drawing, which is why ornPath DISPATCHES.
			--
			-- Every number here is a RELATION and the gate measures it as one (SPEC V314):
			--   * ORN_CROSS_AT lands on the STRAIGHT run. The corner arc gives out at
			--     sqrt((ORN_BOXR + ORN_IN)^2 - ORN_IN^2) = 21.17 along the edge, so 26 has 4.8px
			--     of straight in front of it; at 21 the cross sits on the CURVE and comes out
			--     crooked, with no error and nothing to grep.
			--   * ORN_CROSS2 is CAPPED at ORN_IN - THEME_STROKE. Centred on a rule 9 in, a
			--     half-arm of h reaches 9 - h outward against the box's own 3px outline (V67):
			--     6 touches it, 5 is the last one that clears. It is a ceiling, not a taste.
			--   * inward it reaches ORN_IN + ORN_CROSS2 = 14 against the 20px margin every box
			--     carries (V280a), so the frame draws OVER the content (I72c) and still crosses
			--     no child - with 6px to spare rather than on the tangent.
			--   * ORN_CROSS < ORN_CROSS2 IS the hierarchy: "one slightly larger in the middle" is
			--     what the plate says, and equal arms lose the centre with nothing to say so.
			local ORN_CROSS    = 3;     -- corner cross, half-arm
			local ORN_CROSS2   = 5;     -- centre cross, half-arm
			local ORN_CROSS_AT = 26;    -- the corner cross, along the run from the corner

			-- The room a run needs before it can hold its crosses: both corner crosses and the
			-- centre one, end to end. ONE floor for all three rather than a tighter one per motif -
			-- one fact, one owner (V49) - and what falls below it is the CROSS, never the rule,
			-- which is V279's doctrine in the letter T613 wrote it (SPEC V314e, B59).
			local ORN_CROSS_ROOM = 2 * (ORN_CROSS_AT + ORN_CROSS + ORN_CROSS2);

			-- The CHAIN's one constant (SPEC I90c). Everything else in that drawing is a relation
			-- of the box's own bevel radius and ORN_IN, so this is the only number the style owns
			-- - and it obeys the two ceilings V314 already wrote for the cross rather than
			-- inventing clearances of its own (SPEC I90f):
			--
			--   * outward the link reaches ORN_IN - ORN_LINK = 5.8 against the 3px outline the
			--     theme paints on every box (V67), so 3.2 sits under the ceiling of
			--     ORN_IN - THEME_STROKE = 6, exactly as ORN_CROSS2 does.
			--   * inward it reaches ORN_IN + ORN_LINK = 12.2 against the 20px margin every box
			--     gives its children (V280a), clear by 7.8.
			local ORN_LINK = 3.2;    -- chain link, half-width across the cut

			-- SPEC R111, I78b, V306. The bar's line work is the MARKER's own motif and not the box frame
			-- shrunk: 2*ORN_ARM is 80 against a marker 30 tall. Three motifs, one per LEVEL, because the
			-- two bars are not the same thing on screen (SPEC I78b): the top bar carries two rules with a
			-- fleuron opening the lower one, a sub-bar carries ONE short rule, and a separator carries the
			-- fleuron alone. MEASURING still decides the shape - a rule inset by d from the foot follows
			-- radius ORN_PILLR + d and meets the concave corner at sqrt((r+d)^2 - d^2), so it stops there.
			--
			-- 1px, and that is the whole reason this is a <path> and not a rectangle: applyTheme writes
			-- THEME_STROKE=3 onto every rectangle it repaints (SPEC V67), and the drawing the user chose
			-- is a hairline. The rectangle underneath is a CARRIER now - transparent both ways (SPEC I78a).
			-- ONE FAMILY PER MOTIF since the 111th round (SPEC I80c, V308). They were one heap until B69:
			-- ORN_MARK was read by the `tab` motif AND the `sub` one, and ORN_MARK_RY by `tab` AND `sep`,
			-- so the 110th round moved the top bar and took the other two levels with it - with nothing in
			-- the code able to say `only the top one`. ORN_PILLR is the declared exception and stays shared:
			-- it is not a drawing choice, it is the arc V228 authors on all 19 buttons (one fact, one owner).
			local ORN_PILLR    = 14;    -- the radius V228 authors on every tab button

			-- motif `tab`: two rules, the lower one opened for a fleuron
			local ORN_MARK     = 5;     -- the lower rule, up from the marker's foot
			local ORN_MARK2    = 9;     -- the upper rule
			local ORN_MARK_RX  = 4;     -- fleuron half-width
			local ORN_MARK_RY  = 3;     -- fleuron half-height
			local ORN_MARK_GAP = 4;     -- clear space each side of the fleuron

			-- motif `sub`: one short rule, where the user approved it in the 109th round (SPEC I80b)
			local ORN_SUB      = 0.18;  -- short: this much of the width in from each end
			local ORN_SUB_MARK = 7;     -- the rule, up from the marker's foot

			-- motif `sep`: the fleuron alone, between two sub-tab words
			local ORN_SEP_RX   = 4;     -- fleuron half-width
			local ORN_SEP_RY   = 3.5;   -- fleuron half-height

			-- ---- Dark Ages filete: the BAR (user 2026-08-26, plate `Abas em Filete e Cruz`) --
			--
			-- A SECOND family of bar drawing, chosen by the same palette key the box frame is
			-- (SPEC I88a, I87a). One hairline and the Greek cross at all three levels: the tab
			-- wears the rule with a cross at each foot, the sub-tab the rule alone, and the
			-- separator the cross alone. No second rule and no fleuron anywhere in it.
			--
			-- OWN constants, per STYLE, for the reason V308 gives per MOTIF (B69): a number that
			-- serves two drawings moves both when only one of them was asked for. Two exceptions,
			-- and they are declared rather than accidental - ORN_PILLR, the arc V228 authors on
			-- all nineteen buttons, and ORN_SUB_MARK, the sub-tab height the user approved in the
			-- 109th round, which is one fact in either style (SPEC I88e, V315b).
			--
			-- THE RELATION, and it is why these two numbers are 6 and 3 (SPEC I88d, V315c): in
			-- the default style the topmost ink IS the upper rule, at h - ORN_MARK2 = 21, and
			-- that is the state the user approved on screen in the 112th round. Here the topmost
			-- ink is the cross's UPPER ARM, so keeping the rule at 21 would put the cross at 18 -
			-- inside the label box, which closes at 20, against a measured clearance of about 2.5
			-- at 21. So ORN_FIL_MARK + ORN_FIL_CROSS = ORN_MARK2 and the highest ink stays
			-- exactly where it already was. The GATE welds that; the code does NOT derive one
			-- from the other, which would be one constant serving two styles all over again.
			local ORN_FIL_MARK  = 6;    -- motif `tab`: the one rule, up from the marker's foot
			local ORN_FIL_CROSS = 3;    -- motif `tab`: half-arm of the cross at each end
			local ORN_FIL_SEP   = 2.5;  -- motif `sep`: half-arm, smaller than the tab's by choice

			-- ---- Modern Nights corrente: the BAR (user 2026-08-27, plate `Sombra`) ---------
			--
			-- The THIRD family of bar drawing, chosen by the same palette key the box frame is
			-- (SPEC I91a). The chain era gets its own bar, and `sombra` - the name the plate
			-- carries in its footer - stays out of the code: one era, one key, two painters.
			-- It REVOKES I90h, which one round ago left this era's bar on the default drawing.
			--
			-- TWO rules at every level, and the hierarchy is INSIDE the marker (SPEC I91b, I91c):
			-- the tab's main rule runs LONGER (a fixed inset of 14 against the sub-tab's foot at
			-- 19.80) and its second rule WIDER (40% of the width against 30%). Equal on either
			-- axis and the two bars become the same drawing, which is I78b's hierarchy lost with
			-- nothing anywhere to say so, so the gate welds both (SPEC V319c).
			--
			-- OWN constants, per STYLE and per MOTIF, for the reason V308 gives (B69): a number
			-- that serves two drawings moves both when only one of them was asked for. That is
			-- why ORN_COR_MARK2 and ORN_COR_SUB2 are two constants holding the same 3 - equal
			-- value is not shared ownership, and it is the only shape in which the second rule of
			-- ONE level can be moved. ORN_SUB_MARK stays the declared exception it already is in
			-- markFilete: the sub-tab height the user approved in the 109th round is one fact.
			--
			-- THE INSET IS LITERAL, and that was measured rather than assumed (SPEC I91b): the
			-- foot of a rule 7 up from the marker's bottom is sqrt((14+7)^2 - 7^2) = 19.80, so 14
			-- puts the ends 5.8 OUTSIDE the concave arc. It is not the crossing I72a forbids -
			-- that one is ink crossing a PAINTED outline, and tabOn<X> is transparent on both
			-- sides (WoD20th.lfm:3826), so there is no silhouette on screen to cross. The 14 is
			-- ORN_PILLR's value and NOT its owner: the pill radius moving must not move a rule.
			local ORN_COR_MARK     = 7;     -- motif `tab`: the main rule, up from the marker's foot
			local ORN_COR_IN       = 14;    -- motif `tab`: fixed inset of the main rule, each end
			local ORN_COR_MARK2    = 3;     -- motif `tab`: the second rule, up from the foot
			local ORN_COR_SPAN     = 0.30;  -- motif `tab`: second rule starts this much of the width in
			local ORN_COR_SUB2     = 3;     -- motif `sub`: the second rule, up from the foot
			local ORN_COR_SUB_SPAN = 0.35;  -- motif `sub`: second rule starts this much of the width in
			local ORN_COR_DOT      = 3.2;   -- motif `sep`: radius of the filled dot
			-- Path data is parsed as SVG, where a comma separates x from y. The C library float
			-- verbs follow LC_NUMERIC, so on a pt-BR host they write a decimal COMMA and every
			-- coordinate parses as two numbers. Build the one decimal by hand instead: integer
			-- tostring carries no locale.
			local function ornNum(x)
				local n = math.floor(x * 10 + 0.5);
				local sign = "";

				if n < 0 then sign = "-"; n = -n; end;

				return sign .. tostring(math.floor(n / 10)) .. "." .. tostring(n % 10);
			end;

			-- Every motif is drawn ONCE, in canonical space - the top-left corner, u to the right
			-- and v into the box - and stamped through `t`, which mirrors and transposes it onto the
			-- other seven positions. One drawing per motif, so four corners cannot drift apart the
			-- way four hand-authored copies would.
			local function ornX(t, u, v)
				if t.swap then return t.ox + t.sx * v; end;
				return t.ox + t.sx * u;
			end;

			local function ornY(t, u, v)
				if t.swap then return t.oy + t.sy * u; end;
				return t.oy + t.sy * v;
			end;

			local function ornMove(o, t, u, v)
				o[#o + 1] = "M" .. ornNum(ornX(t, u, v)) .. "," .. ornNum(ornY(t, u, v));
			end;

			local function ornLine(o, t, u, v)
				o[#o + 1] = "L" .. ornNum(ornX(t, u, v)) .. "," .. ornNum(ornY(t, u, v));
			end;

			local function ornCurve(o, t, u1, v1, u2, v2, u, v)
				o[#o + 1] = "C" .. ornNum(ornX(t, u1, v1)) .. "," .. ornNum(ornY(t, u1, v1))
				         .. " " .. ornNum(ornX(t, u2, v2)) .. "," .. ornNum(ornY(t, u2, v2))
				         .. " " .. ornNum(ornX(t, u,  v))  .. "," .. ornNum(ornY(t, u,  v));
			end;

			-- One cubic across an arc centred on the box corner. The box corner is CONCAVE
			-- (cornerType="innerRound", V68), so insetting it by d rides a LARGER circle, radius
			-- ORN_BOXR + d - offset it the wrong way and the rule crosses the outline it decorates.
			local function ornArc(o, t, r, a1, a2)
				local k = (4 / 3) * math.tan((a2 - a1) / 4);

				ornCurve(o, t,
					r * math.cos(a1) - k * r * math.sin(a1), r * math.sin(a1) + k * r * math.cos(a1),
					r * math.cos(a2) + k * r * math.sin(a2), r * math.sin(a2) - k * r * math.cos(a2),
					r * math.cos(a2),                        r * math.sin(a2));
			end;

			-- A scroll: five quarter turns of a spiral that gives up a fifth of its radius each time.
			-- Cubic quarter-arcs rather than one SVG `A`: nothing in this repo has ever asked this
			-- renderer to parse an arc command, and a curl that failed to parse would take its whole
			-- box's ornament down with it.
			local function ornVolute(o, t, cu, cv, r, a0, turn)
				local ang, rad = a0, r;

				ornMove(o, t, cu + rad * math.cos(ang), cv + rad * math.sin(ang));

				for _ = 1, 5, 1 do
					local nAng = ang + turn * (math.pi / 2);
					local nRad = rad * 0.8;
					local k1 = ORN_KAPPA * rad * turn;
					local k2 = ORN_KAPPA * nRad * turn;

					ornCurve(o, t,
						cu + rad  * math.cos(ang)  - k1 * math.sin(ang),
						cv + rad  * math.sin(ang)  + k1 * math.cos(ang),
						cu + nRad * math.cos(nAng) + k2 * math.sin(nAng),
						cv + nRad * math.sin(nAng) - k2 * math.cos(nAng),
						cu + nRad * math.cos(nAng),
						cv + nRad * math.sin(nAng));

					ang, rad = nAng, nRad;
				end;
			end;

			-- A pointed leaf between two points: out along one bulge and back along its mirror, so
			-- the two cubics close a lens. Unfilled like everything here - the ornament is line work,
			-- and a filled leaf reads as a blot at this size.
			local function ornLeaf(o, t, u1, v1, u2, v2, b)
				local du, dv = u2 - u1, v2 - v1;
				local len = math.sqrt(du * du + dv * dv);

				if len < 0.01 then return; end;

				local nu, nv = -dv / len * b, du / len * b;

				ornMove(o, t, u1, v1);
				ornCurve(o, t, u1 + du * 0.28 + nu, v1 + dv * 0.28 + nv,
				               u1 + du * 0.72 + nu, v1 + dv * 0.72 + nv, u2, v2);
				ornCurve(o, t, u1 + du * 0.72 - nu, v1 + dv * 0.72 - nv,
				               u1 + du * 0.28 - nu, v1 + dv * 0.28 - nv, u1, v1);
			end;

			local function ornDot(o, t, cu, cv, r)
				local k = ORN_KAPPA * r;

				ornMove(o, t, cu + r, cv);
				ornCurve(o, t, cu + r, cv + k, cu + k, cv + r, cu,     cv + r);
				ornCurve(o, t, cu - k, cv + r, cu - r, cv + k, cu - r, cv);
				ornCurve(o, t, cu - r, cv - k, cu - k, cv - r, cu,     cv - r);
				ornCurve(o, t, cu + k, cv - r, cu + r, cv - k, cu + r, cv);
			end;

			-- The corner piece: both rules turning the corner along the box's own arc, a bead course
			-- on the curve, a three-leaf palmette on the diagonal, a scroll closing each arm and
			-- beads running back along it.
			local function ornCorner(o, t, a)
				local runs = { {ORN_OUT, a}, {ORN_IN, a - 7} };

				for i = 1, 2, 1 do
					local d = runs[i][1];
					local tip = runs[i][2];
					local r = ORN_BOXR + d;
					local u0 = math.sqrt(r * r - d * d);
					local a1 = math.atan(d / u0);

					ornMove(o, t, d, tip);
					ornLine(o, t, d, u0);
					ornArc(o, t, r, (math.pi / 2) - a1, a1);
					ornLine(o, t, tip, d);
				end;

				local rMid = ORN_BOXR + (ORN_OUT + ORN_IN) / 2;

				for _, ang in ipairs({ (math.pi / 4) - 0.42, math.pi / 4, (math.pi / 4) + 0.42 }) do
					ornDot(o, t, rMid * math.cos(ang), rMid * math.sin(ang), 1.4);
				end;

				local rIn = ORN_BOXR + ORN_IN;
				local q = math.pi / 4;

				ornLeaf(o, t,
					(rIn + 2.5) * math.cos(q), (rIn + 2.5) * math.sin(q),
					(rIn + 9.5) * math.cos(q), (rIn + 9.5) * math.sin(q), 3.4);

				for _, s in ipairs({ -1, 1 }) do
					ornLeaf(o, t,
						(rIn + 2.5) * math.cos(q + s * 0.34), (rIn + 2.5) * math.sin(q + s * 0.34),
						(rIn + 8.0) * math.cos(q + s * 0.62), (rIn + 8.0) * math.sin(q + s * 0.62), 2.2);
				end;

				ornVolute(o, t, ORN_IN, a - 1, 5, -math.pi / 2, 1);
				ornVolute(o, t, a - 1, ORN_IN, 5, math.pi, -1);

				local mid = (ORN_OUT + ORN_IN) / 2;
				local straight = math.sqrt((ORN_BOXR + ORN_IN) * (ORN_BOXR + ORN_IN) - ORN_IN * ORN_IN) + 2;

				for _, u in ipairs({ a - 11, a - 18, a - 25 }) do
					-- Only where the inner rule has already gone straight. Inside the arc a bead sits
					-- ON the corner instead of beside it, and a short box has no straight run left to
					-- put one on: what a short arm drops is the MOTIF, never the rule (SPEC V279).
					if u >= straight then
						ornDot(o, t, u, mid, 1.3);
						ornDot(o, t, mid, u, 1.3);
					end;
				end;
			end;

			-- One edge, corner to corner. A short run is two plain rules; a long one opens at the
			-- middle for a lozenge astride the outer rule, a leaf either side and a scroll closing
			-- each end.
			local function ornEdge(o, t, span, a)
				local i, j = ORN_OUT, ORN_IN;

				if span - 2 * a <= 0 then return; end;

				-- A run is drawn only where it has positive length. On a short edge the inner rule
				-- would otherwise be emitted BACKWARDS - end before start - which draws a line back
				-- across the corner piece (SPEC V279).
				local function rule(from, to, d)
					if to > from then ornMove(o, t, from, d); ornLine(o, t, to, d); end;
				end;

				if span - 2 * a < ORN_FLEUR then
					rule(a, span - a, i);
					rule(a + 7, span - a - 7, j);
					return;
				end;

				local c = span / 2;

				rule(a, c - 24, i);
				rule(c + 24, span - a, i);
				rule(a + 7, c - 24, j);
				rule(c + 24, span - a - 7, j);

				ornMove(o, t, c - 8, i);
				ornLine(o, t, c, i - 3.5);
				ornLine(o, t, c + 8, i);
				ornLine(o, t, c, i + 3.5);
				ornLine(o, t, c - 8, i);

				ornLeaf(o, t, c + 9, i, c + 17, i, 2.5);
				ornLeaf(o, t, c - 9, i, c - 17, i, 2.5);

				ornVolute(o, t, c + 20, i + 2.5, 3, -math.pi / 2, 1);
				ornVolute(o, t, c - 20, i + 2.5, 3, -math.pi / 2, -1);

				ornDot(o, t, c, j + 2, 1.3);
			end;

			-- The arm SHRINKS to fit; the ornament is never skipped (SPEC V279, B59). A corner
			-- overhangs its own arm by the terminal scroll - reach is a + 4 - so two of them need
			-- 2*(a+4) along the shorter side, and 5 buys that plus a pixel. ORN_ARM stays the
			-- ceiling: a big box is not drawn with a bigger corner than the one it was designed at.
			local function ornArm(w, h)
				return math.min(ORN_ARM, math.floor(math.min(w, h) / 2) - 5);
			end;

			local function ornFiligree(w, h)
				local o = {};
				local a = ornArm(w, h);

				-- The floor is the arc itself: below it there is no straight run for a rule to be a
				-- rule on. Nothing in the sheet reaches it - the shortest box is 76 and resolves to
				-- 33 - and the gate holds that gap open (SPEC V279).
				if a < 12 then return ""; end;

				ornCorner(o, {ox = 0, oy = 0, sx =  1, sy =  1}, a);
				ornCorner(o, {ox = w, oy = 0, sx = -1, sy =  1}, a);
				ornCorner(o, {ox = 0, oy = h, sx =  1, sy = -1}, a);
				ornCorner(o, {ox = w, oy = h, sx = -1, sy = -1}, a);

				ornEdge(o, {ox = 0, oy = 0, sx =  1, sy =  1}, w, a);
				ornEdge(o, {ox = 0, oy = h, sx =  1, sy = -1}, w, a);
				ornEdge(o, {ox = 0, oy = 0, sx =  1, sy =  1, swap = true}, h, a);
				ornEdge(o, {ox = w, oy = 0, sx = -1, sy =  1, swap = true}, h, a);

				return table.concat(o, " ");
			end;

			-- A Greek cross: two strokes of equal arm crossing on the rule. Drawn in canonical
			-- space like every other motif, so the same four stamps put it on all four corners
			-- without four hand-authored copies to drift apart (SPEC I87c).
			local function ornCross(o, t, u, v, r)
				ornMove(o, t, u - r, v);
				ornLine(o, t, u + r, v);
				ornMove(o, t, u, v - r);
				ornLine(o, t, u, v + r);
			end;

			-- Where the rule leaves the box's own concave arc, in from the corner along the edge.
			-- Same relation ornCorner uses, and it is the one ORN_CROSS_AT has to clear.
			local function ornFileteStraight()
				local r = ORN_BOXR + ORN_IN;

				return math.sqrt(r * r - ORN_IN * ORN_IN);
			end;

			-- One corner: the rule turning on the box's own arc, plus the minute cross on EACH of
			-- the two runs leaving it. Two per corner and not one - 26 measured on the DIAGONAL
			-- lands 18.4 deep, off the rule entirely, and the cross has to sit ON the line it marks.
			local function ornFileteCorner(o, t, uSpan, vSpan)
				local d  = ORN_IN;
				local r  = ORN_BOXR + d;
				local u0 = ornFileteStraight();
				local a1 = math.atan(d / u0);

				ornMove(o, t, d, u0);
				ornArc(o, t, r, (math.pi / 2) - a1, a1);

				-- Emitted only where the run can hold them, which is the refusal ornEdge already
				-- makes for its own motifs. What a small box drops is the CROSS, never the rule.
				if uSpan >= ORN_CROSS_ROOM then ornCross(o, t, ORN_CROSS_AT, d, ORN_CROSS); end;
				if vSpan >= ORN_CROSS_ROOM then ornCross(o, t, d, ORN_CROSS_AT, ORN_CROSS); end;
			end;

			-- One edge, arc end to arc end. `centre` is the top and the bottom only: the user chose
			-- the plate's reading, where the two SIDES stay clean (2026-08-26). ornEdge puts its
			-- centre motif on all four; this one does not, and that is a decision, not an omission.
			local function ornFileteEdge(o, t, span, centre)
				local u0 = ornFileteStraight();

				if span - 2 * u0 <= 0 then return; end;

				ornMove(o, t, u0, ORN_IN);
				ornLine(o, t, span - u0, ORN_IN);

				if centre and span >= ORN_CROSS_ROOM then
					ornCross(o, t, span / 2, ORN_IN, ORN_CROSS2);
				end;
			end;

			-- One unbroken rule: the four arcs and the four straight runs meet at the same u0, so
			-- what the eye gets is a single line all the way round. No size refusal of its own -
			-- what a small box loses is the cross (SPEC V314e); a guard that CANNOT fire is the
			-- hole B59 came through, and one that skips the rule is the hole it came through twice.
			local function ornFilete(w, h)
				local o = {};

				ornFileteCorner(o, {ox = 0, oy = 0, sx =  1, sy =  1}, w, h);
				ornFileteCorner(o, {ox = w, oy = 0, sx = -1, sy =  1}, w, h);
				ornFileteCorner(o, {ox = 0, oy = h, sx =  1, sy = -1}, w, h);
				ornFileteCorner(o, {ox = w, oy = h, sx = -1, sy = -1}, w, h);

				ornFileteEdge(o, {ox = 0, oy = 0, sx =  1, sy =  1}, w, true);
				ornFileteEdge(o, {ox = 0, oy = h, sx =  1, sy = -1}, w, true);
				ornFileteEdge(o, {ox = 0, oy = 0, sx =  1, sy =  1, swap = true}, h, false);
				ornFileteEdge(o, {ox = w, oy = 0, sx = -1, sy =  1, swap = true}, h, false);

				return table.concat(o, " ");
			end;

			-- Where the run meets the cut, in from the corner along the edge. The box corner here
			-- is a straight 45 degree BEVEL (SPEC I89a), not the concave arc ornFileteStraight
			-- measures: offsetting the cut inward by d moves it out along both edges by
			-- d * (sqrt(2) - 1), so the run stops at r + that and the cut it leaves behind is
			-- sqrt(2) * r - 2 * d * (sqrt(2) - 1) long - 18.0 for the 18/9 the sheet uses.
			--
			-- The radius is a PARAMETER and never a constant of this file: the 18 lives in the
			-- palette's boxCorner (SPEC I89a) and a second copy here is B69 all over again. It
			-- arrives from c.xradius, which is the era's radius by the time this runs only
			-- because applyTheme writes the corner BEFORE it calls ornament() in the same pass
			-- (SPEC I89c) - swap those two and this draws to the PREVIOUS era's corner, with
			-- rdk -l exiting 0 and nothing to grep (SPEC V317d).
			local function ornCorrenteRun(r)
				return r + ORN_IN * (math.sqrt(2) - 1);
			end;

			-- One corner: three links tiling the cut end to end. The run does not turn here - it
			-- STOPS, and the chain is what crosses (SPEC I90c). Endpoints come from dividing the
			-- cut in three, so the links touch without overlapping: that is the user's reading of
			-- the plate (2026-08-26), against the interlocked one where neighbours share a third.
			--
			-- ornLeaf is the link: two cubics closing a lens, bulge PERPENDICULAR to the line it
			-- spans. It is the filigree's own primitive and reusing it is the point - a second
			-- lens generator for this style is what V67 pays not to have.
			--
			-- No size refusal: the cut is sqrt(2)*r - 2*d*(sqrt(2)-1) whatever the box measures,
			-- so all twelve links are there on the 325x111 as much as on the 1410x574. A guard
			-- that cannot fire is the hole B59 came through (SPEC I90e).
			local function ornCorrenteCorner(o, t, r)
				local d  = ORN_IN;
				local u0 = ornCorrenteRun(r);
				local du = (u0 - d) / 3;

				for i = 0, 2, 1 do
					ornLeaf(o, t,
					        d + du * i,       u0 - du * i,
					        d + du * (i + 1), u0 - du * (i + 1),
					        ORN_LINK);
				end;
			end;

			-- One edge, and it is OPEN at both ends - the run is interrupted where the chain
			-- comes in, which is the whole of what the plate is called (SPEC I90c).
			local function ornCorrenteEdge(o, t, span, r)
				local u0 = ornCorrenteRun(r);

				ornMove(o, t, u0, ORN_IN);
				ornLine(o, t, span - u0, ORN_IN);
			end;

			-- Four open runs and twelve links: 16 subpaths, 24 cubics.
			local function ornCorrente(w, h, r)
				local o = {};

				ornCorrenteCorner(o, {ox = 0, oy = 0, sx =  1, sy =  1}, r);
				ornCorrenteCorner(o, {ox = w, oy = 0, sx = -1, sy =  1}, r);
				ornCorrenteCorner(o, {ox = 0, oy = h, sx =  1, sy = -1}, r);
				ornCorrenteCorner(o, {ox = w, oy = h, sx = -1, sy = -1}, r);

				ornCorrenteEdge(o, {ox = 0, oy = 0, sx =  1, sy =  1}, w, r);
				ornCorrenteEdge(o, {ox = 0, oy = h, sx =  1, sy = -1}, w, r);
				ornCorrenteEdge(o, {ox = 0, oy = 0, sx =  1, sy =  1, swap = true}, h, r);
				ornCorrenteEdge(o, {ox = w, oy = 0, sx = -1, sy =  1, swap = true}, h, r);

				return table.concat(o, " ");
			end;

			-- ONE generator, THREE families, and the PALETTE picks (SPEC I87b, I90b). An era that
			-- declares no style falls to the filigree, so nothing changes for the two that were
			-- here first. A second copy of ornament() per style is what V67 pays not to have with
			-- THEME_STROKE and V308 with the marker's three motifs; the price of getting it wrong
			-- here is 73 boxes wearing two frames at once, and no `rdk -l` that would say so.
			--
			-- `radius` is the box's own corner radius and only the chain reads it: the filigree
			-- and the filete are drawn to the CONCAVE ORN_BOXR, which V317b now solders to the
			-- radius the XML authors. The chain is drawn to the era's BEVEL, which lives in the
			-- palette, so it has to be handed in rather than looked up (SPEC V317d).
			-- ---- Classical Age corona muralis: the BOX frame (user 2026-08-27, plate `Corona Muralis`)
			--
			-- The FOURTH branch of ornPath, and the first that is not born of an era asking for a
			-- drawing - it is born of the era being EMPTY (SPEC C 122nd round, I94). Of the four
			-- palettes the Classical was the only one declaring no `ornament`, so V277 hid its path
			-- and its boxes have been bare outline for as long as the system has existed.
			--
			-- THREE things, and the order between them IS the hierarchy (SPEC I94c): one continuous
			-- RULE following the box own concave corner, the MERLON course running between
			-- ORN_MUR_OUT and ORN_MUR_OUT + ORN_MUR_RISE - the crenellation of a city wall, which
			-- is what a mural crown is - and the TOWER standing in each of the four corners, which
			-- is what a wall carries where two curtains meet (user 2026-08-28). The rule is what
			-- never drops; the merlon is what a box without room loses (SPEC V314e, V322c, B59).
			--
			-- The radius arrives as a PARAMETER, exactly as the corrente does (SPEC I96c, V324b).
			-- The V317a check derives from the CODE which branch wants a cut corner, by reading which
			-- one carries ornPath radius - so this third parameter is what makes the gate DEMAND the
			-- boxCorner the palette now declares. It INVERTS I94b/V322b, which crossed out two
			-- parameters and concluded the era could have no corner of its own.
			--
			-- OWN constants, per STYLE, for the reason V308 gives per MOTIF (B69): ORN_OUT and
			-- ORN_IN (5 and 9) belong to the filigree and the filete, and one rule serving two
			-- drawings moves both when only one of them was asked for.
			--
			-- MEASURED before drawing (SPEC I94b): deepest ink is the rule on the corner diagonal at
			-- (ORN_BOXR + ORN_MUR_IN) / sqrt(2) = 20.5, against the 23 the filigree already spends,
			-- so this is SHALLOWER than the drawing that already exists and the 20px margin of I73 is
			-- not a new question. The merlon alone reaches 11, and the tower 14 - still under the
			-- rule that frames it, so the crown's deepest ink is where it always was.
			-- Four corners and four runs, stamped from ONE drawing each, exactly as the filigree and
			-- the filete are: four hand-authored copies are four things that drift apart.
			--
			-- THE WHOLE FAMILY LIVES IN HERE - the five constants and the four helpers - and that
			-- is a CEILING and not taste (SPEC B77, V325c). This chunk holds a measured 105 locals:
			-- the rdk compiler emits its own into it and Lua's limit of 200 is reached in there.
			-- The family cost the chunk SEVEN of those and nothing outside ever read one of them,
			-- so they come in here and the chunk gets seven back. What that buys is a build that
			-- still compiles: at 106 `rdk -l` exits 1 with an EMPTY output, names no file and no
			-- line, and DELETES the .rpk (B19) - which is the whole of why this move is worth a
			-- diff on a drawing nobody asked to change.
			--
			-- Reading only its own family gets STRONGER by it, not weaker (V322d, V308): from in
			-- here ORN_OUT and ORN_IN are still reachable and still unread, and ORN_MUR_* are now
			-- unreachable to everyone else by construction rather than by convention.
			local function ornCoronaMuralis(w, h, radius)
				local ORN_MUR_OUT   = 6;    -- the merlon course, in from the box edge
				local ORN_MUR_IN    = 15;   -- the inner rule
				local ORN_MUR_STEP  = 12;   -- one merlon plus one embrasure
				local ORN_MUR_RISE  = 5;    -- how far the merlon stands above its course
				local ORN_MUR_TOWER = 8;    -- the corner tower, side

				-- Where a run at depth d leaves the corner, in from it along the edge. The era cuts its
				-- corner as innerLine - a STEP, the straight partner of innerRound the way bevel is the
				-- straight partner of round (SPEC I97a, R117) - so offsetting it inward by d moves both of
				-- its legs out by exactly d, and the foot is the plain sum. No root of a difference of
				-- squares, which is what a CONCAVE ARC costs, and no sqrt(2) factor, which is what a 45
				-- degree CUT costs: this is the simplest of the three feet the sheet now carries.
				--
				-- The radius arrives as an UPVALUE of the painter, never from ORN_BOXR: that constant is the
				-- Lua copy of the radius the XML authors and it is still 14, but the era writes its own
				-- through boxCorner, and reading the constant here would give two owners to one number and
				-- draw this frame to the corner some OTHER era has (SPEC V317d, V324c, B69).
				local function ornMuralFoot(d)
					return radius + d;
				end;

				-- One corner, and it is TWO straight runs meeting at the step own inner vertex - never an
				-- arc. The crown is still spent on the RUNS: a merlon riding the corner would have to shrink
				-- to fit it, and the whole reading of a wall is that every tooth is the same size.
				--
				-- TWO and not one is the difference between this and a 45 degree CUT, which closes the same
				-- two feet with a single diagonal. Emitting one here would leave the foot correct and the
				-- shape wrong, pass every measurement of the pair, and show up only on screen (SPEC V324d).
				--
				-- The vertex is (f, f) because the offset moves BOTH legs of the step out by d, so it is the
				-- same number the foot already is - it is not spelled a second time (SPEC V49).
				local function ornMuralCorner(o, t)
					local d = ORN_MUR_IN;
					local f = ornMuralFoot(d);

					ornMove(o, t, f, d);
					ornLine(o, t, f, f);
					ornLine(o, t, d, f);
				end;

				-- The corner TOWER (user 2026-08-28). What a mural crown carries where two curtains
				-- meet, and the answer to the note above ornMuralCorner: a merlon riding the corner
				-- would have to shrink, and every tooth of a wall is the same size. A tower is not a
				-- shrunken tooth - it stands DEEPER than the merlon course, and the course is what
				-- runs out OF it rather than past it.
				--
				-- A SQUARE on the diagonal, so one drawing serves both legs and `swap` cannot tell the
				-- two apart. Its outer faces sit on ORN_MUR_OUT itself and NOT on ornMuralFoot: the
				-- foot says where a RUN leaves the step, and the tower does not leave it - it sits in
				-- it. Reaching 14, it stays under the rule at 15 and well inside the 20 of I73.
				--
				-- FIVE runs and not four: this path vocabulary has no close verb, so the last line is
				-- the first point spelled again. It is a helper of its own for V324d's reason - the
				-- corner closes a step with exactly TWO runs, and a square emitted in there would read
				-- as the 45 degree cut that measures right and draws wrong.
				local function ornMuralTower(o, t)
					local a = ORN_MUR_OUT;
					local b = a + ORN_MUR_TOWER;

					ornMove(o, t, a, a);
					ornLine(o, t, b, a);
					ornLine(o, t, b, b);
					ornLine(o, t, a, b);
					ornLine(o, t, a, a);
				end;

				-- One edge: the rule arc end to arc end, then the crenellation on its own shallower course.
				-- The rule is emitted FIRST and unconditionally, so by the time the merlon can refuse there
				-- is already ink on this edge - a box skipped in silence between ornamented neighbours is
				-- B59 in one line, and what a small box drops is the MERLON (SPEC V322c, V314e).
				local function ornMuralEdge(o, t, span)
					local u0 = ornMuralFoot(ORN_MUR_IN);

					if span - 2 * u0 <= 0 then return; end;

					ornMove(o, t, u0, ORN_MUR_IN);
					ornLine(o, t, span - u0, ORN_MUR_IN);

					-- The course leaves the TOWER now, not the corner, so its start is the tower's far
					-- face and the foot has nothing to say here: what the step costs was already paid
					-- by the square standing in it. ORN_MUR_TOWER >= radius is what keeps this at or
					-- past the ornMuralFoot(ORN_MUR_OUT) it replaces - at 8 against 1 there is room to
					-- spare, and under it the first merlon would walk back INTO the tower.
					local m0  = ORN_MUR_OUT + ORN_MUR_TOWER;
					local run = span - 2 * m0;
					local n   = math.floor(run / ORN_MUR_STEP);

					if n < 1 then return; end;

					local half = ORN_MUR_STEP / 2;
					local lo   = ORN_MUR_OUT;
					local hi   = ORN_MUR_OUT + ORN_MUR_RISE;
					local u    = m0 + (run - n * ORN_MUR_STEP) / 2;

					ornMove(o, t, u, lo);

					for _ = 1, n, 1 do
						ornLine(o, t, u + half, lo);
						ornLine(o, t, u + half, hi);
						ornLine(o, t, u + ORN_MUR_STEP, hi);
						ornLine(o, t, u + ORN_MUR_STEP, lo);

						u = u + ORN_MUR_STEP;
					end;
				end;

				local o = {};

				ornMuralCorner(o, {ox = 0, oy = 0, sx =  1, sy =  1});
				ornMuralCorner(o, {ox = w, oy = 0, sx = -1, sy =  1});
				ornMuralCorner(o, {ox = 0, oy = h, sx =  1, sy = -1});
				ornMuralCorner(o, {ox = w, oy = h, sx = -1, sy = -1});

				ornMuralTower(o, {ox = 0, oy = 0, sx =  1, sy =  1});
				ornMuralTower(o, {ox = w, oy = 0, sx = -1, sy =  1});
				ornMuralTower(o, {ox = 0, oy = h, sx =  1, sy = -1});
				ornMuralTower(o, {ox = w, oy = h, sx = -1, sy = -1});

				ornMuralEdge(o, {ox = 0, oy = 0, sx =  1, sy =  1}, w);
				ornMuralEdge(o, {ox = 0, oy = h, sx =  1, sy = -1}, w);
				ornMuralEdge(o, {ox = 0, oy = 0, sx =  1, sy =  1, swap = true}, h);
				ornMuralEdge(o, {ox = w, oy = 0, sx = -1, sy =  1, swap = true}, h);

				return table.concat(o, " ");
			end;

			local function ornPath(w, h, style, radius)
				if style == "corrente" then return ornCorrente(w, h, radius); end;
				if style == "filete" then return ornFilete(w, h); end;
				if style == "corona-muralis" then return ornCoronaMuralis(w, h, radius); end;

				return ornFiligree(w, h);
			end;
			-- The Dark Ages BAR drawing (SPEC I88c, I88e, I88f). The same shape of answer the
			-- box frame got in the 115th round: one hairline and the Greek cross, with the level
			-- said by how much of it is spent rather than by a motif nobody has seen before.
			local function markFilete(w, h, kind)
				-- Where a rule inset by d from the marker's foot dies in the concave corner:
				-- the offset INWARD rides the BIGGER circle, so the run stops where that arc
				-- gives out. It is the same measuring the box frame does, and it is what puts
				-- the two crosses on the ENDS of the rule instead of somewhere along it.
				local function foot(d)
					return math.sqrt((ORN_PILLR + d) * (ORN_PILLR + d) - d * d);
				end;

				-- One cross, centred, as two unfilled strokes. Every decimal through ornNum: a
				-- float verb follows LC_NUMERIC and a pt-BR host writes a COMMA, which splits
				-- each coordinate into two numbers and kills the whole path, silently (I72b).
				local function greek(x, y, a)
					return "M" .. ornNum(x - a) .. "," .. ornNum(y) ..
					       " L" .. ornNum(x + a) .. "," .. ornNum(y) ..
					       " M" .. ornNum(x) .. "," .. ornNum(y - a) ..
					       " L" .. ornNum(x) .. "," .. ornNum(y + a);
				end;

				-- The separator is the cross alone, centred in its 16x30 carrier, and it is
				-- SMALLER than the tab's by choice: the level that says least says it in less
				-- (SPEC I88f, V315d).
				if kind == "sep" then
					return greek(w / 2, h / 2, ORN_FIL_SEP);
				end;

				-- A sub-tab is the rule alone, at the height the user approved in the 109th
				-- round, and it now runs foot to foot instead of a proportional inset (SPEC
				-- I88e). A FIXED inset, so the widest two gain and the narrowest loses - which
				-- is the trade the user took when the plate replaced the short rule.
				if kind == "sub" then
					local i = foot(ORN_SUB_MARK);
					local y = h - ORN_SUB_MARK;

					if w - 2 * i <= 0 then return ""; end;

					return "M" .. ornNum(i) .. "," .. ornNum(y) ..
					       " L" .. ornNum(w - i) .. "," .. ornNum(y);
				end;

				-- The top bar: one rule foot to foot, and a cross marking each foot, which is
				-- what names the model. What falls out for want of room is the CROSS, never the
				-- rule (SPEC I88h, V279), so the two refusals are ORDERED - and neither can fire
				-- today, the narrowest of the nineteen being 79 against floors of 38.2 and 44.2.
				-- A guard that CANNOT fire is B59's hole, which is why the gate measures the
				-- narrowest button against the floor read from HERE rather than against a number
				-- of its own (SPEC V315e).
				local s = foot(ORN_FIL_MARK);
				local y = h - ORN_FIL_MARK;
				local o = {};

				if w - 2 * s <= 0 then return ""; end;

				o[#o + 1] = "M" .. ornNum(s) .. "," .. ornNum(y);
				o[#o + 1] = "L" .. ornNum(w - s) .. "," .. ornNum(y);

				if w - 2 * s > 2 * ORN_FIL_CROSS then
					o[#o + 1] = greek(s, y, ORN_FIL_CROSS);
					o[#o + 1] = greek(w - s, y, ORN_FIL_CROSS);
				end;

				return table.concat(o, " ");
			end;

			-- The Modern Nights BAR drawing (SPEC I91b, I91c, I91d). Two rules at every level,
			-- and a filled dot for the separator - the one place in the whole sheet where ink is
			-- FILLED rather than stroked.
			--
			-- It returns TWO values, and the second is the whole of how the fill is decided: the
			-- DRAWING says whether its own ink is filled and markRule only applies it. The other
			-- two styles return the path alone, so nil is their answer without a line of their
			-- own, and markRule spells no style and no motif of its own (SPEC I91e, V319e).
			local function markCorrente(w, h, kind)
				-- One horizontal run. Every decimal through ornNum: a float verb follows
				-- LC_NUMERIC and a pt-BR host writes a COMMA, which splits each coordinate into
				-- two numbers and kills the whole path, silently (I72b).
				local function rule(a, b, y)
					return "M" .. ornNum(a) .. "," .. ornNum(y) ..
					       " L" .. ornNum(b) .. "," .. ornNum(y);
				end;

				-- The separator: ONE dot, FILLED, centred in the 16x30 carrier (SPEC I78d, I91d)
				-- and the size of the chain link that crosses the box's bevel - by the
				-- RESEMBLANCE the user asked for, never by shared ownership: ORN_COR_DOT belongs
				-- to the bar painter, so moving ORN_LINK moves the boxes and leaves this alone.
				--
				-- Four cubics, because ORN_KAPPA is how a circle is written here and an SVG arc
				-- verb has no precedent anywhere in this sheet. ORN_KAPPA is the third DECLARED
				-- exception to V315b and the only one that is not a drawing choice: it is the
				-- constant of the arc itself, so it carries none of B69's risk.
				if kind == "sep" then
					local x, y = w / 2, h / 2;
					local r    = ORN_COR_DOT;
					local k    = ORN_KAPPA * r;

					return "M" .. ornNum(x + r) .. "," .. ornNum(y) ..
					       " C" .. ornNum(x + r) .. "," .. ornNum(y + k) .. " " .. ornNum(x + k) .. "," .. ornNum(y + r) .. " " .. ornNum(x) .. "," .. ornNum(y + r) ..
					       " C" .. ornNum(x - k) .. "," .. ornNum(y + r) .. " " .. ornNum(x - r) .. "," .. ornNum(y + k) .. " " .. ornNum(x - r) .. "," .. ornNum(y) ..
					       " C" .. ornNum(x - r) .. "," .. ornNum(y - k) .. " " .. ornNum(x - k) .. "," .. ornNum(y - r) .. " " .. ornNum(x) .. "," .. ornNum(y - r) ..
					       " C" .. ornNum(x + k) .. "," .. ornNum(y - r) .. " " .. ornNum(x + r) .. "," .. ornNum(y - k) .. " " .. ornNum(x + r) .. "," .. ornNum(y) .. " Z", true;
				end;

				-- A sub-tab: the main rule foot to foot, at the height the user approved in the
				-- 109th round (SPEC I80b, I91c), and a second rule NARROWER than the tab's. The
				-- foot is where a rule inset by d dies in the concave corner, which is the same
				-- measuring markFilete does - and the only number this style borrows from another
				-- (ORN_PILLR is the arc V228 authors, ORN_SUB_MARK the approved height).
				if kind == "sub" then
					local i = math.sqrt((ORN_PILLR + ORN_SUB_MARK) * (ORN_PILLR + ORN_SUB_MARK) - ORN_SUB_MARK * ORN_SUB_MARK);
					local s = w * ORN_COR_SUB_SPAN;

					if w - 2 * i <= 0 then return ""; end;

					return rule(i, w - i, h - ORN_SUB_MARK) .. " " ..
					       rule(s, w - s, h - ORN_COR_SUB2);
				end;

				-- The top bar: the main rule on a FIXED inset, longer than the sub-tab's, and a
				-- second rule wider than the sub-tab's. BOTH axes, because one alone leaves the
				-- two levels telling themselves apart by a single number (SPEC I91b, V319c).
				--
				-- The refusal is the MAIN rule's alone, and there is no order of falling to write
				-- here: the second rule is PROPORTIONAL, so it cannot run out of room. It cannot
				-- fire today either - the narrowest of the nineteen is 79 against a floor of 28 -
				-- and a guard that CANNOT fire is B59's hole, which is why the gate measures that
				-- width against the floor read from HERE rather than one spelled in the check
				-- (SPEC I91g, V319d, V314e).
				local s = w * ORN_COR_SPAN;

				if w - 2 * ORN_COR_IN <= 0 then return ""; end;

				return rule(ORN_COR_IN, w - ORN_COR_IN, h - ORN_COR_MARK) .. " " ..
				       rule(s, w - s, h - ORN_COR_MARK2);
			end;

			-- Two straight runs per long edge, and NOTHING on the short ones: there is no straight
			-- run there to be a rule on. A run is emitted only where it has positive length, the
			-- same refusal ornEdge makes, so a pill narrower than its own two corners simply gets
			-- less rule rather than a line drawn backwards across itself (SPEC V279, V282).
			-- One motif per level (SPEC I78b). kind comes from markKind, which reads the NAME and the
			-- bar the control sits in - construction the gate can see from disk, not a roster of 24.
			-- The Classical Age BAR drawing (SPEC I95b, I95c, I95d). Three motifs from one
			-- building: the COLUMN carries the level that names the page, and the GUTTA - the peg
			-- that hangs under a Doric triglyph - carries the two below it. Depth is not how this
			-- one says level: WIDTH is, above, and the gutta says it by hanging under the word.
			--
			-- The style keeps its name. `ornStyle` names the ERA and not the drawing - `filete`
			-- already names two drawings that look nothing alike (ornFilete is the box's fillet,
			-- markFilete is a rule and a Greek cross) - so renaming would cost the palette,
			-- ornPath, V322a and V322b to buy nothing (SPEC I95a).
			--
			-- THE EIGHT CONSTANTS LIVE IN HERE, and that is not taste (SPEC I95b, V325c, B77).
			-- This chunk holds a MEASURED ceiling of 105 locals - the rdk compiler emits its own
			-- into the same chunk and Lua's limit of 200 is reached in there - and the file
			-- already spends 97. Eight at chunk level plus this function is 106, and what that
			-- buys is `rdk -l` exiting 1 with an EMPTY output, no file named, and the .rpk
			-- DELETED (B19). Inside the function they cost the chunk one local instead of nine,
			-- and reading only its own family gets STRONGER rather than weaker: from in here
			-- nothing outside can reach them at all, which is what V308 buys by convention.
			local function markCoronaMuralis(w, h, kind)
				local ORN_COL_W     = 12;   -- motif `tab`: capital and base reach, each side
				local ORN_COL_SHAFT = 6;    -- motif `tab`: the shaft, what survives when room runs out
				local ORN_COL_CAP   = 4;    -- motif `tab`: height of capital and of base
				local ORN_COL_PAD   = 2;    -- motif `tab`: clear space at head and foot of the 30
				local ORN_COL_ROOM  = 44;   -- motif `tab`: the width the word itself asks for
				-- How far in from the button's edge the capital STARTS (SPEC I98d). The 123rd round
				-- centred the column ON ORN_COL_W, which put the capital at 6..18 and left `Traits`
				-- and `Numina` - the two 79px buttons - with 2.0px between the column and the word.
				-- Measured with the gate's own estimator, never a new ruler (SPEC I98b, V327d).
				local ORN_COL_EDGE  = 2;    -- motif `tab`: the capital starts this far in
				local ORN_GUT_STEP  = 14;   -- motif `sub`: one gutta to the next
				local ORN_GUT_W     = 2;    -- motif `sub`: width of one gutta
				local ORN_GUT_DROP  = 5;    -- motif `sub`: how far a gutta hangs below its rule

				-- Where a rule inset by d from the marker's foot dies in the concave corner. The
				-- carrier is still the pill V228 authors, so the run stops where that arc gives
				-- out - the same measure markFilete makes, and what puts the rule's ends under
				-- the word instead of somewhere past it.
				local function foot(d)
					return math.sqrt((ORN_PILLR + d) * (ORN_PILLR + d) - d * d);
				end;

				-- One filled peg, as a closed sub-path. Every decimal through ornNum: a float verb
				-- follows LC_NUMERIC and a pt-BR host writes a COMMA, which splits each coordinate
				-- into two numbers and kills the whole path, silently (I72b).
				local function gutta(cx, y0, y1)
					local a = ORN_GUT_W / 2;

					return "M" .. ornNum(cx - a) .. "," .. ornNum(y0) ..
					       " L" .. ornNum(cx + a) .. "," .. ornNum(y0) ..
					       " L" .. ornNum(cx + a) .. "," .. ornNum(y1) ..
					       " L" .. ornNum(cx - a) .. "," .. ornNum(y1) .. " Z";
				end;

				-- The separator is ONE gutta, alone, centred in its 16x30 carrier (SPEC I95d): the
				-- level that says least says it in less, exactly as the Dark Ages cross does
				-- (I88f), and it is the sub-tab's OWN ink rather than a third vocabulary. FILLED,
				-- like the guttae it is one of - which is what the second return value says.
				if kind == "sep" then
					local cy = h / 2;

					return gutta(w / 2, cy - ORN_GUT_DROP / 2, cy + ORN_GUT_DROP / 2), true;
				end;

				-- A sub-tab is the rule at the height the user approved in the 109th round, with
				-- the guttae hanging from it at a FIXED step - a frieze's peg spacing does not
				-- stretch with the panel, so the COUNT changes and the step does not.
				--
				-- The rule is emitted FIRST and unconditionally: what falls out for want of room
				-- is the GUTTA, never the rule (SPEC I95c, V314e). A sub-tab skipped in silence
				-- between ornamented neighbours is B59 in one line.
				if kind == "sub" then
					local i = foot(ORN_SUB_MARK);
					local y = h - ORN_SUB_MARK;

					if w - 2 * i <= 0 then return ""; end;

					local o = { "M" .. ornNum(i) .. "," .. ornNum(y) ..
					            " L" .. ornNum(w - i) .. "," .. ornNum(y) };
					local n = math.floor((w - 2 * i) / ORN_GUT_STEP);

					-- Centred as a GROUP, so the pegs sit under the word instead of drifting to
					-- one end as the sub-tab gets wider.
					if n >= 1 then
						local x0 = (w - (n - 1) * ORN_GUT_STEP) / 2;

						for k = 1, n, 1 do
							o[#o + 1] = gutta(x0 + (k - 1) * ORN_GUT_STEP, y, y + ORN_GUT_DROP);
						end;
					end;

					return table.concat(o, " "), true;
				end;

				-- The top bar: a Tuscan column at each edge - capital, shaft, base - and the word
				-- read as the intercolumniation. TRACED and not filled, which is the whole of why
				-- there is no second return value here: markRule paints `color` from the drawing's
				-- own answer, and a filled column is a block of gold where a pair of columns
				-- should be (SPEC I95e, V323c).
				--
				-- What falls out for want of room is the CAPITAL and the BASE, and what survives
				-- is the SHAFT - two levels of degradation, and no marker that ever vanishes (SPEC
				-- I95c). Neither refusal can fire today: the narrowest of the eleven is 79 against
				-- floors of 68 and 56.
				local o    = {};
				local y0   = ORN_COL_PAD;
				local y1   = h - ORN_COL_PAD;
				local wide = w - 2 * ORN_COL_W >= ORN_COL_ROOM;
				local reach;

				if wide then
					reach = ORN_COL_W;
				else
					if w - 2 * ORN_COL_SHAFT < ORN_COL_ROOM then return ""; end;

					reach = ORN_COL_SHAFT;
				end;

				-- Drawn ONCE and stamped twice, mirrored by `s`: one drawing, so the two columns
				-- cannot drift apart - the same reason ornCorner is authored in canonical space.
				local function column(cx, s)
					local half = ORN_COL_SHAFT / 2;
					local a    = cx - s * reach / 2;
					local b    = cx + s * reach / 2;

					if wide then
						o[#o + 1] = "M" .. ornNum(a) .. "," .. ornNum(y0) ..
						            " L" .. ornNum(b) .. "," .. ornNum(y0) ..
						            " M" .. ornNum(a) .. "," .. ornNum(y0 + ORN_COL_CAP) ..
						            " L" .. ornNum(b) .. "," .. ornNum(y0 + ORN_COL_CAP) ..
						            " M" .. ornNum(a) .. "," .. ornNum(y1 - ORN_COL_CAP) ..
						            " L" .. ornNum(b) .. "," .. ornNum(y1 - ORN_COL_CAP) ..
						            " M" .. ornNum(a) .. "," .. ornNum(y1) ..
						            " L" .. ornNum(b) .. "," .. ornNum(y1);
					end;

					o[#o + 1] = "M" .. ornNum(cx - half) .. "," .. ornNum(y0 + ORN_COL_CAP) ..
					            " L" .. ornNum(cx - half) .. "," .. ornNum(y1 - ORN_COL_CAP) ..
					            " M" .. ornNum(cx + half) .. "," .. ornNum(y0 + ORN_COL_CAP) ..
					            " L" .. ornNum(cx + half) .. "," .. ornNum(y1 - ORN_COL_CAP);
				end;

				-- The capital starts at ORN_COL_EDGE and runs `reach` inward, so the centre it is
				-- stamped on is EDGE + reach/2 - it is no longer centred on reach itself. That is
				-- the whole of I98d: the column moves toward the rim and the word gets the middle.
				column(ORN_COL_EDGE + reach / 2, 1);
				column(w - ORN_COL_EDGE - reach / 2, -1);

				return table.concat(o, " ");
			end;

			-- The era's own BAR drawing, picked by the palette exactly as the box frame is (SPEC
			-- I88a). The style arrives as an ARGUMENT and is never read as an upvalue: this
			-- function is declared ABOVE the local that holds the era's style, so reading it
			-- from here compiles to a GETGLOBAL and finds nil, with no error and nothing to
			-- grep (SPEC V223, B51). An era declaring no style falls to the drawing below.
			local function markPath(w, h, kind, style)
				if style == "filete" then return markFilete(w, h, kind); end;
				if style == "corrente" then return markCorrente(w, h, kind); end;
				if style == "corona-muralis" then return markCoronaMuralis(w, h, kind); end;

				local o  = {};
				local cx = w / 2;
				local cy = h / 2;

				-- The separator is the fleuron alone, centred in its 16x30 carrier (SPEC I78d).
				if kind == "sep" then
					o[#o + 1] = "M" .. ornNum(cx) .. "," .. ornNum(cy - ORN_SEP_RY);
					o[#o + 1] = "L" .. ornNum(cx + ORN_SEP_RX) .. "," .. ornNum(cy);
					o[#o + 1] = "L" .. ornNum(cx) .. "," .. ornNum(cy + ORN_SEP_RY);
					o[#o + 1] = "L" .. ornNum(cx - ORN_SEP_RX) .. "," .. ornNum(cy);
					o[#o + 1] = "Z";

					return table.concat(o, " ");
				end;

				-- A sub-tab is marked by ONE short rule (SPEC I78b): the level below says less, and it says
				-- it in less width. Proportional and not a literal inset - the eight sub-tabs run from 90 to
				-- 235 wide and a fixed inset would swallow the narrow ones.
				if kind == "sub" then
					local i = w * ORN_SUB;

					if w - 2 * i <= 0 then return ""; end;

					o[#o + 1] = "M" .. ornNum(i) .. "," .. ornNum(h - ORN_SUB_MARK);
					o[#o + 1] = "L" .. ornNum(w - i) .. "," .. ornNum(h - ORN_SUB_MARK);

					return table.concat(o, " ");
				end;

				-- The top bar: two rules, the lower one opened in the middle for the fleuron.
				local s  = math.sqrt((ORN_PILLR + ORN_MARK) * (ORN_PILLR + ORN_MARK) - ORN_MARK * ORN_MARK);
				local y1 = h - ORN_MARK;
				local y2 = h - ORN_MARK2;
				local g  = ORN_MARK_RX + ORN_MARK_GAP;

				-- No room for both halves of the lower rule: the marker draws nothing rather than a fleuron
				-- sitting on a rule drawn end-before-start. The narrowest button on the top bar is 79 against
				-- a half of 39.5, so this cannot fire today - and one that CANNOT fire is B59's hole (V279).
				if cx - g <= s then return ""; end;

				o[#o + 1] = "M" .. ornNum(s) .. "," .. ornNum(y2);
				o[#o + 1] = "L" .. ornNum(w - s) .. "," .. ornNum(y2);

				o[#o + 1] = "M" .. ornNum(s) .. "," .. ornNum(y1);
				o[#o + 1] = "L" .. ornNum(cx - g) .. "," .. ornNum(y1);
				o[#o + 1] = "M" .. ornNum(cx + g) .. "," .. ornNum(y1);
				o[#o + 1] = "L" .. ornNum(w - s) .. "," .. ornNum(y1);

				-- Line work, unfilled, like everything else here.
				o[#o + 1] = "M" .. ornNum(cx) .. "," .. ornNum(y1 - ORN_MARK_RY);
				o[#o + 1] = "L" .. ornNum(cx + ORN_MARK_RX) .. "," .. ornNum(y1);
				o[#o + 1] = "L" .. ornNum(cx) .. "," .. ornNum(y1 + ORN_MARK_RY);
				o[#o + 1] = "L" .. ornNum(cx - ORN_MARK_RX) .. "," .. ornNum(y1);
				o[#o + 1] = "Z";

				return table.concat(o, " ");
			end;

			-- One <path> per box, made the first time an era asks for it and hidden - never destroyed
			-- - when a plainer era takes over. An era that declares no `ornament` gets none, exactly
			-- as a palette with no `paper` gets no backdrop (V56).
			-- The entry is {p, w, h, s, c} and not the path alone: the memo has to tell "already
			-- drawn" from "drawn at THIS size" and from "drawn in THIS drawing". The HEALTH box is
			-- the one section box that resizes at runtime - renderHealthTrack writes its height
			-- from the storyteller's track length (SPEC V49) - and a memo that only knew the first
			-- would keep serving a frame built for the old measure, with no error and no symptom
			-- to grep (SPEC I72d, V284).
			-- `c` is the BOX, and it is what lets ornRemeasure ask the size again after the reveal
			-- without a second walk of the tree: a second collect would be a second owner of "which
			-- rectangles are the boxes", which is what sectionBox exists to be the only one of
			-- (SPEC V358e, V67, I89b).
			local ornPainted = {};

			-- The era's ornament colour, kept from the last applyTheme so a refresh outside the
			-- traversal does not have to resolve the palette a second time. nil is a real value
			-- here: it is what a plainer era leaves behind, and it hides rather than repaints.
			local ornColour = nil;

			-- The era's DRAWING, kept beside its colour and for the same reason (SPEC I87e):
			-- refreshOrnament runs OUTSIDE the theme traversal and has no palette in hand. Written
			-- from the same `t` the colour is, so which frame the memo believes in and which era
			-- the boxes are wearing can never be two different answers. nil is a real value here -
			-- it is what every palette but the Dark Ages one leaves behind, and it means the
			-- filigree, not "no frame": that question is ornColour's and stays ornColour's.
			--
			-- NOT a sixth parameter on ornament(). V285c counts that signature and demands the
			-- refresh branch use the fourth and fifth by name; a style riding in beside them would
			-- be one more thing the refresh path has to be handed and could silently drop.
			local ornStyleNow = nil;

			-- WHO is a section box, decided by CONSTRUCTION rather than by a roster of names
			-- (SPEC V278): authored black fill PLUS a corner radius of its own. That is what
			-- tells the 73 from the tab-strip floor (black and SQUARE, V229), the 19 pills
			-- (DimGray, and their width is priced for the concave arc, V228), the mark boxes
			-- and the avatar frame.
			--
			-- The fill is the AUTHORED colour and not the painted one: by the time this runs
			-- the box is already mahogany (SPEC V62), so reading the current colour would
			-- match nothing.
			--
			-- Lifted out of ornament() in the 117th round so the frame and the CORNER ask the
			-- same question in ONE place (SPEC I89b, V316d). A second copy of "which
			-- rectangles are the boxes" is what V67 pays not to have, and the cost of getting
			-- it wrong here is the 19 pills beveled with a width priced for the arc.
			--
			-- Declared ABOVE both of its callers on purpose: reached from above it would
			-- compile to a GETGLOBAL and answer nil, silently (SPEC V223, B51).
			-- TWO answers and ONE question (SPEC I92b, V320c): the FIRST is who dresses in the era's
			-- SHAPE - the 73 plus BOTH of the avatar's rectangles - and the SECOND is who receives
			-- the era's DRAWING - the 73 plus the carrier alone. A second copy of which rectangles
			-- are the boxes is what V67 pays not to have, and the pair is the shape markPath already
			-- returns as d, f (SPEC V319e).
			--
			-- The avatar arm is a DECLARED ROSTER of two names and cannot be anything else (SPEC
			-- I92a, V320b): by CONSTRUCTION ornAvatar IS the nineteen tabOn<X> carriers - transparent
			-- both ways, its own radius, hitTest false - so a fill-based arm would bevel the pills,
			-- whose width is priced for the concave arc (V228, V316d). The frame wears and does not
			-- draw: under the image its path lands behind the photo, which is B75 (SPEC I92c).
			local function sectionBox(c, fill)
				local nm = c:getName();

				if nm == "ornAvatar" then return true, false; end;
				if nm == "avatarFrame" then return true, false; end;

				if normColor(fill) ~= "#FF000000" then return; end;

				local rad = c.xradius;

				if rad == nil or rad <= 0 then return; end;

				return true, true;
			end;

			-- `w` and `h` are the caller's measurement and they are OPTIONAL. A caller that has
			-- just written the size passes it; the theme traversal, which writes nothing, leaves
			-- them out and the control is read instead (SPEC I72e, V285c).
			local function ornament(c, fill, colour, w, h)
				local e = ornPainted[c.handle];

				if colour == nil then
					if e ~= nil then e.p.visible = false; end;
					return;
				end;

				if e ~= nil then
					-- The box changed size since the data was built. Rebuilding beats stretching:
					-- `mode` stays "original" because the SDK's stretch would scale the volutes and
					-- beads with the box, and the HEALTH box moves on ONE axis only - the motif
					-- would flatten by about a quarter (SPEC R113).
					--
					-- The HANDED-OVER measurement wins over the control's own. renderHealthTrack
					-- has just written the height, and `getHeight` is _gui_getHeight rather than the
					-- plain property getter `getWidth` uses (rrpgGUI.lua:95, :98) - reading it back
					-- in the same tick can answer with the size the box is LEAVING, which compares
					-- equal and skips the redraw in silence. That was B62 (SPEC V285c).
					local bw = w or c.width;
					local bh = h or c.height;

					--
					-- The THIRD thing the stamp has to carry is the DRAWING (SPEC I87d, V313c).
					-- Switching era on an open sheet moves not one pixel of any box, so the size
					-- comparison above answers "same" and the memo hands back the frame it built
					-- for the era before - the Victorian filigree, repainted silver, on all 73.
					-- rdk -l exits 0, the gate stays green and the defect exists only on screen:
					-- B58 and B62 on a third axis, where the memo knew "already drawn" and "drawn
					-- at THIS size" and had no way to know "drawn in THIS drawing".
					if bw ~= nil and bh ~= nil and (bw ~= e.w or bh ~= e.h or ornStyleNow ~= e.s) then
						local d = ornPath(bw, bh, ornStyleNow, c.xradius);

						if d ~= "" then
							e.p.data = d;
							e.w = bw;
							e.h = bh;
							e.s = ornStyleNow;
						end;
					end;
				end;

				if e == nil then
					-- Section boxes AND the avatar's carrier, and the question is still asked in ONE place:
					-- the corner of I89 reads the FIRST answer, this reads the SECOND (SPEC I92b, V316d,
					-- V320c). The frame wears the era's corner and draws NOTHING - it sits under the image,
					-- so a path hung on it would land behind the photo, which is B75 in one line.
					local _, draws = sectionBox(c, fill);

					if not draws then return; end;

					-- READ, never written - the theme still writes no geometry (V57). A box whose size
					-- the layout has not resolved gets no ornament rather than a frame drawn to a wrong
					-- measure. Size no longer decides WHETHER, only how big the corner comes out.
					local bw, bh = c.width, c.height;

					if bw == nil or bh == nil then return; end;

					-- The ONLY refusal left is a box too small for the arc to straighten out - no box
					-- in the sheet is (SPEC V279). The old guard refused anything under 2*ORN_ARM and
					-- TRUE FAITH, 1270 wide and 76 tall, fell through it silently (SPEC B59).
					local d = ornPath(bw, bh, ornStyleNow, c.xradius);

					if d == "" then return; end;

					local p = gui.newPath();

					-- Child of the LAYOUT holding the box, not of the box itself (SPEC I72c, V283a).
					-- A child draws above its parent and below the parent's LATER siblings, so a path
					-- parented to the rectangle sat under every label and field the box carries - the
					-- user reported exactly that: the boxes you type in were covering the filigree.
					-- Created here, after the authored children already exist, it is the last sibling
					-- and draws over all of them. There is no z-order API to do this instead: bringToFront,
					-- sendToBack and zorder are 0 hits across SDK3 (SPEC R112), so creation order IS the
					-- mechanism. Do NOT "re-raise" with p:setParent(p:getParent()) - setParent returns
					-- early when the parent is unchanged (rrpgObjs.lua:637), so it compiles, runs, exits
					-- 0 and does nothing (SPEC V283b).
					--
					-- Safe only because hitTest stays false below (SPEC V283c): over the fields, but it
					-- never eats a click. And it needs the 20px margin of I73 under it (SPEC V283d) -
					-- at 10px the 5/9px rules would cross the full-width children of 68 of the 73 boxes.
					p:setParent(c.parent);

					p.align = "client";
					p.mode = "original";
					p.hitTest = false;
					p.color = "#00000000";
					p.strokeSize = 1;
					p.data = d;

					e = {p = p, w = bw, h = bh, s = ornStyleNow, c = c};

					ornPainted[c.handle] = e;
				end;

				e.p.strokeColor = colour;
				e.p.visible = true;
			end;

			-- The MARKER's line work (SPEC I78, V306). Same contract as ornament(): created once, HIDDEN
			-- by nothing here and never destroyed (SPEC V277).
			--
			-- Who gets one is the NAME and the BAR, not a transparent fill plus a radius. That filter
			-- died with the box: since I77a the button is transparent and carries the same radius as the
			-- marker, so construction alone stopped telling the pair apart. 	abOn and sep are FAMILY
			-- prefixes - the same idiom note<X> and lbl<X> already use - and the parent's name picks the
			-- level between the two 	abOn motifs. One name read, not a roster of 24.
			--
			-- Parented to the CARRIER, which is what makes the open tab need no state of its own: a
			-- child of something invisible does not paint, and applyTabVisibility and renderSubTabs
			-- already write that visible. The separator's carrier never hides, so its fleuron stands.
			local markPainted = {};

			local function markKind(c, nm)
				if nm == nil then return nil; end;
				if string.sub(nm, 1, 3) == "sep" then return "sep"; end;
				if string.sub(nm, 1, 5) ~= "tabOn" then return nil; end;

				local p = c.parent;

				if p ~= nil and p.name == "tabStrip" then return "tab"; end;

				return "sub";
			end;

			local function markRule(c, nm, colour)
				local e = markPainted[c.handle];

				if colour == nil then
					if e ~= nil then e.p.visible = false; end;
					return;
				end;

				if e == nil then
					local kind = markKind(c, nm);

					if kind == nil then return; end;

					-- READ, never written - the theme still writes no geometry (SPEC V57).
					local bw, bh = c.width, c.height;

					if bw == nil or bh == nil then return; end;

					local d, f = markPath(bw, bh, kind, ornStyleNow);

					if d == "" then return; end;

					local p = gui.newPath();

					p:setParent(c);

					p.align = "client";
					p.mode = "original";
					p.hitTest = false;
					-- `color` is NOT nailed shut here any more: the fill changes with the era exactly
					-- as the drawing does, so it is written on every repaint below (SPEC I91f). Nailed
					-- here it would survive a swap on an OPEN sheet and leave the Dark Ages cross
					-- FILLED - rdk -l exit 0, gate green, and only the screen left to tell (B58, B62).
					p.strokeSize = 1;
					p.data = d;

					-- The entry is {p, k, f, s} and not the path alone. The nineteen markers never
					-- change size, so measure is not what this memo has to tell apart - the
					-- DRAWING is. `k` is the kind already resolved, so a restyle does not go
					-- back to the parent to ask which level this is (SPEC I88g, V315f).
					-- `s` stays LAST in the entry on purpose: the gate tells this CREATION stamp from
					-- the rewrite below by the brace that closes right after it (V315f), and its first
					-- draft went green with the stamp deleted for want of exactly that (B7).
					e = {p = p, k = kind, f = f, s = ornStyleNow};

					markPainted[c.handle] = e;
				elseif e.s ~= ornStyleNow then
					-- The era changed under an OPEN sheet. Without this the bar would wear the
					-- PREVIOUS era's drawing repainted in the new colour: nothing here resizes,
					-- so nothing else would ever ask for the path to be rebuilt - exit 0, gate
					-- green, and the defect only on the screen. It is the hole the box memo had
					-- to close one round earlier, on a memo that at least carried a measure
					-- (SPEC I88g, V315f, B58, B62).
					local d, f = markPath(c.width, c.height, e.k, ornStyleNow);

					-- An empty path means no room, and the answer to that is to keep what is
					-- there rather than blank the marker - and to leave `s` alone, so the next
					-- repaint tries again instead of believing the new style was drawn.
					if d ~= "" then
						e.p.data = d;
						e.f = f;
						e.s = ornStyleNow;
					end;
				end;

				-- The FILL, written on EVERY repaint and not once at creation: the dot is filled in
				-- this era and the cross is not in the next (SPEC I91f). The answer comes from the
				-- DRAWING - markRule spells no style and no motif of its own (SPEC I91e, V319e).
				if e.f then e.p.color = colour; else e.p.color = "#00000000"; end;

				e.p.strokeColor = colour;
				e.p.visible = true;
			end;

			-- SPEC I72d / V284. The HEALTH box is resized by renderHealthTrack, which lives on the
			-- root form and has no reach into this chunk's locals - so the refresh is a global, the
			-- same way renderHedgePickers and renderManualEntry already are. Declared BELOW
			-- ornament() because it calls it: the other order compiles to a GETGLOBAL and finds nil
			-- (SPEC V223, B51).
			--
			-- Takes the BOX and finds its own backdrop rather than taking the rectangle, because the
			-- caller holds the layout - `form[prefix .. "box"]` - and nothing else. The backdrop is
			-- the align="client" child, which is how V68 and V278 already tell a section box from
			-- everything else that is black.
			function refreshOrnament(box, w, h)
				if box == nil then return; end;

				local kids = box:getChildren();

				for i = 1, #kids, 1 do
					local k = kids[i];

					-- Found by MEMO, never by reading a property off the control (SPEC V285a).
					-- The first version asked `k.align == "client"`, and `align` had ZERO precedent
					-- as a runtime READ anywhere in the sheet: getClassName is proven by the theme
					-- walk that paints every round, align was proven by nothing. A finder that
					-- matches nothing returns quietly and exits 0 - that was half of B62.
					--
					-- It also sidesteps the order: getChildren walks pairs() of a hash table
					-- (rrpgObjs.lua:577), so which child comes first is undefined.
					--
					-- `fill` goes in as nil on purpose: the memo EXISTS by the test just made, so
					-- the creation branch cannot run and the authored colour is never consulted.
					-- Reading it here would be one more property off the control for nothing.
					if ornPainted[k.handle] ~= nil then
						ornament(k, nil, ornColour, w, h);
						return;
					end;
				end;
			end;

			-- SPEC V358 / B97. The load-time measure is taken with the sheet body HIDDEN: applyTheme
			-- runs from the sheetTheme dataLink and sheetReveal is the LAST line of that same
			-- handler, so every align="client" box is asked for a size the layout has not resolved
			-- yet. `getWidth` is _obj_getProp (rrpgGUI.lua:95) and answers the last value written
			-- rather than nil, so the nil guard in ornament() has nothing to catch, the memo stamps
			-- the wrong measure, and applyTheme has ONE call site - nothing ever asks again. The
			-- crown stayed drawn at its smallest in the top-left corner of all 73 for the rest of
			-- the session, exit 0 and the gate green the whole time (B97).
			--
			-- The cure is to MEASURE AGAIN, never to REFUSE. A floor would leave the box plain,
			-- which is exactly what V279 forbids, and the number would be invented here - this file
			-- cannot read the XML the boxes are authored in.
			--
			-- `w` and `h` go in as nil ON PURPOSE, and that is the INVERSE of refreshOrnament above
			-- (SPEC V285c, V358c): there the caller has just WRITTEN the height and reading it back
			-- in the same tick can answer with the size the box is LEAVING, so the measure has to
			-- arrive by argument; here nothing was written and what is missing is the align pass, so
			-- the control is the only source there is. Handing a measure in would put the stale one
			-- back through another door.
			--
			-- Nothing new is compared: the third door of ornament() already opens on `bw ~= e.w`,
			-- so a box the layout had resolved all along is left exactly as it is (SPEC V284a).
			--
			-- GLOBAL like refreshOrnament, and that is not style: B93 measured this chunk at 99 of
			-- 100 and a `local function` costs a slot exactly like a variable does (SPEC V325,
			-- V358d). Declared BELOW ornament() because it calls it - the other order compiles to a
			-- GETGLOBAL and finds nil, silently (SPEC V223, B51).
			function ornRemeasure()
				for _, e in pairs(ornPainted) do
					if e.c ~= nil then ornament(e.c, nil, ornColour); end;
				end;
			end;

			local function applyTheme(v, from)
				-- Every value the combo offers has a palette since the 16th round, so this never
				-- resolves to nil: a sheet carrying something else - "Present Day" from before
				-- the rename, or plain rubbish - lands on the default rather than on an
				-- unpainted sheet (SPEC V52).
				local t = THEMES[v] or THEMES[THEME_DEFAULT];
				local root = rootOf(from);
				local all = {root};

				-- Kept for refreshOrnament, which runs outside this traversal (SPEC I72d, V284).
				-- Written from the SAME `t` the walk below paints from, so the era the filigree
				-- believes in can never differ from the era the boxes are wearing.
				ornColour = t.ornament;
				ornStyleNow = t.ornStyle;

				-- The bar's accent, read ONCE for the whole repaint (SPEC V205). It is the stroke key the
				-- markers and every framed box already share, so the bar cannot drift from the sheet - and
				-- it exists in all FOUR palettes, which t.ornament does not (SPEC I78e, V304b).
				local accent = t.stroke[normColor("#FFFFFF")];

				collect(root, all);

				-- Once for the whole repaint, not once per picker (SPEC V205).
				local levels = discLevels();

				for i = 1, #all, 1 do
					local c = all[i];
					local cls = c:getClassName();
					local nm = c:getName();

					-- The backdrop exists only for the theme, so it is deliberately OUTSIDE the
					-- ledger: its authored state is "no image, hidden". Matched by PREFIX, like
					-- the `dyn` and `cbo` conventions above, because a control `name` is global
					-- across the sheet's .lfm files - eight tabs sharing one "themePaper" made
					-- the rdk exit 1 with no message at all and delete the .rpk (SPEC B19 / V59).
					if cls == "image" and nm ~= nil and string.sub(nm, 1, 10) == "themePaper" then
						-- An ornament that is not there degrades to no ornament, never to a
						-- half-painted sheet (SPEC V56).
						if t.paper == nil then
							if c.visible then c.visible = false; end;
						else
							c.src = t.paper;
							c.visible = true;
						end;
					elseif cls == "form" then
						paint(c, "theme", t.form, authored(c, "theme"));
					elseif THEME_SHAPES[cls] then
						local fill = authored(c, "color");
						local line = authored(c, "strokeColor");

						paint(c, "color", t.fill[normColor(fill)], fill);
						paint(c, "strokeColor", t.stroke[normColor(line)], line);
						paint(c, "strokeSize", THEME_STROKE, authored(c, "strokeSize"));

						-- The CORNER, and only on the 73 that the SAME selector the ornament uses
						-- says yes to (SPEC I89b, V316d). Without sectionBox this branch runs on
						-- EVERY rectangle the sheet has and would bevel the 19 tab pills along
						-- with the boxes - and their width is priced for the concave arc (V228).
						if sectionBox(c, fill) then
							local bc = t.boxCorner;
							local ct = authored(c, "cornerType");
							local rx = authored(c, "xradius");
							local ry = authored(c, "yradius");

							-- The palette's value OR THE AUTHORED ONE, and that `or` is the whole
							-- way back out (SPEC V316c). paint() returns early on a nil value
							-- (V61), so writing t.boxCorner alone would leave all 73 beveled
							-- FOREVER once Modern Nights had been on: rdk -l exits 0, the gate
							-- stays green and the defect exists only on screen - B58 and B62 on a
							-- fourth axis.
							--
							-- Writing the authored value back is safe HERE and is not B21: that
							-- hazard is the FONT setters, which run _gui_prepareForFontColorChange
							-- and detach the control from the form's theme BEFORE they write. A
							-- geometry setter has no such step.
							--
							-- Before ornament() below, so the box's own shape is settled before
							-- anything measures it.
							paint(c, "cornerType", (bc ~= nil and bc.type) or ct, ct);
							paint(c, "xradius", (bc ~= nil and bc.radius) or rx, rx);
							paint(c, "yradius", (bc ~= nil and bc.radius) or ry, ry);
						end;

						-- The six XP number frames, by NAME (SPEC V427, T960). The palette's cornerType and
						-- the SMALLER of its radius and the authored one: Modern Nights bevels at the frame's
						-- own 6, the Classical Age steps at its 1, and the two eras that declare no boxCorner
						-- fall back to what the XML authored, exactly as the boxes above do. The `or` on each
						-- line is the way back out (SPEC V316c, V427b, V427c).
						if XP_FRAME[nm] then
							local bc = t.boxCorner;
							local ct = authored(c, "cornerType");
							local rx = authored(c, "xradius");
							local ry = authored(c, "yradius");

							paint(c, "cornerType", (bc ~= nil and bc.type) or ct, ct);
							paint(c, "xradius", (bc ~= nil and math.min(bc.radius, rx)) or rx, rx);
							paint(c, "yradius", (bc ~= nil and math.min(bc.radius, ry)) or ry, ry);
						end;

						-- The filigree rides the same walk and the same authored value the fill does,
						-- so the frame and the box can never disagree about which era is in force.
						ornament(c, fill, t.ornament);
						markRule(c, nm, accent);
					elseif THEME_TEXTS[cls] then
						local fc = authored(c, "fontColor");

						paint(c, "fontColor", t.font[normColor(fc)], fc);

						-- Only where the control reports a real family. What the getter returns
						-- for a control that never authored a font is unknown (SPEC R27), and a
						-- font property cannot be un-written - the setter detaches it from the
						-- theme for good. If it reports nothing usable, that control keeps its
						-- font and the serif simply does not reach it.
						local ff = authored(c, "fontFamily");

						if type(ff) == "string" and ff ~= "" then
							paint(c, "fontFamily", t.fontFamily, ff);
						end;

						-- The HEIGHT of the top bar's eleven labels, and only theirs (SPEC I98a,
						-- I98b, V327c). The selector is the GRANDPARENT's name - one name read,
						-- exactly as markKind does it, and not a roster of eleven: the label sits
						-- inside btnTab<X>, which sits inside tabStrip. The eight labels of the
						-- three sub-bars are authored height="30" already, so they are outside
						-- this and stay outside it - writing 30 where 30 already is is the silent
						-- write V122 exists to refuse.
						--
						-- `t.tabLabelHeight or ah` is the WAY BACK OUT, and it is the same shape
						-- V316c cobbled for the corner: paint() returns early on a nil value
						-- (V61), so handing it the palette key alone would leave the eleven at 30
						-- for good the moment another era took over - rdk -l 0, gate green, and
						-- the defect only on screen (B58, B62).
						local lp = c.parent;

						if lp ~= nil and lp.parent ~= nil and lp.parent.name == "tabStrip" then
							local ah = authored(c, "height");

							paint(c, "height", t.tabLabelHeight or ah, ah);
						end;

						-- The era renames what a picker SHOWS, never what it stores (SPEC I22,
						-- V194). It rides the repaint's own traversal rather than a dataLink of
						-- its own, so the words and the palette can never disagree about which
						-- era is in force (SPEC T469). `v` and not currentTheme(): this walk is
						-- the one being asked for, and an unknown value renames nothing, which
						-- is the same fallback the palette takes above.
						if cls == "comboBox" then
							pickerItems(c, (sheet ~= nil and sheet.language) or "en", v, levels);
						end;
					elseif cls == "imageCheckBox" then
						paint(c, "checkedImage", t.dotOn, authored(c, "checkedImage"));
						paint(c, "uncheckedImage", t.dotOff, authored(c, "uncheckedImage"));
					elseif cls == "image" then
						-- The fixed first dot (SPEC B22 / V65). Keyed on the authored art, exactly
						-- like the colour maps: an <image> showing the base dot follows the same
						-- palette entry the ticked boxes do. The avatar authors no src at all and
						-- falls through untouched - it is the player's picture, not theme art.
						local art = normPath(authored(c, "src"));

						if art == DOT_ON then
							paint(c, "src", t.dotOn, authored(c, "src"));
						elseif art == DOT_OFF then
							paint(c, "src", t.dotOff, authored(c, "src"));
						end;
					end;
				end;
			end;
		]]>
	</script>
	<scrollBox align="client">
		<!--
			Settings tab (ex-Credits, SPEC T73/T74). The sponsor image that used to fill the
			right-hand column is gone for good, so both boxes sit at x=0.

			Options first, credits last (SPEC T141): the settings are what a player opens this
			tab to change; the credits are a signature.
		-->
		<layout left="0" top="0" width="420" height="135">
			<rectangle align="client" color="black" xradius="14" yradius="14" cornerType="innerRound"/>

			<label left="20" top="15" width="80" height="20" text="Language" horzTextAlign="center"/>
			<comboBox left="100" top="15" width="300" height="25" field="language" fontColor="white" horzTextAlign="center" items="{'English', 'Português BR'}" values="{'en', 'pt'}"/>
			<dataLink field="language" defaultValue="en">
				<event name="onChange">
					if sheet == nil then return end;
					applyLanguage(sheet.language or "en", self);
					renderAbilityLabels(sheet.language or "en", self);
				</event>
			</dataLink>

			<!-- `Game` says what the table is playing (SPEC C, T75). Nothing reads it - no
			     content is filtered by it, on purpose. Named cbo* so the language switch
			     translates the shown items while `values` stay English (SPEC V24).

			     Three games since the 32nd round, and the box is NOT editable: the value is
			     information, not a choice (SPEC V109). No empty row goes with that - a locked
			     combo carrying a default has nothing to clear - which is the same declared
			     exception to V15 that cboSheetTheme carries. A sheet holding one of the four
			     older, longer names is normalised on load (SPEC V110), because a locked box
			     showing a dead value is one nobody could fix. -->
			<label left="20" top="55" width="80" height="20" text="Game" horzTextAlign="center"/>
			<comboBox left="100" top="55" width="300" height="25" name="cboGame" field="game" enabled="false" fontColor="white" horzTextAlign="center" items="{'Vampire', 'Mortal', 'Mage'}" values="{'Vampire', 'Mortal', 'Mage'}">
			</comboBox>
			<dataLink field="game" defaultValue="Mortal"/>

			<!--
				`Theme` is the era the table plays in and, like `Game`, only stores state today
				(SPEC C, T141). The field is `sheetTheme`, NOT `theme`: sheets saved from the Mage
				plugin still carry theme="Dark", and reusing the name would put that value in a
				combo that never offered it.

				Named cbo* ON PURPOSE: only those combos get their `items` rebuilt when the
				language changes, and without the prefix a [pt] sheet would still read
				"Dark Ages" instead of "Idade das Trevas". `values` stay canonical English
				(SPEC V24). It is the
				one declared exception to V15 - a state combo with a real default has no empty
				first row to clear.

				As of the 14th round this combo PAINTS (SPEC I5/I6): the four values are the
				keys the palette table is looked up by, so changing this list means changing
				THEMES and defaultValue in the same commit (SPEC V12).

				The 16th round dropped the palette-less baseline that used to be called `Modern`
				and gave the name to the contemporary palette. Every value paints now, so there
				is no way back to the sheet as the XML authors it (SPEC C, 16th round).
			-->
			<label left="20" top="95" width="80" height="20" text="Age" horzTextAlign="center"/>
			<comboBox name="cboSheetTheme" left="100" top="95" width="300" height="25" field="sheetTheme" fontColor="white" horzTextAlign="center" items="{'Modern Nights', 'Victorian Age', 'Dark Ages', 'Classical Age'}" values="{'Modern Nights', 'Victorian Age', 'Dark Ages', 'Classical Age'}"/>
			<dataLink field="sheetTheme" defaultValue="Modern Nights">
				<event name="onChange">
					if sheet == nil then return end;
					applyTheme(sheet.sheetTheme or THEME_DEFAULT, self);
					renderAbilityLabels(sheet.language or "en", self);

					-- Last paint of the load, so the sheet is safe to show (SPEC I15, V167).
					-- It lives at the END of this handler on purpose: everything above it is
					-- what the player would otherwise have watched being applied (SPEC B40).
					sheetReveal(self);

					-- The boxes measured again, and AFTER the reveal (SPEC V358a, B97). Everything
					-- above this ran with the sheet still hidden, so an align="client" box could still
					-- answer the size it had before the layout resolved it. A call placed BEFORE
					-- sheetReveal re-reads that same unresolved size and is an expensive no-op.
					--
					-- The interval is POSITIVE because setTimeout is only asynchronous with one
					-- (SPEC R44): at 0 it runs in this very tick, ahead of the align pass, which is
					-- the defect back again.
					setTimeout(function() ornRemeasure(); end, 1);
				</event>
			</dataLink>
		</layout>

		<!--
			Credits (SPEC T532). Andre wrote this sheet; it grew out of another author's sheet
			plugin, and the one line under the developer credits them by name. The roll-call
			this box used to carry - a brand heading, three roles and two contributors - came
			out on the user's call (SPEC C, 2026-08-22). V230 holds both ends of what is left:
			the line cannot grow back into a block, and it cannot quietly disappear either.
			Neither the brand nor the author's name is spelled anywhere else in the source,
			comments included - which is why this one talks around both.

			82nd round (user 2026-08-22, items 7 and 8): the box gained the heading it never had
			and the same 14px above the first line as below the last - it was 10 and 5, and the
			lopsided pair is what showed. Both boxes on this tab went 330 -> 380 wide with it.

			T911 (user 2026-09-02 item 5): a line of thanks under the developer, and the box grew
			206 -> 242 to take it, which the owner authorised in the ask. It costs nothing to any
			neighbour - the Settings tab holds two boxes and this is the lower one (SPEC T141).

			fontSize 12 like the line under it, and that is MEASURED rather than chosen: the pt
			string is 44 characters and so is "Based on the sheet plugin by Vinny (Ambesek)",
			which has sat in this width at this size since the 82nd round. V16 asks a label to fit
			its longest translation, and the sibling is the proof it does (SPEC I136l, V16, V25).
		-->
		<layout left="0" top="140" width="420" height="232">
			<rectangle align="client" color="black" xradius="14" yradius="14" cornerType="innerRound"/>

			<label left="20" top="15" width="380" height="20" text="CREDITS" horzTextAlign="center"/>

			<label left="20" top="55" width="380" height="20" text="Developer: Andre (Styllern)" horzTextAlign="center"/>

			<label left="20" top="89" width="380" height="20" text="Special thanks to Kazuo (YonHoshiahra)" horzTextAlign="center" fontSize="12"/>

			<label left="20" top="125" width="380" height="20" text="Based on the sheet plugin by Vinny (Ambesek)" horzTextAlign="center" fontSize="12"/>

			<label left="20" top="161" width="380" height="20" text="System: Storyteller 20th Anniversary Edition" horzTextAlign="center"/>

			<label left="20" top="197" width="380" height="20" text="Version: 0.1.1" horzTextAlign="center"/>
		</layout>
	</scrollBox>
</form>
